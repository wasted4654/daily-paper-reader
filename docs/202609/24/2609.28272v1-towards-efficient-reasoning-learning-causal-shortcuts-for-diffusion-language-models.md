---
title: "Towards Efficient Reasoning: Learning Causal Shortcuts for Diffusion Language Models"
title_zh: 迈向高效推理：为扩散语言模型学习因果捷径
authors: "Dian Jin, Kairong Han, Baohong Li, Xinpeng Dong, Zijing Hu, Nuanqiao Shan, Fei Wu, Kun Kuang"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.28272v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型与自回归模型在推理方面的对比
tldr: 针对扩散语言模型（DLMs）在双向注意力机制下推理空间过大、难以聚焦关键推理步骤的问题，本文提出了因果捷径学习（CSL）框架。通过提取覆盖全序列的因果捷径并应用并行优先掩码训练，该方法有效引导模型聚焦推理路径，显著提升了推理准确率与收敛效率，在MATH-500等基准测试中表现优于现有SFT方法。
selection_source: fresh_fetch
motivation: DLMs在双向注意力机制下探索空间巨大，导致模型难以在随机掩码下聚焦于关键的推理引导标记。
method: 提出因果捷径学习（CSL）框架，通过逐步提取因果捷径并在训练中实施并行优先掩码，强化模型对正确推理路径的关注。
result: "CSL框架在多个推理基准测试中表现优异，平均准确率提升1.92%，在MATH-500数据集上提升高达4.20%。"
conclusion: 引入因果捷径能有效优化DLMs的推理过程，为提升扩散模型的逻辑推理能力提供了一种高效且通用的训练策略。
---

## 摘要
扩散语言模型（DLMs）因其强大的推理能力而备受关注。然而，在双向注意力机制下，与自回归模型（ARMs）相比，DLMs 在指数级庞大的探索空间中运行，这使得在随机掩码下聚焦于推理引导标记变得极具挑战性。我们将因果捷径定义为覆盖整个序列并为正确推理轨迹提供明确指导的标记链。我们分析了因果捷径对 DLMs 推理准确性和收敛速度的影响，发现它们极大地提高了答案的收敛效率和生成准确性。受此启发，我们为 DLMs 提出了因果捷径学习（CSL）框架。具体而言，我们引入了一种逐步标记提取程序从数据中提取因果捷径，并在训练期间对这些标记应用并行优先级掩码，从而通过因果捷径实现向正确答案的高效且准确收敛。在多个推理基准测试和两个基础模型上的广泛实验表明，CSL 始终优于现有的 SFT 变体基线，较仅使用 SFT 的模型平均提升了 1.92%，在 MATH-500 上最高提升达 4.20%。代码已在 https://github.com/ZJUDianJin/Causal-Shortcuts-Learning 公开。

## Abstract
Diffusion Language Models (DLMs) have attracted significant attention for their strong reasoning ability. However, under a bidirectional attention mechanism, DLMs operate over an exponentially large exploration space compared to autoregressive models (ARMs), making it challenging to focus on reasoning-guiding tokens under random masking. We define causal shortcuts as token chains that cover the full sequence and provide explicit guidance towards correct reasoning trajectories. We analyze the effects of causal shortcuts on the reasoning accuracy and convergence speed of DLMs, and find that they largely improve answer convergence efficiency and generation accuracy. Motivated by this, we propose a Causal Shortcut Learning (CSL) Framework for DLMs. Specifically, we introduce a step-by-step token extraction procedure to extract causal shortcuts from data, and apply parallel prioritized masking on these tokens during training to enable efficient and accurate convergence to correct answers via causal shortcuts. Extensive experiments across multiple reasoning benchmarks and two base models demonstrate that CSL consistently outperforms existing SFT-variant baselines, achieving an average improvement of $1.92\%$ over SFT-only models, and up to $4.20\%$ on MATH-500. The code is available at the \href{https://github.com/ZJUDianJin/Causal-Shortcuts-Learning}{https://github.com/ZJUDianJin/Causal-Shortcuts-Learning

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Efficient Reasoning: Learning Causal Shortcuts for Diffusion Language Models》的结构化总结：

### 1. 核心问题与研究动机
扩散语言模型（DLMs）凭借双向注意力机制展现了强大的推理潜力，但其生成过程并非像自回归模型（ARMs）那样具有严格的左向右确定性。DLMs 在随机掩码下拥有指数级庞大的探索空间，导致模型在训练时难以聚焦于关键的“推理引导标记”，往往倾向于优先学习简单、高频的标记，从而导致推理效率低下和错误累积。本文旨在通过定义并学习“因果捷径”（Causal Shortcuts）来引导模型聚焦于正确的推理轨迹。

### 2. 方法论：因果捷径学习（CSL）框架
*   **核心思想**：通过量化标记对序列的因果贡献，提取出能够覆盖全序列的“因果捷径”，并在训练中通过并行优先掩码机制强制模型关注这些关键节点。
*   **关键技术**：
    *   **条件互信息（CMI）评分**：定义 CMI 来量化标记对剩余掩码序列的解释贡献。通过“揭示-测量”过程，计算揭示某标记后剩余标记熵的减少量。
    *   **逐步提取策略**：为了避免一次性提取导致的高 CMI 标记在局部聚类，采用迭代式提取：每选出一个高 CMI 标记并将其解掩码后，重新计算剩余标记的 CMI，从而确保提取的标记链能覆盖整个推理路径。
    *   **并行优先掩码训练**：在训练目标中，对提取出的因果捷径标记应用特定的并行掩码策略，引导模型在梯度更新时优先学习这些关键路径。
    *   **效率优化**：引入滑动窗口搜索和 CMI 评分模型（通过小规模数据训练），将提取复杂度从 $O(L^2)$ 降低至 $O(L)$。

### 3. 实验设计
*   **数据集**：涵盖数学推理（GSM8K, MATH-500, SAT, Sudoku, GPQA, MMLU-STEM, ARC-C）和代码生成（HumanEval, MBPP）。
*   **基准对比**：对比了多种 SFT 变体，包括 DiBT (DiffusionBert)、MGDM、Blockwise、DSFT 和 GIFT。
*   **基础模型**：基于 LLaDA-8B-Instruct 和 LLaDA-1.5B。

### 4. 资源与算力
*   **硬件配置**：所有实验均在 4 张 NVIDIA A100 (40G) GPU 上完成。
*   **训练时长/规模**：数学任务训练 4-8 个 epoch，代码任务训练 4 个 epoch，上下文长度分别为 2048 和 1024。

### 5. 实验数量与充分性
*   **实验覆盖**：包含主实验（9 个基准测试）、消融实验（不同提取比例 K、不同提取策略）、熵动态分析（熵衰减、累积熵）以及训练稳定性分析。
*   **客观性**：实验设计了多种基线对比，并针对不同生成长度进行了评估，消融实验验证了提取策略的有效性，整体实验设计较为严谨、充分。

### 6. 主要结论与发现
*   **性能提升**：CSL 在所有基准测试中均优于现有基线，平均准确率提升 1.92%，在 MATH-500 上提升高达 4.20%。
*   **推理效率**：因果捷径能显著加速熵的衰减，使模型在更少的解码步数内收敛到正确答案。
*   **稳定性**：CSL 有效缓解了长序列推理中的错误累积问题，在长生成长度下表现出比基线更强的稳定性。

### 7. 优点
*   **因果视角创新**：从因果推断的角度定义标记重要性，比单纯基于熵或损失的重加权方法更具解释力。
*   **通用性强**：该框架独立于模型架构，且通过 CMI 评分模型解决了大规模数据预处理的计算瓶颈。
*   **可解释性**：通过熵动态分析直观展示了模型推理过程的优化，证明了“捷径”确实引导了更高效的推理路径。

### 8. 不足与局限
*   **模型规模限制**：受限于算力，实验仅在 8B 及以下规模模型上进行，未验证在超大规模模型上的扩展性。
*   **预处理依赖**：尽管引入了评分模型，但仍需额外的预处理步骤来提取因果捷径，增加了训练流水线的复杂度。
*   **潜在偏差**：方法继承了预训练模型的偏见，且在安全关键领域应用前需经过严格验证。

（完）
