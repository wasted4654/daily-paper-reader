---
title: "Zarya: A Hybrid Autoregressive--Masked Diffusion Language Model with Flexible Training and Dual-Mode Inference"
title_zh: Zarya：一种具有灵活训练与双模式推理能力的混合自回归-掩码扩散语言模型
authors: "Leonid Sinev, Ilya Koziev, Vladislav Leshchuk"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.19868v1"
tags: ["query:ardm"]
score: 10.0
evidence: 在单一架构中联合优化自回归和掩码扩散目标的混合语言模型
tldr: Zarya是一种新型混合语言模型，通过在单一架构中联合优化自回归（AR）和掩码扩散（MDM）目标，解决了AR模型生成受限与MDM模型计算开销大及生成不连贯的问题。该模型引入了变量槽位训练和课程学习策略，支持两种推理模式：高效的MDM采样和利用KV缓存复用的槽位推测解码，在保持高性能的同时实现了训练与推理的灵活解耦。
selection_source: fresh_fetch
motivation: 旨在克服自回归模型生成顺序受限与掩码扩散模型计算效率低及生成不连贯的局限性。
method: 通过联合优化AR与MDM目标，采用变量槽位训练和课程学习，并支持MDM采样与槽位推测解码两种推理模式。
result: 在0.6B至4B参数规模下，该模型在标准基准测试中表现优异，并实现了训练与推理模式的完全解耦。
conclusion: Zarya成功将自回归与扩散范式有机结合，为语言模型提供了更灵活、高效的训练与推理框架。
---

## 摘要
自回归语言模型（ARMs）受限于序列化的从左至右生成方式，而掩码扩散模型（MDMs）虽然支持并行解码，却因无法复用键值（KV）缓存而面临高计算开销，且在处理难以处理的标记组合空间依赖关系时容易产生不连贯的生成结果。我们推出了 Zarya，这是一个在单一架构内联合优化自回归（AR）目标与掩码扩散目标的混合语言模型系列。Zarya 将训练数据结构化为可变大小的槽位，并采用一种逐渐增加槽位粒度的课程学习策略，实现了从细粒度 AR 学习到粗粒度扩散学习的平滑过渡。在推理阶段，Zarya 通过统一接口提供了两种截然不同的解码范式：（i）基于首次命中去噪的 MDM 采样；（ii）槽位投机解码，通过交替进行槽间基于扩散的选择与槽内自回归填充，实现了 KV 缓存的完全复用。训练与推理机制完全解耦，使得以任何配置训练的模型均可部署在任一模式下。广泛的可配置性——包括分组噪声模式（前缀补全、填充前缀、中间填充）、有序采样调度以及噪声水平排列策略——为研究探索提供了极大的灵活性。我们公开发布了 0.6B、1.7B 和 4B 参数规模的 Zarya 模型，在标准基准测试中展现了优异性能，并为自回归与扩散范式的原则性融合提供了解决方案。

## Abstract
Autoregressive language models (ARMs) are constrained by sequential, left-to-right generation, while masked diffusion models (MDMs) enable parallel decoding but suffer from high computational overhead due to the inability to reuse Key-Value (KV) cache and from incoherent generation arising from learning dependencies over an intractable space of token combinations. We introduce Zarya, a family of hybrid language models that jointly optimizes an autoregressive (AR) objective and a masked-diffusion objective within a single architecture. Zarya structures training data into variable-size slots and employs a curriculum that gradually increases slot granularity, enabling a smooth transition from fine-grained AR learning to coarse-grained diffusion learning. At inference, Zarya provides two distinct decoding paradigms through a unified interface: (i) MDM sampling with first-hitting denoising, and (ii) slotted speculative decoding that interleaves inter-slot diffusion-based selection with intra-slot autoregressive infilling, achieving full KV cache reuse. The training and inference regimes are fully decoupled, allowing a model trained with any configuration to be deployed in either mode. Extensive configurability --- including grouped noise patterns (Prefix Completion, Fill-In-the-Prefix, Fill-In-the-Middle), ordered sampling schedules, and noise-level permutation strategies --- enables flexible research exploration. We release Zarya models publicly in sizes 0.6B, 1.7B, and 4B, demonstrating performance on standard benchmarks while offering a principled integration of autoregressive and diffusion paradigms.

---

## 论文详细总结（自动生成）

这是一份关于论文《Zarya: A Hybrid Autoregressive–Masked Diffusion Language Model with Flexible Training and Dual-Mode Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：自回归模型（ARMs）生成速度受限于序列化解码，而掩码扩散模型（MDMs）虽支持并行生成，但存在 KV 缓存无法复用导致的计算开销大、以及生成内容不连贯等问题。
*   **动机**：旨在构建一种混合架构，既能利用 AR 的序列生成优势，又能发挥 MDM 的并行解码能力，同时解决现有混合模型（如 BD3-LMs, ReFusion）在灵活性和推理效率上的不足。

### 2. 方法论
*   **核心思想**：在单一架构中联合优化 AR 和 MDM 目标，通过“槽位（Slot）”机制将序列划分为固定长度的子序列，实现结构化的并行学习。
*   **关键技术**：
    *   **槽位课程学习**：训练过程中逐渐增加槽位大小，使模型从细粒度的 AR 学习平滑过渡到粗粒度的扩散学习。
    *   **训练-推理解耦**：训练配置（如噪声模式、槽位大小）与推理模式完全解耦，模型可灵活切换至 MDM 采样或槽位投机解码模式。
    *   **双模式推理**：
        *   **MDM 采样**：利用“首次命中去噪”策略，通过重排输入确保 KV 缓存可复用。
        *   **槽位投机解码**：交替进行槽间扩散选择与槽内 AR 填充，实现高效并行。
*   **损失函数**：$L = \lambda \cdot L_{dif} + (1 - \lambda) \cdot L_{seq}$，通过加权平衡扩散重建与下一词预测。

### 3. 实验设计
*   **基准测试**：涵盖数学推理（GSM8K）、常识推理（HellaSwag）、指令遵循（IFEval）及代码生成（MBPP/MBPP+）。
*   **对比方法**：文中提及了与 Qwen3 系列的对比，并参考了 BD3-LMs、Eso-LMs 和 ReFusion 等混合架构的范式。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA A100 GPU 进行训练与评估。
*   **训练规模**：基于 Qwen3（0.6B, 1.7B, 4B）检查点，在 32M 条指令微调数据集（约 37.7B tokens）上训练 1 个 epoch。
*   **注**：文中未明确给出具体的训练时长（小时数）或 GPU 集群规模。

### 5. 实验数量与充分性
*   **实验覆盖**：提供了三个不同参数规模的模型评估，并展示了在多个主流任务上的性能指标。
*   **客观性**：实验采用了标准化的评估工具（lm-eval 0.4.12），但目前仅展示了 Zarya-0.6B 的初步结果，对于更大规模模型（1.7B/4B）的详细对比数据尚不完整，实验的全面性有待后续补充。

### 6. 主要结论与发现
*   Zarya 成功实现了 AR 与 MDM 范式的原则性融合，证明了通过槽位结构化可以有效降低学习复杂度。
*   该架构在保持高性能的同时，通过统一接口提供了极高的推理灵活性，且通过重排机制解决了扩散模型在 KV 缓存复用上的技术瓶颈。

### 7. 优点
*   **架构灵活性**：训练与推理的解耦设计极大地提升了模型的实用性。
*   **推理效率**：槽位投机解码显著提升了生成速度，同时保留了 AR 模型的连贯性。
*   **可配置性**：支持多种噪声模式（如 FIM、Prefix Completion），为研究人员提供了丰富的实验空间。

### 8. 不足与局限
*   **阈值敏感性**：槽位投机解码高度依赖于 `slot_threshold` 和 `token_threshold` 的调优，缺乏自适应机制，配置不当会导致性能下降。
*   **模式错配风险**：部分训练参数（如 `ordered_sampling`）在推理阶段无效，容易导致用户误解。
*   **评估局限**：目前仅发布了初步的 0.6B 模型评估结果，缺乏与当前最先进模型在同等算力下的深度对比。

（完）
