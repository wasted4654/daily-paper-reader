---
title: "A Ticket from Marginals to Joints: Coupled-Noise Distillation for One-Step Block Generation in Diffusion Language Models"
title_zh: 从边缘到联合的入场券：扩散语言模型中用于单步块生成的耦合噪声蒸馏
authors: Lin Yao
date: 2026-09-06
pdf: "https://arxiv.org/pdf/2609.06324v1"
tags: ["query:ardm"]
score: 9.0
evidence: 比较了自回归语言模型与扩散语言模型在块生成方面的差异
tldr: 本文针对扩散语言模型在单步生成文本块时的连贯性难题，提出了CONDOR框架。通过引入耦合噪声蒸馏技术，让学生模型学习教师模型在多步去噪过程中的联合分布，从而在单次前向传播中实现高质量、多样化的文本块生成，显著提升了单步生成的合法性与连贯性。
selection_source: fresh_fetch
motivation: 现有扩散语言模型在单步生成文本块时，噪声控制能力有限，导致输出难以保持语义连贯。
method: 提出CONDOR框架，通过噪声条件下的蒸馏技术，让学生模型学习教师模型在相同噪声场下多步去噪后的联合分布。
result: 在TinyStories数据集上的评估显示，该方法在保持生成多样性的同时，大幅提升了单步生成的文本合法性。
conclusion: CONDOR有效解决了扩散语言模型单步生成质量差的问题，为高效文本生成提供了新路径。
---

## 摘要
自回归语言模型在每次前向传递中生成一个标记；而扩散语言模型则通过多个步骤生成一个标记块。我们探讨了是否可以在单次前向传递中生成一个块。为此，我们研究了一种噪声条件掩码去噪器：将与数据无关的高斯噪声场添加到掩码嵌入中，原则上，每个采样的噪声场都会选择该块的一种联合模式。训练此类模型的既定方法是为每个样本采样多个噪声场，并通过胜者全取（winner-take-all）或重要性加权让它们竞争数据。这种方法对噪声的控制较为粗糙：在我们的实验中，噪声所携带的信息量大致随竞争噪声场数量的对数增长，且在所测试的模型规模下，单步输出往往难以保持连贯性。我们提出了 CONDOR（用于单步读取的耦合噪声蒸馏）。首先训练一个带有随机掩码位置和胜者全取机制的噪声条件教师模型；随后，学生模型提出一个单步块并保留选定的标记，同时学习教师模型在相同噪声场下通过多个步骤填充其余位置所得到的块；此外，通过在真实标签上施加无噪声掩码语言模型（masked-LM）项来锚定学生模型。在 TinyStories 上的评估结果表明，该方法在单步生成的合法性方面取得了显著提升，同时不同的噪声场在单次前向传递中仍能产生不同的块。

## Abstract
Autoregressive language models commit one token per forward pass; diffusion language models commit a block of tokens over several steps. We ask whether a block can be committed in a single forward pass. We study this with a noise-conditioned masked denoiser: a data-independent Gaussian noise field is added to the mask embeddings so that, in principle, each sampled field selects one joint mode of the block. The established way of training such a model is to sample several fields per example and let them compete for the data, by winner-take-all or importance weighting. This gives the noise only coarse control: in our experiments, the information it carries grows roughly with the logarithm of the number of competing fields, and one-step outputs remain rarely coherent across the model sizes tested. We propose CONDOR (Coupled-Noise Distillation for One-Step Readout). A noise-conditioned teacher is trained with a random number of masked positions and winner-take-all. A student proposes a one-step block, retains selected tokens, and learns from the block obtained when the teacher refills the other positions in several steps under the same noise field; a noise-free masked-LM term on the ground truth anchors the student. Human evaluation on TinyStories shows a large gain in one-step legality while different noise fields still yield different blocks, at one forward pass per block.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Ticket from Marginals to Joints: Coupled-Noise Distillation for One-Step Block Generation in Diffusion Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：自回归（AR）模型生成速度受限于序列深度；现有的扩散语言模型（dLLM）虽然能并行生成，但通常需要多个去噪步骤才能保证块内标记的连贯性。
*   **核心问题**：能否在单次前向传播（One-step）中生成一个连贯的标记块？
*   **挑战**：掩码模型在单步生成时，各位置预测相互独立（边缘分布），缺乏选择“联合模式”的机制。现有通过噪声场（Noise Field）进行条件控制的方法，因训练信号粗糙（如胜者全取 WTA），导致噪声携带的信息量极低，无法有效引导生成连贯的联合分布。

### 2. 方法论：CONDOR 框架
*   **核心思想**：通过“耦合噪声蒸馏”将教师模型在多步去噪中积累的联合分布知识，蒸馏到学生模型的单步读取中。
*   **关键技术**：
    *   **噪声条件化**：在掩码嵌入中加入高斯噪声场 $\epsilon$ 作为“入场券”，使生成过程成为 $(c, \epsilon)$ 的确定性函数。
    *   **两阶段训练**：
        *   **阶段 1（教师训练）**：使用随机掩码比例和 WTA 目标训练噪声条件掩码语言模型，使其具备噪声敏感性。
        *   **阶段 2（蒸馏）**：学生模型学习教师模型在相同噪声场下多步填充后的结果。采用“保持-填充”（Keep-and-fill）策略，即保留学生模型高置信度的标记，由教师模型填充其余部分。
    *   **无噪声锚定**：引入一个 $\epsilon=0$ 的无噪声分支，利用真实数据对学生模型进行监督，防止模型忽略噪声场。

### 3. 实验设计
*   **数据集**：TinyStoriesV2-GPT4（约 272 万个故事），采用 8+8（8 个前缀标记 + 8 个生成标记）等任务配置。
*   **基准（Benchmark）**：对比了多种噪声条件下的单步生成基线（Plain, IWAE, WTA 及其变体）。
*   **评估指标**：合法性（Legality，人工评估）、合法唯一性（Legal-unique）、互信息（MI，衡量噪声对输出的控制力）、重复率（Repetition）等。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号（如 A100/H100），但提到所有实验均为单 GPU 运行。
*   **训练时长**：教师模型训练约 1 天；单次蒸馏实验（50k 步）耗时数小时；k=64 的消融实验耗时约 9 小时至 2 天不等。

### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的消融实验，包括不同 $k$ 值（竞争噪声场数量）、不同 keep ratio（保留比例）、不同教师更新策略（冻结 vs. EMA）以及不同标签构建方式。
*   **客观性**：使用了人工评估与自动代理（GPT-2-large NLL 代理）相结合的方式，且在人工评估的 8 个模型上，代理指标与人工指标具有极高的相关性（Spearman $\rho = 0.95$），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能提升**：CONDOR 将单步生成的合法性从基线的 8.6% 提升至 64.8% - 73.4%。
*   **噪声控制**：证明了通过蒸馏，噪声场确实能有效携带联合模式信息（MI 从 2.6 提升至 10 左右），且不同的噪声场能产生不同的连贯块。
*   **必要组件**：共享噪声场、教师模型的多步填充能力、以及无噪声锚定项是实现高质量单步生成的关键。

### 7. 优点
*   **创新性**：巧妙地将多步去噪的连贯性知识蒸馏到单步模型中，解决了非自回归生成中的模式坍缩问题。
*   **可解释性**：通过互信息（MI）和 per-position 分析，深入剖析了为何传统 WTA 方法在单步生成中失效。
*   **实用性**：在保持单次前向传播的高效性前提下，显著提升了生成质量。

### 8. 不足与局限
*   **规模限制**：实验仅在 TinyStories 小规模数据集和 17.6M 参数模型上进行，在大规模语言模型（LLM）上的扩展性尚未验证。
*   **多样性折中**：虽然提升了合法性，但与真实数据分布相比，生成的多样性（Gold hit）有所下降。
*   **EMA 稳定性**：使用 EMA 更新教师模型时，若参数设置不当（如 $\beta$ 过小），容易导致模型坍缩到重复的词汇，对超参数较为敏感。

（完）
