---
title: "From Truncation to Commitment: Persistent Context in Uniform Discrete Diffusion"
title_zh: 从截断到承诺：均匀离散扩散中的持久上下文
authors: Satoshi Hayakawa
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01043v1"
tags: ["query:ardm"]
score: 8.0
evidence: 具有持久上下文的离散扩散模型序列生成
tldr: 本研究针对离散扩散模型中所有位置在采样过程中始终可修改的特性，提出了“承诺揭示采样”（CRS）。CRS 是一种无需训练的采样器，它将选定的高置信度标记作为持久上下文锁定，并输入到后续预测中。实验表明，相比传统的 top-p 截断，CRS 能有效降低生成困惑度，并在生成质量与多样性之间取得更好的平衡。
selection_source: fresh_fetch
motivation: 传统的离散扩散模型在采样时缺乏持久性约束，导致即使是高置信度的选择也可能在后续步骤中被反复修改，影响生成的一致性。
method: 提出了一种名为承诺揭示采样（CRS）的免训练方法，通过将每步选定的 argmax 标记固定并作为后续步骤的输入上下文来增强预测稳定性。
result: 在不同采样步数下，CRS 相比 top-p 截断基准实现了更低的生成困惑度，并提供了更优的困惑度与熵之间的权衡。
conclusion: 研究证明了持久上下文与候选集限制是控制扩散模型生成质量的两个独立维度，为优化离散扩散采样提供了新视角。
---

## 摘要
均匀状态离散扩散模型并行更新所有 token，同时保持每个位置均可修改。即使常用的 top-$p$ 规则在某个位置仅保留一个候选词，该选择也仅影响当前的逆向步骤，并可在下一个采样步骤中被修改。我们探讨了当选定的假设转而成为后续预测的持久上下文时会发生什么变化。为此，我们提出了承诺揭示采样（CRS），这是一种无需训练的采样器，它存储选定的 argmax token 并将其插入到后续的模型输入中。我们的分析为延迟选择以及保持选定 token 可见提供了理论依据。在精确的前向过程中，选择干净 token 的贝叶斯误差不会随着噪声的减少而增加；而在一个简单的潜模式模型中，保持选定 token 可见有助于后续的并行预测在相同的序列级选择上达成一致。实验上，在 Duo-distilled 上进行的配对实验将这种持久效应与单步 top-$p$ 限制和标量温度缩放区分开来。在相同的最终确定规则下，不带 top-$p$ 截断的 CRS 在 8 到 64 次函数评估（NFE）的预算范围内，比固定的 $p=0.95$ 和 $p=0.9$ 基准线达到了更低的生成困惑度（GenPPL）。在 64 NFE 时，在匹配的一元语法熵下进行的比较也显示 CRS 具有更低的 GenPPL，从而实现了更有利的 GenPPL-熵权衡。Base Duo 在描述性比较中显示出相同的趋势，而其他多样性和延续性指标对这些操作点的排序可能有所不同。这些结果表明，支持集限制和持久上下文是该权衡中不同的控制因素。

## Abstract
Uniform-state discrete diffusion models update all tokens in parallel while keeping every position revisable. Even when the commonly used top-$p$ rule leaves only one candidate at a position, that choice affects only the current reverse step and can be revised at the next sampling step. We ask what changes when selected hypotheses instead become persistent context for later predictions. We therefore propose committed reveal sampling (CRS), a training-free sampler that stores selected argmax tokens and inserts them into subsequent model inputs. Our analysis gives a rationale for selecting later and for keeping selected tokens visible. Under the exact forward process, the Bayes error of selecting a clean token cannot increase as noise decreases, while in a simple latent-mode model, keeping the selected token visible helps later parallel predictions agree on the same sequence-level choice. Empirically, paired experiments on Duo-distilled then separate this persistent effect from single-step top-$p$ restriction and scalar temperature scaling. Under the same finalization rule, CRS without top-$p$ truncation reaches lower generative perplexity (GenPPL) than fixed $p=0.95$ and $p=0.9$ baselines across budgets of 8--64 function evaluations (NFE). At 64 NFE, the comparison at matched unigram entropy also gives lower GenPPL for CRS, yielding a more favorable GenPPL--entropy tradeoff. Base Duo shows the same direction in a descriptive comparison, while other diversity and continuation metrics can rank these operating points differently. These results identify support restriction and persistent context as distinct controls of that tradeoff.

---

## 论文详细总结（自动生成）

这篇论文探讨了均匀离散扩散模型（Uniform Discrete Diffusion）中的采样策略，提出了“承诺揭示采样”（CRS）方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在均匀离散扩散模型（如 Duo）中，所有 token 在每一步采样中都是可修改的（revisable）。虽然常用的 top-$p$ 采样能限制单步预测的候选集，但这种限制是暂时的，选定的 token 不会被显式保留到下一步。
*   **核心问题**：当模型在某些位置已经产生高置信度预测时，如果将这些预测作为“持久上下文”（Persistent Context）固定下来并反馈给后续步骤，是否能改善生成质量？
*   **整体含义**：论文旨在区分“支持集限制”（Support Restriction，如 top-$p$）与“持久上下文”对生成质量的影响，并证明后者能更好地协调并行预测，减少序列层面的不一致性。

### 2. 方法论
*   **核心思想**：提出**承诺揭示采样（CRS, Committed Reveal Sampling）**。这是一种无需训练（training-free）的采样算法，它在采样过程中识别置信度高的位置，将其预测结果锁定（Commit），并作为后续步骤的输入。
*   **关键技术细节**：
    *   **预热阶段（Warmup）**：初始阶段使用标准的均匀采样，不进行锁定。理论证明噪声较低时选择干净 token 的误差更小，因此预热能提高锁定 token 的准确性。
    *   **置信度赤字预算（Confidence-deficit Budget）**：引入总预算 $D_{max}$，每步选择预测分布最集中的位置进行锁定，直到耗尽预算。
    *   **空间排除规则（Spatial Exclusion）**：在同一采样步内，避免锁定相邻的 token，以确保选取的上下文在空间上是分散的。
    *   **输入覆盖（Input Overwrite）**：将锁定的 token 插入到模型的输入中，而未锁定的位置继续根据扩散逻辑进行更新。
*   **理论支撑**：通过潜模式模型（Latent-mode model）证明，可见的持久上下文能降低并行预测之间的 KL 散度，使各位置的预测更容易在同一个序列模式上达成一致。

### 3. 实验设计
*   **数据集/场景**：主要在文本生成任务上进行，使用了 OpenWebText 语料库的子集进行前缀续写测试。
*   **模型基准（Benchmark）**：使用了公开的 **Duo-distilled** 和 **Base Duo** 检查点。
*   **对比方法**：
    *   **原生采样（Native Sampling）**：使用不同的固定 top-$p$ 阈值（如 $p=0.95, 0.9$）。
    *   **消融实验**：包括改变预热时长、限制上下文可见时长（Horizon）、以及仅在最后一步锁定（Final-only）等变体。
*   **评估指标**：生成困惑度（GenPPL，使用 GPT-2-Large 评估）、一元语法熵（Unigram Entropy）、重复率（Rep-4）、多样性（Dist-4）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **备注**：由于 CRS 是一种免训练的采样算法，其核心开销在于推理时的函数评估（NFE）。实验涵盖了 8 到 64 NFE 的范围。作者提到使用了 OpenAI 的 ChatGPT 研究员计划获取 API 支持，但这主要用于辅助研究而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同 NFE（8, 16, 32, 64）下的性能曲线对比。
    *   在 64 NFE 下进行了详尽的 GenPPL 与熵的权衡分析（Frontier sweep）。
    *   包含了针对预热比例、持久化时长、标量温度缩放的对比实验。
    *   使用了 4 个随机种子分片（shards），每组实验包含 256 到 5120 个生成样本。
*   **充分性评价**：实验设计非常严谨且客观。通过配对实验（Paired experiments）排除了标量温度和位置选择本身的干扰，深入探讨了“token 身份”作为上下文的实际作用。

### 6. 主要结论与发现
*   **性能提升**：在相同的最终确定规则下，CRS 在 8-64 NFE 范围内均优于 top-$p$ 基准线，实现了更低的生成困惑度。
*   **权衡优化**：在匹配一元语法熵的情况下，CRS 相比 top-$p$ 提供了更优的 GenPPL-熵权衡。
*   **机制验证**：实验证实，持久上下文的作用不能简单地通过调整标量温度来模拟，选定的具体 token 确实引导了后续的预测方向。
*   **预热必要性**：过早锁定 token 会因为噪声过大导致错误累积，适当的预热对维持生成质量至关重要。

### 7. 优点
*   **即插即用**：无需对现有模型进行重新训练或微调，直接作用于采样阶段。
*   **理论与实践结合**：不仅有显著的实验效果，还提供了坚实的数学证明（贝叶斯误差分析和协调理论）。
*   **深入的消融分析**：通过精细的控制变量实验，清晰地界定了“支持集截断”与“持久上下文”是两个独立的控制维度。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 Duo 系列模型上，尚未在更大规模（如 LLaDA）或其他类型的离散扩散架构上广泛验证。
*   **多样性代价**：虽然 GenPPL 降低了，但在某些配置下，CRS 可能会导致重复率（Rep-4）上升或 4-gram 多样性（Dist-4）略微下降。
*   **超参数依赖**：CRS 引入了预算 $D_{max}$、预热比例和空间半径等超参数，虽然论文给出了推荐值，但在不同任务下可能需要额外调优。

（完）
