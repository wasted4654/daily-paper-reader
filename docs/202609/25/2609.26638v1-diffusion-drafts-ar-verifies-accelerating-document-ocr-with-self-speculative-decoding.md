---
title: "Diffusion Drafts, AR Verifies: Accelerating Document OCR with Self-Speculative Decoding"
title_zh: 扩散草稿，自回归验证：利用自推测解码加速文档 OCR
authors: "Dohyun Kim, Sungjun Han, Hyungguk Kim, Yusik Kim, Jamin Shin, Paul Hongsuck Seo, Hongjoon Ahn"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.26638v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于并行草拟和因果验证的参数共享自回归块扩散模型
tldr: 针对自回归OCR模型推理速度慢的问题，本文提出了GravityOCR模型。该模型通过参数共享的AR-块扩散架构，实现了并行草稿生成与因果自回归验证。通过在验证阶段利用GRPO优化，模型在保持高精度的同时，显著提升了文档OCR的推理速度，在OmniDocBench v1.6上实现了3.94倍的区域解码加速。
selection_source: fresh_fetch
motivation: 自回归OCR模型逐词生成导致推理延迟高，难以满足高效文档处理需求。
method: 提出GravityOCR，通过参数共享的扩散模型进行并行草稿生成，并利用自回归路径进行验证与GRPO强化学习优化。
result: 在OmniDocBench v1.6上，该模型实现了3.94倍的区域解码加速和1.32倍的端到端页面处理加速，同时保持了极高的OCR准确率。
conclusion: GravityOCR通过自推测解码机制有效平衡了OCR任务的生成速度与准确性，为高效文档理解提供了新范式。
---

## 摘要
自回归 OCR 视觉语言模型能够准确地将文档图像转换为文本和结构化标记，但每个输出标记都需要一个顺序解码步骤，从而限制了推理速度。与开放式文本生成不同，OCR 输出强烈依赖于输入图像，这使得基于扩散的并行生成具有广阔前景。然而，当在一个扩散步骤中预测多个标记时，每个标记都是在其他标记已知之前被预测出来的，直接提交这些标记可能会引入错误。因此，我们引入了 GravityOCR，这是一种参数共享的 AR-块扩散模型，联合训练用于并行草稿生成和因果 AR 验证。在提交前验证草稿，使模型无需单独的草稿网络即可每轮提交多个输出标记。因果 AR 路径还支持使用序列级和结构级 OCR 奖励进行 GRPO（组相对策略优化），从而在更新共享草稿器参数的同时，避免了扩散轨迹似然估计。在 OmniDocBench v1.6 上，AR 路径 GRPO 将总分从 94.92 提高到 95.16，且未降低扩散草稿的效率，同时最终模型仍保持接近原始 GLM-OCR 95.48 的得分。在 SGLang 服务部署中，GravityOCR 平均每次前向传递提交 9.7 个输出标记，在区域裁剪上实现了 3.94 倍的仅解码加速，在整页处理上实现了 1.32 倍于 AR 解码的端到端加速。

## Abstract
Autoregressive OCR vision-language models accurately convert document images into text and structured markup, but require one sequential decoding step per output token, limiting inference speed. Unlike open-ended text generation, OCR outputs are strongly grounded in the input image, making diffusion-based parallel generation promising. However, when several tokens are predicted in one diffusion step, each is predicted before the others are known. Committing them directly can therefore introduce errors. We therefore introduce GravityOCR, a parameter-shared AR-block-diffusion model jointly trained for parallel drafting and causal AR verification. Verifying drafts before commitment lets the model commit multiple output tokens per round without a separate drafting network. The causal AR path also enables GRPO with sequence- and structure-level OCR rewards, avoiding diffusion-trajectory likelihood estimation while updating the shared drafter parameters. On OmniDocBench v1.6, AR-path GRPO improves the Overall score from 94.92 to 95.16 without reducing diffusion drafting efficiency, while the final model remains close to the original GLM-OCR score of 95.48. In an SGLang serving deployment, GravityOCR commits an average of 9.7 output tokens per forward pass and achieves a $3.94\times$ decode-only speedup on region crops and a $1.32\times$ end-to-end page-processing speedup over AR decoding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diffusion Drafts, AR Verifies: Accelerating Document OCR with Self-Speculative Decoding》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：自回归（AR）OCR 视觉语言模型在文档理解任务中表现优异，但其“逐个标记生成”的特性导致推理延迟高、吞吐量低，难以满足大规模文档处理需求。
*   **核心问题**：虽然基于扩散模型的并行生成可以加速推理，但直接提交并行预测的标记容易因缺乏上下文约束而产生错误（如遗漏、重复或结构不一致）。
*   **动机**：如何在利用并行生成加速的同时，通过有效的验证机制确保 OCR 输出的准确性，并避免引入额外的复杂草稿网络。

### 2. 方法论：GravityOCR
*   **核心思想**：构建一个参数共享的 AR-块扩散模型，将“并行草稿生成”与“因果 AR 验证”结合。
*   **关键技术**：
    *   **联合训练**：模型在单次前向传递中同时处理一个“干净流”（用于 AR 验证）和两个“互补掩码流”（用于扩散草稿生成），共享视觉编码器、解码器和 LM 头。
    *   **自推测解码**：扩散路径并行生成标记块，AR 路径作为验证器，仅提交与 AR 预测一致的最长前缀，从而保证输出与标准 AR 解码完全等价。
    *   **GRPO 优化**：利用因果 AR 路径的精确似然，通过组相对策略优化（GRPO）引入序列级和结构级 OCR 奖励（如 TEDS、编辑距离等），在不增加推理开销的前提下提升模型性能。

### 3. 实验设计
*   **数据集**：涵盖了 DocGenome、PubTables-1M、FinTabNet 等 12.3M 个区域级样本，并使用 OmniDocBench v1.6 作为主要评估基准。
*   **对比方法**：与 GLM-OCR（基准）、MinerU2.5/Pro、PaddleOCR-VL-1.5、HunyuanOCR-1.5 等主流 OCR 系统进行了对比。
*   **评估指标**：OmniDocBench Overall 分数、TEDS（表格）、CDM（公式）、吞吐量（tok/s）及页面处理速度（pages/s）。

### 4. 资源与算力
*   **硬件配置**：使用 16 张 H100 GPU（2 个节点）进行训练。
*   **训练规模**：总计训练 40,000 步，处理约 26B 个前向标记。
*   **推理环境**：主要在 SGLang 框架下进行部署和性能测试。

### 5. 实验数量与充分性
*   **实验覆盖**：包含主实验（性能与速度对比）、消融实验（AR 损失的影响、掩码策略对比）、不同输出类型（文本、表格、公式）的性能分析、以及不同批处理大小（Batch Size）下的吞吐量扩展测试。
*   **客观性**：实验设计严谨，通过对比直接块扩散与自推测解码，明确了验证机制对准确性的贡献；且在相同硬件和推理栈下进行了公平的基准测试。

### 6. 主要结论与发现
*   **性能平衡**：GravityOCR 在 OmniDocBench 上达到 95.16 分，几乎完全保留了原模型（95.48 分）的准确性。
*   **显著加速**：在 SGLang 部署下，实现了 3.94 倍的区域解码加速和 1.32 倍的端到端页面处理加速。
*   **结构化优势**：表格等具有强语法约束的输出类型，其草稿接受率更高，获得的加速效果比纯文本更显著。

### 7. 优点
*   **参数高效**：无需额外的草稿网络或辅助预测头，通过参数共享实现双模态功能。
*   **准确性保证**：通过因果 AR 验证，确保了并行生成不会牺牲输出质量，解决了传统并行解码的“幻觉”或一致性问题。
*   **训练范式创新**：利用 AR 路径进行 GRPO 优化，巧妙避开了扩散模型轨迹似然估计的难题。

### 8. 不足与局限
*   **语言偏差**：训练语料库以英语为主，在多语言环境下的泛化能力可能受限。
*   **依赖基础模型**：该方法高度依赖于预训练的 AR 模型（如 GLM-OCR），若基础模型本身存在缺陷，该框架难以完全弥补。
*   **高并发场景收益递减**：随着 Batch Size 的增大，GPU 利用率提升，自推测解码带来的相对速度优势会逐渐缩小。

（完）
