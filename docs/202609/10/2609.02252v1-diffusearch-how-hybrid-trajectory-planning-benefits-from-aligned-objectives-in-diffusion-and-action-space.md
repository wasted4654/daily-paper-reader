---
title: "DiffuSearch: How Hybrid Trajectory Planning Benefits from Aligned Objectives in Diffusion and Action Space"
title_zh: DiffuSearch：混合轨迹规划如何从扩散空间与动作空间的对齐目标中获益
authors: "Steffen Hagedorn, Aron Distelzweig, Alexandru P. Condurache"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02252v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于自动驾驶联合轨迹预测的引导扩散模型
tldr: DiffuSearch 是一种用于自动驾驶的混合轨迹规划框架，旨在解决传统模块化架构中目标不一致的问题。该方法通过在扩散模型生成阶段和蒙特卡洛树搜索（MCTS）优化阶段共享统一的驾驶目标（如避障、舒适度等），实现了生成与精炼的协同。实验表明，该方法在 nuPlan 和 interPlan 基准测试中显著降低了碰撞率并提升了复杂场景下的驾驶表现。
selection_source: fresh_fetch
motivation: 现有自动驾驶规划架构中各模块目标不统一，导致初始轨迹与精炼轨迹之间存在不一致性。
method: 提出一种两阶段混合规划器，利用扩散模型进行场景一致的轨迹生成，并结合 MCTS 在离散动作空间进行基于相同目标的显式精炼。
result: 在 nuPlan 和 interPlan 闭环基准测试中表现优异，显著减少了碰撞并提升了驾驶舒适度。
conclusion: 通过在扩散引导和显式搜索之间共享统一的驾驶目标，能有效提升复杂交互场景下的轨迹规划性能。
---

## 摘要
在自动驾驶的轨迹规划中，混合规划架构通常由多个各具目标的独立模块组成。这种缺乏统一原则的设计可能导致初始轨迹与优化轨迹之间的不一致，从而产生次优行为。为此，我们提出了 DiffuSearch，这是一种新型混合规划器，在生成和优化阶段采用了统一的目标集合。我们的模型鼓励所有组件遵循相同的共享驾驶目标：避障、符合可行驶区域要求、舒适性以及行进效率。DiffuSearch 采用两阶段架构。首先，引导式扩散模型利用我们的驾驶目标作为可微引导函数，隐式地引导去噪过程，从而生成场景一致的联合轨迹预测。其次，在离散化动作空间中进行的蒙特卡洛树搜索（MCTS）利用相同的驾驶目标作为奖励函数，对该提案进行显式的局部优化。这种协同设计结合了扩散模型在寻找场景一致性解方面的优势，以及 MCTS 在可解释性和约束感知优化方面的能力。在 nuPlan 和 interPlan 反应式闭环基准测试上的实验表明，DiffuSearch 实现了强大且往往达到最先进水平的性能，显著减少了碰撞并提高了舒适度，特别是在复杂的交互场景中。我们的消融研究表明，MCTS 优化是性能提升的主要机制，而在隐式引导和显式搜索之间共享目标则提供了进一步的一致性改进。

## Abstract
In trajectory planning for autonomous driving, hybrid planning architectures are often realized as a collection of disparate modules, each with its own objectives. This lack of a unifying principle can lead to inconsistencies between the initial and refined trajectory, resulting in suboptimal behavior. We address this by introducing DiffuSearch, a novel hybrid planner that uses a unified set of objectives across generation and refinement. Our model encourages all components to follow the same shared driving goals: collision avoidance, drivable area compliance, comfort, and progress. DiffuSearch employs a two-stage architecture. First, a guided diffusion model generates a scene-consistent, joint trajectory prediction, using our driving objectives as differentiable guidance functions to implicitly steer the denoising process. Second, a Monte Carlo Tree Search (MCTS) in a discretized action space performs an explicit, local refinement of this proposal, leveraging the same driving objectives as its reward function. This synergistic design leverages the diffusion model's strength in finding scene-consistent solutions combined with the explainable, constraint-aware refinement of MCTS. Experiments on nuPlan and interPlan reactive closed-loop benchmarks demonstrate that DiffuSearch achieves strong and often state-of-the-art performance, substantially reducing collisions and improving comfort, particularly in complex, interactive scenarios. Our ablation studies indicate that MCTS refinement is the main mechanism behind the gains, while sharing objectives between implicit guidance and explicit search provides further consistent improvements.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《DiffuSearch: How Hybrid Trajectory Planning Benefits from Aligned Objectives in Diffusion and Action Space》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：自动驾驶中的混合轨迹规划通常由多个独立模块组成（如生成器与精炼器），各模块往往拥有不同的优化目标。
*   **问题**：这种缺乏统一原则的架构会导致“目标不一致”问题，即精炼阶段可能会彻底改变初始规划的意图，导致最终轨迹在安全性或舒适性上表现次优。
*   **目标**：通过在生成（扩散模型）和精炼（MCTS）阶段引入统一的驾驶目标（避障、可行驶区域、舒适度、行进效率），实现轨迹规划的连续性与一致性。

### 2. 方法论：DiffuSearch
*   **核心思想**：将“隐式引导”与“显式搜索”结合，并使用同一套目标函数进行约束。
*   **阶段一：引导式扩散模型**：利用 Transformer 架构生成场景一致的联合轨迹。通过分类器自由引导（Classifier-Free Guidance），将驾驶目标函数作为可微梯度项，隐式地引导去噪过程，使初始提案天然具备安全性倾向。
*   **阶段二：动作空间 MCTS 精炼**：以扩散模型生成的轨迹作为强先验，在离散动作空间（加速度与转向角）进行蒙特卡洛树搜索。
*   **关键机制**：MCTS 的奖励函数与扩散模型的引导函数完全一致，确保了精炼过程是在优化相同的驾驶原则，从而实现对初始提案的局部“修复”而非“重写”。

### 3. 实验设计
*   **数据集与基准**：主要使用 **nuPlan**（约 1300 小时真实驾驶数据）和 **interPlan**（车道变换基准）。
*   **评估指标**：闭环反应式（R-score）和 SMART 反应式（SR-score），涵盖碰撞率、TTC（碰撞时间）、可行驶区域合规性、舒适度及行进效率。
*   **对比方法**：与 PDM-Hybrid、GameFormer、DTPP、PLUTO 以及最强的基线 DiffusionPlanner 进行了对比。

### 4. 资源与算力
*   **说明**：论文中未明确列出具体的 GPU 型号、数量或训练时长等硬件资源消耗细节。仅在图 4 中提到运行时间是在 Python 实现下测量的，未进行特定的运行时优化。

### 5. 实验数量与充分性
*   **实验覆盖**：包含主实验（nuPlan/interPlan）、消融实验（引导与 MCTS 的贡献分析）、一致性分析（初始与精炼轨迹的偏差）以及运行效率分析。
*   **客观性**：实验设计较为严谨，通过对比“仅引导”、“仅 MCTS”和“两者结合”的组合，清晰地展示了各组件的贡献。在 Test14-hard 等高难度场景下的测试增强了结论的说服力。

### 6. 主要结论与发现
*   **MCTS 是核心驱动力**：实验表明 MCTS 精炼是性能提升的主要来源。
*   **目标对齐的协同效应**：单独使用扩散引导有时反而会降低性能（可能导致分布外偏移），但将其与 MCTS 结合后，性能显著提升，证明了统一目标能有效缓解扩散模型的局部不一致性。
*   **局部修复优于重写**：DiffuSearch 的精炼过程保持了与初始提案的高度一致性，证明了其作为“局部修复机制”的有效性。

### 7. 优点
*   **架构协同**：巧妙地将生成式模型的全局一致性与搜索算法的局部约束能力结合。
*   **可解释性**：MCTS 在动作空间的显式搜索提供了比纯神经网络更强的可解释性和安全性保障。
*   **性能卓越**：在复杂交互场景（如 Test14-hard）中表现出显著优势，有效降低了碰撞率。

### 8. 不足与局限
*   **计算开销**：MCTS 的引入增加了推理时的计算负担，虽然通过扩散模型提供了强先验以限制搜索空间，但在实时性要求极高的场景下仍需权衡。
*   **模型依赖**：该方法高度依赖于扩散模型生成的初始提案质量，若初始提案偏差过大，MCTS 的局部搜索可能无法找到最优解。
*   **缺乏多模态推理**：目前主要针对单轨迹精炼，尚未显式处理复杂路口下的多模态战略决策（作者已将其列为未来工作）。

（完）
