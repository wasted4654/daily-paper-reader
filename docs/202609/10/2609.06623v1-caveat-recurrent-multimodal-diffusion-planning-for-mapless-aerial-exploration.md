---
title: "CAVEAT: Recurrent Multimodal Diffusion Planning for Mapless Aerial Exploration"
title_zh: CAVEAT：用于无图空中探索的循环多模态扩散规划
authors: "Steven Visch, Nicolò Botteghi, Antonio Franchi, Barbara Bazzana"
date: 2026-09-06
pdf: "https://arxiv.org/pdf/2609.06623v1"
tags: ["query:diffpl"]
score: 8.0
evidence: 用于航点序列生成和机器人路径规划的扩散策略
tldr: CAVEAT 是一种用于无人机无图探索的循环多模态扩散规划模型。它通过融合 LiDAR、视觉和位姿特征更新循环内部状态，无需维护全局地图即可生成探索航点。该方法利用扩散策略模仿 FUELv2 专家轨迹，结合滚动推理和局部符号距离场实现高效避障，在模拟和真实室内环境中验证了其探索能力。
selection_source: fresh_fetch
motivation: 旨在解决无人机在无全局地图依赖的情况下，如何仅凭多模态观测和循环内部状态实现自主探索的问题。
method: 提出一种基于扩散策略的规划框架，通过融合多模态传感器数据更新循环状态，并结合滚动推理与局部符号距离场进行路径规划。
result: 在模拟环境和 Flyability Elios 3 真实无人机实验中，CAVEAT 成功实现了对未知室内环境的探索及目标导向的视觉伺服。
conclusion: 证明了无需持久化全局地图，仅依靠循环内部状态和扩散策略即可实现有效的无人机自主探索。
---

## 摘要
能否在部署策略时不维护持久的全局地图，仅通过多模态机载观测和固定维度的循环内部状态来生成探索性无人机航点序列？我们通过 CAVEAT 研究了这一问题。CAVEAT 是一种扩散策略，它以从融合的激光雷达、视觉和位姿特征中更新的循环内部状态为条件，并基于由基于地图的 FUELv2 专家生成的轨迹进行训练。滚动推理部分地预热了连续预测，而临时的局部符号距离场则提供了启发式障碍物引导。仿真结果评估了两种推理机制，并将 CAVEAT 与其演示生成专家进行了比较。在 Flyability Elios 3 上进行的概念验证实验展示了对先前未见过的室内环境的部分探索，以及使用单独训练的策略进行的视觉伺服目标导向任务。

## Abstract
Can exploratory UAV waypoint sequences be generated from multimodal onboard observations and a fixed-dimensional recurrent internal state without maintaining a persistent global map in the deployed policy? We investigate this question through CAVEAT, a diffusion policy conditioned on a recurrent internal state updated from fused LiDAR, visual, and pose features and trained from trajectories generated by the map-based FUELv2 expert. Rolling inference partially warm-starts consecutive predictions, while a temporary local signed distance field provides heuristic obstacle guidance. Simulation results evaluate both inference mechanisms and compare CAVEAT with its demonstration-generating expert. Proof-of-concept experiments on a Flyability Elios 3 demonstrate partial exploration of a previously unseen indoor environment and target-directed visual servoing using a separately trained policy.

---

## 论文详细总结（自动生成）

这是一份关于论文《CAVEAT: Recurrent Multimodal Diffusion Planning for Mapless Aerial Exploration》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的无人机自主探索通常依赖于显式的全局地图（如点云、八叉树或图结构），这在计算资源受限或环境复杂时会带来沉重的存储与维护负担。
*   **核心问题**：能否在不维护持久化全局地图的前提下，仅依靠机载多模态观测（LiDAR、视觉、位姿）和紧凑的循环内部状态，通过学习到的策略生成有效的探索航点序列？

### 2. 方法论
*   **核心思想**：采用条件扩散策略（Diffusion Policy），将探索任务建模为从观测历史到航点序列的生成过程。
*   **关键技术细节**：
    *   **多模态编码**：分别使用 3D 编码器（LiDAR）、2D 编码器（视觉）和 MLP（位姿）提取特征，并通过 MLP 融合。
    *   **循环内部状态**：利用 GRU（门控循环单元）压缩观测历史，形成固定维度的内部状态 $h_t$，作为扩散模型的条件。
    *   **扩散规划**：基于 1D U-Net 架构，通过 FiLM（特征线性调制）将 $h_t$ 注入扩散过程，生成 4 维航点序列（位置+偏航角）。
    *   **推理优化**：
        *   **滚动推理（Rolling Inference）**：利用上一时刻的预测结果作为当前时刻的初始值，增强轨迹的连续性。
        *   **局部 SDF 引导**：利用当前 LiDAR 扫描构建临时局部符号距离场（SDF），对靠近障碍物的航点进行梯度修正，提供启发式避障。

### 3. 实验设计
*   **数据集/场景**：使用 Gazebo 模拟器，包含 4 种环境家族（从简单连接房间到复杂拓扑结构）。
*   **Benchmark**：对比对象为 FUELv2（一种成熟的、基于地图的专家探索框架）。
*   **评估指标**：以“覆盖率（Occupancy Coverage）”随“无人机行驶距离”的变化作为核心指标。

### 4. 资源与算力
*   **硬件**：训练使用 GPU（未明确型号，但提到 PyTorch 2.4.0 + CUDA 12.4）；推理实验使用 NVIDIA Quadro T1000 Max-Q（4GB VRAM）。
*   **训练时长**：训练 50 个 Epoch，包含 2,674 条轨迹和约 20 万个时间步。

### 5. 实验数量与充分性
*   **实验覆盖**：包含滚动推理消融实验、SDF 阈值敏感性分析、与专家方法（FUELv2）的对比，以及在真实环境（Flyability Elios 3）中的部署实验。
*   **客观性**：实验设计较为客观，通过对比不同推理机制和参数设置，验证了方法的有效性。但由于评估基于随机初始化，且未与除 FUELv2 之外的其他学习型方法对比，其在学术基准上的广泛比较略显不足。

### 6. 主要结论与发现
*   **可行性**：证明了循环内部状态足以捕捉探索任务所需的历史信息，无需全局地图即可实现自主探索。
*   **性能表现**：滚动推理显著提升了探索效率；局部 SDF 引导在狭窄空间内对避障至关重要。
*   **对比结果**：CAVEAT 在最终覆盖率上可媲美专家系统，但在探索效率（单位距离覆盖率）上仍略逊于依赖全局地图的 FUELv2。

### 7. 优点
*   **轻量化**：摆脱了对全局地图的依赖，降低了机载计算和存储压力。
*   **灵活性**：扩散策略能够处理多模态输入，且通过更换训练数据，同一架构可扩展至视觉伺服等其他任务。
*   **工程落地**：结合了滚动推理和局部 SDF，有效解决了纯学习方法在避障和轨迹平滑度上的痛点。

### 8. 不足与局限
*   **泛化能力**：在复杂约束环境（如狭窄通道）下的探索效率仍有待提升。
*   **安全性**：局部 SDF 引导仅为启发式，无法提供严格的碰撞保证。
*   **任务解耦**：目前的探索和视觉伺服任务需要分别训练不同的策略，尚未实现统一的端到端范式。
*   **部署限制**：真实环境实验仅为概念验证，未进行大规模、长时程的鲁棒性测试。

（完）
