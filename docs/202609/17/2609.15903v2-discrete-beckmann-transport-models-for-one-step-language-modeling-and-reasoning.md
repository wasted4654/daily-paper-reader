---
title: Discrete Beckmann Transport Models for One-Step Language Modeling and Reasoning
title_zh: 用于单步语言建模与推理的离散贝克曼传输模型
authors: "Sophia Tang, Shiyi Wang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15903v2"
tags: ["query:ardm"]
score: 8.0
evidence: 离散扩散模型作为自回归语言模型的替代方案
tldr: 本文提出离散贝克曼传输模型（DBTM），旨在解决离散扩散模型在语言建模中采样步数多、依赖教师模型蒸馏的问题。该方法利用时间无关流的固定点特性，通过最小化守恒方程残差直接从数据学习，实现了单步或少步的高质量文本生成，在推理任务中表现优于现有离散扩散和连续流基线。
selection_source: fresh_fetch
motivation: 旨在克服离散扩散模型采样步数多及依赖昂贵的教师模型蒸馏的局限性。
method: 构建基于时间无关流的传输映射，利用固定点特性通过最小化守恒方程残差直接从数据训练。
result: 在语言建模和推理任务中，DBTM实现了更少采样步数下的高质量生成，性能优于现有基线。
conclusion: DBTM为离散生成模型提供了一种无需时间条件和教师蒸馏的高效单步推理新范式。
---

## 摘要
离散扩散与流模型是自回归语言模型的一种极具前景的替代方案，但将多步采样压缩为更少步骤通常需要蒸馏预训练的教师模型。这限制了学生模型的质量上限，且需要昂贵的两阶段训练流程。我们引入了离散贝克曼传输模型（DBTM），该模型基于一种与时间无关的流，其自治传输映射可证明能在单步内将环境空间中的任意点映射至单纯形顶点的固定点。我们证明了这一不动点特性由一个守恒方程表征，其残差可直接从数据中最小化，从而无需教师流和时间条件。在此架构下，部分训练的映射对应于在有限时间截断的流，因此生成过程简化为迭代映射直至达到不动点。我们进一步将该映射扩展为部分上下文插值器，其中额外的函数评估充当细化步骤而非 ODE 积分步骤。在语言建模与推理任务中，DBTM 实现了优于离散扩散和连续流基准的单步及少步生成，提升了质量与准确性。

## Abstract
Discrete diffusion and flow models are a promising alternative to autoregressive language models, but compressing many-step sampling into fewer steps typically requires distilling a pretrained teacher model. This caps the student at the teacher's quality and requires a costly two-stage training pipeline. We introduce Discrete Beckmann Transport Models (DBTM), built on a time-independent flow whose autonomous transport map provably carries any point in the ambient space to a fixed point on the vertices of the simplex in a single step. We show that this fixed-point property is characterized by a conservation equation whose residual can be minimized directly from data, removing the requirement for a teacher flow and time conditioning. Under this construction, a partially trained map corresponds to the flow truncated at finite time, so generation reduces to iterating one map until it reaches a fixed point. We further extend the map to a partial-context interpolant where additional function evaluations act as refinement steps rather than ODE integration steps. On language modeling and reasoning tasks, DBTM enables one- and few-step generation that improves quality and accuracy over discrete diffusion and continuous flow baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Discrete Beckmann Transport Models for One-Step Language Modeling and Reasoning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：离散扩散模型（Discrete Diffusion）和流模型（Flow Models）是自回归语言模型的有力竞争者，但它们通常面临采样步数多、推理速度慢的问题。
*   **核心痛点**：现有的少步采样方法（如蒸馏）通常依赖预训练的教师模型，这不仅限制了学生模型的性能上限，还引入了昂贵的两阶段训练流程。
*   **研究目标**：提出一种无需教师蒸馏、无需时间条件（Time-conditioning）的离散生成模型，实现高效的单步或少步推理。

### 2. 方法论：离散贝克曼传输模型 (DBTM)
*   **核心思想**：利用自治流（Autonomous Flow）的特性，将生成过程建模为从环境空间到单纯形（Simplex）顶点的不动点映射。
*   **关键技术**：
    *   **守恒方程**：通过 $b(x) \cdot \nabla T(x) = 0$ 定义传输映射 $T(x)$，该方程的残差可直接从数据中最小化，无需蒸馏。
    *   **自治流**：与时间相关的流不同，自治流的速度场与时间无关，轨迹更直，且在单纯形顶点处具有稳定的吸引子。
    *   **部分上下文插值器**：在推理时，通过多次函数评估（NFE）进行“重采样-细化”循环，而非传统的 ODE 积分。
    *   **训练策略**：引入“重采样循环”（Refinement-in-Loop, RIL）目标，使模型在训练时即学习推理阶段的上下文提交模式，减少训练与推理的分布偏差。

### 3. 实验设计
*   **数据集/场景**：
    *   **无条件语言建模**：LM1B 和 OpenWebText (OWT)。
    *   **推理任务**：Sudoku（数独）和 TinyGSM（数学推理）。
*   **对比基线**：包括自回归模型（AR）、离散扩散模型（MDLM, Duo）、连续流模型（CANDI, FLM）以及最新的少步流映射模型（FMLM+）。
*   **评价指标**：生成困惑度（Gen-PPL）、样本熵（Entropy）、推理准确率（Accuracy）。

### 4. 资源与算力
*   **硬件**：文中提到在 A100 GPU 上进行实验，并使用了自定义的 Triton 融合内核来优化 JVP 计算。
*   **训练规模**：LM1B 训练步数为 200K 步，OWT 为 100K 步。
*   **说明**：文中未详细列出具体的 GPU 集群规模（如节点数），但强调了其训练效率高于需要 1M 步教师训练的基线方法。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了从基础语言建模到复杂逻辑推理的多个维度，并进行了详细的消融实验（如锚点时间 $t_{anchor}$、阈值 $\kappa$、NFE 预算等）。
*   **客观性**：通过 Pareto 前沿曲线对比了不同方法在困惑度和熵之间的权衡，实验设计较为严谨，且对比了多种架构（DiT 与线性注意力）。

### 6. 主要结论与发现
*   **性能优势**：DBTM 在少步生成（1-4 NFE）下，困惑度显著优于现有的蒸馏离散扩散模型。
*   **推理能力**：在 Sudoku 和 GSM8K 任务中，DBTM 仅需极少的 NFE 即可达到甚至超越许多自回归或多步扩散模型的准确率。
*   **自修正特性**：通过 refinement-in-loop 训练，模型能够有效利用上下文信息进行自我纠错，在低 NFE 预算下表现出极强的鲁棒性。

### 7. 优点
*   **无需蒸馏**：直接从数据学习，消除了对教师模型的依赖，简化了训练流程。
*   **推理灵活**：支持自适应停止规则，推理步数可根据样本质量动态调整。
*   **轨迹更优**：自治流的轨迹比时间相关流更直，减少了采样过程中的累积误差。

### 8. 不足与局限
*   **推理上限**：在 GSM8K 等复杂推理任务上，尽管 DBTM 效率极高，但仍略逊于长序列自回归模型，表明其在处理极长逻辑链时仍有提升空间。
*   **训练复杂性**：虽然省去了蒸馏，但引入了 refinement-in-loop 和复杂的损失函数组合（如 semigroup loss, anchor loss），对超参数调节较为敏感。
*   **应用限制**：目前主要针对离散序列生成，对于连续性极强的长文本生成，其长程依赖建模能力仍需进一步验证。

（完）
