---
title: "CPR: Combining global composing, local performing and full-sequence refining in piano rendering with continuous autoregressive modelling"
title_zh: CPR：基于连续自回归建模的钢琴渲染中全局作曲、局部演奏与全序列精修的结合
authors: "Chong Jing, Junan Zhang, Zhizheng Wu"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18216v1"
tags: ["query:ardm"]
score: 8.0
evidence: 连续自回归建模与流匹配
tldr: 该论文提出了CPR框架，旨在解决钢琴MIDI到音频渲染中离散编码器信息丢失与扩散模型计算成本高的问题。通过结合连续自回归建模、局部流匹配和全序列细化，CPR实现了高效且高质量的钢琴渲染。引入的BREPA和MT-RoPE机制显著增强了音乐语义结构与跨模态对齐能力，在保持音色还原度的同时，有效提升了生成效率与音频质量。
selection_source: fresh_fetch
motivation: 现有方法在离散编码带来的信息丢失与扩散模型的高计算成本之间难以平衡，且难以同时兼顾语义结构与声学细节。
method: 提出CPR框架，通过自回归预测连续隐状态（Composer）、局部流匹配生成声学潜变量（Performer）及波形上采样（Refiner）实现分层渲染。
result: CPR框架成功绕过了量化瓶颈，在降低计算成本的同时，实现了对目标音符的精准渲染与参考录音音色的高质量复刻。
conclusion: 该研究证明了连续自回归建模在钢琴渲染任务中的有效性，为高效、高质量的音乐生成提供了新的技术路径。
---

## 摘要
提示词引导的钢琴 MIDI 转音频渲染旨在忠实地渲染目标音符，同时复现参考录音的音色。现有方法主要遵循两种范式：自回归（AR）建模和流匹配（或扩散模型）。离散编解码器 AR 模型提供了因果时间建模，但量化过程可能会丢失声学细节。流匹配在保留声学结构方面表现更好，但代价是全序列注意力计算成本较高且语义结构较差。连续自回归模型直接在连续表示上进行操作，它不仅结合了 AR 模型的条件遵循能力和流匹配的分布建模能力，还通过更低的计算成本绕过了量化瓶颈。基于这一原则，我们提出了作曲家-演奏家-精修师（CPR）框架。作曲家模块自回归地预测连续隐藏状态，演奏家模块通过局部流匹配生成 24kHz 的声学潜变量，随后精修师模块将波形上采样至 48kHz。我们进一步引入了瓶颈表示对齐（BREPA）和模态-时间旋转位置编码（MT-RoPE），以增强作曲家隐藏状态中的音乐语义结构以及跨模态的时间对齐。代码已开源至 https://github.com/FEAfeatherTHER/CPR_official

## Abstract
Prompt-conditioned piano MIDI-to-Music rendering aims to faithfully render target notes while reproducing the timbre of a reference recording. Existing approaches primarily follow two paradigms: autoregressive (AR) modeling and flow matching (or diffusion). Discrete-codec AR models provide causal temporal modeling, but quantization can discard acoustic detail. Flow matching better preserves acoustic structure in the cost of full-sequence attention costs and worse semantic structure. Continuous autoregressive models operate directly on continuous representations. It not only combines the condition-following ability of AR models and distribution-modeling capacity of flow matching but also bypasses the quantization bottleneck with lower computational costs. Building on this principle, we present Composer--Performer--Refiner (CPR) framework. Composer autoregressively predicts continuous hidden states, Performer generates 24kHz acoustic latents through local flow matching and Refiner then upsamples the waveform to 48 kHz. We further introduce Bottlenecked Representation Alignment (BREPA) and Modality--Time RoPE (MT-RoPE) to strengthen musical semantic structure in Composer hidden states and temporal alignments across modalities. Codes are available at https://github.com/FEAfeatherTHER/CPR_official

---

## 论文详细总结（自动生成）

这是一份关于论文 **《CPR: Combining global composing, local performing and full-sequence refining in piano rendering with continuous autoregressive modelling》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：钢琴 MIDI 转音频（MIDI-to-Music）渲染旨在根据目标 MIDI 生成音频，并迁移参考录音的音色。
*   **痛点**：
    *   **离散 AR 模型**：量化过程会导致声学细节丢失，且存在教师强制（Teacher Forcing）带来的累积误差。
    *   **流匹配/扩散模型**：虽然声学结构保留较好，但全序列注意力机制计算成本高，且难以捕捉复杂的音乐语义结构。
*   **动机**：利用连续自回归建模绕过量化瓶颈，结合 AR 的条件遵循能力与流匹配的分布建模能力，实现高效、高质量的钢琴渲染。

### 2. 方法论：CPR 框架
*   **核心思想**：将任务拆解为三个阶段：**Composer（作曲家）**、**Performer（演奏家）** 和 **Refiner（精修师）**。
*   **关键技术**：
    *   **Composer**：基于 Qwen3-0.6B 初始化，自回归预测连续隐状态，负责全局语义建模。
    *   **Performer**：局部流匹配（Local Flow Matching）Transformer，根据 Composer 的隐状态和历史上下文生成 24kHz 的声学潜变量。
    *   **Refiner**：基于 Vocos 的上采样模块，将 24kHz 音频提升至 48kHz，补偿高频谐波。
    *   **BREPA（瓶颈表示对齐）**：通过瓶颈层压缩 Composer 隐状态并与自监督特征对齐，强化音乐语义结构。
    *   **MT-RoPE（模态-时间旋转位置编码）**：将模态身份与时间位置解耦，实现 MIDI 与音频在物理时间轴上的精确对齐。

### 3. 实验设计
*   **数据集**：整合了 MAESTRO、Slakh 和 Lakh MIDI 数据集，涵盖约 5,000 小时的钢琴音频。
*   **Benchmark**：使用 P-MUSE-eval 测试集（100 个样本）。
*   **对比方法**：P-MUSE（全序列流匹配）和 MIDI-VALLE（离散 AR 模型）。
*   **评价指标**：音色相似度（Sim）、音符触发 F1 分数（Onset F1）、Fréchet 音频距离（FAD_clap）以及主观平均意见得分（MOS）。

### 4. 资源与算力
*   **硬件**：8 张 NVIDIA GeForce RTX 5090 GPU。
*   **训练时长**：Composer-Performer 联合训练 200,000 步；Refiner 预训练 700,000 步，微调 90,000 步。

### 5. 实验数量与充分性
*   **实验覆盖**：包含客观指标对比、主观听感测试（13 名听众）、以及针对 BREPA 和 MT-RoPE 的消融实验。
*   **评价**：实验设计较为充分，通过 NFE（函数评估次数）分析了推理效率，消融实验验证了各模块的必要性，整体评估客观且具有说服力。

### 6. 主要结论与发现
*   **性能表现**：CPR 在音符触发准确度（Onset F1）和整体生成质量（FAD）上优于现有 SOTA 模型。
*   **效率优势**：通过局部流匹配，仅需 4 次 NFE 即可达到最优生成效果，显著降低了计算开销。
*   **语义增强**：BREPA 和 MT-RoPE 有效提升了跨模态对齐能力，证明了在连续隐空间中施加语义约束对音乐生成的价值。

### 7. 优点
*   **架构创新**：成功将连续 AR 与局部流匹配结合，既避免了量化损失，又规避了全序列扩散的高昂成本。
*   **语义对齐**：BREPA 机制巧妙地在语义结构与声学细节之间取得了平衡。
*   **跨模态对齐**：MT-RoPE 解决了 MIDI 与音频在不同采样率下的时间对齐难题。

### 8. 不足与局限
*   **音色还原度**：在音色相似度指标上略逊于 P-MUSE，作者认为这是由于局部建模丢失了部分全局上下文信息。
*   **Refiner 局限**：目前的 Refiner 仅是简单的上采样，无法完全恢复低分辨率生成阶段丢失的复杂高频细节。
*   **应用范围**：目前主要针对钢琴领域，对其他复杂乐器或多乐器组合的泛化能力尚待验证。

（完）
