---
title: "Particle GFlowNets: Rethinking Generative Marginalization Models"
title_zh: 粒子 GFlowNets：重新思考生成边缘化模型
authors: "Tiago da Silva, Diego Mesquita, Salem Lahlou"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11538v1"
tags: ["query:ardm"]
score: 8.0
evidence: 离散分布的任意顺序自回归建模
tldr: 本文揭示了生成边缘化模型（MaMs）与生成流网络（GFlowNets）在离散分布建模中的等价性，并提出了Particle GFlowNets。该方法通过引入基于Gelman-Rubin统计量的全状态重置准则，将MaMs的采样策略扩展至非自回归生成过程，显著提升了在大规模组合空间中的训练收敛速度。
selection_source: fresh_fetch
motivation: 旨在解决现有生成边缘化模型在处理复杂组合空间时训练效率低下的问题。
method: 通过建立MaMs与GFlowNets的等价联系，并引入基于Gelman-Rubin统计量的自动全状态重置准则来优化采样过程。
result: 实验表明，Particle GFlowNets在大型组合空间中能够显著加速模型训练。
conclusion: 该研究统一了两种生成模型范式，并为高效的离散分布采样提供了更具扩展性的解决方案。
---

## 摘要
生成边缘化模型（MaMs）最近被引入作为一种高效的神经采样模型，用于离散分布的任意阶自回归建模。通过学习持久块吉布斯采样器的边缘概率和条件概率，MaMs 能够通过单次神经网络前向传播实现快速的后验评估。尽管先前的工作认为 MaMs 与生成流网络（GFlowNets，一种用于离散随机模型推理的成熟范式）是不同的，但我们证明了它们是等价的。此外，我们将 MaMs 的采样策略扩展到了非自回归生成过程。具体而言，我们描述了一种基于 Gelman-Rubin 统计量的吉布斯采样器全状态重置自动准则，该准则在加速学习收敛方面起着关键作用。我们的实验表明，这种被称为粒子 GFlowNets 的方法显著加速了大型组合空间中的训练过程。

## Abstract
Generative Marginalization Models (MaMs) have been recently introduced as efficient neural sampling models for any-order autoregressive modelling of discrete distributions. By learning both the marginal and conditional probabilities of a persistent-block Gibbs sampler, MaMs enable fast posterior evaluation with a single neural network forward pass. While prior work has considered MaMs to be distinct from Generative Flow Networks (GFlowNets), a well-established paradigm for inference in discrete stochastic models, we show that they are equivalent. Then, we also extend MaMs' sampling strategy to non-autoregressive generative processes. In particular, we describe an automatic criterion for full-state rejuvenation of the Gibbs sampler, derived from the Gelman-Rubin statistic, which plays a key role in speeding up learning convergence. Our experiments show that our method, called Particle GFlowNets, markedly accelerates training in large combinatorial spaces.

---

## 论文详细总结（自动生成）

这是一份关于论文《Particle GFlowNets: Rethinking Generative Marginalization Models》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：生成流网络（GFlowNets）在组合空间采样和贝叶斯推理中表现出色，但其训练过程通常需要模拟长轨迹，导致计算开销巨大。
*   **动机**：生成边缘化模型（MaMs）通过学习吉布斯采样器的边缘和条件分布，实现了单次前向传播的快速评估，但其应用局限于自回归模型。
*   **核心目标**：揭示 MaMs 与 GFlowNets 的数学等价性，并将 MaMs 的高效采样策略扩展至非自回归领域，解决大规模组合空间中训练收敛慢的问题。

### 2. 方法论：Particle GFlowNets
*   **核心思想**：证明了 MaMs 本质上是排列条件（Permutation-Conditioned）的 GFlowNets。通过将 MaMs 的一致性损失函数与 GFlowNets 的详细平衡（Detailed Balance）损失函数统一，实现了计算效率的提升。
*   **关键技术**：
    *   **持久化吉布斯采样**：在训练过程中维护一组持久化的状态，通过移除和添加组件进行更新，将每步梯度更新的计算复杂度降至 $O(1)$。
    *   **链重置（Rejuvenation）准则**：引入 Gelman-Rubin 统计量（$\hat{R}$）作为自动触发器。当采样链相关性过高（$\hat{R} > 1.1$）时，强制重置采样过程，以增强探索能力并加速收敛。
    *   **非自回归扩展**：通过引入随机反向转换核，使模型能够处理非注入式的前向映射，从而支持集合生成等非自回归任务。

### 3. 实验设计
*   **场景与数据集**：
    *   **集合生成**：基于对数加性奖励的固定大小子集生成。
    *   **贝叶斯变量选择**：在具有相关特征的线性模型中进行后验分布采样。
    *   **Ising 模型与位序列生成**：用于评估模型在长轨迹和复杂奖励函数下的表现。
    *   **系统发育推断**：使用简约评分（Parsimony score）作为奖励函数。
*   **对比方法**：与标准 GFlowNets、自适应教师（AT-GFlowNets）和自适应互补探索（ACE）方法进行了对比。

### 4. 资源与算力
*   **硬件环境**：实验在 Apple MacBook Pro (M4 芯片，10 核 CPU，16GB 统一内存) 上运行。
*   **训练细节**：使用了 JAX 框架，采用 Muon 优化器。文中未提及大规模集群训练，强调了该方法在有限计算资源下的高效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型到大规模（如 $d=900$）的多种组合空间，进行了多组对比实验和消融实验（如重置准则的有效性）。
*   **客观性**：实验设计考虑了不同 MDP 视界（Horizon）和奖励查询成本，对比了多种前沿基准方法，实验设置相对公平且具有说服力。

### 6. 主要结论与发现
*   **等价性**：MaMs 与 PC-GFlowNets 在数学上是等价的，MaMs 的一致性损失是 GFlowNets 详细平衡损失的一种高效估计。
*   **性能提升**：Particle GFlowNets 在大规模组合空间中显著降低了训练的墙上时间（Wall-clock time），且随着 MDP 视界的增加，性能优势愈发明显。
*   **重置机制**：基于 $\hat{R}$ 的重置准则对于防止采样链陷入局部最优、加速收敛至关重要。

### 7. 优点
*   **理论统一**：将两种看似独立的生成模型范式统一，增强了对 GFlowNet 训练机制的理解。
*   **计算高效**：通过 $O(1)$ 的前向传播次数，显著降低了长轨迹任务的训练成本。
*   **通用性**：成功将自回归建模的优势扩展到了非自回归的组合优化任务中。

### 8. 不足与局限
*   **应用限制**：目前主要针对离散空间，扩展至连续或混合空间仍是挑战。
*   **超参数敏感性**：重置准则的阈值（如 $\hat{R} > 1.1$）可能需要针对特定任务进行调整。
*   **奖励查询成本**：当奖励函数本身的评估成本远高于策略网络评估时，该方法的计算优势会减弱。

（完）
