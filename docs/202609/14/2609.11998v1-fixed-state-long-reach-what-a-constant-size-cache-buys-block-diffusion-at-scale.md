---
title: "Fixed State, Long Reach: What a Constant-Size Cache Buys Block Diffusion at Scale"
title_zh: 固定状态，长程覆盖：恒定大小缓存为大规模块扩散模型带来的收益
authors: "Vaibhav Singh, Pierre-André Noël, Torsten Scholak, Eugene Belilovsky, Oleksiy Ostapenko"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.11998v1"
tags: ["query:ardm"]
score: 9.0
evidence: 具有恒定大小缓存的块扩散模型，用于类自回归推理
tldr: 本文研究了块扩散（Block Diffusion）模型在长文本生成中的缓存优化问题。针对传统注意力机制缓存随序列长度线性增长（O(L)）的局限，作者提出利用序列混合器将已完成块总结为固定状态，并结合块因果训练目标实现精确缓存。实验表明，基于状态空间模型（Mamba）的缓存实现了O(1)的内存和延迟开销，在256k长文本下显著提升了吞吐量并增强了外推能力。
selection_source: fresh_fetch
motivation: 传统扩散语言模型的注意力缓存随序列长度线性增长，导致长文本推理时内存占用过高且延迟增加。
method: 通过序列混合器将已完成块总结为固定状态，并采用块因果训练目标，使缓存能够精确表示模型计算。
result: 在256k长度下，Mamba缓存相比注意力机制实现了4.3倍的延迟降低、11倍的内存节省及14倍的吞吐量提升。
conclusion: 固定大小的状态缓存不仅解决了长序列下的扩展性瓶颈，还显著增强了模型在超长上下文中的检索与外推能力。
---

## 摘要
扩散语言模型能够并行解码标记，但其双向去噪器排除了快速自回归推理中常用的键值（KV）缓存。块扩散模型通过逐块解码恢复了缓存机制，但目前应用于此的块缓存仍受限于注意力机制：内存占用为 O(L)，且若作为无需训练的改进方案，仅能近似模型的计算过程。上述两个限制均可克服：将已完成块总结为可重用状态的序列混合器支持块缓存，而相应的块因果训练目标则使缓存变得精确。我们在大规模环境下研究了这一方案，在 300B 标记上使用单一前沿目标预训练了三个 3B 参数的块扩散去噪器（注意力机制、Mamba 和混合架构），并通过统一的缓存接口对三者进行解码。只有状态空间缓存的序列长度复杂度为 O(1)：其内存占用和单步延迟在任何上下文长度下均保持恒定，而注意力缓存则保持为 O(L)。在 256k 标记长度下（此时注意力机制的内存占用已增至 82GB，单步延迟为 29 毫秒），Mamba 缓存实现了 4.3 倍的延迟降低、11 倍的内存节省以及 2.6 倍的单流吞吐量；由于其占用空间恒定，它也能随批处理规模扩展，达到 14 倍的总吞吐量，而注意力机制此时已无法在单流之外运行。同样的线性状态偏差使得 Mamba 和混合架构骨干网络能够保持检索能力至训练长度的 8-16 倍，而注意力机制的检索能力在 2 倍长度时即崩溃，且未产生可测量的质量损失。

## Abstract
Diffusion language models decode tokens in parallel, but their bidirectional denoiser rules out the naive key--value (KV) cache behind fast autoregressive inference. Block diffusion restores caching by decoding block-by-block, and the block caches deployed on it so far are tied to attention: O(L)in memory and, if used as training-free retrofits, only an approximation of the model's computation. Both constraints can be overcome: sequence mixers that summarize finalized blocks into a reusable state support block caching, and the corresponding block-causal training objective makes the cache exact. We study this recipe at scale, pretraining three 3B block-diffusion denoisers (attention, mamba, and hybrid) on 300B tokens under one single-frontier objective and decoding all three through a single cached interface. Only the state-space cache is O(1) in sequence length: its memory and per-step latency stay constant at any context length, while an attention cache remains O(L). At 256k tokens (where attention has grown to 82GB and 29 ms/step), the Mamba cache delivers 4.3x lower latency, 11x less memory, and 2.6x higher single-stream throughput; and because that footprint is constant it scales with batch as well, reaching 14x the aggregate throughput, where attention cannot run beyond a single stream. The same linear-state bias lets the Mamba and hybrid backbones keep retrieving out to 8-16x their training length, whereas attention's retrieval collapses at 2x, at no measured quality cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fixed State, Long Reach: What a Constant-Size Cache Buys Block Diffusion at Scale》的结构化总结：

### 1. 核心问题与研究背景
*   **背景**：扩散语言模型（DLMs）通过并行去噪实现生成，但其双向注意力机制导致无法使用传统自回归模型（AR）的 KV 缓存，推理速度较慢。
*   **核心问题**：现有的“块扩散”（Block Diffusion）方案虽然通过分块解码恢复了缓存，但其缓存机制仍依赖注意力机制，导致内存占用随序列长度 $L$ 线性增长（$O(L)$），且在长文本场景下计算开销巨大。
*   **目标**：通过引入状态空间模型（SSM/Mamba）作为序列混合器，实现 $O(1)$ 复杂度的恒定大小缓存，从而在保持生成质量的同时，大幅提升长文本推理的效率与外推能力。

### 2. 方法论
*   **核心思想**：利用具有线性递归特性的序列混合器（如 Mamba-2），将已完成的块总结为固定大小的隐藏状态，替代随长度增长的 KV 缓存。
*   **关键技术**：
    *   **块因果训练目标**：通过“单前沿”训练目标，确保模型在训练时看到的上下文与推理时一致，使缓存计算精确而非近似。
    *   **混合架构**：结合注意力机制（用于全局召回）与 Mamba（用于线性递归），在保持长程记忆的同时实现高效推理。
    *   **推理流程**：采用块级自回归生成，每完成一个块，模型更新一次固定大小的 SSM 状态，后续块仅需读取该状态，无需重新计算整个前缀。

### 3. 实验设计
*   **模型对比**：对比了三种 3B 参数的骨干网络：纯注意力模型（Attn）、纯 Mamba 模型（Mamba）以及混合模型（Hybrid）。
*   **评估指标**：
    *   **效率**：单步延迟、峰值内存、端到端吞吐量（在 64 到 256k 长度下测试）。
    *   **长文本能力**：NIAH（大海捞针）检索任务、LongBench（16 项长文本任务）。
    *   **生成质量**：下游任务准确率、生成困惑度（Gen-PPL）、MAUVE 指标。

### 4. 资源与算力
*   **硬件**：所有效率测量均在单张 NVIDIA H100 80GB GPU 上完成。
*   **训练规模**：在 300B 标记（Tokens）上进行预训练，使用 bf16 精度，AdamW 优化器，全局批大小为 4096。

### 5. 实验数量与充分性
*   **充分性**：实验非常充分。涵盖了从 64 到 256k 的长上下文扩展测试，并进行了批处理（Batch Size 1-8）的压力测试。
*   **公平性**：所有模型共享相同的分词器、训练数据、调度策略和参数规模（约 3B），仅改变序列混合器，对比非常客观、公平。

### 6. 主要结论与发现
*   **效率优势**：在 256k 长度下，Mamba 缓存相比注意力机制实现了 4.3 倍的延迟降低、11 倍的内存节省，以及 14 倍的聚合吞吐量。
*   **外推能力**：Mamba 和混合架构在 8-16 倍训练长度下仍能保持良好的检索能力，而注意力机制在 2 倍长度时即出现显著崩溃。
*   **质量 parity**：上述效率提升几乎没有牺牲模型质量，Mamba 架构在下游任务表现上与注意力模型基本持平。

### 7. 优点
*   **架构创新**：首次在大规模（3B）扩散语言模型中验证了纯 SSM 缓存的可行性。
*   **工程价值**：证明了固定大小缓存不仅能解决内存瓶颈，还能通过批处理实现极高的吞吐量。
*   **鲁棒性**：通过对比实验清晰地展示了线性状态模型在长文本外推上的天然优势。

### 8. 不足与局限
*   **训练长度限制**：模型仅在 1024 长度下训练，长文本表现依赖于外推，而非直接训练所得。
*   **MAUVE 指标噪声**：作者指出在小规模模型生成短文本时，MAUVE 指标存在噪声，可能影响生成质量的精确评估。
*   **混合架构的复杂性**：虽然混合架构表现优异，但其缓存接口需要同时处理 KV 缓存和 SSM 状态，增加了系统实现的复杂性。

（完）
