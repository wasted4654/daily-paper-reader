---
title: "DiTAR+: Dual Optimization for Robust Autoregressive Diffusion Speech Synthesis"
title_zh: DiTAR+：面向鲁棒自回归扩散语音合成的双重优化框架
authors: "Ziyu Zhang, Tianlun Zuo, Hanzhao Li, Haoyu Zhang, Lei Xie"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.13909v1"
tags: ["query:ardm"]
score: 10.0
evidence: 提出了 DiTAR+，一种用于语音合成的自回归扩散 Transformer
tldr: DiTAR+ 是一款针对自回归扩散语音合成（AR-DiT）的优化框架。针对长文本合成中因历史感受野受限和声学惯性导致的解码不稳定问题，该研究提出了扩张上下文采样和分层声学掩码技术，有效解耦了语义对齐与声学细节重建，显著提升了长语音生成的鲁棒性、发音准确度及说话人相似度。
selection_source: fresh_fetch
motivation: 现有自回归扩散模型在处理长文本或复杂语言结构时，因历史感受野不足和声学惯性依赖导致解码不稳定及语义忽略。
method: 提出双重优化框架：通过扩张上下文采样扩大宏观感受野以抑制累积误差，并利用分层声学掩码解耦语义对齐与声学细节。
result: "在ZH-Hard数据集上将词错误率从12.478%降至9.893%，并在长语音合成中显著提升了说话人相似度与生成鲁棒性。"
conclusion: DiTAR+ 有效解决了自回归扩散语音合成中的长程稳定性问题，在长文本和复杂场景下表现优于现有主流基线模型。
---

## 摘要
连续潜空间自回归扩散 Transformer（AR-DiT）模型在零样本语音生成领域展现出巨大潜力。然而，在合成长语音或复杂语言结构时，这些模型仍面临解码稳定性不足的问题。这种不稳定性主要源于扩散解码器中受限的历史感受野和声学惯性依赖，导致模型忽略了语义条件。为解决这些挑战，我们提出了 DiTAR+ 双重优化框架。首先，我们引入了扩张上下文采样（Dilated Context Sampling），在不破坏物理时间连续性的前提下扩大了宏观历史感受野，从而防止了累积误差的传播。其次，我们提出了分层声学掩码（Hierarchical Acoustic Masking），防止浅层网络关注声学前置上下文，从而显式地将语义对齐与声学细节重构解耦。大量实验表明，该框架有效缓解了发音错误和语义幻觉，增强了模型在复杂句子上的生成鲁棒性，并在长语音合成中保持了极高的说话人相似度。在语言难度较高的 ZH-Hard 测试集上，DiTAR+ 将词错误率从 12.478% 降低至 9.893%；在 25 至 35 秒的长语音合成任务中，该模型在将说话人相似度从 0.741 提升至 0.759 的同时，将词错误率从 2.778% 降低至 2.173%，性能优于离散标记（discrete-token）和纯流匹配（pure flow-matching）基线模型。

## Abstract
Continuous-latent Autoregressive Diffusion Transformer (AR-DiT) models have demonstrated immense potential in zero-shot speech generation. However, they still suffer from limited decoding stability when synthesizing long utterances or complex linguistic structures. This instability primarily stems from a restricted historical receptive field and an acoustic inertia dependency within the diffusion decoder, which causes the model to ignore semantic conditions. To address these challenges, we propose DiTAR+, a dual-optimization framework. First, we introduce Dilated Context Sampling to expand the macro-level historical receptive field without violating physical temporal continuity, thereby preventing cumulative error propagation. Second, we propose Hierarchical Acoustic Masking to prevent shallow layers from attending to acoustic pre-context, explicitly decoupling semantic alignment from acoustic detail reconstruction. Extensive experiments show that our framework effectively mitigates pronunciation errors and semantic hallucinations, enhances generation robustness on challenging sentences, and maintains exceptionally high speaker similarity throughout the entirety of long-form utterances. On the linguistically challenging ZH-Hard set, DiTAR+ reduces the word error rate from 12.478% to 9.893%, and on extended utterances of 25 to 35 seconds it improves speaker similarity from 0.741 to 0.759 while simultaneously lowering the word error rate from 2.778% to 2.173%, outperforming both discrete-token and pure flow-matching baselines.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity。很高兴为你分析这篇关于语音合成的学术论文。以下是对《DiTAR+: Dual Optimization for Robust Autoregressive Diffusion Speech Synthesis》的结构化总结：

---

### 1. 核心问题与研究动机
*   **背景**：连续潜空间自回归扩散 Transformer（AR-DiT）在零样本语音生成中表现优异，但在处理长文本或复杂语言结构时，常出现发音错误、语义幻觉、说话人漂移及循环重复等不稳定性问题。
*   **核心痛点**：
    *   **历史感受野受限**：解码器仅依赖紧邻的前序片段，导致长程信息丢失，产生累积误差。
    *   **声学惯性依赖**：浅层网络过度依赖局部声学上下文，导致模型倾向于“复制”前文而非遵循语义指令，从而忽略了上游语言模型的语义对齐。

### 2. 方法论：DiTAR+ 双重优化框架
*   **核心思想**：通过“扩张上下文”扩大视野，通过“分层掩码”强制语义对齐，实现语义与声学细节的解耦。
*   **关键技术**：
    *   **扩张上下文采样 (DCS)**：在保持局部连续性的基础上，以固定步长（stride）稀疏采样远距离的历史片段。这在不破坏物理时间顺序的前提下，显著扩大了宏观感受野。
    *   **分层声学掩码 (HAM)**：在扩散解码器的浅层（前 $N_1$ 层）强制掩盖历史声学上下文，迫使模型仅依据语义条件（$h_i$）进行生成；在深层解除掩码，以恢复声学细节的精细重建。

### 3. 实验设计
*   **数据集**：使用大规模多语言数据集 **Emilia** 进行训练。
*   **评估基准**：
    *   **标准集**：Seed-EN, Seed-ZH。
    *   **压力测试集**：ZH-Hard（复杂语言结构）、ZH-Long（25-35秒长语音）。
*   **对比方法**：DiTAR（基线）、CosyVoice 2（离散 Token 模型）、MaskGCT（非自回归模型）、F5-TTS 和 E2 TTS（纯流匹配模型）。

### 4. 资源与算力
*   **硬件配置**：使用 4 个计算节点，每个节点配备 8 张 **H20 GPU**，总计 **32 张 H20 GPU**。
*   **训练细节**：每张 GPU 的 Batch Size 设置为 8。文中未明确提及具体的训练时长。

### 5. 实验数量与充分性
*   **实验覆盖**：包含客观指标（WER, SIM）和主观指标（N-MOS, Q-MOS, S-MOS, C-MOS），涵盖了标准场景与极端压力测试场景。
*   **消融实验**：针对 DCS 和 HAM 模块进行了拆解分析，并设置了“朴素重排序（Skip）”作为负面控制组，实验设计客观且逻辑严密，充分验证了各模块的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 ZH-Hard 上，WER 从 12.478% 降至 9.893%；在长语音任务中，SIM 从 0.741 提升至 0.759，WER 降至 2.173%。
*   **稳定性**：DCS 有效抑制了长语音中的说话人漂移；HAM 彻底解决了因声学惯性导致的“循环重复”现象。
*   **权衡**：研究指出，虽然 DiTAR+ 在语义鲁棒性上大幅领先，但由于打破了对局部声学惯性的过度拟合，其 Q-MOS（质量主观分）与基线相比有微小波动，这被视为提升鲁棒性所必须的架构权衡。

### 7. 优点
*   **架构创新**：DCS 巧妙地在不破坏时间序列的前提下解决了感受野问题，避免了复杂的重排序逻辑。
*   **解耦设计**：HAM 引入的层级掩码机制非常直观且有效，为解决自回归模型的“语义忽略”问题提供了新思路。
*   **鲁棒性强**：在极端长文本和复杂语言场景下表现出极高的稳定性，优于当前主流的离散 Token 和流匹配模型。

### 8. 不足与局限
*   **计算开销**：虽然 DCS 采样计算成本较低，但引入多层掩码机制和更长的上下文输入，可能会在推理时带来一定的内存或计算压力。
*   **主观质量权衡**：如文中分析，模型在追求语义鲁棒性的同时，牺牲了部分微观层面的声学平滑度（Q-MOS 略有下降），在某些对音质要求极高的场景下可能需要进一步优化。
*   **应用限制**：该方法主要针对 AR-DiT 架构，对于非自回归（NAR）或完全不同的生成范式，其适用性有待进一步验证。

（完）
