---
title: How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus
title_zh: “无损”推测解码究竟有多无损？数值精度在 Orthrus 中的作用
authors: "Ilya Koziev, Leonid Sinev, Ivan Oseledets"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15504v1"
tags: ["query:ardm"]
score: 9.0
evidence: 分析了 Orthrus，一种用于语言模型的自回归-扩散混合架构
tldr: "本文研究了混合自回归扩散架构Orthrus的“无损”推测解码特性。研究发现，在BF16精度下，Orthrus无法完全复现自回归模型的输出轨迹，匹配率仅约45%，且与模型困惑度高度相关。然而，这种轨迹偏差并未导致下游任务性能下降。实验证明，Orthrus的无损性高度依赖于数值精度，在FP32下可实现完全一致，强调了区分轨迹等价性与任务性能评估的重要性。"
selection_source: fresh_fetch
motivation: 旨在验证Orthrus架构声称的“无损”推测解码在不同数值精度下的真实表现。
method: 通过复现Orthrus模型，在BF16和FP32精度下对比其与自回归基座在1190个提示词上的输出轨迹一致性。
result: "BF16精度下仅有约45%的轨迹匹配，而FP32精度下可实现100%匹配，且轨迹偏差未导致下游任务性能显著下降。"
conclusion: Orthrus的无损性受数值精度影响，轨迹等价性与下游任务性能应作为两个独立的指标进行评估。
---

## 摘要
Orthrus 是一种混合自回归-扩散架构，通过在保持自回归主干模型不变的同时并行生成多个 token，从而加速自回归语言模型的推理。其核心主张是利用模型内部的一致性机制实现无损推测解码，即产生与自回归模型完全相同的输出序列。我们独立复现了 Orthrus，并在不同数值精度下检验了这一主张。在 BF16 推理下，针对作者提供的检查点，仅有 45% 的情况实现了轨迹完全匹配，而我们独立训练的模型在涵盖 12 个领域的 1,190 条提示词测试中，匹配率仅为 43%。精确匹配的概率与参考模型在响应条件下的困惑度（perplexity）密切相关。尽管存在轨迹偏差，Orthrus 在下游 lm-eval-harness 基准测试中并未表现出系统性的性能下降。相比之下，在 FP32 精度下重复轨迹评估时，所有测试提示词均实现了轨迹完全匹配。这些结果表明，Orthrus 的实际无损性取决于数值精度，且轨迹的精确等价性应与下游任务性能分开评估。

## Abstract
Orthrus is a hybrid autoregressive-diffusion architecture that accelerates autoregressive language-model inference by generating multiple tokens in parallel while using a frozen autoregressive backbone. Its central claim is that an intra-model consensus mechanism enables lossless speculative decoding, producing the same output sequence as the autoregressive model.   We independently reproduce Orthrus and examine this claim under different numerical precisions. Under BF16 inference, exact trajectory matching occurs in only 45% of cases for the authors' checkpoint and 43% for our independently trained model across 1,190 prompts from 12 domains. The probability of exact matching is also strongly associated with the response-conditional perplexity of the reference model. Despite this trajectory divergence, Orthrus does not show systematic degradation on downstream lm-eval-harness benchmarks. In contrast, repeating the trajectory evaluation with FP32 yields exact trajectory matching on all evaluated prompts.   These results show that the practical losslessness of Orthrus depends on numerical precision and that exact trajectory equivalence should be evaluated separately from downstream task performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《How Lossless Is Lossless Speculative Decoding? The Role of Numerical Precision in Orthrus》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：自回归（AR）语言模型推理存在严重的顺序依赖瓶颈。Orthrus 架构通过引入扩散模型视图实现并行 token 生成，并声称其“一致性机制”能实现**无损推测解码**（即输出序列与原 AR 模型完全一致）。
*   **研究动机**：作者质疑在有限精度算术（如 BF16）下，这种“无损”声明是否在工程实现上依然成立。论文旨在通过独立复现，验证轨迹等价性与数值精度之间的关系。

### 2. 方法论
*   **核心思想**：利用扩散模型并行预测未来 token，并通过 AR 主干模型进行验证。
*   **关键技术**：
    *   **教师强制蒸馏（Teacher-forced Distillation）**：为了让扩散模型更好地拟合 AR 模型的生成轨迹，作者构建了由 AR 模型贪婪解码生成的“在线（on-policy）”数据集。
    *   **一致性机制**：通过 AR 视图对扩散模型提出的候选序列进行验证，以确保输出的确定性。
    *   **轨迹对比评估**：直接对比 Orthrus 生成的 token 序列与原 AR 模型在相同输入下的序列，计算精确匹配率（Exact Match Rate）。

### 3. 实验设计
*   **数据集**：涵盖 12 个不同领域（代码、创意写作、数学、翻译、问答等），共 1,190 条提示词。
*   **基准测试（Benchmark）**：使用 `lm-eval-harness` 评估下游任务性能（GSM8K, HumanEval, IFEval）。
*   **对比方法**：对比了作者发布的 `chiennv/Orthrus-Qwen3-1.7B` 检查点与作者独立训练的 `Orthrus-1.7B-final` 模型。

### 4. 资源与算力
*   **硬件配置**：训练阶段使用了 8 张 NVIDIA H100 GPU。
*   **推理评估**：在 NVIDIA GeForce RTX 3090 (23GB 显存) 上进行。
*   **软件栈**：CUDA 13.3.73 / 12.8，PyTorch 2.13.0 / 2.8.0，Transformers 5.8.0 / 5.8.1。

### 5. 实验数量与充分性
*   **实验覆盖**：实验设计非常充分，涵盖了 12 个不同领域，并对比了两种不同训练配置的模型。
*   **客观性**：通过对比 BF16 和 FP32 两种精度，明确了数值误差的来源，实验逻辑严密且具有高度的可复现性。
*   **公平性**：使用了相同的提示词集和相同的推理参数（greedy decoding, temperature=0.0），确保了对比的公平性。

### 6. 主要结论与发现
*   **精度敏感性**：在 BF16 精度下，Orthrus 并非完全无损，轨迹匹配率仅为 43%-45%；但在 FP32 精度下，轨迹实现 100% 匹配。
*   **困惑度关联**：轨迹偏差与参考模型的响应条件困惑度（PPL）呈强负相关，即模型越“不确定”的生成，越容易出现轨迹分歧。
*   **性能解耦**：轨迹偏差并不等同于下游任务性能的下降，有时甚至会出现性能微弱提升的现象，证明了“轨迹等价性”与“任务性能”是两个独立维度。

### 7. 优点
*   **严谨的定义区分**：明确区分了“理论上的无损”与“工程实现上的无损”，纠正了学术界对“无损”一词的模糊使用。
*   **实证驱动**：通过大规模的轨迹对比实验，揭示了数值精度对 LLM 推理确定性的深层影响。
*   **方法论贡献**：提出了基于在线蒸馏的训练策略，提升了并行生成的效率（TPF）。

### 8. 不足与局限
*   **硬件依赖**：实验结果高度依赖于特定的硬件架构和浮点运算实现，不同 GPU 架构下的表现可能存在差异。
*   **未深入底层**：虽然指出了数值精度是原因，但未深入分析具体是哪一层或哪种算子（如 Attention 机制中的 Softmax）导致了 BF16 下的累积误差。
*   **应用限制**：对于追求极致确定性的应用场景，BF16 下的 Orthrus 可能无法满足合规性要求，必须切换至 FP32，这会牺牲一定的推理速度。

（完）
