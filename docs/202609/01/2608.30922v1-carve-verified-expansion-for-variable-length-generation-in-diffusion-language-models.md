---
title: "CARVE: Verified Expansion for Variable-Length Generation in Diffusion Language Models"
title_zh: CARVE：扩散语言模型中变长生成的验证扩展
authors: "Wail Bouhedja, Amr Mohamed, Guokan Shang"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30922v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于可变长度生成的掩码扩散语言模型
tldr: "针对掩码扩散语言模型（DLM）生成长度固定的局限性，本文提出 CARVE 算法。该方法无需重新训练，通过在解码过程中动态插入 [MASK] 并利用反事实验证机制（基于 JS 散度）来决定是否扩展画布。CARVE 在提升代码生成和数学推理准确性的同时，显著降低了计算开销，实现了高效且灵活的可变长度生成。"
selection_source: fresh_fetch
motivation: 传统的扩散语言模型在生成前需固定长度，过短会导致内容截断，过长则浪费计算资源并干扰去噪过程。
method: 提出一种无需训练的验证扩展算法，通过比较扩展前后未解析位置的预测稳定性（JS 散度）来动态调整生成长度。
result: "在代码生成和数学推理任务中，CARVE 在多个模型系列上均优于固定长度基准，且推理成本最高可降低 50%。"
conclusion: CARVE 证明了通过验证稳定性来实现扩散模型的可变长度生成是提升效率与性能的有效途径。
---

## 摘要
掩码扩散语言模型从部分观测的响应画布中预测 token，从而实现双向调节和并行 token 细化。然而，标准的掩码扩散解码器使用僵化的推理接口：分配给答案的掩码位置数量在生成开始前就已固定。选择这一长度非常困难：画布过短可能会截断推理过程或代码，而画布过长则会浪费计算资源并可能干扰去噪过程。我们提出了 CARVE（Counterfactual-Aware Reveal with Verified Expansion，基于反事实感知与验证扩展），这是一种用于掩码扩散语言模型的无需训练的变长算法。CARVE 从较短的画布开始，通过在解码过程中插入额外的 [MASK] 位置来增加响应长度。CARVE 并非保留每一次插入，而是测试候选的扩展画布并提出一个反事实问题：如果存在额外的掩码空间，模型对原始画布中未解析位置的预测是否会保持相似？只有当插入的掩码在对齐的未解析位置上产生较低的 Jensen-Shannon (JS) 散度时，这些掩码才会被保留。这使得长度增长成为一种经过验证的稳定性决策，而非纯粹的置信度启发式方法。CARVE 无需重新训练即可应用于全画布和分块扩散解码器。在代码生成和数学推理基准测试中，CARVE 在所有评估的模型系列上均一致地优于固定长度基线。至关重要的是，CARVE 在获得这些准确率提升的同时降低了推理成本，在某些设置下其 FLOPs 仅为固定长度解码的一半。

## Abstract
Masked diffusion language models predict tokens from a partially observed response canvas, enabling bidirectional conditioning and parallel token refinement. Yet standard masked-diffusion decoders use a rigid inference interface: the number of masked positions allocated to the answer is fixed before generation begins. Choosing this length is difficult. A short canvas can truncate reasoning or code, while a long canvas wastes computation and can perturb denoising. We introduce CARVE (Counterfactual-Aware Reveal with Verified Expansion), a training-free variable-length algorithm for masked diffusion LMs. Starting from a shorter canvas, CARVE can grow the response during decoding by inserting additional [MASK] positions. Rather than keeping every insertion, CARVE tests a candidate expanded canvas and asks a counterfactual question: would the model make similar predictions for the unresolved positions in the original canvas if the extra masked space were present? The inserted masks are kept only when they induce low Jensen-Shannon (JS) divergence on aligned unresolved positions. This makes length growth a verified stability decision rather than a pure confidence heuristic. CARVE applies without retraining to both full-canvas and blockwise diffusion decoders. Across code generation and mathematical reasoning benchmarks, CARVE consistently improves average performance over fixed-length baselines across all evaluated model families. Crucially, CARVE achieves these accuracy gains while reducing inference cost, reaching half the FLOPs of fixed-length decoding in some settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《CARVE: Verified Expansion for Variable-Length Generation in Diffusion Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
掩码扩散语言模型（dLLMs，如 LLaDA、Dream）在生成文本时，通常要求在推理开始前预先固定“画布”长度（即掩码 token 的数量）。这种**固定长度假设**存在显著缺陷：
*   **长度难以预估**：若画布过短，会截断代码或推理逻辑；若过长，则会浪费计算资源，且可能强迫模型在有效响应之外进行去噪，从而干扰生成质量。
*   **缺乏灵活性**：不同任务甚至同一任务的不同实例对响应长度的需求差异巨大。
*   **现有方案局限**：现有的变长方法要么需要昂贵的重新训练，要么仅依赖简单的置信度启发式规则，忽略了插入新掩码对现有预测状态的扰动。

### 2. 方法论：核心思想与关键技术
论文提出了 **CARVE**（Counterfactual-Aware Reveal with Verified Expansion），一种无需训练的变长解码算法。其核心思想是将长度增长视为一个**稳定性验证决策**。

*   **核心思想**：从较短的画布开始，在解码过程中尝试插入额外的 `[MASK]`。通过一个“反事实”测试来决定是否接受扩展：即“如果我增加了空间，模型对原有位置的预测是否依然稳定？”
*   **关键技术细节**：
    1.  **不确定性引导的分支**：在当前画布中熵（Entropy）最高的区域附近选择锚点，尝试插入 $k$ 个新掩码，形成“扩展分支”。
    2.  **JS 散度验证**：计算原始分支与扩展分支在对齐的未解析位置上的平均 **Jensen-Shannon (JS) 散度**。
    3.  **验证准则**：若平均 JS 散度低于设定阈值 $\tau_{JS}$，说明扩展不会破坏现有预测的稳定性，接受扩展；否则拒绝，回退到原画布。
    4.  **自适应揭示计划**：修改了原有的揭示比例，确保在画布动态增长时，每一步都能按比例揭示 token，避免后期堆积。
    5.  **EOS 触发裁剪**：一旦模型生成了结束符（EOS），立即裁剪掉后续掩码并停止，显著节省算力。

### 3. 实验设计
*   **评估模型**：Dream-v0-Instruct-7B（全画布解码）、LLaDA-1.5（分块解码）、LLaDA-8B-Instruct（分块解码）。
*   **数据集/基准**：
    *   **代码生成**：HumanEval, MBPP（衡量 Pass@1）。
    *   **数学推理**：MATH-500, GSM8K（衡量 Exact Match 准确率）。
*   **对比方法**：
    *   **Baseline**：标准固定长度解码。
    *   **DAEDAL**：另一种无需训练的变长扩散解码基准。

### 4. 资源与算力
*   **硬件**：使用了 **AMD MI210 GPU**。
*   **算力消耗**：总计消耗约 **42 GPU-days**。
*   **并发量**：实验过程中最多同时运行 8 块 GPU。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同架构和规模的模型上进行了 4 个主流基准测试，涵盖了代码和数学两大高难度领域。
*   **消融实验**：非常充分。作者分别验证了自适应揭示计划的贡献、JS 阈值的影响、不同插入模式（中间插入 vs 尾部插入）的效果，并对 CARVE 的各个组件进行了剥离分析。
*   **客观性**：通过 FLOPs（浮点运算数）而非仅靠 wall-clock time 来衡量推理成本，排除了硬件环境干扰，对比更加公平。

### 6. 主要结论与发现
*   **性能提升**：CARVE 在所有评估的模型系列上均一致地提高了平均任务准确率（例如在 Dream-7B 上平均提升 0.92%）。
*   **成本降低**：尽管 CARVE 在验证步骤中增加了额外的模型前向传播，但由于 EOS 裁剪和更高效的揭示计划，其总推理 FLOPs 显著低于固定长度基线，在某些场景下**计算量减少了近 50%**。
*   **稳定性至关重要**：实验证明，简单的“一直扩展”策略不如“经过验证的扩展”稳定，JS 散度是一个有效的稳定性度量指标。

### 7. 优点
*   **无需训练（Training-free）**：直接应用于现有的预训练扩散模型，具有极强的通用性。
*   **模型无关（Model-agnostic）**：同时适用于全画布（Dream）和分块（LLaDA）等不同的扩散后端。
*   **效率与精度双赢**：打破了“变长生成必然增加开销”的直觉，实现了更准、更快的生成。
*   **理论支撑**：引入反事实稳定性的概念，为扩散模型的推理控制提供了新的视角。

### 8. 不足与局限
*   **固定步长限制**：目前每次扩展插入的掩码数量 $k$ 是固定的（如 16），未能根据提示词的复杂程度动态调整插入步长。
*   **对齐机制较简单**：JS 散度仅对比了原始位置，未考虑新插入位置对整体语义分布的潜在长程影响。
*   **超参数敏感性**：JS 阈值 $\tau_{JS}$ 和扩展间隔 $I$ 需要针对不同模型进行微调以达到最优平衡。

（完）
