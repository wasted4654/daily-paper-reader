---
title: Consistent Diffusion Language Models
title_zh: 一致性扩散语言模型
authors: "Hasan Amin, Yuan Gao, Yaser Souri, Subhojit Som, Ming Yin, Rajiv Khanna, Xia Song"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00161v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型作为自回归模型的替代方案
tldr: 针对离散扩散语言模型生成速度慢的问题，本文提出了一致性扩散语言模型（CDLM）。由于离散空间缺乏ODE，研究者利用随机后验桥作为替代，引入多路径离散一致性（MPDC）原则。CDLM是一个单阶段、无需教师模型的训练框架，统一了掩码扩散与一致性模型。实验表明，CDLM在文本生成任务中达到了SOTA，尤其在少步采样下表现优异，显著提升了离散生成模型的效率与质量。
selection_source: fresh_fetch
motivation: 离散扩散模型生成高质量样本需要数百步迭代，且缺乏类似连续域的概率流ODE来实现高效的一致性训练。
method: 提出多路径离散一致性原则，利用随机后验桥训练去噪器实现路径不变性，构建单阶段、无需教师模型的CDLM框架。
result: CDLM在有条件和无条件文本生成任务上均刷新了SOTA，在少步采样场景下相比强基线模型优势尤为显著。
conclusion: CDLM为快速、高保真的离散生成建模提供了原则性且可扩展的基础，成功将一致性训练扩展到了离散扩散领域。
---

## 摘要
扩散语言模型 (DLM) 是自回归模型的一种极具吸引力的替代方案，因为它们有望实现亚线性时间的并行生成，但由于高质量样本仍需数百个细化步骤，实际收益仍然难以捉摸。在连续域中，沿概率流 ODE 进行一致性训练是加速扩散的一种流行方案。对于离散扩散，不存在类似的样本空间 ODE，这使得直接适配变得定义不清。我们认为，自然的离散替代方案不是确定性轨迹，而是其随机对应物：精确后验桥接 (exact posterior bridge)，它在包括掩码扩散和均匀扩散在内的广泛损坏族中具有闭式解。基于这一观察，我们引入了多路径离散一致性 (MPDC)，这是一种新的原则，旨在训练去噪器在这些随机桥接的期望上保持路径不变性，并将其实例化为一致性扩散语言模型 (CDLM)，这是一个单阶段、无需教师模型的训练框架。单个 CDLM 目标将掩码扩散、连续一致性模型以及渐进式/离散蒸馏统一为一个共同视角的解析极限或经验近似。实验表明，CDLM 在条件和无条件文本生成方面均达到了新的 SOTA 水平，在各种采样预算下始终优于强大的基础离散扩散模型，甚至通常优于多阶段蒸馏基准，且在少步采样机制下提升最为显著。总之，这些结果使 CDLM 成为下一代快速、高保真离散生成建模的有原则且可扩展的基础。

## Abstract
Diffusion language models (DLMs) are an attractive alternative to autoregressive models because they promise sublinear-time, parallel generation, yet practical gains remain elusive as high-quality samples still demand hundreds of refinement steps. In continuous domains, consistency training along the probability-flow ODE is a popular recipe to accelerate diffusion. For discrete diffusion, no analogous sample-space ODE exists, making direct adaptation ill-defined. We argue that the natural discrete substitute is not a deterministic trajectory but its stochastic counterpart: the exact posterior bridge, available in closed form for broad corruption families including masked and uniform diffusion. Building on this observation, we introduce Multi-Path Discrete Consistency (MPDC), a new principle that trains a denoiser to be path-invariant in expectation across these stochastic bridges, and instantiate it as the Consistent Diffusion Language Model (CDLM), a single-stage, teacher-free training framework. A single CDLM objective unifies masked diffusion, continuous consistency models, and progressive/discrete distillation as analytic limits or empirical approximations of one common view. Empirically, CDLM establishes a new state of the art on both conditional and unconditional text-generation, consistently outperforming strong base discrete diffusion models and often even multi-stage distilled baselines across sampling budgets, with the largest gains in the few-step regime. Together, these results position CDLM as a principled and scalable foundation for the next generation of fast, high-fidelity discrete generative modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Consistent Diffusion Language Models》（一致性扩散语言模型）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散语言模型（DLM）虽然具备并行生成的潜力，但通常需要数百步迭代才能产生高质量文本，导致推理效率低下。
*   **研究背景**：在连续域（如图像），“一致性模型”（Consistency Models）通过概率流 ODE 实现了一步或少步生成。然而，**离散空间不存在 ODE**，导致无法直接应用连续域的加速技术。
*   **整体含义**：本文旨在为离散扩散模型建立一套原则性的一致性训练框架，通过消除对多阶段蒸馏或教师模型的依赖，实现单阶段训练下的高效、高保真文本生成。

### 2. 方法论：核心思想与关键技术
*   **核心思想（MPDC）**：提出“多路径离散一致性”（Multi-Path Discrete Consistency）。既然离散空间没有确定性轨迹（ODE），就利用离散扩散中天然存在的**精确后验桥接（Exact Posterior Bridge）**作为随机替代方案。
*   **关键技术细节**：
    *   **路径不变性**：训练去噪器 $f_\theta$ 使其在期望上具有路径无关性。即：从噪声状态 $x_t$ 直接预测的结果，应等于先“跳”到中间状态 $x_s$ 再进行预测的期望值。
    *   **训练目标**：通过最小化 $f_\theta(x_t, t)$ 与目标网络（EMA 更新）在 $x_s$ 处预测值的差异（使用 JS 散度）来实施。
    *   **三大设计优化**：
        1.  **步长课程学习**：采样不同的时间步长 $\delta$，并使用 $1/\delta$ 进行路径长度归一化。
        2.  **最大步长锚点（Max-step Anchor）**：引入扩散正则化项，将模型预测与真实数据 $x_0$ 挂钩，防止模型陷入模式崩溃或均匀漂移。
        3.  **优化对称性**：使用指数移动平均（EMA）目标网络和 JS 散度来平衡模式寻求与模式覆盖。
*   **统一视角**：证明了掩码扩散（MDLM）、连续一致性模型、渐进式蒸馏等均可视为 CDLM 框架下的特例或解析极限。

### 3. 实验设计
*   **数据集**：
    *   **无条件生成**：OpenWebText（110M 参数规模）。
    *   **有条件生成（OOD 测试）**：Lambada、Wikitext-103、PTB。
*   **Benchmark 与对比方法**：
    *   **基础模型（从头训练）**：MDLM、DUO、UDLM。
    *   **蒸馏模型（多阶段）**：SDTT（自蒸馏）、DUO+DCD（一致性蒸馏）。
    *   **基准**：自回归模型（AR）。
*   **评估指标**：困惑度（PPL）、熵（Entropy，衡量多样性）、BLEU（衡量条件保持）、MAUVE。

### 4. 资源与算力
*   **模型规模**：110M 参数的 Transformer 架构。
*   **训练细节**：在 OpenWebText 上训练 150k 步，全局 Batch Size 为 2048。
*   **算力资源**：使用了 **NVIDIA A100 GPU** 集群，规模在 **64 到 256 块 GPU** 之间。
*   **训练时长**：文中未明确给出具体的小时数，但提到了 150k 步的训练量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了无条件生成、有条件生成、以及针对掩码（Masked）和均匀（Uniform）两种不同先验的实验。
*   **消融实验**：非常充分。分别针对步长调度器（Scheduler）、最大步长正则化权重（$\kappa_{ms}$）、距离度量函数（KL vs JSD）进行了详细对比。
*   **客观性与公平性**：所有基线模型均在相同的 110M 规模、相同数据集和步数下重新训练或评估，确保了对比的公正性。通过 FP32 和 FP64 两种采样精度进一步验证了鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：CDLM 作为单阶段模型，在少步采样（如 4-64 步）下不仅优于所有基础扩散模型，甚至超越了需要教师模型的多阶段蒸馏模型（如 SDTT）。
*   **效率提升**：相比自回归模型（AR），在保持相近 PPL 的情况下实现了 **16x-32x 的计算效率提升**（以 NFE 衡量）。
*   **多样性保持**：CDLM 在降低 PPL 的同时保持了较高的熵，避免了类似 DUO-DCD 常见的模式崩溃（生成重复字符）问题。

### 7. 优点：亮点总结
*   **理论优雅**：成功将一致性训练从连续 ODE 推广到离散随机桥接，填补了离散扩散加速的理论空白。
*   **无需教师**：打破了“高效生成必须依赖预训练教师模型蒸馏”的思维定式，简化了训练流程。
*   **通用性强**：不仅适用于掩码扩散，也适用于均匀扩散等多种离散损坏过程。

### 8. 不足与局限
*   **少步而非一步**：虽然在少步（4-8 步）表现优异，但在极端的“一步生成”上仍受限于离散空间的标记分解障碍（Token-factorization barrier），无法完全达到连续域一步生成的质量。
*   **算力门槛**：尽管是单阶段训练，但 2048 的大 Batch Size 和数百块 A100 的需求表明其训练成本依然较高。
*   **长文本挑战**：虽然在 1024 长度上表现良好，但在更长序列或更复杂逻辑推理任务上的表现尚待验证。

（完）
