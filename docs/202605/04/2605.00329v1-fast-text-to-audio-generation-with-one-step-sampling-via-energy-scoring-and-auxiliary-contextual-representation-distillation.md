---
title: Fast Text-to-Audio Generation with One-Step Sampling via Energy-Scoring and Auxiliary Contextual Representation Distillation
title_zh: 基于能量评分和辅助上下文表示蒸馏的单步采样快速文本到音频生成
authors: "Kuan-Po Huang, Bo-Ru Lu, Byeonggeun Kim, Mihee Lee, Zalan Fabian, Renard Korzeniowski, Qingming Tang, Greg Ver Steeg, Hung-yi Lee, Chieh-Chi Kao, Chao Wang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00329v1"
tags: ["query:ardm"]
score: 10.0
evidence: 带有扩散头的自回归模型以及从掩码自回归模型中蒸馏
tldr: 针对自回归扩散模型在文本生成音频任务中推理延迟高的问题，本文提出一种单步采样框架。该方法结合能量距离训练目标与表示级蒸馏技术，通过能量评分头将高斯噪声直接映射为音频潜变量，并利用掩码自回归模型的知识蒸馏保留强条件约束。实验证明，该方法在保持高质量音频的同时，推理速度比现有SOTA模型快8.5倍，显著缩小了单步生成与多步采样模型间的质量差距。
selection_source: fresh_fetch
motivation: 现有的自回归扩散音频生成模型因多步采样过程导致推理延迟高，难以满足实时应用需求。
method: 提出一种结合能量评分头单步采样与掩码自回归模型表示级蒸馏的训练框架，实现从噪声到音频潜变量的直接映射。
result: 在AudioCaps基准测试中，该方法在主客观指标上均优于现有单步生成模型，且推理速度比SOTA模型IMPACT快8.5倍。
conclusion: 结合能量距离训练与表示级蒸馏是实现快速、高质量文本到音频合成的有效方案。
---

## 摘要
带有扩散头的自回归（AR）模型近期在文本到音频生成方面取得了强劲性能，但其迭代解码和多步采样过程带来了高延迟问题。为解决这一瓶颈，我们提出了一种单步采样框架，该框架结合了能量距离训练目标与表示级蒸馏。能量评分头通过单步操作将高斯噪声直接映射到音频潜变量，消除了昂贵的递归扩散采样过程，而来自掩码自回归（MAR）文本到音频模型的蒸馏则保留了扩散训练期间学习到的强条件约束。在 AudioCaps 基准测试中，我们的方法在客观和主观指标上均持续优于先前的单步基准模型（如 ConsistencyTTA、SoundCTM、AudioLCM 和 AudioTurbo），同时大幅缩小了与多步采样 AR 扩散系统之间的质量差距。与最先进的 AR 扩散系统 IMPACT 相比，我们的方法在保持极具竞争力的音频质量的同时，实现了高达 8.5 倍的批量推理加速。这些结果表明，将能量距离训练与表示级蒸馏相结合，为快速、高质量的文本到音频合成提供了一种有效的方案。

## Abstract
Autoregressive (AR) models with diffusion heads have recently achieved strong text-to-audio performance, yet their iterative decoding and multi-step sampling process introduce high-latency issues. To address this bottleneck, we propose a one-step sampling framework that combines an energy-distance training objective with representation-level distillation. An energy-scoring head maps Gaussian noise directly to audio latents in one step, eliminating the need for a costly recursive diffusion sampling process, while distillation from a masked autoregressive (MAR) text-to-audio model preserves the strong conditioning learned during diffusion training. On the AudioCaps benchmark, our method consistently outperforms prior one-step baselines such as ConsistencyTTA, SoundCTM, AudioLCM and AudioTurbo, on both objective and subjective metrics, while substantially narrowing the quality gap to AR diffusion systems with multi-step sampling. Compared to the state-of-the-art AR diffusion system, IMPACT, our approach achieves up to $8.5$x faster batch inference with highly competitive audio quality. These results demonstrate that combining energy-distance training with representation-level distillation provides an effective recipe for fast, high-quality text-to-audio synthesis.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast Text-to-Audio Generation with One-Step Sampling via Energy-Scoring and Auxiliary Contextual Representation Distillation》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
现有的文本到音频（TTA）生成模型主要采用**自回归（AR）架构结合扩散（Diffusion）或流匹配（Flow Matching）头**。虽然这种组合能产生高质量音频，但面临严重的**推理延迟**问题：
*   **双重迭代瓶颈**：生成过程既需要自回归的 $r$ 次迭代解码，每一步又需要扩散模型的 $n$ 步采样，总步数为 $r \times n$。
*   **单步采样质量差**：现有的加速技术（如一致性模型）在单步采样（$n=1$）时，音频质量和语义一致性往往大幅下降。
*   **研究目标**：开发一种能够实现**单步采样**且保持高保真度、强语义相关性的快速 TTA 框架。

### 2. 方法论
论文提出了 **AUDIO DEAR**（Distillation-enhanced Energy-scoring Auto Regressive）框架，核心技术包括：

*   **能量评分头（Energy-Scoring Head）**：
    *   放弃了传统的扩散/流匹配递归采样，改用基于**能量距离（Energy-distance）**的训练目标。
    *   **核心公式**：最小化生成分布 $P$ 与真实分布 $Q$ 之间的能量距离：
        $L_{energy} = \|x_1 - y\| + \|x_2 - y\| - \|x_1 - x_2\|$
        其中 $x_1, x_2$ 是模型从不同噪声中生成的样本，$y$ 是真实潜变量。该目标强制模型将高斯噪声直接映射到目标分布，同时保持生成样本的多样性。
*   **辅助上下文表示蒸馏（Auxiliary Contextual Representation Distillation）**：
    *   使用预训练的 SOTA 扩散模型（如 IMPACT）作为**教师模型**。
    *   **技术细节**：在训练过程中，通过 MSE 损失对齐学生模型与教师模型在 Transformer 最后一层的隐藏表示（Hidden Representations）。这使得单步模型能够继承扩散模型在多步训练中学习到的强语义条件约束能力。
*   **表示级分类器自由引导（Representation-level CFG）**：
    *   在 Transformer 的表示层进行条件与无条件的线性插值，避免了在单步采样头进行多次前向计算，进一步降低延迟。

### 3. 实验设计
*   **数据集**：
    *   混合训练集：AudioCaps、WavCaps 及部分 AudioSet，总计约 **1700 小时**。
    *   评估集：AudioCaps 官方测试集（964 个样本）。
*   **基准（Benchmark）与对比方法**：
    *   **少步/单步模型**：ConsistencyTTA、SoundCTM、AudioLCM、AudioTurbo。
    *   **多步 SOTA 模型**：Tango 2、TangoFlux、EzAudio、AudioLDM 2、IMPACT。
    *   **评估指标**：客观指标（FD, FAD, KL, IS, CLAP score）及主观指标（REL 文本相关性, OVL 整体质量）。

### 4. 资源与算力
*   **硬件**：推理延迟测试在 **Nvidia V100 (32GB VRAM)** GPU 上完成。
*   **训练细节**：Batch size 为 2048，学习率为 1e-3。
*   **算力说明**：文中未明确给出总训练时长（如多少 GPU-days），但提到了教师模型 IMPACT 的参数量（193M）和学生模型的参数量（191M），属于中等规模的学术/工业模型。

### 5. 实验数量与充分性
论文进行了多维度的实验，论证较为充分：
*   **系统级对比**：在 AudioCaps 上与近 10 种主流模型进行了全指标对比。
*   **消融实验**：
    *   **蒸馏权重 $\lambda$**：验证了蒸馏对提升质量的关键作用。
    *   **CFG 尺度**：寻找单步采样下的最佳引导强度。
    *   **采样数 $m$**：探讨了训练时使用多少个噪声样本来估计能量距离效果最好（结论是 $m=2$ 平衡性最佳）。
    *   **采样方法对比**：对比了 Shortcut 和 MeanFlow 等其他单步方案，证明了能量评分的优越性。
*   **主观评价**：引入了人类听感评分，增强了结论的可信度。

### 6. 主要结论与发现
*   **速度优势**：AUDIO DEAR 实现了单步采样，在批量推理时比 SOTA 扩散模型 IMPACT 快 **8.5 倍**。
*   **质量突破**：在单步采样模型中，AUDIO DEAR 在 FD、KL、CLAP 等指标上均取得第一，显著缩小了与多步采样模型（如 100 步扩散）之间的质量差距。
*   **蒸馏的必要性**：单纯使用能量评分效果有限，结合表示级蒸馏后，模型在语义对齐（CLAP 分数）上有质的飞跃。

### 7. 优点
*   **创新性**：首次将能量距离（Energy-distance）目标引入 TTA 领域，解决了扩散模型必须递归采样的痛点。
*   **高效性**：通过表示层 CFG 和单步采样，极大地提升了生成效率，使其更接近实时应用要求。
*   **理论支撑**：附录中提供了详尽的能量距离数学证明和 Toy Example 可视化，解释了为何该方法比 MeanFlow 等方法分布覆盖更广。

### 8. 不足与局限
*   **AR 迭代依然存在**：虽然采样头是单步的，但模型仍需进行 64 次自回归迭代解码，这在处理极长音频时仍有计算开销。
*   **指标微弱差距**：尽管缩小了差距，但在 FAD（音频保真度）和 IS（多样性）上，单步模型仍略逊于最顶尖的多步扩散模型（如 IMPACT 的 100 步模式）。
*   **应用限制**：目前主要在 10 秒长度的音频上验证，对于更长、更复杂的音频场景（如长达数分钟的音效合成）表现未知。

（完）
