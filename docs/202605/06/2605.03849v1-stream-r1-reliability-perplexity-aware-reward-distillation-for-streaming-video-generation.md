---
title: "Stream-R1: Reliability-Perplexity Aware Reward Distillation for Streaming Video Generation"
title_zh: Stream-R1：面向流式视频生成的可靠性-困惑度感知奖励蒸馏
authors: "Bin Wu, Mengqi Huang, Shaojin Wu, Weinan Jia, Yuxin Wang, Zhendong Mao, Yongdong Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03849v1"
tags: ["query:ardm"]
score: 9.0
evidence: 自回归流式视频扩散模型的蒸馏
tldr: Stream-R1 针对流式视频生成中的蒸馏加速问题，提出了一种可靠性与困惑度感知的奖励蒸馏框架。该方法通过预训练视频奖励模型，在样本层面（样本间可靠性）和时空元素层面（样本内困惑度）动态调整蒸馏权重，使模型专注于高质量样本及关键时空区域的优化。在不增加推理成本的前提下，显著提升了视频的视觉质量、运动一致性和文本对齐度。
selection_source: fresh_fetch
motivation: 现有的蒸馏方法无差别地对待所有教师输出，忽略了不同样本及同一视频内不同时空区域在监督可靠性和优化价值上的差异。
method: 提出 Stream-R1 框架，利用奖励模型在样本间按可靠性加权，并在样本内通过梯度显著性识别关键时空区域进行重点优化。
result: 在标准流式视频生成基准上，该方法在视觉质量、运动质量和文本对齐三个维度上均优于现有的蒸馏基准。
conclusion: 通过引入奖励引导的自适应加权机制，Stream-R1 有效突破了传统蒸馏方法的性能瓶颈，实现了更高效、更高质量的视频生成加速。
---

## 摘要
基于蒸馏的加速已成为使自回归流式视频扩散模型走向实用的基础，其中分布匹配蒸馏（DMD）是事实上的首选。然而，现有方法不加区分地训练学生模型以匹配教师模型的输出，将每一次展开（rollout）、每一帧和每一个像素都视为同等可靠的监督信号。我们认为这限制了蒸馏质量，因为它忽略了 DMD 监督中两个互补的方差维度：不同学生展开之间监督可靠性存在差异的“间可靠性”（Inter-Reliability），以及空间区域和时间帧对质量提升贡献不均的“内困惑度”（Intra-Perplexity）。因此，该目标在统一权重下混淆了两个问题：是否从每个展开中学习，以及在展开内部应将优化集中在何处。为了解决这一问题，我们提出了 Stream-R1，这是一个可靠性-困惑度感知的奖励蒸馏框架，通过单一的共享奖励引导机制，在展开和时空元素两个层面上自适应地重新加权蒸馏目标。在间可靠性层面，Stream-R1 通过预训练视频奖励分数的指数函数对每个展开的损失进行缩放，使得具有可靠监督的展开在优化中占据主导地位。在内困惑度层面，它对同一个奖励模型进行反向传播以提取逐像素的梯度显著性，并将其分解为空间和时间权重，从而将优化压力集中在细化能产生最大预期收益的区域和帧上。一种自适应平衡机制防止了视觉质量、运动质量和文本对齐中任何单一质量维度占据主导。在标准流式视频生成基准测试中，Stream-R1 在所有三个维度上均优于蒸馏基准模型，且无需修改架构或增加额外的推理成本。

## Abstract
Distillation-based acceleration has become foundational for making autoregressive streaming video diffusion models practical, with distribution matching distillation (DMD) as the de facto choice. Existing methods, however, train the student to match the teacher's output indiscriminately, treating every rollout, frame, and pixel as equally reliable supervision. We argue that this caps distilled quality, since it overlooks two complementary axes of variance in DMD supervision: Inter-Reliability across student rollouts whose supervision varies in reliability, and Intra-Perplexity across spatial regions and temporal frames that contribute unequally to where quality can still be improved. The objective thus conflates two questions under a uniform weight: whether to learn from each rollout, and where to concentrate optimization within it. To address this, we propose Stream-R1, a Reliability-Perplexity Aware Reward Distillation framework that adaptively reweights the distillation objective at both rollout and spatiotemporal-element levels through a single shared reward-guided mechanism. At the Inter-Reliability level, Stream-R1 rescales each rollout's loss by an exponential of a pretrained video reward score, so that rollouts with reliable supervision dominate optimization. At the Intra-Perplexity level, it back-propagates the same reward model to extract per-pixel gradient saliency, which is factored into spatial and temporal weights that concentrate optimization pressure on regions and frames where refinement yields the largest expected gain. An adaptive balancing mechanism prevents any single quality axis from dominating across visual quality, motion quality, and text alignment. Stream-R1 attains consistent improvements on all three dimensions over distillation baselines on standard streaming video generation benchmarks, without architectural modification or additional inference cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stream-R1: Reliability-Perplexity Aware Reward Distillation for Streaming Video Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的流式视频生成模型主要依赖**分布匹配蒸馏（DMD）**来加速推理，但现有的蒸馏方法存在“无差别对待”的问题。它们将每一个生成的视频样本（rollout）、每一帧、甚至每一个像素都视为同等可靠的监督信号。
*   **研究动机**：作者指出 DMD 监督信号中存在两个维度的方差：
    1.  **样本间可靠性（Inter-Reliability）**：教师模型的修正信号并非总是指向高质量区域，当学生模型偏离过远时，监督信号可能失效。
    2.  **样本内困惑度（Intra-Perplexity）**：视频内部不同区域对质量提升的潜力不同，均匀分配优化权重会导致在已达标区域浪费算力，而在缺陷区域监督不足。
*   **整体含义**：Stream-R1 旨在通过引入奖励引导的自适应加权机制，让蒸馏过程学会“在何处重点优化”以及“是否值得学习该样本”。

### 2. 方法论：核心思想与关键技术
Stream-R1 提出了一个统一的奖励引导框架，在不改变模型架构的前提下，通过以下技术实现自适应蒸馏：
*   **样本间可靠性加权（Inter-Reliability Weighting）**：
    *   利用预训练视频奖励模型对学生生成的视频打分。
    *   使用指数函数 $W_{inter} = \exp(\beta \cdot r_{final})$ 对损失函数进行缩放，使高奖励（即监督信号更可靠）的样本在梯度更新中占据主导。
*   **样本内困惑度感知（Intra-Perplexity Saliency）**：
    *   **梯度显著性提取**：通过对奖励模型进行反向传播，计算奖励分数对输入像素的梯度绝对值。梯度大的区域代表奖励模型最敏感、最需改进的“高困惑度”区域。
    *   **时空分解（Spatiotemporal Decomposition）**：将显著性图分解为空间权重（关注帧内关键物体/细节）和时间权重（关注运动剧烈或质量差的帧），并进行归一化处理。
*   **平衡多维奖励机制**：
    *   同时考虑视觉质量（VQ）、运动质量（MQ）和文本对齐（TA）。
    *   引入**自适应组合**（根据各维度得分动态分配权重）和**平衡惩罚项**（防止模型过度优化某一维度而忽略其他维度）。
*   **最终目标函数**：将上述权重整合进 DMD 损失中：$L_{Stream-R1} = W_{inter} \cdot (W_{intra} \odot L_{DMD})$。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练**：使用 VidProM 数据集（经 LLM 重写增强）。
    *   **评测**：VBench（短视频基准，946 个提示词）、MovieGen Video Bench（长视频基准，128 个提示词）。
*   **Benchmark 与对比方法**：
    *   **对比对象**：包括扩散模型（LTX-Video, Wan2.1）、流式/自回归模型（SkyReels-V2, CausVid, Self Forcing, LongLive）以及最新的奖励引导蒸馏方法（Reward Forcing）。
    *   **评估指标**：VBench 总分、视觉质量、语义对齐、长视频漂移率（Drift）、VLM（Qwen3-VL）评分及人工偏好投票。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置**：单卡 Batch Size 为 1，梯度累积 8 步，有效 Batch Size 为 64。
*   **时长**：总训练时间约为 **56 小时**。
*   **模型规模**：学生模型为 Wan2.1-T2V-1.3B，教师模型为 Wan2.1-T2V-14B。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 5 秒短视频到 180 秒超长视频的生成测试。
*   **消融实验**：详细测试了空间奖励、时间分解、多维度平衡等组件的贡献，验证了各模块的有效性。
*   **充分性与客观性**：
    *   使用了 5 个不同随机种子进行采样以减少偶然性。
    *   引入了第三方大模型（VLM）评估和双盲人工评测，确保了结果的客观性。
    *   可视化分析（如注入高斯模糊测试权重响应）直观证明了方法论的合理性。

### 6. 主要结论与发现
*   **性能超越教师**：Stream-R1 作为 4 步生成的蒸馏模型，在 VBench 总分和语义对齐上**超越了其 14B 规模的教师模型**（Wan2.1），且推理速度提升了约 30 倍。
*   **长视频稳定性**：在长达 180 秒的生成中，Stream-R1 的质量衰减（漂移）显著低于其他方法，证明了时空加权对维持长期一致性的作用。
*   **多维平衡**：通过自适应机制，模型在提升视觉美感的同时，并未牺牲运动连贯性或文本对齐度。

### 7. 优点
*   **零推理成本**：所有奖励引导和权重计算均在训练阶段完成，推理时与普通蒸馏模型完全一致。
*   **精细化控制**：将全局标量奖励转化为局部时空引导，解决了蒸馏中“眉毛胡子一把抓”的低效问题。
*   **通用性强**：该原理（将奖励梯度转化为时空权重）可推广至图像生成、3D 生成等其他模态。

### 8. 不足与局限
*   **奖励模型依赖**：效果高度依赖于预训练视频奖励模型的准确性。如果奖励模型本身存在偏见或无法识别某些伪影，蒸馏效果会受限。
*   **训练开销**：虽然推理无成本，但训练时需要对奖励模型进行额外的反向传播，增加了训练阶段的显存占用和计算耗时。
*   **极端场景**：对于极高动态或极复杂语义的视频，单一奖励模型可能难以提供足够精细的梯度引导。

（完）
