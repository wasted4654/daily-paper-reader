---
title: "ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space"
title_zh: ABC：通过连续时空中的非马尔可夫扩散桥实现任意子集自回归
authors: "Gabe Guo, Thanawat Sornwanee, Lutong Hao, Elon Litman, Stefano Ermon, Jose Blanchet"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27443v2"
tags: ["query:ardm"]
score: 9.0
evidence: 通过非马尔可夫扩散桥实现的任意子集自回归模型
tldr: 本研究针对连续时间与空间随机过程（如视频、天气预报）的生成挑战，提出ABC模型。该模型利用非马尔可夫扩散桥，在连续时空中实现任意子集自回归。通过单一连续SDE建模，使生成过程与物理时间同步，解决了传统扩散模型在物理结构捕捉、噪声注入及任意观测条件下的局限性，显著提升了生成质量和物理一致性。
selection_source: fresh_fetch
motivation: 传统扩散模型在处理连续物理过程时，难以捕捉物理时间结构，且无法灵活处理任意子集的条件观测。
method: 提出一种基于非马尔可夫扩散桥的连续SDE建模方法，通过路径空间测度变换实现路径依赖的条件生成，并扩展了去噪得分匹配算法。
result: 在视频生成和天气预报等多个领域的实验表明，ABC模型在生成质量和物理一致性上均优于现有竞争方法。
conclusion: ABC模型通过将生成动力学与物理时间对齐，为连续时空随机过程的建模和任意条件生成提供了一种高效且物理上更合理的新框架。
---

## 摘要
基于部分观测（如首尾帧）生成连续时间、连续空间的随机过程（如视频、天气预报）是一项基础性挑战。现有方法（如扩散模型）存在关键局限性：(1) 从噪声到数据的演化过程无法捕捉物理时间上接近的状态之间的结构相似性，且在低步长机制下积分不稳定；(2) 注入的随机噪声对物理过程流逝的时间不敏感，导致动力学过程不正确；(3) 它们忽视了对任意状态子集（如不规则采样的时刻、未来观测值）的条件约束。我们提出了 ABC：通过连续时空中的非马尔可夫扩散桥实现的任意子集自回归模型。关键在于，我们使用一个连续的 SDE 对该过程进行建模，其时间变量和中间状态追踪真实的时间和过程状态。这具有可证明的优势：(1) 生成未来状态的起点是已经接近的前一状态，而非无信息的噪声；(2) 随机噪声的注入随物理时间的流逝而缩放，从而促进产生具有相似时间相邻状态的物理上合理的动力学。我们通过路径空间上的测度变换推导出 SDE 动力学，从而产生另一个优势：(3) 对状态历史和/或未来的任意子集进行路径依赖的条件约束。为了学习这些动力学，我们推导出了去噪得分匹配的一种路径和时间依赖的扩展。我们的实验表明，ABC 在包括视频生成和天气预报在内的多个领域均优于竞争方法。

## Abstract
Generating continuous-time, continuous-space stochastic processes (e.g., videos, weather forecasts) conditioned on partial observations (e.g., first and last frames) is a fundamental challenge. Existing approaches, (e.g., diffusion models), suffer from key limitations: (1) noise-to-data evolution fails to capture structural similarity between states close in physical time and has unstable integration in low-step regimes; (2) random noise injected is insensitive to the physical process's time elapsed, resulting in incorrect dynamics; (3) they overlook conditioning on arbitrary subsets of states (e.g., irregularly sampled timesteps, future observations). We propose ABC: Any-Subset Autoregressive Models via Non-Markovian Diffusion Bridges in Continuous Time and Space. Crucially, we model the process with one continual SDE whose time variable and intermediate states track the real time and process states. This has provable advantages: (1) the starting point for generating future states is the already-close previous state, rather than uninformative noise; (2) random noise injection scales with physical time elapsed, encouraging physically plausible dynamics with similar time-adjacent states. We derive SDE dynamics via changes-of-measure on path space, yielding another advantage: (3) path-dependent conditioning on arbitrary subsets of the state history and/or future. To learn these dynamics, we derive a path- and time-dependent extension of denoising score matching. Our experiments show ABC's superiority to competing methods on multiple domains, including video generation and weather forecasting.

---

## 论文详细总结（自动生成）

这是一份关于论文《ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**连续时间、连续空间随机过程**（如视频、天气、股价等）的生成问题。
*   **研究动机**：现有的生成模型（如扩散模型或流匹配）在处理此类数据时存在三大缺陷：
    1.  **缺乏状态连续性**：通常从无信息的噪声开始生成，忽略了物理时间上相邻状态之间的极高相似性，导致生成动力学在物理上不合理。
    2.  **波动率与物理时间脱节**：现有模型无论物理时间跨度多大，都使用固定的噪声计划（0到1的辅助时间），导致生成过程出现闪烁或轨迹错误。
    3.  **难以处理任意子集条件**：无法灵活处理不规则采样、非因果观测（如给定首尾帧填补中间）等复杂条件。
*   **核心目标**：建立一个统一的框架，使生成过程的 SDE（随机微分方程）时间与物理时间对齐，并支持对路径上任意观测子集的条件生成。

### 2. 方法论：核心思想与关键技术
ABC 模型的核心思想是**将生成动力学建模为物理时间轴上的单一连续 SDE**。
*   **核心思想**：利用**Doob h-transform（测度变换）**，将一个独立于数据的基过程（如 OU 过程）转换为一个其有限维分布与目标数据分布匹配的非马尔可夫过程。
*   **关键技术细节**：
    *   **连续 SDE 追踪**：SDE 的漂移项（Drift）由路径依赖的得分函数（Score Function）驱动。这意味着生成当前状态时，模型会考虑已观测或已生成的整个路径历史。
    *   **时间自适应波动率**：噪声注入的大小随物理时间间隔缩放。如果两个观测点物理时间接近，注入的噪声就小，从而保证状态的平滑演变。
    *   **路径依赖的得分匹配（DSM）**：推导了一种新的训练目标，将传统的去噪得分匹配扩展到非马尔可夫、时间依赖的场景。
    *   **任意子集自回归**：通过 Cross-Attention Transformer 架构，模型可以同时接收过去和未来的观测点作为输入，实现非因果的填补（Infilling）和预测。
    *   **工程技巧（Brownian Bridge Hack）**：为了强制模型精确命中给定的未来观测点，在推理时引入了一个辅助的布朗桥漂移项。

### 3. 实验设计
*   **数据集/场景**：
    1.  **视频生成**：CelebV-HQ（人脸视频）、Sky Timelapse（天空延时摄影）。
    2.  **天气预报**：SEVIR VIL（雷达回波序列）。
    3.  **玩具实验**：一维布朗桥模拟，用于验证路径测度的正确性。
*   **Benchmark 与对比方法**：
    *   **条件扩散桥（如 DDBM, PFI）**：代表了固定波动率、数据到数据的基线。
    *   **噪声到数据扩散（如 DDPM, SMLD）**：代表了传统的扩散模型基线。
*   **评估指标**：FVD（视频一致性）、FID（单帧质量）、MAE/RMSE（天气预测误差）、CSI（关键成功指数）。

### 4. 资源与算力
论文详细记录了实验配置：
*   **硬件**：主要使用 **NVIDIA A100 80GB GPU**。
*   **训练规模**：
    *   **基础模型**：202M 参数，使用 4 块 A100 训练约 36 小时（24万步）。
    *   **中型模型**：594M 参数，使用 8 块 A100 训练（最高达32万步）。
*   **推理**：测试了 250 步和 500 步的 SDE 离散化积分。

### 5. 实验数量与充分性
实验设计非常全面且具有客观性：
*   **多维度对比**：在视频任务中测试了不同的采样频率（16帧/32帧）和不同的提示间隔（每4/8/16帧给定一个观测）。
*   **消融实验**：
    1.  **条件策略消融**：对比了全路径条件、马尔可夫条件（仅首帧+当前帧）和随机采样条件的差异。
    2.  **超参数敏感性**：测试了不同波动率系数 $\sigma$ 对生成质量的影响。
    3.  **模型缩放**：验证了增加参数量和训练步数能持续提升性能。
*   **公平性**：所有对比方法使用相同的 Transformer 架构和训练设置，仅改变 SDE 动力学和损失函数，确保了方法论层面的公平比较。

### 6. 主要结论与发现
1.  **物理时间对齐至关重要**：ABC 通过让 SDE 时间对应物理时间，显著减少了视频生成的闪烁感，并在 FVD 指标上大幅领先。
2.  **数据到数据优于噪声到数据**：从前一状态开始演化比从纯噪声开始生成更稳定，尤其在低采样步数下表现更优。
3.  **非马尔可夫特性的优势**：利用全路径历史（而非仅前一帧）能更好地捕捉长程依赖和复杂的物理动力学。
4.  **任意子集能力的验证**：模型在非因果填补任务中表现强劲，能够根据未来帧的约束调整中间生成路径。

### 7. 优点
*   **理论严谨**：基于测度变换和随机分析推导，解决了扩散模型在连续时间建模上的理论缺失。
*   **高度灵活**：一个模型即可处理预测、填补、超分辨率等多种任务，且支持不规则的时间采样。
*   **物理一致性**：引入的时间自适应波动率是该领域的一个重要发现，解决了生成过程中的“能量不守恒”或动力学突变问题。

### 8. 不足与局限
*   **计算开销**：由于采用全路径注意力机制，随着序列长度增加，Transformer 的计算复杂度会呈平方级增长（虽然文中提到了可以使用 SSM 等架构缓解）。
*   **推理速度**：作为 SDE 模型，仍需要多步迭代采样，相比单步生成模型速度较慢。
*   **工程依赖**：为了精确命中未来观测点，仍需依赖“Brownian Bridge Hack”等工程手段，这在理论完美性上略有妥协。
*   **实验覆盖**：虽然涵盖了视频和天气，但尚未在更高维或更复杂的 3D 物理模拟数据上进行验证。

（完）
