---
title: "Orthrus: Memory-Efficient Parallel Token Generation via Dual-View Diffusion"
title_zh: Orthrus：通过双视图扩散实现显存高效的并行 Token 生成
authors: "Chien Van Nguyen, Chaitra Hegde, Van Cuong Pham, Ryan A. Rossi, Franck Dernoncourt, Thien Huu Nguyen"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12825v1"
tags: ["query:ardm"]
score: 10.0
evidence: 将自回归大语言模型与扩散模型的高速并行生成相结合
tldr: Orthrus 是一种结合自回归（AR）与扩散模型的双视图框架，旨在解决 LLM 推理的串行瓶颈。它在冻结的 LLM 上添加轻量级扩散模块并共享 KV 缓存，由 AR 负责预填充，扩散模块负责并行生成。通过共识机制，该框架在仅增加极少参数和 O(1) 内存开销的前提下，实现了高达 7.8 倍的无损加速。
selection_source: fresh_fetch
motivation: 针对自回归解码速度慢以及现有扩散模型训练成本高、性能差的问题，寻求一种高效且无损的并行生成方案。
method: 在冻结的 LLM 中引入轻量级扩散模块，通过共享 KV 缓存和双视图共识机制实现并行 Token 生成。
result: 实验表明 Orthrus 在保持推理无损的同时，实现了最高 7.8 倍的加速，且仅产生常数级的额外内存开销。
conclusion: Orthrus 证明了通过双视图架构可以有效统一自回归的保真度与扩散模型的生成速度，是提升大模型推理效率的有效途径。
---

## 摘要
我们介绍了 Orthrus，这是一个简单且高效的双架构框架，它将自回归大语言模型（LLM）的精确生成保真度与扩散模型的高速并行 Token 生成统一起来。标准自回归解码的顺序特性构成了高吞吐量推理的根本瓶颈。虽然扩散语言模型试图通过并行生成来打破这一障碍，但它们面临着显著的性能下降、高昂的训练成本以及缺乏严格收敛保证的问题。Orthrus 从原生层面解决了这种对立。该框架旨在无缝集成到现有的 Transformer 中，通过为冻结的 LLM 增加一个轻量级的可训练模块，在标准自回归视图之外创建了一个并行扩散视图。在这个统一的系统中，两个视图都访问完全相同的高保真键值（KV）缓存；自回归头执行上下文预填充以构建准确的 KV 表示，而扩散头则执行并行生成。通过在两个视图之间采用精确的共识机制，Orthrus 保证了无损推理，在仅有 O(1) 显存缓存开销和极少参数增加的情况下，实现了高达 7.8 倍的加速。

## Abstract
We introduce Orthrus, a simple and efficient dual-architecture framework that unifies the exact generation fidelity of autoregressive Large Language Models (LLMs) with the high-speed parallel token generation of diffusion models. The sequential nature of standard autoregressive decoding represents a fundamental bottleneck for high-throughput inference. While diffusion language models attempt to break this barrier via parallel generation, they suffer from significant performance degradation, high training costs, and a lack of rigorous convergence guarantees. Orthrus resolves this dichotomy natively. Designed to seamlessly integrate into existing Transformers, the framework augments a frozen LLM with a lightweight, trainable module to create a parallel diffusion view alongside the standard autoregressive view. In this unified system, both views attend to the exact same high-fidelity Key-Value (KV) cache; the autoregressive head executes context pre-filling to construct accurate KV representations, while the diffusion head executes parallel generation. By employing an exact consensus mechanism between the two views, Orthrus guarantees lossless inference, delivering up to a 7.8x speedup with only an O(1) memory cache overhead and minimal parameter additions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Orthrus** 的新型双架构框架，旨在解决大语言模型（LLM）推理中的串行瓶颈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：自回归（AR）模型虽然生成质量高，但其“逐个 Token 生成”的特性导致了严重的内存带宽瓶颈和高延迟。扩散语言模型（DLM）虽能并行生成，但通常存在性能下降（分布偏移）、训练成本极高且缺乏严谨收敛保证的问题。
*   **核心目标**：在不牺牲自回归模型精确预测分布（保真度）的前提下，实现高倍率的并行生成加速，并保持极低的内存开销。

### 2. 方法论
Orthrus 的核心思想是在一个统一的 Transformer 架构中融合两种视图：
*   **双视图架构（Dual-View Architecture）**：
    *   **自回归视图（AR View）**：使用预训练好的、冻结的 LLM 权重，负责预填充（Pre-filling）以构建高保真的键值（KV）缓存。
    *   **扩散视图（Diffusion View）**：在每个 Transformer 层中注入一个轻量级的、可训练的扩散注意力模块。
*   **共享 KV 缓存**：扩散头直接在自回归头生成的 KV 缓存上进行操作，实现了 **零冗余缓存开销**。
*   **训练流程（双路径块掩码）**：
    *   使用 **KL 散度蒸馏**：让扩散头学习预测被掩码的 Token 块，目标是最小化其预测分布与冻结 AR 老师模型分布之间的差异。
    *   仅训练约 16% 的参数（新增的扩散投影矩阵），主干网络保持冻结。
*   **推理流程（内模型共识机制）**：
    1.  **并行投影**：扩散头根据当前上下文一次性预测 $K$ 个候选 Token。
    2.  **分布匹配与验证**：将这 $K$ 个 Token 输入冻结的 AR 头进行单次并行验证。
    3.  **接受/拒绝采样**：通过拒绝采样机制确保最终输出与原始 AR 模型的贪婪预测或采样分布完全一致，从而实现 **无损（Lossless）加速**。

### 3. 实验设计
*   **基础模型**：选用 Qwen3 系列（1.7B、4B、8B 版本）作为基座。
*   **数据集/场景**：
    *   **数学推理**：GSM8K, MATH-500, AIME24, AIME25。
    *   **代码生成**：HumanEval, MBPP, Pseudo2code, LiveCodeBench-v5。
*   **Benchmark 与对比方法**：
    *   **对比 DLM 模型**：Dream, LLaDA, SDAR, Fast-dLLM-v2 等。
    *   **对比投机解码（Speculative Decoding）**：EAGLE-3, DFlash。
    *   **评估指标**：每个前向传播生成的有效 Token 数（TPF）、相对加速比、显存占用。

### 4. 资源与算力
*   **硬件**：使用了单个 **8×H200 GPU 节点**。
*   **训练时长**：在不到 **24 小时** 内完成训练。
*   **数据量**：使用了不到 **1B（10 亿）Token** 的数据（约 60 万个样本），采样自 Nemotron-Post-Training-Dataset-v2。
*   **参数量**：仅微调了总参数的 **16%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.7B 到 8B 的多个模型量级，并在 8 个极具挑战性的推理和代码基准测试上进行了验证。
*   **消融实验**：
    *   测试了不同并行块大小 $K$（从 4 到 32）对吞吐量和延迟的影响。
    *   对比了“硬标签（交叉熵）”与“软标签（KL 散度）”训练目标的差异。
    *   验证了单步预测与多步迭代预测的效率对比。
*   **充分性评价**：实验设计较为全面，不仅对比了同类扩散模型，还与最先进的投机解码方案进行了内存和接受率的对比，客观地展示了该方法在无损前提下的加速能力。

### 6. 主要结论与发现
*   **显著加速**：Orthrus 实现了高达 **7.8 倍** 的推理加速，在 8B 模型上的平均 TPF 达到 5.39。
*   **严格无损**：由于采用了内模型共识机制，其推理精度与原始 AR 基座模型完全一致，解决了以往扩散模型性能退化的问题。
*   **内存高效**：相比于需要维护两个独立 KV 缓存的投机解码，Orthrus 的 KV 缓存开销是 **O(1)** 常数级的（仅约 4.5 MiB 额外开销），且峰值显存增加小于 1%。

### 7. 优点
*   **架构优雅**：通过共享 KV 缓存巧妙地统一了 AR 和扩散模型，避免了投机解码中常见的模型间通信和冗余存储。
*   **即插即用**：可以作为现有强力 AR 模型的轻量级插件，无需对基座模型进行破坏性微调。
*   **训练高效**：相比于动辄需要数百亿 Token 预训练的扩散模型，Orthrus 仅需极少量数据和算力即可达到更优效果。

### 8. 不足与局限
*   **性能上限受限**：由于 AR 基座被冻结，Orthrus 的生成能力完全取决于基座模型，无法通过扩散训练超越基座本身的知识水平或纠正其幻觉。
*   **训练依赖**：虽然轻量，但仍需要针对特定基座模型进行一次性的蒸馏训练。
*   **上下文长度限制**：实验中主要测试了 2048 的序列长度，在超长文本（如 100k+）下的表现和内存稳定性尚需进一步验证。

（完）
