---
title: "BlockVLA: Accelerating Autoregressive VLA via Block Diffusion Finetuning"
title_zh: BlockVLA：通过块扩散微调加速自回归 VLA
authors: "Ruiheng Wang, Shuanghao Bai, Haoran Zhang, Badong Chen, Xiangyu Xu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13382v1"
tags: ["query:genad"]
score: 9.0
evidence: 将自回归VLA骨干网络适配为机器人扩散策略
tldr: 针对自回归视觉语言动作模型（VLA）推理延迟高和长程任务误差累积的问题，本文提出BlockVLA框架。该框架通过块扩散（Block Diffusion）范式将预训练自回归模型转换为高效的离散扩散策略，在保持块间因果一致性的同时实现块内并行去噪。BlockVLA支持前缀KV缓存复用，显著提升了推理速度和训练效率，在LIBERO等基准测试中实现了3.3倍的加速，并展现出更强的长程任务处理能力。
selection_source: fresh_fetch
motivation: 旨在解决自回归VLA模型推理慢、误差累积严重，以及离散扩散模型在机器人任务中计算开销大且难以利用KV缓存的挑战。
method: 提出块扩散范式，在块级别维持自回归依赖以保证全局一致性，在块内部进行并行去噪以提升效率，并支持前缀KV缓存复用。
result: 实验表明BlockVLA在推理速度上比标准离散扩散基线快3.3倍，且在复杂长程任务中具有更快的训练收敛速度和更高的成功率。
conclusion: BlockVLA通过结合自回归预训练与块扩散微调，为实现高效、高频的实时机器人控制提供了一种稳健且高性能的解决方案。
---

## 摘要
虽然自回归（AR）视觉-语言-动作（VLA）模型在机器人任务中展现了强大的推理能力，但其顺序解码过程通常会导致高推理延迟，并可能在长时程执行过程中放大误差累积。离散扩散语言模型（dLLMs）通过并行 Token 细化提供了一种极具前景的替代方案，但由于重复的去噪函数评估（NFEs）以及难以将标准 KV 缓存直接应用于双向迭代解码，其在机器人领域的实际部署仍受到限制。为了衔接这些范式，我们提出了 BlockVLA，这是一个通过块扩散范式将预训练的 AR 骨干网络适配为高效离散扩散策略的框架。BlockVLA 在块级别保持自回归依赖，同时在每个块内实现并行去噪，从而将全局因果一致性与局部并行生成相结合。这种设计实现了跨已完成块的前缀 KV 缓存复用，降低了迭代去噪的有效成本，并提供了从 AR 预训练到基于扩散的策略微调的更平滑过渡。我们在 LIBERO 和 SimplerEnv 基准测试上进行了广泛的评估。实验结果表明，与标准离散扩散基准相比，我们的 BlockVLA 实现了 3.3 倍的推理加速。此外，我们的模型表现出卓越的训练效率，成功率的收敛速度明显快于基准模型，这种优势在复杂的长时程任务中尤为突出，BlockVLA 在训练早期阶段就取得了显著的性能提升。这项工作确立了块扩散作为大规模预训练 AR 模型与高效、高频实时机器人控制之间的稳健桥梁。

## Abstract
While autoregressive (AR) Vision-Language-Action (VLA) models have demonstrated formidable reasoning capabilities in robotic tasks, their sequential decoding process often incurs high inference latency and may amplify error accumulation during long-horizon execution. Discrete Diffusion Language Models (dLLMs) provide a promising alternative through parallel token refinement, but their practical deployment in robotics remains limited by repeated denoising function evaluations (NFEs) and the difficulty of directly applying standard KV caching to bidirectional iterative decoding. To bridge these paradigms, we propose BlockVLA, a framework that adapts pretrained AR backbones into an efficient discrete diffusion policy through a block diffusion paradigm. BlockVLA maintains autoregressive dependencies at the block level while enabling parallel denoising within each block, thereby combining global causal coherence with local parallel generation. This design enables prefix KV-cache reuse across completed blocks, reduces the effective cost of iterative denoising, and provides a smoother transition from AR pretraining to diffusion-based policy fine-tuning. We conduct extensive evaluations on the LIBERO and SimplerEnv benchmarks. Experimental results demonstrate that our BlockVLA achieves a 3.3$\times$ inference acceleration over standard discrete diffusion baselines. Furthermore, our model exhibits superior training efficiency, with success rates converging substantially faster than baselines, a gain that is particularly pronounced in complex, long-horizon tasks, where BlockVLA achieves significant performance gains in the early stages of training. This work establishes Block Diffusion as a robust bridge between large-scale pretrained AR models and efficient, high-frequency real-time robotic control.

---

## 论文详细总结（自动生成）

这是一份关于论文《BlockVLA: Accelerating Autoregressive VLA via Block Diffusion Finetuning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前**视觉-语言-动作（VLA）模型**在机器人控制中的两大痛点：
*   **自回归（AR）模型的局限性**：如 OpenVLA 等模型采用逐 Token 生成，推理延迟高，且在长程任务中容易产生复合误差（Error Accumulation）。
*   **离散扩散模型（dLLM）的部署难题**：虽然支持并行生成，但通常需要大量的去噪步数（NFE），且由于其双向注意力机制，无法直接复用标准的 KV 缓存（KV Caching），导致实际推理速度受限。
*   **适配成本**：从头训练扩散模型成本极高。

**BlockVLA 的核心意义**在于提出了一种“块扩散”（Block Diffusion）范式，作为预训练 AR 模型与高效扩散策略之间的桥梁，兼顾了全局因果一致性与局部并行生成的效率。

### 2. 方法论：核心思想与关键技术
BlockVLA 将动作生成重构为**块级自回归 + 块内并行扩散**的混合模式：
*   **核心思想**：将动作序列划分为多个块（Blocks）。块与块之间遵循自回归顺序（前一个块作为后一个块的因果上下文），而块内部的 Token 则通过离散扩散过程并行生成。
*   **关键技术细节**：
    *   **非对称掩码策略**：针对 VLA 任务，将视觉-语言输入视为持久的前缀条件（Prefix），仅对动作 Token 进行掩码和去噪，确保生成过程严格基于多模态上下文。
    *   **KV 缓存复用**：由于块之间是自回归的，已完成去噪的块可以像 AR 模型一样存储 KV 状态，后续块只需在当前块内迭代，显著降低了计算开销。
    *   **扩散强迫（Diffusion Forcing）**：在训练时让模型基于带有噪声的历史块预测当前块，增强了模型对推理时预测误差的鲁棒性。
    *   **取消 Token 偏移（No Token Shift）**：实验发现，在扩散微调阶段取消 AR 常用的位置偏移能获得更好的性能。

### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：包含 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个子集，共 40 个任务。
    *   **SimplerEnv**：高保真真实到仿真（Real-to-Sim）基准，使用 WidowX 机器人进行抓取和放置任务。
*   **基准方法（Benchmarks）**：
    *   **OpenVLA**：代表性的自回归 VLA 基准。
    *   **DDVLA**：标准的离散扩散 VLA 基准（全序列双向扩散）。

### 4. 资源与算力
*   **训练配置**：论文提到在仅使用 **2 张 GPU** 的情况下，在 50k 训练步数内完成了微调。
*   **推理测试**：推理吞吐量测试是在单张 **NVIDIA RTX 4090 GPU** 上进行的。
*   **模型规模**：基于 Prismatic-7B 骨干网络（包含 SigLIP/DINOv2 视觉编码器和 Llama 语言模型）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 40 个 LIBERO 模拟任务和多个 SimplerEnv 任务，每项任务均进行了多次评估（如 SimplerEnv 每项评估 24 个回合）。
*   **消融实验**：非常充分。作者研究了**块大小（Block Size）**、**训练策略（Teacher vs. Diffusion Forcing）**、**去噪步数**以及 **Token 偏移**对性能的影响。
*   **客观性**：通过对比训练收敛曲线和推理吞吐量曲线，直观展示了该方法在效率和性能上的双重优势，实验设计符合当前具身智能领域的标准流程。

### 6. 主要结论与发现
*   **推理加速**：BlockVLA 实现了比标准离散扩散模型快 **3.3 倍**、比自回归 OpenVLA 快 **8.0 倍**的推理吞吐量（达到 186.7 tokens/s）。
*   **训练效率**：BlockVLA 的收敛速度显著快于基线，尤其在**长程任务（LIBERO-Long）**中，基线在 15k 步时几乎无法完成任务，而 BlockVLA 已达到 60% 以上的成功率。
*   **性能提升**：在 LIBERO 平均成功率上达到 91.7%，优于 DDVLA 的 83.2% 和 OpenVLA 的 76.5%。

### 7. 优点（亮点）
*   **架构优雅**：巧妙地解决了扩散模型与 KV 缓存不兼容的问题，使大模型在机器人高频控制中变得实用。
*   **平滑迁移**：提供了一种将现有大规模预训练 AR 模型（如 Llama 系列）转化为扩散策略的高效路径，无需从头训练。
*   **长程鲁棒性**：通过块级因果约束，有效缓解了纯扩散模型在极长序列上的建模压力。

### 8. 不足与局限
*   **离散化依赖**：该方法仍基于动作空间离散化（Tokenization），对于需要极高连续精度的微操任务，可能受限于 Bin 的划分精度。
*   **块大小权衡**：块大小的选择对性能影响较大，过大的块会降低 KV 缓存的增量收益，过小的块则削弱并行优势，需针对不同硬件手动调优。
*   **环境覆盖**：虽然在仿真中表现优异，但尚未在复杂的真实物理世界多机协作或非结构化环境中进行大规模验证。

（完）
