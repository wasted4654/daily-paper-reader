---
title: "AlphaGRPO: Unlocking Self-Reflective Multimodal Generation in UMMs via Decompositional Verifiable Reward"
title_zh: AlphaGRPO：通过分解式可验证奖励解锁统一多模态模型（UMMs）中的自反思多模态生成能力
authors: "Runhui Huang, Jie Wu, Rui Yang, Zhe Liu, Hengshuang Zhao"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12495v1"
tags: ["query:ardm"]
score: 9.0
evidence: 将策略优化应用于自回归扩散统一多模态模型
tldr: AlphaGRPO 是一种将 GRPO 算法应用于 AR-Diffusion 统一多模态模型（UMMs）的新框架，旨在提升多模态生成能力。它通过引入分解式可验证奖励（DVReward），利用 LLM 将复杂指令拆解为原子化问题并由 MLLM 评分，从而解锁了模型的推理生成和自我反思修正能力。实验证明该方法在多个生成和编辑基准测试中均取得了显著提升。
selection_source: fresh_fetch
motivation: 旨在解决多模态生成模型在理解复杂意图、自我纠错以及缺乏稳定监督反馈机制方面的挑战。
method: 提出 AlphaGRPO 框架，利用 DVReward 将复杂请求分解为可验证的原子语义问题，并通过 GRPO 算法进行强化学习。
result: 在 GenEval、TIIF-Bench 等多个生成基准测试中取得显著进步，并在图像编辑任务中展现出强大的泛化能力。
conclusion: 该研究验证了通过自我反思强化学习可以有效激发统一多模态模型的内在潜力，实现更高质量的生成与精细化控制。
---

## 摘要
在本文中，我们提出了 AlphaGRPO，这是一个将组相对策略优化（GRPO）应用于自回归扩散（AR-Diffusion）统一多模态模型（UMMs）的新型框架，旨在无需额外冷启动阶段的情况下增强多模态生成能力。我们的方法解锁了模型执行高级推理任务的内在潜力：推理式文本到图像生成（Reasoning Text-to-Image Generation），即模型主动推断用户的隐含意图；以及自反思细化（Self-Reflective Refinement），即模型自主诊断并纠正生成输出中的不一致。为了解决为现实世界多模态生成提供稳定监督的挑战，我们引入了分解式可验证奖励（DVReward）。与整体标量奖励不同，DVReward 利用大语言模型（LLM）将复杂的用户请求分解为原子级、可验证的语义和质量问题，随后由通用多模态大语言模型（MLLM）进行评估，以提供可靠且可解释的反馈。大量实验表明，AlphaGRPO 在包括 GenEval、TIIF-Bench、DPG-Bench 和 WISE 在内的多模态生成基准测试中取得了稳健的改进，同时在未针对编辑任务进行训练的情况下，在 GEdit 的编辑任务中也获得了显著提升。这些结果验证了我们的自反思强化学习方法能够有效利用内在理解来引导高保真生成。项目主页：https://huangrh99.github.io/AlphaGRPO/

## Abstract
In this paper, we propose AlphaGRPO, a novel framework that applies Group Relative Policy Optimization (GRPO) to AR-Diffusion Unified Multimodal Models (UMMs) to enhance multimodal generation capabilities without an additional cold-start stage. Our approach unlocks the model's intrinsic potential to perform advanced reasoning tasks: Reasoning Text-to-Image Generation, where the model actively infers implicit user intents, and Self-Reflective Refinement, where it autonomously diagnoses and corrects misalignments in generated outputs. To address the challenge of providing stable supervision for real-world multimodal generation, we introduce the Decompositional Verifiable Reward (DVReward). Unlike holistic scalar rewards, DVReward utilizes an LLM to decompose complex user requests into atomic, verifiable semantic and quality questions, which are then evaluated by a general MLLM to provide reliable and interpretable feedback. Extensive experiments demonstrate that AlphaGRPO yields robust improvements across multimodal generation benchmarks, including GenEval, TIIF-Bench, DPG-Bench and WISE, while also achieving significant gains in editing tasks on GEdit without training on editing tasks. These results validate that our self-reflective reinforcement approach effectively leverages inherent understanding to guide high-fidelity generation. Project page: https://huangrh99.github.io/AlphaGRPO/

---

## 论文详细总结（自动生成）

这是一份关于论文《AlphaGRPO: Unlocking Self-Reflective Multimodal Generation in UMMs via Decompositional Verifiable Reward》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决**统一多模态模型（UMMs）**在图像生成任务中面临的两个核心挑战：
*   **内在潜力未被激活**：现有的 UMMs 虽然具备理解和生成的双重能力，但往往需要依赖外部强模型（如 GPT-4o）生成的合成数据进行“冷启动”微调（SFT），这实际上是知识蒸馏而非模型自身能力的进化。
*   **奖励信号不稳定**：传统的强化学习（RL）在视觉生成中通常使用整体标量评分（如 VIEScore），这种“黑盒”评分缺乏判别力且容易导致模型过拟合到特定的指标，难以处理复杂的现实指令。

**AlphaGRPO** 的核心意义在于：通过强化学习（GRPO）直接激活 UMMs 内部潜藏的推理和自反思能力，无需冷启动阶段，并引入了一种更细粒度、可解释的奖励机制。

### 2. 方法论
AlphaGRPO 框架包含以下关键技术：
*   **统一轨迹建模（Unified Trajectory）**：将多模态生成视为一个连续过程，轨迹 $\tau$ 包含“自回归推理文本 $y$”和“扩散生成图像 $z$”。通过 GRPO 同时优化文本推理（逻辑规划/错误诊断）和图像生成（视觉对齐）。
*   **分解式可验证奖励（DVReward）**：
    *   **分解**：利用 LLM 将复杂的用户指令拆解为多个原子级的、可验证的问题（涵盖语义对齐和视觉质量，如“杯子里是否有蒸汽？”）。
    *   **验证**：使用通用 MLLM（如 Qwen3-VL）作为验证器，通过计算“Yes”标记的概率对每个原子问题进行置信度评分。
    *   **聚合**：最终奖励是语义分数和质量分数的几何平均值，提供稳定且可解释的反馈。
*   **误报纠正（False-Positive Rectification, FPR）**：在自反思任务中，如果细化后的图像奖励不高于初始图像，则赋予该轨迹最低奖励，防止模型学习到退化的修正策略。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：采用“从基元到提示词”的策略，合成 19,500 个涵盖空间推理、计数、属性绑定等 39 类任务的提示词。
    *   **测试基准**：文本到图像生成（GenEval, TIIF-Bench, DPG-Bench, WISE）；图像编辑（GEdit-Bench）。
*   **对比方法**：
    *   **生成专用模型**：SD3 Medium, FLUX.1 dev。
    *   **统一多模态模型**：Show-o, JanusPro, 以及基座模型 BAGEL。
    *   **奖励模型对比**：PickScore, VIEScore, HPSv3, UnifiedReward。

### 4. 资源与算力
*   **硬件**：使用了 **64 张 NVIDIA A100 GPU**。
*   **训练细节**：在每个 8-GPU 节点中，7 张用于训练，1 张用于部署奖励模型（Qwen3-VL-30B）。
*   **优化**：采用 LoRA 微调，训练 380 个步骤。通过异步调度和去中心化奖励服务，将奖励反馈的等待时间从 40.8 秒优化至近乎忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流下游基准测试上进行了验证，并进行了大量的消融实验。
*   **消融实验内容**：包括 DVReward 的有效性、置信度评分 vs. 二值评分、FPR 的影响、问题数量的影响、以及 KL 系数的调节。
*   **充分性评价**：实验设计非常充分且客观。作者不仅在生成任务上测试，还验证了模型在未训练过的编辑任务上的**零样本泛化能力**，并引入了人工评估（Human Evaluation）来验证 DVReward 与人类偏好的一致性。

### 6. 主要结论与发现
*   **自反思能力是关键**：通过显式的“错误寻找”模式（Reflect Mode），UMMs 能打破确认偏差，准确识别并修正生成图像中的细微错误。
*   **AlphaGRPO 效果显著**：在 TIIF-Bench 上，AlphaGRPO 配合推理时自反思（Inf. SRR）比基座模型 BAGEL 提升了 5.8%。
*   **泛化性强**：在推理式 T2I 任务上训练的模型，在图像编辑任务（GEdit）上也取得了显著提升（+0.52 分），证明了模型学到的是通用的对齐能力。
*   **DVReward 优于标量奖励**：相比直接打分，通过回答原子问题获取的奖励信号更具判别力，能有效引导模型优化。

### 7. 优点
*   **无需冷启动**：证明了通过 RL 可以直接从预训练模型中“榨取”推理能力，减少了对高质量人工标注或强模型蒸馏数据的依赖。
*   **奖励机制创新**：DVReward 解决了 MLLM 作为奖励模型时输出不稳定、判别力差的问题，且具有极高的可解释性。
*   **端到端优化**：实现了文本推理链和图像扩散过程的联合优化。

### 8. 不足与局限
*   **基座模型限制**：受限于基座模型 BAGEL，在 512 分辨率下偶尔会出现噪声或模糊伪影。
*   **过程奖励缺失**：目前的奖励主要基于最终结果（Outcome Reward），尚未深入探索对推理中间步骤（Process Reward）的监督。
*   **计算开销**：虽然经过优化，但 DVReward 在推理时需要多次调用 MLLM，对实时性要求极高的场景可能存在压力。

（完）
