---
title: "BitLM: Unlocking Multi-Token Language Generation with Bitwise Continuous Diffusion"
title_zh: BitLM：通过位级连续扩散开启多标记语言生成
authors: "Shaobin Zhuang, Yuang Ai, Jiaming Han, Xiaohui Li, Huaibo Huang, Xiangyu Yue, Xuefeng Hu, Kun Xu, Yali Wang, Hao Chen"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11577v1"
tags: ["query:ardm"]
score: 9.0
evidence: 结合自回归结构与位级连续扩散
tldr: BitLM 针对自回归语言模型逐个生成 token 的效率瓶颈，提出将 token 表示为定长二进制码，并结合轻量级扩散头在每个块内并行去噪生成多个 token。该模型在保留因果注意力机制的同时，通过位级去噪取代大规模 Softmax，实现了更高效的预训练和更快的推理速度，证明了多 token 并行生成是提升语言模型性能的有效路径。
selection_source: fresh_fetch
motivation: 传统的逐 token 自回归生成方式限制了模型的表达能力和推理吞吐量，且难以捕捉自然语言中的多 token 结构。
method: 将 token 编码为二进制位，利用轻量级扩散头在保持跨块因果关系的前提下，在每个块内并行去噪生成多个 token。
result: BitLM 摆脱了大规模 Softmax 的计算负担，在预训练效率和推理速度上均有显著提升，且性能优于传统模型。
conclusion: 该研究表明逐 token 生成并非语言模型的必然选择，通过位级扩散实现的多 token 生成为下一代高效语言模型架构提供了新方向。
---

## 摘要
自回归语言模型一次生成一个标记（token），然而自然语言本质上是由多标记单元构成的，包括共同承载意义的短语、n-gram 和搭配。这种单标记瓶颈限制了模型在预训练期间的表达能力以及推理时的吞吐量。现有的补救措施，如投机采样或基于扩散的语言模型，要么未能解决底层的瓶颈问题，要么牺牲了对语言建模至关重要的因果结构。我们提出了 BitLM，这是一种将每个标记表示为固定长度二进制代码的语言模型，并采用轻量级扩散头在每个块（block）内并行去噪多个标记。至关重要的是，BitLM 在跨块时保留了从左到右的因果注意力，同时在每个块内进行联合词汇决策，将自回归建模的可靠性与迭代细化的并行性相结合。通过用位级去噪取代大词汇量的 softmax，BitLM 将标记生成重新定义为紧凑二进制空间中的迭代承诺，从而实现了更高效的预训练和显著更快的推理，且无需改变使语言模型有效的因果基础。我们的结果表明，一次一个标记的范式并非根本要求，而是一种接口选择，改变这一范式可以产生更强大、更快速的语言模型。我们希望 BitLM 为下一代语言模型架构指明一个充满前景的方向。

## Abstract
Autoregressive language models generate text one token at a time, yet natural language is inherently structured in multi-token units, including phrases, n-grams, and collocations that carry meaning jointly. This one-token bottleneck limits both the expressiveness of the model during pre-training and its throughput at inference time. Existing remedies such as speculative decoding or diffusion-based language models either leave the underlying bottleneck intact or sacrifice the causal structure essential to language modeling. We propose BitLM, a language model that represents each token as a fixed-length binary code and employs a lightweight diffusion head to denoise multiple tokens in parallel within each block. Crucially, BitLM preserves left-to-right causal attention across blocks while making joint lexical decisions within each block, combining the reliability of autoregressive modeling with the parallelism of iterative refinement. By replacing the large-vocabulary softmax with bitwise denoising, BitLM reframes token generation as iterative commitment in a compact binary space, enabling more efficient pre-training and substantially faster inference without altering the causal foundation that makes language models effective. Our results demonstrate that the one-token-at-a-time paradigm is not a fundamental requirement but an interface choice, and that changing it can yield a stronger and faster language model. We hope BitLM points toward a promising direction for next-generation language model architectures.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **BitLM**，一种通过位级（Bitwise）连续扩散机制实现多标记（Multi-Token）并行生成的语言模型架构。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的自回归（AR）语言模型采用“逐标记生成”范式，将标记视为原子类别，通过大规模 Softmax 层输出概率。这种方式存在两个瓶颈：一是**推理效率低**（串行生成）；二是**表达能力受限**（忽略了自然语言中短语、n-gram 等多标记单元的结构性）。
*   **核心目标**：探索是否可以改变词汇表 Softmax 这一传统接口，通过将标记表示为二进制码并引入扩散模型，在保持因果结构的同时实现多标记的并行联合生成。

### 2. 方法论
*   **核心思想**：将每个 Token ID 映射为固定长度的二进制代码（如 18 位），并在连续的二进制空间中通过迭代去噪（Diffusion）来预测标记，从而将词汇选择问题转化为结构化离散对象的细化过程。
*   **关键技术细节**：
    *   **二进制接口**：使用 $B = \lceil \log_2 V \rceil$ 长度的二进制向量表示 Token，并将其映射到 $\{-1, 1\}$ 的超立方体顶点上。
    *   **块因果（Block-Causal）注意力**：将序列划分为大小为 $m$ 的块。块与块之间保持从左到右的因果关系，但块内部的 Token 可以相互注意，从而允许块内并行生成。
    *   **条件去噪头**：模型由一个因果 Transformer 主干（负责推理上下文）和一个轻量级扩散头（负责词汇实现）组成。扩散头根据主干提供的上下文，通过高斯噪声插值和迭代去噪，同时预测一个块内的 $m$ 个标记。
    *   **算法流程**：训练时，模型学习从加噪的二进制状态恢复原始代码；推理时，从高斯噪声开始，经过 $K$ 步迭代去噪，最后通过 `sign` 函数投影回二进制空间并解码为 Token ID。

### 3. 实验设计
*   **数据集**：预训练使用 FineWeb 的子集（约 350B Tokens）；下游微调使用 XSum 摘要数据集。
*   **模型规模**：构建了 0.6B、1.7B、4B 和 8B 四种参数规模的模型。
*   **对比基准（Benchmark）**：
    *   在 XSum 上对比了 Lead-3、PTGEN（指针生成网络）等传统基准。
    *   对比了使用标准 LM Head（Softmax）和使用扩散头（Diff. Head）的 BitLM 性能。
*   **消融实验**：研究了去噪步数 $K$ 和无分类器指导（CFG）强度对生成质量的影响。

### 4. 资源与算力
*   **算力细节**：论文提到在 FineWeb-350BT 上进行了预训练，使用了 AdamW 优化器，序列长度为 16384。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号（如 H100/A100）、GPU 数量以及总训练时长（仅提到预训练了 1 个 epoch）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 8B 的模型扩展性实验，证明了该架构具有良好的 Scaling Law 特性。
*   **充分性评价**：
    *   **优点**：消融实验展示了推理参数（步数和指导值）的优化过程，模型规模跨度较大。
    *   **不足**：下游任务仅展示了 XSum 摘要任务，缺乏在代码生成、逻辑推理或通用对话等更广泛 LLM 基准上的评估。实验主要侧重于验证架构的可行性，而非全面超越现有 SOTA 模型。

### 6. 主要结论与发现
*   **可扩展性**：BitLM 的训练损失随模型规模增大而平滑下降，证明二进制扩散接口支持大规模预训练。
*   **并行生成可行性**：通过位级去噪取代 Softmax 是可行的，模型能够学习到非平凡的文本生成能力。
*   **性能表现**：在 XSum 任务上，BitLM 的表现优于简单的 Lead-3 基准，虽然目前仍略低于专门的指针生成网络，但验证了其作为通用生成器的潜力。

### 7. 优点
*   **架构创新**：打破了 Softmax 瓶颈，将离散 Token 生成转化为连续空间的去噪问题，为多标记并行生成提供了原生支持。
*   **推理效率潜力**：通过块生成（Block-wise generation），理论上可以显著减少 Transformer 主干的调用次数，提升吞吐量。
*   **兼容性**：保留了因果主干网络，能够利用现有成熟的 Transformer 优化技术。

### 8. 不足与局限
*   **生成精度**：在需要极高词汇精确度的任务（如 XSum）中，目前的二进制扩散方案与成熟的 Softmax 模型相比仍有差距。
*   **推理开销平衡**：虽然减少了主干网络的步数，但扩散头需要多次迭代（如 15 步），这可能抵消部分并行带来的速度优势，需进一步优化去噪步数。
*   **评估局限**：缺乏对长文本生成质量、幻觉率以及计算延迟（Latency）的详细对比数据。

（完）
