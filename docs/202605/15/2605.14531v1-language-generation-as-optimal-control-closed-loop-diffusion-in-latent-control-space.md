---
title: "Language Generation as Optimal Control: Closed-Loop Diffusion in Latent Control Space"
title_zh: 语言生成作为最优控制：潜控制空间中的闭环扩散
authors: "ZiYi Dong, Yuliang Huang, Weijian Deng, Xiangyang Ji, Liang Lin, Pengxu Wei"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14531v1"
tags: ["query:ardm"]
score: 9.0
evidence: 分析自回归和扩散模型的统一理论视角
tldr: 本研究将语言生成重新定义为随机最优控制问题，从理论上分析了自回归和扩散模型在效率与保真度等方面的局限性。为解决这些问题，提出了Manta-LM，在修正的潜控制空间中利用流匹配和全局积分算子来近似Hamilton-Jacobi-Bellman方程的解。该方法实现了高保真度的文本生成和高效的并行采样，在语言建模和条件生成任务中表现出卓越的稳定性与可控性。
selection_source: fresh_fetch
motivation: 旨在通过随机最优控制理论统一分析并解决自回归与扩散模型在效率、保真度及优化过程中的固有缺陷。
method: 在修正的潜控制空间中应用流匹配技术，并结合全局积分算子来近似求解HJB方程的最优策略。
result: 该方法在语言建模和条件生成任务中取得了强劲性能，同时实现了低成本的高效并行采样。
conclusion: 研究提供了一个统一的理论框架，并证明了闭环控制模型在实现高保真、高效率文本生成方面的巨大潜力。
---

## 摘要
本研究将语言生成重新表述为一个随机最优控制问题，提供了一个统一的理论视角来分析自回归模型和扩散模型，并从轨迹奇异性、伴随状态消失和梯度缺失的结合角度，解释了它们的局限性（效率-保真度悖论、不可逆误差传播、优化可解性与保真度）。为了解决这些问题，我们近似求解了 Hamilton-Jacobi-Bellman (HJB) 方程，从而得到一个作为闭环控制器的最优策略。为了绕过直接求解 HJB 偏微分方程的不可解性，我们在修正的潜控制空间中采用流匹配（Flow Matching）作为最优轨迹求解器。这使得我们的带有全局积分算子的 Manta-LM 能够近似全局向量场，从而有效地实现了一个同时具备高保真文本生成和高效、低成本并行采样的模型。实验表明，我们的方法在语言建模和条件生成任务上取得了优异的性能，同时表现出更高的稳定性、效率和可控性。

## Abstract
This work reformulates language generation as a stochastic optimal control problem, providing a unified theoretical perspective to analyze autoregressive and diffusion models and explain their limitations (Efficiency-Fidelity Paradox, Irreversibility Error Propagation, Optimization Tractability and Fidelity) in terms of combination of trajectory singularity, adjoint state vanishing, and gradient absence. To address these issues, we approximate the solution to the Hamilton-Jacobi-Bellman (HJB) equation, yielding an optimal policy that acts as a closed-loop controller. To bypass the intractability of directly solving the HJB PDE, we employ Flow Matching as the optimal trajectory solver within the rectified latent control space. This allows our Manta-LM with Global Integral Operator to approximate the global vector field, effectively realizing a model that simultaneously achieves high-fidelity text generation and efficient, low-cost parallel sampling. Empirically, our method achieves strong performance on language modeling and conditional generation tasks, while exhibiting improved stability, efficiency, and controllability.

---

## 论文详细总结（自动生成）

以下是对论文《Language Generation as Optimal Control: Closed-Loop Diffusion in Latent Control Space》的结构化总结：

### 1. 论文的核心问题与整体含义
该论文旨在解决当前大语言模型（LLM）中两种主流范式的固有缺陷：
*   **自回归模型（ARM）**：受限于串行解码（效率低）和“开环”预测导致的误差累积（易幻觉）。
*   **扩散模型（DLM）**：虽然支持并行，但离散空间的非连续性导致优化困难，且通常需要大量采样步骤（效率-保真度悖论）。
*   **核心动机**：通过**随机最优控制（Stochastic Optimal Control, SOC）**理论，将语言生成重新定义为一个在连续潜空间中寻找最优轨迹的问题，从而统一分析并克服上述模型的局限性。

### 2. 核心方法论
论文提出了 **Manta-LM** 框架，其核心思想是将生成过程视为受 Hamilton-Jacobi-Bellman (HJB) 方程指导的闭环控制过程：
*   **流形修正（Manifold Rectification）**：使用正则化的变分自编码器（TextVAE）将离散、稀疏的词向量空间映射到一个紧凑、局部欧几里得的潜流形（Latent Control Space）。这解决了离散空间缺乏梯度的问题。
*   **最优闭环控制器**：将生成建模为最小化传输成本的问题。通过**流匹配（Flow Matching）**技术在潜空间中近似 HJB 方程的解，学习一个向量场作为反馈控制器，使生成轨迹趋向于高概率数据区域。
*   **全局积分算子（Global Integral Operator）**：利用 Transformer 的自注意力机制作为全局算子的离散近似，捕捉潜空间中所有“粒子”（Token）之间的相互作用，从而实现全局轨迹规划。
*   **边界值问题（BVP）求解**：不同于 ARM 只能从左向右生成，Manta-LM 可以处理任意位置的约束（如填空、纠错），将其视为给定边界条件的轨迹优化。

### 3. 实验设计
*   **数据集**：
    *   **零样本语言建模**：OpenWebText, WikiText-2/103, LAMBADA, PTB, 1BW。
    *   **条件文本生成**：QQP（改写）、Quasar-T（问题生成）、Wiki-Auto（文本简化）、CCD（对话）。
    *   **特殊任务**：受限自我纠错（WikiText/OpenWebText 扰动版）、长文本填充（Infilling）。
*   **Benchmark 与对比方法**：
    *   **基准**：GPT-2 (ARM)。
    *   **离散扩散模型**：D3PM, SEDD, MDLM, LLaDA 等。
    *   **连续扩散模型**：DiffuSeq, RDLM 等。

### 4. 资源与算力
论文明确提到了训练细节：
*   **硬件**：使用 2 台 NVIDIA RTX A6000 GPU。
*   **时长**：训练 80,000 个步数，总计约 **658.6 GPU 小时**。
*   **模型规模**：主要实验基于 101M 和 0.6B 参数规模的模型。

### 5. 实验数量与充分性
实验设计非常全面且具有深度：
*   **多维度评估**：涵盖了密度估计（PPL）、生成质量（BLEU/ROUGE）、多样性（Entropy）以及推理效率（Throughput）。
*   **消融与分析实验**：包括对潜空间“刚度”（Stiffness）和曲率的几何分析、CFG 指导强度的影响分析、以及 TextVAE 的鲁棒性测试。
*   **充分性评价**：实验不仅对比了主流模型，还通过 BVP 任务展示了该模型在非因果生成场景下的独特优势，论证过程较为客观、公平。

### 6. 主要结论与发现
*   **性能卓越**：Manta-LM 在零样本困惑度（PPL）上超越了现有的离散扩散模型和 GPT-2 基准。
*   **效率提升**：由于潜空间轨迹更平滑（低曲率），模型可以使用更大的步长进行采样。在 1024 长度的生成中，比最高效的基准快 **4.6 倍**。
*   **闭环纠错能力**：模型表现出强大的自我纠错能力，能有效将偏离流形的错误状态拉回正确轨迹，显著减少了幻觉累积。
*   **统一性**：证明了 Transformer 架构本质上是 HJB 动力学中全局梯度流的结构化离散实现。

### 7. 优点（亮点）
*   **理论严密**：将控制理论与生成模型结合，为理解 ARM 和 DLM 的失败模式（如伴随状态消失）提供了深刻的数学解释。
*   **几何优化**：通过 VAE 进行流形修正，解决了连续扩散模型在原始嵌入空间中“刚度”过大的问题。
*   **灵活性**：天然支持非因果生成任务（如长文本中间填充），无需专门的 FIM（Fill-in-the-Middle）预训练。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在亿级参数规模，尚未在百亿或千亿级参数的超大规模模型上验证扩展性（Scaling Laws）。
*   **任务复杂性**：对于需要频繁中断或工具调用的交互式任务（Agentic tasks），这种全局规划范式的计算开销可能高于极细粒度的自回归推理。
*   **VAE 依赖**：模型的上限部分取决于 TextVAE 的重建质量，尽管论文显示重建率很高，但在处理极罕见词汇或特殊符号时可能存在信息损失。

（完）
