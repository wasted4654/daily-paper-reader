---
title: "Causal Forcing++: Scalable Few-Step Autoregressive Diffusion Distillation for Real-Time Interactive Video Generation"
title_zh: Causal Forcing++：用于实时交互式视频生成的可扩展少步自回归扩散蒸馏
authors: "Min Zhao, Hongzhou Zhu, Kaiwen Zheng, Zihan Zhou, Bokai Yan, Xinyuan Li, Xiao Yang, Chongxuan Li, Jun Zhu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15141v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于视频生成的自回归扩散蒸馏
tldr: 本研究针对实时交互式视频生成中的低延迟和流式输出需求，提出了Causal Forcing++。该方法通过因果一致性蒸馏（Causal CD）解决了少步自回归扩散模型初始化的瓶颈，实现了仅需1-2步采样的逐帧自回归生成。相比现有4步分块生成方法，该方案在显著降低延迟和训练成本的同时，提升了视频质量和交互性能，并成功应用于动作条件世界模型。
selection_source: fresh_fetch
motivation: 现有的自回归扩散蒸馏方法在实时交互场景中存在响应粒度粗和采样延迟高的问题，且少步AR学生的初始化效率低下且难以扩展。
method: 提出因果一致性蒸馏（Causal CD），通过单步在线教师ODE监督学习自回归条件流映射，避免了预计算完整轨迹的成本。
result: "在逐帧2步设置下，该方法在VBench和VisionReward指标上均超越了SOTA的4步分块方法，同时首帧延迟降低50%，训练成本降低约4倍。"
conclusion: Causal Forcing++为实时交互式视频生成提供了一个高效、可扩展且高质量的蒸馏框架，并展示了在动作条件世界模型生成中的应用潜力。
---

## 摘要
实时交互式视频生成需要低延迟、流式传输和可控的展开。现有的自回归（AR）扩散蒸馏方法通过将双向基础模型蒸馏为少步 AR 学生模型，在分块式（chunk-wise）4 步机制下取得了显著成果，但它们仍受限于粗粒度的响应和不可忽视的采样延迟。在本文中，我们研究了一种更激进的设置：仅需 1-2 个采样步的逐帧自回归。在这种机制下，我们发现少步 AR 学生模型的初始化是关键瓶颈：现有策略要么目标不匹配，要么无法进行少步生成，要么扩展成本过高。我们提出了 Causal Forcing++，这是一个基于原则且可扩展的流水线，它使用因果一致性蒸馏（causal CD）进行少步 AR 初始化。其核心思想是，causal CD 学习与因果 ODE 蒸馏相同的 AR 条件流映射，但通过相邻时间步之间的单个在线教师 ODE 步获取监督，从而避免了预计算和存储完整 PF-ODE 轨迹的需求。这使得初始化更加高效且更易于优化。由此产生的流水线在逐帧 2 步设置下，在 VBench Total 上超过了最先进的（SOTA）4 步分块式 Causal Forcing 0.1 分，在 VBench Quality 上超过 0.3 分，在 VisionReward 上超过 0.335 分，同时将首帧延迟降低了 50%，并将第二阶段的训练成本降低了约 4 倍。我们本着 Genie3 的精神，进一步将该流水线扩展到动作条件世界模型生成。

## Abstract
Real-time interactive video generation requires low-latency, streaming, and controllable rollout. Existing autoregressive (AR) diffusion distillation methods have achieved strong results in the chunk-wise 4-step regime by distilling bidirectional base models into few-step AR students, but they remain limited by coarse response granularity and non-negligible sampling latency. In this paper, we study a more aggressive setting: frame-wise autoregression with only 1--2 sampling steps. In this regime, we identify the initialization of a few-step AR student as the key bottleneck: existing strategies are either target-misaligned, incapable of few-step generation, or too costly to scale. We propose \textbf{Causal Forcing++}, a principled and scalable pipeline that uses \emph{causal consistency distillation} (causal CD) for few-step AR initialization. The core idea is that causal CD learns the same AR-conditional flow map as causal ODE distillation, but obtains supervision from a single online teacher ODE step between adjacent timesteps, avoiding the need to precompute and store full PF-ODE trajectories. This makes the initialization both more efficient and easier to optimize. The resulting pipeline, \ours, surpasses the SOTA 4-step chunk-wise Causal Forcing under the \textit{\textbf{frame-wise 2-step setting}} by 0.1 in VBench Total, 0.3 in VBench Quality, and 0.335 in VisionReward, while reducing first-frame latency by 50\% and Stage 2 training cost by $\sim$$4\times$. We further extend the pipeline to action-conditioned world model generation in the spirit of Genie3. Project Page: https://github.com/thu-ml/Causal-Forcing and https://github.com/shengshu-ai/minWM .

---

## 论文详细总结（自动生成）

这篇论文由清华大学、生数科技和中国人民大学的研究团队合作完成，旨在解决实时交互式视频生成中的高延迟和训练效率问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：视频生成正从被动内容生成转向交互式世界模型，这要求模型具备低延迟、流式输出和用户可控性。
*   **核心问题**：现有的自回归（AR）扩散蒸馏方法（如 Causal Forcing, Self Forcing）通常采用“分块生成（chunk-wise）”和“4步采样”，导致响应粒度粗且采样延迟高，无法满足真正的实时交互需求。
*   **瓶颈识别**：在更激进的“逐帧（frame-wise）”和“1-2步采样”设置下，少步 AR 学生的**初始化**成为关键瓶颈。现有初始化策略要么目标不匹配（双向教师误导），要么缺乏少步生成能力（误差累积），要么扩展成本极高（需要预计算并存储海量 ODE 轨迹）。

### 2. 方法论
*   **核心思想**：提出 **Causal Forcing++**，利用**因果一致性蒸馏（Causal Consistency Distillation, Causal CD）**作为少步 AR 学生的初始化手段。
*   **关键技术细节**：
    *   **等效性原理**：Causal CD 与因果 ODE 蒸馏的目标一致，都是学习教师模型的 AR 条件流映射（即一致性函数）。
    *   **在线监督**：不同于 ODE 蒸馏需要预计算完整的 PF-ODE 轨迹，Causal CD 仅需在相邻时间步之间进行单步在线教师 ODE 采样。
    *   **三阶段流水线**：
        1.  **阶段 1**：通过 Teacher Forcing 训练多步 AR 扩散模型（作为教师）。
        2.  **阶段 2**：使用 Causal CD 进行少步 AR 学生初始化，通过减小每步优化间隙（相邻步对比直接回归到端点）提升质量。
        3.  **阶段 3**：采用非对称 DMD（分布匹配蒸馏）配合学生自回滚（Self-rollout）进行最终微调。
*   **扩展应用**：将该框架扩展到动作条件（Action-conditioned）世界模型，通过注入相机姿态信息实现交互式生成。

### 3. 实验设计
*   **基础模型**：Wan2.1-1.3B。
*   **数据集**：使用 80K 视频数据集（包含 OpenVid 采样）进行前两阶段训练，使用 VidProM 数据集进行第三阶段训练。
*   **Benchmark**：VBench（评估总体、质量、语义、动态程度）和 VisionReward（评估人类偏好和指令遵循）。
*   **对比方法**：CausVid、Self Forcing、Causal Forcing（原版）。
*   **实验设置**：重点测试了逐帧（frame-wise）下的 1 步、2 步和 4 步生成效果。

### 4. 资源与算力
*   **GPU 型号**：主要在 **NVIDIA A800** 上进行测试和训练。
*   **算力消耗**：
    *   **阶段 2 效率提升**：相比原版 Causal Forcing 的 ODE 初始化（需 11,600 GPU 小时），Causal Forcing++ 的 Causal CD 仅需 **2,900 GPU 小时**，训练成本降低约 **4 倍**。
    *   **存储节省**：无需存储原版方法所需的约 1,900 GiB 离线轨迹数据，存储成本降为 **0**。
*   **推理性能**：在 A800 上，2 步生成的首帧延迟降低了 **50%**，吞吐量提升了约 1.4 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，对比了五种不同的初始化策略（Self Forcing、多步 AR、因果 ODE、因果 DMD、因果 CD）在不同采样步数（1/2/4 步）下的表现。
*   **充分性评价**：实验设计非常充分且客观。研究者不仅对比了最终指标，还深入分析了阶段 2 初始化质量对阶段 3 最终结果的影响，并探讨了“模式寻求（Mode-seeking）”与“模式覆盖（Mode-covering）”行为在自回归场景下的优劣。

### 6. 主要结论与发现
*   **性能超越**：在逐帧 2 步设置下，Causal Forcing++ 在 VBench 和 VisionReward 指标上全面超越了之前 4 步分块生成的 SOTA 方法。
*   **初始化至关重要**：实验证明，如果没有强力的少步初始化，DMD 阶段无法自行修复严重的自回归漂移。
*   **Causal CD 的优越性**：相比因果 DMD（容易产生曝光偏差/Exposure Bias），Causal CD 表现出更好的鲁棒性，因为它具有模式覆盖特性，对自回归过程中的历史误差累积不那么敏感。

### 7. 优点
*   **高效可扩展**：通过在线单步监督取代离线轨迹存储，极大地降低了算力和存储门槛。
*   **低延迟**：实现了真正的逐帧交互，首帧延迟极低，适合实时应用。
*   **理论完备**：对不同蒸馏目标的理论关联（ODE vs. CD vs. DMD）在 AR 语境下做了清晰的阐述和实验验证。

### 8. 不足与局限
*   **动作条件限制**：虽然展示了动作条件世界模型，但该部分目前仍基于分块（chunk-wise）4 步设置，尚未完全迁移到更激进的逐帧 2 步设置。
*   **长程漂移**：尽管有所缓解，但在极低步数（如 1 步）下，长时间自回归生成的质量衰减和曝光偏差依然是一个挑战。
*   **依赖强教师**：蒸馏效果高度依赖于阶段 1 训练出的 AR 教师模型的质量。

（完）
