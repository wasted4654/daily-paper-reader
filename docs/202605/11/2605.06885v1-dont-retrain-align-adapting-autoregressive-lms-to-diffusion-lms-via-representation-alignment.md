---
title: "Don't Retrain, Align: Adapting Autoregressive LMs to Diffusion LMs via Representation Alignment"
title_zh: 莫重训，重对齐：通过表示对齐将自回归语言模型适配为扩散语言模型
authors: "Fred Zhangzhi Peng, Alexis Fox, Anru R. Zhang, Alexander Tong"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06885v1"
tags: ["query:ardm"]
score: 9.0
evidence: 通过表示对齐将自回归语言模型适配为扩散语言模型
tldr: 本研究提出 REPR-ALIGN，旨在将预训练自回归（AR）模型高效转换为扩散语言模型（DLM）。该方法通过余弦相似度将 DLM 的隐藏状态与冻结的 AR 模型对齐，避免了从头学习语言表示。实验显示，该方法在不改变架构的前提下实现了 4 倍的训练加速，且在低数据场景下表现优异，证明了语言表示在不同生成顺序间的可迁移性。
selection_source: fresh_fetch
motivation: 现有 AR 到 DLM 的转换方法主要依赖去噪训练，忽略了自回归预训练中已蕴含的丰富语义结构。
method: 引入 REPR-ALIGN 目标，在优化去噪任务的同时，利用余弦相似度将 DLM 的每一层隐藏状态与对应的冻结 AR 模型表示进行对齐。
result: 该方法在保持模型架构不变的情况下，实现了高达 4 倍的训练加速，并在低数据量环境下表现出极强的适应性。
conclusion: 语言表示具有跨生成顺序的迁移能力，表示对齐是提升扩散语言模型训练效率的一种简单且有效的手段。
---

## 摘要
扩散语言模型（DLM）近期展现出了与标准自回归（AR）模型互补的能力，特别是在非序列生成和双向编辑方面。尽管最近的研究表明预训练的自回归检查点可以转换为扩散语言模型，但现有的方案主要通过在目标函数和注意力层面进行修改，并进行持续的去噪训练来迁移参数。与此不同，我们探究在 AR 到 DLM 的转换过程中，通过预测下一个 token 学习到的内部表示几何结构是否可以被显式地保留。我们假设 AR 预训练学习到的大部分语义结构可以跨生成顺序进行迁移，因此 DLM 训练应被视为重新学习解码路径，而非重新学习语言表示。为了研究这一点，我们引入了 REPR-ALIGN，这是一种表示对齐目标，它使双向掩码扩散模型能够重用来自相同架构的预训练 AR 模型的表示。具体而言，我们在优化标准掩码去噪目标的同时，使用余弦相似度将 DLM 每一层的隐藏状态与冻结的 AR 模型进行对齐。这种简单的对齐方式无需适配器，除了注意力掩码外不涉及任何架构更改，在我们的实验设置中实现了高达 4 倍的训练加速，并且在低数据量的情况下尤为有效。我们的结果表明，语言表示可以跨生成顺序迁移，且表示对齐为训练扩散语言模型提供了一种简单而有效的技术。代码可在 https://github.com/pengzhangzhi/Open-dLLM 获取。

## Abstract
Diffusion language models (DLMs) have recently demonstrated capabilities that complement standard autoregressive (AR) models, particularly in non-sequential generation and bidirectional editing. Although recent work has shown that pretrained autoregressive checkpoints can be converted into diffusion language models, existing recipes primarily transfer parameters through continued denoising training with objective- and attention-level modifications. We instead ask whether the internal representation geometry learned by next-token prediction can be explicitly preserved during AR-to-DLM conversion. We hypothesize that much of the semantic structure learned by AR pretraining can transfer across generation orders, and thus DLM training should be viewed as relearning the decoding path rather than relearning language representations. To investigate this, we introduce REPR-ALIGN, a representation alignment objective that adapts a bidirectional masked diffusion model to reuse representations from a pretrained AR model of identical architecture. Concretely, we align the hidden states of the DLM to the frozen AR model at every layer using cosine similarity, while optimizing the standard masked denoising objective. This simple alignment, with no adapters and no architectural changes beyond the attention mask, yields up to 4x training acceleration in our setting and is particularly effective in low-data regimes. Our results suggest that linguistic representations can transfer across generation order, and that representation alignment provides a simple and effective technique for training diffusion language models. Code is available at https://github.com/pengzhangzhi/Open-dLLM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Don't Retrain, Align: Adapting Autoregressive LMs to Diffusion LMs via Representation Alignment》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：扩散语言模型（DLM）在非序列生成和双向编辑方面具有优势，但从头训练（Scratch）成本极高。虽然现有研究尝试将预训练的自回归（AR）模型转换为 DLM，但通常只是简单的参数初始化或目标函数微调，忽略了 AR 模型在预训练阶段已经习得的深层语义和语法结构。
*   **核心假设**：语言表示（Representation）在不同的生成顺序（从左到右 vs. 任意顺序）之间是通用的。DLM 的训练本质上应该是学习一种新的“解码路径（Mechanism）”，而不是重新学习语言本身的表示。
*   **核心问题**：能否通过显式地保留 AR 模型的内部表示几何结构，来加速并优化 AR 到 DLM 的转换过程？

### 2. 方法论（REPR-ALIGN）
*   **核心思想**：引入“表示对齐（Representation Alignment）”目标，将 DLM 学生模型锚定在一个冻结的 AR 老师模型上。
*   **关键技术细节**：
    *   **双模型架构**：使用两个架构完全相同的 Transformer。老师模型（AR）使用因果注意力（Causal Attention）并保持冻结；学生模型（DLM）初始化自 AR 权重，但切换为双向注意力（Bidirectional Attention）。
    *   **对齐机制**：在训练过程中，将干净的序列输入 AR 老师，将掩码后的序列输入 DLM 学生。通过**层级余弦相似度损失（Layer-wise Cosine Similarity Loss）**，强制学生模型每一层的隐藏状态（Hidden States）向老师模型对齐。
    *   **损失函数**：总损失 $L = L_{diff} + \lambda L_{align} + L_{path}$。其中 $L_{diff}$ 是标准掩码去噪损失，$\lambda L_{align}$ 是表示对齐损失（默认权重为 10），$L_{path}$ 是路径规划损失（PAPL）。
    *   **极简设计**：不引入任何适配器（Adapters）或额外的架构修改，仅改变注意力掩码。

### 3. 实验设计
*   **数据集**：主要使用 `Nemotron-SFT-Code` 数据集（约 50B tokens，70M 序列），涵盖 STEM、学术、推理和多语言代码数据。
*   **模型规模**：基于 Qwen3 检查点，测试了 0.6B、1.7B 和 4B 三种参数规模。
*   **Benchmark**：代码生成任务，包括 HumanEval、HumanEval+、MBPP 和 MBPP+。
*   **对比方法**：
    *   从头训练（Scratch）。
    *   标准 AR 到 DLM 的微调（不带对齐）。
    *   公开的前沿 DLM 模型（如 LLaDA 8B、Dream 7B、DiffuCoder 等）。

### 4. 资源与算力
*   **算力配置**：使用了 **16 张 GPU** 进行分布式数据并行（DDP）训练。
*   **具体型号**：文中未明确说明具体的 GPU 型号（如 A100 或 H100），但提到了混合精度训练（bfloat16）和 FlashAttention-2。
*   **训练细节**：全局 Batch Size 为 96，最大序列长度 4096，优化器为 AdamW，学习率 3e-4。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 4B 的参数缩放实验，验证了对齐增益随模型规模增大而增加的趋势。
*   **消融实验**：非常充分，包括：
    *   对齐度量方式（L2 vs. Cosine）。
    *   对齐权重 $\lambda$ 的敏感度。
    *   对齐层级的选择（底层、中层、高层或全层）。
    *   参数冻结策略（冻结 Embedding 和 MLP 块的影响）。
    *   数据效率实验（全量数据 vs. 0.8B 极小数据集）。
*   **客观性**：通过在相同步数、相同数据集下对比有无对齐的效果，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **加速显著**：在相同性能下，REPR-ALIGN 实现了高达 **4 倍的训练加速**。
*   **数据高效**：在仅有 0.8B tokens 的极小数据集上，带对齐的转换效果甚至优于在 50B tokens 上不带对齐的转换效果。
*   **性能优越**：4B 规模的 oDLM 在 HumanEval 家族测试中超过了参数量更大的 Dream-7B，且使用的计算资源和数据量大幅减少。
*   **参数冗余**：发现一旦表示被锚定，冻结 Embedding 和 MLP 块不仅能提升 2 倍的吞吐量，且几乎不损失性能，证明了转换过程主要是注意力机制的适配。

### 7. 优点
*   **理论深刻**：重新定义了 DLM 训练的本质——从“表示学习”转向“机制适配”。
*   **实现简单**：无需修改架构或增加参数，易于集成到现有的训练管线中。
*   **效率极高**：显著降低了 DLM 的研发门槛，尤其适合算力受限的团队。

### 8. 不足与局限
*   **任务覆盖**：实验主要集中在代码生成（Code Generation）任务上，尚未在更广泛的通用自然语言理解或长文本生成任务上进行全面验证。
*   **架构限制**：目前仅在 Qwen3（Decoder-only）架构上验证，对于其他架构（如 Encoder-Decoder 或纯非因果架构）的适用性有待观察。
*   **依赖老师模型**：该方法的效果高度依赖于预训练 AR 老师模型的质量，如果 AR 模型本身存在偏差，可能会通过对齐传递给 DLM。

（完）
