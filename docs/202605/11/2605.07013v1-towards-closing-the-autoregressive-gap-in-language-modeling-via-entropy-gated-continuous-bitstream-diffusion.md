---
title: Towards Closing the Autoregressive Gap in Language Modeling via Entropy-Gated Continuous Bitstream Diffusion
title_zh: 通过熵门控连续比特流扩散缩小语言建模中的自回归差距
authors: "Georgios Batzolis, Mark Girolami, Luca Ambrogioni"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.07013v1"
tags: ["query:ardm"]
score: 9.0
evidence: 通过连续比特流扩散缩小语言模型中的自回归差距
tldr: 本研究旨在缩小扩散语言模型与自回归模型之间的性能差距。作者提出将文本建模为固定宽度二进制比特流上的连续扩散过程，利用匹配滤波器残差参数化和熵率门控的随机采样器来优化生成。该方法不仅在LM1B和OpenWebText基准上达到了与自回归模型相当的生成质量，还将词表缩放复杂度从O(V)降低至O(log V)，显著提升了内存效率和吞吐量，为高效语言生成提供了新范式。
selection_source: fresh_fetch
motivation: 解决扩散语言模型在生成质量上落后于自回归模型，以及传统大词表带来的计算和内存瓶颈问题。
method: 将文本表示为模拟比特序列，通过连续比特流扩散过程建模，并引入基于熵率门控的随机采样器进行推理。
result: 在LM1B和OWT数据集上，该模型以更少的采样步数达到了与自回归模型相当的生成性能，刷新了扩散模型的SOTA。
conclusion: 比特流扩散模型通过优化参数化和采样策略，成功弥合了与自回归模型的差距，并展现出极佳的词表扩展性。
---

## 摘要
扩散语言模型 (DLM) 具有并行、顺序无关生成的潜力，但在标准基准测试中，其样本质量和多样性历来落后于自回归模型。最近在 token 嵌入上进行的连续流和扩散方法缩小了这一差距，表明连续状态空间对语言建模非常有效。在这项工作中，我们通过将文本建模为固定宽度二进制比特流上的连续扩散过程，进一步缩小了自回归差距。我们的方法将语义 token 表示为模拟比特序列，并利用匹配滤波器残差参数化将上下文学习与解析独立比特后验隔离开来。关键在于，我们采用了一种随机采样器，该采样器应用由熵率剖面门控的 Langevin 型修正，自动将随机性集中在高信息区域，而在其他区域保持近乎确定性。在 One Billion Word 基准测试 (LM1B) 上，我们的 130M 参数比特流模型在使用 256 次神经函数评估 (NFE) 的情况下，在匹配真实数据熵 (4.31) 时达到了 59.76 的生成困惑度 ($\GenPPL$)，显著优于先前的 DLM 基准并达到了自回归参考水平。在 OpenWebText (OWT) 上，我们的随机采样器建立了新的连续 DLM 帕累托前沿，在使用比之前 1024-NFE 基准少 4 倍步数的情况下，在 5.26 的熵下实现了 $\GenPPL=27.06$。作为额外的架构优势，比特流扩散消除了标准 DLM 共同面临的 $\mathcal{O}(V)$ 词表缩放瓶颈。通过语义比特补丁预测 $\mathcal{O}(\log V)$ 的逐比特 logit，我们的模型减少了内存占用并提高了吞吐量，展示了随着词表大小增长而具有可扩展性的语言生成范式。

## Abstract
Diffusion language models (DLMs) promise parallel, order-agnostic generation, but on standard benchmarks they have historically lagged behind autoregressive models in sample quality and diversity. Recent continuous flow and diffusion approaches over token embeddings have narrowed this gap, suggesting continuous state spaces are highly effective for language. In this work, we further close the autoregressive gap by modeling text as a continuous diffusion process over fixed-width binary bitstreams. Our approach represents semantic tokens as analog bit sequences and utilizes a matched-filter residual parameterization to isolate contextual learning from analytic independent-bit posteriors. Crucially, we adopt a stochastic sampler that applies Langevin-type corrections gated by the entropy-rate profile, automatically concentrating stochasticity in high-information regions while remaining nearly deterministic elsewhere. On the One Billion Word Benchmark (LM1B), our 130M-parameter bitstream model reaches a generative perplexity ($\GenPPL$) of $59.76$ at matched real-data entropy ($4.31$) using 256 neural function evaluations (NFEs), decisively outperforming prior DLM baselines and reaching the autoregressive reference. On OpenWebText (OWT), our stochastic sampler establishes a new continuous-DLM Pareto frontier, achieving $\GenPPL=27.06$ at an entropy of $5.26$ using $4\times$ fewer steps than previous 1024-NFE baselines. As an additional architectural benefit, bitstream diffusion removes the $\mathcal{O}(V)$ vocabulary scaling bottleneck shared by standard DLMs. By predicting $\mathcal{O}(\log V)$ bitwise logits via semantic bit-patching, our model yields a reduced memory footprint and higher throughput, demonstrating a scalable paradigm for language generation as vocabulary sizes grow.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“比特流扩散（Bitstream Diffusion）”的新型语言建模框架，旨在解决扩散语言模型（DLM）在生成质量上长期落后于自回归（AR）模型的问题，并消除大词表带来的计算瓶颈。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然扩散模型在图像和视频领域取得了巨大成功，但在语言建模中，其生成质量和多样性（帕累托前沿）一直难以企及自回归模型。
*   **核心挑战**：
    1.  **性能差距**：扩散模型往往容易生成过于保守、高频的 token，导致样本熵（多样性）塌缩。
    2.  **扩展性瓶颈**：传统 DLM 的输出层复杂度与词表大小 $V$ 成正比（$O(V)$），在处理超大词表或多模态 token 时内存和计算压力巨大。
*   **整体含义**：通过将文本建模为连续空间中的二进制比特流，并结合创新的参数化与采样策略，证明了连续扩散模型可以达到甚至超越自回归模型的生成水平。

### 2. 方法论：核心思想与关键技术
*   **比特流表示（Bitstream Representation）**：将 $T$ 个语义 token 编码为固定宽度的二进制序列（如每个 token 15-16 位），并在连续空间中对其进行高斯加噪。
*   **匹配滤波器残差参数化（Matched-Filter Residual Parameterization）**：
    *   **核心思想**：利用解析解处理独立的比特去噪，让神经网络只学习上下文相关的残差。
    *   **技术细节**：公式将最终 logit 分解为“解析项”（基于高斯后验的闭式解）和“神经网络预测的残差项”。这避免了模型浪费容量去学习简单的独立比特去噪逻辑。
*   **熵率门控随机采样器（Entropy-Gated Stochastic Sampler）**：
    *   **核心思想**：根据“熵率剖面（Entropy-rate profile）”动态分配随机性。
    *   **技术细节**：在信息解析最剧烈的噪声区间（高熵区域）应用 Langevin 型修正（随机扰动），而在信息确定的区间保持确定性。这种“熵门控”机制有效防止了生成过程中的过早收缩。
*   **序列扩散 Transformer (SDT)**：采用 Patch 机制，将每组比特作为一个补丁输入 Transformer，保持语义长度 $T$ 不变，但将输出层复杂度降至 $O(\log V)$。

### 3. 实验设计
*   **数据集**：
    1.  **LM1B (One Billion Word)**：标准语言建模基准，序列长度 128。
    2.  **OpenWebText (OWT)**：更大规模的网页文本数据，序列长度 1024。
*   **对比方法（Baselines）**：
    *   **自回归模型**：AR Transformer。
    *   **离散扩散模型**：MDLM, Duo, SEDD 等。
    *   **连续扩散模型**：FLM, LangFlow, RDLM, CANDI 等。
*   **评估指标**：生成困惑度 ($\GenPPL$) 与 token 频率熵（Entropy）的联合评估，确保模型在提升质量的同时不损失多样性。

### 4. 资源与算力
*   **模型规模**：主要实验基于 130M 参数的模型。
*   **训练细节**：LM1B 训练 100 万步，OWT 训练 75 万步。
*   **硬件支持**：文中提到使用 **NVIDIA GH200** GPU 进行性能分析（Profiling）。
*   **效率提升**：相比 token 空间模型，比特流模型在 OWT 规模下训练吞吐量提升了 3.3 倍，生成时的内存占用降低了 19 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的采样步数（NFE）扫描、随机扰动（Churn）参数扫描以及消融实验。
*   **消融实验**：涵盖了自校正（Self-conditioning）、匹配滤波器、噪声调度（EDM vs 熵率调度）、损失函数（分数匹配 vs 交叉熵）等多个维度。
*   **客观性**：通过 GenPPL-Entropy 帕累托曲线进行对比，而非单一指标，这种评估方式在扩散模型领域被认为更加客观和公平。

### 6. 主要结论与发现
*   **弥合差距**：在 LM1B 上，256 步采样的比特流模型达到了 $\GenPPL=59.76$，超越了自回归基准（66.70），彻底弥合了扩散模型与自回归模型的质量差距。
*   **效率飞跃**：在 OWT 上，仅需 256 步即可达到之前 SOTA 模型（LangFlow）1024 步的效果，且计算资源消耗更低。
*   **随机性的重要性**：实验证明，熵门控的随机采样是提升连续 DLM 性能的关键，单纯的确定性采样会导致多样性不足。

### 7. 优点
*   **架构优雅**：将词表复杂度从 $O(V)$ 降至 $O(\log V)$，解决了长序列和大词表的扩展性难题。
*   **理论与实践结合**：引入匹配滤波器作为归纳偏置，显著降低了训练难度。
*   **采样创新**：熵门控采样器为扩散模型如何平衡确定性与随机性提供了新的理论视角。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 130M 参数规模，尚未在百亿（Billion）级参数的超大规模模型上验证其扩展定律（Scaling Laws）。
*   **推理延迟**：尽管步数已大幅减少，但相比自回归模型的单步生成（或投机采样），扩散模型的多步迭代在低延迟场景下仍面临挑战。
*   **任务覆盖**：主要关注无条件文本生成，对于下游任务（如摘要、翻译、指令遵循）的微调效果尚待进一步验证。

（完）
