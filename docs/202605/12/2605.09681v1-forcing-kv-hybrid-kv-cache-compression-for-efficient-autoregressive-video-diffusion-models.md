---
title: "Forcing-KV: Hybrid KV Cache Compression for Efficient Autoregressive Video Diffusion Models"
title_zh: Forcing-KV：用于高效自回归视频扩散模型的混合 KV 缓存压缩
authors: "Yicheng Ji, Zhizhou Zhong, Jun Zhang, Qin Yang, XiTai Jin, Ying Qin, Wenhan Luo, Shuiyang Mao, Wei Liu, Huan Li"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09681v1"
tags: ["query:ardm"]
score: 9.0
evidence: 高效自回归视频扩散模型架构
tldr: 针对自回归视频扩散模型在长视频生成中面临的KV缓存冗余和内存开销问题，本文提出Forcing-KV压缩策略。该方法基于注意力头的职能分工，将头分为静态头和动态头，分别实施结构化静态剪枝和基于相似度的动态剪枝。在保持视频质量的前提下，该方法显著降低了内存占用，并在高分辨率下实现了大幅度的推理加速。
selection_source: fresh_fetch
motivation: 自回归视频扩散模型中历史帧产生的冗余KV缓存导致了巨大的内存压力和计算复杂度，限制了长视频生成的效率。
method: 根据注意力头在时空建模中的不同角色，对静态头进行结构化剪枝，对动态头进行基于段间相似度的动态剪枝。
result: "在H200 GPU上实现了超过29 FPS的生成速度，减少了30%的内存消耗，并在1080P分辨率下获得2.82倍的加速。"
conclusion: Forcing-KV通过混合压缩策略有效解决了自回归视频生成的效率瓶颈，证明了注意力头功能专业化在模型优化中的重要性。
---

## 摘要
自回归 (AR) 视频扩散模型采用流式生成框架，能够实现具有实时响应能力的长时视频生成，例如 Self Forcing 训练范式。然而，现有的 AR 视频扩散模型由于历史帧之间冗余的键值 (KV) 缓存，仍然面临显著的注意力复杂度和严重的内存开销，这限制了其可扩展性。在本文中，我们通过将 KV 缓存压缩引入自回归视频扩散来应对这一挑战。我们观察到，主流 AR 扩散模型中的注意力头表现出明显不同的注意力模式和功能角色，且这些模式在不同样本和去噪步骤中保持稳定。基于对注意力头功能专业化的实证研究，我们将注意力头分为两类：静态头，专注于自回归块之间的过渡和帧内保真度；动态头，负责帧间运动和一致性。随后，我们提出了 Forcing-KV，这是一种混合 KV 缓存压缩策略，对静态头执行结构化静态剪枝，对动态头执行基于段相似性的动态剪枝。在保持输出质量的同时，我们的方法在单张 NVIDIA H200 GPU 上实现了超过每秒 29 帧的生成速度，并减少了 30% 的缓存内存，在 480P 分辨率下的 LongLive 和 Self Forcing 上分别实现了高达 1.35 倍和 1.50 倍的加速，在 1080P 分辨率下进一步扩展至 2.82 倍加速。代码和演示视频可在 https://zju-jiyicheng.github.io/Forcing-KV-Page 获取。

## Abstract
Autoregressive (AR) video diffusion models adopt a streaming generation framework, enabling long-horizon video generation with real-time responsiveness, as exemplified by the Self Forcing training paradigm. However, existing AR video diffusion models still suffer from significant attention complexity and severe memory overhead due to the redundant key-value (KV) caches across historical frames, which limits scalability. In this paper, we tackle this challenge by introducing KV cache compression into autoregressive video diffusion. We observe that attention heads in mainstream AR diffusion models exhibit markedly distinct attention patterns and functional roles that remain stable across samples and denoising steps. Building on our empirical study of head-wise functional specialization, we divide the attention heads into two categories: static heads, which focus on transitions across autoregressive chunks and intra-frame fidelity, and dynamic heads, which govern inter-frame motion and consistency. We then propose Forcing-KV, a hybrid KV cache compression strategy that performs structured static pruning for static heads and dynamic pruning based on segment-wise similarity for dynamic heads. While maintaining output quality, our method achieves a generation speed of over 29 frames per second on a single NVIDIA H200 GPU along with 30% cache memory reduction, delivering up to 1.35x and 1.50x speedups on LongLive and Self Forcing at 480P resolution, and further scaling to 2.82x speedup at 1080P resolution. Code and demo videos are provided at https://zju-jiyicheng.github.io/Forcing-KV-Page.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Forcing-KV**，一种针对自回归（AR）视频扩散模型的高效 KV 缓存压缩方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：自回归视频生成（如 Self Forcing 框架）通过逐块（chunk-by-chunk）生成来实现长视频创作。然而，随着视频长度增加，历史帧积累的 KV 缓存（Key-Value Cache）会导致巨大的内存开销和计算复杂度（注意力机制的平方级增长），严重限制了长视频和高分辨率视频的实时生成能力。
*   **核心发现**：论文观察到 AR 视频模型中的注意力头具有**功能专业化**特征：
    *   **静态头（Static Heads）**：关注当前块和紧邻的前一帧（过渡锚点帧），负责帧内细节和块间过渡。
    *   **动态头（Dynamic Heads）**：关注跨帧的对应区域，负责长程运动一致性和动态演变。

### 2. 方法论
Forcing-KV 采用了一种**混合压缩策略**，核心步骤包括：
*   **离线头分析（Offline Head Profiling）**：在推理前进行一次性分析，根据注意力权重分布将头分类。如果一个头的大部分注意力集中在当前块和过渡帧，则归为静态头，否则归为动态头。
*   **静态结构化剪枝（Static Structural Pruning）**：针对静态头，仅保留“过渡锚点帧”（即历史缓存中的最后一帧）和当前块的 KV，直接丢弃更远的历史信息。这保证了视频在块边界处不会出现闪烁或断裂。
*   **动态相似度剪枝（Dynamic Similarity Pruning）**：针对动态头，将帧划分为多个段（segments），计算相邻帧对应段之间的余弦相似度。剔除相似度极高的冗余段（如静态背景），保留变化显著的段（如运动物体），从而在减少缓存的同时保留动态信息。

### 3. 实验设计
*   **模型与基准**：在主流 AR 模型 **Self Forcing**、**LongLive** 以及大规模模型 **Krea-Realtime-14B** 上进行了验证。
*   **数据集**：使用 **VBench**（短视频）和 **VBenchLong**（长视频，基于 MovieGen 提示词）进行评估。
*   **对比方法**：对比了 Full KV（无压缩）、StreamingLLM（滑动窗口剪枝）和 Dummy Forcing（激进的局部剪枝）。
*   **评估指标**：除了标准的质量指标，还引入了基于光流的 **Chunk Discontinuity（块不连续性）** 指标，专门量化自回归生成中的边界跳变问题。

### 4. 资源与算力
*   **硬件平台**：实验主要在单张 **NVIDIA H200 GPU** 上运行。
*   **性能表现**：在 480P 分辨率下，生成速度超过 **29 FPS**。
*   **算力开销**：头分析过程是离线的且仅需单次运行（几分钟内完成）。推理时，该方法显著降低了显存占用（约 30%）并提升了吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 秒、30 秒及 60 秒不同长度的视频生成，使用了 946 个 VBench 提示词和 128 个长视频提示词。
*   **消融实验**：详细探讨了头分类阈值 $\alpha$、不同剪枝策略（仅静态、仅动态、随机剪枝等）的影响。
*   **主观评估**：组织了 12 名参与者进行人类偏好研究（User Study），共收集 540 份评价。
*   **充分性评价**：实验设计非常全面，不仅验证了质量和速度，还展示了在不同分辨率（从 480P 到 1080P）下的扩展性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **高效性**：在 1080P 分辨率下实现了高达 **2.82 倍** 的加速，同时减少了 30% 的缓存内存。
*   **质量保持**：相比于 Dummy Forcing 等方法，Forcing-KV 在大幅加速的同时，几乎没有损失视频的动态程度和视觉一致性。
*   **关键机制**：证实了“过渡锚点帧”对维持自回归视频连贯性的至关重要性，单纯的滑动窗口或局部注意力会破坏视频结构。

### 7. 优点
*   **无需训练（Training-free）**：作为一种即插即用的推理优化方案，不需要对模型进行重训或微调。
*   **理论支撑扎实**：基于对注意力头功能的深入实证观察，而非盲目剪枝。
*   **兼容性强**：可以与 FP8 量化等其他加速技术无缝结合，进一步提升性能。

### 8. 不足与局限
*   **模型族限制**：虽然在多个主流模型上有效，但主要针对基于“Forcing”训练范式的模型，对于其他架构（如非 Transformer 架构）的适用性尚未验证。
*   **内存缩减上限**：虽然计算加速显著（接近 3 倍），但内存缩减（30%）相对温和，因为动态头仍需保留部分历史段以维持长程一致性。
*   **离线依赖**：虽然头分析很快，但仍需针对每个新模型进行一次离线 Profile。

（完）
