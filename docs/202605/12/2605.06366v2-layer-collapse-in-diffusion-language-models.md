---
title: Layer Collapse in Diffusion Language Models
title_zh: 扩散语言模型中的层坍缩
authors: "Alexander Conzelmann, Albert Catalan-Tatjer, Shiwei Liu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06366v2"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型与自回归模型的动力学对比
tldr: 本研究揭示了扩散语言模型（DLM）中独特的“层坍缩”现象。以LLaDA-8B为例，研究发现其早期层存在由超大离群值主导的冗余激活模式，且该离群值对模型性能至关重要。与自回归模型不同，DLM的冗余源于过度训练而非训练不足。这种特性使DLM在量化和稀疏化方面表现出极强的鲁棒性，为模型压缩和部署提供了新视角。
selection_source: fresh_fetch
motivation: 旨在探索扩散语言模型与自回归模型在激活动态、层冗余性以及对模型压缩敏感度方面的本质差异。
method: 通过对LLaDA-8B进行激活模式表征、关键离群值剪枝实验以及在不同量化和稀疏策略下的性能对比分析。
result: 发现DLM早期层存在关键的超大离群值，且在3位量化下性能损失极小，其最优稀疏分配策略与自回归模型完全相反。
conclusion: DLM的训练目标重塑了其层动态，使其表现出由过度训练驱动的层坍缩特性，在模型压缩和高效部署方面具有显著潜力。
---

## 摘要
扩散语言模型 (DLMs) 近期已成为自回归 (AR) 语言模型的有力替代方案，但其激活动态方面的差异仍未得到充分理解。我们刻画了 LLaDA-8B 中的这些动态，并发现了一个引人注目的层坍缩特性：少数早期层表现出高度相似且坍缩的激活模式，这种模式由一个在长 token 范围内持续存在的巨大超离群值 (super-outlier) 所主导。尽管该离群值看似冗余，但它至关重要：剪枝该离群值会导致输出退化为重复的随机 token 循环。矛盾的是，LLaDA 中的各层总体上包含更多冗余表示，且冗余在早期层中最为显著——这与 AR 模型相反，在 AR 模型中，深层由于训练不足而变得冗余。我们的分析表明，DLM 中的层坍缩并非由训练不足驱动，而是由过度训练驱动：一个主导性的离群值成为了不可或缺的信息载体，而其余表示则坍缩为冗余结构。这些发现具有重要的实际意义，并通过受控预训练实验得到了验证。DLM 对压缩表现出惊人的鲁棒性：在 3-bit GPTQ 量化下，LLaDA 在 GSM8K 上的性能仅下降了 1.8%，而 Llama-3.1-8B 则下降了 64.7%。最优稀疏度分配在两类模型之间也发生了反转：在 50% 的平均稀疏度下，为 LLaDA 的早期层分配更多稀疏度比反向策略提升了 8.4%，而同样的分配策略却使 Llama 损失了 8.4%。我们的研究结果揭示，相对于 AR 模型，DLM 的训练目标从根本上重塑了层动态，这对压缩和部署具有直接影响。

## Abstract
Diffusion language models (DLMs) have recently emerged as competitive alternatives to autoregressive (AR) language models, yet differences in their activation dynamics remain poorly understood. We characterize these dynamics in LLaDA-8B and identify a striking layer-collapse property: a few early layers exhibit highly similar, collapsed activation patterns dominated by a single large super-outlier persisting over a long token range. Despite its apparent redundancy, this outlier is critical: pruning it causes outputs to degrade into repetitive random token loops. Paradoxically, layers in LLaDA contain more redundant representations overall, with redundancy most pronounced in earlier layers -- the reverse of AR models, where deeper layers grow redundant due to undertraining. Our analysis indicates that layer collapse in DLMs is not driven by undertraining but by overtraining: a dominant outlier becomes an indispensable information carrier while remaining representations collapse into redundant structure. These findings have strong practical implications, verified through controlled pre-training experiments. DLMs are surprisingly robust to compression: LLaDA under 3-bit GPTQ quantization drops only -1.8% on GSM8K, whereas Llama-3.1-8B drops -64.7%. Optimal sparsity allocation also reverses between families: at 50% average sparsity, allocating more to early layers in LLaDA yields +8.4% over the reverse strategy, while the same allocation costs Llama -8.4%. Our findings reveal that the DLM training objective fundamentally reshapes layer dynamics relative to AR models, with direct consequences for compression and deployment. Code: github.com/Conzel/super-outlier-dlm.

---

## 论文详细总结（自动生成）

这篇论文对扩散语言模型（DLM）与自回归（AR）语言模型在内部激活动态上的本质差异进行了深入研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 LLaDA 等扩散语言模型的兴起，其性能已能与自回归模型媲美。然而，学术界对其内部表示如何传播、层间动态如何演变知之甚少。
*   **核心问题**：DLM 的内部激活动态是否遵循 AR 模型的规律（如“深度诅咒”，即深层由于训练不足而变得冗余）？这种动态差异如何影响模型的压缩（量化与剪枝）策略？
*   **整体含义**：研究发现 DLM 存在独特的“层坍缩”现象，其早期层高度冗余且由一个“超离群值”主导，这与 AR 模型的动态完全相反。

### 2. 论文提出的方法论
*   **层相似度分析**：使用每 token 的余弦相似度来衡量不同层之间隐藏状态的重合度，以此作为层冗余性的指标。
*   **超离群值（Super-Outlier）识别**：通过分析激活热图，发现在 LLaDA-8B 的前一半层中，存在一个持续高度激活的单一通道（Channel 3848），其强度远超其他通道。
*   **权重谱重尾性分析（HT-SR 理论）**：引入 Hill 估计器（$\alpha_{Hill}$）来衡量权重矩阵特征值分布的重尾程度。较小的 $\alpha_{Hill}$ 值代表更强的特征学习（过度训练），较大的值代表训练不足。
*   **受控预训练实验**：为了排除架构干扰，作者在相同条件下（相同数据、相同超参数）预训练了一对 160M 参数的 AR 和 DLM 模型，仅改变训练目标（掩码预测 vs. 下一个 token 预测）。

### 3. 实验设计
*   **对比模型**：
    *   大尺寸：LLaDA-8B (DLM) vs. Llama-3.1-8B (AR)；DREAM-7B (DLM) vs. Qwen-2.5-7B (AR)。
    *   小尺寸（受控组）：AR-160M vs. DLM-160M。
*   **数据集与任务**：
    *   **校准集**：C4 数据集（用于估计激活统计量）。
    *   **评估任务**：6 项常识问答（ARC-C, HellaSwag, PIQA, Winogrande, BoolQ, OBQA）和数学推理（GSM8K）。
*   **压缩方法**：使用 WANDA 进行剪枝，使用 GPTQ 进行量化。对比了“早期更稀疏（EIS）”和“后期更稀疏（DIS）”等不同稀疏度分配策略。

### 4. 资源与算力
*   **评估算力**：所有评估均在单张 **NVIDIA H100 GPU** 上完成。DLM 的评估耗时约 6 小时，AR 模型约 20 分钟。
*   **训练算力**：160M 参数模型的预训练使用了 **8 张 A100-80GB GPU**，在 FineWebEdu 数据集的 100B token 上进行训练。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了从 160M 到 8B 的参数规模，对比了多个模型家族，并进行了消融实验（如移除超离群值后的性能测试）。
*   **充分性与公平性**：实验设计非常严谨。特别是 160M 的受控实验排除了架构、初始化和训练数据的干扰，证明了观察到的现象确实源于“扩散训练目标”本身。

### 6. 主要结论与发现
*   **层坍缩与超离群值**：DLM（如 LLaDA）的早期层高度冗余，由一个巨大的超离群值主导。剪枝该离群值会导致模型彻底崩溃（GSM8K 准确率从 83% 降至 0%），而 AR 模型剪枝最大离群值仅有轻微下降。
*   **冗余模式反转**：AR 模型通常是深层冗余（训练不足），而 DLM 是早期层冗余（过度训练）。
*   **压缩鲁棒性极强**：DLM 对量化极其不敏感。在 3-bit GPTQ 量化下，LLaDA 的性能几乎无损（-1.8%），而 Llama-3.1-8B 则发生崩溃（-64.7%）。
*   **稀疏策略反转**：对于 DLM，将更多稀疏度分配给早期层（EIS）效果更好；而对于 AR 模型，分配给深层（DIS）才是最优。

### 7. 优点
*   **发现新颖**：首次识别并命名了 DLM 中的“超离群值”和“层坍缩”现象。
*   **因果论证有力**：通过 160M 的受控实验，清晰地将现象归因于训练目标，而非模型架构。
*   **实战指导意义强**：为 DLM 的高效部署提供了明确的量化和剪枝指导方案。

### 8. 不足与局限
*   **模型覆盖有限**：主要的发现集中在 LLaDA-8B 上。虽然有 160M 实验支撑，但目前市面上从头预训练的大规模 DLM 较少，结论在更多架构上的普适性仍待验证。
*   **超离群值的成因不明**：论文虽然指出了该离群值源于扩散训练目标，但并未从数学或理论层面解释为什么扩散目标会诱导产生这种极端的单一通道偏置。
*   **小模型未复现离群值**：160M 的受控模型虽然复现了层相似度模式，但未出现超离群值，说明该现象可能具有规模效应（Scale-dependent）。

（完）
