---
title: "FlowS: One-Step Motion Prediction via Local Transport Conditioning"
title_zh: FlowS：通过局部传输条件化实现单步运动预测
authors: "Leandro Di Bella, Adrian Munteanu, Bruno Cornelis"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26065v1"
tags: ["query:genad"]
score: 8.0
evidence: 自动驾驶的生成式运动预测
tldr: 针对自动驾驶中生成式运动预测的高精度、多模态和低延迟需求，本文提出 FlowS 框架。该方法通过“局部传输调节”策略，将复杂的模式发现转化为局部修正，利用在线学习的先验锚点和步长一致性位移场，实现了仅需单步推理的高质量预测。在 Waymo 数据集上达到了 SOTA 性能且推理速度达 75 FPS，解决了扩散模型推理慢的痛点。
selection_source: fresh_fetch
motivation: 现有的扩散模型在运动预测中虽能提供多样化结果，但因多步迭代导致延迟过高，难以满足安全关键型自动驾驶的实时性要求。
method: 提出一种条件流匹配框架，通过学习场景相关的先验锚点将全局预测简化为局部修正，并结合步长一致性位移场确保单步推理的准确性。
result: 在 Waymo 开放运动数据集上，FlowS 以单步推理实现了 0.4804 的 Soft mAP 和 75 FPS 的高帧率，达到了领域领先水平。
conclusion: 局部传输调节策略证明了通过将传输问题局部化，单步生成式运动预测可以在保证精度的同时满足实时自动驾驶的严苛要求。
---

## 摘要
生成式运动预测在现实世界的自动驾驶中必须同时满足三个要求：高精度、多样化的多模态未来以及严格受限的延迟。扩散模型满足前两个要求，但违反了第三个，因为它们需要数十到数百个去噪步骤。我们确定了一种解决这种矛盾的条件化策略：当底层的传输问题是局部的时候，单步积分是准确的。一个必须在单步内既发现正确的行为模式又跨越长距离位移的模型会积累巨大的离散化误差；将基础分布条件化，使其位于合理的未来附近，可以将问题简化为短程细化，即单步欧拉积分足以胜任的范畴。我们在 FlowS 中实例化了这种“局部传输条件化”，这是一个具有两种机制的条件流匹配框架。首先，一个在线的、基于场景条件化的学习先验为每个智能体生成 K 条校准的锚点轨迹，每条轨迹都已经接近一个合理的未来，从而将模式发现转化为局部修正。其次，步进一致的位移场强制执行半群自一致性，保证单步推理能够继承多步推理的精度。关键在于，将该场锚定在沿直线路径的学习先验上，可以产生稳定、低方差的训练目标，这与以往在弯曲扩散路径上遭受高方差自举信号困扰的自一致性方法不同。在 Waymo 开放运动数据集上，FlowS 通过单步推理实现了最先进的 Soft mAP (0.4804) 和 mAP (0.4703)（集成模型在 75 FPS 下），证明了局部传输条件化使单步生成式运动预测在安全至上的自动驾驶中具有实用性。代码和预训练模型将在论文接收后发布。

## Abstract
Generative motion prediction must satisfy three simultaneous requirements for real-world autonomy: high accuracy, diverse multimodal futures, and strictly bounded latency. Diffusion models meet the first two but violate the third, requiring tens to hundreds of denoising steps. We identify a conditioning strategy that resolves this tension: \textit{single-step integration is accurate when the underlying transport problem is local}. A model that must both discover the correct behavioral mode and traverse a long displacement in one step accumulates large discretization errors; conditioning the base distribution to lie near plausible futures reduces the problem to short-range refinement, the regime where a single Euler step suffices. We instantiate this \emph{local transport conditioning} in FlowS, a conditional flow matching framework with two mechanisms. First, an online, scene-conditioned learned prior emits $K$ calibrated anchor trajectories per agent, each already near a plausible future, converting mode discovery into local correction. Second, a step-consistent displacement field enforces semigroup self-consistency, guaranteeing that a single step inherits multi-step accuracy. Crucially, anchoring this field at learned priors along straight-line paths yields a {stable, low-variance} training target, unlike prior self-consistency methods that suffer from {high-variance bootstrap} signals on curved diffusion paths. On the Waymo Open Motion Dataset, FlowS achieves state-of-the-art Soft mAP {(0.4804) and mAP (0.4703) with ensemble at 75\,FPS} with single-step inference, demonstrating that local transport conditioning makes one-step generative motion prediction practical for safety-critical autonomy. Code and pretrained models will be released upon acceptance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FlowS** 的新型运动预测框架，旨在解决自动驾驶中生成式模型推理延迟过高的核心痛点。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动驾驶要求运动预测同时具备**高精度**、**多模态覆盖**和**极低延迟**。
*   **研究背景**：
    *   **扩散模型（Diffusion Models）**虽然在精度和多样性上表现优异，但需要数十次甚至上百次迭代，无法满足安全关键型系统的实时性要求。
    *   **流匹配（Flow Matching）**虽然比扩散模型快，但在单步推理（One-step）时，由于初始分布（通常是高斯噪声）距离目标过远，会导致巨大的离散化误差，难以平衡速度与精度。
*   **核心动机**：提出“局部传输条件化（Local Transport Conditioning）”策略。作者认为，只要传输路径足够短（局部化），单步积分（Euler step）就能达到极高的精度。

### 2. 方法论
FlowS 结合了条件流匹配（CFM）框架，并引入了两个关键机制：
*   **在线学习的场景条件先验（Learned Scene-Conditioned Prior）**：
    *   不再从无意义的高斯噪声开始，而是利用 Transformer 编码器提取的场景特征，生成 $K$ 个校准过的“锚点轨迹（Anchors）”。
    *   这些锚点已经位于潜在的未来轨迹附近，将“全局模式搜索”问题简化为“局部短程修正”。
*   **步长一致性位移场（Step-Consistent Displacement Field）**：
    *   引入一个位移场 $s_\psi$，通过强制执行**半群自一致性（Semigroup Self-consistency）**进行训练。
    *   **核心公式思想**：确保两个连续的小步更新与一个大步更新的结果一致（$T_{\Delta t} \circ T_{\Delta t} \approx T_{2\Delta t}$）。
    *   **稳定性优化**：将位移场锚定在基于先验的直线路径上，相比于在弯曲的扩散路径上进行自举（Bootstrap），这种方法提供了更稳定、低方差的训练目标。

### 3. 实验设计
*   **数据集**：使用自动驾驶领域最具挑战性的 **Waymo Open Motion Dataset (WOMD)**。
*   **Benchmark**：WOMD 官方测试集排行榜。
*   **对比方法**：
    *   **确定性/Transformer 类**：MTR, MTR++, Wayformer, QCNet 等。
    *   **扩散/生成类**：MotionDiffuser, Diffusion-Drive, BridgeDrive 等。
    *   **流匹配类**：TrajFlow, GoalFlow。
*   **评估指标**：Soft mAP, mAP, minADE, minFDE, Miss Rate (MR), Overlap Rate (OR)。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 (96GB)** GPU。
*   **训练配置**：Batch size 为 30（每张 GPU），训练 **40 个 epoch**，使用 ADAM 优化器。
*   **推理速度**：在单张 A100 上，单步推理速度约为 **75 FPS**（约 13.2 ms/场景），比 MTR 快 34%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在包含 48.7 万个场景的训练集上训练，在 4.4 万个验证集和测试集上评估。
    *   **消融实验**：分别验证了“学习先验”和“步长一致性位移场”的独立贡献及协同效应。
    *   **鲁棒性测试**：针对不同智能体密度（稀疏、中等、密集场景）进行了漂移分析。
    *   **稳定性验证**：报告了 7 次独立运行的均值和标准差，mAP 的相对标准差小于 0.4%。
*   **评价**：实验设计非常充分，涵盖了精度、速度、稳定性及定性可视化分析，对比基准涵盖了近年来的 SOTA 模型，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先**：FlowS (Ensemble) 在 Waymo 排行榜上取得了 **0.4804 Soft mAP** 和 **0.4703 mAP**，位居榜首。
*   **单步即巅峰**：通过半群约束，模型成功将多步推理的精度“蒸馏”到了单步中。实验显示，从 1 步增加到 16 步推理，mAP 仅提升了 0.0017，但延迟增加了近 10 倍，证明了单步推理的极高效率。
*   **协同效应**：学习先验减少了 36% 的传输距离，而位移场吸收了剩余的边际曲率，两者缺一不可。

### 7. 优点
*   **极速推理**：真正实现了生成式模型在复杂场景下的实时应用（75 FPS）。
*   **理论优雅**：利用直线路径（Optimal Transport）简化了自一致性训练的复杂度和不稳定性。
*   **即插即用**：其局部传输条件化的思想可以推广到其他基于流匹配的生成任务中。

### 8. 不足与局限
*   **长尾覆盖**：固定的 $K=6$ 模式先验可能无法完全覆盖极少数的罕见行为（如骑行者的罕见掉头）。
*   **独立预测风险**：目前主要针对每个智能体进行边缘分布预测，虽然有场景上下文，但缺乏显式的联合一致性约束，在极拥堵场景下可能存在轨迹重叠风险。
*   **泛化性待验证**：实验仅限于 Waymo 数据集，对于不同地图表示或不同传感器配置的泛化能力尚需进一步验证。

（完）
