---
title: "HDFlow: Hierarchical Diffusion-Flow Planning for Long-horizon Tasks"
title_zh: HDFlow：面向长时程任务的分层扩散-流规划
authors: "Nandiraju Gireesh, Yuanliang Ju, Chaoyi Xu, Weiheng Liu, Yuxuan Wan, He Wang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04525v1"
tags: ["query:diffpl"]
score: 9.0
evidence: 用于长程任务的分层扩散规划器
tldr: 针对长程、稀疏奖励任务中生成式规划器缺乏层次化分解且实时性差的问题，本文提出HDFlow框架。该方法结合了扩散模型强大的探索能力和修正流模型的高效生成速度。高层扩散规划器在潜空间生成战略子目标，低层修正流规划器则生成平滑轨迹。实验证明，HDFlow在家具组装及多种长程任务中表现优于现有技术，兼顾了规划质量与执行效率。
selection_source: fresh_fetch
motivation: 现有的生成式规划模型在处理长程任务时，缺乏有效的层次化分解机制，且受限于迭代去噪过程导致计算效率低下。
method: 提出一种分层规划框架，利用高层扩散模型生成潜空间子目标，并结合低层修正流模型通过ODE快速生成稠密轨迹。
result: 在仿真和真实环境下的家具组装任务以及多个长程基准测试中，HDFlow的性能显著超过了当前最先进的方法。
conclusion: HDFlow通过融合扩散与修正流模型的优势，为长程复杂任务提供了一种兼具强大探索能力与高效执行性能的层次化规划方案。
---

## 摘要
生成式模型的最新进展在为长时程、稀疏奖励任务生成行为规划方面展现出巨大潜力。尽管这些方法取得了显著成果，但它们往往缺乏分层分解的原则性框架，且由于其迭代去噪过程，难以满足实时执行的计算需求。在这项工作中，我们提出了分层扩散-流（HDFlow），这是一种新颖的分层规划框架，它优化利用了扩散模型（diffusion）和修正流模型（rectified flow）的优势，以克服单一范式生成式规划器的局限性。HDFlow 采用高层扩散规划器在学习到的潜空间中生成战略子目标序列，充分利用了扩散模型强大的探索能力。随后，这些子目标引导底层修正流规划器生成平滑且密集的轨迹，利用了基于常微分方程（ODE）的轨迹生成的快速与高效。我们在仿真和真实世界的四个具有挑战性的家具组装任务上评估了 HDFlow，结果表明其显著优于现有最先进的方法。此外，我们还在包含多种运动和操纵任务的两个长时程基准测试中展示了该方法的泛化能力。项目网站：https://hdflow-page.github.io/

## Abstract
Recent advances in generative models have shown promise in generating behavior plans for long-horizon, sparse reward tasks. While these approaches have achieved promising results, they often lack a principled framework for hierarchical decomposition and struggle with the computational demands of real-time execution, due to their iterative denoising process. In this work, we introduce Hierarchical Diffusion-Flow (HDFlow), a novel hierarchical planning framework that optimally leverages the strengths of diffusion and rectified flow models to overcome the limitations of single-paradigm generative planners. HDFlow employs a high-level diffusion planner to generate sequences of strategic subgoals in a learned latent space, capitalizing on diffusion's powerful exploratory capabilities. These subgoals then guide a low-level rectified flow planner that generates smooth and dense trajectories, exploiting the speed and efficiency of ordinary differential equation (ODE)-based trajectory generation. We evaluate HDFlow on four challenging furniture assembly tasks in both simulation and real-world, where it significantly outperforms state-of-the-art methods. Furthermore, we also showcase our method's generalizability on two long-horizon benchmarks comprising diverse locomotion and manipulation tasks. Project website: https://hdflow-page.github.io/

---

## 论文详细总结（自动生成）

这篇论文介绍了 **HDFlow**，一种专门为解决长时程（Long-horizon）、稀疏奖励机器人任务而设计的层次化生成式规划框架。

### 1. 核心问题与整体含义
*   **研究动机**：传统的扩散模型（Diffusion Models）在机器人规划中表现出色，但存在两个痛点：一是**计算效率低**，迭代去噪过程难以满足实时控制需求；二是**缺乏层次化结构**，在处理长时程任务时容易产生累积误差。
*   **核心问题**：单一的生成范式是否适用于规划的所有层级？作者认为高层规划需要“探索性”（扩散模型的强项），而底层执行需要“速度与平滑性”（修正流模型的强项）。
*   **整体含义**：HDFlow 通过结合扩散模型和修正流（Rectified Flow）模型，构建了一个既能进行长程战略推理又能快速生成执行轨迹的混合架构。

### 2. 方法论
HDFlow 的训练分为两个阶段：

*   **阶段 1：世界模型学习（World Model Learning）**
    *   使用 **RSSM（循环状态空间模型）** 结合 **DINOv2** 视觉编码器学习环境动力学。
    *   引入**对比学习损失（Contrastive Loss）**，将成功轨迹的中间状态拉向目标状态，使潜空间具备“进度感”。
    *   训练一个**逆动力学模型（IDM）**，用于将潜空间的状态转换映射为具体的机器人动作。
*   **阶段 2：层次化规划器训练**
    *   **高层规划器（High-Level Diffusion）**：在潜空间生成稀疏的子目标序列。为了解决稀疏奖励下的规划失败，引入了**流形感知（Manifold-Aware）的能量模型（EBM）引导**。EBM 给成功策略分配低能量，引导扩散过程远离“死胡同”，并通过流形投影防止生成的状态偏离可行域。
    *   **底层规划器（Low-Level Rectified Flow）**：接收高层给出的子目标，利用修正流模型通过求解常微分方程（ODE）快速生成稠密的潜空间轨迹。
*   **公式/流程简述**：
    1.  高层：$z_{1:K} \sim \text{Diffusion}(z_0, z_G)$，结合 $\nabla EBM$ 引导。
    2.  底层：$\tau \sim \text{Rectified Flow}(z_{k-1}, z_k)$，通过单步或少量步数 ODE 求解。
    3.  执行：通过 IDM 将 $\tau$ 转化为动作序列 $a_t$。

### 3. 实验设计
*   **数据集与场景**：
    *   **FurnitureBench**：复杂的家具组装任务（如装桌腿、灯泡、柜子），包含 Low/Med/High 三种随机化程度。
    *   **RLBench**：18 个操纵任务（如开抽屉、插木栓），测试泛化能力。
    *   **OGBench**：包含 AntMaze（运动）和多种操纵任务，测试长程推理。
*   **对比方法**：
    *   模仿学习：BC, Diffusion Policy, JUICER。
    *   扩散规划器：Diffuser, Decision Diffuser (DD), HDMI, SHD（简单层次化扩散）。
    *   SOTA 基准：PerAct, RVT, 3D Diffuser Actor 等。
*   **真实世界实验**：在 Franka Research 3 机器人上进行了实机验证。

### 4. 资源与算力
*   **文中说明**：论文提到了训练参数（如世界模型训练 100 epoch，规划器训练 10,000 epoch，Batch Size 为 64），并给出了推理延迟（如在 Lamp 任务中推理时间为 88ms/step）。
*   **未明确说明**：文中**未明确指出**具体的 GPU 型号、数量以及总的训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个家具组装任务、18 个 RLBench 任务和 14 个 OGBench 任务，实验基数大。
*   **消融实验**：非常详尽，包括：
    *   生成模型组合（全扩散 vs 全流 vs 混合）。
    *   核心组件贡献（有无 EBM、有无流形投影、有无对比学习世界模型）。
    *   视觉编码器对比（CNN, R3M, VIP, MAE, DINOv2）。
    *   子目标数量 $K$ 的影响。
*   **评价**：实验设计客观且充分，通过跨领域（运动+操纵）和跨环境（仿真+现实）的对比，有力证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **混合架构最优**：高层扩散+底层修正流的组合在成功率和推理速度之间达到了最佳平衡。
*   **流形感知至关重要**：在潜空间进行 EBM 引导时，如果没有流形投影，生成的子目标容易偏离可行区域，导致规划失败。
*   **性能领先**：在 FurnitureBench 的高难度设置下，HDFlow 的成功率显著高于之前的层次化扩散模型（如 SHD）。

### 7. 优点
*   **效率提升**：利用修正流模型替代底层扩散模型，显著降低了实时控制时的计算开销。
*   **理论支撑**：对 EBM 引导在潜空间中的“引导间隙（Guidance Gap）”给出了理论分析和证明。
*   **强泛化性**：不仅在操纵任务上表现好，在 AntMaze 等运动任务上也展现了极强的长程规划能力。

### 8. 不足与局限
*   **数据依赖**：训练 EBM 需要成功和失败的演示数据，这在某些极端复杂的任务中可能难以获取。
*   **固定步长**：子目标的时间间隔 $H$ 是预设的固定值，对于动作节奏变化剧烈的任务，动态调整 $H$ 可能会更有效。
*   **计算开销**：虽然底层很快，但高层扩散规划在每一轮重规划（Replanning）时仍有一定的计算负担。

（完）
