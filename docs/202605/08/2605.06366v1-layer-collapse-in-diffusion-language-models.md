---
title: Layer Collapse in Diffusion Language Models
title_zh: 扩散语言模型中的层崩溃
authors: "Alexander Conzelmann, Albert Catalan-Tatjer, Shiwei Liu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06366v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型与自回归模型激活动态的对比分析
tldr: 本研究揭示了扩散语言模型（DLM）中独特的“层坍缩”现象。以LLaDA-8B为例，研究发现其早期层存在由超大离群值主导的冗余激活模式，且该离群值对模型性能至关重要。与自回归模型不同，DLM的冗余源于过度训练而非训练不足。这种特性使DLM在量化和稀疏化方面表现出极强的鲁棒性，为模型压缩和部署提供了新思路。
selection_source: fresh_fetch
motivation: 旨在理解扩散语言模型与自回归模型在激活动态和层冗余性方面的本质差异。
method: 通过分析LLaDA-8B的激活模式，并结合剪枝、量化及受控预训练实验，探究层坍缩的成因及其对压缩的影响。
result: 发现DLM早期层存在关键的超大离群值，且在3位量化下性能损失远低于Llama模型，其最优稀疏策略与自回归模型完全相反。
conclusion: 扩散训练目标从根本上重塑了层动态，使DLM在模型压缩方面具有显著优势。
---

## 摘要
扩散语言模型 (DLMs) 近期已成为自回归 (AR) 语言模型极具竞争力的替代方案，但两者在激活动态方面的差异仍不为人所知。我们对 LLaDA-8B 中的这些动态进行了表征，并发现了一种显著的“层崩溃” (layer-collapse) 特性：少数浅层表现出高度相似且崩溃的激活模式，这种模式由一个在长 token 范围内持续存在的巨大“超级离群值” (super-outlier) 所主导。尽管该离群值看似冗余，但它至关重要：剪枝该离群值会导致输出退化为重复的随机 token 循环。矛盾的是，LLaDA 中的层总体上包含更多冗余表示，且冗余在浅层中最为显著——这与 AR 模型相反，在 AR 模型中，深层由于训练不足而变得冗余。我们的分析表明，DLM 中的层崩溃并非由训练不足驱动，而是由过度训练驱动：一个主导的离群值成为了不可或缺的信息载体，而其余表示则崩溃为冗余结构。这些发现具有重要的实际意义，并已通过受控预训练实验得到验证。DLM 对压缩具有惊人的鲁棒性：在 3-bit GPTQ 量化下，LLaDA 在 GSM8K 上的性能仅下降了 1.8%，而 Llama-3.1-8B 则下降了 64.7%。最佳稀疏度分配在不同模型家族之间也发生了反转：在 50% 的平均稀疏度下，为 LLaDA 的浅层分配更多稀疏度比相反策略的性能高出 8.4%，而同样的分配策略会导致 Llama 性能下降 8.4%。我们的研究结果表明，与 AR 模型相比，DLM 的训练目标从根本上重塑了层动态，这对模型压缩和部署具有直接影响。代码：github.com/Conzel/super-outlier-dlm。

## Abstract
Diffusion language models (DLMs) have recently emerged as competitive alternatives to autoregressive (AR) language models, yet differences in their activation dynamics remain poorly understood. We characterize these dynamics in LLaDA-8B and identify a striking layer-collapse property: a few early layers exhibit highly similar, collapsed activation patterns dominated by a single large super-outlier persisting over a long token range. Despite its apparent redundancy, this outlier is critical: pruning it causes outputs to degrade into repetitive random token loops. Paradoxically, layers in LLaDA contain more redundant representations overall, with redundancy most pronounced in earlier layers -- the reverse of AR models, where deeper layers grow redundant due to undertraining. Our analysis indicates that layer collapse in DLMs is not driven by undertraining but by overtraining: a dominant outlier becomes an indispensable information carrier while remaining representations collapse into redundant structure. These findings have strong practical implications, verified through controlled pre-training experiments. DLMs are surprisingly robust to compression: LLaDA under 3-bit GPTQ quantization drops only -1.8% on GSM8K, whereas Llama-3.1-8B drops -64.7%. Optimal sparsity allocation also reverses between families: at 50% average sparsity, allocating more to early layers in LLaDA yields +8.4% over the reverse strategy, while the same allocation costs Llama -8.4%. Our findings reveal that the DLM training objective fundamentally reshapes layer dynamics relative to AR models, with direct consequences for compression and deployment. Code: github.com/Conzel/super-outlier-dlm.

---

## 论文详细总结（自动生成）

这篇论文对扩散语言模型（DLM）的内部激活动态进行了深入分析，揭示了其与传统自回归（AR）模型截然不同的结构特性。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：扩散语言模型（如 LLaDA）近期表现出可媲美自回归模型（如 Llama）的性能，且具备并行生成和避免“逆转诅咒”等优势。
*   **核心问题**：尽管性能强劲，但 DLM 的内部激活动态和层间冗余性尚不明确。研究者试图探究：DLM 的层级表示结构是否遵循 AR 模型的规律（如深层冗余的“深度诅咒”），以及这些特性如何影响模型的压缩与部署。
*   **动机**：通过对比 DLM 与 AR 模型的激活动态，填补理论空白，并为 DLM 的高效压缩提供实践指导。

### 2. 论文提出的方法论
*   **层相似度分析**：使用每 token 的余弦相似度来衡量不同层之间隐藏状态的重合度，以此作为层冗余性的指标。
*   **超级离群值（Super-Outlier）识别**：通过分析激活热图，识别出在长 token 范围内持续占据主导地位的单一激活通道。
*   **权重谱密度分析（HT-SR 理论）**：引入 Hill 估计器 ($\alpha_{Hill}$) 来衡量权重的重构程度。较小的 $\alpha_{Hill}$ 值代表权重谱具有更重的尾部，暗示该层经过了更充分的特征学习（甚至过度训练）。
*   **受控预训练实验**：为了排除架构干扰，作者在相同条件下（数据集、超参数、架构）分别训练了一个 1.6 亿参数的 AR 模型和 DLM 模型，进行“双生子”对比。

### 3. 实验设计
*   **对比模型**：
    *   大尺寸模型：LLaDA-8B (DLM) vs. Llama-3.1-8B (AR)；DREAM-7B (DLM) vs. Qwen-2.5-7B (AR)。
    *   小尺寸受控模型：AR-160M vs. DLM-160M。
*   **数据集**：
    *   校准集：C4 数据集。
    *   评估集：常识问答（ARC-C, HellaSwag, PIQA, Winogrande, BoolQ, OBQA）和数学推理（GSM8K）。
*   **压缩方法**：
    *   剪枝：WANDA 算法。
    *   量化：GPTQ 算法。
*   **稀疏策略对比**：均匀稀疏、浅层更稀疏（EIS）、深层更稀疏（DIS）。

### 4. 资源与算力
*   **评估算力**：所有评估均在单张 **NVIDIA H100 GPU** 上完成。DLM 的评估耗时约 6 小时，AR 模型约 20 分钟。
*   **预训练算力**：1.6 亿参数模型的预训练使用了 **8 张 A100-80GB GPU**，在 FineWebEdu 数据集的 100B token 上进行训练。
*   **大规模训练**：文中提到大规模 DLM 从头预训练需要极高的计算资源，因此主要分析了已有的开源模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.6 亿到 80 亿参数的不同规模模型。
*   **维度丰富**：不仅对比了激活动态，还进行了剪枝、量化、不同稀疏分配策略以及权重谱分析。
*   **充分性**：通过受控实验（160M 模型对）排除了训练数据和架构的干扰，证明了观察到的现象确实源于“扩散训练目标”本身。实验设计客观、公平，对比基准清晰。

### 6. 论文的主要结论与发现
*   **层崩溃现象**：DLM（如 LLaDA）的早期层表现出极高的相似度，存在严重的“层崩溃”。
*   **超级离群值的关键性**：LLaDA 的早期层由一个极大的离群通道（ch 3848）主导。剪枝该通道会导致模型彻底崩溃（输出重复循环），而 AR 模型剪枝最大离群值仅导致轻微性能下降。
*   **冗余逻辑反转**：AR 模型通常深层更冗余（训练不足），而 DLM 则是**浅层更冗余**（由于过度训练导致的结构坍缩）。
*   **压缩鲁棒性**：DLM 对量化极其鲁棒。在 3-bit 量化下，LLaDA 性能几乎无损（-1.8%），而 Llama 性能骤降（-64.7%）。
*   **最优策略反转**：对于 DLM，**“浅层多剪枝、深层少剪枝”**（EIS）是最佳策略，这与 AR 模型的经验完全相反。

### 7. 优点
*   **视角独特**：首次系统性地揭示了 DLM 与 AR 模型在激活动态上的本质区别。
*   **实践指导意义强**：直接给出了 DLM 压缩的最优分配方案，具有很高的工程价值。
*   **严谨的受控实验**：通过 160M 模型的对比实验，有力地证明了结论的普适性，而非特定模型的偶然特性。

### 8. 不足与局限
*   **模型覆盖有限**：由于从头预训练 DLM 成本极高，大规模分析主要集中在 LLaDA-8B 上。
*   **规模效应**：超级离群值现象在 1.6 亿参数的小模型中未观察到，说明该现象可能仅在模型达到一定规模（如 >7B）时才会显现。
*   **成因待深入**：虽然确定了扩散目标是诱因，但扩散过程中的具体哪个环节（如掩码策略、时间步分布）导致了超级离群值的产生，仍需进一步研究。

（完）
