---
title: Continuous Latent Diffusion Language Model
title_zh: 连续潜扩散语言模型
authors: "Hongcan Guo, Qinyu Zhao, Yian Zhao, Shen Nie, Rui Zhu, Qiushan Guo, Feng Wang, Tao Yang, Hengshuang Zhao, Guoqiang Wei, Yan Zeng"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06548v1"
tags: ["query:ardm"]
score: 9.0
evidence: 具有块因果DiT的分层潜扩散语言模型，用于文本生成
tldr: 针对自回归语言模型固定生成顺序及现有非自回归模型在效率和全局建模上的不足，本文提出Cola DLM。该模型采用分层潜在扩散架构，通过Text VAE将文本映射至连续空间，利用块因果DiT建模全局语义先验，实现了语义组织与文本生成的解耦。实验证明其在2B参数规模下具有优异的扩展性和生成质量，为离散文本与连续模态的统一建模提供了新路径。
selection_source: fresh_fetch
motivation: 旨在克服自回归模型固定生成顺序的局限，并解决现有非自回归模型在生成效率、可扩展表示学习和全局语义建模方面的挑战。
method: 提出一种分层潜在扩散模型，结合Text VAE进行文本-潜在空间映射，并利用块因果DiT在连续空间内进行全局语义先验建模。
result: 在约20亿参数规模和多个基准测试中，Cola DLM展现出强大的扩展能力，性能可比肩严格对齐的自回归模型和LLaDA基准。
conclusion: 分层连续潜在先验建模是令牌级语言建模的有效替代方案，为实现离散文本与连续模态的统一建模奠定了基础。
---

## 摘要
大语言模型在自回归范式下取得了显著成功，但高质量的文本生成并不一定需要绑定在固定的从左到右的顺序上。现有的替代方案在同时实现生成效率、可扩展的表示学习以及有效的全局语义建模方面仍面临挑战。我们提出了 Cola DLM，这是一种分层潜扩散语言模型，通过分层信息分解来构建文本生成框架。Cola DLM 首先利用 Text VAE 学习稳定的文本到潜变量映射，然后使用块因果 DiT 在连续潜空间中建模全局语义先验，最后通过条件解码生成文本。从统一的马尔可夫路径视角来看，其扩散过程执行的是潜先验传输，而非词元级的观测恢复，从而将全局语义组织与局部文本实现分离开来。这种设计产生了一种更灵活的非自回归归纳偏置，支持连续空间中的语义压缩和先验拟合，并能自然地扩展到其他连续模态。通过涵盖 4 个研究问题、8 个基准测试、严格匹配的约 20 亿参数自回归和 LLaDA 基线，以及高达约 2000 EFLOPs 的扩展曲线实验，我们确定了 Cola DLM 的有效整体配置，并验证了其在文本生成方面的强大扩展行为。综上所述，实验结果确立了分层连续潜先验建模作为严格词元级语言建模的一种原则性替代方案，其中生成质量和扩展行为可能比似然度更能反映模型能力，同时也为离散文本与连续模态之间的统一建模提供了一条具体的路径。

## Abstract
Large language models have achieved remarkable success under the autoregressive paradigm, yet high-quality text generation need not be tied to a fixed left-to-right order. Existing alternatives still struggle to jointly achieve generation efficiency, scalable representation learning, and effective global semantic modeling. We propose Cola DLM, a hierarchical latent diffusion language model that frames text generation through hierarchical information decomposition. Cola DLM first learns a stable text-to-latent mapping with a Text VAE, then models a global semantic prior in continuous latent space with a block-causal DiT, and finally generates text through conditional decoding. From a unified Markov-path perspective, its diffusion process performs latent prior transport rather than token-level observation recovery, thereby separating global semantic organization from local textual realization. This design yields a more flexible non-autoregressive inductive bias, supports semantic compression and prior fitting in continuous space, and naturally extends to other continuous modalities. Through experiments spanning 4 research questions, 8 benchmarks, strictly matched ~2B-parameter autoregressive and LLaDA baselines, and scaling curves up to about 2000 EFLOPs, we identify an effective overall configuration of Cola DLM and verify its strong scaling behavior for text generation. Taken together, the results establish hierarchical continuous latent prior modeling as a principled alternative to strictly token-level language modeling, where generation quality and scaling behavior may better reflect model capability than likelihood, while also suggesting a concrete path toward unified modeling across discrete text and continuous modalities.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Cola DLM**（Continuous Latent Diffusion Language Model）的新型语言模型架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统的自回归（AR）语言模型受限于固定的从左到右生成顺序，导致推理具有串行依赖性，且在处理非单调任务（如填空、编辑）时存在局限。现有的离散扩散模型生成效率较低，而连续扩散模型多用于恢复词元嵌入，缺乏显式的分层语义建模。
*   **研究动机**：提出一种分层信息分解框架，将文本生成解耦为“全局语义组织”和“局部文本实现”，利用连续潜空间（Latent Space）的几何特性来提升非自回归生成的灵活性和扩展性。

### 2. 方法论：核心思想与关键技术
Cola DLM 采用分层潜变量建模，分为三个核心组件和两个训练阶段：
*   **核心思想**：扩散过程不再用于词元级的观测恢复，而是用于**潜先验传输（Latent Prior Transport）**。
*   **关键技术细节**：
    1.  **Text VAE**：建立文本与连续潜变量 $z_0$ 之间的稳定映射。采用严格因果的编码器和解码器，并引入 BERT 风格的掩码损失（Masking Loss）防止语义坍缩。
    2.  **块因果 DiT（Block-causal DiT）**：在连续潜空间中建模先验。它允许块内双向注意力以提高并行效率，同时保持块间的因果关系。
    3.  **流匹配（Flow Matching, FM）**：作为先验学习的求解器，通过学习向量场将简单的高斯分布传输到复杂的潜变量分布。
*   **算法流程**：
    *   **训练阶段 1**：预训练 Text VAE，稳定潜空间接口。
    *   **训练阶段 2**：联合训练 VAE 和 DiT，通过参考编码器正则化防止潜空间漂移。
    *   **推理过程**：编码前缀（Prefix） $\rightarrow$ 在潜空间中按块生成响应潜变量 $\rightarrow$ 条件解码器生成最终文本。

### 3. 实验设计
*   **数据集/场景**：涵盖了 8 个主流基准测试，包括：
    *   **延续性任务**：LAMBADA。
    *   **推理与知识**：MMLU、SIQA、SQuAD、Story Cloze、OBQA、RACE、HellaSwag。
*   **对比基线（Baselines）**：
    *   **AR（自回归）**：严格匹配参数规模（约 2B）的 LLaMA 风格模型。
    *   **LLaDA**：一种先进的离散扩散语言模型基线。
*   **评估协议**：采用统一的 Few-shot 生成式评估，而非单纯依赖似然度（Likelihood），以更客观地衡量生成质量。

### 4. 资源与算力
*   **模型规模**：DiT 部分约 1.8B 参数，VAE 部分约 500M 参数，总规模约 2.3B。
*   **训练细节**：训练步数高达 1,000,000 步，最大序列长度 512。
*   **算力消耗**：论文展示了高达 **2000 EFLOPs** 的扩展曲线（Scaling Curves）。虽然未明确列出具体的 GPU 型号和数量，但从其所属的 ByteDance Seed 团队背景及 EFLOPs 规模推断，使用了大规模 GPU 集群（如 H100 或 A100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 4 个核心研究问题（RQ）进行了详尽实验，包括潜空间维度、动态 vs 静态潜空间、噪声调度（Noise Schedule）、块大小（Block Size）、无分类器引导（CFG）比例等。
*   **充分性与公平性**：实验设计非常严谨，确保了 Cola DLM 与 AR、LLaDA 基线在预训练数据、分词器、计算预算和参数规模上完全对齐。通过多维度的消融实验验证了各组件的有效性，实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **扩展性**：Cola DLM 展现了强大的 Scaling 潜力，在计算量增加时，其性能提升曲线优于或可比肩自回归模型。
*   **语义建模**：验证了潜空间中存在全局语义结构，且这种结构随维度增加呈现系统性漂移。
*   **生成质量 vs 似然度**：发现连续潜模型中存在“似然度与生成质量脱节”的现象，即 PPL 较高时仍能生成高质量文本，这强调了生成式评估的重要性。
*   **统一建模潜力**：初步实验证明该框架可自然扩展至图像等连续模态，实现原生多模态统一建模。

### 7. 优点（亮点）
*   **架构创新**：成功将扩散模型从词元级提升到分层潜变量级，解决了非自回归模型在长文本生成上的连贯性问题。
*   **解耦设计**：将语义规划与文本实现分离，使得模型在处理复杂推理任务时更具优势。
*   **灵活性**：块因果设计平衡了推理效率与因果依赖，且对连续模态具有天然的亲和力。

### 8. 不足与局限
*   **似然度评估难题**：由于变分推断的间隙，该模型在传统 PPL 指标上表现较差，难以直接使用标准似然度进行模型选择。
*   **潜空间压缩瓶颈**：实验显示，当 VAE 压缩倍率增加（Patch Size > 1）时，对非整除长度的前缀处理不够鲁棒，存在边界对齐问题。
*   **训练复杂性**：分层架构涉及 VAE 和 DiT 的协同优化，相比纯 AR 模型，训练流程和超参数调优（如噪声调度校准）更为复杂。

（完）
