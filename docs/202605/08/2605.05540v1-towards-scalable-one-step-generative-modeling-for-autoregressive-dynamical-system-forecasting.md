---
title: Towards Scalable One-Step Generative Modeling for Autoregressive Dynamical System Forecasting
title_zh: 迈向自回归动力系统预测的可扩展单步生成建模
authors: "Tianyue Yang, Xiao Xue"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05540v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于动力系统预测的无潜变量自回归生成代理模型
tldr: 针对高维物理动力学模拟中神经算子易漂移且生成模型推理慢的问题，本文提出MeLISA。这是一种基于像素空间MeanFlow的无潜变量自回归生成代理模型，通过单步评估实现快速预测。结合窗口一致性和时间增量一致性损失，MeLISA在保持高推理速度的同时，显著提升了长程滚动的统计准确性，并在高分辨率湍流基准上展现了优异的扩展性。
selection_source: fresh_fetch
motivation: 现有的物理动力学代理模型在长期预测中容易产生统计漂移，且基于扩散的生成模型推理效率较低。
method: 提出MeLISA模型，利用单步MeanFlow生成技术结合窗口一致性与时间增量一致性损失，实现高效且物理一致的自回归预测。
result: 在多项高分辨率湍流基准测试中，MeLISA在预测精度和能量谱等统计指标上均优于神经算子，且推理速度极快。
conclusion: MeLISA为大规模、高效率且具备长期统计稳定性的动力学系统建模提供了一种可扩展的单步生成方案。
---

## 摘要
高维物理动力学的快速代理建模不仅需要较低的短期误差：有用的模型必须在保持长轨迹统计结构的同时实现高效的外推（rollout）。神经算子（Neural operators）提供了低成本的自回归预测，但在湍流状态下可能会发生漂移；而滚动扩散（rolling diffusion）和潜变量生成代理模型虽然可以表示随机转移，但代价是需要多步去噪、噪声调度设计或辅助压缩模型。我们提出了 MeLISA（MeanFlow 长期不变时空一致性自回归模型），这是一种基于像素空间 MeanFlow 构建的无潜变量自回归生成代理模型。MeLISA 定义了一个分块随机转移核，通过单次模型评估即可生成每个预测块，从而在推理时避免了潜变量编码器和迭代扩散求解器。为了稳定长时程外推，MeLISA 结合了窗口一致性 MeanFlow 目标（从部分观测的时间窗口学习条件时空生成）和时间增量一致性损失（约束多滞后有限增量并针对时间相关结构）。我们在两个高分辨率基准测试上评估了采用紧凑型 UNet 和可扩展 DiT 骨干网络的 MeLISA：256 × 256 的扩展二维 Kolmogorov 流和 192 × 192 的湍流槽道流切片。MeLISA 在短期预测精度和长时程统计指标（包括能谱、湍动能和与混合速率相关的动力学）方面均优于神经算子基准，同时实现了与神经算子相当、甚至在某些情况下更快的推理速度。参数量为 3.

## Abstract
Fast surrogate modeling for high-dimensional physical dynamics requires more than low short-term error: useful models must roll out efficiently while preserving the statistical structure of long trajectories. Neural operators provide inexpensive autoregressive forecasts but can drift in turbulent regimes, whereas rolling diffusion and latent generative surrogates can represent stochastic transitions at the cost of multi-step denoising, noise-schedule design, or auxiliary compression models. We propose MeanFlow Long-term Invariant Spatiotemporal Consistency Autoregressive Models (MeLISA), a latent-free autoregressive generative surrogate built on pixel-space MeanFlow. MeLISA defines a blockwise stochastic transition kernel that generates each forecast block with a single model evaluation, avoiding latent encoders and iterative diffusion solvers at inference time. To stabilize long-horizon rollouts, MeLISA combines a Window-Consistency MeanFlow objective that learns conditional spatiotemporal generation from partially observed temporal windows with a Time Increment Consistency loss that constrains multi-lag finite increments and targets temporal-correlation structure. We evaluate MeLISA with compact UNet and scalable DiT backbones on two high-resolution benchmarks, extended 2D Kolmogorov flow at $256 \times 256$ and turbulent channel-flow slice at $192 \times 192$. MeLISA outperforms neural-operator baselines on short-term forecasting accuracy and long-horizon statistical metrics, including energy spectra, turbulent kinetic energy, and mixing-rate-related dynamics, while achieving inference speeds comparable to, and in some cases faster than, neural operators. Compact 3.7-5.7M-parameter variants already deliver strong parameter efficiency, and DiT variants provide a scalable path up to 150M parameters. Overall, MeLISA benefits both rollout efficiency and long-horizon statistical accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Scalable One-Step Generative Modeling for Autoregressive Dynamical System Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是如何构建既**高效**（推理快）又**物理一致**（长期预测不漂移）的高维动力学系统（如流体动力学）代理模型。
*   **研究动机**：传统的数值模拟（DNS）成本极高。现有的数据驱动方法中，**神经算子**（如 FNO）虽然推理快，但在处理湍流等复杂动力学时，长期自回归预测容易产生累积误差和统计漂移；而**生成式扩散模型**虽然能捕捉随机转移，但推理时需要多步迭代去噪，计算开销巨大，且往往依赖复杂的潜变量空间或噪声调度。
*   **整体含义**：本文提出了 **MeLISA** 框架，旨在通过“单步生成”技术（MeanFlow）实现与神经算子相当的推理速度，同时利用专门设计的损失函数确保长期物理统计特性的准确性。

### 2. 方法论：核心思想与关键技术
MeLISA 的核心思想是将动力学预测建模为一个基于像素空间的单步随机转移核。
*   **核心架构**：基于 **Pixel MeanFlow (p-MF)**。不同于需要多步迭代的扩散模型，MeanFlow 通过预测时间平均速度，实现从基础分布到目标分布的单步（1-NFE）映射。
*   **关键技术 1：窗口一致性 MeanFlow (WinC-MF)**
    *   将单帧生成扩展为窗口条件生成。在训练时，对时间窗口内的帧进行随机掩码（Masking），迫使模型根据部分观测的上下文预测完整的时空演化，从而学习非平凡的时空转移规律。
*   **关键技术 2：时间增量一致性 (TIC) 损失**
    *   **公式逻辑**：$L_{TIC} = \sum \kappa_w \| \Delta x_{true} - \Delta \hat{x}_{pred} \|^2$，其中 $\Delta x$ 是不同时间滞后（lag）下的状态增量。
    *   **作用**：该损失函数直接约束了预测轨迹的二阶时间结构（如时间协方差和混合行为），弥补了逐帧重建损失无法捕捉长期相关性的缺陷。
*   **算法流程**：在推理时，模型接收一个上下文窗口，通过一次前向计算生成未来的一组预测块（Block），然后以自回归方式滑动窗口进行长程预测。

### 3. 实验设计
*   **数据集/场景**：
    1.  **2D Kolmogorov Flow (256×256)**：受迫二维流体，具有典型的多尺度湍流特征。
    2.  **Turbulent Channel Flow (192×192)**：三维湍流槽道流的二维切片，具有强烈的非马尔可夫效应和边界层特征。
*   **Benchmark 与对比方法**：
    *   **对比基准**：经典的确定性自回归模型——傅里叶神经算子 (**FNO**)、U型神经算子 (**UNO**) 和局部傅里叶神经算子 (**Local FNO**)。
    *   **评估指标**：短期指标（相对 L2 误差、SSIM）和长期统计指标（能谱 PSDD、湍动能 TKED、混合速率 MRD）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 个 GH200 节点**（每个节点 4 张 GPU，共 128 张卡），训练时长约为 **4 小时**。作者指出，这相当于在单张 GH200 上训练约 5 天。
*   **推理算力**：推理测试在单张 **NVIDIA RTX 4090** 上完成。
*   **模型规模**：提供了从紧凑型 UNet（3.7M - 5.7M 参数）到可扩展 DiT（10M - 150M 参数）的多种变体。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同性质的高分辨率流体基准上进行了测试，涵盖了从短期预测到长达数千帧的长期外推。
*   **消融实验**：详细探讨了 TIC 损失的作用（证明其能防止预测向均值塌陷）、WinC-MF 的掩码机制以及模型规模（Scaling）对性能的影响。
*   **客观性与公平性**：对比实验中，神经算子基准的参数量与 MeLISA 变体进行了匹配，且输入输出窗口设置一致。实验不仅关注像素误差，还引入了物理学家关心的能谱和动能分布，评估维度全面且客观。

### 6. 主要结论与发现
*   **性能超越**：MeLISA 在短期精度上匹配或优于神经算子，在长期统计指标（如高频能谱恢复、边界层动能分布）上显著领先。
*   **推理极快**：由于采用单步生成（1-NFE），MeLISA 的推理速度与神经算子处于同一量级，甚至在某些配置下更快，远超传统的扩散模型。
*   **稳定性强**：在长达 10,000 帧的压力测试中，MeLISA 依然能保持物理合理的流场结构，而部分神经算子基准会出现数值崩溃。
*   **可扩展性**：基于 DiT 的变体展示了良好的 Scaling Law 潜力，随着参数量增加，长期统计特性的捕捉能力进一步提升。

### 7. 优点（亮点）
*   **无潜变量设计**：直接在像素空间操作，避免了 VAE 编码器带来的信息损失和训练复杂性。
*   **单步高效性**：解决了生成式模型在动力学预测中推理慢的痛点，使其具备实际工程应用价值。
*   **物理启发损失**：TIC 损失的设计深刻理解了湍流统计特性的需求，而非盲目追求像素级匹配。

### 8. 不足与局限
*   **维度限制**：目前实验仅限于 2D 场（虽然包含 3D 数据的切片），尚未在全 3D 动力学系统上验证。
*   **训练成本**：虽然推理快，但训练 MeLISA（尤其是大规模 DiT 变体）所需的算力显著高于简单的神经算子。
*   **非马尔可夫性处理**：虽然在槽道流切片上表现良好，但对于具有极长记忆效应的系统，其窗口长度的选取和计算开销之间仍存在权衡。

（完）
