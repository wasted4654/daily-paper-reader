---
title: "Language Generation as Optimal Control: Closed-Loop Diffusion in Latent Control Space"
title_zh: 语言生成作为最优控制：潜在控制空间中的闭环扩散
authors: "ZiYi Dong, Yuliang Huang, Weijian Deng, Xiangyang Ji, Liang Lin, Pengxu Wei"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14531v2"
tags: ["query:ardm"]
score: 8.0
evidence: 分析自回归和扩散模型的统一理论视角
tldr: 本研究将语言生成重新表述为随机最优控制问题，揭示了自回归和扩散模型在效率与保真度等方面的理论局限。提出了一种基于Hamilton-Jacobi-Bellman (HJB) 方程近似解的闭环控制策略，并利用流匹配在纠偏潜控制空间内求解最优轨迹。通过引入带有全局积分算子的 Manta-LM，实现了高保真且高效的并行文本生成，在多项任务中表现出优异的稳定性与可控性。
selection_source: fresh_fetch
motivation: 旨在通过随机最优控制理论统一分析并解决现有生成模型在效率、保真度及误差累积方面的理论局限。
method: 将生成过程建模为闭环控制，利用流匹配在纠偏潜空间中近似 HJB 方程的最优策略，并结合全局积分算子构建 Manta-LM。
result: 该方法在语言建模和条件生成任务中取得了强劲表现，同时显著提升了采样效率和生成的稳定性。
conclusion: 将语言生成视为最优控制问题为开发兼具高保真度和高效率的并行生成模型提供了一条有效的理论与实践路径。
---

## 摘要
本研究将语言生成重新表述为一个随机最优控制问题，提供了一个统一的理论视角来分析自回归模型和扩散模型，并从轨迹奇异性、伴随状态消失和梯度缺失的结合角度，解释了它们的局限性（效率-保真度悖论、不可逆误差传播、优化可处理性与保真度）。为了解决这些问题，我们近似求解了 Hamilton-Jacobi-Bellman (HJB) 方程，从而得到一个作为闭环控制器的最优策略。为了绕过直接求解 HJB 偏微分方程的不可处理性，我们在修正的潜在控制空间中采用流匹配（Flow Matching）作为最优轨迹求解器。这使得我们带有全局积分算子的 Manta-LM 能够近似全局向量场，从而有效地实现了一个同时具备高保真文本生成和高效、低成本并行采样的模型。实验结果表明，我们的方法在语言建模和条件生成任务上取得了强劲的性能，同时表现出更高的稳定性、效率和可控性。

## Abstract
This work reformulates language generation as a stochastic optimal control problem, providing a unified theoretical perspective to analyze autoregressive and diffusion models and explain their limitations (Efficiency-Fidelity Paradox, Irreversibility Error Propagation, Optimization Tractability and Fidelity) in terms of combination of trajectory singularity, adjoint state vanishing, and gradient absence. To address these issues, we approximate the solution to the Hamilton-Jacobi-Bellman (HJB) equation, yielding an optimal policy that acts as a closed-loop controller. To bypass the intractability of directly solving the HJB PDE, we employ Flow Matching as the optimal trajectory solver within the rectified latent control space. This allows our Manta-LM with Global Integral Operator to approximate the global vector field, effectively realizing a model that simultaneously achieves high-fidelity text generation and efficient, low-cost parallel sampling. Empirically, our method achieves strong performance on language modeling and conditional generation tasks, while exhibiting improved stability, efficiency, and controllability.

---

## 论文详细总结（自动生成）

这是一份关于论文《Language Generation as Optimal Control: Closed-Loop Diffusion in Latent Control Space》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心动机在于**统一并解决现有语言生成范式的理论缺陷**。作者指出，当前的自回归模型（ARM）和扩散语言模型（DLM）面临三大挑战：
*   **效率-保真度悖论**：ARM 受限于串行解码（$O(N)$ 复杂度）；DLM 虽然理论上可并行，但依赖启发式高斯去噪，需要数百步迭代。
*   **不可逆误差传播**：ARM 是开环控制，一旦生成错误（幻觉）便会持续累积；标准 DLM 缺乏全局引导，容易漂移到低密度语义区。
*   **优化可处理性与保真度**：离散空间缺乏梯度，而直接在嵌入空间进行连续扩散会因空间稀疏、非流形特征导致“刚性（Stiffness）”问题，产生高曲率轨迹和量化误差。

论文通过**随机最优控制（Stochastic Optimal Control, SOC）**理论，将语言生成重新定义为寻找最优控制律的过程，旨在实现高保真、高效率且具备自我纠偏能力的生成。

### 2. 方法论
论文提出了 **Manta-LM** 框架，其核心思想包含三个支柱：
*   **流形纠偏（Manifold Rectification）**：利用正则化变分自编码器（TextVAE）将离散、病态的 Token 空间映射到一个紧凑、局部欧几里得的**潜在控制空间**。这为定义梯度算子 $\nabla_z$ 提供了几何基础，降低了系统的拓扑刚性。
*   **流匹配作为拉格朗日求解器**：直接求解 Hamilton-Jacobi-Bellman (HJB) 偏微分方程是不可行的。作者利用对偶性，采用**条件流匹配（CFM）**来近似 HJB 的解。通过学习一个常速度向量场，使生成轨迹尽可能接近 Wasserstein 空间中的测地线（直线），从而最小化传输能量。
*   **Transformer 作为全局积分算子**：论文从理论上证明了 Transformer 的自注意力机制本质上是对全局梯度流所需的积分算子的离散近似。模型利用 Transformer 学习全局向量场，实现**闭环反馈控制**。

### 3. 实验设计
*   **数据集**：
    *   **零样本语言建模**：OpenWebText（训练），评估于 LAMBADA, WikiText-2/103, PTB, 1BW。
    *   **条件文本生成**：QQP（改写）、Quasar-T（问题生成）、Wiki-Auto（文本简化）、CCD（对话）。
    *   **边界值问题（BVP）**：WikiText-2 和 OpenWebText 的加噪版本（用于纠错实验）及长文本填充。
*   **Benchmark 与对比方法**：
    *   对比了 GPT-2（自回归基准）、D3PM、SEDD、MD4、RADD（离散/连续扩散基准）以及 DiffuSeq 等非自回归模型。
*   **评估指标**：Perplexity (PPL)、BLEU、ROUGE-L、BERTScore、MAUVE（用于填充）、以及用于衡量纠错能力的奖励模型（RM）分数。

### 4. 资源与算力
*   **训练配置**：使用了 **2 张 NVIDIA RTX A6000 GPU**。
*   **训练时长**：总计训练了 80k 步，消耗约 **658.6 GPU 小时**。
*   **推理效率**：在单张 A6000 上，batch size 为 64 时，单样本延迟约为 29.33ms，每步延迟仅 1.47ms。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础密度估计（PPL）到复杂条件生成、再到极具挑战性的边界值问题（纠错与填充）的多维度测试。
*   **消融与分析**：
    *   进行了**刚性分析（Stiffness Analysis）**，证明了潜在空间比原始嵌入空间更平滑。
    *   探讨了 **CFG（分类器自由引导）强度**与采样步数之间的平衡。
    *   可视化了优化景观（Optimization Landscapes），对比了 ARM、离散扩散与 Manta-LM 的平滑度。
*   **充分性评价**：实验设计非常全面且具有针对性，不仅验证了性能指标，还通过几何分析验证了其理论假设的正确性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：Manta-LM 在 1BW 等大规模数据集上的 PPL 显著优于 GPT-2 和现有的扩散模型（如 MD4）。
*   **闭环优势**：在纠错任务中，Manta-LM 表现出极强的自我修正能力，能以极小的编辑距离将偏离流形的错误状态拉回正确分布。
*   **高效并行**：推理成本与序列长度脱钩（$O(1)$ 步数），在 1024 token 长度下比高效基准快 4.6 倍。
*   **几何平滑**：TextVAE 成功实现了流形纠偏，使得流匹配能够以更大的步长进行稳定积分，解决了扩散模型常见的 NFE（函数调用次数）过高问题。

### 7. 优点
*   **理论严谨**：将语言生成提升到最优控制的数学高度，为理解不同生成范式提供了统一框架。
*   **架构创新**：通过潜在空间纠偏和流匹配的结合，解决了离散数据连续化的几何难题。
*   **多功能性**：天然支持“边界值问题”（如任意位置的文本填充），无需像 ARM 那样进行专门的 FIM（Fill-in-the-Middle）预训练。

### 8. 不足与局限
*   **代理任务限制**：虽然在生成任务上表现优异，但对于需要频繁中断、逐步推理的“智能体任务”（如工具调用），其全局规划特性可能不如 ARM 灵活。
*   **VAE 依赖**：模型的上限受限于 TextVAE 的重建质量，尽管实验显示重建率接近 100%，但在极大规模词汇表或多语言场景下可能存在压力。
*   **模型规模**：实验主要集中在 1B 参数量级以下，尚未在大规模参数（如 70B+）上验证其 Scaling Law 表现。

（完）
