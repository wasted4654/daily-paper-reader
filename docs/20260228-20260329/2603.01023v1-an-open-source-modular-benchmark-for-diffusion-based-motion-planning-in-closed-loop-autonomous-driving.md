---
title: An Open-Source Modular Benchmark for Diffusion-Based Motion Planning in Closed-Loop Autonomous Driving
title_zh: 一种用于闭环自动驾驶中基于扩散模型的运动规划的开源模块化基准测试
authors: "Yun Li, Simon Thompson, Yidu Zhang, Ehsan Javanmardi, Manabu Tsukada"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01023v1"
tags: ["query:genad"]
score: 10.0
evidence: 自动驾驶中基于扩散模型的运动规划模块化基准
tldr: 本研究针对基于扩散模型的运动规划器在闭环自动驾驶系统中评估不足的问题，提出了一个开源模块化基准测试框架。通过ONNX GraphSurgeon将庞大的扩散模型分解为三个独立模块，并在Autoware（ROS 2）中实现了原生C++的DPM-Solver++去噪循环。该框架支持运行时参数配置和去噪过程的可观测性，解决了传统单体部署的黑盒问题。实验证明，编码器缓存可降低3.2倍延迟，二阶求解器在低步数下显著提升了规划精度，为扩散模型从仿真到实车部署提供了高效路径。
selection_source: fresh_fetch
motivation: 现有的扩散模型规划器评估往往忽略了ROS 2通信延迟和实时调度约束，且单体ONNX部署缺乏参数配置的灵活性。
method: 利用ONNX GraphSurgeon将模型分解为独立模块，并在Autoware生产级堆栈中以原生C++重构去噪循环，实现模块化部署与实时观测。
result: "实验表明编码器缓存使延迟降低了3.2倍，且二阶DPM-Solver++在仅3步去噪时比一阶方法减少了41%的终点位移误差。"
conclusion: 该开源基准测试框架打破了扩散模型部署的黑盒限制，为在真实自动驾驶系统中优化和验证扩散模型规划器提供了重要工具。
---

## 摘要
基于扩散模型的运动规划器在 nuPlan 等基准测试中取得了最先进的结果，但其在闭环生产级自动驾驶技术栈中的评估在很大程度上仍未得到探索。现有的评估往往抽象掉了 ROS 2 的通信延迟和实时调度约束，而单体式 ONNX 部署在导出时会冻结所有求解器参数。我们提出了一个开源的模块化基准测试来填补这两个空白：利用 ONNX GraphSurgeon，我们将一个包含 18,398 个节点的单体扩散规划器分解为三个独立的可执行模块，并用原生 C++ 重新实现了 DPM-Solver++ 去噪循环。该系统作为 ROS 2 节点集成在 Autoware（部署在全球真实车辆上的开源自动驾驶技术栈）中，实现了无需模型重新编译的运行时可配置求解器参数，以及去噪过程的逐步可观测性，打破了单体部署的黑盒。与 CARLA 等独立模拟器中的评估不同，我们的基准测试在生产级技术栈中运行，并通过 AWSIM 闭环仿真进行了验证。通过在六种步数配置（N ∈ {3, 5, 7, 10, 15, 20}）下对 DPM-Solver++（一阶和二阶）和 DDIM 进行系统比较，我们表明编码器缓存可使延迟降低 3.2 倍，且在 N=3 时，二阶求解相比一阶求解可将 FDE 降低 41%。完整的代码库将开源发布，为从仿真基准测试到实车部署提供了一条直接路径。

## Abstract
Diffusion-based motion planners have achieved state-of-the-art results on benchmarks such as nuPlan, yet their evaluation within closed-loop production autonomous driving stacks remains largely unexplored. Existing evaluations abstract away ROS 2 communication latency and real-time scheduling constraints, while monolithic ONNX deployment freezes all solver parameters at export time. We present an open-source modular benchmark that addresses both gaps: using ONNX GraphSurgeon, we decompose a monolithic 18,398 node diffusion planner into three independently executable modules and reimplement the DPM-Solver++ denoising loop in native C++. Integrated as a ROS 2 node within Autoware, the open-source AD stack deployed on real vehicles worldwide, the system enables runtime-configurable solver parameters without model recompilation and per-step observability of the denoising process, breaking the black box of monolithic deployment. Unlike evaluations in standalone simulators such as CARLA, our benchmark operates within a production-grade stack and is validated through AWSIM closed-loop simulation. Through systematic comparison of DPM-Solver++ (first- and second-order) and DDIM across six step-count configurations (N in {3, 5, 7, 10, 15, 20}), we show that encoder caching yields a 3.2x latency reduction, and that second-order solving reduces FDE by 41% at N=3 compared to first-order. The complete codebase will be released as open-source, providing a direct path from simulation benchmarks to real-vehicle deployment.

---

## 论文详细总结（自动生成）

### 论文总结：用于闭环自动驾驶中基于扩散模型的运动规划的开源模块化基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
扩散模型（Diffusion Models）在自动驾驶运动规划领域表现优异，但从学术基准（如 nuPlan）走向实际生产级自动驾驶技术栈（如 Autoware）面临两大挑战：
*   **环境抽象化：** 现有评估多在 Python 模拟器中进行，忽略了 ROS 2 通信延迟、传感器预处理和实时调度等真实约束。
*   **部署黑盒化：** 现有的部署方案通常将整个去噪循环（包含上万个节点）导出为单个单体式 ONNX 图。这导致去噪步数（$N$）、求解器阶数等参数在导出时被冻结，无法在运行时动态调整，且去噪过程不可观测。

本研究旨在通过**模块化分解**和**原生 C++ 实现**，打破扩散模型部署的“黑盒”，提供一个支持实时配置和闭环评估的开源基准测试框架。

#### 2. 方法论
核心思想是将迭代去噪循环从计算图中剥离，移至外部 C++ 逻辑中控制。
*   **图分解（Graph Decomposition）：** 利用 `ONNX GraphSurgeon` 将包含 18,398 个节点的单体模型分解为三个独立模块：
    1.  **上下文编码器（Context Encoder）：** 处理场景输入，生成嵌入向量。
    2.  **DiT 核心（DiT Core）：** 执行单步去噪推理。
    3.  **转向指示器预测器（Turn Indicator Predictor）：** 后处理信号预测。
*   **原生 C++ 求解器：** 在 ROS 2 节点中用 C++ 重新实现 `DPM-Solver++`。该求解器支持运行时配置步数（$N \in [1, 50]$）、求解器阶数（一阶/二阶）和噪声调度参数。
*   **编码器缓存（Encoder Caching）：** 由于上下文嵌入在单次规划周期内是不变的，模块化设计允许编码器仅运行一次并缓存结果，供后续 $N$ 次 DiT 调用复用，大幅降低计算开销。
*   **系统集成：** 作为插件集成到 Autoware 架构中，通过 ROS 2 与感知、地图和控制模块交互。

#### 3. 实验设计
*   **实验环境：** 使用 AWSIM 闭环模拟器，地图为东京西新宿（Nishishinjuku）城市道路场景。
*   **基准测试（Benchmark）：** 在 Autoware 生产级堆栈中运行，对比单体架构与模块化架构。
*   **对比方法：** 
    *   **求解器：** DPM-Solver++（一阶、二阶）与 DDIM。
    *   **配置：** 不同的去噪步数 $N \in \{3, 5, 7, 10, 15, 20\}$。
*   **评估指标：** 终点位移误差（FDE）、平均位移误差（ADE）、推理延迟（Latency）。

#### 4. 资源与算力
*   **硬件平台：** Intel Core i9-12900KS CPU, 32 GB RAM, NVIDIA GeForce RTX 3090 Ti GPU。
*   **软件框架：** Docker 容器环境，Ubuntu 22.04, ROS 2 Humble, Autoware, ONNX Runtime 1.23。
*   **算力说明：** 论文重点在于推理阶段的优化。模块化模型在 CPU 上运行（使用 ONNX Runtime CPU 提供程序），而单体模型作为对比在 GPU 上运行（TensorRT）。文中未提及模型训练的具体时长，因为其核心贡献在于部署与评估框架。

#### 5. 实验数量与充分性
*   **实验规模：** 
    *   进行了 50 帧离线数据的系统性求解器对比。
    *   在 AWSIM 中进行了超过 600 个规划周期的闭环仿真测试。
*   **充分性：** 实验涵盖了数值等效性验证（误差 < $10^{-5}$）、消融实验（编码器缓存的影响）、求解器性能对比以及端到端闭环验证。
*   **客观性：** 通过在相同硬件上对比 CPU 端的架构差异，排除了硬件加速的干扰，客观证明了模块化设计在减少冗余计算方面的优势。

#### 6. 主要结论与发现
*   **延迟大幅降低：** 编码器缓存消除了 90.9% 的冗余计算，使推理延迟降低了 **3.2 倍**。在 $N=10$ 时，延迟从 328ms 降至 53ms，成功进入 100ms 的实时规划预算。
*   **高阶求解器优势：** 在极低步数（$N=3$）下，二阶 DPM-Solver++ 比一阶方法减少了 **41% 的 FDE**。
*   **调度策略至关重要：** 优化的时间步调度比简单的提前停止（Early Stopping）在质量上提升了 7 倍。
*   **随时规划（Anytime Planning）：** 证明了可以根据剩余计算预算动态调整去噪步数，而无需重新编译模型。

#### 7. 优点
*   **开源与实用性：** 直接对接 Autoware 这一工业级开源堆栈，为研究人员提供了从仿真到实车的直接路径。
*   **打破黑盒：** 提供了去噪过程的逐步可观测性，有助于深入理解扩散模型在动态场景下的收敛行为。
*   **灵活性：** 求解器与神经网络权重解耦，支持在不重新训练的情况下测试新的采样算法。

#### 8. 不足与局限
*   **场景覆盖有限：** 目前仅在单一城市场景（西新宿）进行了验证，缺乏更多极端场景（Edge Cases）的测试。
*   **GPU 性能分析不足：** 虽然 CPU 端的优化显著，但缺乏在不同车载嵌入式 GPU（如 Jetson Orin）上的详细延迟分析和算子融合对比。
*   **模型依赖：** 该分解方法依赖于模型具有“上下文-去噪核心”的重复结构，虽然这是目前主流扩散规划器的通用结构，但对于某些特殊架构可能需要额外适配。

（完）
