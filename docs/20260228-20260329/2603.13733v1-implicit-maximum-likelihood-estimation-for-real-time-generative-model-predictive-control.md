---
title: Implicit Maximum Likelihood Estimation for Real-time Generative Model Predictive Control
title_zh: 用于实时生成式模型预测控制的隐式极大似然估计
authors: "Grayson Lee, Minh Bui, Shuzi Zhou, Yankai Li, Mo Chen, Ke Li"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13733v1"
tags: ["query:diffpl"]
score: 8.0
evidence: 扩散模型与其他生成式规划器的比较
tldr: 针对扩散模型在轨迹规划中推理速度慢、难以满足实时模型预测控制（MPC）需求的问题，本文提出使用隐式极大似然估计（IMLE）作为替代生成建模方法。IMLE在保持多模态分布覆盖能力的同时，推理速度提升了两个数量级。实验证明，IMLE在离线强化学习基准测试中表现优异，并在实时人体导航任务中实现了快速且自适应的规划。
selection_source: fresh_fetch
motivation: 扩散模型因迭代去噪导致推理缓慢，限制了其在需要快速响应的实时闭环控制场景中的应用。
method: 采用隐式极大似然估计（IMLE）构建生成式规划模型，利用其单次前向传播即可生成样本的特性提升效率。
result: IMLE在离线强化学习基准上达到了与扩散模型相当的性能，且推理速度快了两个数量级，支持实时闭环规划。
conclusion: IMLE是一种高效的生成式建模方案，能够有效解决复杂动态环境下实时模型预测控制的计算瓶颈。
---

## 摘要
基于扩散的模型近期在轨迹规划中表现出强大的性能，因为它们能够捕捉复杂行为的多样化、多模态分布。这些模型的一个关键局限是其推理速度较慢，这是由迭代去噪过程导致的。这使得它们不太适用于实时应用，例如闭环模型预测控制（MPC），在这种场景下，规划必须快速生成并持续适应不断变化的环境。在本文中，我们研究了隐式极大似然估计（IMLE）作为规划的一种替代生成建模方法。IMLE 提供了强大的模式覆盖能力，同时使推理速度提高了两个数量级，使其特别适用于实时 MPC 任务。我们的结果表明，与标准的基于扩散的规划器相比，IMLE 在标准离线强化学习基准测试中取得了具有竞争力的性能，同时在开环和闭环设置中都显著提高了规划速度。我们进一步在实时运行的闭环人体导航场景中验证了 IMLE，展示了它如何在动态环境中实现快速且自适应的规划生成。

## Abstract
Diffusion-based models have recently shown strong performance in trajectory planning, as they are capable of capturing diverse, multimodal distributions of complex behaviors. A key limitation of these models is their slow inference speed, which results from the iterative denoising process. This makes them less suitable for real-time applications such as closed-loop model predictive control (MPC), where plans must be generated quickly and adapted continuously to a changing environment. In this paper, we investigate Implicit Maximum Likelihood Estimation (IMLE) as an alternative generative modeling approach for planning. IMLE offers strong mode coverage while enabling inference that is two orders of magnitude faster, making it particularly well suited for real-time MPC tasks. Our results demonstrate that IMLE achieves competitive performance on standard offline reinforcement learning benchmarks compared to the standard diffusion-based planner, while substantially improving planning speed in both open-loop and closed-loop settings. We further validate IMLE in a closed-loop human navigation scenario, operating in real-time, demonstrating how it enables rapid and adaptive plan generation in dynamic environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种基于**隐式极大似然估计（Implicit Maximum Likelihood Estimation, IMLE）**的实时生成式模型预测控制（MPC）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前主流的生成式规划器（如基于扩散模型 Diffuser）虽然能捕捉复杂的多模态行为分布，但其**推理速度极慢**。由于扩散模型依赖于多步迭代去噪过程，难以满足实时闭环控制（如机器人避障、动态导航）对高频重规划（Replanning）的需求。
*   **研究动机**：寻找一种既能保持多模态覆盖能力（避免模式崩溃），又能实现**单次前向传播（Single-shot）**快速推理的生成模型，从而将生成式规划推向实时应用领域。

### 2. 方法论
*   **核心思想**：采用 **IMLE** 替代扩散模型。IMLE 像 GAN 一样通过单次前向传播生成样本，但其目标函数通过“为每个训练数据点寻找最近的生成样本”来强制覆盖所有数据模式，从而避免了 GAN 的模式崩溃问题。
*   **关键技术细节**：
    *   **条件生成（cIMLE）**：使用 U-Net 架构作为生成器，通过 **FiLM（特征线性调制）** 层将初始状态、目标等条件注入网络。
    *   **奖励加权 IMLE（Reward-Weighted IMLE）**：为了从混合质量的离线数据中学习最优策略，论文在训练损失函数中引入了权重 $w_i$。采用 **Boltzmann（指数）加权**（基于 $R(\tau)$ 累积回报），使模型偏向于生成高回报轨迹，这在理论上接近“控制即推理（CAI）”的最优分布。
    *   **集成 MPC**：
        *   在离线 RL 中，生成一批候选轨迹并使用学习到的奖励函数进行打分排序（Score-Ranked）。
        *   在动态导航中，将 IMLE 作为 **MPPI（模型预测路径积分）** 的建议分布，结合控制屏障函数（CBF）进行安全性微调。

### 3. 实验设计
*   **数据集与场景**：
    *   **D4RL MuJoCo 运动基准**：HalfCheetah, Hopper, Walker2d（Medium-Expert, Medium, Medium-Replay 等子集）。
    *   **Maze2D**：测试稀疏奖励下的长时程规划能力。
    *   **行人导航（ETH/UCY）**：模拟动态环境下的避障。
    *   **真实世界部署**：在移动机器人上进行实时人体避障导航。
*   **对比方法（Benchmarks）**：
    *   **生成式规划器**：Diffuser（扩散模型）、CoBL-Diffusion、条件流匹配（CFM）。
    *   **离线 RL 算法**：CQL, IQL, Decision Transformer (DT) 等。
    *   **传统控制**：高斯分布采样的 MPPI。

### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：AMD EPYC 9655 (Zen 5)。
    *   **GPU**：NVIDIA H100（使用 MIG 实例，分配 20GB 显存）。
*   **训练时长**：D4RL 任务训练 100k 步；导航任务训练 250k 至 1M 步。
*   **推理效率**：在 GPU 上，IMLE 的采样频率可达 **50Hz - 130Hz**，比扩散模型快约 **100 倍**。

### 5. 实验数量与充分性
*   **实验规模**：D4RL 实验重复了 **150 个随机种子**，远超同类论文通常使用的 3-5 个种子，统计显著性极强。
*   **消融实验**：对比了无权重、线性权重和指数权重对性能的影响，验证了 Boltzmann 加权的有效性。
*   **多样性测试**：在不同时程（Horizon = 20, 40, 80）和不同安全半径下进行了鲁棒性测试。
*   **充分性评价**：实验设计覆盖了从标准仿真基准到真实机器人部署的全流程，对比方法包含最新的扩散模型变体，评价客观且公平。

### 6. 主要结论与发现
*   **速度优势**：IMLE 在 CPU 和 GPU 上的推理速度均比扩散模型快两个数量级，使实时闭环 MPC 成为可能。
*   **性能持平**：在 D4RL  locomotion 和 Maze2D 任务中，IMLE 的得分与 Diffuser 相当，甚至在某些任务（如 Maze2D）中略优。
*   **实时导航**：在机器人实机上实现了 **50Hz** 的重规划频率，能够灵活应对不可预测的行人运动。
*   **热启动价值**：IMLE 生成的轨迹作为 MPPI 的热启动（Warm-start）方案，比传统的直线或高斯采样能显著降低碰撞率并提高轨迹平滑度。

### 7. 优点
*   **极高的推理效率**：解决了生成式规划器落地的最大痛点（计算延迟）。
*   **训练简单**：无需扩散模型复杂的噪声调度（Noise Schedule）或迭代去噪，训练目标直接且稳定。
*   **强大的模式覆盖**：继承了 IMLE 的特性，能够生成多样化的避障路径，不会陷入单一解。
*   **工程实用性**：提供了 JAX 实现，进一步压榨了硬件性能。

### 8. 不足与局限
*   **分布外（OOD）风险**：与所有生成式模型一样，如果最优轨迹完全不在训练数据分布内，IMLE 难以生成超越数据上限的轨迹（尽管奖励加权有所缓解）。
*   **动力学一致性**：在行人导航中，IMLE 生成的是运动学轨迹，虽然通过 MPPI 进行了微调，但未在生成阶段显式约束机器人的非完整性动力学。
*   **数据依赖**：需要高质量或覆盖广泛的多模态数据集来发挥其模式覆盖的优势。

（完）
