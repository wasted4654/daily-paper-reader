---
title: Compositional Visual Planning via Inference-Time Diffusion Scaling
title_zh: 通过推理时扩散缩放实现的组合式视觉规划
authors: "Yixin Zhang, Yunhao Luo, Utkarsh Aashu Mishra, Woo Chul Shin, Yongxin Chen, Danfei Xu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02646v1"
tags: ["query:diffpl"]
score: 8.0
evidence: 用于机器人规划的扩散模型
tldr: 针对扩散模型在长程机器人规划中面临的计算限制和数据短缺问题，本文提出了一种无需训练的组合式视觉规划框架。该方法通过在推理阶段利用Tweedie估计值强制执行重叠视频块之间的边界一致性，而非在噪声空间进行平均，从而解决了现有方法的不稳定性。通过在因子图上结合同步与异步消息传递，实现了长程任务的全局一致性规划，并能有效泛化至未见过的起始-目标组合。
selection_source: fresh_fetch
motivation: 现有的组合式扩散规划方法在噪声空间进行片段拼接，导致长程规划中出现全局不一致和不稳定的问题。
method: 提出在推理时基于Tweedie估计值对重叠视频块进行边界对齐，并利用链式因子图的消息传递机制实现全局引导。
result: 该方法在无需额外训练的情况下，显著优于现有基准，并能成功泛化到训练集中未出现的长程起始-目标组合。
conclusion: 通过在推理阶段对估计的干净数据施加一致性约束，可以有效地将短程视频扩散模型扩展到稳定且一致的长程视觉规划任务中。
---

## 摘要
扩散模型在短时界机器人规划方面表现出色，但由于计算限制和训练数据有限，将其扩展到长时界任务仍然具有挑战性。现有的组合方法通过分别对每个组件进行去噪并对重叠区域取平均值来缝合短片段。然而，由于分解假设在噪声数据空间中失效，这种方法存在不稳定性，导致全局规划不一致。我们提出，稳定组合生成的关键在于对估计的干净数据（Tweedie 估计值）而非噪声中间状态强制执行边界一致性。我们的方法将长时界规划建模为在重叠视频块的链式结构因子图上的推理，其中预训练的短时界视频扩散模型提供局部先验。在推理阶段，我们通过一种作用于 Tweedie 估计值的同步和异步消息传递的新颖组合来强制执行边界一致性，在无需额外训练的情况下产生全局一致的引导。我们的免训练框架在现有基准上表现出显著改进，能够有效泛化到原始训练数据中不存在的未见过的起点-目标组合。

## Abstract
Diffusion models excel at short-horizon robot planning, yet scaling them to long-horizon tasks remains challenging due to computational constraints and limited training data. Existing compositional approaches stitch together short segments by separately denoising each component and averaging overlapping regions. However, this suffers from instability as the factorization assumption breaks down in noisy data space, leading to inconsistent global plans. We propose that the key to stable compositional generation lies in enforcing boundary agreement on the estimated clean data (Tweedie estimates) rather than on noisy intermediate states. Our method formulates long-horizon planning as inference over a chain-structured factor graph of overlapping video chunks, where pretrained short-horizon video diffusion models provide local priors. At inference time, we enforce boundary agreement through a novel combination of synchronous and asynchronous message passing that operates on Tweedie estimates, producing globally consistent guidance without requiring additional training. Our training-free framework demonstrates significant improvements over existing baselines, effectively generalizing to unseen start-goal combinations that were not present in the original training data. Project website: https://comp-visual-planning.github.io/

---

## 论文详细总结（自动生成）

以下是对论文《Compositional Visual Planning via Inference-Time Diffusion Scaling》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：扩散模型在短时界（short-horizon）机器人规划中表现优异，但在长时界（long-horizon）任务中面临计算量巨大和训练数据稀缺的挑战。
*   **核心问题**：现有的组合式生成方法（如 DiffCollage）通过在“噪声空间”对重叠区域取平均值来拼接短片段。论文指出，这种做法在理论上存在缺陷（即 **Noisy-Bethe Gap**），因为扩散过程中的噪声会破坏因子分解假设，导致生成的全局规划在时间上不连贯、边界不匹配且容易崩溃。
*   **整体含义**：本文提出了一种在推理阶段通过“干净数据估计值”（Tweedie estimates）进行边界对齐的免训练框架，旨在实现稳定、长程且具备泛化能力的视觉规划。

### 2. 论文提出的方法论
*   **核心思想**：将长程规划建模为一个链式结构的因子图（Factor Graph），利用预训练的短程视频扩散模型作为局部先验，在推理时通过消息传递（Message Passing）强制执行边界一致性。
*   **关键技术细节**：
    *   **Tweedie 估计对齐**：不在噪声状态 $x_t$ 上操作，而是在每一步去噪时预测的原始数据估计值 $x_{0|t}$ 上施加约束。
    *   **同步消息传递（Synchronous MP）**：将整个规划链视为一个高斯线性系统，通过并行更新驱动全局残差趋于零。
    *   **异步消息传递（Asynchronous MP）**：引入类似强化学习中时序差分（TD）更新的机制，使用停止梯度（stop-gradient）和折扣因子，提高长链优化的稳定性和收敛速度。
    *   **扩散球引导（Diffusion-Sphere Guidance, DSG）**：在 DDIM 采样过程中，利用消息传递产生的残差梯度引导采样，平衡生成的多样性与约束的满足度。
*   **算法流程**：预训练一个短程视频扩散模型 $\rightarrow$ 推理时将长程任务分解为重叠块 $\rightarrow$ 执行同步/异步联合消息传递优化边界 $\rightarrow$ 得到连贯视频规划 $\rightarrow$ 通过逆动力学模型转化为机器人动作。

### 3. 实验设计
*   **数据集/场景**：基于 ManiSkill 机器人仿真环境开发了组合规划基准，包含四个场景：**Tool-Use（工具使用）**、**Drawer（抽屉操作）**、**Cube（方块排序）**、**Puzzle（拼图）**。
*   **Benchmark 设置**：共 100 个任务，分为 18 个分布内（In-Distribution, IND）任务和 82 个分布外（Out-of-Distribution, OOD）任务。OOD 任务要求模型组合训练集中从未见过的起始-目标组合。
*   **对比方法**：
    *   **策略学习类**：LCBC、LCDP（语言条件）、GCBC、GCDP（目标条件）。
    *   **组合生成类**：DiffCollage/GSC（基于噪声空间拼接）、CompDiffuser（基于训练的联合去噪）。
*   **真实世界实验**：在 Franka Emika Panda 机器人上进行了实机部署验证。

### 4. 资源与算力
*   **训练算力**：使用了 **NVIDIA H200 GPU** 进行模型训练（训练时长未明确给出，但提到迭代次数为 1M 次）。
*   **推理/部署算力**：使用单张 **NVIDIA L40S GPU**。
*   **推理效率**：由于涉及推理时的反向传播优化，采样时间比简单的 DiffCollage 长（例如在 Puzzle 场景下约为 61.8 秒），但通过批处理优化了重叠块的计算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同难度的仿真场景，每种场景包含大量 IND 和 OOD 测试用例，总计 100 个任务，每个任务运行 30 个回合。
*   **消融实验**：针对“同步 vs 异步消息传递”的效果、以及“采样步数（50 到 300 步）”对成功率的影响做了详细对比。
*   **客观性与公平性**：所有实验均使用 5 个随机种子并报告标准差；对比基准涵盖了从传统的行为克隆到最先进的扩散策略，实验设计较为全面且客观。

### 6. 论文的主要结论与发现
*   **性能飞跃**：在 OOD 任务上，本文方法显著优于所有基线。例如在 Tool-Use 场景下，OOD 成功率达到 96%，而最强的策略学习基线仅为 42%，DiffCollage 则几乎为 0%。
*   **视觉质量**：通过 VBench++ 评估，本文方法在运动平滑度和背景一致性等动态指标上远超现有组合方法。
*   **推理缩放**：实验证明，增加推理时的采样步数（Test-time compute）能直接提升规划的成功率，体现了良好的 Scaling 特性。

### 7. 优点
*   **免训练（Training-free）**：无需针对长程任务重新训练或微调模型，具有极强的即插即用性。
*   **理论严谨**：通过证明“Noisy-Bethe Gap”揭示了现有方法的失效原因，并提出了基于 Tweedie 估计的合理解释。
*   **泛化能力强**：能够通过组合短程技能解决从未见过的长程复杂任务。

### 8. 不足与局限
*   **推理延迟**：由于需要在推理阶段计算梯度并进行多次迭代，实时性较差，不适合对反应速度要求极高的动态环境。
*   **超参数依赖**：需要手动指定组合片段的数量 $n$，且对 Tweedie 估计的准确性有一定依赖。
*   **计算开销**：相比于直接采样，反向传播带来的计算负担限制了其在低功耗嵌入式设备上的应用。

（完）
