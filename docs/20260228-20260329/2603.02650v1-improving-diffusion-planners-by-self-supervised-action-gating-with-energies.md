---
title: Improving Diffusion Planners by Self-Supervised Action Gating with Energies
title_zh: 通过基于能量的自监督动作门控改进扩散规划器
authors: "Yuan Lu, Dongqi Han, Yansen Wang, Dongsheng Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02650v1"
tags: ["query:diffpl"]
score: 9.0
evidence: 改进扩散规划器
tldr: 针对扩散规划器在离线强化学习中因价值引导导致轨迹动力学不一致的问题，本文提出SAGE方法。SAGE利用自监督学习的JEPA架构，在推理阶段通过潜在预测误差评估候选轨迹的可行性，并将其作为能量分数与价值估计结合进行重排序。该方法无需环境交互或重新训练策略，显著提升了扩散规划器在多种任务中的性能和鲁棒性。
selection_source: fresh_fetch
motivation: 扩散规划器常因追求高价值而选择在动力学上不一致的轨迹，导致执行过程脆弱且易失败。
method: 提出SAGE方法，通过训练JEPA编码器和潜在预测器，在推理时利用潜在预测误差作为能量分数对候选轨迹进行重排序。
result: 在运动、导航和操作等多个基准测试中，SAGE显著增强了现有扩散规划器的性能和鲁棒性。
conclusion: SAGE提供了一种无需环境回放或策略重训的即插即用方案，有效解决了扩散规划中的可行性评估难题。
---

## 摘要
扩散规划器是离线强化学习的一种强大方法，但当价值引导的选择倾向于得分高但在局部与环境动力学不一致的轨迹时，它们可能会失效，从而导致执行的脆弱性。我们提出了基于能量的自监督动作门控（SAGE），这是一种推理阶段的重排序方法，利用潜在一致性信号来惩罚动力学不一致的规划。SAGE 在离线状态序列上训练联合嵌入预测架构（JEPA）编码器，并针对短时域转换训练动作条件潜在预测器。在测试时，SAGE 根据潜在预测误差为每个采样的候选轨迹分配一个能量值，并将该可行性评分与价值估计相结合来选择动作。SAGE 可以集成到现有的扩散规划流水线中，这些流水线能够采样轨迹并通过价值评分选择动作；它不需要环境交互回放，也不需要重新训练策略。在运动、导航和操作基准测试中，SAGE 提高了扩散规划器的性能和鲁棒性。

## Abstract
Diffusion planners are a strong approach for offline reinforcement learning, but they can fail when value-guided selection favours trajectories that score well yet are locally inconsistent with the environment dynamics, resulting in brittle execution. We propose Self-supervised Action Gating with Energies (SAGE), an inference-time re-ranking method that penalises dynamically inconsistent plans using a latent consistency signal. SAGE trains a Joint-Embedding Predictive Architecture (JEPA) encoder on offline state sequences and an action-conditioned latent predictor for short horizon transitions. At test time, SAGE assigns each sampled candidate an energy given by its latent prediction error and combines this feasibility score with value estimates to select actions. SAGE can integrate into existing diffusion planning pipelines that can sample trajectories and select actions via value scoring; it requires no environment rollouts and no policy re-training. Across locomotion, navigation, and manipulation benchmarks, SAGE improves the performance and robustness of diffusion planners.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Diffusion Planners by Self-Supervised Action Gating with Energies》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：扩散规划器（Diffusion Planners）在离线强化学习中表现优异，但存在一个关键缺陷：**可行性与价值的脱节**。在推理阶段，规划器通常采样多个候选轨迹并根据价值函数（Critic）评分。然而，价值函数往往会误选那些看起来回报很高、但在局部物理动力学上不可行（即“幻觉”轨迹）的方案。
*   **核心目标**：提出一种名为 **SAGE** 的方法，在推理阶段显式地分离“可行性”与“价值”信号，通过惩罚动力学不一致的规划来增强执行的鲁棒性。

### 2. 方法论
SAGE 是一种推理阶段的重排序（Re-ranking）方法，包含两个离线训练阶段和一个推理阶段：
*   **阶段 I：自监督表示学习（JEPA）**：
    *   使用联合嵌入预测架构（JEPA）在离线状态序列上训练编码器。
    *   通过掩码（Masking）预测机制，学习一个能够捕捉数据集一致动力学的潜在空间（Latent Space），并使用 VICReg 正则化防止表示崩溃。
*   **阶段 II：动作条件潜在预测器**：
    *   在冻结的 JEPA 潜在空间中，训练一个基于 Transformer 的预测器 $f_\eta$。
    *   **关键技术**：结合了单步教师强制损失、短时域滚动（Rollout）损失以及一个**动作使用铰链损失（Action-usage hinge loss）**。后者通过打乱动作序列来强制预测器必须依赖动作信息，而非仅仅进行状态外推。
*   **推理阶段：基于能量的门控**：
    *   扩散规划器生成 $C$ 个候选轨迹。
    *   SAGE 计算每个候选轨迹前 $K$ 步的**潜在一致性能量（Energy）**，即预测潜在状态与计划潜在状态之间的 $L_1$ 误差。
    *   **选择策略**：首先过滤掉能量最高（最不可行）的比例 $(1-P)$ 的候选者，然后在剩余候选者中根据“价值 - $\lambda \times 能量”的加权得分选择最终动作。

### 3. 实验设计
*   **数据集/场景**：使用了 D4RL 标准基准测试，涵盖：
    *   **运动（Locomotion）**：HalfCheetah, Hopper, Walker2d。
    *   **操作（Manipulation）**：Franka Kitchen（长时域任务）。
    *   **导航（Navigation）**：AntMaze（稀疏奖励）, Maze2D（几何约束）。
*   **对比方法**：
    *   传统离线 RL：BC, BCQ, CQL, IQL。
    *   扩散策略：DQL, IDQL。
    *   扩散规划器：Diffuser, DV（SOTA 基准）, 以及针对可靠性改进的 RGG, LoMAP, LDCQ。

### 4. 资源与算力
*   **硬件**：论文明确提到实验是在 **Nvidia A100 GPU** 上进行的。
*   **训练时长**：JEPA 编码器和动作预测器各训练 200,000 步，扩散规划器训练 1,000,000 步。
*   **推理开销**：SAGE 引入的计算负担较小，相比于基础的 MCSS 采样方法，推理延迟仅增加了约 **6.8%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个大类领域共 18 个子任务。
*   **评估严谨性**：每个任务使用了 **500 个随机种子**进行评估（远超通常的 10-50 个），并进行了统计显著性测试（Welch's t-test 和 Bootstrap 分析）。
*   **消融实验**：详细分析了前缀长度 $K$、保留率 $P$ 和惩罚权重 $\lambda$ 的影响，并对比了不同架构（如直接在状态空间预测）的效果。实验设计非常充分且客观。

### 6. 主要结论与发现
*   **性能提升**：SAGE 在所有领域均优于现有的扩散规划器，特别是在复杂的 Kitchen 和 AntMaze 任务中提升显著。
*   **可行性检测**：实验证明，SAGE 的能量分数能准确识别出被人工篡改或动力学不一致的轨迹（AUROC 接近 1.0）。
*   **即插即用**：SAGE 可以在不重新训练扩散模型或价值模型的情况下，直接提升现有规划器的表现。

### 7. 优点
*   **模块化设计**：作为推理插件，不改变原始规划器的训练流程，易于集成。
*   **自监督学习**：无需环境交互或奖励信号即可学习可行性，适合大规模离线数据。
*   **鲁棒性**：通过显式惩罚“幻觉”轨迹，解决了扩散规划中常见的执行脆弱性问题。
*   **统计效力**：500 个种子的评估确保了结论的高度可靠性。

### 8. 不足与局限
*   **超参数依赖**：推理时的三个核心超参数（$K, P, \lambda$）虽然有推荐值，但在极端不同的任务中可能仍需微调。
*   **逆动力学依赖**：如果基础规划器只生成状态，SAGE 依赖逆动力学模型来推断动作，这可能引入额外的建模误差。
*   **简单任务的边际效应**：在 Maze2D 等动力学极其简单的任务中，由于性能已接近上限，SAGE 的提升幅度相对较小。

（完）
