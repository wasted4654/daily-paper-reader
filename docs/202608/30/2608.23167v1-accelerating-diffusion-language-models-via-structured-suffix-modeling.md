---
title: Accelerating Diffusion Language Models via Structured Suffix Modeling
title_zh: 通过结构化后缀建模加速扩散语言模型
authors: "Zifeng Cheng, Keda Li, Zhiwei Jiang, Cong Wang, Fei Shen, Qing Gu"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23167v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型作为自回归生成的替代方案
tldr: 扩散语言模型（DLM）虽具备并行解码能力，但全后缀交互导致计算开销巨大。本文提出结构化后缀建模（SSM），通过将后缀划分为局部、中部和尾部区域并分配不同数量的令牌，同时引入跨步信息传递，在无需训练的情况下显著提升了推理速度和性能，长序列推理加速可达72.81倍。
selection_source: fresh_fetch
motivation: 现有DLM加速方法忽略了后缀区域的结构异质性，且在每一步都重新初始化后缀表示，导致效率和信息利用率受限。
method: 提出将后缀划分为三个具有不同权重的结构化区域，并利用前一步的解码结果更新当前步的后缀令牌表示。
result: 在多个基准测试中实现了显著加速，结合其他技术在长序列推理中最高可获得72.81倍的加速比。
conclusion: SSM是一种无需训练且可与其他加速技术兼容的高效推理方法，有效平衡了DLM的推理速度与生成质量。
---

## 摘要
扩散语言模型 (DLMs) 通过在单个生成步骤中对多个 token 进行去噪，展现出强大的并行解码能力。然而，这种并行性带来了巨大的计算开销，因为每个步骤都需要与所有后缀 token 进行交互。现有方法通常通过仅保留局部后缀窗口来替代完整后缀，从而降低这一成本。尽管这些方法有效，但它们忽略了后缀区域之间的结构异质性，并在每个时间步使用相同的表示重新初始化后缀 token。为此，我们提出了一种用于高效 DLM 推理的结构化后缀建模方法。具体而言，我们将后缀划分为三个区域，即局部、中间和尾部区域，并根据其结构作用在每个区域保留不同数量的后缀 token。此外，我们将上一步的解码结果整合到当前步骤的后缀 token 表示中，使其能够跨生成步骤携带演化的去噪信息。值得注意的是，我们的方法无需训练，且与并行解码策略和 KV 缓存等多种现有加速技术正交。在三个 DLM 上的多个基准测试结果表明，我们的方法可以进一步加速 DLM 推理，并在大多数情况下提高性能。特别是在长序列推理中，当与其他加速技术结合时，我们的方法实现了高达 72.81 倍的加速。我们的代码可在 https://github.com/zifengcheng/SSM 获取。

## Abstract
Diffusion Language Models (DLMs) exhibit strong parallel decoding capabilities by denoising multiple tokens in a single generation step. However, this parallelism comes with substantial computational overhead, as each step requires interactions with all suffix tokens. Existing methods typically reduce this cost by retaining only a local suffix window as a substitute for the full suffix. Despite their effectiveness, these methods overlook the structural heterogeneity across suffix regions and re-initialize suffix tokens with identical representations at each timestep. To this end, we propose a structured suffix modeling method for efficient DLM inference. Specifically, we divide the suffix into three regions, i.e., the local, middle, and tail regions, and retain different numbers of suffix tokens in each region according to their structural roles. Moreover, we incorporate the decoding results from the previous step into the suffix token representations at the current step, allowing them to carry evolving denoising information across generation steps. Notably, our method is training-free and orthogonal to several existing acceleration techniques, such as parallel decoding strategies and KV cache. Empirical results across multiple benchmarks on three DLMs demonstrate that our method can further accelerate DLM inference and improve performance in most cases. In particular, in long-sequence inference, our method achieves up to a \(72.81\times\) speedup when combined with other acceleration techniques. Our code is available at https://github.com/zifengcheng/SSM.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SSM (Structured Suffix Modeling)** 的方法，旨在解决扩散语言模型（DLM）在推理过程中由于处理大量后缀 token 而导致的计算效率低下问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散语言模型（DLM）虽然具备并行解码能力，但在每一个去噪步骤中，模型都需要与序列中所有的后缀（即尚未生成的 [MASK] 部分）进行交互。随着生成长度增加，这些后缀 token 带来的注意力计算开销成为严重的推理瓶颈。
*   **研究动机**：
    1.  **结构异质性被忽视**：现有方法（如 DPad）通常简单地保留局部窗口或随机丢弃后缀，忽略了不同位置的后缀 token（如局部上下文、结构骨架、末端边界）在去噪过程中扮演着不同的角色。
    2.  **信息利用率低**：现有方法在每一步推理时都会重新初始化后缀 token 的表示，丢弃了上一步迭代中已经积累的去噪信号。

### 2. 核心方法论
SSM 是一种**无需训练（Training-free）**的推理加速框架，包含两个核心组件：
*   **结构化后缀分区与保留（Suffix Region Partition）**：
    *   **局部区域（Local Region）**：保留当前解码块附近的所有 token，以提供精细的局部上下文。
    *   **中部区域（Middle Region）**：仅保留每个后缀块（Block）的起始 token，作为轻量级的“结构锚点”，维持序列的粗略骨架。
    *   **尾部区域（Tail Region）**：保留最后一个块的起始和结束 token，以明确捕捉终端边界信息。
*   **软后缀嵌入混合（Soft Suffix Embedding Mixing）**：
    *   不再将保留的后缀 token 简单初始化为固定的 `[MASK]` 嵌入。
    *   **技术细节**：提取上一步预测概率最高的 Top-K 个 token，计算其嵌入的加权平均值，并以比例 $\alpha$ 与 `[MASK]` 嵌入进行混合。这使得后缀 token 能够携带跨步骤演化的去噪信息。
*   **流程**：分区 -> 筛选保留 token -> 软嵌入初始化 -> 输入模型解码 -> 结合早停技术（Early Termination）。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：GSM8K, MATH。
    *   **代码生成**：HumanEval, MBPP。
*   **基准模型（Backbones）**：LLaDA-8B-Instruct, LLaDA-1.5, Dream-7B-Base。
*   **对比方法**：
    *   Vanilla（Top-1 解码）。
    *   Parallel Decoding（基于阈值的并行解码）。
    *   DPad（现有的后缀丢弃方法）。
    *   Streaming-dLLM（现有的后缀剪枝方法）。
*   **评估指标**：延迟（Latency）、每秒 Token 数（TPS）、生成长度比率、准确率（Flexible/Strict/Pass@1）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A800 40GB GPU** 上进行（Checklist 中提到使用了 4 块 A800）。
*   **训练时长**：由于该方法是 **Training-free** 的，因此不需要训练时长，直接应用于推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流 DLM 模型和 4 个权威基准数据集上进行了全面测试。
    *   **消融实验**：分别验证了局部、中部、尾部区域保留策略以及软嵌入机制的有效性。
    *   **超参数分析**：探讨了局部窗口大小 $w$ 和混合比例 $\alpha$ 对性能的影响。
    *   **长序列测试**：专门测试了 1024 token 长度下的加速表现。
*   **充分性评价**：实验设计较为充分，涵盖了不同任务类型（数学 vs 代码）和不同模型架构，对比了最新的加速基准，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显著加速**：SSM 在所有基准上均实现了最低延迟。在长序列推理中，结合并行解码和 KV 缓存技术，相比 Vanilla 解码实现了高达 **72.81 倍** 的加速。
*   **性能提升**：令人意外的是，SSM 在加速的同时，在多数情况下还提高了准确率（尤其是 Strict Match 指标）。这可能是因为后缀剪枝减少了冗余信息的干扰，使模型生成的格式更规范。
*   **正交性**：证明了该方法可以与并行解码策略、KV 缓存、早停技术等完美兼容，产生叠加加速效果。

### 7. 优点
*   **角色感知**：通过对后缀进行功能分区，比之前的随机或统一剪枝更符合 Transformer 的注意力分布规律。
*   **信息连续性**：引入软嵌入混合，解决了 DLM 推理中信息在步骤间断层的问题。
*   **即插即用**：无需重新训练模型，部署成本极低。
*   **紧凑生成**：实验发现 SSM 能诱导模型生成更简洁的内容，进一步降低了计算量。

### 8. 不足与局限
*   **后缀长度依赖**：加速效果高度依赖于后缀的长度。如果 Prompt 极长而生成内容极短，加速效果会减弱。
*   **任务敏感性**：在某些特定数据集上可能存在轻微的性能波动，对于准确率要求极其严苛的场景需谨慎调参。
*   **模型规模覆盖**：实验主要集中在 10B 参数以下的模型，尚未在超大规模（如 70B+）的扩散语言模型上验证。

（完）
