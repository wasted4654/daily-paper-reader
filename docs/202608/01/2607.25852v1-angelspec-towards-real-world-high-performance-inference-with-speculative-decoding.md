---
title: "AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding"
title_zh: AngelSpec：面向真实场景下投机解码的高性能推理
authors: "Hong Liu, Rui Cen, Junhan Shi, Guangshuo Qin, Jiebin Zhang, Tianyu Liu, Runzhi Fan, Guoliang Zhao, Ruobing Xie, Kai Zhang, Song Liu, Guanghua Yu, Jianchen Zhu"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25852v1"
tags: ["query:ardm"]
score: 8.0
evidence: 结合了自回归多标记预测与块并行扩散
tldr: AngelSpec是一个统一的投机采样训练框架，旨在解决不同现实任务下投机结构性能不一的问题。它在训练层面实现了结构与数据的协同优化，将MTP用于高熵对话，块并行扩散用于高预测性代码任务；在架构上提出DFly以增强依赖建模；在推理层引入动态验证策略。实验显示，该方法显著提升了接受长度和吞吐量，实现了最高2.4倍的加速。
selection_source: fresh_fetch
motivation: 针对不同现实负载（如对话与代码）缺乏单一最优投机采样结构的问题，寻求一种能自适应多种输出分布的高性能推理方案。
method: 提出AngelSpec框架，结合了数据-结构协同训练、改进的DFly块并行架构以及基于成本模型的在线动态验证深度调整。
result: "DFly在Hy3-A21B上提升了30%的接受长度，相比自回归解码实现1.98-2.40倍加速，且吞吐量全面超越DFlash。"
conclusion: 通过在训练、架构和推理三个层面进行协同优化，AngelSpec为实现真实场景下的高性能大模型推理提供了强有力的支持。
---

## 摘要
投机解码在不改变目标分布的情况下加速了大语言模型推理，但在真实世界的工作负载中，没有单一的草稿结构能表现得最好。自回归多标记预测（MTP）是一种轻量级、稳定的提议机制，而块并行扩散则在更长的候选序列上分摊了草稿延迟；更好的选择很大程度上取决于输出分布。我们提出了 AngelSpec，这是一个针对 MTP 和块并行投机解码的统一训练框架，从三个层面解决了这种异构性。在训练层面，我们不再将单一的通用草稿器拟合到均匀的数据混合中，而是对结构和数据进行协同专门化：MTP 草稿器在多样化的对话数据上进行训练，以应对高熵的开放式聊天；而块扩散草稿器则在代码和数学数据上进行训练，以应对更长的可预测续写。在架构层面，我们提出了 DFly，这是一个块扩散框架，它结合了混合目标条件主干网络和前驱条件的自回归头，在保持并行生成的过程中，提高了目标特征的利用率和块内依赖建模。在推理层面，由于接受长度和验证成本随领域、请求、在线负载和硬件而变化，DFly 将验证视为共享的批次级资源：它在不同请求之间将计算资源重新分配给高置信度的前缀，并将预期效用与经过分析的成本模型相结合，以在线调整验证深度。在 Hy3 系列中，DFly 将 Hy3-A21B 的平均接受长度提高了约 30%，并在 4 到 64 的所有测试并发量下实现了最高的平均吞吐量，相比自回归解码实现了 1.98-2.40 倍的加速，吞吐量比 DFlash 高出 10.5-11.8%。我们发布了 AngelSpec，以支持这些方法的训练和扩展。

## Abstract
Speculative decoding accelerates large language model inference without changing the target distribution, but no single drafting structure performs best across real-world workloads. Autoregressive multi-token prediction (MTP) is a lightweight, stable proposal mechanism, whereas block-parallel diffusion amortizes drafting latency over much longer candidate sequences; the better choice depends strongly on the output distribution. We present AngelSpec, a unified training framework for MTP and block-parallel speculative decoding that addresses this heterogeneity at three levels. At the training level, rather than fitting one universal drafter to a uniform data mixture, we co-specialize structure and data: the MTP drafter is trained on diverse conversational data for high-entropy open-ended chat, and the block-diffusion drafter on code and mathematics data for longer predictable continuations. At the architecture level, we propose DFly, a block-diffusion framework combining a hybrid target-conditioning backbone with a predecessor-conditioned autoregressive head, improving target-feature utilization and intra-block dependency modeling while keeping generation parallel. At the inference level, both acceptance length and verification cost vary with domain, request, online load, and hardware, so DFly treats verification as a shared batch-level resource: it reallocates compute toward high-confidence prefixes across requests and combines expected utility with a profiled cost model to adapt verification depth online. Across the Hy3 series, DFly raises the average accepted length on Hy3-A21B by roughly 30% and attains the highest average throughput at every tested concurrency from 4 to 64, a 1.98-2.40x speedup over autoregressive decoding and 10.5-11.8% higher throughput than DFlash. We release AngelSpec to support training and extending these methods.

---

## 论文详细总结（自动生成）

以下是对论文《AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：投机解码（Speculative Decoding）虽然能加速LLM推理，但现有的草稿模型（Drafter）通常采用单一结构，难以应对真实世界中**异构的工作负载**。例如，对话任务（高熵、开放式）与代码/数学任务（结构化、长序列可预测性）对草稿器的需求截然不同。
*   **研究背景**：自回归多标记预测（MTP）适合短步长提议，而块并行扩散（Block-parallel Diffusion）适合长序列提议。目前缺乏一个能统一训练、优化并根据任务特性自适应调整的框架。
*   **整体含义**：腾讯混元团队提出了 **AngelSpec** 框架，通过训练专门化、架构改进和推理自适应，实现了在不同现实场景下的高性能推理加速。

### 2. 方法论：核心思想与关键技术
AngelSpec 从三个维度进行了创新：
*   **训练层面（专门化与对齐）**：
    *   **协同专门化**：针对对话任务训练 MTP 草稿器；针对代码/数学任务训练块扩散草稿器。
    *   **训练时测试（TTT）**：在训练 MTP 时引入自回归展开，解决训练（教师强制）与推理（自循环）之间的分布偏移。
    *   **接受度对齐蒸馏**：使用总变差（TV）距离和负对齐对数（Negative Log-overlap）作为损失函数，直接优化预期接受长度。
*   **架构层面（DFly 架构）**：
    *   **混合目标条件主干**：结合了全局变换的目标上下文与层特定的目标视图，提升特征利用率。
    *   **前驱条件自回归头（Hidden-correction）**：在并行主干后添加轻量级顺序头，利用已生成的草稿标记修正后续分布，增强块内依赖建模。
*   **推理层面（D-cut 策略）**：
    *   **动态验证深度裁剪**：将验证视为批次级共享资源。根据草稿标记的置信度计算预期效用，并结合硬件运行时的成本模型（Profiled Cost Model），动态决定每个请求的验证长度，避免在低价值后缀上浪费算力。

### 3. 实验设计
*   **目标模型**：Hy3-A21B（MoE架构）、Qwen3-8B。
*   **数据集/场景**：
    *   **数学**：GSM8K, Math500。
    *   **代码**：HumanEval, MBPP, LiveCodeBench。
    *   **对话**：MT-Bench, AlpacaEval。
*   **对比方法**：自回归解码（AR）、基础 MTP、DFlash、DSpark。
*   **评估指标**：平均接受长度（MAL）、吞吐量（Tokens/s）、加速比（Speedup）。

### 4. 资源与算力
*   **推理测试**：使用了 8 张 **NVIDIA H20 GPU**（单节点，TP=8）进行性能分析和吞吐量测试。
*   **训练框架**：AngelSpec 基于 TorchSpec 构建，采用解耦设计。推理引擎生成目标隐藏状态并通过 Mooncake（基于 RDMA 的存储）流式传输给训练工人。
*   **算力细节**：文中未详细列出训练总时长或总 GPU 小时数，但强调了其长文训练支持（最高 128k 上下文）和高效的序列打包技术。

### 5. 实验数量与充分性
*   **实验规模**：进行了系统性的对比实验，涵盖了从并发量 4 到 64 的多种服务条件。
*   **消融实验**：非常充分。分别对目标条件主干（DFlash vs. DFly）、自回归头类型（Markov vs. Hidden-correction）、训练数据扩充、草稿深度、推理块大小以及“思考模式”（Thinking-mode）进行了详细消融。
*   **客观性**：实验不仅在标准基准测试集上运行，还在**混元真实生产流量**（Live Traffic）上验证了 D-cut 的有效性，证明了其在真实高负载环境下的增益。

### 6. 主要结论与发现
*   **性能提升**：在 Hy3-A21B 上，DFly 将平均接受长度提升了约 **30%**。
*   **加速比**：相比 AR 解码实现 **1.98–2.40×** 的加速；相比 DFlash 提升了 **10.5–11.8%** 的吞吐量。
*   **动态性**：D-cut 在高并发（c64）下通过裁剪低置信度草稿，使吞吐量提升了 **15.7%**，而接受长度仅下降不到 3%。
*   **专门化必要性**：实验证明，针对不同任务（如思考模式 vs. 非思考模式）训练专门的草稿器比通用草稿器效果更好。

### 7. 优点
*   **系统性强**：从训练、架构到推理全链路优化，而非单一组件的改进。
*   **工程落地导向**：引入了运行时成本模型和动态裁剪，直接解决了投机解码在批次推理中“木桶效应”的痛点。
*   **开源贡献**：发布了 AngelSpec 框架，支持多种投机解码方法的统一训练和评估。

### 8. 不足与局限
*   **维护成本**：针对不同领域（对话 vs. 代码）维护多个专门化草稿器会增加部署和管理的复杂性。
*   **硬件依赖**：D-cut 的成本模型需要针对特定硬件（如 H20）进行预先分析（Profiling），跨硬件迁移时可能需要重新校准。
*   **模型规模限制**：虽然在 21B 和 8B 模型上效果显著，但在超大规模模型（如千亿级以上）上的训练开销和收益平衡仍需进一步验证。

（完）
