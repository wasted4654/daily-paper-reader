---
title: "AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding"
title_zh: AngelSpec：利用投机解码迈向真实场景下的高性能推理
authors: "Hong Liu, Rui Cen, Junhan Shi, Guangshuo Qin, Jiebin Zhang, Tianyu Liu, Runzhi Fan, Guoliang Zhao, Ruobing Xie, Kai Zhang, Song Liu, Guanghua Yu, Jianchen Zhu"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25852v2"
tags: ["query:ardm"]
score: 8.0
evidence: 结合了自回归多标记预测与块并行扩散
tldr: AngelSpec是一个统一的投机采样训练框架，旨在解决不同现实负载下推理性能不均的问题。它通过协同优化模型结构与数据分布，为高熵对话采用多Token预测（MTP）架构，为代码和数学任务采用块并行扩散架构（DFly）。DFly引入了混合目标调节骨干和前驱调节自回归头，并结合动态验证策略，在Hy3系列模型上实现了显著的吞吐量提升和加速比。
selection_source: fresh_fetch
motivation: 现有的投机采样方法在不同现实工作负载下表现不一，缺乏一种能兼顾高熵对话和长序列预测的统一高效方案。
method: 提出了AngelSpec框架，通过数据与结构的协同专业化，结合改进的DFly块并行架构以及基于成本模型的在线动态验证深度调整。
result: "在Hy3-A21B模型上，DFly将平均接受长度提升了约30%，相比自回归解码实现了1.98-2.40倍的加速，且吞吐量优于DFlash。"
conclusion: AngelSpec通过结构与数据的深度融合以及推理时的动态资源分配，为大规模语言模型在复杂现实场景下的高性能推理提供了有效路径。
---

## 摘要
投机解码在不改变目标分布的情况下加速了大语言模型推理，但在真实世界的工作负载中，没有单一的草稿结构能表现得最好。自回归多 Token 预测（MTP）是一种轻量级、稳定的提议机制，而块并行扩散则在更长的候选序列上分摊了草稿延迟；更好的选择很大程度上取决于输出分布。我们提出了 AngelSpec，这是一个针对 MTP 和块并行投机解码的统一训练框架，从三个层面解决了这种异构性。在训练层面，我们没有将一个通用的草稿器拟合到均匀的数据混合中，而是对结构和数据进行了协同专门化：MTP 草稿器在多样化的对话数据上进行训练，以应对高熵的开放式聊天；而块扩散草稿器则在代码和数学数据上进行训练，以应对更长的可预测续写。在架构层面，我们提出了 DFly，这是一个块扩散框架，它结合了混合目标条件主干网络和前驱条件的自回归头，在保持生成并行的同时，提高了目标特征的利用率和块内依赖建模。在推理层面，由于接受长度和验证成本随领域、请求、在线负载和硬件而变化，DFly 将验证视为共享的批次级资源：它在不同请求之间将计算资源重新分配给高置信度的前缀，并将预期效用与经过分析的成本模型相结合，以在线调整验证深度。在 Hy3 系列中，DFly 将 Hy3-A21B 的平均接受长度提高了约 30%，并在 4 到 64 的所有测试并发下实现了最高的平均吞吐量，相比自回归解码实现了 1.98-2.40 倍的加速，吞吐量比 DFlash 高出 10.5-11.8%。我们发布了 AngelSpec 以支持这些方法的训练和扩展。

## Abstract
Speculative decoding accelerates large language model inference without changing the target distribution, but no single drafting structure performs best across real-world workloads. Autoregressive multi-token prediction (MTP) is a lightweight, stable proposal mechanism, whereas block-parallel diffusion amortizes drafting latency over much longer candidate sequences; the better choice depends strongly on the output distribution. We present AngelSpec, a unified training framework for MTP and block-parallel speculative decoding that addresses this heterogeneity at three levels. At the training level, rather than fitting one universal drafter to a uniform data mixture, we co-specialize structure and data: the MTP drafter is trained on diverse conversational data for high-entropy open-ended chat, and the block-diffusion drafter on code and mathematics data for longer predictable continuations. At the architecture level, we propose DFly, a block-diffusion framework combining a hybrid target-conditioning backbone with a predecessor-conditioned autoregressive head, improving target-feature utilization and intra-block dependency modeling while keeping generation parallel. At the inference level, both acceptance length and verification cost vary with domain, request, online load, and hardware, so DFly treats verification as a shared batch-level resource: it reallocates compute toward high-confidence prefixes across requests and combines expected utility with a profiled cost model to adapt verification depth online. Across the Hy3 series, DFly raises the average accepted length on Hy3-A21B by roughly 30% and attains the highest average throughput at every tested concurrency from 4 to 64, a 1.98-2.40x speedup over autoregressive decoding and 10.5-11.8% higher throughput than DFlash. We release AngelSpec to support training and extending these methods.

---

## 论文详细总结（自动生成）

以下是对论文《AngelSpec: Towards Real-World High Performance Inference with Speculative Decoding》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：投机解码（Speculative Decoding）虽能加速LLM推理，但现实场景中的工作负载具有高度**异构性**。例如，开放式对话（高熵）与代码/数学推理（结构化、低熵）的预测难度和模式完全不同。
*   **核心问题**：现有的投机采样方法通常试图用单一的草稿模型（Drafter）适配所有场景，导致在特定任务上效率低下。此外，块并行投机（如DFlash）在处理块内依赖关系和目标特征利用上仍有提升空间。
*   **整体含义**：本文提出了 **AngelSpec**，一个统一的训练与推理框架，通过“结构与数据的协同专业化”来应对异构负载，并引入了改进的块并行架构 **DFly** 和动态验证策略 **D-cut**。

### 2. 方法论
*   **协同专业化（Co-specialization）**：
    *   **MTP（多标记预测）**：针对高熵对话，采用轻量级自回归MTP。通过**训练时测试（TTT）**解决训练（Teacher Forcing）与推理（自回归生成）之间的分布偏移，并使用**端到端全变分（TV）损失**直接优化接受率。
    *   **DFly（块并行扩散）**：针对代码和数学，采用块并行架构以分摊延迟。
*   **DFly 关键技术**：
    *   **混合目标调节骨干（Hybrid Backbone）**：结合了全局变换的目标上下文和层特定的目标视图，使不同草稿层能利用最适合其深度的目标模型特征。
    *   **隐藏层修正头（Hidden-correction Head）**：在并行骨干网络后添加轻量级自回归头，利用前一个预测Token修正当前位置的隐藏状态，在保持并行性的同时建模块内依赖。
*   **D-cut 动态验证**：
    *   将验证计算视为批次级共享资源。根据草稿Token的置信度估计预期收益，并结合硬件性能分析（Profile）的成本模型，动态调整每个请求的验证深度，以在不同并发压力下最大化吞吐量。

### 3. 实验设计
*   **目标模型**：腾讯混元 Hy3-A21B（MoE架构）、Qwen3-8B。
*   **数据集/场景**：
    *   **数学**：GSM8K, Math500。
    *   **代码**：HumanEval, MBPP, LiveCodeBench。
    *   **对话**：MT-Bench, AlpacaEval。
*   **对比方法**：自回归解码（AR）、基础MTP、DFlash、DSpark。
*   **评估指标**：平均接受长度（MAL）、每秒Token数（Tok/s）、加速比（Speedup）。

### 4. 资源与算力
*   **推理/分析算力**：实验主要在配备 **8张 NVIDIA H20 GPU**（每张96GB显存）的单节点上进行，采用张量并行（TP=8）。
*   **训练算力**：文中未详细列出训练总时长，但强调了其**解耦式训练架构**：推理引擎生成目标模型隐藏状态，通过RDMA流式传输给训练工人，支持长达128k上下文的训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了系统性的消融实验，包括：
    *   **损失函数消融**：对比了CE、KL、LK及TV损失的效果。
    *   **架构消融**：验证了混合骨干网络和不同AR头（Markov vs. Hidden-correction）的增益。
    *   **数据消融**：验证了领域特定数据增强的效果。
    *   **超参数消融**：探讨了草稿层数（3/5/7层）和块大小（5/8）的影响。
*   **充分性评价**：实验覆盖了从模型训练、架构改进到推理部署的全流程，并在真实生产流量（混元直播流量）上进行了验证，实验设计客观且对比公平。

### 6. 主要结论与发现
*   **性能提升**：在Hy3-A21B上，DFly将平均接受长度提升了约30%。在4-64并发下，相比AR实现 **1.98–2.40倍** 加速，比DFlash吞吐量高出 **10.5–11.8%**。
*   **专业化优势**：MTP在对话类任务表现更稳健，而DFly在结构化任务（代码/数学）中优势巨大。
*   **动态验证**：D-cut在超高并发（c64）下通过剪枝低置信度草稿，在几乎不损失接受长度的前提下，将吞吐量提升了 **15.7%**。

### 7. 优点
*   **系统性强**：不仅改进了模型，还发布了完整的开源训练框架 AngelSpec，解决了SD模型训练难的问题。
*   **架构创新**：DFly的隐藏层修正头巧妙地平衡了并行效率与序列依赖建模。
*   **实战导向**：D-cut 考虑了真实部署中的硬件成本和在线负载，具有极强的工程落地价值。

### 8. 不足与局限
*   **维护成本**：针对不同任务维护两套不同的草稿模型（MTP和DFly）会增加系统部署的复杂性。
*   **硬件依赖**：D-cut 的成本模型需要针对特定硬件（如H20）进行预先分析，不同硬件环境下的迁移可能需要重新校准。
*   **思维链限制**：实验发现“思考模式”（Thinking Mode）对接受率影响极大，目前的草稿模型仍需针对是否开启思考模式进行专门训练。

（完）
