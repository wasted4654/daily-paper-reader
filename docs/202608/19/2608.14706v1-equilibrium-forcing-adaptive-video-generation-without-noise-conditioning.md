---
title: "Equilibrium Forcing: Adaptive Video Generation Without Noise Conditioning"
title_zh: 平衡强迫：无需噪声条件化的自适应视频生成
authors: "Hansen Jin Lillemark, Alex Rojas, Zachary Novack, Runqian Wang, Yilun Du, Yian Ma, Taylor Berg-Kirkpatrick, Rose Yu"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.14706v1"
tags: ["query:ardm"]
score: 9.0
evidence: 利用扩散去噪模型改进自回归视频生成
tldr: 本研究针对自回归视频生成中采样策略僵化的问题，提出了 Equilibrium Forcing (EqF) 框架。该框架通过移除传统的噪声水平调节，实现了去噪场学习与采样过程的解耦。这种设计允许在推理阶段采用闭环算法，根据生成样本的反馈动态调整，从而在保持生成一致性的同时显著提升视频质量，克服了标准扩散模型在长视频生成中的局限性。
selection_source: fresh_fetch
motivation: 传统的自回归视频生成模型受限于固定的噪声调节和采样计划，无法在推理时根据数据反馈进行自适应调整。
method: 提出 EqF 框架，通过移除噪声水平调节来解耦去噪学习与采样过程，支持推理时的闭环反馈机制。
result: 在多个自回归视频生成基准测试中，EqF 在视频质量和长序列一致性方面均优于标准的噪声调节方法。
conclusion: 移除噪声水平调节赋予了模型数据依赖的推理能力，为开发更灵活、高质量的视频生成算法提供了新路径。
---

## 摘要
基于扩散（Diffusion）和流匹配（Flow Matching）的标准自回归视频生成算法依赖于僵化的训练目标和静态采样调度，限制了推理过程对数据的自适应能力。我们引入了平衡强迫（Equilibrium Forcing, EqF），这是一个用于无需噪声水平条件化的视频去噪生成模型的简化框架。EqF 开创了用于噪声无关生成的模块化训练和推理时间设计，将去噪场的学习与采样解耦。这种灵活性允许推理时间算法通过适应来自样本的反馈以闭环方式运行，从而在具有挑战性的自回归视频生成基准测试中提高视频质量和一致性。广泛的分析阐明了移除噪声水平条件化究竟是如何使 EqF 的数据依赖型推理特性超越标准的噪声水平条件化去噪视频方法的性能。

## Abstract
Standard autoregressive video generation algorithms based on Diffusion and Flow Matching rely on rigid training objectives and static sampling schedules, limiting inference procedures from adapting to the data. We introduce Equilibrium Forcing (EqF), a simplified framework for video denoising generative models without noise level conditioning. EqF pioneers modular training- and inference-time designs for noise-unconditional generation that decouple learning the denoising field from sampling. This flexibility allows for inference-time algorithms that operate in a closed loop by adapting to feedback from the sample, improving video quality and consistency on challenging autoregressive video generation benchmarks. Extensive analysis elucidates exactly how removing the noise level conditioning enables EqF's data-dependent inference properties to surpass the performance of standard noise level-conditional denoising video methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Equilibrium Forcing: Adaptive Video Generation Without Noise Conditioning》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：当前的视频生成模型（如基于扩散模型 Diffusion 或流匹配 Flow Matching 的模型）主要依赖于**噪声水平条件化（Noise Level Conditioning）**。这意味着模型在推理时必须严格遵循预设的、与时间步 $t$ 相关的静态采样计划。
*   **核心问题**：这种“开环”采样方式具有僵化性，模型无法根据当前生成的具体内容（数据反馈）动态调整去噪力度。在自回归视频生成中，微小的误差会随时间累积，导致长视频出现伪影或崩溃。
*   **整体含义**：本研究提出了 **Equilibrium Forcing (EqF)** 框架，通过**移除**模型对噪声水平（$t$ 或 $\sigma$）的依赖，实现了去噪能力与采样过程的解耦。这使得推理过程转变为一种“闭环”机制，能够根据样本状态自适应地进行修正。

### 2. 论文提出的方法论
*   **核心思想**：将生成过程视为寻找一个平衡点（Equilibrium），即模型不再需要知道当前处于第几个“时间步”，而是直接学习如何将带噪图像推向数据流形。
*   **关键技术细节**：
    *   **无噪声条件化训练**：传统的模型输入是 $(x_t, t)$，而 EqF 的模型输入仅为 $x$。训练时，模型学习一个去噪场 $f_\theta(x)$，目标是预测原始清晰图像 $x_0$。
    *   **解耦采样（Decoupled Sampling）**：由于模型不依赖 $t$，推理时可以灵活定义步长和迭代次数。
    *   **闭环反馈算法**：引入了一种基于反馈的迭代方案。在每一步采样中，模型根据当前样本 $x_k$ 与预测目标 $f_\theta(x_k)$ 之间的差异动态调整更新量。这种方式类似于优化算法中的固定点迭代，允许模型在发现生成质量下降时自动“多做功”。
*   **算法流程**：在自回归生成下一帧时，EqF 不使用固定的 $T$ 步调度，而是通过监控能量函数或重构误差，动态决定何时停止去噪或加强去噪。

### 3. 实验设计
*   **数据集/场景**：
    *   **Moving MNIST**：用于测试基础的物理运动规律掌握。
    *   **Physion**：复杂的物理交互预测数据集。
    *   **UCF-101**：真实的动作识别与视频生成基准。
*   **Benchmark 与对比方法**：
    *   对比了标准的**扩散模型（Diffusion Models）**和**流匹配（Flow Matching）**方法。
    *   对比了传统的自回归模型（如带有噪声增强的视频模型）。
    *   评估指标包括 FVD（Fréchet Video Distance）、MSE 以及长序列的一致性评分。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出具体的 GPU 型号和总训练时长（这在某些学术预印本中常见）。但根据其使用的模型架构（通常为基于 Transformer 或 U-Net 的视频生成器）和数据集规模（UCF-101），推测其在 8 至 32 块 A100/H100 级别的 GPU 集群上进行实验。
*   **效率备注**：作者提到 EqF 在推理时虽然增加了自适应迭代，但由于去噪场的高效性，整体推理开销与标准扩散模型相当。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据（MNIST）到真实复杂场景（UCF-101）的跨度。
*   **消融实验**：论文进行了充分的消融研究，验证了“移除 $t$”这一操作对模型性能的具体影响，并对比了不同反馈机制（如固定步长 vs. 自适应步长）的效果。
*   **客观性**：通过长达 100 帧以上的长视频生成测试，客观展示了 EqF 在抑制自回归漂移（Autoregressive Drift）方面的优势。

### 6. 论文的主要结论与发现
*   **去噪场独立性**：移除噪声水平条件化并不会损害模型的生成能力，反而赋予了模型更强的鲁棒性。
*   **自适应性优势**：EqF 能够根据生成过程中的“困难程度”自动分配计算资源，在关键帧或复杂运动帧上进行更多迭代。
*   **长视频一致性**：相比于标准扩散模型，EqF 在长序列生成中表现出极强的稳定性，有效解决了视频生成中常见的“逐渐模糊”或“内容突变”问题。

### 7. 优点
*   **架构简化**：去除了复杂的噪声调度（Noise Schedule）超参数调节，使模型更易于训练。
*   **推理灵活性**：实现了真正的“闭环”生成，是迈向更智能、可交互生成算法的重要一步。
*   **通用性**：该框架不仅适用于视频，理论上也可扩展到音频或 3D 序列生成。

### 8. 不足与局限
*   **训练稳定性风险**：在没有 $t$ 指引的情况下，模型在训练初期可能面临收敛较慢的问题，需要精细的初始化或学习率管理。
*   **推理耗时波动**：由于是自适应采样，生成每一帧的时间可能不固定，这在对实时性要求极高的应用场景中可能存在挑战。
*   **极端噪声处理**：在极高噪声水平下，缺乏 $t$ 的先验信息可能会使模型在定位数据流形时产生方向性偏差。

（完）
