---
title: "In-Context Forcing: Uncovering Context Effects in Autoregressive Video Diffusion"
title_zh: 上下文强制：揭示自回归视频扩散中的上下文效应
authors: "Lingxiao Yang, Liu Liu, Moran Li, Han Feng, Wenjian Cao, Jiangning Zhang, Ye Shi"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05237v1"
tags: ["query:ardm"]
score: 9.0
evidence: 自回归视频扩散模型及渐进式自回归范式
tldr: 针对自回归视频扩散模型中依赖干净帧作为上下文导致的时间动态性差和计算效率低的问题，本文提出了 In-Context Forcing (ICF) 范式。该方法通过引入具有递减噪声水平的上下文，对远端帧应用较少掩码，对近端帧应用较多掩码，实现了自适应引导。ICF 不仅增强了时间一致性和动态性，还通过解耦对干净帧的依赖实现了跨帧并行去噪，显著提升了推理速度和视觉质量。
selection_source: fresh_fetch
motivation: 现有模型依赖完全去噪的干净帧作为上下文，导致模型产生局部细节泄露的捷径，从而损害了视频的时间语义和动态表现。
method: 提出一种渐进式自回归范式，利用噪声水平递减的上下文提供自适应引导，并支持跨帧并行去噪以加速推理。
result: 在 VBench 上的实验表明，该方法在视觉保真度和推理速度上均显著优于现有的最先进方法。
conclusion: In-Context Forcing 有效解决了视频扩散中的上下文泄露问题，在提升生成质量的同时大幅加快了推理效率。
---

## 摘要
当前的少步自回归视频扩散模型在当前帧的所有去噪步骤中，都依赖于先前完全去噪的清晰帧作为上下文。然而，这些清晰帧会泄露过多的局部细节，导致模型采取“捷径”，从而损害了时间语义和动态特性。受扩散即掩码（diffusion as masking）视角的启发，我们探索了噪声上下文对少步自回归生成的影响。然而，简单地应用具有相同噪声水平的上下文提供的引导不足，会导致时间一致性较差。为了解决这一困境，我们引入了“上下文强制”（In-Context Forcing），这是一种利用噪声水平递减的上下文的渐进式自回归范式。通过对远距离帧应用较少的掩码，对相邻帧应用较多的掩码，该方法提供了自适应引导，有效地确保了稳健的时间一致性和高帧间动态。此外，通过解耦对先前清晰帧的严格依赖，我们的范式实现了跨帧并行去噪，在不牺牲性能的情况下实现了显著的推理加速。在 VBench 上的广泛实验表明，我们的方法在视觉保真度和推理速度方面均显著优于现有最先进的方法。

## Abstract
Current few-step autoregressive video diffusion models depend on previous fully denoised clean frames as context for all denoising steps of the current frame. However, these clean frames leak excessive local details, which causes the model to take shortcuts, resulting in compromised temporal semantics and dynamics. Inspired by the perspective of diffusion as masking, we explore the impact of noisy contexts on few-step autoregressive generation. Yet, simply applying contexts with the same noise levels provides insufficient guidance, leading to poor temporal consistency. To resolve this dilemma, we introduce In-Context Forcing, a progressive autoregressive paradigm that utilizes contexts with decreasing noise levels. By applying less masking to distant frames and more masking to adjacent ones, this approach provides adaptive guidance, effectively ensuring both robust temporal consistency and high inter-frame dynamics. Furthermore, by decoupling the strict dependence on previous clean frames, our paradigm enables cross-frame parallel denoising, achieving substantial inference acceleration without sacrificing performance. Extensive experiments on VBench demonstrate that our method significantly outperforms state-of-the-art approaches in both visual fidelity and inference speed.

---

## 论文详细总结（自动生成）

这篇论文提出了 **In-Context Forcing (ICF)**，旨在解决自回归视频扩散模型在生成过程中面临的时间动态性不足和推理效率低下的核心挑战。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自回归（AR）视频扩散模型在生成当前帧时，通常依赖于之前已经完全去噪的“干净”帧作为上下文。
*   **研究背景**：虽然干净帧提供了清晰的参考，但它们会泄露过多的局部细节。这导致模型在去噪过程中倾向于寻找“捷径”（Shortcuts），即简单地复制前一帧的像素或纹理，而不是学习复杂的时间语义和动态变化。
*   **后果**：这种“上下文泄露”导致生成的视频往往动态性差（看起来像静态图或微动图），且由于必须等待前一帧完全生成才能开始下一帧，推理速度受到严重限制。

### 2. 论文提出的方法论
*   **核心思想**：受“扩散即掩码”（Diffusion as Masking）视角的启发，作者提出不再使用完全干净的帧，而是使用带有**递减噪声水平**的帧作为上下文。
*   **关键技术细节（ICF 范式）**：
    *   **渐进式上下文噪声**：在生成当前帧时，对其历史参考帧施加不同程度的噪声。具体而言，距离当前时刻较远的帧应用较少的噪声（提供全局结构引导），而距离较近的相邻帧应用较多的噪声（减少细节泄露，强制模型预测动态变化）。
    *   **自适应引导**：通过这种噪声梯度的设计，模型被“强制”在缺乏近距离细节参考的情况下，根据远期目标和近期模糊趋势来推断当前帧的内容。
    *   **跨帧并行去噪（Parallel Denoising）**：由于当前帧的去噪不再严格依赖于前一帧的“最终干净状态”，模型可以在前一帧尚未完全去噪时就开始后续帧的计算。通过解耦依赖链，实现了多帧在时间轴上的重叠并行处理。

### 3. 实验设计
*   **评测基准**：主要使用了 **VBench**，这是目前视频生成领域最全面、最权威的基准测试之一。
*   **对比方法**：对比了多种最先进的（SOTA）视频生成模型，包括传统的自回归扩散模型以及非自回归模型。
*   **评估维度**：涵盖了视觉保真度（Visual Fidelity）、时间一致性（Temporal Consistency）、运动幅度（Motion Amount）以及推理速度（Inference Speed）。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心部分未详细列出具体的 GPU 型号、数量或总训练时长。通常此类研究基于 A100 或 H100 集群进行，但文中重点强调的是**推理阶段的加速**（通过并行化实现的加速比）。
*   **实现细节**：该方法主要作为一种推理策略或微调范式，其核心优势在于对现有自回归架构的优化，而非单纯依赖堆砌算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 VBench 的多个子项（如物体一致性、背景一致性、运动流畅度等）上进行了详尽对比。
    *   进行了**消融实验**，验证了不同噪声水平组合对生成质量的影响。
    *   对比了并行步数与生成质量之间的权衡（Trade-off）。
*   **充分性评价**：实验设计较为客观，通过定量指标（VBench 分数）和定性结果（视觉对比图）双重验证了 ICF 在提升动态性和速度方面的有效性。

### 6. 主要结论与发现
*   **解决泄露问题**：引入噪声上下文能有效打破模型对局部细节的依赖，显著提升视频的运动幅度和动态自然度。
*   **性能提升**：在 VBench 上的表现优于现有 SOTA 方法，尤其是在处理长视频和复杂运动时。
*   **推理加速**：通过跨帧并行去噪，ICF 在不损失（甚至提升）视觉质量的前提下，大幅缩短了视频生成的总时长，解决了自回归模型推理慢的顽疾。

### 7. 优点
*   **视角新颖**：将扩散过程视为一种动态掩码机制，并巧妙地应用于自回归上下文管理。
*   **兼顾质量与效率**：不仅提升了视频的动态表现（质量），还通过并行化解决了工程上的痛点（效率）。
*   **即插即用潜力**：这种渐进式自回归范式具有一定的通用性，可能适用于多种基于扩散的视频生成架构。

### 8. 不足与局限
*   **超参数敏感性**：噪声水平的递减策略（Schedule）需要精细调优，不同类型的视频内容可能需要不同的噪声配置。
*   **长序列漂移风险**：虽然缓解了局部泄露，但作为自回归模型，在生成极长视频时，噪声的累积是否会导致语义漂移仍需进一步长期验证。
*   **算力细节缺失**：文中缺乏对训练成本的详细描述，使得其他研究者难以评估其训练门槛。

（完）
