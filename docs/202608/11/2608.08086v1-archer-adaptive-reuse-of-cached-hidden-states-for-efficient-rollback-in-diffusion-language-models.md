---
title: "Archer: Adaptive Reuse of Cached Hidden States for Efficient Rollback in Diffusion Language Models"
title_zh: Archer：扩散语言模型中用于高效回滚的缓存隐藏状态自适应重用
authors: "Xuning He, Zinan Sheng, Yongding Tao, Huanyu Liu, Ge Li, Xue Jiang, Yihong Dong"
date: 2026-08-08
pdf: "https://arxiv.org/pdf/2608.08086v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型回滚中的KV缓存与自回归生成的对比
tldr: Archer 针对扩散语言模型（DLM）推理成本高的问题，提出了一种无需训练的自适应 KV 缓存方法。它通过在有界状态邻域内重用 Prompt 的 KV 状态，同时保持 Response 状态同步，解决了 DLM 回滚机制与传统缓存不兼容的矛盾。该方法不仅实现了显著的推理加速，还通过延迟反馈减少了早期错误强化，提升了生成质量。
selection_source: fresh_fetch
motivation: 扩散语言模型的回滚能力导致推理时需频繁重新计算全局上下文，传统 KV 缓存因无法处理动态变化的隐藏状态而难以应用。
method: 提出 Archer 方法，在有界邻域内非对称地重用 Prompt 的 KV 缓存，并结合状态感知刷新机制来平衡计算效率与近似误差。
result: "在主流测试集上实现了平均 2.57 倍的加速，同时将 Pass@1 指标最高提升了 3.05 个百分点。"
conclusion: Archer 成功打破了 DLM 加速中“质量换速度”的权衡，证明了合理利用缓存边界能同时提升扩散生成效率与准确性。
---

## 摘要
扩散语言模型 (DLM) 通过迭代优化序列，允许随着上下文的演变对早期预测进行修正。这种回滚能力使其区别于不可逆的自回归生成，但也导致推理成本高昂。每次去噪更新都会改变全局上下文，迫使 Prompt 和响应状态重新计算，尽管只有响应 Token 是可修改的。键值 (KV) 缓存可以降低这种成本，但传统的缓存机制假设历史状态是不可变的，因此难以与回滚机制协调。在本文中，我们提出了 Archer（Adaptive Reuse of Cached Hidden States for Efficient Rollback），这是一种适用于具备回滚能力的 DLM 的无需训练的 KV 缓存方法。Archer 不对称地保持可变响应与当前假设同步，同时在有界状态邻域内重用 Prompt 的 K/V。尽管在双向注意力机制下 Prompt 的表示也会发生变化，但其 Token 身份保持固定；因此，有界重用在不缓存可变响应状态的情况下，摊销了重复的 Prompt 计算。它还延迟了来自暂定 Token 的反馈，减少了对瞬时高置信度错误的过早强化，并为回滚提供了更多纠正错误的机会。我们的分析将 Prompt 重用描述为一种与可逆性对齐的缓存边界，限制了其依赖于状态的近似误差，并为保留全刷新决策提供了解码器边际条件。现有的 DLM 加速方法通常以质量换取速度。Archer 改变了这一权衡边界，在主测试集上实现了 33.63% 的最佳平均性能，同时获得了 2.57 倍的平均加速。在所有评估设置中，它将 Pass@1 提高了多达 3.05 个百分点，并实现了高达 2.95 倍的加速。受控分析将质量提升归因于延迟的 Prompt 反馈，并验证了状态感知刷新的有效性。我们的代码可在 https://github.com/Hxnng/Archer 获取。

## Abstract
Diffusion language models (DLMs) iteratively refine a sequence, allowing earlier predictions to be revised as context evolves. This rollback capability distinguishes them from irreversible autoregressive generation, but makes inference costly. Every denoising update alters the global context, forcing both prompt and response states to be recomputed even though only response tokens are revisable. Key-value (KV) caching could reduce this cost, yet conventional caching assumes immutable historical states and is therefore difficult to reconcile with rollback.In this paper, we introduce Adaptive Reuse of Cached Hidden States for Efficient Rollback (Archer), a training-free KV caching method for rollback-capable DLMs. Archer asymmetrically keeps the mutable response synchronized with the current hypothesis while reusing prompt K/V within a bounded state neighborhood. Although prompt representations also change under bidirectional attention, their token identities remain fixed; bounded reuse therefore amortizes repeated prompt computation without caching mutable response states. It also delays feedback from tentative tokens, reducing premature reinforcement of transient high-confidence errors and giving rollback more opportunity to correct them. Our analysis characterizes prompt reuse as a reversibility-aligned cache boundary, bounds its state-dependent approximation error, and gives a decoder-margin condition for preserving full-refresh decisions.Existing DLM acceleration often trades quality for speed. Archer shifts this frontier, attaining the best mean performance of 33.63% together with a 2.57x mean speedup on the main suite. Across evaluated settings, it improves Pass@1 by up to 3.05 points and reaches up to 2.95x speedup. Controlled analyses connect the quality gain to delayed prompt feedback and validate state-aware refresh. Our code is available at https://github.com/Hxnng/Archer.

---

## 论文详细总结（自动生成）

以下是对论文《Archer: Adaptive Reuse of Cached Hidden States for Efficient Rollback in Diffusion Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散语言模型（DLM）通过双向注意力机制实现了“回滚”（Rollback）能力，即可以根据后续生成的上下文修正之前的错误。然而，这种灵活性带来了巨大的计算开销：每次去噪更新都会改变全局上下文，导致即使 Prompt（提示词）部分未变，也必须与 Response（响应）部分一起重新计算。
*   **研究背景**：传统的 KV 缓存（KV Caching）是为自回归模型设计的，假设历史状态不可变。在 DLM 中，由于双向注意力的存在，响应部分的任何微小变动都会通过注意力机制影响 Prompt 的隐藏状态，使得传统缓存失效。
*   **整体含义**：论文旨在寻找一种既能保留 DLM 回滚能力，又能显著降低推理延迟的缓存策略，打破“加速必损质量”的固有局面。

### 2. 方法论：Archer 核心思想与技术细节
Archer 是一种**无需训练**的自适应缓存控制方法，其核心逻辑如下：
*   **非对称状态重用（Asymmetric State Reuse）**：
    *   **Prompt 部分**：虽然其表示会随响应改变，但 Token 身份固定。Archer 选择在一定时间内重用 Prompt 的 KV 缓存。
    *   **Response 部分**：作为可变区域，在每一步都进行完全重新计算，以确保回滚机制（重新掩码或替换 Token）的完整性。
*   **状态感知刷新（State-Anchored Refresh）**：
    *   系统维护一个“锚点”响应 $g_r$。
    *   计算当前响应 $g_t$ 与锚点之间的**汉明距离（Hamming Distance）** $D_t$。
    *   当 $D_t \ge K$（阈值）时，触发全量刷新，更新 Prompt 缓存并重置锚点；否则，重用旧的 Prompt 缓存进行推理。
*   **延迟反馈机制（Delayed Feedback）**：
    *   通过重用旧缓存，暂时抑制了响应到 Prompt 再回到响应的反馈回路。这种“时间锚定”效应可以防止模型过早强化瞬时的、高置信度的错误，给回滚机制留出更多修正空间。

### 3. 实验设计
*   **数据集与场景**：
    *   **代码生成（主战场）**：MBPP、HumanEval、LiveCodeBench 及其扩展测试集（ET 版）。
    *   **通用推理（泛化性测试）**：MATH-500（数学）、FinQA（金融）、PubMedQA（生物医学）。
*   **基准模型（Backbones）**：LLaDA-8B、Dream-7B、DiffuCoder-7B。
*   **对比方法**：
    *   标准采样：LLaDA-Confidence。
    *   高效采样/缓存方法：Fast-dLLM、dKV-Cache、dLLM-Cache。
    *   具备回滚能力的解码器：Saber（本文的主要对比对象）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推理性能指标**：实验主要关注“墙钟时间”（Wall-clock time）和加速比。例如，在 MBPP 上，Archer 将平均延迟从 8.45 秒降至 6.20 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同的 DLM 架构和 6 个跨领域数据集。
    *   进行了详尽的超参数 $K$（刷新半径）的敏感度分析（从 $K=1$ 到 $K=\infty$）。
    *   **影子前向实验（Shadow-forward Analysis）**：通过 5,122 组配对探测，验证了汉明距离作为刷新信号的优越性。
*   **充分性评价**：实验设计非常充分且严谨。通过“匹配状态反馈干预”实验，排除了随机性干扰，客观证明了延迟反馈对质量提升的直接贡献。

### 6. 主要结论与发现
*   **性能突破**：Archer 在主测试集上实现了 **2.57 倍**的平均加速，同时将 Pass@1 性能最高提升了 **3.05 个百分点**。
*   **打破权衡**：与现有加速方法不同，Archer 在提升速度的同时提升了生成质量，成为目前质量-速度前沿面（Frontier）上的最优解。
*   **刷新机制**：实验证明基于“状态距离”的刷新比基于“步数/时间”的刷新更能准确捕捉缓存失效的时机。

### 7. 优点
*   **无需训练**：作为一种插件式方法，可直接应用于现有的预训练 DLM。
*   **理论支撑**：提供了关于缓存边界、近似误差界限以及决策保真度的形式化证明。
*   **双重收益**：不仅通过减少计算量加速，还通过“时间锚定”效应改善了回滚的纠错动态。

### 8. 不足与局限
*   **缓存边界保守**：目前仅缓存了 Prompt 部分，响应部分的计算依然存在冗余（尽管这是为了保证回滚的安全性）。
*   **超参数依赖**：刷新半径 $K$ 对不同任务的敏感度不同，需要针对特定场景进行微调以获得最佳平衡。
*   **长文本挑战**：虽然在 2048 Token 长度下表现良好，但在极长上下文（如 100k+）中的表现和内存压力尚未深入探讨。

（完）
