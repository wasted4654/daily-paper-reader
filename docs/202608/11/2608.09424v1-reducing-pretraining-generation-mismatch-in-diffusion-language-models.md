---
title: Reducing Pretraining-Generation Mismatch in Diffusion Language Models
title_zh: 减少扩散语言模型中预训练与生成的失配
authors: "Xiaocheng Lu, Huabin Liu, Song Guo, Jianguo Li"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09424v1"
tags: ["query:ardm"]
score: 9.0
evidence: 结合自回归前缀监督与扩散去噪
tldr: 本研究针对扩散语言模型（dLLM）在预训练与生成阶段存在的分布不匹配问题，提出了前缀条件扩散（PCD）预训练目标。传统 dLLM 在训练时随机破坏前缀和后续文本，而 PCD 通过结合自回归前缀监督与后缀去噪，使训练接口更贴近推理时的提示词生成模式。该方法无需改变推理架构，在 LLaDA2-Mini 和 Qwen-1.7B 模型上均取得显著提升，证明了对齐预训练上下文分布对提升扩散模型生成能力的有效性。
selection_source: fresh_fetch
motivation: 扩散语言模型在预训练时随机破坏 Prompt 和后续文本，导致其与推理阶段基于干净 Prompt 进行生成的实际需求存在严重不匹配。
method: 提出 PCD 预训练目标，通过调整注意力掩码和破坏掩码，对前缀部分实施自回归监督，而仅对未知的后续文本进行扩散去噪。
result: "在 LLaDA2-Mini 和 Qwen-1.7B 基准测试中，PCD 相比原生扩散模型基线分别实现了 4.2% 和 14.2% 的相对性能提升。"
conclusion: 通过使预训练时的上下文分布与提示词生成过程保持一致，可以在不改变推理模式的前提下有效弥补扩散语言模型的生成性能差距。
---

## 摘要
自回归语言模型在训练和使用上保持一致：生成过程以干净的提示词（prompt）为条件，而训练过程则根据干净的左侧上下文预测未来的标记（tokens）。扩散语言模型（dLLM）提供并行去噪能力，但原生的 dLLM 预训练可能会随机地同时损坏提示词和后续标记，从而削弱了提示词条件生成所需的干净前缀接口。我们针对提示词续写识别出了这种失配，并提出了 PCD（前缀条件扩散），这是一种结合了自回归（AR）前缀监督与无偏移后缀去噪的预训练目标。在训练目标层面，PCD 改变了持续预训练中的注意力掩码、损坏掩码和标签构建；它不需要自回归解码器、验证器或新的推理模式。通过以自回归方式监督干净的前缀端，并仅对未知的续写部分应用扩散，PCD 使得局部训练接口类似于块扩散模型在评估时的查询方式。我们进一步将样本内的前缀条件作用与样本间的训练目标混合分离开来，使我们能够将局部对齐信号与可选的批次级混合调节旋钮区分开。在 LLaDA2-Mini 和 Qwen-1.7B 骨干网络上，PCD 相比同系列的各种原生 dLLM 稳定基准均有持续提升，在 LLaDA2-Mini 的六项主要基准平均测试中实现了 4.2% 的相对增益（+2.56 分），在主要的 Qwen 机制对比中实现了 14.2% 的相对增益（+4.86 分）。这些结果表明，将预训练上下文分布与提示词条件生成相对齐，可以在不改变推理过程的情况下，弥补 dLLM 续写差距中可衡量的部分。

## Abstract
Autoregressive language models align training and use: generation conditions on a clean prompt, and training predicts future tokens from clean left context. Diffusion language models offer parallel denoising, but native dLLM pretraining can randomly corrupt prompt and continuation tokens together, weakening the clean-prefix interface needed for prompt-conditioned generation. We identify this mismatch for prompt continuation and propose PCD (Prefix-Conditioned Diffusion), a pretraining objective that combines AR prefix supervision with no-shift suffix denoising. At the training-objective level, PCD changes the attention mask, corruption mask, and label construction in continued pretraining; it does not require an autoregressive decoder, verifier, or new inference mode. By supervising the clean-prefix side autoregressively and applying diffusion only to the unknown continuation, PCD makes the local training interface resemble how block-diffusion models are queried at evaluation time. We further separate intra-sample prefix conditioning from inter-sample objective mixing, allowing us to identify the local alignment signal separately from the optional batch-level mixing knob. Across LLaDA2-Mini and Qwen-1.7B backbones, PCD consistently improves over same-family native dLLM stable baselines, reaching a 4.2% relative gain on the main LLaDA2-Mini six-benchmark average (+2.56 points) and a 14.2% relative gain in the primary Qwen mechanism comparison (+4.86 points). These results suggest that aligning the pretraining context distribution with prompt-conditioned generation can recover a measurable part of the dLLM continuation gap without changing inference.

---

## 论文详细总结（自动生成）

这篇论文针对扩散语言模型（dLLM）在预训练与推理阶段存在的“分布不匹配”问题提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）模型在训练和推理时都遵循“干净前缀预测下一标记”的模式，而扩散语言模型（dLLM）在预训练时通常会在整个序列中随机添加噪声（掩码）。
*   **失配现象**：在实际应用（如提示词续写）中，模型接收的是**干净的提示词（Prompt）**，但 dLLM 在预训练阶段习惯了处理**被破坏的前缀**。这种预训练与生成接口的不一致，导致 dLLM 在提示词续写任务上的表现弱于 AR 模型。
*   **研究目标**：在不改变扩散模型并行生成优势的前提下，通过改进预训练目标，使其在训练时就能模拟推理时的“干净前缀-未知后续”接口。

### 2. 方法论：前缀条件扩散（PCD）
*   **核心思想**：将训练序列划分为“自回归前缀”和“扩散后缀”两部分。
*   **关键技术细节**：
    *   **混合损失函数**：对序列的前 $k$ 个标记应用自回归（AR）损失（预测下一个标记）；对剩余的后缀部分应用掩码扩散建模（MDM）损失（在给定干净前缀的情况下重建被破坏的后缀）。
    *   **注意力掩码调整**：前缀部分采用因果注意力（Causal Attention），后缀部分则可以同时看到干净的前缀和部分损坏的后缀。
    *   **两种混合模式**：
        *   **样本内混合（Intra-sample）**：在同一个训练样本中同时包含 AR 前缀和 MDM 后缀。
        *   **样本间混合（Inter-sample）**：在同一个 Batch 中混合使用 PCD 样本和原生扩散样本，作为一种正则化手段。
    *   **WSD 框架集成**：PCD 被设计为集成在“预热-稳定-衰减”（Warmup-Stable-Decay）转换流程的“稳定阶段”，用于将 AR 模型转换为高效的块扩散模型。

### 3. 实验设计
*   **骨干网络**：使用了 LLaDA2-Mini（16B 参数，A1B 激活的 MoE 架构）和 Qwen-1.7B。
*   **数据集/场景**：涵盖了推理、编程、数学和知识类任务。
*   **Benchmark**：使用了 12 个主流基准测试，包括 GSM8K、MATH、BBH、MBPP、HumanEval-FIM、MMLU-Pro 等。
*   **对比方法**：
    *   **原生 dLLM 基线**：同系列的、采用标准随机掩码扩散目标的模型。
    *   **外部参考**：LLaDA、LLaDA-MoE、Dream 等已发布的扩散模型，以及 LLaMA3、Qwen2.5 等 AR 模型（作为规模参考）。

### 4. 资源与算力
*   **算力设备**：文中提到 Qwen-1.7B 的消融实验使用了 **64 台 H20 GPU** 的配置。
*   **训练规模**：LLaDA2-Mini 的主实验在持续预训练阶段消耗了高达 **400B（4000 亿）Tokens** 的数据量。
*   **时长**：未明确给出具体的总训练小时数，但从 Token 规模和 GPU 数量可以推断属于中大规模训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 LLaDA2-Mini 进行了从 100B 到 400B Token 的扩展性实验。
    *   针对 Qwen-1.7B 进行了详细的机制消融实验（包括混合比例、前缀范围等）。
    *   进行了 SFT（指令微调）后的迁移能力测试。
*   **充分性与客观性**：实验设计较为严谨，特别强调了“同族对比”（Matched Comparison），即在相同的初始化、数据和算力预算下对比 PCD 与原生目标，排除了数据差异带来的干扰。

### 6. 主要结论与发现
*   **性能提升**：PCD 在 LLaDA2-Mini 上相比原生基线平均提升了 **4.2%**（+2.56 分）；在 Qwen-1.7B 上实现了 **14.2%** 的显著相对增益。
*   **机制验证**：实验证明“样本内混合”是主要的性能驱动力，因为它直接解决了前缀分布的失配问题。
*   **兼容性**：PCD 提升的性能在经过 WSD 的“衰减阶段”和后期的 SFT 阶段后依然能够保留，证明了该方法在模型生命周期中的稳定性。

### 7. 优点
*   **无需改变推理**：该方法仅在预训练阶段修改目标函数，推理时不需要额外的解码器、验证器或复杂的采样策略。
*   **理论支撑**：通过变分视角（Variational View）量化了原生扩散模型中存在的“上下文偏移”信息间隙。
*   **即插即用**：可以无缝集成到现有的 AR-to-Diffusion 转换流程中。

### 8. 不足与局限
*   **超参数敏感性**：前缀比例（Prefix Ratio）的选择对结果有影响，最佳比例可能随模型规模和任务类型而变化。
*   **实验覆盖**：虽然在 16B 规模上做了验证，但尚未在千亿级（100B+）参数模型上进行超大规模验证。
*   **任务偏向**：提升主要集中在提示词续写类任务，对于非对称的填充（Infilling）任务，其优势可能不如续写任务明显。

（完）
