---
title: "WA-JEPA: Rethinking the Video JEPA Paradigm for World-Action Modeling in Autonomous Driving"
title_zh: WA-JEPA：重新思考自动驾驶中世界-动作建模的视频 JEPA 范式
authors: "Xinlin Wang, Yujiao Xiang, Yuheng Zhou, Jingqi Wang, Minqing Huang, Jiajie Huang, Dongxu Wei, Tingguang Zhou, Xiyang Wang, Gong Chen, Zhi Xu, Feiyang Tan, Hangning Zhou, Mu Yang"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.20974v1"
tags: ["query:genad"]
score: 8.0
evidence: 自动驾驶规划中的世界动作建模
tldr: WA-JEPA 针对自动驾驶规划重新设计了 V-JEPA 架构。它摒弃了随机掩码和确定性回归，引入混合未来掩码预训练、基于条件流匹配的潜在未来预测，以及联合未来-动作预测器。该模型在 nuPlan 和 NAVSIM 等基准测试中表现优异，显著提升了自动驾驶的规划性能，证明了 V-JEPA 原生世界-动作模型在自动驾驶领域的潜力。
selection_source: fresh_fetch
motivation: 传统的 V-JEPA 采用随机掩码和确定性回归，无法满足自动驾驶中对未来导向预测和动作耦合的严苛需求。
method: 提出 WA-JEPA 模型，采用混合未来掩码预训练、条件流匹配生成潜在未来，并利用联合预测器在统一空间内同时去噪场景特征和自车轨迹。
result: 在 NAVSIM-v2 基准上达到 91.7 EPDMS，超越了现有的端到端和世界-动作模型基准，并在 HUGSIM 闭环测试中取得领先成绩。
conclusion: WA-JEPA 证明了将 V-JEPA 范式转化为原生世界-动作模型是实现强大且可扩展的自动驾驶规划的有效途径。
---

## 摘要
视频联合嵌入预测架构（V-JEPA）通过自监督潜特征预测从视频中学习强大的时空表示。然而，V-JEPA 是基于随机掩码补全和确定性回归构建的，这使其在本质上不适用于需要与动作紧密耦合且面向未来预测的自动驾驶规划。为了解决这一问题，我们重新思考了 V-JEPA 范式，并提出了 WA-JEPA，这是一种专为自动驾驶规划设计的原生 V-JEPA 世界-动作模型。WA-JEPA 采用混合未来掩码预训练，而非随机时空掩码，使模型能够从观察到的上下文中推断未来潜变量。不同于确定性回归，我们将未来预测重构为潜未来上的条件流匹配（conditional flow matching），这显著提升了模型为下游规划生成合理未来潜变量的能力。最后，本文提出了一个联合未来-动作预测器，在统一的时空潜空间中共同去噪未来场景标记和自车轨迹，从而允许动作监督直接塑造与规划相关的世界表示。在 nuPlan 视频上进行预训练并在 NAVSIM 上进行微调后，WA-JEPA 在 NAVSIM-v2 上达到了 91.7 EPDMS，分别超过最强的端到端和世界-动作基准模型 1.6 和 1.3 EPDMS；且在没有针对 HUGSIM 进行特定微调的情况下，在相同的评估协议下，于闭环 HUGSIM 基准测试中获得了 0.4462 的最佳 HD-Score。这些结果验证了原生 V-JEPA 的世界-动作建模是自动驾驶规划中一种强大且可扩展的范式。代码可在 https://github.com/AFARI-Research/WA-JEPA 获取。

## Abstract
Video Joint Embedding Predictive Architecture (V-JEPA) learns powerful spatiotemporal representations from video through self-supervised latent feature prediction. However, V-JEPA is built around random-mask completion and deterministic regression, making it fundamentally ill-suited for autonomous driving planning that demands future-directed prediction tightly coupled with action. To address this, we rethink the V-JEPA paradigm and present WA-JEPA, a V-JEPA-native world-action model designed for autonomous driving planning. Instead of random spatiotemporal masking, WA-JEPA employs hybrid future-masked pre-training, where the model infers future latents from observed context. Departing from deterministic regression, we recast future prediction as conditional flow matching over latent futures, which substantially improves the model's ability to generate plausible future latents for downstream planning. Finally, a joint future-action predictor is proposed to denoise future scene tokens and ego trajectories together in a unified spatiotemporal latent space, allowing action supervision to directly shape planning-relevant world representations. Pre-trained on nuPlan videos and fine-tuned on NAVSIM, WA-JEPA reaches 91.7 EPDMS on NAVSIM-v2, surpassing the strongest end-to-end and world-action baselines by 1.6 and 1.3 EPDMS, and, without HUGSIM-specific fine-tuning, attains the best HD-Score of 0.4462 on the closed-loop HUGSIM benchmark under the same evaluation protocol. These results validate V-JEPA-native world-action modeling as a powerful and scalable paradigm for autonomous driving planning. Code is available at https://github.com/AFARI-Research/WA-JEPA.

---

## 论文详细总结（自动生成）

### 论文总结：WA-JEPA —— 重新思考自动驾驶中的视频 JEPA 世界-动作建模

#### 1. 论文的核心问题与整体含义（研究动机和背景）
传统的端到端（E2E）自动驾驶模型往往缺乏显式的推理能力，且在处理长尾场景时鲁棒性不足。现有的改进方案主要分为两条路径：**VLA（视觉-语言-动作）**和**WAM（世界-动作模型）**。
*   **研究动机：** 现有的 WAM 通常基于 VAE 潜空间进行视频生成，其语义表达能力有限。而 **V-JEPA** 虽然能学习到强大的时空语义表示，但存在三个缺陷：1) 采用随机掩码补全而非面向未来的预测；2) 使用确定性回归而非生成式建模，难以生成未见的未来；3) 缺乏与动作（Action）的紧密耦合。
*   **核心问题：** 如何保留 V-JEPA 强大的时空表示能力，同时将其改造为适用于自动驾驶规划的、具备未来预测和动作耦合能力的“原生”世界-动作模型。

#### 2. 论文提出的方法论
WA-JEPA 提出了一个两阶段的训练框架，核心思想是将特征预测任务从“补全”转变为“生成与规划”。
*   **混合未来掩码预训练（Stage 1）：** 摒弃随机掩码，采用“全未来掩码（Full-mask）”和“补全掩码（Patch-mask）”的混合策略。模型观察过去帧，预测未来帧的潜特征，使预训练目标与规划任务的因果性对齐。
*   **基于流匹配（Flow Matching）的潜未来预测：** 不同于 V-JEPA 的 MSE 回归，WA-JEPA 将未来特征预测建模为条件流匹配过程。通过在潜空间进行去噪生成，提升了模型生成合理未来场景特征的能力。
*   **联合未来-动作建模（Stage 2）：** 引入动作流，使用多模态扩散 Transformer（MMDiT）架构。在统一的时空潜空间中，同时对未来场景 Token 和自车轨迹进行去噪。
*   **非对称梯度设计：** 在联合预测器中，动作监督产生的梯度可以塑造世界表示，但场景预测损失的梯度在进入动作流前被截断，确保世界建模服务于规划。

#### 3. 实验设计
*   **数据集：** 
    *   **预训练：** 使用 nuPlan 数据集的多视角视频。
    *   **微调与评估：** 使用 NAVSIM 数据集的 navtrain/navtest 拆分。
    *   **闭环验证：** 在 HUGSIM 模拟器上进行零样本（Zero-shot）闭环评估。
*   **Benchmark 与指标：** 
    *   NAVSIM-v1（PDMS 指标）和 NAVSIM-v2（EPDMS 指标）。
    *   HUGSIM（HD-Score、NC、DAC 等闭环指标）。
*   **对比方法：** 
    *   **E2E 方法：** TransFuser, SparseDriveV2, DiffusionDrive 等。
    *   **VLA 方法：** DriveVLA, ReCogDrive, WAM-Diff 等。
    *   **WAM 方法：** Latent-WAM, DriveFuture, Discrete-WAM 等。

#### 4. 资源与算力
论文明确提到了算力使用情况：
*   **Stage 1（预训练）：** 使用 **64 台 NVIDIA A800 GPU**。
*   **Stage 2（微调）：** 使用 **32 台 NVIDIA A800 GPU**。
*   **训练细节：** 每台 GPU 的 Batch Size 为 4，采用 DeepSpeed ZeRO-2 优化器，使用 bfloat16 精度。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的基准测试对比（涵盖了 20 余种主流模型）和详尽的消融实验。
*   **消融实验：** 针对视觉编码器初始化（MAE vs DINO vs V-JEPA）、掩码策略（Patch vs Full）、预测目标（回归 vs 流匹配）以及场景-动作耦合方式进行了 4 组核心消融。
*   **充分性与公平性：** 实验覆盖了开环（NAVSIM）和闭环（HUGSIM）场景，且在 HUGSIM 评估中统一了控制器和评估协议，并对随机性进行了 10 个 Seed 的均值统计，实验设计客观且充分。

#### 6. 论文的主要结论与发现
*   **性能领先：** WA-JEPA 在 NAVSIM-v2 上达到 **91.7 EPDMS**，刷新了 SOTA 纪录，超过了所有已知的 E2E 和 WAM 基准。
*   **闭环泛化：** 在零样本闭环测试中，WA-JEPA 的 HD-Score 从 0.3252 提升至 **0.4462**，证明了其学习到的特征具有极强的迁移能力。
*   **流匹配的优势：** 相比于直接回归，流匹配能显著减少特征的“时间坍缩（Temporal Collapse）”，保留更清晰的空间结构和动态变化。
*   **V-JEPA 的潜力：** 验证了 V-JEPA 架构在经过适当改造后，是自动驾驶规划中比纯图像预训练（如 MAE）更强大的骨干网络。

#### 7. 优点
*   **范式创新：** 成功将非生成式的 V-JEPA 改造为生成式的世界-动作模型，填补了特征级预测与实际规划之间的鸿沟。
*   **语义丰富：** 相比于基于 VAE 的模型，WA-JEPA 利用了 V-JEPA 预训练带来的深层时空语义，推理能力更强。
*   **端到端一致性：** 实现了世界建模与动作规划在潜空间内的深度耦合，而非简单的串联。

#### 8. 不足与局限
*   **计算开销：** 尽管推理时使用了 12 步采样，但相比于确定性回归模型，流匹配生成的计算成本仍然较高。
*   **感知显式性：** 作为一个纯潜空间模型，它缺乏显式的感知输出（如 3D 检测框或占用栅格），这可能在某些需要高可解释性的工业应用中受到限制。
*   **数据依赖：** 性能高度依赖于大规模高质量视频数据的预训练，对于缺乏此类数据的场景可能难以发挥优势。

（完）
