---
title: Retrofitting Linear Attention into Diffusion Language Models
title_zh: 将线性注意力引入扩散语言模型
authors: "Jinha Kim, Younghun Roh, Jaeyeon Kim"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.06628v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型 (dLLM) 中的半自回归解码
tldr: 扩散语言模型（dLLM）通过并行解码加速推理，但在处理长前缀时仍面临注意力计算开销大的挑战。本文提出块混合注意力（block-hybrid attention），在当前去噪块内保留精确Softmax注意力，而对历史块应用线性注意力。通过对16B规模的LLaDA模型进行微调，仅需60小时训练即可在保持性能的同时，实现高达1.7倍的推理吞吐量提升，证明了预训练dLLM线性化改造的高效性。
selection_source: fresh_fetch
motivation: 旨在解决扩散语言模型在分块半自回归解码时，因重复计算历史块前缀注意力而产生的推理延迟和计算瓶颈。
method: 提出块混合注意力机制，在当前去噪块使用Softmax注意力，对历史块使用线性注意力，并对预训练模型进行部分层替换与快速微调。
result: 在16B规模的LLaDA模型上，该方法仅需60小时微调即可保持原有性能，并实现高达1.7倍的吞吐量提升和更好的内存支持。
conclusion: 研究证明了通过线性化改造可以有效提升预训练扩散语言模型的推理效率，为大规模生成模型的高效部署提供了新思路。
---

## 摘要
扩散语言模型 (dLLMs) 通过并行解码加速推理，为自回归模型提供了一种极具前景的替代方案。最近的 dLLM 通常采用分块半自回归解码，在自回归地生成块的同时，并行地对每个活动块内的 token 进行去噪。然而，尽管使用了 KV 缓存，每个去噪步骤仍需关注之前的所有块，从而反复产生前缀注意力开销。受此瓶颈启发，我们探究是否可以通过对先前块的注意力进行线性化来进一步加速 dLLM 推理。我们引入了块混合注意力 (block-hybrid attention)，它在活动去噪块内保留精确的 softmax 注意力，同时对先前的块应用线性注意力。我们展示了这种混合注意力可以通过极少的训练后处理改进到预训练的 dLLM 中：LLaDA-Hybrid 替换了 16B 开源 dLLM LLaDA 2.1 中 20 个注意力层中的 6 个，主要遵循了 LoLCAT (Zhang et al, 2024) 的方法。转换过程仅需约 60 小时，同时保持了基准测试性能：HumanEval 为 72.0% vs. 75.6%，MBPP+ 为 63.0% vs. 57.7%，CMATH 为 86.7% vs. 88.3%。通过 Triton 实现，LLaDA-Hybrid 实现了高达 1.7 倍的解码吞吐量提升，并在内存耗尽前支持更多并发请求，表明预训练的 dLLM 可以被高效地线性化以实现更快的推理。我们的代码可在以下地址获取：https://github.com/Diuven/LLaDA-Hybrid。

## Abstract
Diffusion language models (dLLMs) offer a promising alternative to autoregressive models by accelerating inference through parallel decoding. Recent dLLMs commonly use blockwise semi-autoregressive decoding, generating blocks autoregressively while denoising tokens within each active block in parallel. However, despite KV caching, each denoising step still attends to all previous blocks, repeatedly incurring prefix-attention cost. Motivated by this bottleneck, we ask whether dLLM inference can be further accelerated by linearizing attention over previous blocks. We introduce block-hybrid attention, which retains exact softmax attention within the active denoising block while applying linear attention over previous blocks. We show that this hybrid attention can be retrofitted into a pretrained dLLM with minimal post-training: LLaDA-Hybrid replaces 6 of the 20 attention layers in LLaDA~2.1, a 16B open-source dLLM, largely following LoLCAT (Zhang et al, 2024). The conversion takes only approximately 60 hours while preserving benchmark performance: 72.0% vs. 75.6% on HumanEval, 63.0% vs. 57.7% on MBPP+, and 86.7% vs. 88.3% on CMATH. With a Triton implementation, LLaDA-Hybrid achieves up to $1.7\times$ higher decoding throughput and supports more concurrent requests before exhausting memory, showing that pretrained dLLMs can be efficiently linearized for faster inference. Our code is available at: https://github.com/Diuven/LLaDA-Hybrid.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrofitting Linear Attention into Diffusion Language Models》的结构化技术总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：扩散语言模型（dLLMs）通过并行解码提供了优于传统自回归模型的推理速度。目前主流的 dLLM（如 LLaDA）采用“分块半自回归解码”策略：块与块之间是自回归的，而块内部的 Token 是并行去噪的。
*   **核心问题**：尽管使用了 KV 缓存，但在对当前块进行去噪时，模型仍需对之前所有已生成的块进行 Softmax 注意力计算。随着序列长度增加，这种“前缀注意力”产生的计算开销和内存占用成为了推理瓶颈。
*   **研究目的**：探索是否可以通过将 dLLM 中针对历史块的注意力“线性化”（Linear Attention），在保持生成质量的同时进一步提升推理吞吐量。

### 2. 方法论：块混合注意力（Block-hybrid Attention）
*   **核心思想**：将注意力机制拆分为两个分支。在当前活动的去噪块内部保留精确的**双向 Softmax 注意力**以确保局部交互质量；对于已经完成生成的历史块，则将其压缩为固定大小的**线性递归状态**。
*   **关键技术细节**：
    *   **线性化内核**：采用 Hedgehog 线性注意力方案，通过学习特征映射 $\phi(x)$ 将指数内核近似为特征向量的内积。
    *   **混合计算**：对于当前块的查询 $q$，最终输出是“块内 Softmax 分支”和“跨块线性分支”的加权组合，并使用共享的分母进行归一化，以平衡两者的注意力权重。
    *   **两阶段改造流程**：
        1.  **阶段 1：注意力转移（Attention Transfer）**：冻结主干，仅训练线性特征映射和门控参数，使混合层在局部输出上匹配原始 Softmax 层的输出（使用 MSE 损失）。
        2.  **阶段 2：端到端适配（End-to-end Adaptation）**：使用 LoRA 插件对全模型进行微调，通过标准的掩码扩散目标函数（Masked Diffusion Objective）修复层间累积误差。

### 3. 实验设计
*   **基础模型**：16B 规模的开源 dLLM **LLaDA 2.1-mini**（共 20 层注意力）。
*   **实验配置**：将其中 6 层（索引为 0, 4, 8, 12, 16, 18）替换为块混合注意力层。
*   **评估基准（Benchmarks）**：
    *   **代码能力**：HumanEval, HumanEval+, MBPP, MBPP+。
    *   **数学与推理**：CMATH, GPQA-Diamond。
*   **对比维度**：与原始全 Softmax 注意力的 LLaDA 模型在准确率、解码吞吐量（Tokens/s）以及内存承载能力（最大并发请求数）上进行对比。

### 4. 资源与算力
*   **硬件设备**：训练使用了 **2 台 NVIDIA L40S GPU**；推理测试使用了 **NVIDIA H200 GPU**。
*   **训练时长**：
    *   阶段 1（注意力转移）：约 24 小时（处理约 1700 万个 Token）。
    *   阶段 2（LoRA 适配）：约 6 小时（处理约 500 万个 Token）。
    *   **总计**：论文摘要提到整个转换过程约需 **60 小时**（包含数据准备与调试）。
*   **数据**：使用公开的 Tulu SFT 混合数据集。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了代码、数学、科学推理等多个核心任务，并进行了不同并发压力下的吞吐量测试。
*   **消融实验**：作者对比了线性化不同层数（6 层 vs 8 层）对性能的影响，发现 6 层是质量与效率的最佳平衡点。
*   **微观测试**：提供了针对 Triton 内核的算子级测试（Microbenchmark），展示了线性化层在长前缀下的常数级延迟优势。
*   **评价**：实验设计较为客观，直接在 SGLang 推理框架中实现并对比，具有较强的工程参考价值。

### 6. 主要结论与发现
*   **性能保持**：LLaDA-Hybrid 在大多数基准测试中保持了与原模型相当的水平（如 HumanEval 72.0% vs 75.6%），在 MBPP+ 上甚至有所提升。
*   **效率提升**：在 SGLang 框架下，相比优化后的 Softmax 基准，实现了高达 **1.7 倍** 的端到端解码吞吐量提升。
*   **内存优势**：由于线性层使用固定大小的状态，模型在相同内存预算下能支持多出约 **30%** 的并发请求。
*   **长序列优势**：随着前缀长度增加，线性化层的加速比呈线性增长（在 32K 前缀下，算子级加速可达 30-80 倍）。

### 7. 优点
*   **首创性**：这是首个将线性注意力蒸馏应用于扩散语言模型的研究。
*   **高效改造**：无需从头预训练，通过极低成本的训练后处理（Post-training）即可完成 16B 模型的高效线性化。
*   **工程落地**：提供了高性能的 Triton 内核实现，并集成到主流推理框架 SGLang 中，证明了实际部署的可行性。

### 8. 不足与局限
*   **性能损耗**：在极高难度的推理任务（如 GPQA-Diamond）上仍存在约 8 个百分点的性能下降。
*   **部分线性化**：仅线性化了 6/20 的层，这意味着模型仍保留了部分 $O(L^2)$ 的开销，未能完全消除序列长度依赖。
*   **长文本验证不足**：目前的评估主要集中在 2048 Token 左右的长度，对于超长文本（如 100K+）的生成质量和推理稳定性尚需进一步验证。
*   **块大小依赖**：模型假设了固定的推理块大小，对不同推理配置的鲁棒性有待研究。

（完）
