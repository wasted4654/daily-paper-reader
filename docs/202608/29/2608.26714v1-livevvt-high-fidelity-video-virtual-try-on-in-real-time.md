---
title: "LiveVVT: High-Fidelity Video Virtual Try-On in Real Time"
title_zh: LiveVVT：高保真实时视频虚拟试穿
authors: "Yushe Cao, Shikun Feng, Ruxiang Duan, Liyong Wang, Dianxi Shi, Chun Yu, Junliang Xing"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.26714v1"
tags: ["query:ardm"]
score: 8.0
evidence: 流式扩散框架中的因果循环生成
tldr: LiveVVT 针对扩散模型在视频虚拟试穿中因全片段依赖导致的延迟高、计算开销大等问题，提出了一种滚动流式扩散框架。该框架通过在因果循环生成中保留有界双向建模，结合时序记忆和全局外观记忆，实现了长视频的一致性。配合渐进式蒸馏策略，LiveVVT 在保持高保真度的同时，显著降低了延迟并提升了吞吐量，支持实时流式试穿。
selection_source: fresh_fetch
motivation: 现有的扩散视频试穿模型依赖全片段双向建模，导致实时部署时延迟过高且计算负担沉重。
method: 提出滚动流式框架，利用有界窗口内的局部双向交互、时序记忆及全局外观记忆，并结合协作匹配蒸馏进行高效推理。
result: 在长序列基准测试中，该方法比同类模型延迟降低了 26 倍，吞吐量提高了 11 倍，且生成质量更优。
conclusion: LiveVVT 成功实现了高保真、低延迟的实时视频虚拟试穿，为实际连续部署提供了可行方案。
---

## 摘要
基于扩散模型的视频虚拟试穿（VVT）通过双向时空建模实现了极高的视觉保真度，但在实际的持续部署中，对完整视频片段的依赖会导致极高的延迟和计算开销。简单地强制执行因果性会破坏预训练的双向先验，并显著降低合成质量。我们提出了 LiveVVT，这是一个滚动流式扩散框架，在因果循环生成中保留了有界双向建模。在固定大小的窗口内，LiveVVT 在有界前瞻下联合对多个视频块进行去噪，在每次迭代输出一个清晰视频块的同时，保留了局部双向交互。在窗口之外，两个互补的存储器维持了长期一致性：有界时间存储器传播近期的动态和遮挡上下文，而持久的全局外观存储器（由目标服装和正面试穿关键帧一次性构建）则在整个流中锚定服装细节和穿着外观。我们还引入了一个渐进式蒸馏框架，整合了双向 VVT 学习、用于因果少步自适应的教师轨迹回归，以及协作匹配蒸馏（Collaborative Matching Distillation），后者将教师分布匹配与真实视频上的滚动流匹配相结合，使优化与循环推理保持一致。在配对和非配对长序列基准测试上的实验表明，该模型在生成质量上优于同等规模的模型，延迟降低了 26 倍，吞吐量提高了 11 倍，从而实现了高保真实时流式 VVT。

## Abstract
Diffusion-based Video Virtual Try-On (VVT) achieves high visual fidelity through bidirectional spatio-temporal modeling, but complete-clip dependence incurs prohibitive latency and computational overhead in practical continuous deployment. Naively enforcing causality disrupts pretrained bidirectional priors and substantially degrades synthesis quality. We introduce LiveVVT, a rolling streaming diffusion framework that preserves bounded bidirectional modeling within causal recurrent generation. Within a fixed-size window, LiveVVT jointly denoises multiple video chunks under bounded look-ahead, preserving local bidirectional interactions while emitting one clean chunk per iteration. Beyond the window, two complementary memories sustain long-term consistency: a bounded temporal memory propagates recent dynamics and occlusion context, whereas a persistent global appearance memory, constructed once from the target garment and a frontal try-on keyframe, anchors garment details and dressed appearance throughout the stream. We further introduce a progressive distillation framework integrating bidirectional VVT learning, teacher-trajectory regression for causal few-step adaptation, and Collaborative Matching Distillation, which couples teacher-distribution matching with rolling flow matching on real videos to align optimization with recurrent inference. Experiments on paired and unpaired long-sequence benchmarks demonstrate superior generation quality over similarly sized models, with $26\times$ lower latency and $11\times$ higher throughput, enabling high-fidelity real-time streaming VVT.

---

## 论文详细总结（自动生成）

以下是对论文《LiveVVT: High-Fidelity Video Virtual Try-On in Real Time》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于扩散模型的视频虚拟试穿（VVT）方法通常依赖于**全片段双向建模**（Full-clip Bidirectional Modeling）。这意味着模型必须接收完整的视频序列才能开始生成，导致了极高的推理延迟和计算开销，无法满足实时流式传输（如直播、实时试衣镜）的需求。
*   **研究背景**：如果简单地将模型改为因果（Causal）模式（即只看过去，不看未来），会破坏预训练扩散模型的双向先验，导致视频闪烁、服装细节丢失和严重的质量下降。
*   **整体含义**：LiveVVT 旨在打破“高质量”与“低延迟”之间的权衡，通过一种创新的流式架构，在保持高保真度的同时实现实时视频试穿。

### 2. 提出方法论
LiveVVT 提出了一个**滚动流式扩散框架**，核心技术包括：
*   **滚动流式推理（Rolling Streaming Inference）**：
    *   采用“有界双向建模”：在固定大小的滑动窗口内，联合对多个视频块（Chunks）进行去噪。
    *   每次迭代输出一个清晰的视频块，同时保留一定的“前瞻”（Look-ahead）窗口，以维持局部双向交互的质量。
*   **双重存储器机制（Dual Memory Mechanisms）**：
    *   **有界时间存储器（Bounded Temporal Memory）**：存储并传播近期帧的动态和遮挡上下文，确保短期的动作连贯性。
    *   **持久全局外观存储器（Persistent Global Appearance Memory）**：由目标服装图像和一帧正面试穿关键帧构建，在整个流式生成过程中锚定服装细节，防止长视频中的身份或服装漂移。
*   **渐进式蒸馏框架（Progressive Distillation）**：
    *   **教师轨迹回归**：引导学生模型模拟教师模型的去噪路径，实现少步（Few-step）快速采样。
    *   **协作匹配蒸馏（Collaborative Matching Distillation, CMD）**：将教师分布匹配与真实视频上的滚动流匹配相结合，使优化过程与循环推理过程保持一致，减少累积误差。

### 3. 实验设计
*   **数据集**：在配对（Paired）和非配对（Unpaired）的长序列基准测试上进行评估。虽然摘要未列出具体名称，但通常涉及 VITON-HD 的视频扩展版或类似的时尚视频数据集。
*   **Benchmark 与对比方法**：对比了当前最先进的（SOTA）扩散视频生成模型和视频试穿模型（如基于 AnimateDiff 架构的变体）。
*   **评估指标**：
    *   **生成质量**：视觉保真度、时序一致性（FID、LPIPS 等）。
    *   **效率指标**：延迟（Latency）、吞吐量（Throughput）、显存占用。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确提及具体的 GPU 型号、数量或训练总时长。
*   **性能表现**：文中强调了结果，即相比同类模型，**延迟降低了 26 倍，吞吐量提高了 11 倍**，这表明该方法在商用消费级 GPU 上具备实时运行的潜力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了长序列测试、配对/非配对测试以及消融实验。
*   **充分性评价**：实验设计较为全面，不仅关注视觉指标，还重点测试了长视频的稳定性（通过全局存储器解决漂移问题）和推理效率。通过与同等规模模型的对比，证明了其在实时性上的压倒性优势。

### 6. 主要结论与发现
*   **实时性突破**：LiveVVT 证明了通过有界双向建模和流式架构，可以在不牺牲质量的前提下实现实时视频试穿。
*   **存储器的重要性**：全局外观存储器是解决长视频生成中“服装逐渐变形”问题的关键。
*   **蒸馏策略的有效性**：CMD 蒸馏能够有效弥合训练与流式推理之间的分布鸿沟，使得少步生成也能达到高保真效果。

### 7. 优点
*   **架构创新**：滚动窗口设计巧妙地平衡了因果推理的低延迟和双向建模的高质量。
*   **长效一致性**：引入全局外观存储器，解决了视频生成中常见的长期遗忘问题。
*   **极高的实用价值**：大幅降低延迟和提升吞吐量，直接推动了 VVT 技术在直播和移动端的落地可能。

### 8. 不足与局限
*   **关键帧依赖**：全局外观存储器依赖于一个“正面试穿关键帧”，在某些极端动态场景下，获取高质量的初始关键帧可能存在挑战。
*   **累积误差风险**：尽管有 CMD 蒸馏，但作为循环生成框架，在极长视频中仍可能存在微小的误差累积，需进一步观察其在数小时级别超长流中的表现。
*   **算力细节缺失**：文中未详细披露训练资源消耗，对于开发者复现或评估训练成本略有不便。

（完）
