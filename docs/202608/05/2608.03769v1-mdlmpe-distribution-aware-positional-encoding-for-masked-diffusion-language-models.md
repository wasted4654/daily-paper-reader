---
title: "MDLMPE: Distribution Aware Positional Encoding for Masked Diffusion Language Models"
title_zh: MDLMPE：针对掩码扩散语言模型的分布感知位置编码
authors: "Tong Ling, Hang Lei, Feng Xiao, Changhui Sun, Jiahang Xie, Hao Liu, Lu Liu, Yanlong Du"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03769v1"
tags: ["query:ardm"]
score: 8.0
evidence: 掩码扩散语言模型与自回归模型的对比及位置编码设计
tldr: 针对掩码扩散语言模型（MDLM）中动态且非连续的标记可用性结构，本文提出了MDLMPE，这是首个使位置表示显式感知已揭示/掩码配置的方法。该方法通过高斯加权和余弦基函数将标记可用性转化为分布感知特征，并以此调制RoPE相位。实验证明，MDLMPE在LLaDA和DREAM等模型上的多种任务中均优于传统位置编码，确立了标记可用性分布作为MDLM有效位置信号的地位。
selection_source: fresh_fetch
motivation: 传统位置编码（如RoPE）无法捕捉掩码扩散模型在去噪过程中动态变化的非连续标记可用性结构。
method: 提出MDLMPE，通过将标记可用性序列进行高斯加权和频谱投影，生成分布感知特征来动态调制RoPE的相位偏移。
result: 在LLaDA和DREAM模型上的实验表明，MDLMPE在监督微调、预训练和零样本评估等多个场景下均优于传统方法。
conclusion: 演进中的标记可用性分布是提升掩码扩散语言模型性能的关键位置信号。
---

## 摘要
掩码扩散语言模型 (MDLM) 支持并行生成和双向上下文建模，但其位置上下文与自回归 (AR) 模型有着本质区别。自回归解码暴露的是连续的前缀，而 MDLM 去噪过程则产生已显露和已掩码标记的动态、非连续配置。传统的位置编码（如 RoPE）虽然捕捉了序列顺序和成对位移，但对这种不断演变的标记可用性结构并不敏感。为了解决这一局限性，我们提出了 MDLMPE，一种专门为掩码扩散设计的位置编码。据我们所知，MDLMPE 是首个使位置表示能够显式感知不断变化的显露/掩码配置的方法。它将标记可用性表示为二进制序列，应用距离感知的高斯加权，并通过余弦基投影所得模式，从而获得分布感知的位置特征。这些特征被添加到标记嵌入中，并通过轻量级 MLP 映射为角偏移量，用以调制标准的 RoPE 相位。在 LLaDA 和 DREAM 上的广泛实验表明，MDLMPE 在有监督微调、预训练、零样本评估和块扩散设置中通常优于传统的位置编码方法。进一步的消融实验表明，可用性状态、高斯局部性、谱基和嵌入注入的完整组合能产生最强的效果。这些结果确立了不断演变的标记可用性分布是掩码扩散语言模型的一种有效位置信号。

## Abstract
Masked diffusion language models (MDLMs) enable parallel generation and bidirectional context modeling, but their positional context differs fundamentally from that of autoregressive (AR) models. Whereas AR decoding exposes a contiguous prefix, MDLM denoising produces dynamic, non-contiguous configurations of revealed and masked tokens. Conventional positional encodings such as RoPE capture sequence order and pairwise displacement but remain insensitive to this evolving token-availability structure. To address this limitation, we propose MDLMPE, a positional encoding designed specifically for masked diffusion. To the best of our knowledge, MDLMPE is the first method to make positional representations explicitly aware of the changing revealed/masked configuration. It represents token availability as a binary sequence, applies distance-aware Gaussian weighting, and projects the resulting pattern through a cosine basis to obtain distribution-aware positional features. These features are added to token embeddings and mapped by a lightweight MLP to angular offsets that modulate the standard RoPE phases. Extensive experiments on LLaDA and DREAM demonstrate that MDLMPE generally outperforms conventional positional encoding methods across supervised fine-tuning, pretraining, zero-shot evaluation, and block-diffusion settings. Further ablations show that the complete combination of availability state, Gaussian locality, spectral basis, and embedding injection yields the strongest result. These results establish the evolving token-availability distribution as a useful positional signal for masked diffusion language models.

---

## 论文详细总结（自动生成）

### MDLMPE：针对掩码扩散语言模型的分布感知位置编码——论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
掩码扩散语言模型（MDLM）通过迭代去噪来生成文本，支持并行生成和双向上下文建模。然而，MDLM 的去噪过程与自回归（AR）模型有本质区别：AR 模型在解码时暴露的是连续的前缀，而 MDLM 在去噪过程中，已显露（Revealed）和仍被掩码（Masked）的标记呈现出**动态、非连续且不断演变**的分布结构。
现有的主流位置编码（如 RoPE）仅捕捉序列顺序和成对位移，无法感知这种“可用标记”的分布变化。论文的核心动机在于：一个标记的预测不仅取决于它在序列中的绝对位置，还取决于其周围可用上下文的组织方式。因此，需要一种能够显式感知显露/掩码配置的位置编码。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MDLMPE**（Masked Diffusion Language Model Positional Encoding），其核心思想是将当前的标记可用性状态转化为位置信号。

*   **可用性表示**：将序列状态表示为二进制序列 $b \in \{0, 1\}^L$，其中 1 代表已显露标记，0 代表掩码或无效标记。
*   **高斯局部加权**：为了体现上下文的相关性，应用以目标标记为中心的高斯窗口进行加权，使得近距离的可用标记对当前位置的影响更大。
*   **频谱投影（RoPE 对齐）**：利用一组与 RoPE 频率网格对齐的余弦基函数（Cosine Basis），将高斯加权后的可用性模式投影为多尺度的分布特征。低频捕捉全局可用性，高频捕捉局部细节。
*   **双路径注入机制**：
    *   **标记路径（Embedding 注入）**：将提取的分布特征通过线性投影后，以残差形式添加到标记的嵌入（Embedding）中。
    *   **成对路径（Rotary 调制）**：计算两个位置间的联合可用性特征，通过轻量级 MLP 映射为角度偏移量 $\delta$，直接调制 RoPE 的旋转相位。
*   **兼容性设计**：为了不破坏预训练模型的几何结构，MDLMPE 采用零初始化和渐进式激活策略，确保模型可以平滑地从标准 RoPE 过渡。

#### 3. 实验设计
研究者在多种场景下验证了 MDLMPE 的有效性：
*   **模型架构**：基于 **LLaDA**（8B）和 **DREAM**（7B）两个主流 MDLM 框架。
*   **实验场景**：
    1.  **指令后训练（SFT）**：在 Tulu-3 和 SmolTalk 数据集上进行微调。
    2.  **从零预训练**：在 100M 参数规模上使用 OpenWebText 进行训练。
    3.  **块扩散（Block Diffusion）**：测试在不同块大小设置下的鲁棒性。
*   **Benchmark**：涵盖 MMLU、MMLU-Pro（综合知识）、HellaSwag（常识推理）、ARC-C（科学挑战）、GSM8K、MATH（数学推理）、HumanEval、MBPP（代码生成）。
*   **对比方法**：主要对比 **RoPE**（基线），并在小规模实验中对比了 **ALiBi** 和**绝对位置编码**。

#### 4. 资源与算力
论文明确提到了算力使用情况：
*   **硬件**：所有训练实验均在 **8 张 NVIDIA A100 80GB GPU** 上完成。
*   **训练细节**：100M 模型预训练运行了 200K 步；7B/8B 模型的 SFT 采用了 bfloat16 精度和 FSDP（完全分片数据并行）技术。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：不仅在大规模模型（7B/8B）上验证了微调效果，还在小模型（100M）上验证了作为归纳偏置的预训练效果。
*   **消融实验**：详细分析了二进制状态、高斯局部性、频谱基函数和双路径注入等组件的贡献，证明了完整组合的最优性。
*   **客观性**：通过固定随机种子、数据顺序、解码预算和评估框架，确保了 MDLMPE 与 RoPE 基线之间的公平对比。

#### 6. 主要结论与发现
*   **性能提升**：MDLMPE 在几乎所有下游任务中均优于 RoPE。例如，在 LLaDA-8B 上，GSM8K 准确率从 67.4 提升至 70.1。
*   **分布感知的重要性**：实验证明，动态标记分布为 MDLM 提供了除绝对索引之外的关键位置线索，有助于模型在非连续上下文中更好地定位语义证据。
*   **架构鲁棒性**：MDLMPE 能够很好地适应不同的去噪策略（如块扩散），无需修改目标函数或采样规则。

#### 7. 优点
*   **首创性**：首次针对 MDLM 的动态去噪特性设计了专门的位置编码。
*   **设计精巧**：通过高斯加权和频谱投影，既保留了 RoPE 的相对位置优势，又引入了状态感知能力。
*   **良好的兼容性**：能够无缝集成到现有的 RoPE 预训练模型中，通过渐进式微调即可获得增益。
*   **理论完备**：附录中提供了详尽的确定性机制分析（如 Jacobian 边界、边界归一化等）。

#### 8. 不足与局限
*   **计算开销**：虽然论文提到增加了轻量级 MLP，但成对相位的计算在注意力机制内部引入了额外的 $O(L^2)$ 相关计算，尽管通过查询分块（Query Blocking）进行了优化，但在极长序列下的开销仍需关注。
*   **模型覆盖**：实验主要集中在 LLaDA 和 DREAM 两种架构上，尚未在超大规模（如 100B+）或其他非 Transformer 骨干网络上进行验证。
*   **超参数敏感性**：高斯窗口半径 $W$ 和带宽 $\sigma$ 需要根据序列长度进行调整，可能存在一定的调参成本。

（完）
