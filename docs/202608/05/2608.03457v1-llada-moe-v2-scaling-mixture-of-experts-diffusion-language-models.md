---
title: "LLaDA MoE v2: Scaling Mixture-of-Experts Diffusion Language Models"
title_zh: LLaDA MoE v2：扩展混合专家扩散语言模型
authors: "Fengqi Zhu, Shaoxuan Xu, Jingyang Ou, Zebin You, Yipeng Xing, Huabin Liu, Xiaolu Zhang, Jun Zhou, Zhenzhong Lan, Yankai Lin, Wayne Xin Zhao, Jianguo Li, Chongxuan Li, Ji-Rong Wen"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03457v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型作为自回归模型替代方案的缩放行为研究
tldr: 本研究系统探讨了混合专家扩散语言模型（MoE dLLMs）的缩放定律，揭示了其在优化超参数、计算分配和架构设计上与自回归模型的显著差异。基于这些发现，团队训练了拥有30B总参数、3B激活参数的LLaDA MoE v2模型。该模型在23.5T token上预训练后，在知识、推理和代码基准测试中表现优异，接近Qwen3并超越了SDAR Chat，为扩散语言模型的大规模应用提供了实践指南。
selection_source: fresh_fetch
motivation: 旨在填补混合专家扩散语言模型在缩放行为和架构优化规律研究上的空白。
method: 通过系统性实验分析优化超参数、计算分配及MoE架构随规模变化的定量趋势，并据此训练大规模模型。
result: 发现MoE dLLMs的最优批量大小增长更快且学习率衰减更剧烈，训练出的LLaDA MoE v2在多项基准测试中接近或超越主流模型。
conclusion: 本研究确立了MoE dLLMs的实用缩放定律和设计原则，证明了扩散模型在语言建模领域的竞争力。
---

## 摘要
扩散语言模型 (dLLMs) 为自回归 (AR) 语言建模提供了一种替代方案，但混合专家 (MoE) dLLMs 的扩展行为仍未得到充分理解。我们系统地刻画了 MoE dLLMs 的优化超参数、计算分配和架构如何随规模扩展，并识别出其与此前报道的 AR 模型扩展趋势之间的定量差异。具体而言，在优化方面，最优标称批大小增长更快，而最优学习率随计算量的增加衰减得更迅速。在模型-数据分配方面，IsoFLOP 分析揭示了轻微的数据侧倾斜：最优 token 预算的增长速度快于激活的模型侧计算量。在 MoE 架构方面，在固定激活容量的情况下，更大的规模越来越倾向于更大的专家池，而中等的专家粒度始终保持有效，且分配给共享专家的激活容量比例在不同规模下保持稳定。在这些发现的指导下，我们从零开始在 23.5T token 上训练了 LLaDA MoE v2，这是一个 30B-A3B 的 dLLM。在预训练 token 数量仅为 Qwen3 约 65% 的情况下，LLaDA MoE v2 在多个知识、推理和编程基准测试中接近 Qwen3 的表现。仅经过有监督微调，它在八个推理和编程基准测试中的七个上优于 SDAR Chat，并在多项任务中保持与 Qwen3 接近的水平。这些结果为 MoE dLLMs 确立了实用的扩展定律和设计原则。

## Abstract
Diffusion language models (dLLMs) offer an alternative to autoregressive (AR) language modeling, yet the scaling behavior of Mixture-of-Experts (MoE) dLLMs remains poorly understood. We systematically characterize how optimization hyperparameters, compute allocation, and architecture scale for MoE dLLMs, identifying quantitative differences from scaling trends previously reported for AR models. Specifically, for optimization, the optimal nominal batch size grows faster, while the optimal learning rate decays more rapidly with compute. For model--data allocation, IsoFLOP analysis reveals a slight data-side tilt: the optimal token budget grows faster than activated model-side computation. For MoE architecture, larger scales increasingly favor larger expert pools at fixed activated capacity, while moderate expert granularity remains consistently effective and the preferred fraction of activated capacity assigned to shared experts remains stable across scales. Guided by these findings, we train LLaDA MoE v2, a 30B-A3B dLLM, from scratch on 23.5T tokens. With approximately 65\% as many pretraining tokens as Qwen3, LLaDA MoE v2 approaches Qwen3 on several knowledge, reasoning, and coding benchmarks. After supervised fine-tuning alone, it outperforms SDAR Chat on seven of eight reasoning and coding benchmarks and remains close to Qwen3 on several tasks. These results establish practical scaling laws and design principles for MoE dLLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLaDA MoE v2: Scaling Mixture-of-Experts Diffusion Language Models》的深度学术总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然自回归（AR）模型的缩放定律（Scaling Laws）已得到深入研究，但扩散语言模型（dLLMs）尤其是结合混合专家架构（MoE）后的缩放行为尚不明确。
*   **核心问题**：AR 模型的经验（如超参数设置、模型与数据的分配比例）能否直接迁移到 MoE dLLMs？如果不能，MoE dLLMs 的最优缩放路径是什么？
*   **整体含义**：本研究通过系统性的实验，首次确立了 MoE dLLMs 的缩放定律，并据此训练出高性能的 30B 规模模型 LLaDA MoE v2，证明了扩散模型在大规模语言建模上的潜力。

### 2. 方法论
论文将缩放定律的研究分为三个阶段：
*   **优化超参数缩放**：研究最优标称批大小（Batch Size, $B^*$）和学习率（Learning Rate, $\eta^*$）随计算量 $C$ 的变化。
    *   发现 $B^*$ 的增长指数（0.3481）高于 AR 模型，$\eta^*$ 的衰减速度（-0.2447）也更快。
*   **计算分配缩放（IsoFLOP 分析）**：在固定计算预算下，平衡激活的模型参数量（$M$）与训练 Token 数（$D$）。
    *   得出公式：$M^* \propto C^{0.475}$，$D^* \propto C^{0.525}$。这表明 MoE dLLMs 相比密集型模型更倾向于“数据侧倾斜”，即增加数据量带来的收益更高。
*   **MoE 架构缩放**：分解激活预算，研究三个维度：
    *   **激活比例（A）**：随规模增大，更倾向于更稀疏的激活（更小的 A）。
    *   **专家粒度（G）**：中等粒度（G=8~16）在各规模下表现稳健。
    *   **共享专家比例（S）**：发现 $S=33.3\%$ 是最优的，这与 AR 模型（通常不使用或使用极少共享专家）显著不同。
*   **LLaDA MoE v2 模型设计**：基于上述定律，采用 30B 总参数（3B 激活），128 个路由专家（Top-8 激活）加 1 个 4 倍宽度的共享专家。

### 3. 实验设计
*   **数据集**：预训练使用了 23.5T 的高质量中英文及代码数据；SFT 阶段使用了 7M 条指令遵循数据。
*   **Benchmark**：涵盖 15 个主流基准测试，包括：
    *   **通用知识**：MMLU, MMLU-Pro, C-Eval, CMMLU 等。
    *   **推理能力**：GSM8K, MATH, OlympiadBench。
    *   **代码生成**：HumanEval, MBPP, MultiPL-E, BigCodeBench 等。
*   **对比方法**：
    *   **dLLM 基线**：SDAR Sci, LLaDA 8B, LLaDA MoE v1 (7B-A1B)。
    *   **AR 基线**：Qwen3 30B-A3B。

### 4. 资源与算力
*   **算力消耗**：整个预训练过程消耗了约 **460,000 NVIDIA B200 GPU 小时**。
*   **训练细节**：分为五个阶段，包括基础预训练、退火（Annealing）以及长文本扩展（从 4K 扩展到 32K）。

### 5. 实验数量与充分性
*   **实验规模**：在推导缩放定律阶段，进行了从 158M 到 3.6B 不同规模的密集实验，计算量跨越 $10^{18}$ 到 $6 \times 10^{20}$ FLOPs。
*   **充分性**：实验设计非常系统，涵盖了优化、分配和架构三个关键层面。通过 IsoFLOP 曲线验证了最优点的存在，并进行了跨规模的趋势拟合。
*   **客观性**：对比实验中，LLaDA MoE v2 在仅使用 Qwen3 约 65% 的数据量下达到了相近的性能，且在 SFT 后对比 SDAR Chat 具有明显优势，实验对比具有说服力。

### 6. 主要结论与发现
*   **dLLM 独特规律**：dLLM 的梯度噪声和监督模式与 AR 不同，导致其需要更大的批处理量和更快的学习率衰减。
*   **数据效率**：MoE dLLM 展现出极高的数据利用效率，在较少 Token 下即可匹配强力 AR 模型的性能。
*   **共享专家的重要性**：在扩散模型中，共享专家对于稳定跨噪声水平的通用表征至关重要，其最优比例远高于 AR 模型。
*   **性能跨越**：LLaDA MoE v2 在推理和代码任务上表现卓越，SFT 后的推理能力甚至在部分指标上超越了经过 RL 强化的 Qwen3。

### 7. 优点
*   **理论指导实践**：并非盲目堆算力，而是先通过小规模实验确立 Scaling Laws，再指导大规模模型训练，极具科学性。
*   **架构创新**：针对扩散模型双向注意力和掩码机制，优化了 MoE 的共享专家设计。
*   **开源贡献**：为扩散语言模型社区提供了宝贵的超参数设置和架构设计准则。

### 8. 不足与局限
*   **维度交互缺失**：研究主要采用单变量控制法（每次改变一个维度），未能完全捕捉超参数、分配和架构之间的多维非线性交互。
*   **RL 阶段缺失**：目前仅完成了 SFT，尚未引入强化学习（RL），这可能限制了模型在复杂逻辑对齐上的上限。
*   **推理开销**：尽管是 MoE 架构，但扩散模型通常需要多步迭代生成，其推理延迟（Latency）仍高于单步生成的 AR 模型（尽管文中提到了并行解码的潜力）。

（完）
