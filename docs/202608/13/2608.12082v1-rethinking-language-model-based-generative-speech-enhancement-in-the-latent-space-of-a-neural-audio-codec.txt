Title: Rethinking Language Model-Based Generative Speech Enhancement in the Latent Space of a Neural Audio Codec

URL Source: https://arxiv.org/pdf/2608.12082v1

Published Time: Thu, 13 Aug 2026 00:57:39 GMT

Number of Pages: 5

Markdown Content:
# RETHINKING LANGUAGE MODEL-BASED GENERATIVE SPEECH ENHANCEMENT IN THE LATENT SPACE OF A NEURAL AUDIO CODEC 

# Yihui Fu, Zhengyang Li, Tim Fingscheidt 

# Institute for Communications Technology, TU Braunschweig, Braunschweig, Germany 

ABSTRACT 

Language model (LM)-based speech enhancement (SE) has recently emerged rapidly using latent space features of neural audio codecs (NACs). In this paper, first, we present a unified framework covering 

six popular LM-based generative SE modeling paradigms based on discrete/continuous latent NAC features: discrete or continuous au-toregressive ( D/CAR ) SE, discrete or continuous non-autoregressive (D/CNAR ) SE, discrete diffusion ( DDiff ) SE, and continuous flow matching ( CFM ) SE. Second, we are the first to compare their per-formance in a unified experimental setup and synopsis with diverse intrusive and non-intrusive metrics, enabling a fair and compre-hensive evaluation. Third, we propose a fine-tuning strategy with 

auxiliary losses on reconstructed speech to improve both intrusive and non-intrusive metrics. Trained and evaluated on URGENT 2025 Speech Enhancement Challenge data splits, all continuous-domain paradigms excel their discrete-domain counterparts. The overall best approach turns out to be CNAR . We further show that our pro-posed auxiliary loss fine-tuning strategy helps to improve DNSMOS, NISQA, PESQ, and POLQA consistently in all six paradigms 1.

Index Terms — LM-based SE, discrete/continuous NAC fea-tures, (non-) autoregressive, auxiliary losses 

1. INTRODUCTION 

Language model (LM)-based generative speech enhancement (SE) approaches have gained immense attention. Employing LMs in the enhancement process in a generative manner can harness their pow-erful semantic modeling capabilities, enabling better speech recon-struction beyond simple noise removal. LM-based SE typically takes discrete tokens or continuous latent features of the noisy speech as input to predict the corresponding clean speech features. Au-toregressive (AR), non-autoregressive (NAR), diffusion (typically NAR), and flow matching (typically NAR) are the most popular modeling paradigms in terms of LM-based generative SE. Concretely, AR SE takes noisy speech tokens and optionally noisy speech semantic features as the prompt to predict clean speech tokens in an AR manner. An embedding layer transforms the discrete tokens into continuous embeddings, followed by causal transformer-based modules (e.g., Llama [1]), is commonly adopted [2, 3, 4, 5, 6]. NAR SE takes the noisy speech tokens or continuous latent fea-tures as the input to generate the entire sequence of the correspond-ing clean speech features at once. A non-causal transformer-based architecture is commonly adopted [7]. Diffusion SE aims at mod-eling two processes between the clean speech distribution and the noisy signal prior distribution. The forward process transforms the 

> 1https://github.com/felixfuyihui/AR_NAR_ Diffusion_SE.git

clean features distribution into a known noisy features prior distri-bution, while the reverse process aims at sampling the clean fea-tures distribution starting from the prior distribution [8, 9, 10]. Flow matching SE [11, 12] aims at estimating a vector field to transform a prior distribution (noisy speech features) into a target data dis-tribution (clean speech features). Solving an ordinary differential equation (ODE) during inference is adopted to sample the enhanced speech feature. However, the research community lacks a broad and fair com-parison of all these LM-based generative SE paradigms under a uni-fied experimental setup and metrics . Furthermore, while achieving promising results on non-intrusive metrics, their performance on in-trusive metrics is often neither reported nor discussed. On the other hand, neural audio codecs (NACs) are being widely adopted in LM-based SE, benefiting from their compressed repre-sentations of noisy and clean speech for sequence modeling. Recent successful and widely adopted NACs are EnCodec [13] (at 1.5, 3, 6, and 12 kbps at 24 kHz waveform), DAC [14] (at 6 kbps at 16 kHz waveform), BiCodec [15] (at 0.65 kbps at 16 kHz waveform), and X-Codec [16] (at 0.5, 1, 1.5, 2, and 4 kbps at 16 kHz wave-form), etc. Features in the latent space, including continuous fea-tures from NACs’ encoders and discrete tokens from NACs’ resid-ual vector quantization (RVQ)-based quantizer [17], are commonly adopted in LM-based generative SE. Our contributions in this paper are threefold. First, we pro-pose a unified decoder-only LM framework for the six most pop-ular LM-based generative SE modeling paradigms based on latent NAC features, namely discrete/continuous autoregressive ( D/CAR )SE, discrete/continuous non-autoregressive ( D/CNAR ) SE, discrete diffusion ( DDiff ) SE in NAR manner, and continuous flow match-ing ( CFM ) SE in NAR manner. Second, we deliver a comprehen-sive and fair evaluation of the paradigms above, employing both 

non-intrusive and intrusive metrics, which has rarely been done in LM-based generative SE research so far. Our research claims to be the first synopsis of (non-) intrusive metrics of a range of LM-based SE paradigms . Third, a fine-tuning strategy with auxiliary losses on reconstructed speech is proposed to further improve their perfor-mance, consistently showing better DNSMOS, NISQA, PESQ, and POLQA. Note that the fine-tuning only applies to the LM model pa-rameters, requiring no updates to the pretrained codec model. This ensures the fine-tuned model to retain a high versatility. 

2. METHODS 2.1. Neural Audio Codecs 

Neural audio codecs (NACs) [13, 14, 15, 16] learn to map a time-domain signal into compact sequences of discrete tokens via a resid-ual vector quantization (RVQ) scheme [17]. Letting y and x repre-sent the time-domain noisy and clean speech vectors, respectively, arXiv:2608.12082v1 [eess.AS] 12 Aug 2026 WavLM DAC WavLM   

> SNC
> E

# DAC   

> SENNCC
> 🔥
> ❄❄
> 🔥 🔥 🔥
> ❄❄
> 🔥
> SS

# DAC  

> ❄

# DAC  

> ❄
> NC
> 🔥

Fig. 1 . Architecture of the proposed (a) discrete autoregressive ( DAR ) SE and (b) discrete non-autoregressive ( DNAR ) SE. Continuous (non-) autoregressive ( C(N)AR ) SE, discrete diffusion ( DDiff ) SE, and continuous flow matching ( CFM ) share similar topologies with the corresponding discrete domain SE, with slight modifications as described in the text. the tokenization and detokenization process of a NAC is: 

x E

−→ X ∈ RD×L Q

−→ Xc = ( Xc 

> ℓ

) ∈ NC×L = {0, ..., N −1}C×L,

(1) 

Xc Q−1

−−−→ ˜X ∈ RD×L D

−→ ˜x, (2) where E and D represent the NAC encoder and decoder, respectively, 

Q and Q−1 represent the quantization and dequantization processes, respectively, X and Xc represent the continuous features from en-coder and discrete tokens from the quantizer, with sequence length L

in frames, encoder output/decoder input feature dimension D, num-ber of codebooks C, and codebook size N , respectively. The same process can be applied to y. In this paper, we select the popular DAC 

[14] as the NAC model, with 20 ms non-overlapping frames on a 16 kHz waveform, which is further justified in Section 4.1. 

2.2. Condition Features Extractor 

We adopt the pre-trained WavLM [18] encoder as the condition fea-ture extractor to provide auxiliary semantic information. WavLM is a self-supervised speech representation model designed to learn robust and generalizable audio features from large-scale unlabeled speech data. Benefiting from its 20 ms non-overlapping frames on a 16 kHz waveform, the condition features extracted by the WavLM encoder are time-aligned with DAC features. 

2.3. LM-Based Generative SE Modeling Paradigms (i) Discrete Autoregressive ( DAR ) SE 

As shown in Fig. 1 (a), during training, the WavLM encoder and a fully connected (FC) linear layer extract the frame-by-frame con-dition features C ∈ RH×L from noisy speech y, where H is the hidden dimension of the subsequent LM. DAC tokens of both noisy and clean speech Yc, Xc ∈ NC×L are extracted as the input to the 

C parallel embedding layers with embedding dimension H. The sum of the C output embeddings are Ye, Xe ∈ RH×L, which are appended to C along the sequence length dimension as the input to the decoder-only AR LM to estimate the C logits/tokens for each frame. Unlike the token flattening strategy in [6], which increases the sequence length by a factor of C times, our strategy ensures that the sequence length remains unchanged to improve training and inference efficiency. Note that a set of special tokens/embeddings is employed to segment different domain features, including start of sequence token/embedding ( S /S ), end of sequence token ( E ), noisy start token/embedding ( N /N ), clean start token/embedding ( C /C ). The LM is trained in a teacher-forcing manner, and the cross-entropy (CE) loss between the predicted logits and the clean tokens Xc is minimized. The LM, together with FC linear and embedding layers, is updated, while the WavLM encoder and DAC modules are frozen. During inference, C and Ye are concatenated as prompt to the LM to predict the enhanced tokens ˆXc in a step-by-step manner, which are further input to the DAC dequantizer Q−1 and decoder D to estimate the enhanced speech waveform vector ˆx.

(ii) Continuous Autoregressive ( CAR ) SE 

During training, the DAC dequantizer output of noisy and clean speech ˜Y and ˜X are extracted, followed by an FC linear layer (sub-stitution of the embedding layers in DAR in Fig. 1 (a) to estimate embeddings Ye, Xe ∈ RH×L, which are appended to WavLM con-dition features C along the sequence length dimension as the input to the decoder-only AR LM. The LM estimates the enhanced un-quantized features ˆX. Minimizing the mean absolute error (MAE) loss between ˆX and clean speech DAC encoder output X is adopted during training in a teacher-forcing manner, which corresponds to minimizing the CE loss in the discrete token domain [5]. The model updating/freezing strategies are the same as with DAR . During infer-ence, C and ˜Y are concatenated as the prompt to estimate ˆX in a step-by-step manner. It is noticed that in both training and inference, we always use the dequantized representations ˜Y and ˜X as the input to the LM, instead of the unquantized DAC encoder output features 

Y and X. This can mitigate the error accumulation problem in the AR model [5]. For the same reason, in inference, the LM predicts the unquantized feature ˆX, which is further quantized ( Q) and de-quantized ( Q−1) to ˆ˜X as input for the next autoregressive step. The estimated ˆ˜X is further input to the DAC decoder D to estimate the enhanced speech waveform vector ˆx.

(iii) Discrete Non-Autoregressive ( DNAR ) SE 

As shown in Fig. 1 (b), DNAR aims to perform SE in the discrete token domain in a NAR manner. During training, the condition features C and the noisy speech embeddings Ye are extracted and added as the input to the decoder-only NAR LM. The CE loss be-tween the predicted logits and the clean tokens Xc is minimized. The module updating/freezing strategies are the same as with DAR .During inference, the same pipeline is adopted to estimate all the enhanced tokens ˆXc simultaneously in a single forward process, which are further input to the DAC dequantizer Q−1 and decoder D

to estimate the enhanced speech waveform vector ˆx.

(iv) Continuous Non-Autoregressive ( CNAR ) SE 

During training, an FC linear layer (substitution of the embedding layers in DNAR in Fig. 1 (b) is applied to DAC encoder output Y to estimate Ye, which is appended to C as input to the decoder-only NAR LM. The MAE loss between the enhanced features ˆX and the clean DAC encoder output X is minimized. The module updat-ing/freezing strategies are the same as with DNAR . During inference, the same pipeline is adopted to estimate all the enhanced features 

ˆX simultaneously in a single forward process, which are further input to the DAC quantizer Q, dequantizer Q−1, and decoder D to estimate the enhanced speech waveform vector ˆx.

(v) Discrete Diffusion ( DDiff ) SE 

DDiff largely follows the pattern of DNAR in Fig. 1 (b). During training, MaskGIT [19] is adopted as a random mask generator to apply masking on the clean tokens Xc with a special mask token [M] 

with index value N and time step index t ∽ U(0 , 1) to generate the masked token sequence Xc 

> t

. Then, the embeddings of Xc 

> t

, namely 

Xe 

> t

, are appended to the condition features C as the input to the decoder-only NAR LM. The CE loss between the predicted logits and the clean tokens Xc on the masked positions is minimized. The model training strategy is the same as for DNAR . During inference, a fully masked sequence of tokens Xc 

> T

∈ NC×L with time step index T =1 .0 is taken as the initial state of input to the model to predict the tokens on the masked positions. Then, an N ′-iteration diffusion process is applied. For each iteration, a portion of entry positions of the model output tokens, which are considered as esti-mated low-confidence tokens [19], are remasked by [M] as the next step’s input semantic token sequence Xc

> t−∆t

, with ∆t = T /N ′ and 

t ∈ { T, T −∆t ..., ∆t}. More details about inference are in [10]. The last-step output Xc0 is input to the DAC dequantizer Q−1 and decoder D to estimate the enhanced speech waveform vector ˆx.

(vi) Continuous Flow Matching ( CFM ) SE 

CFM [11, 12] largely follows the pattern of CNAR in Fig. 1 (b) and is applied in the DAC encoder output feature domain. During training, the state of the encoder output X with time step index t ∽ U(0 , 1) 

is calculated by 

Xt = t · X1 + (1 − t) · X0, (3) where X1 = X, X0 = Y. An FC linear layer (substitution of the embedding layers in DNAR in Fig. 1 (b) is applied to Xt to out-put Xe 

> t

, which is appended to C as the input to the decoder-only NAR LM to estimate the velocity vθ (X1, X0, C, t ), where θ are the trainable parameters of CFM . The FM loss 

Jfm = || vθ (X1, X0, Yc, t ) − (X1 − X0)|| , (4) is adopted. The model updating/freezing strategies are the same as with CNAR . During inference, starting from T = 0 .0, an ordinary differential equation (ODE) with N ′ iterations is adopted: 

Xt+∆ t = Xt + vθ (X1, X0, Yc, t ) · ∆t, (5) where ∆t=1 .0/N ′, t ∈ { 0.0, ∆t, ..., 1.0−∆t}. Finally, the esti-mated X1 is input to the DAC quantizer Q, dequantizer Q−1, and decoder D to estimate the enhanced speech waveform vector ˆx.

2.4. Fine-tuning with Auxiliary Losses 

To improve the performance of generative SE models, after the pre-training of the six paradigms above, we propose a fine-tuning strat-egy with auxiliary losses on reconstructed speech: 

J = Jtask + Jaux (ˆ x, x), (6) 

Jaux (ˆ x, x) = α · JBraun (STFT(ˆ x), STFT( x)) + γ · JL1 (ˆ x, x)+ δ · JPESQ (ˆ x, x) + η · JSTOI (ˆ x, x),

(7) where Jtask is the corresponding CE or MAE loss for each paradigm, 

JBraun is Braun’s and Tashev’s loss in the frequency domain fol-lowing [20, 21], JL1 is the time-domain L1 loss, JPESQ and JSTOI 

are differentiable PESQ [22] and STOI [23] losses, respectively, 

STFT( ·) represents the short-term Fourier transform with a frame length of 512 samples and 25% frameshift, α, γ, δ, η are hyperpa-rameters. The module updating/freezing strategies are the same as the corresponding pre-training strategies. It is noticed that we use a straight-through estimator (STE) [24] in discrete-domain paradigms when estimating tokens from logits to enable gradient backpropaga-tion. 

3. EXPERIMENTAL SETUP 3.1. Networks 

In the proposed SE models, the pretrained 16 kHz DAC [25] and 

WavLM [26] weights are adopted. For DAC , the encoder output di-mension is D=1024 , the codebook size is N =1024 , and the number of codebooks is C=12 , respectively. For WavLM , the 6th encoder layer’s output is adopted [2] with dimension D=1024 . For the AR and NAR tasks, we use a 24-layer Llama [1] and non-causal trans-former [27], respectively, with both hidden dimension H=1024 and 16 heads. For fair comparison, all Llama and transformer models are trained from scratch, thereby not using any pre-trained models. The total number of trainable parameters in the proposed DAR , CAR ,

DNAR , CNAR , DDiff , and CFM are 278.0 M, 256.0 M, 227.8 M, 204.7 M, 227.8 M, and 204.7 M, respectively, while the number of frozen parameters of the DAC and WavLM encoders is 74.2 M and 158.3 M, respectively. 

3.2. Data and Metrics 

We train and evaluate our proposed LM-based SE models on the large-scale diverse-source URGENT 2025 Speech Enhancement Challenge data splits [28], however, excluding CommonVoice 19.0 [29] due to its occasional background noise. For training data Dtrain ,we mainly follow the strategies in the challenge [28], in which six kinds of distortions are adopted: additive noise, reverberation, clip-ping, codec loss (MP3 and OGG), packet loss, and wind noise. We do not adopt the bandwidth limitation distortion from the challenge. The signal-to-noise ratio (SNR) range is [-5, 20] dB. An active speech level with a range [-36, -16] dB is adopted before mixing speech and noise data [30]. All waveforms are downsampled to 16 kHz. We configure a 1202.2 h training set Dtrain . We adopt the officially published validation and non-blind test set of the challenge and downsample it to 16 kHz, leading to our validation set Dval and test set Dtest . Each contains 1000 waveforms. We employ intrusive SE metrics, including PESQ [31], and ESTOI [32], non-intrusive SE metrics, including DNSMOS [33], NISQA [34], and UTMOS [35], downstream-task-independent met-rics, including Levenshtein phone similarity (LPS = 1 - Levenshtein Table 1 . Ablation study on popular NACs (on clean speech) and SE inference strategies of DAR , DDiff , and CFM (on noisy speech) on 

Dval . For DAR , greedy search (GS) and beam search (BS) with dif-ferent beam sizes ( B) are explored. For DDiff and CFM , different numbers of iterations ( N ′) are explored. Selected setups in italic. Method Strategy DNS MOS NISQA UTMOS PESQ ESTOI Clean - 3.05 3.64 2.81 - --DAC [14] - 3.08 3.68 2.79 3.83 0.93                                                                                                     

> -BiCodec [15] -3.21 3.74 3.11 2.18 0.77 -X-Codec -1 [16] -3.27 3.77 3.17 1.64 0.65 -X-Codec -8 [16] -3.19 4.04 3.11 2.61 0.78 Noisy -1.78 1.47 1.51 1.26 0.57
> DAR
> GS 2.74 2.60 2.03 1.50 0.56
> BS, B= 3 2.74 2.60 2.03 1.50 0.56 BS, B= 5 2.74 2.61 2.04 1.50 0.56
> DDiff
> N′= 1 2.69 2.87 2.04 1.54 0.63
> N′= 3 2.79 2.97 2.14 1.59 0.64
> N′= 10 2.79 2.98 2.15 1.59 0.63
> N′= 20 2.77 2.94 2.16 1.57 0.63
> CFM
> N′= 1 2.96 3.22 2.34 1.89 0.69
> N′= 3 2.99 3.63 2.35 1.89 0.69
> N′= 10 2.98 3.64 2.27 1.84 0.69
> N′= 20 2.96 3.61 2.25 1.82 0.68

phone distance (LPD)) [36], as a language-independent phone fi-delity metric being sensitive to hallucinations. 

3.3. Model Training 

The proposed LM-based SE models are trained on 4 Nvidia H100 , using a batch size of 8 with a 5-second waveform in each mini-batch, for 150 K steps, taking approximately 2 days. An AdamW optimizer with β = (0 .9, 0.95) and weight decay 0.05 is adopted. The learning rate is 0.0001 with 5 K steps of warmup. For fine-tuning (-FT) with auxiliary losses, the models are trained for 50 K further steps, with α=10 .0, γ=20 .0, δ=0 .5, and η=1 .0. The learning rate is 0.00001 with 5 K steps of warmup. For a fair com-parison on Dtest , the non-fine-tuned models are also further trained for 50 K steps. 

4. RESULTS AND DISCUSSION 4.1. Ablation on the Validation Set 

In Table 1, we perform an ablation study on Dval . We first apply the recent popular NACs to the clean speech, including DAC [14] with 12 codebooks, the single-codebook BiCodec [15], X-Codec with 1 and 8 codebooks ( X-Codec -1 and X-Codec -8), respectively. Concerning the non-intrusive metrics (DNSMOS, NISQA, and UTMOS), we observe that all NACs’ performance is in the range or even better than the uncoded (clean) waveform. Concerning the intrusive metrics PESQ and ESTOI, we observe X-Codec -1 to be the poorest, followed by BiCodec and then the X-Codec -8. With a significant margin, DAC is the clear winner of this ablation. So we choose it for all further experiments in Tables 1 and 2. In the lower three segments of Table 1, we investigate differ-ent hyperparameter setups during the inference of DAR , DDiff , and 

CFM . While DAR is anyway quite robust to hyperparameter choices, we observe for DDiff and CFM that N ′ = 3 iterations already de-liver the best results. Chosen settings are in italic. 

Table 2 . Performance comparison of LM-based SE methods using 

DAC on Dtest , and corresponding fine-tuning (-FT) including an aux-iliary loss (6). The best performance in each of the last two table segments for each metric is in bold; the second-best is underlined. Method DNS MOS NISQA UTMOS PESQ POLQA ESTOI LPS Noisy 1.84 1.65 1.56 1.31 1.85 0.61 0.62 Clean 2.93 3.33 2.50 - - - --DAC [14] 2.99 3.39 2.45 3.85 4.24 0.93 0.93                                                                                     

> DAR 2.71 2.63 1.96 1.50 1.99 0.58 0.66
> CAR 2.95 3.20 2.27 1.83 2.61 0.66 0.73
> DNAR 2.67 2.82 1.97 1.55 2.11 0.66 0.69
> CNAR 3.01 3.39 2.40 1.99 2.75 0.75 0.76
> DDiff 2.75 2.93 2.05 1.58 2.17 0.66 0.68
> CFM 2.95 3.46 2.15 1.81 2.50 0.71 0.70
> DAR -FT 2.73 2.73 1.95 1.61 2.09 0.56 0.63
> CAR -FT 3.02 3.33 2.28 1.99 2.66 0.64 0.71
> DNAR -FT 2.72 2.88 1.98 1.84 2.32 0.66 0.67
> CNAR -FT 3.03 3.41 2.38 2.41 3.00 0.76 0.76
> DDiff -FT 2.80 3.01 2.08 1.81 2.36 0.67 0.68
> CFM -FT 2.96 3.53 2.25 2.15 2.76 0.73 0.72

4.2. Performance Analysis on the Test Set 

Table 2 shows the performance comparison among the proposed six paradigms (second table segment) and the corresponding fine-tuning (-FT) results (third table segment). 

Across all metrics, we observe that the continuous-domain paradigms (methods named “ C...”) are consistently outperforming their discrete-domain counterparts (methods named “ D...”) . This is due to predicting D = 1024 continuous features can offer greater fault tolerance than predicting C = 12 discrete tokens per frame, and quantization improves the likelihood of correct token recovery. Among the discrete approaches, DAR is the worst and DDiff is clearly the best. All of them are excelled by CFM , then CAR , and the final winning approach CNAR is top in all but one metric and second in the other. It is impressive to see that CNAR is leading with a significant margin vs. all the other methods in quite some metrics ,benefiting from its totally matched training/inference pattern. In the bottom segment of Table 2, we observe the very same rank orders, with continuous-domain paradigms in the lead, with method 

CNAR -FT being clearly top-ranked. Interestingly, the fine-tuning is able to consistently improve non-intrusive metrics (DNSMOS and NISQA) and intrusive metrics (PESQ and POLQA). 

5. CONCLUSIONS 

In this paper, we present a unified framework for six popular decoder-only LM-based generative SE modeling paradigms based on discrete/continuous latent NAC features. We are the first to de-liver a comprehensive and fair synopsis of the paradigms by employ-ing both non-intrusive and intrusive metrics. Continuous-domain paradigms excel discrete ones, with a continuous non-autoregressive approach being clearly on top. Our proposed fine-tuning strat-egy consistently improves DNSMOS, NISQA, PESQ, and POLQA across all six paradigms. 

6. ACKNOWLEDGMENT 

Computational resources were provided by the German AI Service Center WestAI. 7. REFERENCES 

[1] Hugo Touvron et al., “Llama: Open and Efficient Foundation Language Models,” arXiv:2302.13971 , pp. 1–27, Feb. 2023. [2] Jixun Yao et al., “GenSE: Generative Speech Enhancement via Language Models using Hierarchical Modeling,” in Proc. of ICLR , Singapore, Apr. 2025, pp. 1–21. [3] Boyi Kang et al., “LLaSE-G1: Incentivizing Generalization Capability for Llama-based Speech Enhancement,” in Proc. of ACL , Vienna, Austria, Jul. 2025, pp. 13292–13305. [4] Haoyin Yan et al., “UniSE: A Unified Framework for Decoder-only Autoregressive LM-based Speech Enhance-ment,” arXiv:2510.20441 , pp. 1–5, Oct. 2025. [5] Sofiene Kammoun et al., “Modeling Strategies for Speech En-hancement in the Latent Space of a Neural Audio Codec,” pp. 17407–17411, May 2026. [6] Luca A. Lanzend¨ orfer et al., “High-Fidelity Speech Enhance-ment via Discrete Audio Tokens,” pp. 17397–17401, May 2026. [7] Haoyang Li et al., “Speech Enhancement Using Continuous Embeddings of Neural Audio Codec,” in Proc. of ICASSP ,Hyderabad, India, Apr. 2025, pp. 1–5. [8] Junan Zhang et al., “AnyEnhance: A Unified Generative Model with Prompt-Guidance and Self-Critic for Voice En-hancement,” arXiv:2501.15417 , pp. 1–5, Jan. 2025. [9] Haici Yang et al., “Genhancer: High-Fidelity Speech Enhance-ment via Generative Modeling on Discrete Codec Tokens,” in 

Proc. of Interspeech , Kos, Greece, Sep. 2024, pp. 1170–1174. [10] Yihui Fu et al., “DisContSE: Single-Step Diffusion Speech Enhancement Based on Joint Discrete and Continuous Embed-dings,” in Proc. of ICASSP , Barcelona, Spain, May 2026, pp. 17392–17396. [11] Yaron Lipman et al., “Flow Matching for Generative Model-ing,” in Proc. of ICLR , Kigali, Rwanda, May 2023, pp. 1–28. [12] Simon Welker et al., “FlowDec: A Flow-Based Full-Band General Audio Codec with High Perceptual Quality,” in Proc. of ICLR , Singapore, Apr. 2025, pp. 1–28. [13] Alexandre D´ efossez et al., “High Fidelity Neural Audio Com-pression,” Transactions on Machine Learning Research , vol. 2023, pp. 1–20, Sep. 2023. [14] Rithesh Kumar et al., “High-Fidelity Audio Compression with Improved RVQGAN,” in Proc. of NeurIPS , New Orleans, LA, Dec. 2023, pp. 27980–27993. [15] Xinsheng Wang et al., “Spark-TTS: An Efficient LLM-Based Text-to-Speech Model with Single-Stream Decoupled Speech Tokens,” arXiv:2503.01710 , pp. 1–22, Mar. 2025. [16] Zhen Ye et al., “Codec Does Matter: Exploring the Seman-tic Shortcoming of Codec for Audio Language Model,” in 

Proc. of Annual AAAI Conference on Artificial Intelligence ,Kos, Greece, Feb. 2025, pp. 25697–25705. [17] Iris A. M. Huijben et al., “Residual Quantization with Implicit Neural Codebooks,” in Proc. of ICML , Vienna, Austria, Jul. 2024, pp. 20682–20699. [18] Sanyuan Chen et al., “WavLM: Large-Scale Self-Supervised Pre-Training for Full Stack Speech Processing,” IEEE Jour-nal of Selected Topics in Signal Processing , vol. 16, no. 6, pp. 1505–1518, Oct. 2022. [19] Huiwen Chang et al., “MaskGIT: Masked Generative Image Transformer,” in Proc. of CVPR , New Orleans, LA, Jun. 2022, pp. 11315–11325. [20] Sebastian Braun et al., “A Consolidated View of Loss Func-tions for Supervised Deep Learning-Based Speech Enhance-ment,” in Proc. of TSP , Brno, Czech Republic, Jul 2021, pp. 72–76. [21] Marvin Sach et al., “EffCRN: An Efficient Convolutional Re-current Network for High-Performance Speech Enhancement,” in Proc. of ICASSP , Dublin, Ireland, Aug. 2023, pp. 2508– 2512. [22] Lorenz Schmidt et al., “Loss Function Inspired by the PESQ Score,” 2022–, https://github.com/audiolabs/ torch-pesq .[23] Manuel Pariente et al., “PyTorch Implementation of STOI ,” 2020–, https://github.com/mpariente/ pytorch_stoi .[24] Penghang Yin et al., “Understanding Straight-Through Esti-mator in Training Activation Quantized Neural Nets,” in Proc. of ICLR , New Orleans, LA, May 2019, pp. 1–30. [25] Rithesh Kumar et al., “Descript Audio Codec (.dac): High-Fidelity Audio Compression with Improved RVQ-GAN,” 2023–, https://github.com/descriptinc/ descript-audio-codec .[26] Sanyuan Chen et al., “WavLM-Large,” 2023–, https:// huggingface.co/microsoft/wavlm-large .[27] Ashish Vaswani et al., “Attention Is All You Need,” in Proc. of NeurIPS , Long Beach, CA, Jan. 2017, pp. 1–11. [28] Kohei Saijo et al., “Interspeech 2025 URGENT Speech En-hancement Challenge,” in Proc. of Interspeech , Rotterdam, The Netherlands, Aug. 2025, pp. 858–862. [29] Rosana Ardila et al., “Common Voice: A Massively-Multilingual Speech Corpus,” in Proc. of LREC , Marseille, France, May. 2020, pp. 4218–4222. [30] ITU, Rec. P.56: Objective Measurement of Active Speech Level , International Telecommunication Union, Telecommu-nication Standardization Sector (ITU-T), Dec. 2011. [31] Antony W. Rix et al., “Perceptual Evaluation of Speech Quality (PESQ)—A New Method for Speech Quality Assessment of Telephone Networks and Codecs,” in Proc. of ICASSP , Salt Lake City, UT, USA, May 2001, pp. 749–752. [32] Jesper Jensen et al., “An Algorithm for Predicting the Intel-ligibility of Speech Masked by Modulated Noise Maskers,” 

IEEE/ACM T-ASLP , vol. 24, no. 11, pp. 2009–2022, Nov. 2016. [33] Chandan K. A. Reddy et al., “DNSMOS P. 835: A Non-Intrusive Perceptual Objective Speech Quality Metric to Eval-uate Noise Suppressors,” in Proc. of ICASSP , Singapore, May 2022, pp. 886–890. [34] Gabriel Mittag et al., “NISQA: A Deep CNN-Self-Attention Model for Multidimensional Speech Quality Prediction with Crowdsourced Datasets,” in Proc. of Interspeech , Brno, Czech Republic, Aug. 2021, pp. 2127–2131. [35] Takaaki Saeki et al., “UTMOS: UTokyo-SaruLab System for VoiceMOS Challenge 2022,” in Proc. of Interspeech , Incheon, Korea, Sep. 2022, pp. 4521–4525. [36] Jan Pirklbauer et al., “Evaluation Metrics for Generative Speech Enhancement Methods: Issues and Perspectives,” in 

Proc. of ITG Speech Communication , Aachen, Germany, Sep. 2023, p. 265–269.