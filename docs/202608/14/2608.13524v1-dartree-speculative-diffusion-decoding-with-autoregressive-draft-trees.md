---
title: "DARTree: Speculative Diffusion Decoding with Autoregressive Draft Trees"
title_zh: DARTree：基于自回归草稿树的推测性扩散解码
authors: "Tianyi Li, Yaxin Luo, Xinyi Shang, Zhiqiang Shen"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13524v1"
tags: ["query:ardm"]
score: 9.0
evidence: 结合自回归草稿树的投机扩散解码
tldr: DARTree 是一种无需训练的投机解码方法，旨在加速自回归语言模型。它结合了扩散模型并行生成 Token 块的能力与自回归修正头的因果建模优势。通过将自回归修正从单链扩展到树状结构，并采用先扩展后剪枝的策略，DARTree 在多个基准测试中显著提高了平均接受长度和推理速度，最高实现 9.73 倍的无损加速。
selection_source: fresh_fetch
motivation: 现有的扩散投机解码方法在生成 Token 树时缺乏有效的因果修正，导致候选路径质量受限。
method: 提出 DARTree，通过将预训练的自回归修正头应用于树状结构，并结合批量节点评分与最佳优先剪枝策略来构建验证树。
result: "在数学、代码和聊天等七个基准测试中，DARTree 的接受长度比同类方法提升高达 98.6%，并实现了最高 9.73 倍的无损加速。"
conclusion: DARTree 证明了将自回归修正与树状搜索结合能有效提升扩散投机解码的效率，是加速大模型推理的有力工具。
---

## 摘要
推测性解码通过并行验证多个草稿标记（tokens），无损地加速了自回归语言模型。基于扩散的草稿生成器通过并行预测整个标记块进一步降低了提议延迟，但其基于位置的分布是边缘分布，而非基于每条草稿路径中所选标记的条件分布。现有的循环修正方法沿单一草稿链整合因果信息，而基于扩散的树构建方法虽然扩大了候选覆盖范围，但未能将这种修正带入各个分支。我们提出了 DARTree，这是一种无需训练的推测性解码方法，它将预训练的自回归（AR）修正头从链式结构扩展到了树状结构。DARTree 首先通过在单次批处理中扩展并评分每个深度的所有节点来构建固定宽度的候选树，然后仅应用最佳优先剪枝来选择验证树，从而将 AR 头的推理与顺序堆操作解耦。在涵盖数学、代码和聊天的七个基准测试中，DARTree 在所有四种模型-温度配置下均实现了最高的平均接受长度和加速比，每轮验证最多可接受 12.97 个标记，在相同设置下比 DFlash 高出 98.6%，比 Domino 高出 27.9%，并实现了相对于本地测量的自回归解码高达 9.73 倍的无损加速。

## Abstract
Speculative decoding losslessly accelerates autoregressive language models by verifying multiple draft tokens in parallel. Diffusion-based drafters further reduce proposal latency by predicting an entire token block in parallel, but their position-wise distributions are marginal rather than conditioned on tokens selected along each draft path. Existing recurrent correction incorporates causal information along a single draft chain, whereas diffusion-based tree construction broadens candidate coverage without carrying this correction along individual branches. We introduce DARTree, a training-free speculative decoding method that extends a pretrained AR correction head from chains to trees. DARTree first constructs a fixed-width candidate tree by expanding and scoring all nodes at each depth in a single batch, and then only applies best-first pruning to select the verification tree, decoupling AR-head inference from sequential heap operations. Across seven math, code, and chat benchmarks, DARTree achieves the highest average acceptance length and speedup in all four model--temperature configurations, accepting up to 12.97 tokens per verification round, 98.6\% more than DFlash and 27.9\% more than Domino in the same setting, and reaching up to 9.73$\times$ lossless speedup over locally measured autoregressive decoding.

---

## 论文详细总结（自动生成）

### DARTree: 基于自回归草稿树的推测性扩散解码总结

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）推理速度受限于自回归（AR）顺序生成的 I/O 瓶颈问题，探讨了**推测性解码（Speculative Decoding）**的优化。
*   **背景**：基于扩散的草稿生成器（如 DFlash）能并行预测 Token 块，降低延迟，但缺乏 Token 间的因果依赖（Causal Dependency）。
*   **核心冲突**：现有的因果修正头（AR Head）虽能修复依赖，但将其应用于树状搜索时，会导致“节点级顺序推理”的瓶颈，使得树构建过程极其缓慢。
*   **目标**：DARTree 旨在将因果修正从单链扩展到树结构，同时通过并行化技术消除搜索过程中的顺序延迟。

#### 2. 论文提出的方法论
DARTree 是一种**无需训练**的方法，其核心思想是将自回归修正头的推理与树搜索逻辑解耦：
*   **深度优先批量扩展（Depth-Wise Batched Expansion）**：不再逐个节点进行堆操作，而是在每个深度上并行扩展所有活跃分支。利用预训练的修正头对当前深度的所有候选 Token 进行批量评分。
*   **超树构建（Supertree Construction）**：在每一层保留固定宽度（W）的候选节点，构建一个比最终验证树更宽的“超树”。
*   **延迟最佳优先剪枝（Deferred Best-First Pruning）**：在超树构建完成后，根据路径对数概率和深度惩罚项（$\beta$）计算全局得分。论文证明了在 $\beta \le 0$ 时，全局 Top-B 选择与顺序堆搜索在数学上是等价的。
*   **算法流程**：1. 执行一次扩散草稿生成；2. 逐深度批量应用 AR 修正头并评分；3. 全局 Top-B 剪枝生成验证树；4. 目标模型并行验证。

#### 3. 实验设计
*   **模型**：Qwen3-4B 和 Qwen3-8B。
*   **数据集/场景**：涵盖三大领域共 7 个基准测试：
    *   **数学**：GSM8K, MATH-500, AIME25。
    *   **代码**：HumanEval, MBPP。
    *   **对话**：MT-Bench, Alpaca。
*   **对比方法（Baselines）**：
    *   标准自回归解码（AR）。
    *   DFlash（并行块扩散草稿）。
    *   DDTree（基于扩散边缘分布的树构建）。
    *   Domino（带因果修正的单链草稿）。
*   **配置**：测试了贪婪解码（T=0）和随机采样（T=1）两种模式。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 6000 Ada Generation GPU**。
*   **系统**：Ubuntu 22.04，双路 AMD EPYC 9374F CPU。
*   **训练时长**：由于 DARTree 是**无需训练（Training-free）**的方法，它直接使用预训练的草稿模型和修正头，因此不涉及训练算力消耗。

#### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模、2 种温度设置、7 个数据集上进行了全面测试。
*   **消融实验**：
    *   对比了不同的树构建策略（顺序堆 vs. 批量扩展）。
    *   验证了在不同修正头（如 DSpark 的 Markov 头）上的迁移能力。
    *   分析了超参数（层宽度 W、验证预算 B、深度奖金 $\beta$、候选集大小 K）的影响。
    *   评估了不同并发数（Batch Size）下的吞吐量表现。
*   **评价**：实验设计客观公平，通过多维度对比证明了方法在提升接受长度（Acceptance Length）和降低延迟方面的有效性。

#### 6. 主要结论与发现
*   **性能提升**：DARTree 在所有配置下均优于现有方法。在 GSM8K 上实现了最高 **9.73 倍**的无损加速。
*   **接受长度**：每轮验证接受的 Token 数显著增加，最高达 12.97 个，比 Domino 提升了 27.9%，比 DFlash 提升了 98.6%。
*   **效率**：通过批量化处理，DARTree 成功将 AR 修正头的推理开销压缩到极低水平，解决了树构建的延迟问题。

#### 7. 优点
*   **无需训练**：能够直接适配现有的带修正头的扩散草稿模型，部署成本低。
*   **高并行度**：深度优先的批量处理充分利用了 GPU 的并行计算能力。
*   **理论支撑**：通过引理证明了延迟剪枝与传统最佳优先搜索的等价性，确保了候选树的质量。
*   **鲁棒性**：在数学、代码和对话等多种任务上表现稳定。

#### 8. 不足与局限
*   **计算开销**：虽然降低了延迟，但树验证过程相比单链验证消耗了更多的 FLOPs，在计算资源极度受限或高并发（Compute-bound）场景下，加速效果会减弱。
*   **依赖性**：性能高度依赖于预训练扩散生成器和修正头的质量。
*   **块长度限制**：目前的加速受限于预设的草稿块长度（如 16），若能支持更长的草稿块，潜力可能更大。

（完）
