---
title: "LayerRecall: A State-Conditioned Memory Router for Long-Horizon Consistency in Video Generation"
title_zh: LayerRecall：一种面向视频生成长程一致性的状态条件记忆路由
authors: "Yixuan Ding, Jiahao Kong, Wei Huang, Ruijie Quan, Yi Yang"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.28460v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于长视频生成的自回归视频扩散模型
tldr: 针对自回归视频生成中长程一致性缺失的问题，本文提出 LayerRecall。该方法通过分析视频 DiT 层的上下文偏好，设计了一个层选择性存储路由，将历史 K/V 状态注入特定的存储敏感层。结合跨时域预测匹配（CHPM）训练策略，LayerRecall 在保持局部连续性的同时，显著提升了长视频中主体和场景的一致性，且推理开销极低。
selection_source: fresh_fetch
motivation: 自回归视频生成因受限于有限的近期上下文，在处理长视频时容易丢失远期历史信息，导致主体或场景属性不一致。
method: 提出一种基于当前状态调节的层选择性存储路由，仅在特定敏感层注入历史 K/V 状态，并利用 CHPM 策略进行无监督训练。
result: 在 MemoBench 和 MovieBench 上取得最佳结果，实现了记忆引导的自我修正，且具有跨模型的可移植性和极低的推理开销。
conclusion: LayerRecall 有效解决了长视频生成中的长程一致性挑战，证明了层级选择性记忆注入在视频扩散模型中的重要性。
---

## 摘要
自回归视频扩散通过从有限的近期上下文中生成分块，实现了可扩展的长视频生成。虽然基于近期性的缓存保留了局部连续性，但它会剔除当主体、物体、场景或属性再次出现时所需的历史线索。现有的记忆机制使模型能够接触到非局部历史，但仅凭访问并不能确保有效利用。我们的分析表明，视频 DiT 层对当前、近期和远期上下文表现出明显的偏好，这暗示长程记忆需要同时决定检索什么以及在哪里使用它。我们提出了 LayerRecall，这是一种当前状态条件、层选择性的记忆路由，它检索相关的历史 K/V 状态，并仅将其注入到特定于骨干网络的记忆敏感层中，同时在其他地方保留局部注意力。为了减少对稀缺的高质量长程视频和显式记忆分配标签的依赖，我们进一步提出了跨时界预测匹配（CHPM），该方法利用特权长上下文参考在预测空间中监督受限记忆路由。在 100 个多镜头评估提示词中，LayerRecall 在 MemoBench 和 MovieBench 上取得了最佳的综合结果，同时在 VBench-Long 上与其骨干网络表现持平，证明了在不牺牲局部连续性的情况下具有更强的长程恢复能力。定性分析进一步揭示了记忆引导的自我修正，即最初不匹配的局部属性会恢复到其历史外观，而不会重置正在进行的动作或场景结构。额外的分析显示了跨骨干网络的可移植性和微乎其微的推理开销。

## Abstract
Autoregressive video diffusion enables scalable long-video generation by producing chunks from a bounded recent context. While recency-based caching preserves local continuity, it evicts historical cues needed when subjects, objects, scenes, or attributes reappear. Existing memory mechanisms expose models to nonlocal history, but access alone does not ensure effective use. Our analysis reveals that video DiT layers exhibit distinct preferences for current, recent, and distant context, suggesting that long-range memory requires deciding both what to retrieve and where to use it. We introduce LayerRecall, a current-conditioned, layer-selective memory router that retrieves relevant historical K/V states and injects them only into backbone-specific memory-sensitive layers while preserving local attention elsewhere. To reduce reliance on scarce high-quality long-horizon videos and explicit memory-allocation labels, we further propose Cross-Horizon Prediction Matching (CHPM), which uses a privileged long-context reference to supervise the bounded-memory router in prediction space. Across 100 multi-shot evaluation prompts, LayerRecall achieves the best overall results on MemoBench and MovieBench while matching its backbone on VBench-Long, demonstrating stronger long-range recovery without sacrificing local continuity. Qualitative analyses further reveal memory-guided self-correction, whereby initially mismatched local attributes return to their historical appearance without resetting ongoing motion or scene structure. Additional analyses show cross-backbone portability and negligible inference overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《LayerRecall: A State-Conditioned Memory Router for Long-Horizon Consistency in Video Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归视频生成模型（如基于 DiT 的模型）在生成长视频时，通常采用滑动窗口机制（仅关注近期上下文）。这导致模型会“遗忘”远期的历史信息，当特定的主体、物体或场景在较长时间后重新出现时，会出现属性漂移、身份不一致或场景结构改变的问题。
*   **研究背景**：现有的长视频生成方法要么受限于计算开销（全注意力机制），要么虽然引入了记忆模块，但未能有效解决“在何处”以及“如何”利用这些记忆的问题。作者发现，视频生成网络的不同层对不同时间跨度的上下文有明显的偏好差异。

### 2. 核心方法论
*   **核心思想**：提出 **LayerRecall**，这是一种状态条件的层选择性记忆路由。它不盲目地向所有层注入历史信息，而是识别并仅在“记忆敏感层”注入相关的历史 K/V（键/值）状态。
*   **关键技术细节**：
    *   **层偏好分析**：通过实验发现，DiT 的中间层通常对远期上下文更敏感，而底层和顶层更关注当前帧或近期连续性。
    *   **状态条件路由（State-Conditioned Router）**：利用当前层的隐藏状态作为查询（Query），从历史 K/V 缓存中检索相关信息。通过一个门控机制决定记忆注入的强度。
    *   **跨时域预测匹配（CHPM）训练策略**：为了解决高质量长视频数据稀缺的问题，提出 CHPM。该方法使用一个拥有“特权”长上下文参考的模型来监督受限的 LayerRecall 模型，在预测空间（Noise Prediction）进行匹配，实现无监督/自监督学习。
*   **算法流程**：在推理时，模型维持一个长程 K/V 缓存；在特定层，路由根据当前状态检索历史缓存并与局部注意力结果融合，从而实现对历史属性的“召回”和自我修正。

### 3. 实验设计
*   **数据集/场景**：
    *   **MemoBench**：专门用于评估多镜头一致性和长程记忆恢复的基准。
    *   **MovieBench**：包含复杂场景切换和角色回归的电影级视频评估。
    *   **VBench-Long**：用于评估长视频生成的通用质量（如动作流畅度、图像质量）。
*   **对比方法**：对比了 **CogVideoX-5B**（基座模型）、**StreamingT2V**、**FreeNoise** 以及其他主流的自回归增强方案。
*   **评估指标**：主体一致性（Subject Consistency）、背景一致性、时间流畅度以及 VBench 的综合评分。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心部分未详细列出具体的 GPU 型号和总训练时长（通常此类 5B 规模的模型微调需要 8-32 张 A100/H100 级别的 GPU）。
*   **推理开销**：作者强调 LayerRecall 的推理开销“微乎其微”（negligible），因为它只在少数选定层增加少量的检索计算，且不需要重新编码历史帧。

### 5. 实验数量与充分性
*   **实验规模**：在 100 个复杂的多镜头提示词上进行了详尽评估。
*   **消融实验**：作者进行了充分的消融实验，验证了“层选择性”的必要性、CHPM 训练策略的有效性以及不同记忆长度对结果的影响。
*   **客观性**：通过定量指标（VBench 分数）和定性可视化（展示模型如何修正已经漂移的属性）共同验证，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **记忆引导的自我修正**：LayerRecall 能够实现“自我修正”，即如果模型在生成过程中开始偏离主体特征，记忆路由能将其拉回到历史设定的外观，而不会破坏当前的动作流。
*   **层级特异性**：并非所有 Transformer 层都需要长程记忆，针对性的注入比全局注入更高效且能减少伪影。
*   **性能领先**：在保持局部运动流畅性的同时，长程一致性显著优于现有的自回归基准模型。

### 7. 优点
*   **高效性**：通过层选择性注入，避免了全注意力机制带来的二次方计算复杂度增长。
*   **即插即用与可移植性**：该方法展示了在不同骨干网络（Backbone）上的可移植性。
*   **训练策略巧妙**：CHPM 解决了长视频标注数据不足的痛点，利用模型自身的能力进行知识迁移。

### 8. 不足与局限
*   **数据依赖**：虽然 CHPM 减少了依赖，但效果仍受限于基座模型处理长上下文的潜在上限。
*   **复杂场景限制**：在极端的场景剧变或完全不相关的镜头切换中，记忆路由可能会引入不必要的历史干扰（尽管门控机制旨在缓解此问题）。
*   **硬件门槛**：尽管推理开销低，但维护大规模的 K/V 缓存仍需要较大的显存空间。

（完）
