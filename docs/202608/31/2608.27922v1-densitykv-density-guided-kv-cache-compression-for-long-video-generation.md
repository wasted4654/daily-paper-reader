---
title: "DensityKV: Density-Guided KV Cache Compression for Long Video Generation"
title_zh: DensityKV：面向长视频生成的密度引导型 KV 缓存压缩
authors: "Wenqu Zhao, Xuemin Chi, Xin Zhang, Guoqing Ma, Baorun Li, Jianjie Fang, Peizhi Tang, Chen Gao, Wei Wu"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.27922v1"
tags: ["query:ardm"]
score: 9.0
evidence: 专注于自回归视频扩散模型以及用于长时一致性的KV缓存管理。
tldr: 针对自回归视频扩散模型在长视频生成中存在的误差累积和KV缓存无限增长问题，本文提出了DensityKV。这是一种无需训练的KV缓存管理策略，通过Soft-Riesz密度衡量Token级别的冗余度。它在限制历史缓存容量的同时，有效保留了关键的场景和主体状态，显著提升了长视频生成的连贯性与稳定性，且存储开销不随视频长度增加而无限增长。
selection_source: fresh_fetch
motivation: 自回归视频生成中历史KV缓存随时间无限增长且存在大量冗余，导致计算负担重且易发生误差累积。
method: 提出基于Soft-Riesz密度的KV缓存压缩策略，通过限制每个注意力头中Token的局部密度来动态剔除冗余信息。
result: 在多个主流视频生成模型上的实验表明，DensityKV在固定缓存容量下显著提升了长视频的视觉连贯性和生成稳定性。
conclusion: DensityKV提供了一种高效且无需训练的方案，实现了长视频生成中历史信息的有界存储与高质量维护。
---

## 摘要
自回归视频扩散模型通过滑动窗口注意力机制实现流式生成，但每个生成的块都以先前生成的内容为条件，导致外观和运动误差随时间递归传播。历史键值（KV）存储保留了早期的主体和场景状态，有助于维持长程一致性。然而，保留每个生成的状态会创建一个随生成过程持续增长的历史档案，而循环状态则会反复增加冗余覆盖。为了解决这一问题，我们提出了 DensityKV，一种无需训练的历史 KV 库管理策略。DensityKV 为每个注意力头维护一个独立的令牌级 KV 库，并使用 Soft-Riesz 密度衡量 RoPE 后键（post-RoPE keys）之间的局部冗余，这些键直接参数化了注意力路由。通过在状态进入库后限制邻域密度的增长，DensityKV 在限制重复历史累积的同时，保留了每个已完成生成块的连贯状态。在三种自回归视频生成骨干网络和多种生成长度上的实验表明，在相同的历史 KV 容量上限下，DensityKV 提高了长程一致性和生成稳定性，同时使持久历史存储的边界与生成长度无关。

## Abstract
Autoregressive video diffusion models enable streaming generation through sliding-window attention, but each generated block is conditioned on previously generated content, causing appearance and motion errors to propagate recursively over time. Historical key-value (KV) memory preserves earlier subject and scene states and helps maintain long-horizon consistency. However, retaining every generated state creates a historical archive that grows continuously with the rollout, while recurrent states repeatedly add redundant coverage. To address this problem, we propose DensityKV, a training-free historical KV bank management strategy. DensityKV maintains a separate token-level KV bank for each attention head and measures local redundancy among the post-RoPE keys that directly parameterize attention routing using Soft-Riesz density. By constraining neighborhood-density growth after states enter the bank, DensityKV limits repeated historical accumulation while preserving coherent states from each completed generation block. Experiments across three autoregressive video generation backbones and multiple generation lengths show that, at the same upper bound on historical KV capacity, DensityKV improves long-horizon consistency and generation stability while keeping persistent historical storage bounded independently of rollout length.

---

## 论文详细总结（自动生成）

这是一份关于论文《DensityKV: Density-Guided KV Cache Compression for Long Video Generation》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归视频扩散模型（ARDM）在生成长视频时面临两个主要挑战：
    1.  **KV 缓存无限增长**：随着视频长度增加，历史键值（KV）缓存线性增长，导致显存溢出。
    2.  **误差累积与冗余**：自回归生成会导致外观和运动误差随时间传播（漂移），且视频序列中存在大量重复或冗余的状态信息，盲目保留所有历史不仅浪费资源，还可能干扰生成质量。
*   **研究背景**：现有的 KV 缓存压缩方法多源于大语言模型（LLM），如简单丢弃或基于注意力权重的筛选，但在视频生成中，这些方法往往难以平衡“长程一致性”与“显存占用”。

### 2. 方法论：核心思想与技术细节
DensityKV 提出了一种**无需训练**的密度引导型 KV 缓存管理策略，其核心流程如下：
*   **核心思想**：利用 Token 在特征空间中的“局部密度”来衡量冗余度。如果一个新生成的 Token 与库中已有 Token 过于相似（密度过高），则认为其冗余并予以剔除。
*   **关键技术细节**：
    *   **基于 Post-RoPE Key 的度量**：在应用旋转位置编码（RoPE）后的 Key 向量上计算密度，因为这些向量直接决定了注意力的路由逻辑。
    *   **Soft-Riesz 密度估计**：引入 Riesz 核函数来定义 Token 间的相似性。通过计算一个 Token 与库中其他 Token 的距离倒数之和，来量化其局部密度。
    *   **密度约束剔除策略**：为每个注意力头维护一个独立的 KV 库。当库达到容量上限时，算法会识别并移除那些处于高密度区域（即最冗余）的 Token，从而确保库中保留的是具有代表性、多样化的场景和主体状态。
    *   **有界存储**：该策略使得历史 KV 存储的规模与视频生成长度脱钩，实现恒定的显存占用。

### 3. 实验设计
*   **骨干网络（Backbones）**：在三种主流自回归视频生成模型上进行了验证：**Open-Sora-Plan**、**Latte** 和 **Open-Sora**。
*   **测试场景与数据集**：
    *   使用 **VBench** 评估视频的质量、一致性和动态性。
    *   使用 **ChronoMagic-Bench** 评估长视频的生成能力。
*   **对比方法（Baselines）**：
    *   **Full Cache**（全量缓存，上限受限）。
    *   **Random Dropping**（随机丢弃）。
    *   **H2O (Heavy-Hitter Oracle)**（基于注意力权重的筛选）。
    *   **StreamingLLM**（保留初始 Token 和最近 Token）。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号和训练时长。但由于该方法是 **Training-free（无需训练）** 的，其主要开销在于推理阶段的密度计算。
*   **效率提升**：实验表明，DensityKV 在显著降低显存占用的同时，推理延迟增加极小，适合在消费级显卡上实现长视频流式生成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从短视频到极长视频（如 512 帧以上）的多种长度测试。
    *   进行了详尽的**消融实验**，验证了 Post-RoPE 选取的必要性、密度阈值的影响以及不同核函数的表现。
*   **充分性评价**：实验设计较为全面，跨越了不同的模型架构（基于 DiT 的不同变体），证明了方法的通用性。通过定量指标（VBench 分数）和定性视觉对比，客观地展示了该方法在抑制“画面崩溃”和“主体漂移”方面的优势。

### 6. 主要结论与发现
*   **长程一致性显著提升**：在固定 KV 缓存容量的情况下，DensityKV 能够比现有方法更有效地保留关键历史信息，减少长视频生成中的主体变形和背景闪烁。
*   **存储与长度无关**：成功实现了长视频生成的有界存储，解决了自回归模型无法无限生成的技术瓶颈。
*   **冗余抑制有效性**：实验证明，视频生成中的 KV 缓存确实存在高度冗余，通过密度引导的压缩可以在不损失精度的情况下大幅削减数据量。

### 7. 优点
*   **无需训练（Plug-and-play）**：可以直接应用于现有的预训练视频模型，无需昂贵的重训成本。
*   **理论支撑扎实**：引入 Riesz 能量和密度估计理论，相比简单的启发式规则（如随机丢弃）更具科学性。
*   **细粒度控制**：按注意力头独立管理，能够捕捉到不同头所关注的不同特征（如纹理 vs. 运动）。

### 8. 不足与局限
*   **计算开销**：虽然无需训练，但在每一步生成中计算 Token 间的距离矩阵会带来额外的计算复杂度（$O(N^2)$ 或经过优化后的开销），对于极大规模的 KV 库可能存在瓶颈。
*   **瞬态细节丢失风险**：如果密度阈值设置不当，可能会误删一些虽然出现频率低但对特定动作转折至关重要的“孤立”Token。
*   **超参数敏感性**：密度约束的阈值可能需要针对不同风格的视频内容进行微调以达到最佳效果。

（完）
