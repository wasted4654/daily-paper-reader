---
title: "REFLEX: Rethinking MoE Inference as Refinement-Aware Compute Allocation in Diffusion Language Models"
title_zh: REFLEX：将扩散语言模型中的 MoE 推理重新构想为细化感知的计算分配
authors: "Xiang Xia, Cheng Yan, Yiming Zhang, Jiazheng Liu, Hongyu Zhang, Wuyang Zhang"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01784v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型与自回归模型中的 MoE 推理对比
tldr: "本研究针对扩散语言模型（DLM）中混合专家（MoE）推理的计算分配效率问题，提出了REFLEX。传统DLM在去噪过程中对所有Token分配统一的专家预算，忽略了不同Token细化需求的差异。REFLEX是一种无需训练的方法，通过引入粗到细的专家预算分配层级和前沿进度评分，根据Token的细化状态动态调整计算资源。在LLaDA-MoE等模型上的实验表明，该方法在减少15%计算量的同时，保持甚至提升了生成质量。"
selection_source: fresh_fetch
motivation: 扩散语言模型在去噪过程中对具有不同细化需求的Token分配了统一的专家预算，导致计算资源与实际需求不匹配。
method: 提出REFLEX方法，利用粗到细的层级结构和前沿进度评分，在不改变原始路由的情况下根据Token细化状态动态重组专家计算。
result: "在多个基准测试中，REFLEX平均减少了15%的专家计算量，且在大多数任务上维持或提升了生成质量。"
conclusion: 将MoE推理视为细化感知的计算分配，能有效解决扩散语言模型中的计算冗余，实现更优的质量与计算权衡。
---

## 摘要
混合专家（MoE）模型通过为每个 token 仅激活一小部分专家来增加参数容量。这种条件计算范式使得自回归语言模型能够在不按比例增加每个 token 计算量的情况下扩展模型容量。然而，在扩散语言模型（DLM）中，尽管每个 token 位置的细化需求（refinement demands）截然不同，但每次去噪前向传播都会共同重新访问所有 token 位置，而默认的固定 token 选择路由会为它们分配统一的专家预算，从而导致专家计算与细化需求之间的不匹配。我们认为，DLM 中的 MoE 推理因此应被视为跨异构 token 细化状态的细化感知计算分配。我们提出了 REFLEX（细化感知灵活专家分配），这是一种无需训练的方法，它在保持默认路由器不变的同时，围绕不断演进的细化过程重新组织专家计算。具体而言，REFLEX 引入了一种用于专家预算分配的从粗到细的层级结构，使计算与块相对细化角色保持一致，同时使用前沿进度分数（Frontier-Progress Score）来解决活动块的优先级。在两个代表性的基于 MoE 的 DLM（LLaDA-MoE 和 LLaDA2.0-mini）的多个广泛使用的基准测试中，REFLEX 平均减少了 15% 的分配专家计算量，同时在大多数基准测试中相对于默认路由保持甚至提高了生成质量。与自回归风格的可变专家路由方法相比，REFLEX 还产生了更一致的质量-计算权衡，进一步证明了根据每次去噪前向传播中暴露的异构细化需求分配专家计算的重要性。

## Abstract
Mixture-of-experts (MoE) models increase parameter capacity by activating only a small subset of experts for each token. This conditional-computation paradigm has enabled autoregressive language models to scale model capacity without a proportional increase in per-token computation. In diffusion language models (DLMs), however, each denoising forward jointly revisits all token positions despite their sharply different refinement demands, while the default fixed token-choice routing assigns them a uniform expert budget, creating a mismatch between expert computation and refinement demand. We argue that MoE inference in DLMs should therefore be viewed as refinement-aware compute allocation across heterogeneous token refinement states. We propose REFLEX (\textbf{RE}finement-aware \textbf{FLEX}ible expert allocation), a training-free method that keeps the default router unchanged while reorganizing expert computation around the evolving refinement process. Specifically, REFLEX introduces a coarse-to-fine hierarchy for expert-budget allocation that aligns computation with block-relative refinement roles while using the Frontier-Progress Score to resolve active-block priorities. Across multiple widely used benchmarks on two representative MoE-based DLMs, LLaDA-MoE and LLaDA2.0-mini, REFLEX reduces allocated expert computation by 15\% on average while preserving or even improving generation quality on most benchmarks relative to default routing. Compared with autoregressive-style variable-expert routing methods, REFLEX also yields a more consistent quality--computation trade-off, further supporting the importance of allocating expert computation according to the heterogeneous refinement demands exposed within each denoising forward.

---

## 论文详细总结（自动生成）

以下是对论文《REFLEX: Rethinking MoE Inference as Refinement-Aware Compute Allocation in Diffusion Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：混合专家（MoE）模型在自回归模型中表现优异，但在扩散语言模型（DLM）中存在计算失配问题。DLM 通过迭代去噪生成文本，每次前向传播都会同时处理所有 Token。
*   **核心问题**：传统的 MoE 路由（如固定 Token-Choice）为所有 Token 分配相同的专家数量（Budget），忽略了不同 Token 处于不同的“细化状态”（如已确定的 Prompt、正在细化的活动块、尚未处理的远端块）。这导致在需求低的地方浪费计算，而在需求高的地方投入不足。
*   **整体含义**：论文提出将 DLM 中的 MoE 推理重新定义为一种“细化感知”的计算分配问题，旨在根据 Token 的实时细化需求动态调整专家数量。

### 2. 方法论
论文提出了 **REFLEX**，一种无需训练（Training-free）的推理优化方法，核心思想是保持原始路由器对专家的排名不变，仅动态调整每个 Token 激活的专家数量 $k$。它包含一个从粗到细的二级分配体系：
*   **细化角色预算（RRB - Refinement-Role Budgeting）**：
    *   **结构先验**：根据 Token 距离当前“活动解码块”的相对距离（$d$）分配基础预算。
    *   **分配逻辑**：活动块及邻近块（$d \le 1$）分配高预算；中间块（$d=2$）保持名义预算；远端块（$d \ge 3$）降低预算。
*   **前沿引导专家重分配（FGER - Frontier-Guided Expert Reallocation）**：
    *   **动态证据**：在活动块内部，利用置信度历史计算**前沿进度分数（FPS）**。
    *   **FPS 计算**：结合“前沿因子”（预测置信度是否接近提交阈值）和“进度因子”（置信度增长是否缓慢）。
    *   **重分配**：在不改变活动块总预算的前提下，将专家资源从已接近完成或进度极快的 Token 转移到处于“细化前沿”且进度缓慢的 Token。

### 3. 实验设计
*   **模型**：LLaDA-MoE (7B) 和 LLaDA2.0-mini (16B)。
*   **数据集/场景**：
    *   数学推理：GSM8K (5-shot), Math500 (4-shot)。
    *   代码生成：HumanEval (0-shot), MBPP (3-shot)。
*   **对比方法（Baselines）**：
    *   **Fixed TC**：固定专家数（Top-4, Top-8）。
    *   **Budgeted TC**：基于全局预算的变量专家分配。
    *   **Threshold-based**：基于路由分数阈值的方法（Top-p, DTop-p, Expert-Threshold）。
    *   **EC-DLM-style**：基于去噪时间步的全局调度方法。

### 4. 资源与算力
*   **硬件**：所有实验均在 **NVIDIA A100 80GB GPU** 上进行。
*   **训练时长**：由于 REFLEX 是**无需训练**的推理侧方法，因此不涉及模型训练算力消耗。实验主要集中在推理性能的评估上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同规模的代表性 MoE-DLM 模型，并在数学和代码两个高难度领域进行了测试。
*   **消融实验**：分别验证了 RRB（结构先验）和 FGER（动态重分配）的贡献，并对 FPS 信号的组成部分进行了对比。
*   **敏感性分析**：对超参数（如前沿宽度 $w_{cf}$、温度 $\tau$ 等）进行了详尽的网格搜索和鲁棒性测试。
*   **充分性评价**：实验设计客观、公平，通过与多种自回归风格的动态路由方法对比，证明了针对 DLM 特性进行设计的必要性。

### 6. 主要结论与发现
*   **计算效率提升**：REFLEX 平均减少了 **15%** 的专家计算量（以专家-Token 对衡量）。
*   **质量保持与优化**：在减少计算量的同时，REFLEX 在大多数基准测试中保持了生成质量，甚至在某些任务上有所提升（如 Math500 和 HumanEval）。
*   **帕累托最优**：在质量-计算权衡曲线（Pareto Frontier）上，REFLEX 显著优于传统的固定路由和通用的动态路由方法。
*   **细化状态的重要性**：实验证明，Token 在扩散过程中的结构角色和进度证据是分配计算资源的关键指标，优于单纯的路由器分数。

### 7. 优点
*   **即插即用**：无需重新训练模型，可直接应用于现有的 MoE-DLM。
*   **理论创新**：首次将 DLM 的 MoE 推理建模为跨异构细化状态的资源分配问题。
*   **层级化设计**：结合了静态的结构先验（RRB）和动态的运行证据（FGER），兼顾了稳定性和灵活性。

### 8. 不足与局限
*   **端到端延迟**：论文主要衡量的是“专家-Token 对”的减少（理论计算量），但实际推理延迟还受限于专家加载开销、内存带宽和内核效率，文中未提供详细的端到端 Wall-clock Time 提升数据。
*   **层间差异忽略**：目前 REFLEX 在所有 MoE 层使用相同的专家数量分配，未考虑不同深度层对细化需求的差异化响应。
*   **应用范围**：主要针对离散扩散语言模型，在连续扩散模型或其他生成范式上的适用性有待验证。

（完）
