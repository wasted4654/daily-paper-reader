---
title: "Escaping Confidence Trap: Evolutionary Decoding for Mathematical Reasoning in Diffusion LLMs"
title_zh: 逃离置信度陷阱：扩散大语言模型中用于数学推理的进化解码
authors: "Zhenhong Sun, Hanqing Zhao, Yatao Bian, Rongcheng Tu, Liuyue Xie, Xu Zhang, Jue Wang, Davide Modolo, Daoyi Dong, Dacheng Tao"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00605v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散大语言模型中数学推理的解码
tldr: 扩散大语言模型（dLLM）在数学推理中常陷入“置信度陷阱”，即局部高置信度与全局正确性失调。本文针对 LLaDA 2.0 分析了采样敏感和采样一致两种失败模式，提出了进化解码（Evolutionary Decoding）框架。该框架无需训练，通过步进式选择保留有效信号并抑制重复，结合块级变异引入结构化替代方案以跳出错误的高置信度陷阱，显著提升了 dLLM 的数学推理能力。
selection_source: fresh_fetch
motivation: 扩散大语言模型在解码过程中存在局部置信度与全局推理正确性不一致的“置信度陷阱”，导致数学推理任务失败。
method: 提出一种无需训练的进化解码框架，通过步进式选择保留关键推理信号并利用块级变异跳出错误的高置信度区域。
result: 在多个基准测试中，进化解码显著提升了 LLaDA 2.0 的数学推理性能，优于传统的基于置信度的解码方法。
conclusion: 进化解码为提升扩散大语言模型的逻辑推理可靠性提供了一种有效的测试时缩放方案。
---

## 摘要
扩散大语言模型（dLLMs）已成为自回归大语言模型的一种极具前景的替代方案，通过块状渐进式去掩码提供高效的生成能力。然而，它们强大的通用性能并不一定能转化为可靠的数学推理能力，因为数学推理的正确性取决于保持连贯的数值-符号推理轨迹。在这项工作中，我们分析了 LLaDA 2.0 的解码轨迹，并发现了一个反复出现的扩散置信度陷阱：在渐进式块解码过程中，局部 token 的置信度可能与全局推理的正确性发生错位。我们的分析揭示了两种典型的失败模式：采样敏感型失败（即存在正确路径但其不稳定）和采样一致型失败（即重复采样收敛于重复的高置信度但错误的后续内容）。受此启发，我们提出了进化解码（Evolutionary Decoding），这是一种无需训练的测试时缩放框架，将扩散解码视为候选推理状态上的进化过程。该框架结合了逐步选择（step-wise selection）和块状变异（block-wise mutation）；前者保留有用的数值-符号信号并抑制重复模式，后者引入结构化的替代方案以逃离错误的置信度高地。在多个基准测试上的实验表明，进化解码在 LLaDA 2.0 上优于基于置信度的解码，从而实现了更可靠的数学推理。

## Abstract
Diffusion large language models (dLLMs) have emerged as a promising alternative to autoregressive LLMs, offering efficient generation through block-wise progressive unmasking. However, their strong general-purpose performance does not necessarily translate into reliable mathematical reasoning, where correctness depends on preserving coherent numerical-symbolic reasoning trajectories. In this work, we analyze the decoding trajectories of LLaDA 2.0 and identify a recurring diffusion confidence trap: local token confidence can become misaligned with global reasoning correctness during progressive block decoding. Our analysis reveals two representative failure regimes: sampling-sensitive failures, where correct paths exist but are unstable, and sampling-consistent failures, where repeated sampling converges to repetitive high-confidence but incorrect continuations. Motivated by this observation, we propose Evolutionary Decoding, a training-free test-time scaling framework that views diffusion decoding as an evolutionary process over candidate reasoning states. The framework combines step-wise selection, which preserves useful numerical-symbolic signals and suppresses repetitive patterns, with block-wise mutation, which introduces structured alternatives to escape incorrect high-confidence basins. Experiments on multiple benchmarks show that Evolutionary Decoding improves LLaDA 2.0 over confidence-based decoding, leading to more reliable mathematical reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Escaping Confidence Trap: Evolutionary Decoding for Mathematical Reasoning in Diffusion LLMs》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：扩散大语言模型（dLLMs，如 LLaDA）通过块状渐进式去掩码生成文本，展现出挑战自回归模型的潜力。但在数学推理等高精度领域，其表现往往不可靠。
*   **核心问题**：作者发现了**“置信度陷阱”（Confidence Trap）**现象，即局部 token 的高置信度与全局推理的正确性之间存在错位。
*   **失败模式分析**：
    *   **采样敏感型失败**：正确路径存在但由于扩散过程中的不稳定性（如数值/符号 token 被延迟释放）而丢失。
    *   **采样一致型失败**：模型反复收敛于高置信度但错误的重复性推理盆地，单纯增加采样次数无法解决。

### 2. 方法论：进化解码（Evolutionary Decoding）
该方法将扩散解码视为候选推理状态的进化过程，无需重新训练模型，仅在推理阶段干预。
*   **核心思想**：通过“选择”保留关键信号，通过“变异”跳出局部最优陷阱。
*   **关键技术细节**：
    *   **步进式选择（Step-wise Selection）**：
        *   **数值-符号增强**：利用信息熵识别具有中度不确定性的数值和符号 token，并人为提升其释放分数，防止其被平庸的自然语言 token 淹没。
        *   **块重复惩罚**：计算当前块与前一块的重复率，若重复度过高则统一降低释放分数，抑制无意义的循环生成。
    *   **块级变异（Block-wise Mutation）**：
        *   **结构化扰动**：当检测到当前块的数值-符号支持度较低时，在 Logits 层面注入偏置，强制生成数值、符号或混合的替代分支。
        *   **生存竞争**：在多个变异分支中，根据模型原始分布的平均置信度选择最优秀的候选块进入下一阶段。

### 3. 实验设计
*   **数据集**：涵盖了不同难度的数学竞赛和基础算术任务，包括 **AIME (2024-2026)**、**AMC 2023**、**MATH500** 和 **GSM8K**。
*   **基准模型**：以 **LLaDA 2.0 (Flash 和 Mini 版本)** 作为代表性的扩散 LLM 基座。
*   **对比方法**：
    *   默认的基于置信度的解码（Baseline）。
    *   仅包含选择机制的变体（Selection only）。
    *   多样本采样策略（Pass@8, Majority@8）。
*   **评估指标**：Pass@1 准确率、Pass@8 准确率、平均解码步数、轨迹诊断指标（重复率、符号密度等）。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 台 NVIDIA A100 GPU** 上运行。
*   **软件框架**：使用 **SGLang** 推理引擎进行 KV-cache 加速。
*   **训练成本**：该方法是 **Training-free（无需训练）** 的，所有改进均在测试时（Test-time）通过修改解码逻辑实现。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流基准测试上进行了全面评估，涵盖了从小学算术到高中竞赛级别的题目。
*   **消融实验**：针对增强系数 $\alpha$、惩罚系数 $\beta$、变异强度 $\delta$ 以及变异触发阈值等关键超参数进行了详细的消融研究。
*   **公平性**：所有对比实验均在相同的 Prompt 格式、最大长度和硬件环境下进行；超参数在 AIME 2025 上调优后，直接迁移至其他数据集，验证了泛化性。

### 6. 主要结论与发现
*   **性能提升**：进化解码在 AIME 和 AMC 等高难度竞赛题目上显著优于基准解码。例如，在 AIME 2025 上，LLaDA 2.0-Flash 的准确率从 56.7% 提升至 70.0%。
*   **跳出陷阱**：实验证明变异机制能有效帮助模型逃离“采样一致型”错误，而选择机制则稳定了“采样敏感型”轨迹。
*   **测试时缩放**：该方法展示了良好的测试时缩放特性，即通过投入更多的推理步数（计算量）换取更高的推理质量。

### 7. 优点与亮点
*   **无需训练**：作为一种插件式的解码策略，它不需要昂贵的微调或强化学习过程。
*   **轨迹级干预**：不同于以往只关注最终输出的重采样方法，该方法深入扩散模型的内部解码轨迹，从生成动力学角度解决问题。
*   **可解释性**：将解码过程类比为生物进化（选择与变异），为理解扩散模型的生成行为提供了直观的视角。

### 8. 不足与局限
*   **推理开销**：由于引入了多分支变异和更谨慎的选择机制，平均解码步数显著增加（通常增加 2-3 倍），导致推理延迟上升。
*   **任务敏感性**：在 GSM8K 等简单任务上提升有限，甚至可能因为过度变异干扰原本正确的短路径。
*   **超参数依赖**：虽然具有一定的泛化性，但针对不同规模的模型或完全不同的领域（如代码生成），可能仍需微调超参数以达到最优效果。

（完）
