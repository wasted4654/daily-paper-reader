---
title: "KnowDiffuser: A Knowledge-Guided Diffusion Planner with LM Reasoning and Prior-Informed Trajectory Initialization"
title_zh: KnowDiffuser：一种结合语言模型推理与先验引导轨迹初始化的知识引导扩散规划器
authors: "Fan Ding, Xuewen Luo, Fengze Yang, Bo Yu, HwaHui Tew, Ganesh Krishnasamy, Junn Yong Loo"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10441v1"
tags: ["query:genad"]
score: 10.0
evidence: 知识引导的自动驾驶扩散规划器
tldr: 针对自动驾驶中大语言模型（LM）缺乏连续轨迹生成能力及扩散模型缺乏语义理解的问题，本文提出 KnowDiffuser 框架。该框架利用 LM 从场景中推理元动作，并将其映射为先验轨迹以引导扩散模型的去噪过程。通过两阶段截断去噪机制，实现了语义对齐与物理可行性的结合。在 nuPlan 基准测试中，该方法在开环和闭环评估中均表现优异，有效弥合了语义推理与物理规划之间的鸿沟。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在自动驾驶中难以生成连续物理轨迹，以及扩散模型缺乏场景语义理解和解释性的局限。
method: 结合 LM 推理出的元动作生成先验轨迹，并利用两阶段截断去噪机制对轨迹进行精细化调整。
result: 在 nuPlan 自动驾驶基准测试中，KnowDiffuser 在开环和闭环评估指标上均显著优于现有的规划器。
conclusion: 该研究成功构建了一个将语义理解与生成式规划紧密结合的鲁棒框架，为自动驾驶系统提供了更具解释性的规划方案。
---

## 摘要
语言模型（LMs）的最新进展展示了强大的语义推理能力，使其能够应用于自动驾驶（AD）的高层决策。然而，语言模型在离散的 token 空间上运行，缺乏生成运动规划所需的连续且物理可行的轨迹的能力。与此同时，扩散模型已被证明在生成可靠且动力学一致的轨迹方面非常有效，但往往缺乏语义可解释性以及与场景级理解的对齐。为了解决这些局限性，我们提出了 KnowDiffuser，这是一个知识引导的运动规划框架，它将语言模型的语义理解与扩散模型的生成能力紧密结合。该框架利用语言模型从结构化场景表示中推断出上下文感知的元动作（meta-actions），并将其映射到先验轨迹，作为后续去噪过程的锚点。一种两阶段截断去噪机制高效地细化了这些轨迹，同时保持了语义对齐和物理可行性。在 nuPlan 基准测试上的实验表明，KnowDiffuser 在开环和闭环评估中均显著优于现有的规划器，建立了一个稳健且可解释的框架，有效地弥合了自动驾驶系统中从语义到物理的鸿沟。

## Abstract
Recent advancements in Language Models (LMs) have demonstrated strong semantic reasoning capabilities, enabling their application in high-level decision-making for autonomous driving (AD). However, LMs operate over discrete token spaces and lack the ability to generate continuous, physically feasible trajectories required for motion planning. Meanwhile, diffusion models have proven effective at generating reliable and dynamically consistent trajectories, but often lack semantic interpretability and alignment with scene-level understanding. To address these limitations, we propose \textbf{KnowDiffuser}, a knowledge-guided motion planning framework that tightly integrates the semantic understanding of language models with the generative power of diffusion models. The framework employs a language model to infer context-aware meta-actions from structured scene representations, which are then mapped to prior trajectories that anchor the subsequent denoising process. A two-stage truncated denoising mechanism refines these trajectories efficiently, preserving both semantic alignment and physical feasibility. Experiments on the nuPlan benchmark demonstrate that KnowDiffuser significantly outperforms existing planners in both open-loop and closed-loop evaluations, establishing a robust and interpretable framework that effectively bridges the semantic-to-physical gap in AD systems.

---

## 论文详细总结（自动生成）

以下是对论文《KnowDiffuser: A Knowledge-Guided Diffusion Planner with LM Reasoning and Prior-Informed Trajectory Initialization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动驾驶（AD）领域目前面临“语义-物理”脱节的问题。
    *   **大语言模型（LM）**：具备强大的语义推理和常识判断能力，但其输出是离散的 Token，难以直接生成精确、符合物理规律的连续轨迹。
    *   **扩散模型（Diffusion Models）**：擅长生成多样且物理可行的轨迹，但缺乏语义解释性，难以直接对齐高层驾驶意图或复杂的交通规则。
*   **研究动机**：提出一种名为 **KnowDiffuser** 的框架，旨在将 LM 的高层语义推理与扩散模型的底层生成能力紧密结合，通过“知识引导”实现既有逻辑性又符合物理约束的运动规划。

### 2. 方法论：核心思想与关键技术
KnowDiffuser 采用多阶段流水线设计，核心思想是**“语义推理 -> 先验匹配 -> 扩散细化”**：
*   **元动作与先验轨迹库构建**：
    *   从 nuPlan 历史数据中提取 8 秒轨迹片段，根据速度、加速度、转向角等特征进行聚类。
    *   定义一组“元动作”（如：左转+加速、直行+减速），并将每类动作的平均轨迹作为“先验轨迹模板”，形成映射库。
*   **高层决策模块（LM Reasoning）**：
    *   将车辆状态、周围障碍物、地图拓扑和交通信号编码为结构化提示词（Prompt）。
    *   利用 LM（如 GPT-4o）推理出当前场景下的最佳“元动作”。
*   **桥接机制（Matching Bridge）**：
    *   根据 LM 输出的元动作，从预构建库中检索对应的先验轨迹。这为后续生成提供了语义对齐的初始“锚点”。
*   **底层扩散轨迹生成器（Low-Level Planner）**：
    *   **核心算法**：采用基于 Transformer 的扩散模型（DiT）。
    *   **两阶段截断去噪（Truncated Denoising）**：不同于从纯高斯噪声开始的传统扩散模型，该方法在先验轨迹上加入少量噪声，仅通过两步去噪迭代即可生成最终轨迹。这极大地降低了推理延迟，同时保留了 LM 的决策意图。

### 3. 实验设计
*   **数据集**：使用大规模真实世界自动驾驶基准测试 **nuPlan**。
*   **评估场景**：
    *   **开环评估（Open-loop）**：在非交互式离线数据上测试轨迹预测精度。
    *   **闭环评估（Closed-loop）**：在 Val-14 场景中进行实时仿真，评估成功率。
*   **对比方法（Baselines）**：
    *   规则类：IDM。
    *   学习类：UrbanDriver, PlanCNN, GameFormer, PlanTF, GC-PGP。
    *   生成类：CKS-1.5b, GUMP-m（基于扩散模型的规划器）。

### 4. 资源与算力
*   **模型使用**：高层推理使用了 **GPT-4o** (2024版本) 作为推理引擎。
*   **算力说明**：论文**未明确说明**训练扩散模型所使用的具体 GPU 型号、数量及训练总时长。文中仅提到数据集包含约 50,000 个样本，且推理阶段通过两步去噪显著降低了延迟以满足实时性需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 nuPlan 测试集上进行了全面的开环对比。
    *   在 Val-14 闭环基准上测试了非反应式（NR）和反应式（R）两种模式。
    *   针对 LM 规模进行了消融实验（对比了 LLaMA-3B、Qwen-32B 和 GPT-4o）。
*   **充分性评价**：实验设计较为充分，涵盖了从预测精度到仿真成功率的核心指标。消融实验证明了高层推理能力（模型规模）对最终规划效果的关键影响。对比实验涵盖了从传统规则到最新生成式模型的多种 SOTA 方法，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：KnowDiffuser 在开环指标（8s ADE 为 0.298）和闭环成功率（Test R 达到 81.10）上均显著优于所有基线模型。
*   **语义-物理桥接有效性**：通过先验轨迹初始化扩散过程，比直接从噪声生成轨迹更准确且更符合驾驶意图。
*   **效率提升**：两阶段截断去噪机制在保证轨迹质量的前提下，解决了扩散模型推理慢的痛点，使其具备实时部署潜力。
*   **模型规模效应**：较小的 LM（如 LLaMA-3B）在复杂场景下容易产生决策崩溃（如反复输出“停止”），而强大的 LM 能显著提升规划的鲁棒性。

### 7. 优点与亮点
*   **架构创新**：成功将 LM 的逻辑推理与扩散模型的生成能力解耦并有机结合，解决了 LM 无法输出连续控制信号的问题。
*   **可解释性强**：通过“元动作”作为中间层，使得自动驾驶系统的决策过程变得透明、可理解。
*   **推理高效**：截断去噪策略是扩散模型应用于实时控制系统的一个非常实用的优化。

### 8. 不足与局限
*   **依赖库质量**：系统的表现高度依赖于预构建的“元动作-轨迹库”，如果库中缺乏某些极端或罕见场景的模板，性能可能受限。
*   **LM 延迟与成本**：虽然扩散过程缩短了，但调用大型 LM（如 GPT-4o）的 API 延迟和计算成本在车载实时系统中仍是巨大挑战。
*   **硬件细节缺失**：缺乏具体的算力消耗和训练成本数据，不便于其他研究者评估其训练门槛。
*   **感知假设**：实验基于结构化场景输入，未讨论从原始传感器数据（如图像/点云）到结构化信息的转换误差对系统的影响。

（完）
