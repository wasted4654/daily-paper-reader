---
title: Efficient and Explainable End-to-End Autonomous Driving via Masked Vision-Language-Action Diffusion
title_zh: 基于掩码视觉-语言-动作扩散模型的高效且可解释端到端自动驾驶
authors: "Jiaru Zhang, Manav Gagvani, Can Cui, Juntong Peng, Ruqi Zhang, Ziran Wang"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20577v1"
tags: ["query:genad"]
score: 10.0
evidence: 用于端到端自动驾驶的掩码视觉-语言-动作扩散模型
tldr: 针对端到端自动驾驶中大模型推理延迟高、动作精度低及解释性不足的问题，本文提出MVLAD-AD框架。该框架通过掩码视觉-语言-动作扩散模型，结合离散动作标记化策略和几何感知嵌入学习，实现了高效且具备物理解释性的轨迹规划。在nuScenes基准测试中，该方法在规划精度和推理效率上均优于现有的自回归和扩散基准模型，同时提供了高保真度的推理说明。
selection_source: fresh_fetch
motivation: 现有的自动驾驶大模型面临推理速度慢、动作空间缺乏几何约束以及语义解释性不足的挑战。
method: 提出一种掩码扩散模型，引入离散动作标记化、几何感知嵌入学习以及动作优先解码策略来优化轨迹生成。
result: 在nuScenes数据集上，该方法在规划精度和推理效率方面均显著超越了最先进的自回归和扩散基准模型。
conclusion: MVLAD-AD成功平衡了自动驾驶系统的执行效率、规划精度与语义解释性，为端到端驾驶提供了高效且可靠的解决方案。
---

## 摘要
大语言模型 (LLMs) 和视觉语言模型 (VLMs) 已成为端到端自动驾驶领域极具前景的候选方案。然而，这些模型通常在推理延迟、动作精度和可解释性方面面临挑战。现有的自回归方法受困于缓慢的逐标记生成，而先前的基于扩散的规划器通常依赖于冗长且通用的语言标记，缺乏明确的几何结构。在这项工作中，我们提出了用于自动驾驶的掩码视觉-语言-动作扩散模型 (MVLAD-AD)，这是一个旨在通过掩码视觉-语言-动作扩散模型弥合高效规划与语义可解释性之间差距的新型框架。与将动作强行纳入语言空间的方法不同，我们引入了一种离散动作标记化策略，从真实驾驶分布中构建了一个包含运动学可行路径点的紧凑码本。此外，我们提出了几何感知嵌入学习，以确保潜空间中的嵌入能够逼近物理几何度量。最后，引入了一种动作优先解码策略，以优先生成轨迹。在 nuScenes 及其衍生基准测试上的大量实验表明，MVLAD-AD 实现了卓越的效率，在规划精度上优于最先进的自回归和扩散基准模型，同时提供了高保真且可解释的推理。

## Abstract
Large Language Models (LLMs) and Vision-Language Models (VLMs) have emerged as promising candidates for end-to-end autonomous driving. However, these models typically face challenges in inference latency, action precision, and explainability. Existing autoregressive approaches struggle with slow token-by-token generation, while prior diffusion-based planners often rely on verbose, general-purpose language tokens that lack explicit geometric structure. In this work, we propose Masked Vision-Language-Action Diffusion for Autonomous Driving (MVLAD-AD), a novel framework designed to bridge the gap between efficient planning and semantic explainability via a masked vision-language-action diffusion model. Unlike methods that force actions into the language space, we introduce a discrete action tokenization strategy that constructs a compact codebook of kinematically feasible waypoints from real-world driving distributions. Moreover, we propose geometry-aware embedding learning to ensure that embeddings in the latent space approximate physical geometric metrics. Finally, an action-priority decoding strategy is introduced to prioritize trajectory generation. Extensive experiments on nuScenes and derived benchmarks demonstrate that MVLAD-AD achieves superior efficiency and outperforms state-of-the-art autoregressive and diffusion baselines in planning precision, while providing high-fidelity and explainable reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：基于掩码视觉-语言-动作扩散模型的高效且可解释端到端自动驾驶 (MVLAD-AD)

#### 1. 核心问题与研究动机
*   **研究背景**：自动驾驶正从模块化流水线向端到端学习转型。虽然大语言模型（LLM）和视觉语言模型（VLM）在场景理解和推理上表现出色，但应用于自动驾驶时面临三大挑战：
    *   **推理延迟高**：传统的自回归生成方式（逐标记生成）速度太慢，难以满足实时驾驶需求。
    *   **动作精度低**：将连续的驾驶轨迹强行转化为冗长的文本描述，导致表示冗余且缺乏几何约束。
    *   **解释性不足**：语义推理往往与实际物理动作脱节，缺乏一致性。
*   **核心问题**：如何构建一个既能实现高效并行规划，又能提供精确动作控制和物理一致性语义解释的统一框架。

#### 2. 方法论
MVLAD-AD 将自动驾驶建模为一个**条件掩码生成问题**，核心技术包括：
*   **离散动作标记化 (Discrete Action Tokenization)**：
    *   不使用文本描述坐标，而是通过 K-Means 算法对真实驾驶数据中的路径点进行聚类，构建一个包含 $N$ 个运动学可行原语的**紧凑码本 (Codebook)**。
    *   将连续轨迹映射为离散的动作标记（Action Tokens），显著缩短序列长度。
*   **几何感知嵌入学习 (Geometry-Aware Embedding Learning)**：
    *   为了让模型理解标记间的物理关系，引入预训练阶段。
    *   通过**重构损失**、**几何一致性损失**（确保潜空间距离与物理距离正相关）和**对比聚类损失**，使嵌入空间具备物理度量属性。
*   **统一掩码 VLA 扩散 Transformer**：
    *   将视觉特征、文本指令、动作标记和推理标记统一拼接为单个序列。
    *   采用**两阶段训练策略**：先进行动作中心预热（Action-Centric Warm-up），再进行联合 VLA 微调，以平衡物理动作学习与语义推理。
*   **动作优先解码策略 (Action-Priority Decoding)**：
    *   推理时，优先对动作标记进行去掩码（Unmasking），在轨迹确定后再生成解释文本。这既降低了规划延迟，又确保了生成的解释是基于已确定的动作。

#### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：用于评估规划精度（L2 误差）。
    *   **Nu-X**：用于评估驾驶决策的解释能力。
    *   **nuScenes-QA**：用于评估视觉问答（VQA）能力。
*   **对比基准 (Benchmarks)**：
    *   **自回归 VLM**：LLaVA-1.6, Llama-3.2, Qwen2-VL, OpenEMMA, DriveLM。
    *   **扩散模型**：ViLaD（该领域的主要竞争对手）。
    *   **传统端到端**：UniAD（单帧版本）。
    *   **闭源模型**：GPT-4o, Gemini-1.5（仅用于推理任务对比）。

#### 4. 资源与算力
*   **训练配置**：使用了 **4 张 NVIDIA H100 GPU**。
*   **训练细节**：采用 bfloat16 精度，全局 Batch Size 为 32。两个训练阶段各进行 8 个 epoch，总训练时长约为 **9 小时**。
*   **推理配置**：推理实验在单张 **NVIDIA A100 GPU** 上完成。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文在规划、解释、问答三个维度进行了广泛测试，并提供了详细的定量指标（L2 误差、BLEU-4、CIDEr、准确率等）。
*   **消融实验**：针对动作码本大小 $N$、几何感知嵌入的必要性、以及动作表示方式（绝对路径点 vs 相对位移）进行了对比实验。
*   **评价**：实验设计客观且充分，对比了当前最先进的开源和商业模型，验证了该方法在精度和效率上的双重优势。

#### 6. 主要结论与发现
*   **规划精度领先**：在 nuScenes 上，MVLAD-AD 的平均 L2 误差为 **1.28m**，显著优于 ViLaD (1.81m) 和 UniAD (1.80m)。
*   **推理效率极高**：推理延迟仅为 **1.72s**，比扩散模型基准 ViLaD 快 1.6 倍，比自回归模型 LLaVA-1.6 快 1.84 倍。
*   **零失败率**：由于采用了码本约束，模型完全避免了自回归模型常见的格式幻觉问题，规划失败率为 **0%**。
*   **解释性强**：在 Nu-X 和 nuScenes-QA 任务中，其语义对齐指标（如 METEOR 和 ROUGE-L）均达到或刷新了 SOTA 记录。

#### 7. 优点
*   **架构创新**：首次将掩码扩散模型与 VLA（视觉-语言-动作）结合应用于自动驾驶，平衡了速度与精度。
*   **物理对齐**：通过几何感知嵌入，解决了纯语言模型难以理解物理空间距离的问题。
*   **解码优化**：动作优先解码策略非常符合驾驶逻辑（先决定怎么开，再解释为什么）。

#### 8. 不足与局限
*   **场景局限性**：实验主要集中在 nuScenes 离线数据集，尚未在 CARLA 等闭环仿真环境或真实道路上进行长距离测试。
*   **码本瓶颈**：规划精度受限于码本大小 $N$。虽然 $N=256$ 表现最好，但在极端复杂的避障场景下，离散原语可能无法覆盖所有最优路径。
*   **实时性挑战**：虽然 1.72s 的推理速度已大幅提升，但对于需要毫秒级响应的高速自动驾驶场景，仍有进一步优化的空间。

（完）
