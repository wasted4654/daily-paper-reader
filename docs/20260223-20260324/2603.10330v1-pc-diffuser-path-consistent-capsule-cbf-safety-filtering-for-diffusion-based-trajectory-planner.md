---
title: "PC-Diffuser: Path-Consistent Capsule CBF Safety Filtering for Diffusion-Based Trajectory Planner"
title_zh: PC-Diffuser：面向扩散轨迹规划器的路径一致性胶囊 CBF 安全滤波
authors: "Eugene Ku, Yiwei Lyu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10330v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于自动驾驶的安全增强型扩散轨迹规划器
tldr: 针对扩散模型轨迹规划器在复杂场景下缺乏安全性保证的问题，本文提出PC-Diffuser框架。该方法将可验证的路径一致性屏障函数（CBF）直接嵌入去噪循环，利用胶囊体几何建模减少保守性，并结合运动学模型确保动力学可行性。通过在每一步去噪中进行安全修正与反馈，PC-Diffuser实现了迭代式的上下文感知安全保障，在消除碰撞风险的同时保留了扩散模型生成的路径特征。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的轨迹规划器在处理罕见或分布外场景时，难以提供确定性的安全性证明且容易发生灾难性失效。
method: 在扩散去噪过程中引入基于胶囊体距离的CBF安全过滤和运动学约束，通过路径一致性修正确保轨迹的安全与可行。
result: 该框架能在不扭曲原始路径几何特征的前提下，有效消除残余的约束违规，使修正后的计划紧贴学习到的专家分布。
conclusion: PC-Diffuser通过将安全性转化为生成过程的内在组成部分，为数据驱动的自动驾驶规划提供了一种兼顾性能与安全的新方案。
---

## 摘要
复杂交通环境下的自动驾驶要求规划器具备超越手工规则的泛化能力，这促使了从专家演示中学习行为的数据驱动方法的发展。基于扩散的轨迹规划器最近通过迭代去噪全时域规划展现了强大的闭环性能，但它们仍难以进行安全性认证，并且在罕见或分布外场景中可能会发生灾难性失败。为了应对这一挑战，我们提出了 PC-Diffuser，这是一个安全增强框架，它将可认证的、路径一致的控制屏障函数（CBF）结构直接嵌入到扩散规划的去噪循环中。其核心思想是将安全性作为轨迹生成的内在组成部分，而非事后修正：我们在保持扩散模型预期的路径几何形状的同时，强制执行轨迹展开过程中的前向不变性。具体而言，PC-Diffuser (i) 使用胶囊距离屏障函数评估碰撞风险，该函数能更好地反映车辆几何形状并减少不必要的保守性；(ii) 在运动学自行车模型下将去噪后的路径点转换为动力学可行的运动；以及 (iii) 应用路径一致的安全滤波器，在不产生几何失真的情况下消除残余的约束违反，从而使修正后的规划保持接近所学习的分布。通过在每个去噪步骤中注入这些安全一致性修正，并将优化后的轨迹反馈回扩散过程，PC-Diffuser 实现了迭代的、上下文感知的安全保障，而非事后修复。

## Abstract
Autonomous driving in complex traffic requires planners that generalize beyond hand-crafted rules, motivating data-driven approaches that learn behavior from expert demonstrations. Diffusion-based trajectory planners have recently shown strong closed-loop performance by iteratively denoising a full-horizon plan, but they remain difficult to certify and can fail catastrophically in rare or out-of-distribution scenarios. To address this challenge, we present PC-Diffuser, a safety augmentation framework that embeds a certifiable, path-consistent barrier-function structure directly into the denoising loop of diffusion planning. The key idea is to make safety an intrinsic part of trajectory generation rather than a post-hoc fix: we enforce forward invariance along the rollout while preserving the diffusion model's intended path geometry. Specifically, PC-Diffuser (i) evaluates collision risk using a capsule-distance barrier function that better reflects vehicle geometry and reduces unnecessary conservativeness, (ii) converts denoised waypoints into dynamically feasible motion under a kinematic bicycle model, and (iii) applies a path-consistent safety filter that eliminates residual constraint violations without geometric distortion, so the corrected plan remains close to the learned distribution. By injecting these safety-consistent corrections at every denoising step and feeding the refined trajectory back into the diffusion process, PC-Diffuser enables iterative, context-aware safeguarding instead of post-hoc repair...

---

## 论文详细总结（自动生成）

### PC-Diffuser: 基于路径一致性胶囊 CBF 的扩散轨迹规划器安全滤波论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：基于扩散模型（Diffusion-based）的轨迹规划器虽然在长时域规划和处理复杂交互方面表现优异，但由于其本质是数据驱动的生成模型，在面对罕见或分布外（OOD）场景时缺乏形式化的安全性保证，容易产生碰撞等灾难性后果。
*   **研究动机**：现有的安全增强方法（如分类器引导、事后优化滤波等）往往存在三个缺陷：(1) 未针对车辆实际执行的动力学展开（Rollout）进行验证；(2) 修正过程可能导致动力学不可行；(3) 修正过于激进，扭曲了规划器的原始路径几何特征，导致驾驶质量下降。

#### 2. 方法论
PC-Diffuser 的核心思想是将**可验证的、路径一致的屏障函数结构**直接嵌入到扩散模型的去噪循环中。
*   **胶囊距离屏障函数（Capsule Distance CBF）**：
    *   将车辆建模为沿纵轴的线段（胶囊体），相比传统的圆点模型，能更精确地描述车辆几何，减少在狭窄空间（如路口）的保守性。
    *   定义屏障函数 $h(x)$ 为胶囊间的最小距离减去安全阈值。
*   **动力学可行性与执行验证**：
    *   利用 LQR 控制器将去噪后的路径点转换为符合**运动学自行车模型**的控制输入（加速度 $a$ 和转向角 $\delta$）。
    *   确保安全性验证是基于车辆物理上可执行的轨迹，而非抽象的坐标点。
*   **路径一致性修正（Path-Consistent Correction）**：
    *   **核心约束**：在修正时固定转向角 $\delta$，仅通过调节纵向速度/加速度来满足安全约束。
    *   **目的**：保持扩散模型生成的空间路径几何不变，避免因避障而产生剧烈打方向等不符合专家分布的行为。
*   **迭代式去噪嵌入**：
    *   在扩散模型的每一个去噪步骤 $t$ 中，提取预测的清洁轨迹 $\hat{\tau}_0$，应用 PC-CBF 滤波器进行修正，然后将修正后的轨迹重新加噪注入扩散过程。这种“协同进化”的方式让模型在生成过程中不断适应安全约束。

#### 3. 实验设计
*   **数据集与 Benchmark**：使用 **nuPlan** 闭环仿真基准（包含约 1300 小时专家驾驶数据）。
*   **评估场景**：
    *   **Val14**：标准验证集。
    *   **Test14-hard**：专门挑选的规则规划器难以处理的困难场景。
    *   **All-collision Challenge Set**：从 Val14 中提取的原始扩散规划器发生碰撞的 68 个极端场景（碰撞率 100%）。
*   **对比方法**：
    *   Vanilla DiffusionPlanner（基准模型）。
    *   Classifier Guidance（基于梯度的引导）。
    *   SafeDiffuser（扩散时间域的 CBF 约束）。
    *   MPC-CBF（基于优化控制的后处理滤波）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练时长。由于该方法主要是在推理阶段（去噪循环）嵌入安全层，其核心计算开销在于每步去噪中求解一个简单的二次规划（QP）问题。

#### 5. 实验数量与充分性
*   **实验规模**：在 nuPlan 的多个标准切片上进行了大规模闭环仿真，涵盖了数千个场景。
*   **消融实验**：针对“迭代安全保障”、“选择性滤波（仅针对关键障碍物）”和“动力学可行性”三个核心组件进行了消融测试。
*   **充分性评价**：实验设计较为充分。通过引入“全碰撞挑战集”，极大地增强了对安全性边界情况的测试压力。对比实验涵盖了当前主流的扩散模型安全增强路径，对比具有客观性。

#### 6. 主要结论与发现
*   **安全性显著提升**：在“全碰撞挑战集”中，PC-Diffuser 将碰撞率从 **100% 降低至 10.29%**，远优于其他基线方法（其他方法碰撞率仍在 74%-89% 之间）。
*   **驾驶质量不降反升**：在 Val14 和 Test14-hard 上，PC-Diffuser 的综合驾驶得分（Composite Score）均高于原始模型，证明了安全约束与驾驶性能可以兼得。
*   **迭代修正的必要性**：消融实验证明，在去噪过程中多次迭代修正比单次事后修正能产生更平滑、更符合分布的轨迹。

#### 7. 优点
*   **几何精确性**：胶囊体模型比圆形模型更贴合车辆实际形状，减少了不必要的急刹。
*   **路径一致性**：通过锁定转向仅调速，解决了安全滤波容易导致车辆“乱晃”或偏离车道的痛点。
*   **深度集成**：将安全滤波作为生成过程的一部分，而非简单的后处理，使最终轨迹在统计分布上更自然。

#### 8. 不足与局限
*   **反应式局限**：该框架目前仍属于反应式（Reactive）规划，虽然考虑了邻车的预测轨迹，但难以主动诱导或预测邻车对自车行为的复杂反馈。
*   **仿真环境偏差**：实验依赖于 nuPlan 的 IDM 反应式仿真，这与真实世界的交互仍有差距（如文中提到 10% 的剩余碰撞多发生在 IDM 车辆不合理抢行的情况下）。
*   **计算延迟**：虽然 QP 求解很快，但在扩散模型的几十次去噪迭代中反复调用 LQR 和 QP，可能会对实时性提出更高要求（文中未详细讨论推理耗时）。

（完）
