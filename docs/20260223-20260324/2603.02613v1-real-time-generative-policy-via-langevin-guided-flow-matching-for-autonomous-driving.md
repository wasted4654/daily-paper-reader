---
title: Real-Time Generative Policy via Langevin-Guided Flow Matching for Autonomous Driving
title_zh: 基于朗之万引导流匹配的自动驾驶实时生成式策略
authors: "Tianze Zhu, Yinuo Wang, Wenjun Zou, Tianyi Zhang, Likun Wang, Letian Tao, Feihong Zhang, Yao Lyu, Shengbo Eben Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02613v1"
tags: ["query:genad"]
score: 9.0
evidence: 通过流匹配实现的自动驾驶实时生成式策略
tldr: 针对自动驾驶中生成式强化学习策略推理延迟高的问题，本文提出DACER-F算法。该方法将流匹配（Flow Matching）引入在线强化学习，利用Langevin动力学和Q函数梯度优化经验回放中的动作分布，使策略能通过单步推理生成高质量动作。实验表明，DACER-F在复杂驾驶场景和DMC基准测试中均优于现有方法，且具备极低的推理延迟。
selection_source: fresh_fetch
motivation: 生成式策略虽能处理复杂分布但推理速度慢，难以满足自动驾驶的实时性需求。
method: 提出DACER-F，结合流匹配技术与Langevin动力学引导，通过单步推理实现从先验分布到目标动作分布的高效映射。
result: 在多车道、交叉口仿真及DMC人形任务中表现优异，超越了DACER和DSAC等基准模型。
conclusion: DACER-F是一种兼具高性能与计算效率的强化学习算法，成功解决了生成式策略在实时决策中的部署难题。
---

## 摘要
强化学习（RL）是自动驾驶系统中的基础方法，其中生成式策略通过利用其建模复杂分布的能力来增强探索，展现出巨大的潜力。然而，其固有的高推理延迟严重阻碍了它们在实时决策和控制中的部署。为了解决这一问题，我们通过将流匹配（flow matching）引入在线强化学习，提出了基于流匹配且带有熵调节器的扩散 Actor-Critic（DACER-F），从而能够在单次推理步骤中生成具有竞争力的动作。通过利用朗之万动力学（Langevin dynamics）和 Q 函数的梯度，DACER-F 动态地将经验回放中的动作优化至一个平衡高 Q 值信息与探索行为的目标分布。随后，训练流策略以高效地学习从简单先验分布到该动态目标的映射。在复杂的多车道和交叉路口模拟中，DACER-F 在保持极低推理延迟的同时，性能优于基准方法——带有熵调节器的扩散 Actor-Critic（DACER）和分布软 Actor-Critic（DSAC）。DACER-F 进一步在标准强化学习基准测试 DeepMind Control Suite (DMC) 上展示了其可扩展性，在 humanoid-stand 任务中获得了 775.8 分，超越了以往的方法。总之，这些结果确立了 DACER-F 作为一种高性能且计算高效的强化学习算法。

## Abstract
Reinforcement learning (RL) is a fundamental methodology in autonomous driving systems, where generative policies exhibit considerable potential by leveraging their ability to model complex distributions to enhance exploration. However, their inherent high inference latency severely impedes their deployment in real-time decision-making and control. To address this issue, we propose diffusion actor-critic with entropy regulator via flow matching (DACER-F) by introducing flow matching into online RL, enabling the generation of competitive actions in a single inference step. By leveraging Langevin dynamics and gradients of the Q-function, DACER-F dynamically optimizes actions from experience replay toward a target distribution that balances high Q-value information with exploratory behavior. The flow policy is then trained to efficiently learn a mapping from a simple prior distribution to this dynamic target. In complex multi-lane and intersection simulations, DACER-F outperforms baselines diffusion actor-critic with entropy regulator (DACER) and distributional soft actor-critic (DSAC), while maintaining an ultra-low inference latency. DACER-F further demonstrates its scalability on standard RL benchmark DeepMind Control Suite (DMC), achieving a score of 775.8 in the humanoid-stand task and surpassing prior methods. Collectively, these results establish DACER-F as a high-performance and computationally efficient RL algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Real-Time Generative Policy via Langevin-Guided Flow Matching for Autonomous Driving》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在自动驾驶中，强化学习（RL）策略需要处理复杂、多峰的动作分布以增强探索和鲁棒性。生成式模型（如扩散模型）虽具备强大的表达能力，但其**多步迭代采样导致的推理延迟高**，难以满足自动驾驶实时决策（通常要求毫秒级响应）的需求。
*   **核心问题**：如何在保持生成式策略高表达能力的同时，实现极低的推理延迟，并解决在线强化学习中缺乏稳定目标分布（Target Distribution）的难题。

### 2. 方法论：DACER-F
*   **核心思想**：将**流匹配（Flow Matching）**引入在线强化学习，利用其确定性常微分方程（ODE）轨迹实现单步动作生成，并结合**朗之万动力学（Langevin Dynamics）**为在线学习提供动态目标引导。
*   **关键技术细节**：
    *   **流策略表示**：将策略 $\pi_\theta$ 建模为一个向量场，学习从简单高斯先验到高价值动作空间的映射。通过单步 Euler 法即可完成推理。
    *   **动态目标引导机制**：针对在线 RL 缺乏目标分布的问题，将最优策略视为由 Q 函数诱导的能量分布。利用 Q 函数的梯度并加入高斯噪声（即朗之万采样），对经验回放池中的动作进行优化，生成兼顾高 Q 值和探索性的目标动作 $a^*$。
    *   **混合损失函数**：训练目标包含两部分：一是标准的策略梯度（最大化 Q 值），二是流匹配模仿项（使策略输出靠近朗之万优化后的目标 $a^*$）。
    *   **双 Q 网络**：采用双 Critic 结构和目标网络更新机制，以减轻 Q 值的过估计偏差。

### 3. 实验设计
*   **实验场景**：
    1.  **自动驾驶仿真**：基于程序化生成的场景，包括多车道高速公路（换道、汇入）和城市交叉口（左转、掉头等复杂交互）。
    2.  **通用 RL 基准**：DeepMind Control Suite (DMC)，选取了 Humanoid（人形）和 Dog（狗）等 6 个高维连续控制任务。
*   **对比方法（Benchmarks）**：
    *   **DACER**：基于扩散模型的在线 RL 算法（本文的主要改进对象）。
    *   **DSAC**：分布软 Actor-Critic 算法。
    *   **其他生成式/标准 RL**：SAC, DIPO, QSM, SDAC, QVPO 等。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-12900K CPU，NVIDIA RTX 3090 Ti GPU。
*   **软件框架**：基于 PyTorch 和 GOPS（通用最优控制问题求解器）软件库实现。
*   **训练细节**：论文给出了单步迭代时间（20.8 ms）和推理延迟（0.28 ms），但未明确说明总训练时长（通常此类任务在单卡上需数小时至数天不等）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 类驾驶场景和 6 个 DMC 任务，每组实验均使用 **5 个随机种子**运行。
*   **评价指标**：包括总平均奖励（TAR）、到达率、碰撞率、推理延迟、训练迭代时间。
*   **充分性评价**：实验设计较为充分且客观。不仅对比了任务性能，还专门分析了计算效率（推理与训练耗时），并通过可视化案例展示了策略在复杂交互中的行为，证明了算法的实用性。

### 6. 主要结论与发现
*   **性能卓越**：在驾驶场景中，DACER-F 的平均奖励比 DACER 提升了 28%，比 DSAC 提升了 34%。
*   **实时性极佳**：DACER-F 的推理延迟仅为 **0.28 ms**，比扩散模型（DACER）降低了 **84%**，达到了与简单 MLP 策略（DSAC）相当的水平。
*   **高维任务突破**：在极具挑战性的 Humanoid-stand 任务中，DACER-F 取得了 775.8 的高分，而传统方法（如 SAC, DACER）在该任务上几乎无法收敛。

### 7. 优点
*   **高效推理**：通过流匹配技术解决了生成式策略在实时控制系统中的部署瓶颈。
*   **引导有效**：朗之万引导机制成功解决了在线生成式 RL 缺乏“专家数据”的问题，使策略能高效探索高价值区域。
*   **通用性强**：不仅在自动驾驶领域表现优异，在复杂的机器人高维控制任务中也展现了极强的扩展性。

### 8. 不足与局限
*   **训练开销**：虽然推理极快，但训练过程中需要进行朗之万采样和梯度计算，其单步训练迭代时间（20.8 ms）仍高于传统的非生成式方法（如 DSAC 的 11.7 ms）。
*   **仿真依赖**：实验完全基于仿真环境，尚未在真实车辆或具有传感器噪声的现实场景中进行验证。
*   **超参数敏感性**：朗之万动力学的步数、温度参数以及流匹配的权重系数可能需要针对不同任务进行精细调优。

（完）
