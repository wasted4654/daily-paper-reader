---
title: "NaviDriveVLM: Decoupling High-Level Reasoning and Motion Planning for Autonomous Driving"
title_zh: NaviDriveVLM：解耦自动驾驶中的高层推理与运动规划
authors: "Ximeng Tao, Pardis Taghavi, Dimitar Filev, Reza Langari, Gaurav Pandey"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07901v1"
tags: ["query:genad"]
score: 9.0
evidence: 端到端驾驶中推理与运动规划的解耦
tldr: 针对自动驾驶中视觉语言模型（VLM）在高级推理与运动规划效率之间的权衡难题，本文提出NaviDriveVLM框架。该框架采用解耦设计，由大规模“导航器”负责语义理解与推理，轻量级“驱动器”负责具体的动作生成。这种设计在保留强大推理能力的同时，显著降低了训练成本，并提供了可解释的中间表示。实验证明，该方法在nuScenes基准上的表现优于现有大型VLM基准。
selection_source: fresh_fetch
motivation: 现有自动驾驶视觉语言模型在高级语义推理能力与精确运动规划的计算效率之间存在难以调和的矛盾。
method: 提出一种解耦框架，将系统分为负责高层推理的大规模导航器（Navigator）和负责动作生成的轻量级驱动器（Driver）。
result: 在nuScenes基准测试中，NaviDriveVLM在端到端运动规划任务上的表现优于传统的大型视觉语言模型基准。
conclusion: 通过解耦推理与执行过程，该研究实现了高性能、低成本且具有良好可解释性的自动驾驶运动规划方案。
---

## 摘要
视觉语言模型（VLMs）通过对视觉观测、驾驶上下文和基于语言的推理进行联合建模，已成为端到端自动驾驶（AD）的一个极具前景的方向。然而，现有的基于 VLM 的系统在高级推理和运动规划之间面临权衡：大型模型具有强大的语义理解能力，但针对精确控制进行适配的成本很高；而小型 VLM 模型虽然可以高效微调，但推理能力通常较弱。我们提出了 NaviDriveVLM，这是一个解耦框架，通过使用大规模导航器（Navigator）和轻量级可训练驾驶员（Driver）将推理与动作生成分离。该设计保留了推理能力，降低了训练成本，并为下游规划提供了显式的、可解释的中间表示。在 nuScenes 基准测试上的实验表明，NaviDriveVLM 在端到端运动规划方面优于大型 VLM 基准模型。

## Abstract
Vision-language models (VLMs) have emerged as a promising direction for end-to-end autonomous driving (AD) by jointly modeling visual observations, driving context, and language-based reasoning. However, existing VLM-based systems face a trade-off between high-level reasoning and motion planning: large models offer strong semantic understanding but are costly to adapt for precise control, whereas small VLM models can be fine-tuned efficiently but often exhibit weaker reasoning. We propose NaviDriveVLM, a decoupled framework that separates reasoning from action generation using a large-scale Navigator and a lightweight trainable Driver. This design preserves reasoning ability, reduces training cost, and provides an explicit interpretable intermediate representation for downstream planning. Experiments on the nuScenes benchmark show that NaviDriveVLM outperforms large VLM baselines in end-to-end motion planning.

---

## 论文详细总结（自动生成）

这是一份关于论文《NaviDriveVLM: Decoupling High-Level Reasoning and Motion Planning for Autonomous Driving》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：现有的视觉语言模型（VLM）在自动驾驶中面临“推理能力”与“规划精度”的权衡。大型 VLM 语义理解强但微调成本极高且难以直接输出精确控制指令；小型 VLM 易于针对轨迹预测进行微调，但往往丧失了复杂的语义推理能力。
*   **研究动机**：为了同时获得大模型的推理深度和小模型的执行效率，本文提出将“高层推理”与“底层运动规划”解耦，通过显式的语言推理作为中间桥梁来指导驾驶决策。

### 2. 方法论：核心思想与技术细节
NaviDriveVLM 采用**导航器（Navigator）- 驾驶员（Driver）**的解耦架构：
*   **导航器 (Navigator)**：
    *   **角色**：负责场景理解和高层逻辑推理。
    *   **模型**：使用冻结（Frozen）的大规模预训练 VLM（如 Qwen3-VL-8B），无需重新训练以保留其强大的常识推理能力。
    *   **输入**：环视图像、车辆状态（速度、偏航率、加速度）、历史轨迹及高层导航指令（如“左转”、“减速停止”）。
    *   **输出**：场景描述、建议动作及其背后的推理逻辑（Reasoning Tokens）。
*   **驾驶员 (Driver)**：
    *   **角色**：负责具体的运动规划（轨迹预测）。
    *   **模型**：轻量级可训练 VLM（如 Qwen3-VL-2B），通过全监督微调（SFT）成为驾驶专家。
    *   **输入**：导航器生成的推理 Token、前视图像、车辆状态及任务提示。
    *   **输出**：未来 6 秒的路径点（Waypoints）或控制动作（加速度、曲率）。
*   **关键流程**：系统先由导航器生成语义引导，再将其作为辅助输入传递给驾驶员，从而将复杂的语义信息转化为精确的几何坐标。

### 3. 实验设计
*   **数据集**：基于 **nuScenes** 构建了 **nuScenes-Reason** 衍生数据集。包含 850 个场景，通过 8 秒滑动窗口提取数据（2秒历史+6秒未来预测），共计 16.54k 训练样本和 3618 个测试样本。
*   **基准测试 (Benchmark)**：在端到端运动规划任务上进行评估。
*   **对比方法**：
    *   **VLM 基准**：OpenEMMA, LLaVA-7B, 以及单模型 Driver-VLM。
    *   **传统端到端模型**：UniAD, ST-P3, GenAD, VAD-Base, Verdi 等。
*   **评估指标**：不同时间跨度（1s, 2s, 3s, 6s）的平均 L2 误差（米）。

### 4. 资源与算力
*   **硬件**：所有实验均在**单张 NVIDIA RTX 4090 GPU** 上完成。
*   **训练细节**：
    *   使用 AdamW 优化器，初始学习率 $1 \times 10^{-5}$。
    *   训练 3 个 Epoch，梯度累积 16 步。
    *   针对 8B 规模模型应用了 **8-bit 量化**和 **LoRA 适配**（秩为 64）。
    *   为了加速训练，采用了两阶段策略：先预记录导航器的推理结果，再训练驾驶员模型，避免重复推理。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主对比实验、消融实验以及定性可视化分析。
*   **消融实验**：分别验证了“推理信息”、“导航指令”和“图像输入”对最终规划精度的贡献。
*   **充分性评价**：实验设计较为客观，对比了从传统感知规划模型到最新 VLM 模型的多种基线。通过 1s-6s 的长短期误差分析，证明了该架构在不同时间尺度下的有效性。

### 6. 主要结论与发现
*   **解耦优于统一**：解耦架构在 nuScenes 上的表现优于直接微调的单一大模型（如 OpenEMMA）。
*   **推理的价值**：导航器提供的显式推理 Token 显著提升了驾驶员的路径预测精度（3秒 L2 误差达到 0.93m，优于 UniAD 的 0.96m）。
*   **预测目标差异**：在短期预测（1-3s）中，直接预测路径点（Waypoints）更准；而在长期预测（6s）中，预测控制动作（Action）表现更好。
*   **图像冗余性**：消融实验发现，在已有强推理和状态输入的情况下，额外加入图像 Token 对降低 L2 误差的边际贡献较小。

### 7. 优点与亮点
*   **可解释性强**：通过导航器输出的自然语言，系统能清晰解释“为什么要停车”（如：检测到红灯或行人），增强了自动驾驶的透明度。
*   **高效适配**：冻结大模型、微调小模型的策略极大地降低了算力门槛，使得在消费级显卡（4090）上训练高性能驾驶模型成为可能。
*   **性能卓越**：在保持轻量化的同时，运动规划精度达到了与 SOTA（如 UniAD）持平甚至更优的水平。

### 8. 不足与局限
*   **闭环验证缺失**：实验主要基于 nuScenes 的开环评估（Open-loop），未在 CARLA 等模拟器中进行闭环（Closed-loop）测试，难以评估其在动态交互中的实时反应能力。
*   **推理延迟**：虽然使用了轻量级驱动器，但系统仍依赖大型导航器的推理，在实际车载部署时，双模型的串行推理可能会带来延迟挑战。
*   **数据依赖**：导航器的表现高度依赖于预训练 VLM 的质量，且 nuScenes-Reason 数据集的构建可能存在标注偏差。

（完）
