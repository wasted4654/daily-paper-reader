---
title: "UniUncer: Unified Dynamic Static Uncertainty for End to End Driving"
title_zh: UniUncer：面向端到端驾驶的统一动静态不确定性
authors: "Yu Gao, Jijun Wang, Zongzheng Zhang, Anqing Jiang, Yiru Wang, Yuwen Heng, Shuo Wang, Hao Sun, Zhangfeng Hu, Hao Zhao"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07686v1"
tags: ["query:genad"]
score: 9.0
evidence: 端到端驾驶的统一不确定性框架
tldr: UniUncer是首个统一处理端到端驾驶中静态地图和动态障碍物不确定性的轻量级框架。它通过拉普拉斯回归器估计位置不确定性，并利用不确定性融合模块和自适应门控机制，将不确定性信息注入规划查询并动态调节对历史信息的依赖。该方法在nuScenes和NavsimV2基准测试中显著提升了规划性能，且计算开销极低，具有良好的通用性。
selection_source: fresh_fetch
motivation: 现有端到端驾驶模型大多仅关注静态地图不确定性，忽视了动态障碍物的不确定性，导致规划在复杂交互场景下容易产生过度自信且不可靠的决策。
method: 引入拉普拉斯回归器输出概率分布，并通过不确定性融合模块与自适应门控机制，将动静态元素的不确定性整合进查询向量并动态调节历史信息的权重。
result: "在nuScenes开环测试中L2误差降低7%，在NavsimV2伪闭环测试中EPDMS提升10.8%，且推理速度仅下降约0.5 FPS。"
conclusion: 统一建模静态与动态场景元素的不确定性，并将其深度集成到规划流程中，是提升端到端自动驾驶系统可靠性和鲁棒性的关键。
---

## 摘要
端到端（E2E）驾驶已成为工业部署和学术研究的基石，它提供了一个单一的可学习流水线，将多传感器输入映射到动作，同时避免了手工设计的模块。然而，此类流水线的可靠性在很大程度上取决于它们处理不确定性的能力：传感器存在噪声，语义可能具有歧义，且与其他道路使用者的交互本质上是随机的。不确定性还以多种形式出现：分类与定位，以及至关重要的静态地图元素和动态智能体。现有的端到端方法仅对静态地图的不确定性进行建模，使得规划容易受到过度自信和不可靠输入的影响。我们提出了 UniUncer，这是第一个轻量级的统一不确定性框架，它在端到端规划器内部联合估计并利用静态和动态场景元素的不确定性。具体而言：(1) 我们将确定性预测头转换为概率拉普拉斯回归器，为矢量化的静态和动态实体输出逐顶点的坐标位置和尺度；(2) 我们引入了一个不确定性融合模块，对这些参数进行编码并将其注入到物体/地图查询中，以形成不确定性感知查询；(3) 我们设计了一个不确定性感知门，根据当前的不确定性水平自适应地调节对历史输入（自身状态或时序感知查询）的依赖程度。该设计增加了极小的开销，吞吐量仅下降约 0.5 FPS，同时对常见的端到端骨干网络保持即插即用。在 nuScenes（开环）数据集上，UniUncer 将平均 L2 轨迹误差降低了 7%。在 NavsimV2（伪闭环）数据集上，它将整体 EPDMS 提高了 10.8%，并在具有挑战性的重交互场景中取得了显著的第二阶段增益。消融实验证实，动态智能体不确定性和不确定性感知门都是必不可少的。

## Abstract
End-to-end (E2E) driving has become a cornerstone of both industry deployment and academic research, offering a single learnable pipeline that maps multi-sensor inputs to actions while avoiding hand-engineered modules. However, the reliability of such pipelines strongly depends on how well they handle uncertainty: sensors are noisy, semantics can be ambiguous, and interaction with other road users is inherently stochastic. Uncertainty also appears in multiple forms: classification vs. localization, and, crucially, in both static map elements and dynamic agents. Existing E2E approaches model only static-map uncertainty, leaving planning vulnerable to overconfident and unreliable inputs. We present UniUncer, the first lightweight, unified uncertainty framework that jointly estimates and uses uncertainty for both static and dynamic scene elements inside an E2E planner. Concretely: (1) we convert deterministic heads to probabilistic Laplace regressors that output per-vertex location and scale for vectorized static and dynamic entities; (2) we introduce an uncertainty-fusion module that encodes these parameters and injects them into object/map queries to form uncertainty-aware queries; and (3) we design an uncertainty-aware gate that adaptively modulates reliance on historical inputs (ego status or temporal perception queries) based on current uncertainty levels. The design adds minimal overhead and drops throughput by only $\sim$0.5 FPS while remaining plug-and-play for common E2E backbones. On nuScenes (open-loop), UniUncer reduces average L2 trajectory error by 7\%. On NavsimV2 (pseudo closed-loop), it improves overall EPDMS by 10.8\% and notable stage two gains in challenging, interaction-heavy scenes. Ablations confirm that dynamic-agent uncertainty and the uncertainty-aware gate are both necessary.

---

## 论文详细总结（自动生成）

这篇论文提出了 **UniUncer**，一个旨在提升端到端（E2E）自动驾驶系统鲁棒性的轻量级框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
端到端驾驶模型虽然简化了自动驾驶的流水线，但其可靠性高度依赖于对环境不确定性的处理。传感器噪声、语义歧义以及交通参与者的随机行为都是不确定性的来源。
*   **现有局限：** 现有的端到端模型大多将感知输出视为确定性的，或者仅关注静态地图的不确定性（如 UncAD）。
*   **研究动机：** 忽视动态障碍物（车辆、行人等）的不确定性会导致规划器在复杂交互场景中产生“过度自信”的错误决策。
*   **核心目标：** 建立一个统一的框架，同时建模静态地图和动态智能体的不确定性，并利用这些信息指导规划。

### 2. 方法论（核心思想与技术细节）
UniUncer 通过以下三个关键模块实现不确定性的建模与利用：
*   **统一不确定性估计（Unified Uncertainty Estimation）：**
    *   将传统的确定性回归头改为**概率拉普拉斯回归器**。
    *   对矢量化实体的每个顶点预测位置参数 $\mu$（坐标）和尺度参数 $b$（不确定性程度）。
    *   使用负对数似然（NLL）损失函数进行训练，使模型能够学习预测误差的分布。
*   **不确定性融合模块（Uncertainty Fusion Module）：**
    *   使用 MLP 编码器将预测的拉普拉斯参数转换为不确定性特征。
    *   通过**交叉注意力机制**，将这些特征注入到原始的静态/动态查询（Queries）中，生成“不确定性感知查询”。
*   **不确定性感知门（Uncertainty-aware Gate）：**
    *   设计了一个自适应门控机制。根据当前场景的总不确定性水平，动态调节模型对**历史信息**（如历史自身状态、时序感知特征）的依赖程度。
    *   在环境高度不确定时，该机制会抑制可能过时的历史信息，促使模型做出更谨慎的决策。

### 3. 实验设计
*   **数据集与场景：**
    *   **nuScenes：** 用于开环评估，关注轨迹精度和碰撞率。
    *   **NavsimV2：** 用于伪闭环评估，利用 3D 高斯泼溅（3DGS）技术模拟更真实的交互场景。
*   **基准模型（Backbones）：** 选取了 **SparseDrive**（用于 nuScenes）和 **DiffusionDrive**（用于 NavsimV2）作为基础模型。
*   **对比指标：** L2 位移误差、碰撞率（Collision Rate）以及扩展预测驾驶员模型得分（EPDMS）。

### 4. 资源与算力
*   **硬件设备：** 实验使用了 **4 块 NVIDIA RTX 3090 GPU**。
*   **训练细节：** 
    *   在 nuScenes 上，从官方预训练权重开始，分别进行 10 个 epoch 的第一阶段微调和 10 个 epoch 的第二阶段微调。
    *   在 NavsimV2 上，由于数据格式差异，采用了从头训练（from scratch）的方式。
*   **效率表现：** 相比基准模型，UniUncer 仅增加了 5.6M 参数，推理速度仅下降约 **0.5 FPS**（从 6.6 降至 6.1），证明了其轻量化特性。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流基准测试集上进行了详尽的对比实验，并针对静态不确定性、动态不确定性以及门控机制分别进行了**消融实验**。
*   **充分性评价：** 实验设计较为充分。通过在开环和伪闭环两种模式下的验证，证明了该方法在不同评估体系下的有效性。消融实验清晰地展示了动态障碍物不确定性建模对降低碰撞率的关键作用。

### 6 主要结论与发现
*   **精度提升：** 在 nuScenes 上，平均 L2 轨迹误差降低了 **7%**。
*   **安全性增强：** 在 NavsimV2 挑战性场景中，EPDMS 得分提升了 **10.8%**，显著改善了重交互场景下的表现。
*   **协同效应：** 实验发现，仅建模静态地图不确定性提升有限，而将动静态不确定性统一建模并配合门控机制，能产生最佳的规划效果。
*   **鲁棒性：** 在感知结果受干扰（如 3DGS 渲染伪影）时，模型能通过高不确定性权重采取更保守、更类人的避障策略。

### 7. 优点
*   **即插即用：** 模块设计简洁，可以轻松集成到现有的基于 Query 的端到端驾驶框架中。
*   **低开销：** 在显著提升性能的同时，几乎不影响实时推理性能。
*   **首创性：** 填补了端到端驾驶中动态障碍物不确定性建模的空白，实现了动静态要素的统一处理。

### 8. 不足与局限
*   **数据依赖：** 在 NavsimV2 实验中，由于该数据集缺乏矢量化地图标注，无法验证静态不确定性在该基准上的表现。
*   **不确定性类型：** 目前主要关注偶然不确定性（Aleatoric Uncertainty，由数据噪声引起），对认知不确定性（Epistemic Uncertainty，由模型知识不足引起）的探讨较少。
*   **闭环局限：** 虽然使用了 NavsimV2 伪闭环测试，但与真实的物理模拟器或实车测试相比，仍存在一定的环境反馈偏差。

（完）
