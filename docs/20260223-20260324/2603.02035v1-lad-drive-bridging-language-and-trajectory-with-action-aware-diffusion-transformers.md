---
title: "LAD-Drive: Bridging Language and Trajectory with Action-Aware Diffusion Transformers"
title_zh: LAD-Drive：利用动作感知扩散 Transformer 桥接语言与轨迹
authors: "Fabian Schmidt, Karol Fedurko, Markus Enzweiler, Abhinav Valada"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02035v1"
tags: ["query:genad"]
score: 9.0
evidence: 使用扩散Transformer的生成式自动驾驶框架
tldr: "LAD-Drive 针对自动驾驶中多模态大语言模型难以将离散语义转化为连续轨迹的问题，提出了一种将高层意图与底层空间规划解耦的生成式框架。该框架通过动作解码器推断概率元动作分布以保留导航不确定性，并结合动作感知扩散解码器，利用截断去噪过程将运动锚点细化为安全且符合动力学的轨迹。在 LangAuto 基准测试中，其驾驶得分提升高达 59%，显著降低了偏航和碰撞率。"
selection_source: fresh_fetch
motivation: 现有方法在将语义知识转化为轨迹时，受限于单模态规划头且忽略了复杂场景中的导航不确定性。
method: 提出 LAD-Drive 框架，利用动作解码器生成概率元动作分布，并结合扩散变换器通过截断去噪过程优化轨迹。
result: "在 LangAuto 基准测试中达到 SOTA 水平，驾驶得分比竞争基准提高 59%，并大幅减少了路线偏差和碰撞。"
conclusion: 该研究证明了通过解耦意图与规划并引入动作感知扩散机制，能有效提升自动驾驶系统在复杂语义环境下的规划性能。
---

## 摘要
虽然多模态大语言模型 (MLLMs) 为自动驾驶提供了先进的推理能力，但将其离散的语义知识转化为连续轨迹仍然是一个根本性的挑战。现有方法通常依赖于单模态规划头，这本质上限制了它们表示多模态驾驶行为的能力。此外，大多数生成式方法通常以 one-hot 编码的动作为条件，丢弃了对复杂场景至关重要的细微导航不确定性。为了解决这些局限性，我们引入了 LAD-Drive，这是一个在结构上将高层意图与底层空间规划解耦的生成式框架。LAD-Drive 采用动作解码器来推断概率元动作分布，建立一个显式的信念状态，从而保留了 one-hot 编码通常会丢失的细微意图。该分布与车辆的运动学状态融合，作为动作感知扩散解码器的条件，该解码器利用截断去噪过程将学习到的运动锚点细化为安全且符合运动学原理的轨迹。在 LangAuto 基准测试上的广泛评估表明，LAD-Drive 取得了最先进的结果，在驾驶评分 (Driving Score) 方面优于竞争基准高达 59%，同时显著减少了路线偏离和碰撞。我们将在 https://github.com/iis-esslingen/lad-drive 公开发布代码和模型。

## Abstract
While multimodal large language models (MLLMs) provide advanced reasoning for autonomous driving, translating their discrete semantic knowledge into continuous trajectories remains a fundamental challenge. Existing methods often rely on unimodal planning heads that inherently limit their ability to represent multimodal driving behavior. Furthermore, most generative approaches frequently condition on one-hot encoded actions, discarding the nuanced navigational uncertainty critical for complex scenarios. To resolve these limitations, we introduce LAD-Drive, a generative framework that structurally disentangles high-level intention from low-level spatial planning. LAD-Drive employs an action decoder to infer a probabilistic meta-action distribution, establishing an explicit belief state that preserves the nuanced intent typically lost by one-hot encodings. This distribution, fused with the vehicle's kinematic state, conditions an action-aware diffusion decoder that utilizes a truncated denoising process to refine learned motion anchors into safe, kinematically feasible trajectories. Extensive evaluations on the LangAuto benchmark demonstrate that LAD-Drive achieves state-of-the-art results, outperforming competitive baselines by up to 59% in Driving Score while significantly reducing route deviations and collisions. We will publicly release the code and models on https://github.com/iis-esslingen/lad-drive.

---

## 论文详细总结（自动生成）

### LAD-Drive 论文深度总结

#### 1. 核心问题与研究动机
论文针对自动驾驶中**多模态大语言模型（MLLM）推理能力与连续轨迹规划之间的断层**提出了解决方案。
*   **研究背景**：虽然 LLM 具备强大的语义推理能力，但直接将离散的文本 Token 转化为连续坐标（如直接预测路点）会导致物理不可行、输出畸变及长期规划不稳定。
*   **核心问题**：
    1.  **意图与执行的耦合**：现有方法强迫单一网络头同时处理“做什么”（高层语义）和“怎么做”（底层空间规划），导致模型容量过载。
    2.  **多模态建模缺失**：传统的回归损失（L1/L2）在面对“左转或右转避障”等对等选择时会产生均值模糊，导致轨迹失效。
    3.  **条件约束不足**：现有的生成式方法多采用 One-hot 编码动作，丢弃了导航中的不确定性（概率分布）。

#### 2. 方法论：LAD-Drive 框架
LAD-Drive 提出了一个**结构化解耦意图与规划**的生成式框架，包含三个核心模块：
*   **语言模块 (AD-MLLM)**：利用视觉编码器和 Q-Former 将多视角图像和 LiDAR 数据压缩为场景 Token，输入 LLM（如 LLaVA）生成上下文隐藏状态。
*   **动作解码器 (Action Decoder)**：从 LLM 隐藏状态中推断**概率元动作分布**（如左转、直行等 6 类动作的概率）。这建立了一个显式的“信念状态”，保留了导航中的细微不确定性。
*   **动作感知扩散解码器 (Diffusion Decoder)**：
    *   **特征瓶颈 (Feature Bottleneck)**：通过 MLP 将 4096 维的 LLM 状态压缩至 512 维，过滤掉与空间规划无关的语义噪声。
    *   **状态-意图融合**：将概率动作分布与车辆运动状态（速度、偏航角）拼接，作为扩散过程的条件。
    *   **截断去噪过程**：不从纯噪声开始，而是从基于 K-means 聚类生成的 20 个**运动锚点 (Anchors)** 开始，仅需 2 步去噪即可生成安全、符合动力学的多模态轨迹。

#### 3. 实验设计
*   **数据集与场景**：在 **CARLA 模拟器**的 **LangAuto 基准测试**上进行评估。该基准包含 Tiny、Short、Long 三种长度的路线，涵盖 8 个不同的城镇。
*   **评估指标**：驾驶得分 (DS)、路线完成率 (RC) 和违章得分 (IS)。
*   **对比方法**：包括 LMDrive (CVPR'24)、AD-H、BEVDriver (IROS'25)、SToRM、VLDrive (ICCV'25) 和 AdaDrive (ICCV'25) 等最先进的 LLM 驱动的自动驾驶模型。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA L40s GPU**。
*   **训练细节**：采用两阶段训练策略（空间接地 + 语义对齐）。总训练时长约为 **7 小时**。
*   **推理效率**：模型总实时延迟为 **47.04ms**，其中扩散解码器仅引入了约 6.89ms 的额外开销，满足实时性要求。

#### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的闭环仿真测试，所有结果均为 3 次独立运行的平均值。
*   **消融实验**：非常充分。作者系统地验证了：
    1.  扩散解码器 vs. 回归头的效果。
    2.  横向动作、纵向动作及自我状态（Ego-status）作为条件的各自贡献。
    3.  特征瓶颈维度（128 到 4096）对性能的影响。
*   **客观性**：通过对比不同长度的路线和多种违章类型（碰撞、偏航等），客观展示了模型在安全性和导航准确性上的提升。

#### 6. 主要结论与发现
*   **性能飞跃**：LAD-Drive 在 LangAuto 上的平均驾驶得分 (DS) 达到 68.2，比基准 LMDrive 提升了 **59%**。
*   **安全性显著提升**：与车辆碰撞率降低了 76%，与行人碰撞率降低了 80%，路线偏离率降低了 81%。
*   **解耦的必要性**：实验证明，直接将 LLM 原始高维特征输入规划器会导致性能崩溃（DS 从 68.2 降至 48.5），证明了特征瓶颈和结构化解耦的必要性。
*   **纵向动作冗余**：发现加入纵向动作头反而会降低性能，因为纵向动力学主要受物理状态约束而非高层语义。

#### 7. 优点
*   **概率化建模**：通过概率分布而非 One-hot 编码来传递意图，有效处理了导航中的歧义。
*   **高效生成**：利用截断扩散和运动锚点，在极少的去噪步数下实现了高质量的多模态轨迹生成。
*   **可解释性**：显式的动作解码器输出（如“左转”概率）增强了黑盒 LLM 决策的可解释性。
*   **轻量化规划器**：扩散解码器的参数量比基准回归头减少了 1.48M。

#### 8. 不足与局限
*   **仿真环境局限**：实验完全基于 CARLA 模拟器，尚未在真实世界数据集（如 nuPlan 或真实道路）上验证其泛化能力。
*   **纵向控制简化**：模型主要优化了横向意图，纵向控制仍较大程度上依赖于基础的 PID 控制器和物理状态。
*   **长尾场景覆盖**：虽然 LLM 擅长推理，但论文未详细讨论在极端罕见传感器故障或极端天气下的鲁棒性表现。

（完）
