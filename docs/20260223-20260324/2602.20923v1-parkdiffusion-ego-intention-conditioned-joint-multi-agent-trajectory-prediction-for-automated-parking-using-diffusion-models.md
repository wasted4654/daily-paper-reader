---
title: "ParkDiffusion++: Ego Intention Conditioned Joint Multi-Agent Trajectory Prediction for Automated Parking using Diffusion Models"
title_zh: ParkDiffusion++：利用扩散模型进行自动泊车中基于自车意图条件的联合多智能体轨迹预测
authors: "Jiarong Wei, Anna Rehr, Christian Feist, Abhinav Valada"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20923v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于多智能体轨迹预测的扩散模型
tldr: ParkDiffusion++ 针对自动泊车场景，提出了一种基于扩散模型的自我意图条件联合多智能体轨迹预测框架。该方法通过自我意图分词器预测离散终点，并以此为条件生成周围智能体的协同反应。引入轻量化安全引导去噪器和反事实知识蒸馏技术，显著提升了预测的社会一致性和安全性。在 DLP 和 inD 数据集上达到 SOTA 水平，能有效模拟“如果-那么”决策场景。
selection_source: fresh_fetch
motivation: 现有自动泊车预测方法往往将自我意图预测与周围智能体反应孤立处理，难以实现有效的交互式决策。
method: 提出包含自我意图分词器、条件联合预测器、安全引导去噪器及反事实知识蒸馏的扩散模型框架。
result: 在 Dragon Lake Parking 和 Intersections Drone 数据集上均取得了最先进的性能，并展现出良好的交互反应可视化效果。
conclusion: ParkDiffusion++ 通过整合自我意图与联合轨迹预测，为自动泊车中的复杂交互建模提供了高效且安全的解决方案。
---

## 摘要
自动泊车是高级驾驶辅助系统（ADAS）中一个具有挑战性的运行领域，需要鲁棒的场景理解和交互推理。其核心挑战包含两个方面：(i) 根据上下文预测多个合理的自车意图；(ii) 针对每个意图，预测周围智能体的联合响应，从而实现有效的“假设（what-if）”决策。然而，现有方法往往表现不足，通常将这些相互依赖的问题孤立对待。我们提出了 ParkDiffusion++，它联合学习了一个多模态自车意图预测器和一个基于自车条件的自动泊车多智能体联合轨迹预测器。我们的方法具有以下几项关键贡献。首先，我们引入了一个自车意图分词器（tokenizer），从智能体历史轨迹和矢量化地图折线中预测一小组离散的终点意图。其次，我们执行基于自车意图条件的联合预测，为每个可能的自车意图生成符合社会一致性的周围智能体预测。第三，我们采用了一个带有不同约束的轻量级安全引导去噪器，在训练过程中优化联合场景，从而提高准确性和安全性。第四，我们提出了反事实知识蒸馏，其中由冻结的安全引导去噪器优化的 EMA 教师模型提供伪目标，以捕捉智能体对备选自车意图的反应。广泛的评估表明，ParkDiffusion++ 在龙湖泊车（DLP）数据集和交叉口无人机（inD）数据集上均达到了最先进的性能。重要的是，定性的“假设”可视化结果显示，其他智能体能够对不同的自车意图做出恰当的反应。

## Abstract
Automated parking is a challenging operational domain for advanced driver assistance systems, requiring robust scene understanding and interaction reasoning. The key challenge is twofold: (i) predict multiple plausible ego intentions according to context and (ii) for each intention, predict the joint responses of surrounding agents, enabling effective what-if decision-making. However, existing methods often fall short, typically treating these interdependent problems in isolation. We propose ParkDiffusion++, which jointly learns a multi-modal ego intention predictor and an ego-conditioned multi-agent joint trajectory predictor for automated parking. Our approach makes several key contributions. First, we introduce an ego intention tokenizer that predicts a small set of discrete endpoint intentions from agent histories and vectorized map polylines. Second, we perform ego-intention-conditioned joint prediction, yielding socially consistent predictions of the surrounding agents for each possible ego intention. Third, we employ a lightweight safety-guided denoiser with different constraints to refine joint scenes during training, thus improving accuracy and safety. Fourth, we propose counterfactual knowledge distillation, where an EMA teacher refined by a frozen safety-guided denoiser provides pseudo-targets that capture how agents react to alternative ego intentions. Extensive evaluations demonstrate that ParkDiffusion++ achieves state-of-the-art performance on the Dragon Lake Parking (DLP) dataset and the Intersections Drone (inD) dataset. Importantly, qualitative what-if visualizations show that other agents react appropriately to different ego intentions.

---

## 论文详细总结（自动生成）

以下是对论文《ParkDiffusion++: Ego Intention Conditioned Joint Multi-Agent Trajectory Prediction for Automated Parking using Diffusion Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在自动泊车等非结构化场景中，如何准确预测自车（Ego）的不同意图，并同步预测周围智能体对这些意图的“假设性（What-if）”反应。
*   **研究动机**：
    *   **交互复杂性**：泊车环境车道语义弱、人车混行，智能体间的交互（如避让、跟随）高度依赖于自车的决策。
    *   **现有方法局限**：多数方法为开环预测，未建模对他车反应的条件约束；或仅提供边缘预测（Marginal），缺乏场景一致性和安全性约束。
    *   **数据稀缺性**：真实数据集仅记录了已发生的单一现实，缺乏自车采取其他意图时他车反应的监督信号（反事实标签）。

### 2. 方法论
ParkDiffusion++ 采用两阶段框架，结合扩散模型与知识蒸馏技术：
*   **阶段一：自车意图分词器（Ego Intention Tokenizer）**
    *   利用 Transformer 编码器融合智能体历史和矢量化地图特征。
    *   通过学习到的模式嵌入（Mode Embeddings）预测 $K$ 个离散的未来终点意图，作为后续预测的条件。
*   **阶段二：条件联合预测器（Conditioned Joint Predictor）**
    *   **反应式社会建模**：引入“暴露门（Exposure Gate）”机制，根据自车意图路径动态调整周围智能体的注意力权重。
    *   **联合场景选择**：先生成各智能体的边缘轨迹，再通过束搜索（Beam Search）和学习到的评分器筛选出 $K_{scene}$ 个一致的联合场景。
    *   **安全引导去噪器（Safety-Guided Denoiser）**：预训练一个基于扩散模型的去噪器，在推理/训练中通过几何势函数（如碰撞惩罚、障碍物距离、路径约束等）的梯度引导，对轨迹进行确定性优化。
    *   **反事实知识蒸馏（CKD）**：使用 EMA（指数移动平均）更新的教师模型配合冻结的去噪器，为未发生的自车意图生成“安全且合理”的伪标签，指导学生模型学习交互反应。

### 3. 实验设计
*   **数据集**：
    *   **Dragon Lake Parking (DLP)**：复杂的真实泊车场景。
    *   **Intersections Drone (inD)**：具有挑战性人车交互的城市交叉口场景。
*   **Benchmark 与对比方法**：
    *   对比了 WIMP、SceneTransformer、ScePT、MotionLM、DTPP 等先进的条件轨迹预测模型。
    *   以 ParkDiffusion†（基于边缘预测的随机组合）作为基准。
*   **评价指标**：ADE/FDE（位移误差）、MR（漏检率）、OR（碰撞率/重叠率）、mAP（平均精度）。

### 4. 资源与算力
*   **硬件使用**：使用了 **2 台 NVIDIA A100 GPU** 进行训练。
*   **训练时长**：阶段一训练 20 个 epoch，阶段二在冻结阶段一的基础上再训练 20 个 epoch。
*   **参数设置**：意图数 $K=6$，边缘轨迹数 $M=6$，反事实采样概率 $p_{cf}=0.5$。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的数据集（泊车 vs 交叉口）上进行了全面测试。
*   **消融实验**：
    *   **组件分析**：验证了联合选择器（JS）、安全引导去噪器（SGD）和反事实蒸馏（CKD）的增量贡献。
    *   **参数敏感性**：测试了意图数量 $K$、采样策略以及 CKD 采样概率对性能的影响。
*   **充分性评价**：实验设计较为充分，涵盖了定量指标对比、定性可视化分析以及深入的消融研究，客观地展示了模型在安全性（OR 指标）上的显著优势。

### 6. 主要结论与发现
*   **性能领先**：ParkDiffusion++ 在两个数据集上的 FDE、MR、OR 和 mAP 指标均达到 SOTA 水平。
*   **安全性提升**：引入安全引导去噪器显著降低了碰撞率（OR），在 DLP 数据集上将最终碰撞率从 0.28 降至 0.05。
*   **交互建模有效**：定性分析证明，模型能根据自车的不同意图（如加速通过或停车避让），生成他车相应的减速或绕行反应。

### 7. 优点与亮点
*   **反事实学习**：通过 CKD 解决了轨迹预测中“未观测意图”缺乏监督信号的难题。
*   **规则与数据结合**：将硬性的物理/安全规则（通过去噪器引导）与数据驱动的扩散模型结合，兼顾了预测的准确性与合规性。
*   **场景一致性**：通过联合选择机制而非简单的边缘预测组合，确保了多智能体轨迹在空间上的互斥性和逻辑上的协同性。

### 8. 不足与局限
*   **计算开销**：虽然去噪器是轻量级的，但扩散模型的迭代优化过程在实时性要求极高的在线规划中可能仍存在压力。
*   **人工势函数依赖**：安全引导依赖于手工设计的几何势函数（如半径设定），可能无法覆盖所有极端复杂的交通规则。
*   **闭环差距**：目前仍属于离线轨迹预测范畴，与真实的闭环控制和动态规划之间仍存在一定差距。

（完）
