---
title: "MeanFuser: Fast One-Step Multi-Modal Trajectory Generation and Adaptive Reconstruction via MeanFlow for End-to-End Autonomous Driving"
title_zh: MeanFuser：通过 MeanFlow 实现端到端自动驾驶的快速单步多模态轨迹生成与自适应重构
authors: "Junli Wang, Xueyi Liu, Yinan Zheng, Zebing Xing, Pengfei Li, Guang Li, Kun Ma, Guang Chen, Hangjun Ye, Zhongpu Xia, Long Chen, Qichao Zhang"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20060v1"
tags: ["query:genad"]
score: 9.0
evidence: 使用MeanFlow进行端到端驾驶的生成式轨迹规划
tldr: MeanFuser 是一种端到端自动驾驶方法，旨在解决生成式轨迹规划中离散锚点带来的鲁棒性与效率权衡。它通过高斯混合噪声实现连续轨迹表示，并利用 MeanFlow Identity 建模平均速度场，实现无 ODE 误差的一步快速推理。结合自适应重构模块，该方法在 NAVSIM 基准测试中无需 PDM 监督即可实现卓越性能与高效推理。
selection_source: fresh_fetch
motivation: 解决生成式轨迹规划中离散锚点词表导致的覆盖率不足及推理效率低下的问题。
method: 引入高斯混合噪声引导生成，并利用 MeanFlow 建模平均速度场以实现单步推理，配合自适应重构模块优化轨迹。
result: 在 NAVSIM 闭环基准测试中，MeanFuser 在无 PDM Score 监督下取得了领先的性能和极高的推理速度。
conclusion: MeanFuser 为端到端自动驾驶提供了一种兼顾鲁棒性、多模态生成能力和实时性的高效解决方案。
---

## 摘要
生成模型在轨迹规划中展现出巨大潜力。最近的研究表明，锚点引导（anchor-guided）的生成模型在建模驾驶行为的不确定性和提高整体性能方面非常有效。然而，这些方法依赖于离散的锚点词表，这些词表在测试期间必须充分覆盖轨迹分布以确保鲁棒性，从而导致了词表大小与模型性能之间的固有权衡。为了克服这一局限性，我们提出了 MeanFuser，这是一种端到端的自动驾驶方法，通过三项关键设计增强了效率和鲁棒性：(1) 我们引入了高斯混合噪声（GMN）来引导生成采样，实现了轨迹空间的连续表示，并消除了对离散锚点词表的依赖。(2) 我们将“MeanFlow Identity”应用于端到端规划，它建模了 GMN 与轨迹分布之间的平均速度场，而非传统流匹配（flow matching）方法中使用的瞬时速度场，从而有效消除了 ODE 求解器的数值误差，并显著加速了推理。(3) 我们设计了一个轻量级的自适应重构模块（ARM），使模型能够通过注意力权重隐式地从所有采样提案中进行选择，或者在没有满意提案时重构出一条新轨迹。在 NAVSIM 闭环基准测试上的实验表明，MeanFuser 在没有 PDM Score 监督的情况下实现了卓越的性能和极高的推理效率，为端到端自动驾驶提供了一种鲁棒且高效的解决方案。我们的代码和模型可在 https://github.com/wjl2244/MeanFuser 获取。

## Abstract
Generative models have shown great potential in trajectory planning. Recent studies demonstrate that anchor-guided generative models are effective in modeling the uncertainty of driving behaviors and improving overall performance. However, these methods rely on discrete anchor vocabularies that must sufficiently cover the trajectory distribution during testing to ensure robustness, inducing an inherent trade-off between vocabulary size and model performance. To overcome this limitation, we propose MeanFuser, an end-to-end autonomous driving method that enhances both efficiency and robustness through three key designs. (1) We introduce Gaussian Mixture Noise (GMN) to guide generative sampling, enabling a continuous representation of the trajectory space and eliminating the dependency on discrete anchor vocabularies. (2) We adapt ``MeanFlow Identity" to end-to-end planning, which models the mean velocity field between GMN and trajectory distribution instead of the instantaneous velocity field used in vanilla flow matching methods, effectively eliminating numerical errors from ODE solvers and significantly accelerating inference. (3) We design a lightweight Adaptive Reconstruction Module (ARM) that enables the model to implicitly select from all sampled proposals or reconstruct a new trajectory when none is satisfactory via attention weights. Experiments on the NAVSIM closed-loop benchmark demonstrate that MeanFuser achieves outstanding performance without the supervision of the PDM Score. and exceptional inference efficiency, offering a robust and efficient solution for end-to-end autonomous driving. Our code and model are available at https://github.com/wjl2244/MeanFuser.

---

## 论文详细总结（自动生成）

以下是对论文《MeanFuser: Fast One-Step Multi-Modal Trajectory Generation and Adaptive Reconstruction via MeanFlow for End-to-End Autonomous Driving》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的生成式端到端自动驾驶模型（如基于扩散模型或流匹配的模型）在轨迹规划中面临两个主要挑战：
    1.  **离散锚点依赖**：许多方法依赖预定义的离散轨迹词表（锚点）来引导生成，这导致了词表大小（计算开销）与覆盖范围（鲁棒性）之间的权衡。
    2.  **推理效率低下**：传统的扩散模型和流匹配方法需要多次迭代采样（多步 ODE 求解），难以满足自动驾驶对实时性的严苛要求。
*   **研究背景**：为了捕捉驾驶行为的多模态不确定性，生成模型被引入规划领域，但如何在保证多模态表达能力的同时实现单步快速推理，仍是一个待解决的难题。

### 2. 核心方法论
MeanFuser 提出了三个关键设计来实现高效、鲁棒的规划：
*   **高斯混合噪声 (Gaussian Mixture Noise, GMN)**：
    *   放弃了离散锚点词表，改用连续的高斯混合分布作为先验。
    *   通过对专家轨迹进行聚类，提取出 $K$ 个不同的驾驶模式（如跟车、换道、激进、保守），每个模式对应一个高斯分量。这为模型提供了连续且覆盖广泛的轨迹空间表示。
*   **MeanFlow Identity (平均流恒等式)**：
    *   不同于传统流匹配建模“瞬时速度场”，MeanFuser 引入了 **MeanFlow** 范式，直接建模噪声分布与目标轨迹分布之间的**平均速度场**。
    *   **公式逻辑**：通过学习平均速度 $u_\theta$，模型可以强制样本轨迹在向量空间中呈直线运动。这消除了 ODE 求解器的数值误差，使得模型仅需**单步（One-Step）**即可生成高质量轨迹。
*   **自适应重构模块 (Adaptive Reconstruction Module, ARM)**：
    *   这是一个轻量级的交叉注意力模块。它接收所有采样的候选提案（Proposals）和场景上下文特征。
    *   **功能**：模型通过注意力权重隐式地评估提案。如果现有提案表现良好，则进行选择；如果所有提案都不理想，ARM 能够基于上下文信息重构出一条更优的轨迹。

### 3. 实验设计
*   **数据集与基准**：
    *   **NAVSIM (v1 & v2)**：基于 nuPlan 的闭环评估基准，使用 PDM Score (PDMS) 和扩展 PDMS (EPDMS) 作为主要指标。
    *   **CARLA (Longest6)**：经典的闭环仿真基准，评估 Driving Score (DS)。
*   **对比方法**：包括 TransFuser, UniAD, VADv2, Hydra-MDP, GoalFlow, DiffusionDrive 等当前最先进的（SOTA）端到端模型。
*   **评估指标**：安全性（无碰撞 NC）、合规性（道路区域 DAC）、舒适度（Comf.）、进度（EP）以及推理速度（FPS）。

### 4. 资源与算力
*   **推理算力**：论文明确指出推理速度是在单张 **NVIDIA H20 GPU** 上测量的。
*   **性能表现**：MeanFuser 达到了 **59 FPS** 的全流程速度。在规划模块（Plan FPS）上，其速度高达 **434 FPS**，比 GoalFlow 快 39.45 倍，比 DiffusionDrive 快 5.78 倍。
*   **训练细节**：使用了 AdamW 优化器，权重衰减 0.1，余弦退火调度，包含 3 个 epoch 的预热。隐藏层维度设为 128。文中未明确提及训练总时长和使用的 GPU 总数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 NAVSIM v1 和 v2 上进行了全面的性能对比。
    *   在 CARLA Longest6 上进行了闭环验证。
    *   **消融实验**：针对 GMN 分量数量（2, 8, 16, 32）、GMN 生成方式（数据聚类 vs. 手动设计）、ARM 模块的有效性以及多模态多样性指标（D）进行了详细测试。
*   **充分性评价**：实验设计非常充分，涵盖了从开环指标到闭环仿真的全过程。通过对比不同噪声分布和重构策略，客观地证明了 MeanFlow 在自动驾驶任务中的优越性。

### 6. 主要结论与发现
*   **性能领先**：在仅使用图像输入的情况下，MeanFuser 在 NAVSIMv1 上达到 **89.0 PDMS**，在 NAVSIMv2 上达到 **89.5 EPDMS**，超越了所有包含 LiDAR 输入的竞争对手。
*   **效率极高**：单步采样机制使其成为目前最快的生成式规划器之一，能够满足车载实时部署需求。
*   **多模态优势**：GMN 能够自然地产生不同的驾驶风格（从保守到激进），且 ARM 显著增强了模型在复杂、分布外（OOD）场景下的鲁棒性。

### 7. 优点与亮点
*   **创新性应用**：首次将 MeanFlow 范式引入端到端自动驾驶，解决了生成模型推理慢的痛点。
*   **摆脱锚点限制**：通过连续的 GMN 建模，解决了离散词表覆盖不足的问题，提高了泛化能力。
*   **端到端简洁性**：模型设计轻量，不依赖复杂的规则评分函数（如 PDM Score 监督），纯靠专家轨迹学习。

### 8. 不足与局限
*   **模仿学习局限**：尽管性能优异，但本质仍属于模仿学习，其上限受限于专家数据的质量和多样性。
*   **数据饥饿风险**：消融实验显示，当 GMN 分量过多（如 32 个）时，性能反而下降，这表明每个分量分配到的训练数据变少，可能导致学习不充分。
*   **感知依赖**：虽然规划器很强，但整体性能仍受限于 ResNet-34 等视觉骨干网络的感知精度，在极端天气或传感器故障下的表现未被深入探讨。

（完）
