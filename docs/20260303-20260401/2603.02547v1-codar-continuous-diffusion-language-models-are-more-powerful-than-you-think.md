---
title: "CoDAR: Continuous Diffusion Language Models are More Powerful Than You Think"
title_zh: CoDAR：连续扩散语言模型比你想象的更强大
authors: "Junzhe Shen, Jieru Zhao, Ziwei He, Zhouhan Lin"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02547v1"
tags: ["query:ardm"]
score: 10.0
evidence: 带有上下文自回归解码器的连续扩散模型
tldr: 本研究探讨了连续扩散语言模型（DLM）表现落后于离散模型的原因，发现将去噪嵌入映射回标记的“舍入”过程是性能瓶颈。为此，作者提出了CoDAR框架，将扩散过程保持在连续嵌入空间，并引入自回归Transformer解码器进行上下文相关的离散化。实验证明，CoDAR在LM1B和OpenWebText上显著提升了生成质量，达到了与领先离散DLM相当的水平，并提供了灵活的生成调节手段。
selection_source: fresh_fetch
motivation: 旨在解决连续扩散语言模型在生成质量上长期落后于离散扩散模型的问题，并识别出传统的简单舍入机制是核心瓶颈。
method: 提出CoDAR框架，结合了连续空间的扩散动力学与一个利用交叉注意力进行上下文还原的自回归解码器。
result: 在多个大规模文本数据集上，CoDAR显著优于传统的潜在扩散模型，并展现出与强力离散扩散模型竞争的实力。
conclusion: 通过引入强大的上下文解码器，连续扩散模型在文本生成领域展现出比以往认知中更强大的性能和潜力。
---

## 摘要
我们研究了为什么连续扩散语言模型（DLMs）尽管具有吸引人的连续生成动力学，但在性能上却落后于离散扩散方法。通过一项受控的 token 恢复研究，我们发现 token 舍入（即从去噪嵌入到 token 的最终投影）是主要的瓶颈。基于这些见解，我们提出了 CoDAR（带有上下文自回归解码器的连续扩散），这是一个两阶段框架，它在嵌入空间中保持扩散过程完全连续，同时学习一个强大的、依赖上下文的离散化器：一个自回归 Transformer 解码器，它对去噪后的嵌入序列进行交叉注意力计算，并执行上下文相关的 token 舍入。在 LM1B 和 OpenWebText 上的实验表明，CoDAR 相比潜空间扩散（latent diffusion）显著提高了生成质量，并达到了与强力离散 DLMs 相当的竞争力，同时提供了一个简单的解码器温度调节旋钮，用于在流畅度与多样性之间进行权衡。

## Abstract
We study why continuous diffusion language models (DLMs) have lagged behind discrete diffusion approaches despite their appealing continuous generative dynamics. Under a controlled token--recovery study, we identify token rounding, the final projection from denoised embeddings to tokens, as a primary bottleneck. Building on these insights, we propose CoDAR (Continuous Diffusion with Contextual AutoRegressive Decoder), a two--stage framework that keeps diffusion entirely continuous in an embedding space while learning a strong, context--conditional discretizer: an autoregressive Transformer decoder that cross--attends to the denoised embedding sequence and performs contextualized rounding to tokens. Experiments on LM1B and OpenWebText demonstrate that CoDAR substantially improves generation quality over latent diffusion and becomes competitive with strong discrete DLMs, while exposing a simple decoder--temperature knob to navigate the fluency--diversity trade off.

---

## 论文详细总结（自动生成）

以下是对论文《CoDAR: Continuous Diffusion Language Models are More Powerful Than You Think》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管连续扩散模型（Continuous Diffusion Models）在图像生成领域取得了巨大成功，但在自然语言处理（NLP）领域一直落后于离散扩散模型。
*   **研究动机**：作者认为性能差距并非源于扩散目标本身，而是源于**“舍入瓶颈”（Rounding Bottleneck）**。传统的连续扩散模型通常使用简单的点对点（Point-wise）线性层将去噪后的连续嵌入映射回离散标记（Tokens），这种方式忽略了语言的序列依赖性和上下文约束，导致在嵌入空间稍有偏差时无法准确恢复标记。

### 2. 方法论
CoDAR 提出了一个两阶段框架，将生成过程解耦：
*   **核心思想**：在连续嵌入空间进行扩散生成，但使用一个强大的、具备上下文感知能力的自回归解码器进行“舍入”。
*   **关键技术细节**：
    *   **第一阶段：连续扩散生成**。在预训练嵌入空间（如 Qwen3-Embedding）中进行扩散。采用**速度参数化（v-prediction）**和余弦噪声调度，通过训练一个去噪网络（Denoiser）来预测速度向量 $v_t$，从而在推理时从高斯噪声中恢复出连续嵌入序列 $\hat{x}_0$。
    *   **第二阶段：上下文自回归解码（Contextualized Rounding）**。引入一个带有**交叉注意力（Cross-Attention）**机制的 Transformer 解码器。该解码器以第一阶段生成的 $\hat{x}_0$ 为条件，通过自回归方式逐个生成离散标记。
    *   **噪声增强训练**：为了提高解码器对扩散模型输出误差的容忍度，在训练解码器时对输入嵌入添加了少量的随机高斯噪声。
*   **理论支撑**：论文通过数学证明（命题1）指出，点对点解码器与序列解码器之间存在“最优性差距”，该差距由条件全相关性（Conditional Total Correlation）和局部性差距（Locality Gap）决定。

### 3. 实验设计
*   **数据集**：
    *   **LM1B (One Billion Word)**：用于与潜在扩散模型（LD4LG）进行对比。
    *   **OpenWebText**：用于与强力离散扩散模型（MDLM, SEDD）进行大规模无条件生成对比。
*   **Benchmark 与对比方法**：
    *   **对比方法**：LD4LG（潜在扩散）、MDLM（离散掩码扩散）、SEDD（离散分数扩散）。
    *   **评估指标**：生成困惑度（Gen. PPL，衡量流畅度）和基于 n-gram 的多样性得分（Diversity）。
*   **受控实验**：进行了一项标记恢复实验，对比线性分类器与 AR 解码器在不同维度下从隐状态恢复原始标记的准确率。

### 4. 资源与算力
*   **训练细节**：
    *   扩散模型和解码器均使用 Adam 优化器，Batch Size 为 512。
    *   CoDAR 在 OpenWebText 上训练了 25 万步（250k steps），上下文长度为 512。
    *   对比模型 MDLM 和 SEDD 按照惯例训练了 100 万步（1M steps）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但提到了训练步数、学习率调度（余弦退火）和梯度裁剪等超参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流数据集的生成任务。
    *   **消融实验**非常充分，包括：
        1.  **采样器对比**：对比了祖先采样（Ancestral Sampling）与二阶 ODE 求解器（DPM-Solver）。
        2.  **隐状态维度**：测试了 64、256、768 不同维度对生成质量的影响。
        3.  **解码器架构**：对比了线性头与 Transformer 解码器的表现。
*   **客观性**：通过引入“训练集参考”、“恢复训练集”和“解码噪声”作为基准线，客观地展示了模型在流畅度与多样性之间的权衡。

### 6. 主要结论与发现
*   **舍入是关键**：实验证实，使用 Transformer 解码器进行上下文舍入，其标记恢复准确率远高于线性分类器（尤其在低维空间）。
*   **性能领先**：CoDAR 在流畅度上显著优于 LD4LG，且在多样性匹配的情况下，其生成质量可与最先进的离散扩散模型（MDLM/SEDD）竞争。
*   **维度悖论**：在连续扩散中，**较低的隐状态维度（如 64）反而比较高维度（如 768）表现更好**。高维度虽然增加了表示能力，但显著增加了扩散过程的优化难度。
*   **灵活调节**：通过调节解码器的温度（Temperature），CoDAR 可以在极高流畅度（低温度）和极高多样性（高温度）之间平滑切换。

### 7. 优点
*   **理论与实践结合**：不仅提出了方法，还通过数学证明解释了为什么传统方法会失败。
*   **兼容性强**：由于扩散过程完全在连续空间，可以无缝集成图像领域成熟的快速 ODE 求解器（如 DPM-Solver），实现少步数（如 25 步）高质量生成。
*   **解耦设计**：将“生成语义”与“语言实现”分离，使得模型可以使用现成的、非生成式的预训练嵌入模型。

### 8. 不足与局限
*   **推理延迟**：由于引入了自回归解码器，CoDAR 的最后一步离散化过程是逐标记进行的，这在一定程度上牺牲了扩散模型原本具备的并行解码潜力。
*   **维度限制**：实验发现高维嵌入会导致扩散训练困难，这限制了模型利用更强大高维表示的能力。
*   **实验覆盖**：虽然在无条件生成上表现优异，但尚未在条件生成（如摘要、翻译）或长文本建模上进行大规模验证。

（完）
