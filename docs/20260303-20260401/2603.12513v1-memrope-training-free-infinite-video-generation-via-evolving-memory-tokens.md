---
title: "MemRoPE: Training-Free Infinite Video Generation via Evolving Memory Tokens"
title_zh: MemRoPE：通过演化记忆令牌实现无需训练的无限视频生成
authors: "Youngrae Kim, Qixin Hu, C. -C. Jay Kuo, Peter A. Beerel"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12513v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于实时帧流生成的自回归扩散
tldr: 针对自回归视频生成中长序列导致的身份漂移和运动停滞问题，本文提出MemRoPE框架。该方法无需训练，通过“记忆令牌”利用指数移动平均将过去信息压缩为长短期双流缓存，并结合“在线RoPE索引”动态应用位置编码，解决了位置相位冲突。MemRoPE在保持固定缓存大小的同时实现了无限长度的视频生成，显著提升了长视频的视觉保真度和时空一致性。
selection_source: fresh_fetch
motivation: 现有的滑动窗口缓存机制因丢弃历史上下文，导致长视频生成过程中出现身份漂移、画质下降和运动停滞等问题。
method: 提出记忆令牌机制通过EMA压缩历史信息，并配合在线RoPE索引在注意力计算时动态注入位置信息以避免相位冲突。
result: 实验证明MemRoPE在分钟级至小时级的视频生成中，其时空连贯性、视觉保真度和主体一致性均优于现有方法。
conclusion: MemRoPE通过解耦位置编码与高效聚合历史信息，为实现高质量、无限长度的视频生成提供了一种无需训练的有效方案。
---

## 摘要
自回归扩散模型能够实现实时帧流式传输，但现有的滑动窗口缓存会丢弃过去的上下文，导致在长时生成中出现保真度下降、身份漂移和运动停滞。目前的方法保留一组固定的早期令牌作为注意力汇（attention sinks），但这种静态锚点无法反映不断增长的视频中演化的内容。我们引入了 MemRoPE，这是一个包含两个协同设计组件的无需训练的框架。记忆令牌（Memory Tokens）通过指数移动平均将所有过去的键（keys）持续压缩为长短期双流，在固定大小的缓存中同时维持全局身份和近期动态。在线 RoPE 索引（Online RoPE Indexing）缓存未旋转的键，并在注意力计算时动态应用位置嵌入，确保聚合过程不受冲突位置相位的影响。这两种机制相辅相成：位置解耦使时间聚合定义明确，而聚合则使固定大小的缓存对于无限生成变得可行。大量实验验证了 MemRoPE 在分钟级到小时级生成中的时间连贯性、视觉保真度和主体一致性方面均优于现有方法。

## Abstract
Autoregressive diffusion enables real-time frame streaming, yet existing sliding-window caches discard past context, causing fidelity degradation, identity drift, and motion stagnation over long horizons. Current approaches preserve a fixed set of early tokens as attention sinks, but this static anchor cannot reflect the evolving content of a growing video. We introduce MemRoPE, a training-free framework with two co-designed components. Memory Tokens continuously compress all past keys into dual long-term and short-term streams via exponential moving averages, maintaining both global identity and recent dynamics within a fixed-size cache. Online RoPE Indexing caches unrotated keys and applies positional embeddings dynamically at attention time, ensuring the aggregation is free of conflicting positional phases. These two mechanisms are mutually enabling: positional decoupling makes temporal aggregation well-defined, while aggregation makes fixed-size caching viable for unbounded generation. Extensive experiments validate that MemRoPE outperforms existing methods in temporal coherence, visual fidelity, and subject consistency across minute- to hour-scale generation.

---

## 论文详细总结（自动生成）

以下是对论文《MemRoPE: Training-Free Infinite Video Generation via Evolving Memory Tokens》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自回归视频扩散模型在生成长视频（分钟级到小时级）时，面临**身份漂移（Identity Drift）**、**运动停滞**和**画质下降**的问题。
*   **研究背景**：
    *   现有的滑动窗口 KV 缓存机制（FIFO）会丢弃旧帧，导致模型“忘记”初始设定。
    *   虽然引入“注意力汇（Attention Sinks）”保留初始帧能缓解问题，但静态锚点无法适应视频内容的演化。
    *   现有的缓存压缩技术（如 Deep Forcing）存在令牌选择偏见，导致缓存更新时出现明显的视觉跳变。
    *   旋转位置编码（RoPE）在长序列中存在外推难题，且直接对带 RoPE 的键（Key）进行时间聚合会导致相位冲突，破坏表征。

### 2. 方法论
MemRoPE 提出了一种无需训练的框架，包含两个协同设计的核心组件：
*   **演化记忆令牌（Memory Tokens）**：
    *   采用**双流指数移动平均（EMA）**机制：将移出滑动窗口的帧压缩为“长期记忆”（捕捉全局身份）和“短期记忆”（捕捉近期动态）。
    *   公式逻辑：通过不同的衰减率 $\alpha_L$（极小）和 $\alpha_S$（较大）持续更新记忆令牌，确保缓存大小固定且内容平滑演化。
*   **在线 RoPE 索引（Online RoPE Indexing）**：
    *   **位置解耦存储**：在 KV 缓存中存储未经过 RoPE 旋转的原始键向量。这解决了 RoPE 不满足加法分配律的问题，使得 EMA 聚合在数学上变得有效。
    *   **动态索引分配**：仅在注意力计算阶段，根据当前窗口动态分配块相对位置索引并应用 RoPE。这不仅消除了相位冲突，还自然解决了位置外推问题，使索引始终处于训练范围内。
*   **三层缓存结构**：将缓存分为“初始汇（Sink）”、“记忆令牌（Memory）”和“局部窗口（Local）”三部分，兼顾初始一致性、历史演化和近期细节。

### 3. 实验设计
*   **基础模型**：基于 Wan2.1-T2V-1.3B 架构，应用于 Self-Forcing 和 LongLive 两个先进的自回归基座。
*   **数据集与提示词**：使用 MovieGenBench 的提示词，分辨率 480×832，帧率 16 fps。
*   **对比方法**：包括 Deep Forcing、$\infty$-RoPE、CausVid、Rolling Forcing 等主流训练/非训练方法。
*   **评估指标**：
    *   **VBench-Long**：涵盖审美质量、背景一致性、成像质量、运动平滑度、主体一致性和时间闪烁。
    *   **人工评价**：30 名参与者的 2AFC 偏好测试。
    *   **VLM 评估**：利用 Gemini 3.1-Pro 对视频稳定性进行 5 分制评分。
*   **生成时长**：涵盖 30s、60s、120s、240s、480s 乃至 **1 小时**的超长视频生成。

### 4. 资源与算力
*   **硬件环境**：推理实验在单张 **NVIDIA A6000 GPU** 上完成。
*   **算力开销**：
    *   由于是 **Training-Free（无需训练）** 方法，该方案不需要额外的训练算力和时长。
    *   **推理效率**：在 Self-Forcing 基座上，由于 MemRoPE 使用了更紧凑的缓存（12 帧 vs 原 21 帧），推理速度从 3.63 FPS 提升至 5.12 FPS；在 LongLive 上速度基本持平。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 120s 和 240s 时长使用了 128 个提示词进行大规模测试。
    *   针对 1 小时超长生成使用了 10 个随机提示词。
    *   进行了详细的**消融实验**，验证了长期/短期记忆的必要性、EMA 衰减率的敏感度以及“在线 RoPE”对画质的提升。
*   **充分性评价**：实验设计非常充分且客观。通过跨多个基座模型、多种时长、以及自动指标/人工/大模型三方评估，有力证明了方法的鲁棒性和普适性。

### 6. 主要结论与发现
*   **长效一致性**：MemRoPE 在长达 1 小时的生成中，其主体身份和视觉保真度保持得远好于基准方法。
*   **平滑演化**：相比于离散的令牌选择，EMA 机制能实现更平滑的场景过渡，避免了视觉突变。
*   **位置编码的关键作用**：解耦 RoPE 是实现有效记忆聚合的前提，动态索引分配成功解决了长视频的坐标溢出问题。
*   **鲁棒性**：模型对 EMA 超参数不敏感，在较大范围的衰减率下均能保持稳定性能。

### 7. 优点与亮点
*   **无需训练**：作为插件式方案，可直接提升现有自回归模型的能力，极具实用价值。
*   **数学严谨性**：识别并解决了 RoPE 在 KV 聚合中的相位冲突问题，这是相比前人工作的重大改进。
*   **固定内存开销**：无论生成多长的视频，KV 缓存大小始终恒定，支持真正的“无限”生成。
*   **双流设计**：巧妙地平衡了全局身份的稳定性与局部动态的灵敏度。

### 8. 不足与局限
*   **基座依赖**：作为非训练方法，其单帧上限受限于预训练基座模型的质量。
*   **有损压缩**：EMA 聚合本质上是有损的，对于极远距离的特定细节（如 30 分钟前出现的特定小物体）可能无法实现精确检索。
*   **复杂场景限制**：在场景剧烈切换或多主体频繁进出的极复杂叙事中，固定大小的记忆令牌可能面临表征瓶颈。

（完）
