---
title: "HiAR: Efficient Autoregressive Long Video Generation via Hierarchical Denoising"
title_zh: HiAR：通过分层去噪实现高效自回归长视频生成
authors: "Kai Zou, Dian Zheng, Hongbo Liu, Tiankai Hang, Bin Liu, Nenghai Yu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08703v1"
tags: ["query:ardm"]
score: 10.0
evidence: 用于长视频生成的自回归扩散
tldr: 自回归扩散模型在生成长视频时常面临误差累积导致的质量下降。本文提出HiAR，一种分层去噪框架，打破了传统的顺序生成模式。HiAR让所有视频块在每个去噪步骤中同步进行因果生成，使当前块始终基于相同噪声水平的上下文进行预测，从而有效抑制误差传播。该方法支持流水线并行推理，提速1.8倍，并通过引入前向KL正则化解决了蒸馏过程中的低运动问题，在长视频生成质量和一致性上达到领先水平。
selection_source: fresh_fetch
motivation: 传统的自回归视频生成依赖高度去噪的上下文作为条件，这会导致预测误差在长序列中不断累积并放大，造成严重的画质退化。
method: 提出分层去噪框架HiAR，在每个去噪步对所有视频块进行同步因果推理，并引入前向KL正则化以增强运动多样性。
result: 在VBench 20秒长视频生成测试中，HiAR在综合评分和时间漂移指标上均优于现有方法，且推理速度提升了1.8倍。
conclusion: HiAR通过同噪声水平的上下文约束和并行化架构，证明了在保持时间一致性的同时，能有效实现高效且高质量的长视频自回归生成。
---

## 摘要
自回归 (AR) 扩散为生成理论上无限长度的视频提供了一个极具前景的框架。然而，一个主要的挑战是在保持时间连续性的同时，防止由误差累积导致的渐进式质量退化。为了确保连续性，现有方法通常以高度去噪的上下文为条件；然而，这种做法会以高确定性传播预测误差，从而加剧退化。在本文中，我们认为高度干净的上下文并非必要。受双向扩散模型的启发（该模型在保持连贯性的同时以共享的噪声水平对帧进行去噪），我们提出，以与当前块相同噪声水平的上下文为条件，可以为时间一致性提供足够的信号，同时有效缓解误差传播。基于这一见解，我们提出了 HiAR，一种分层去噪框架，它反转了传统的生成顺序：它不是按顺序完成每个块，而是在每个去噪步骤中对所有块执行因果生成，使得每个块始终以相同噪声水平的上下文为条件。这种层级结构自然地支持流水线并行推理，在我们的 4 步设置中实现了 1.8 倍的实际耗时加速。我们进一步观察到，在这种范式下的自展开蒸馏（self-rollout distillation）放大了寻找模式（mode-seeking）的逆 KL 目标中固有的低运动捷径。为了对抗这一点，我们在双向注意力模式中引入了正向 KL 正则化项，它在不干扰蒸馏损失的情况下，为因果推理保留了运动多样性。在 VBench（20 秒生成）上，HiAR 在所有对比方法中获得了最佳的总分和最低的时间漂移。

## Abstract
Autoregressive (AR) diffusion offers a promising framework for generating videos of theoretically infinite length. However, a major challenge is maintaining temporal continuity while preventing the progressive quality degradation caused by error accumulation. To ensure continuity, existing methods typically condition on highly denoised contexts; yet, this practice propagates prediction errors with high certainty, thereby exacerbating degradation. In this paper, we argue that a highly clean context is unnecessary. Drawing inspiration from bidirectional diffusion models, which denoise frames at a shared noise level while maintaining coherence, we propose that conditioning on context at the same noise level as the current block provides sufficient signal for temporal consistency while effectively mitigating error propagation. Building on this insight, we propose HiAR, a hierarchical denoising framework that reverses the conventional generation order: instead of completing each block sequentially, it performs causal generation across all blocks at every denoising step, so that each block is always conditioned on context at the same noise level. This hierarchy naturally admits pipelined parallel inference, yielding a 1.8 wall-clock speedup in our 4-step setting. We further observe that self-rollout distillation under this paradigm amplifies a low-motion shortcut inherent to the mode-seeking reverse-KL objective. To counteract this, we introduce a forward-KL regulariser in bidirectional-attention mode, which preserves motion diversity for causal inference without interfering with the distillation loss. On VBench (20s generation), HiAR achieves the best overall score and the lowest temporal drift among all compared methods.

---

## 论文详细总结（自动生成）

### 论文总结：HiAR —— 通过分层去噪实现高效自回归长视频生成

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）视频扩散模型在生成长视频时，面临**时间连续性**与**质量退化（分布漂移）**之间的矛盾。
*   **研究动机**：现有方法通常在生成当前视频块时，依赖于前一个已完全去噪的“干净”块作为上下文。这种做法虽然保证了连贯性，但会以极高的确定性传播预测误差，导致误差随时间累积，表现为画面过饱和、运动重复或语义漂移。
*   **核心见解**：作者认为“高度干净的上下文”并非必要。受双向扩散模型启发，在相同的噪声水平下提供上下文已足以维持一致性，且能有效抑制误差传播。

#### 2. 核心方法论
*   **分层去噪（Hierarchical Denoising）**：
    *   **从“块优先”到“步优先”**：打破传统的“完成一个块再生成下一个块”的顺序。在每一个去噪步（Step）中，对所有视频块同步进行因果推理。
    *   **噪声匹配上下文**：设定上下文的噪声水平 $t_c$ 等于当前步输出的噪声水平 $t_{j+1}$。这在满足时间因果律的同时，最大限度地减少了误差传播。
*   **流水线并行推理（Pipelined Parallelism）**：
    *   利用分层结构的依赖特性（块 $n$ 在步 $j$ 仅依赖于块 $n-1$ 的步 $j$ 和块 $n$ 的步 $j-1$），在推理时实现跨去噪步的流水线并行，显著提升生成速度。
*   **前向 KL 正则化（Forward-KL Regularizer）**：
    *   **解决低运动捷径**：自回归蒸馏（DMD）常用的逆 KL 目标具有“寻找模式”的特性，容易导致模型生成近乎静止的视频以降低损失。
    *   **双向注意力蒸馏**：在训练中引入基于双向注意力的前向 KL 损失，强制模型覆盖教师模型的运动分布，从而在不干扰因果推理路径的前提下保留运动多样性。

#### 3. 实验设计
*   **数据集与场景**：在 5 秒片段上进行训练，在 20 秒长视频生成上进行评估。
*   **Benchmark**：主要使用 **VBench**（涵盖质量、语义、动态等 16 个维度）以及作者提出的 **Drift Metric（漂移指标）**（通过测量饱和度、清晰度、相似度等随时间的变化率来量化退化）。
*   **对比方法**：
    *   **双向模型**：LTX-Video, Wan2.1-1.3B。
    *   **自回归模型**：NOVA, Pyramid Flow, SkyReels-V2, MAGI-1。
    *   **蒸馏自回归模型**：CausVid, Self-Forcing, Causal Forcing。

#### 4. 资源与算力
*   **模型底座**：使用 Wan2.1-1.3B 作为基础模型，Wan2.1-14B 作为蒸馏的教师模型（Critic）。
*   **训练细节**：
    *   使用 16k 个 ODE 解对进行微调。
    *   训练 20k 步，总 Batch Size 为 64。
    *   **算力说明**：文中未明确给出具体的 GPU 型号和数量（如 A100/H100 的具体个数），但提到了在 4 步去噪设置下实现了约 1.8 倍的实际耗时加速。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量对比（VBench 20s 测试）和定性视觉对比。
*   **消融实验**：
    *   对比了不同的上下文噪声水平（$t_j, t_{j+1}, 0$）。
    *   验证了前向 KL 正则化的不同设计（注意力模式、约束步数 $K$）。
    *   验证了分层去噪和重新训练的必要性。
*   **充分性评价**：实验设计较为全面，特别是针对“长视频漂移”这一痛点引入了专门的量化指标，对比了当前最前沿的多个开源基准，论证具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：HiAR 在 VBench 20s 生成任务中取得了最高总分（0.821）和最低的时间漂移（0.257）。
*   **质量稳定**：相比其他 AR 方法，HiAR 在长达 20 秒的生成中能保持稳定的色彩、清晰度和结构，无明显过饱和现象。
*   **高效推理**：通过流水线并行，吞吐量达到 30 fps，比同类蒸馏 AR 模型快 1.8 倍。
*   **运动保持**：前向 KL 正则化成功解决了蒸馏模型常见的“画面静止”问题，动态得分接近原始双向教师模型。

#### 7. 优点（亮点）
*   **理论优雅**：通过简单的去噪顺序重排，从理论上解决了自回归扩散模型中长期存在的误差传播问题。
*   **架构创新**：分层去噪天然适配流水线并行，在不损失质量的情况下实现了显著的推理加速。
*   **训练稳定**：引入双向注意力模式下的正则化，巧妙地解决了因果推理训练中的模式塌陷问题。

#### 8. 不足与局限
*   **训练成本**：虽然推理高效，但仍需要复杂的自展开蒸馏过程和教师模型引导，训练资源消耗较大。
*   **模型依赖**：实验主要基于 Wan2.1 架构，虽然理论具有通用性，但在其他架构（如基于 VQ 的模型）上的表现尚未验证。
*   **长程语义**：虽然缓解了物理质量的漂移，但对于极长视频中的复杂逻辑一致性（如人物身份在数分钟后的完全一致）仍面临生成模型通有的挑战。

（完）
