---
title: "Helios: Real Real-Time Long Video Generation Model"
title_zh: Helios：真正的实时长视频生成模型
authors: "Shenghai Yuan, Yuanyang Yin, Zongjian Li, Xinwei Huang, Xiao Yang, Li Yuan"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04379v1"
tags: ["query:ardm"]
score: 10.0
evidence: 用于视频生成的14B自回归扩散模型
tldr: Helios是首个14B参数的实时长视频生成模型，在单张H100 GPU上可达19.5 FPS。它采用自回归扩散架构，支持文生视频、图生视频及视频编辑。通过模拟漂移的训练策略和高效的上下文压缩技术，Helios解决了长视频生成的漂移与重复动作问题，且无需复杂的加速手段即可实现分钟级视频生成，在质量和效率上均优于现有模型。
selection_source: fresh_fetch
motivation: 旨在解决长视频生成中的内容漂移、计算开销巨大以及推理速度慢等核心挑战。
method: 提出了一种14B自回归扩散模型，通过压缩历史上下文、减少采样步数及模拟漂移训练来提升效率与稳定性。
result: 在单卡H100上实现了19.5 FPS的生成速度，且在长短视频生成质量上均超越了之前的基准模型。
conclusion: Helios证明了通过架构优化和训练策略改进，可以在不依赖复杂加速技术的情况下实现高质量、实时的长视频生成。
---

## 摘要
我们介绍了 Helios，这是首个 14B 参数规模的视频生成模型，在单张 NVIDIA H100 GPU 上运行速度达到 19.5 FPS，支持分钟级视频生成，同时质量可媲美强基准模型。我们在三个关键维度上取得了突破：(1) 对长视频漂移（drifting）的鲁棒性，且无需使用常用的抗漂移启发式方法，如自强制（self-forcing）、误差库（error-banks）或关键帧采样；(2) 无需 KV 缓存（KV-cache）、稀疏/线性注意力或量化等标准加速技术即可实现实时生成；(3) 无需并行或分片（sharding）框架即可进行训练，在 80 GB GPU 显存内可容纳多达四个 14B 模型，并实现图像扩散规模的批大小（batch sizes）。具体而言，Helios 是一个 14B 参数的自回归扩散模型，具有统一的输入表示，原生支持文本生成视频（T2V）、图像生成视频（I2V）和视频生成视频（V2V）任务。为了缓解长视频生成中的漂移问题，我们刻画了典型的失败模式，并提出了简单而有效的训练策略，在训练期间显式模拟漂移，同时从源头上消除重复运动。为了提高效率，我们对历史和噪声上下文进行了深度压缩，并减少了采样步数，使得计算成本与 1.3B 视频生成模型相当甚至更低。此外，我们引入了基础设施级的优化，在降低显存消耗的同时加速了推理和训练。大量实验表明，Helios 在短视频和长视频生成方面均持续优于先前的方法。我们计划发布代码、基础模型和蒸馏模型，以支持社区的进一步发展。

## Abstract
We introduce Helios, the first 14B video generation model that runs at 19.5 FPS on a single NVIDIA H100 GPU and supports minute-scale generation while matching the quality of a strong baseline. We make breakthroughs along three key dimensions: (1) robustness to long-video drifting without commonly used anti-drifting heuristics such as self-forcing, error-banks, or keyframe sampling; (2) real-time generation without standard acceleration techniques such as KV-cache, sparse/linear attention, or quantization; and (3) training without parallelism or sharding frameworks, enabling image-diffusion-scale batch sizes while fitting up to four 14B models within 80 GB of GPU memory. Specifically, Helios is a 14B autoregressive diffusion model with a unified input representation that natively supports T2V, I2V, and V2V tasks. To mitigate drifting in long-video generation, we characterize typical failure modes and propose simple yet effective training strategies that explicitly simulate drifting during training, while eliminating repetitive motion at its source. For efficiency, we heavily compress the historical and noisy context and reduce the number of sampling steps, yielding computational costs comparable to -- or lower than -- those of 1.3B video generative models. Moreover, we introduce infrastructure-level optimizations that accelerate both inference and training while reducing memory consumption. Extensive experiments demonstrate that Helios consistently outperforms prior methods on both short- and long-video generation. We plan to release the code, base model, and distilled model to support further development by the community.

---

## 论文详细总结（自动生成）

### Helios：真正的实时长视频生成模型论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视频生成模型普遍面临“不可能三角”：难以同时实现**实时生成速度**、**长视频持续性（分钟级）**以及**高质量视觉效果**。
*   **研究动机**：
    *   主流模型（如 Sora 类）生成 5-10 秒视频往往需要数十分钟，远达不到交互式应用的要求。
    *   现有的实时模型多基于 1.3B 小参数量，缺乏复杂运动表达和高频细节。
    *   长视频生成中普遍存在“内容漂移”（Drifting）和重复动作问题，且依赖昂贵的训练策略（如 Self-Forcing）。
    *   推理开销随视频长度增加而剧增，显存占用极高。

#### 2. 核心方法论
Helios 是一个 **14B 参数的自回归扩散 Transformer (DiT)**，其核心技术包括：
*   **统一历史注入（Unified History Injection）**：
    *   **表示控制**：将长视频生成建模为视频续写，输入由历史上下文（$X_{Hist}$）和噪声上下文（$X_{Noisy}$）拼接而成，原生支持 T2V、I2V 和 V2V。
    *   **引导注意力（Guidance Attention）**：引入放大系数（amp）来调节历史信息对当前生成的引导作用，并对历史帧固定时间步为 0（表示清洁数据）。
*   **简易抗漂移策略（Easy Anti-Drifting）**：
    *   **相对 RoPE**：使用相对时间索引而非绝对索引，消除因超出训练长度导致的漂移和重复动作。
    *   **首帧锚点（First-Frame Anchor）**：在生成过程中始终保留第一帧作为全局视觉参考，稳定颜色和身份一致性。
    *   **帧感知损坏（Frame-Aware Corrupt）**：训练时对历史帧施加独立的噪声、曝光和下采样扰动，模拟推理时的误差累积，增强鲁棒性。
*   **深度压缩流（Deep Compression Flow）**：
    *   **多期限记忆补丁化**：对历史帧按距离进行分级压缩（近距离轻度压缩，远距离重度压缩），使 Token 总量保持恒定。
    *   **金字塔统一预测校正器**：采用多尺度采样，早期步骤在低分辨率下运行，后期逐步提升，大幅减少计算量。
*   **对抗分层蒸馏（Adversarial Hierarchical Distillation）**：
    *   基于 DMD 框架，引入纯教师强制（Pure Teacher Forcing）和 GAN 损失，将采样步数从 50 步压缩至 3 步。

#### 3. 实验设计
*   **数据集**：使用 0.8M 条时长小于 10 秒的视频剪辑进行训练。
*   **Benchmark**：构建了 **HeliosBench**，包含 240 个由 LLM 优化的提示词，涵盖四个时长维度：极短（81 帧）、短（240 帧）、中（720 帧）、长（1440 帧）。
*   **对比方法**：对比了超过 20 种模型，包括基础模型（Wan 2.1、CogVideoX、Mochi-1、HunyuanVideo 等）和蒸馏模型（Self-Forcing、Krea、TurboDiffusion 等）。
*   **评估指标**：美学评分、动态性、平滑度、语义对齐、自然度，以及专门针对长视频的“漂移评分”。

#### 4. 资源与算力
*   **硬件**：主要在 NVIDIA H100 GPU 上进行实验。
*   **训练配置**：
    *   **阶段 1 & 2**：使用 64 张 H100，训练步数分别为 13k 和 36k 步。
    *   **阶段 3（蒸馏）**：使用 128 张 H100，训练约 6k 步。
*   **显存优化**：通过异步显存释放、梯度缓存（Cache Grad）和分片 EMA 技术，使得 80GB 显存的单卡可以容纳 4 个 14B 模型进行训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖 20+ 基准模型）和定性展示。
*   **消融实验**：非常充分，针对引导注意力、首帧锚点、帧感知损坏、压缩策略、蒸馏组件等 12 个关键变量进行了详细的对比实验。
*   **客观性**：引入了双盲人类偏好研究（User Study），收集了 200 份有效反馈，证明了模型在视觉质量和长视频稳定性上的优势。

#### 6. 主要结论与发现
*   **性能突破**：Helios 是首个在单张 H100 上达到 **19.5 FPS** 的 14B 视频模型，速度比同规模模型快 50 倍以上。
*   **长视频能力**：通过简单的模拟漂移训练，无需复杂的启发式算法即可生成分钟级（1440 帧+）的连贯视频。
*   **效率优势**：通过 Token 压缩，14B 模型的计算开销被降低到了 1.3B 模型的水平，甚至更低。
*   **任务统一**：单一架构即可完美兼顾文生视频、图生视频及交互式视频编辑。

#### 7. 优点（亮点）
*   **极致效率**：在不使用 KV-cache、量化或稀疏注意力的情况下实现实时性，极具工程参考价值。
*   **训练创新**：提出了无需并行框架即可训练 14B 模型的基础设施级优化方案。
*   **鲁棒性强**：通过“首帧锚点”和“帧感知损坏”解决了困扰自回归生成已久的颜色漂移和伪影累积问题。
*   **开源贡献**：承诺发布代码、基础模型和蒸馏模型，并提供了长视频评估基准 HeliosBench。

#### 8. 不足与局限
*   **分辨率限制**：受限于资源，目前实验最高分辨率为 384x640，尚未探索更高清（如 1080P）的实时生成。
*   **接缝闪烁**：尽管优于前人，但在自回归生成的段落衔接处仍可能存在轻微的闪烁（Flickering）。
*   **评估指标局限**：现有的自动化视频评估指标（如美学评分）与人类真实感知之间仍存在偏差，难以完全体现模型的生成优势。

（完）
