---
title: Reinforcement Learning for Diffusion LLMs with Entropy-Guided Step Selection and Stepwise Advantages
title_zh: 结合熵引导步骤选择与逐步优势的扩散大语言模型强化学习
authors: "Vishnu Teja Kunde, Fatemeh Doudi, Mahdi Farahbakhsh, Dileep Kalathil, Krishna Narayanan, Jean-Francois Chamberland"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12554v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型 (DLM) 与序列生成的强化学习
tldr: 针对扩散语言模型（DLM）在强化学习中面临的序列似然计算难题，本文提出了一种基于有限步马尔可夫决策过程（MDP）的新框架。通过推导无偏策略梯度并引入熵引导的步长选择与单步去噪奖励估计，该方法有效解决了去噪轨迹中的优势估计问题。实验证明，该方法在代码生成、逻辑推理和数学任务上达到了SOTA水平，显著优于现有的DLM强化学习后训练方法。
selection_source: fresh_fetch
motivation: 现有的扩散语言模型强化学习方法依赖于近似似然或启发式方法，难以准确捕捉去噪过程的序列结构且存在偏差。
method: 将去噪过程建模为有限步MDP，利用熵引导的近似界限选择更新步，并结合单步去噪奖励计算中间优势。
result: 在代码、逻辑推理和数学基准测试中取得了最先进的性能，超越了现有的DLM强化学习后训练技术。
conclusion: 该研究为扩散语言模型的强化学习提供了一个理论完备且计算高效的框架，证明了去噪轨迹建模在提升模型推理能力方面的潜力。
---

## 摘要
强化学习 (RL) 在自回归 (AR) 语言模型的训练后阶段非常有效，但由于序列级似然难以计算，将这些方法扩展到扩散语言模型 (DLMs) 具有挑战性。因此，现有方法依赖于代理似然或启发式近似，这可能会引入偏差并掩盖去噪的序列结构。我们将基于扩散的序列生成建模为去噪轨迹上的有限时界马尔可夫决策过程，并推导出一个精确、无偏的策略梯度，该梯度在去噪步骤上进行分解，并以中间优势的形式表示，无需显式评估序列似然。为了获得实用且计算高效的估计器，我们：(i) 通过熵引导的近似界限选择用于策略更新的去噪步骤；(ii) 利用扩散模型自然提供的单步去噪奖励来估计中间优势，从而避免了昂贵的多步展开。在代码编写和逻辑推理基准测试上的实验证明了该方法达到了最先进的水平，在数学推理方面也具有极强的竞争力，优于现有的 DLMs 强化学习训练后方法。代码可在 https://github.com/vishnutez/egspo-dllm-rl 获取。

## Abstract
Reinforcement learning (RL) has been effective for post-training autoregressive (AR) language models, but extending these methods to diffusion language models (DLMs) is challenging due to intractable sequence-level likelihoods. Existing approaches therefore rely on surrogate likelihoods or heuristic approximations, which can introduce bias and obscure the sequential structure of denoising. We formulate diffusion-based sequence generation as a finite-horizon Markov decision process over the denoising trajectory and derive an exact, unbiased policy gradient that decomposes over denoising steps and is expressed in terms of intermediate advantages, without requiring explicit evaluation of the sequence likelihood. To obtain a practical and compute-efficient estimator, we (i) select denoising steps for policy updates via an entropy-guided approximation bound, and (ii) estimate intermediate advantages using a one-step denoising reward naturally provided by the diffusion model, avoiding costly multi-step rollouts. Experiments on coding and logical reasoning benchmarks demonstrate state-of-the-art results, with strong competitive performance on mathematical reasoning, outperforming existing RL post-training approaches for DLMs. Code is available at https://github.com/vishnutez/egspo-dllm-rl.

---

## 论文详细总结（自动生成）

以下是对论文《Reinforcement Learning for Diffusion LLMs with Entropy-Guided Step Selection and Stepwise Advantages》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：自回归（AR）语言模型通过强化学习（RL）在推理任务上取得了巨大成功，但扩散语言模型（DLM）由于其非因果的并行生成特性，难以直接套用 AR 模型的 RL 方法。
*   **核心问题**：DLM 的序列级似然函数（Likelihood）计算极其复杂且不可导，导致现有的 DLM 强化学习方法通常依赖于有偏的代理目标或启发式近似，忽略了去噪轨迹中的序列结构和信用分配（Credit Assignment）问题。
*   **研究意义**：本文旨在为 DLM 构建一个理论完备且计算高效的强化学习框架，通过显式建模去噪过程来提升模型的逻辑推理和代码生成能力。

### 2. 论文提出的方法论
论文提出了 **EGSPO-SA**（结合逐步优势的熵引导步长策略优化）框架，核心包含三个部分：
*   **扩散 MDP 建模**：将掩码扩散生成过程形式化为一个有限时界的马尔可夫决策过程（MDP）。状态为当前的掩码序列，动作为下一步的去噪结果。基于此推导出了一个**精确且无偏的策略梯度定理**，该定理将全局奖励分解为各个去噪步骤的“逐步优势”（Stepwise Advantages）。
*   **熵引导的步骤选择（EGSPO）**：由于扩散过程通常包含数百步，对每一步计算梯度开销巨大。论文证明了策略梯度误差的上界与未选中步骤的熵之和成正比。因此，算法优先选择**信息熵最高（即模型最不确定）的 $K$ 个步骤**进行参数更新，从而在固定计算预算下最大化学习效率。
*   **逐步优势估计（SA）**：为了避免昂贵的多步采样（Rollout），利用 DLM 的特性，从中间状态进行“单步贪婪解码”得到一个预测序列，并以此计算中间奖励作为基准（Baseline），从而实现更精准的步骤级信用分配。

### 3. 实验设计
*   **基准模型**：LLaDA-8B-Instruct（一种先进的掩码扩散语言模型）。
*   **数据集与场景**：
    *   **数学推理**：GSM8K, MATH500。
    *   **逻辑推理**：4x4 Sudoku（数独）, Countdown（数字运算游戏）。
    *   **代码生成**：MBPP, HumanEval。
*   **对比方法**：包括现有的 DLM 强化学习前沿方法：d1、d2、wd1、SPG 以及原始的 LLaDA 基础模型。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 LoRA（Rank=128）进行高效微调。
    *   有效 Batch Size 为 96 个 Prompt。
    *   训练时长：代码任务约 4k 步，数学任务约 6k 步，逻辑任务约 10k 步。
    *   去噪步数：训练时设为 128 步，每步更新选取的 $K=8$。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大类推理任务（数学、逻辑、代码），每类任务均在多个主流基准数据集上进行了测试。
*   **消融实验**：针对“熵引导选择”与“随机/均匀选择”进行了对比，并分析了选中步骤在整个扩散轨迹中的分布情况。
*   **充分性评价**：实验设计较为充分，不仅对比了最终准确率，还详细对比了训练效率（FLOPs vs. Reward）、样本效率和收敛速度。对比基线涵盖了 2025 年最新的同类研究，具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：EGSPO-SA 在逻辑推理（Sudoku, Countdown）和代码生成（MBPP, HumanEval）上达到了 **SOTA（最先进）** 水平。
*   **效率优势**：相比于之前的 d1 方法，EGSPO-SA 在达到相同奖励水平时所需的 FLOPs 和样本量显著更少。
*   **信用分配的重要性**：实验表明，在逻辑严密的任务（如数独）中，步骤级的优势估计（SA）比单纯的序列级奖励更能引导模型学习正确的推理路径。
*   **熵的指导作用**：模型在去噪轨迹的中间阶段通常表现出最高的不确定性，针对这些步骤进行优化比均匀优化所有步骤更有效。

### 7. 优点
*   **理论严谨**：从 MDP 出发推导无偏梯度，为 DLM 的强化学习提供了坚实的数学基础。
*   **计算高效**：通过熵引导选择更新步骤，巧妙解决了扩散模型训练计算量过大的痛点。
*   **原生支持**：充分利用了扩散模型自身的单步预测能力来估计优势，无需额外训练复杂的价值网络（Value Network）。

### 8. 不足与局限
*   **超参数敏感性**：逐步优势估计中的权重参数 $\lambda$ 需要针对不同任务进行调整（如数学任务需动态衰减），增加了调参成本。
*   **早期步骤偏差**：在去噪轨迹的极早期（掩码比例极高时），单步贪婪解码生成的序列质量较差，可能导致优势估计存在偏差。
*   **任务覆盖**：虽然在推理任务上表现优异，但在长文本生成或创意写作等非验证性奖励（Non-verifiable Reward）任务上的表现尚未验证。

（完）
