---
title: "Skip to the Good Part: Representation Structure & Inference-Time Layer Skipping in Diffusion vs. Autoregressive LLMs"
title_zh: 直奔主题：扩散模型与自回归大语言模型中的表征结构与推理时层跳过
authors: "Raghavv Goel, Risheek Garrepalli, Sudhanshu Agrawal, Chris Lott, Mingu Lee, Fatih Porikli"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07475v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散与自回归大语言模型内部表示的对比研究
tldr: "本研究对比了扩散语言模型（dLLM）与自回归（AR）模型的内部表示结构。研究发现，dLLM 具有更强的层次化抽象和早期层冗余，而 AR 模型表示则紧密耦合且高度依赖深度。基于此发现，作者提出一种无需架构修改的静态跳层推理方法。实验表明，原生 dLLM 在减少 18.75% 计算量的情况下仍能保持 90% 以上的推理和代码生成性能，显著优于 AR 模型，为提升模型推理效率提供了新路径。"
selection_source: fresh_fetch
motivation: 探究扩散训练目标与自回归目标在塑造模型内部表示结构上的本质差异，以及这些差异如何影响推理效率。
method: 通过对不同初始化背景的模型进行层级和标记维度的表示分析，并设计了一种无需架构调整的静态推理时跳层方案。
result: "原生 dLLM 在减少 18.75% FLOPs 的同时保留了超过 90% 的性能，而 AR 模型在相同跳层配置下性能严重退化。"
conclusion: 训练目标决定了表示的冗余度，dLLM 特有的层次化结构使其在不改变架构的情况下比 AR 模型具有更大的推理加速潜力。
---

## 摘要
自回归（AR）语言模型通过自左向右的预测增量式地构建表征，而扩散语言模型（dLLM）则通过全序列去噪进行训练。尽管最近的 dLLM 在性能上已能与 AR 模型媲美，但扩散目标是否从根本上重塑了跨深度的内部表征仍不清楚。我们首次进行了层级和标记级的表征分析，对比了原生 dLLM（LLaDA）、原生 AR 模型（Qwen2.5）以及 AR 初始化的 dLLM（Dream-7B）。我们发现，扩散目标导致了不同的、更具层次性的抽象，具有显著的早期层冗余并减少了近因偏差，而 AR 目标则产生了紧密耦合且依赖深度的表征。关键在于，尽管经过了扩散训练，AR 初始化的 dLLM 仍保留了类 AR 的表征动态，揭示了持久的初始化偏差。利用观察到的这种表征冗余，我们提出了一种静态的、与任务无关的推理时层跳过方法，该方法无需更改架构或共享 KV 缓存。原生 dLLM 在推理和代码生成基准测试中，在保留 90% 以上性能的同时，实现了高达 18.75% 的 FLOPs 减少，而 AR 模型在同等跳过比例下性能急剧下降。这些结果将训练目标与表征结构联系起来，并实现了实用的、与缓存正交的效率提升。

## Abstract
Autoregressive (AR) language models form representations incrementally through left-to-right prediction, whereas diffusion language models (dLLMs) are trained via full-sequence denoising. Although recent dLLMs match AR performance, it remains unclear whether diffusion objectives fundamentally reshape internal representations across depth. We perform the first layer- and token-wise representational analysis comparing native dLLMs (LLaDA), native AR models (Qwen2.5), and AR-initialized dLLMs (Dream-7B). We find that diffusion objectives result in different, more hierarchical abstractions with substantial early-layer redundancy and reduced recency bias, while AR objectives produce tightly coupled, depth-dependent representations. Critically, AR-initialized dLLMs retain AR-like representational dynamics despite diffusion training, revealing persistent initialization bias. Leveraging this observed representational redundancy, we introduce a static, task-agnostic inference-time layer-skipping method requiring no architectural changes or KV-cache sharing. Native dLLMs achieve up to 18.75% FLOPs reduction while preserving over 90% performance on reasoning and code generation benchmarks, whereas AR models degrade sharply under comparable skipping. These results link training objectives to representational structure and enable practical, cache-orthogonal efficiency gains.

---

## 论文详细总结（自动生成）

这是一份关于论文《Skip to the Good Part: Representation Structure & Inference-Time Layer Skipping in Diffusion vs. Autoregressive LLMs》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散语言模型（dLLM）与传统的自回归（AR）语言模型在训练目标上存在本质差异（全序列去噪 vs. 自左向右预测），这种差异是否导致了它们内部表征结构的不同？
*   **研究动机**：
    *   尽管 dLLM 在性能上已追平 AR 模型，但其内部特征抽象和整合过程尚不明确。
    *   探索是否可以利用模型内部的表征冗余，在不改变架构、不重新训练的情况下，通过“跳过”某些层来提升推理效率。
    *   研究初始化权重（如从 AR 模型初始化）对扩散模型最终表征结构的持久影响。

### 2. 方法论
*   **表征相似性分析**：
    *   使用**余弦相似度**衡量相邻层（$h_l$ 与 $h_{l+1}$）之间以及不同标记（Token）位置之间的表示变化。
    *   分析不同去噪步数（$t$）下 dLLM 的表征演变。
*   **静态跳层策略（Layer-Skipping）**：
    *   **核心思想**：识别相似度极高（余弦相似度 $> 0.95$）的“冗余层”并在推理时直接跳过，将前一层的输出直接传给下一层。
    *   **算法流程**：
        1.  在离线状态下，通过少量样本计算各层的平均相似度。
        2.  根据相似度从高到低排序，选择前 $k$ 个候选层。
        3.  **约束条件**：不允许跳过相邻层，以确保表征的连续性和残差结构的稳定性。
        4.  推理时，被选中的层（Transformer Block）被旁路化，仅保留残差连接。
*   **正交性**：该方法与 KV 缓存优化、并行解码等现有加速技术互补，无需修改模型权重。

### 3. 实验设计
*   **对比模型**：
    *   **原生 dLLM**：LLaDA (8B)。
    *   **原生 AR 模型**：Qwen2.5 (7B)。
    *   **AR 初始化的 dLLM**：Dream-7B（基于 Qwen2.5 权重进行扩散训练）。
*   **基准测试（Benchmarks）**：
    *   **数学推理**：GSM8K、MATH-500。
    *   **代码生成**：HumanEval、MBPP。
*   **评估指标**：性能保留率（相对于全深度模型的准确率）、FLOPs 减少比例、等效生成率。

### 4. 资源与算力
*   **算力说明**：论文未详细列出训练这些模型所消耗的具体 GPU 小时数，因为研究主要基于已发布的开源模型权重进行推理分析。
*   **推理评估**：实验在标准 AI 推理环境下完成，重点在于衡量 FLOPs（浮点运算量）的理论减少值（例如跳过 6 层对应约 18.75% 的计算量削减）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同演化路径的模型家族，并在 4 个主流硬核任务基准上进行了测试。
*   **消融实验**：对比了“允许连续跳层”与“禁止连续跳层”的效果，验证了算法中约束条件的必要性。
*   **分析维度**：不仅做了层级分析，还深入到了标记级（Token-wise）和去噪步数维度的分析，实验设计全面且逻辑严密。
*   **客观性**：通过对比 Dream-7B 和 Qwen2.5，客观地揭示了初始化偏差（Initialization Bias）的存在。

### 6. 主要结论与发现
*   **表征结构差异**：
    *   **dLLM（原生）**：呈现明显的“粗到精”层次化结构，早期层存在大量冗余，且几乎没有“近因偏差”（Recency Bias），表现出全局抽象能力。
    *   **AR 模型**：表征随深度增量更新，层与层之间耦合紧密，具有强烈的近因偏差（即新生成的 Token 会剧烈改变表征）。
*   **初始化偏差**：Dream-7B 虽然使用了扩散目标训练，但其表征模式仍高度接近其 AR 祖先（Qwen），说明 AR 预训练的结构印记极难被后续训练抹去。
*   **跳层鲁棒性**：原生 dLLM 对跳层极其鲁棒，跳过 6 层仍能保持 >90% 的性能；而 AR 模型和 AR 初始化的 dLLM 在跳过 2 层时性能就会大幅崩溃。

### 7. 优点
*   **揭示本质**：首次从表征动力学角度解释了扩散模型与自回归模型在处理序列信息时的内在区别。
*   **实用性强**：提出的跳层方法是“零成本”的，不需要微调，不需要改变 KV 缓存机制，直接实现推理加速。
*   **洞察深刻**：指出了“初始化”比“训练目标”在某些情况下更能决定模型的内部几何结构。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B-8B 规模的模型，尚未验证在超大规模（如 70B+）模型上的表现。
*   **静态策略**：目前采用的是静态跳层，未实现根据输入难度动态调整跳层数量的“动态早期退出”。
*   **任务覆盖**：虽然涵盖了逻辑性强的数学和代码任务，但对于长文本摘要、创意写作等任务的表征冗余研究较少。

（完）
