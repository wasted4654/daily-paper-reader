---
title: Fractal Autoregressive Depth Estimation with Continuous Token Diffusion
title_zh: 基于连续 Token 扩散的分形自回归深度估计
authors: "Jinchang Zhang, Xinrou Kang, Guoyu Lu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14702v1"
tags: ["query:ardm"]
score: 9.0
evidence: 分形视觉自回归扩散框架
tldr: 本研究针对单目深度估计中自回归生成的效率与稳定性问题，提出了一种分形视觉自回归扩散框架。该框架将深度估计重构为由粗到细的跨尺度自回归生成过程，利用VCFR模块融合多尺度特征，并结合条件去噪扩散损失在连续空间建模深度分布，有效避免了离散化误差。通过分形递归架构提升计算效率，并引入不确定性感知聚合方案增强推理稳定性，在多个基准测试中展现了卓越性能。
selection_source: fresh_fetch
motivation: 旨在解决单目深度估计中自回归模型存在的跨模态差异、像素级生成效率低下以及连续深度预测不稳定的挑战。
method: 提出一种分形视觉自回归扩散框架，通过递归重用基础单元实现由粗到细的生成，并结合连续扩散损失和不确定性感知聚合。
result: 在标准基准测试上取得了强劲的性能表现，验证了分形架构和连续扩散建模在深度估计中的有效性。
conclusion: 该方法通过结合自回归生成与扩散模型，为高效、高精度的连续深度估计提供了一种创新的分形递归解决方案。
---

## 摘要
单目深度估计可以从自回归（AR）生成中获益，但直接的 AR 建模受到 RGB 与深度之间的模态差异、低效的逐像素生成以及连续深度预测不稳定性等因素的阻碍。我们提出了一种分形视觉自回归扩散框架，将深度估计重新表述为一个从粗到精、下一尺度的自回归生成过程。VCFR 模块将多尺度图像特征与当前的深度预测融合，以改善跨模态条件约束，而条件去噪扩散损失直接在连续空间中对深度分布进行建模，并减轻了由离散量化引起的误差。为了提高计算效率，我们将按尺度划分的生成器组织成一个分形递归架构，在自相似层次结构中重用基础视觉 AR 单元。我们进一步引入了一种用于多样本推理的不确定性感知鲁棒共识聚合方案，以提高融合稳定性并提供实用的逐像素可靠性估计。在标准基准测试上的实验证明了强大的性能，并验证了所提设计的有效性。

## Abstract
Monocular depth estimation can benefit from autoregressive (AR) generation, but direct AR modeling is hindered by the modality gap between RGB and depth, inefficient pixel-wise generation, and instability in continuous depth prediction. We propose a Fractal Visual Autoregressive Diffusion framework that reformulates depth estimation as a coarse-to-fine, next-scale autoregressive generation process. A VCFR module fuses multi-scale image features with current depth predictions to improve cross-modal conditioning, while a conditional denoising diffusion loss models depth distributions directly in continuous space and mitigates errors caused by discrete quantization. To improve computational efficiency, we organize the scale-wise generators into a fractal recursive architecture, reusing a base visual AR unit in a self-similar hierarchy. We further introduce an uncertainty-aware robust consensus aggregation scheme for multi-sample inference to improve fusion stability and provide a practical pixel-wise reliability estimate. Experiments on standard benchmarks demonstrate strong performance and validate the effectiveness of the proposed design.

---

## 论文详细总结（自动生成）

### 论文总结：基于连续 Token 扩散的分形自回归深度估计

#### 1. 核心问题与研究动机
单目深度估计（MDE）旨在从单张 RGB 图像预测像素级深度。虽然自回归（AR）模型在序列建模上表现强劲，但直接应用于深度估计面临三大挑战：
*   **模态不匹配**：RGB 外观与深度几何结构之间难以建立准确的结构对应关系。
*   **推理效率低下**：传统的逐像素生成方式导致串行推理成本极高。
*   **预测不稳定**：连续深度值的离散化量化误差以及生成过程中的误差累积会破坏预测的可靠性。

#### 2. 方法论
论文提出了一种**分形视觉自回归扩散框架（Fractal VAR Diffusion）**，核心思想是将深度估计重构为“由粗到细”的跨尺度自回归生成过程：
*   **分形递归架构**：将不同尺度的生成器抽象为可重用的基础自回归单元，构建自相似的递归层次结构（从 $g_4$ 到 $g_1$）。这种设计通过模块重用缓解了参数增长，并使推理路径更加高效。
*   **视觉条件特征细化（VCFR）**：在每个尺度上，通过 VCFR 模块融合多尺度图像特征与当前深度预测，构建“视觉-深度联合 Token”，增强跨模态结构对齐。
*   **连续 Token 扩散损失**：放弃了传统的离散 Token 量化，转而在连续潜空间（Continuous Latent Space）中使用条件去噪扩散损失建模深度分布。这避免了量化误差，并能更好地处理局部几何歧义。
*   **不确定性感知鲁棒共识聚合（URCA）**：在推理阶段，通过多次采样并利用鲁棒优化算法进行融合，同时输出像素级的“不确定性代理图”，作为预测可靠性的指标。

#### 3. 实验设计
*   **数据集**：在户外场景基准 **KITTI** 和室内场景基准 **NYUv2** 上进行了评估。
*   **对比方法**：涵盖了监督学习回归方法（如 AdaBins, PixelFormer, VA-DepthNet）、基于扩散模型的方法（如 Marigold, DiffusionDepth, Lotus）以及自回归基准（如 DAR-Base）。
*   **评价指标**：使用了 AbsRel（绝对相对误差）、RMSE（均方根误差）、$\delta < 1.25$（阈值精度）等标准指标。

#### 4. 资源与算力
*   **训练细节**：使用了 AdamW 优化器，基础学习率为 $5 \times 10^{-5}$，采用线性预热和余弦衰减策略。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。文中仅提到全局 Batch Size 为 32，并给出了推理时的 MACs（132G）和在 1024x320 分辨率下的运行时间（64ms）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了详尽的定量对比，并提供了丰富的定性可视化结果。
*   **消融实验**：针对 AR vs VAR、扩散损失、分形结构、VCFR 模块以及推理采样次数 $N$ 做了多组消融实验。
*   **充分性评价**：实验设计较为全面，涵盖了从架构设计到损失函数选择的各个方面。通过与最新 SOTA 方法（如 Depth Anything, ZoeDepth）的对比，验证了其在精度和效率上的平衡，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：在 KITTI 上实现了 1.712 的 RMSE，在 NYUv2 上实现了 0.197 的 RMSE，均优于现有的监督学习和生成式方法。
*   **效率优势**：相比于 Marigold 等纯扩散模型（推理需数秒），该方法在保持高精度的同时，推理速度大幅提升（仅需 64ms）。
*   **连续建模的必要性**：实验证明，在连续空间建模深度分布比离散化 Token 预测能更有效地保留精细几何细节，减少阶梯效应。

#### 7. 优点与亮点
*   **架构创新**：引入分形递归思想，实现了模型参数的高效重用和跨尺度的一致性建模。
*   **理论融合**：巧妙地结合了自回归模型的跨尺度依赖建模能力与扩散模型的连续分布表达能力。
*   **实用性强**：URCA 模块不仅提升了稳定性，还提供了无需额外训练的不确定性估计，对下游任务（如机器人导航）极具价值。

#### 8. 不足与局限
*   **硬件细节缺失**：缺乏具体的算力消耗记录，不便于其他研究者评估训练成本。
*   **推理延迟**：虽然比纯扩散模型快，但相比于轻量级回归模型（如 Monodepth2 的 33ms），其 64ms 的延迟在极高性能要求的实时场景中仍有优化空间。
*   **超参数敏感性**：多样本聚合（URCA）的效果依赖于采样次数 $N$ 的选择，在性能提升与计算开销之间存在权衡。

（完）
