---
title: "Autoregressive vs. Masked Diffusion Language Models: A Controlled Comparison"
title_zh: 自回归与掩码扩散语言模型：受控对比研究
authors: Caio Vicentino
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22075v1"
tags: ["query:ardm"]
score: 10.0
evidence: 自回归与掩码扩散语言模型的受控对比研究
tldr: 本研究对自回归（AR）与掩码扩散（MDLM）语言模型进行了严格的受控对比实验。在相同数据、算力和硬件条件下，研究发现两者训练吞吐量相当。AR模型收敛更快但易过拟合且生成内容高度重复；MDLM收敛较慢但生成内容更具多样性，尽管在语法连贯性上略逊一筹。该研究揭示了两种范式在多样性与流畅度之间的权衡，并开源了所有代码和模型权重。
selection_source: fresh_fetch
motivation: 旨在通过受控实验消除变量干扰，公平对比自回归与掩码扩散两种语言模型生成范式的性能差异。
method: 在相同的TinyStories数据集和计算预算下训练AR与MDLM模型，并从吞吐量、收敛性及生成样本多样性等维度进行定量分析。
result: AR模型收敛快但生成内容极度重复，而MDLM虽然收敛较慢且存在少量语法问题，但在叙事多样性上显著优于AR。
conclusion: 研究证明了两种范式存在结构性的多样性与流畅度权衡，为未来选择或改进语言模型生成机制提供了实证依据。
---

## 摘要
我们对自回归（AR）和掩码扩散（MDLM）语言模型进行了受控实证对比。两种模型均在相同的数据（来自 TinyStories 的 5000 万个 token）、相同的计算预算（20,000 步，批大小 32，序列长度 512）以及相同的硬件（NVIDIA H100 80GB）上进行训练，从而将生成范式作为唯一变量进行隔离。我们报告了三项发现。首先，两种范式实现了相当的训练吞吐量（约 5 万 token/秒），MDLM 仅多消耗了 4.7% 的实际运行时间。其次，AR 收敛更快，并在第 14,000 步开始出现过拟合，而 MDLM 收敛较慢，在第 20,000 步时仍在改进，这表明两者具有不同的计算最优训练机制。第三，对 1,000 个生成样本进行的定量多样性分析揭示了结构性的多样性-流畅度权衡：AR 生成的输出流畅但具有重复性（99.8% 以相同的单词开头），而 MDLM 生成的叙述更具多样性（93.4% 的 5 词开头是唯一的，具有更高的 Distinct-n 和更低的 Self-BLEU），代价是偶尔会出现语法不一致。所有代码、训练好的检查点和数据流水线均已发布，以供复现。

## Abstract
We present a controlled empirical comparison between autoregressive (AR) and masked diffusion (MDLM) language models. Both models are trained on identical data (50M tokens from TinyStories), identical compute budget (20,000 steps, batch size 32, sequence length 512), and identical hardware (NVIDIA H100 80GB), isolating the generation paradigm as the sole variable. We report three findings. First, both paradigms achieve comparable training throughput (~50K tokens/second), with MDLM requiring only 4.7% more wall-clock time. Second, AR converges faster and begins overfitting by step 14,000, while MDLM converges more slowly and is still improving at step 20,000, suggesting different compute-optimal training regimes. Third, quantitative diversity analysis over 1,000 generated samples reveals a structural diversity-fluency trade-off: AR produces fluent but repetitive outputs (99.8% begin with the same word), while MDLM generates more diverse narratives (93.4% unique 5-word openings, higher Distinct-n, lower Self-BLEU), at the cost of occasional grammatical inconsistencies. All code, trained checkpoints, and data pipelines are released for reproducibility.

---

## 论文详细总结（自动生成）

以下是对论文《Autoregressive vs. Masked Diffusion Language Models: A Controlled Comparison》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管自回归（AR）模型是目前语言建模的主流，但其存在无法修改已生成内容、生成过程受限于左向右偏置以及容易产生重复输出等局限。新兴的掩码扩散语言模型（MDLM）展现了潜力，但现有研究往往在不同规模、数据或计算预算下进行对比，导致难以区分性能差异是源于“生成范式”还是“实验变量”。
*   **核心问题**：在完全相同的训练数据、计算预算和硬件条件下，AR 和 MDLM 在训练效率、收敛动态和生成质量（多样性与流畅度）方面究竟有何本质差异？

### 2. 方法论
*   **核心思想**：通过控制变量法，将生成范式（自回归 vs. 迭代去噪）作为唯一变量进行隔离对比。
*   **关键技术细节**：
    *   **AR 模型**：标准的 Decoder-only Transformer，采用因果注意力（Causal Attention）和交叉熵损失进行下一词预测。
    *   **MDLM 模型**：采用双向 Transformer 架构。训练时根据余弦调度（Cosine Schedule）对输入进行随机掩码，模型预测被掩码位置的原始 token。
    *   **MDLM 生成算法**：从全掩码序列开始，在 100 个步骤内迭代去噪。每步选择置信度最高的 token 进行取消掩码，并结合温度退火（Temperature Annealing）和重复惩罚（Repetition Penalty）来优化输出。

### 3. 实验设计
*   **数据集**：TinyStories（50M tokens），这是一个专门用于评估小型语言模型连贯性的儿童故事数据集。
*   **对比基准**：AR Transformer vs. MDLM Transformer。
*   **评估指标**：
    *   **训练效率**：吞吐量（tokens/sec）、实际运行时间。
    *   **收敛性**：验证集损失曲线（注意：两者损失函数目标不同，仅对比趋势）。
    *   **生成多样性**：Distinct-n（唯一 n-gram 比例）、Self-BLEU（样本间相似度）、开头词唯一性。

### 4. 资源与算力
*   **硬件**：单台 NVIDIA H100 80GB GPU。
*   **训练时长**：
    *   AR 模型：107.9 分钟（20,000 步）。
    *   MDLM 模型：113.0 分钟（20,000 步）。
*   **参数规模**：AR 为 123.6M，MDLM 为 162.7M（MDLM 略大是因为增加了时间步条件嵌入模块）。

### 5. 实验数量与充分性
*   **实验规模**：属于小规模受控实验（1.2亿-1.6亿参数）。
*   **充分性与公平性**：
    *   **公平性**：在步数、批大小、序列长度、优化器和硬件上实现了高度统一，是目前该领域最严苛的受控对比之一。
    *   **局限性**：仅使用了单一数据集（TinyStories）和单一随机种子，且 MDLM 在 20,000 步时仍未完全收敛，实验的广度（如多任务评估）尚有提升空间。

### 6. 主要结论与发现
*   **吞吐量近乎持平**：MDLM 的训练速度仅比 AR 慢 4.7%，打破了“扩散模型训练成本极高”的固有认知。
*   **收敛机制不同**：AR 收敛极快但在 14,000 步左右开始过拟合；MDLM 收敛较慢但持续改进，显示出更好的正则化特性。
*   **多样性-流畅度权衡**：
    *   **AR**：极度流畅但结构僵化，99.8% 的样本以同一个词（“Once”）开头，存在严重的“前缀模式坍塌”。
    *   **MDLM**：结构多样性极高（93.4% 的样本有唯一的 5 词开头），能从故事中间开始叙述，但偶尔会出现语法错误。

### 7. 优点
*   **严谨的受控变量**：成功隔离了架构带来的干扰，为理解两种范式的本质区别提供了基准。
*   **揭示结构性差异**：定量证明了扩散模型在解决自回归模型“开头重复”问题上的天然优势。
*   **完全开源**：提供了代码、数据流水线和模型权重，极大地便利了社区复现。

### 8. 不足与局限
*   **参数量不完全对等**：由于 MDLM 必须包含时间步嵌入，其参数量比 AR 多出约 31.6%，这可能对收敛速度和容量产生一定影响。
*   **规模效应未知**：实验仅在亿级参数和 50M token 规模下进行，结论是否能推广至百亿级（Billion-scale）模型仍需验证。
*   **评估维度单一**：缺乏下游任务（如问答、摘要、代码生成）的定量评估，主要集中在开放式文本生成。

（完）
