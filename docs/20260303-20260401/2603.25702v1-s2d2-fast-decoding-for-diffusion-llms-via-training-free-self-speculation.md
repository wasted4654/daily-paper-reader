---
title: "S2D2: Fast Decoding for Diffusion LLMs via Training-Free Self-Speculation"
title_zh: S2D2：通过免训练自投机实现扩散大语言模型的快速解码
authors: "Ligong Han, Hao Wang, Han Gao, Kai Xu, Akash Srivastava"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25702v1"
tags: ["query:ardm"]
score: 9.0
evidence: 结合分块自回归解码与并行去噪
tldr: 针对块扩散语言模型在少步数解码时质量与速度难以平衡的问题，本文提出S2D2框架。该方法利用块扩散模型在块大小为1时可作为自回归验证器的特性，实现无需额外训练的自投机解码。通过轻量级路由策略动态决定验证时机，S2D2在保持或提升生成质量的同时，显著加快了推理速度，为扩散模型的高效生成提供了新方案。
selection_source: fresh_fetch
motivation: 现有的块扩散模型解码策略在追求加速时往往导致质量下降，且改进方法通常需要昂贵的重训练或额外计算资源。
method: 利用同一预训练模型同时充当生成器（并行扩散）和验证器（自回归模式），通过自投机验证步骤优化解码轨迹。
result: 在多个主流模型上实现了显著的性能提升，相比自回归解码最高加速4.7倍，且在速度提升的同时改善了生成准确率。
conclusion: S2D2通过训练无关的自投机机制，有效解决了扩散语言模型的解码瓶颈，是实现快速且高质量生成的有力工具。
---

## 摘要
块扩散语言模型（Block-diffusion language models）通过结合块级自回归解码与块内并行去噪，为实现快于自回归生成的生成方式提供了一条极具前景的路径。然而，在实际加速所需的少步数（few-step）场景下，标准的置信度阈值解码往往表现脆弱：激进的阈值会损害生成质量，而保守的阈值则需要不必要的去噪步骤。现有解决该问题的方法要么需要额外的训练，要么会产生额外的测试时计算开销。我们提出了 S2D2，一种用于块扩散语言模型的免训练自投机解码框架。我们的核心观察是，当块大小减小为 1 时，块扩散模型会退化为自回归模型，从而允许同一个预训练模型同时充当草稿器（drafter）和验证器（verifier）。S2D2 在标准的块扩散解码中插入了一个投机验证步骤，并使用轻量级路由策略来决定何时验证是值得的。这产生了一种混合解码轨迹，其中扩散模式并行地提出 token，而自回归模式则充当局部序列级评论器。在三个主流的块扩散模型系列中，S2D2 在准确率与速度的权衡上始终优于强置信度阈值基线。在 SDAR 上，我们观察到相比自回归解码最高实现了 4.7 倍的加速，相比调优后的动态解码基线最高实现了 1.57 倍的加速，同时准确率提升了多达 4.5 个百分点。在 LLaDA2.1-Mini 上，S2D2 与内置的自校正功能保持互补，在一种保守设置下，其速度比静态基线快 4.4 倍，且准确率略高。

## Abstract
Block-diffusion language models offer a promising path toward faster-than-autoregressive generation by combining block-wise autoregressive decoding with within-block parallel denoising. However, in the few-step regime needed for practical acceleration, standard confidence-thresholded decoding is often brittle: aggressive thresholds hurt quality, while conservative thresholds require unnecessary denoising steps. Existing approaches that address this issue either require additional training or incur extra test-time compute. We present S2D2, a training-free self-speculative decoding framework for block-diffusion language models. Our key observation is that a block-diffusion model becomes autoregressive when the block size is reduced to one, allowing the same pretrained model to act as both drafter and verifier. S2D2 inserts a speculative verification step into standard block-diffusion decoding and uses lightweight routing policies to decide when verification is worth its cost. This yields a hybrid decoding trajectory in which diffusion proposes tokens in parallel, while the autoregressive mode acts as a local sequence-level critic. Across three mainstream block-diffusion families, S2D2 consistently improves the accuracy-speed tradeoff over strong confidence-thresholding baselines. On SDAR, we observe up to $4.7\times$ speedup over autoregressive decoding, and up to $1.57\times$ over a tuned dynamic decoding baseline while improving accuracy by up to $4.5$ points. On LLaDA2.1-Mini, S2D2 remains complementary to built-in self-correction, including a conservative setting where it is $4.4\times$ faster than the static baseline with slightly higher accuracy.

---

## 论文详细总结（自动生成）

### S2D2：通过免训练自投机实现扩散大语言模型的快速解码 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **研究背景**：自回归（AR）模型虽然强大，但推理速度受限于逐个 token 生成的模式。扩散大语言模型（Diffusion LLMs）通过块级（block-wise）并行去噪提供了加速潜力。
*   **核心问题**：在实际应用所需的“少步数（few-step）”解码场景下，现有的块扩散模型依赖置信度阈值来决定接受哪些 token。这种方式非常脆弱：阈值过高会导致生成步数过多（变慢），阈值过低则会积累错误（质量下降）。
*   **研究动机**：现有的改进方案通常需要额外的模型训练或显著的测试时计算开销。作者希望探索一种**免训练（training-free）**的方法，利用模型自身的结构来实现更高效、更准确的解码。

#### 2. 方法论：S2D2 框架
*   **核心思想**：作者观察到，块扩散模型在块大小（Block Size）设为 1 时，其行为等同于自回归模型。因此，可以利用**同一个预训练模型**同时扮演两个角色：
    *   **草稿器（Drafter）**：使用标准的块扩散模式并行提出多个 token 建议。
    *   **验证器（Verifier）**：切换到块大小为 1 的自回归模式，对草稿进行序列级的验证。
*   **关键技术细节**：
    *   **自验证模式（Self-Verification）**：对于位置对齐的模型（如 LLaDA），使用“2L trick”构造特殊的注意力掩码，在一次前向传播中并行计算出草稿中所有 token 的自回归概率。
    *   **投机拒绝采样（Speculative Rejection Sampling）**：不再简单地使用置信度阈值，而是根据验证器与草稿器的概率比率进行拒绝采样，确保局部序列的逻辑一致性。
    *   **轻量级路由策略（Routing Policies）**：由于验证步骤会增加一次额外的前向传播开销，S2D2 引入了路由机制（如最小跨度策略、得分阈值策略、迟滞策略等）来动态决定当前步骤是否值得进行验证。
    *   **残差重采样（Residual Resampling）**：当验证失败时，从残差分布中重新采样，修正错误并终止当前投机段。

#### 3. 实验设计
*   **实验对象**：涵盖了三个主流块扩散模型家族的 5 个模型：
    *   **SDAR 系列**（1.7B, 4B, 8B）
    *   **Fast-dLLM v2**
    *   **LLaDA 2.1-Mini**
*   **评测任务（Benchmarks）**：
    *   数学推理：**GSM8K**
    *   代码生成：**MBPP**、**HumanEval**
    *   指令遵循：**IFEval**
*   **对比基线（Baselines）**：
    *   标准的自回归解码（AR）。
    *   标准的块扩散解码（BD3），包括静态阈值和动态阈值（Dynamic Thresholding）策略。

#### 4. 资源与算力
*   **训练算力**：论文明确指出该方法是**免训练（Training-free）**的，因此不需要额外的训练算力。
*   **推理算力**：论文未详细列出具体的 GPU 型号和数量（通常此类论文在 A100 或 H100 上测试），但重点通过**加速比（Speedup）**和**端到端墙钟时间（Wall-clock time）**来衡量效率提升。

#### 5. 实验数量与充分性
*   **实验规模**：作者在 5 个不同规模和架构的模型上进行了测试，涵盖了数学、代码和通用指令等多种任务，实验设计较为全面。
*   **消融实验**：非常充分。作者对不同的路由策略（Routing Policies）、接受长度估计器（Estimators）、拒绝采样比率调节（Tempering）等进行了详细的消融研究。
*   **客观性**：通过展示“准确率-速度”前沿曲线（Frontier Curves），客观地证明了 S2D2 在相同速度下比基线更准，或在相同准确率下比基线更快。

#### 6. 主要结论与发现
*   **性能提升**：在 SDAR-1.7B 上，S2D2 相比自回归解码实现了 **4.7 倍**的加速，且在比动态解码基线快 1.57 倍的同时，准确率提升了 4.5 个点。
*   **鲁棒性**：S2D2 解决了标准扩散解码在大块大小（Large Block Size）下不稳定的问题，显著改善了推理时间的缩放特性（Inference-time scaling）。
*   **互补性**：在 LLaDA 2.1 上，S2D2 可以与模型内置的“令牌编辑（Token Editing）”自校正机制协同工作，进一步提升性能。
*   **理论关联**：分析表明，S2D2 可以被视为一种随机贪婪的**自回归能量修正**机制，通过局部修正来弥补扩散模型在序列依赖建模上的不足。

#### 7. 优点与亮点
*   **即插即用**：无需任何微调、蒸馏或架构修改，直接适用于现有的块扩散模型。
*   **双重增益**：罕见地实现了**速度和准确率的双重提升**，打破了传统解码中“快则不准”的权衡。
*   **设计精巧**：巧妙利用了块扩散模型在 $B=1$ 时的退化特性，将自投机概念引入扩散领域。
*   **路由智能**：通过轻量级路由避免了盲目验证带来的计算浪费。

#### 8. 不足与局限
*   **局部验证限制**：目前的实现仅验证“第一个连续的掩码跨度（first contiguous masked span）”，对于块内非连续的预测缺乏全局验证。
*   **超参数依赖**：路由策略引入了一些新的超参数（如阈值 $\tau$、成本系数 $c$），在不同模型或任务上可能需要微调以达到最优平衡。
*   **架构要求**：虽然适用于主流块扩散模型，但对于不支持 KV 缓存或非块状解码的纯扩散模型可能无法直接应用。
*   **KV 缓存开销**：虽然复用了缓存，但验证过程中的注意力掩码切换可能在某些推理框架下存在实现复杂度。

（完）
