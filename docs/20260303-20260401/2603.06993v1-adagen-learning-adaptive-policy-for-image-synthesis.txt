Title: AdaGen: Learning Adaptive Policy for Image Synthesis

URL Source: https://arxiv.org/pdf/2603.06993v1

Published Time: Tue, 10 Mar 2026 00:25:51 GMT

Number of Pages: 22

Markdown Content:
> 1

# AdaGen: Learning Adaptive Policy for Image Synthesis 

Zanlin Ni ∗, Yulin Wang ∗, Yeguo Hua, Renping Zhou, Jiayi Guo, Jun Song, Bo Zheng, Gao Huang †

Abstract —Recent advances in image synthesis have been pro-pelled by powerful generative models, such as Masked Gen-erative Transformers (MaskGIT), autoregressive models, diffu-sion models, and rectified flow models. A common principle behind their success is the decomposition of complex synthesis tasks into multiple tractable steps. However, this introduces a proliferation of step-specific parameters to be configured for modulating the iterative generation process ( e.g ., mask ratio, noise level, or temperature at each step). Existing approaches typically rely on manually-designed scheduling rules to manage this complexity, demanding expert knowledge and extensive trial-and-error. Furthermore, these static schedules lack the flexibility to adapt to the unique characteristics of each individual sample, yielding sub-optimal performance. To address this issue, we present AdaGen, a general , learnable , and sample-adaptive frame-work for scheduling the iterative generation process. Specifically, we formulate the scheduling problem as a Markov Decision Process, where a lightweight policy network is introduced to adaptively determine the most suitable parameters given the current generation state, and can be trained through reinforce-ment learning. Importantly, we demonstrate that simple reward designs, such as FID or pre-trained reward models, can be easily hacked and may not reliably guarantee the desired quality or diversity of generated samples. Therefore, we propose an adversarial reward design to guide the training of the policy networks effectively. Finally, we introduce an inference-time refinement strategy and a controllable fidelity-diversity trade-off mechanism to further enhance the performance and flexibility of AdaGen. Comprehensive experiments across five benchmark datasets (ImageNet-256 ×256 & 512 ×512, MS-COCO, CC3M, and LAION-5B) and four distinct generative paradigms validate the superiority of AdaGen. For example, AdaGen achieves better performance on DiT-XL with ∼ 3× lower inference cost and improves the FID of VAR from 1.92 to 1.59 with negligible additional computational overhead. Code and pre-trained models are available at https://github.com/LeapLabTHU/AdaGen. 

Index Terms —Adaptive image generation, reinforcement learn-ing, iterative generation models 

I. I NTRODUCTION 

Over the past few years, image synthesis has experienced dramatic advancements through the emergence of powerful generative models. Language-inspired frameworks such as Masked Generative Transformers (MaskGIT) [1], [2], [17], [18] and autoregressive models [5], [6], [19], [20] concep-tualize image generation as a form of token modeling, either by predicting masked tokens or by sequentially sampling one token at a time. Concurrently, generative paradigms like dif-fusion models [9], [11], [21] and rectified flow methods [13], 

Z. Ni, Y. Wang, Y. Hua, R. Zhou, J. Guo, and G. Huang are with the Department of Automation, BNRist, Tsinghua University, Beijing, China. J. Song and B. Zheng are with Taobao & Tmall Group of Alibaba. Email: nzl22@mails.tsinghua.edu.cn; gaohuang@tsinghua.edu.cn 

> ∗

. Equal contribution. †. Corresponding author. ×𝑻 Generator 

> (learned via RL)
> generate
> pre -defined, static

Existing Works AdaGen 

Generator ×𝑻 

Policies 

Samples 

PolicyNet  

> learnable, adaptive
> generate

Fig. 1. Main idea of AdaGen. Existing multi-step generative models, such as diffusion models, typically utilize pre-defined, static schedules to configure the generation policy ( e.g ., noise level) of all samples. Instead, AdaGen leverages reinforcement learning (RL) to train a policy network that directly learns the optimal generation policies adaptively tailored for each sample. 

[22] formulate generation as an iterative denoising process guided by learned score or velocity functions. These diverse modeling paradigms have all demonstrated impressive scala-bility and the ability to produce high-fidelity visual content. Despite different underlying mechanisms, these methodolo-gies share a key principle: decomposing the complex task of photorealistic image generation into a sequence of more man-ageable sub-problems. For instance, MaskGIT only decodes a subset of masked tokens per step, while diffusion models iteratively remove small amounts of noise from the sample. While such multi-step strategies have proven instrumental in their success, they also create a significant challenge: a prolif-eration of step-conditional parameters, which we collectively term the generation policy , must be properly configured to ensure their effectiveness. For example, at each step, diffusion models need to determine an appropriate noise reduction level, while MaskGIT requires proper calibration of masked token proportions. As the number of steps increases, the configuration complexity may quickly become unmanageable. Existing approaches tackle this challenge by pre-defining multiple scheduling functions (see Table I), which require expert knowledge and may still fail to yield optimal results. Moreover, a globally shared policy may lack the flexibility to adapt to the unique characteristics of individual samples. To address these limitations, this paper presents AdaGen. As illustrated in Figure 1, our key insight is to consider a 

learnable policy network to automatically configure the proper generation policy adaptively tailored for each sample. By treating policy design as a data-driven optimization problem rather than a manual art, our approach removes the burden                                                                                                                                                                                

> arXiv:2603.06993v1 [cs.CV] 7 Mar 2026 2
> TABLE I SUMMARY OF GENERATION POLICIES FOR DIFFERENT GENERATION PARADIGMS
> Type Generation Policy Range Scheduling Rules MaskGIT Mask ratio, mtmt∈[0 ,1] mt= cos(0 .5π·t/T )[1], [2], 2 arccos( t/T )/π [3], [4] Sampling temperature, τtτt≥0τt= 1 [1], [2], 0.5 + 0 .8×(1 −t/T )[4] Masking temperature, ζtζt≥0ζt=C·(1 −t/T )[1], [2] Guidance scale, wtwt≥0wt=C·t/T [1], [2], C(1 −cos π(t/T )C′
> )/2[4] Autoregressive Sampling temperature, τtτt≥0τt=C[5], [6] Guidance scale, wtwt≥0wt=C[6], C·t/T [5] Top-kparameter, ktkt∈N+kt=C[5], [6] Top-pparameter, ρtρt∈[0 ,1] ρt=C[5], [6] Diffusion (ODE) Timestep, κtκt∈ { 0, . . . , κ max }κt=⌊(1 −t/T )·κmax ⌋[7], [8], ⌊(1 −t/T )2·κmax ⌋[7], [8] Guidance scale, wtwt≥0wt=C[9]–[11], C(1 −cos π(κt/κ max )C′
> )/2[12] Rectified Flow Timestep, κtκt∈[0 ,1] κt= 1 −t/T [13] Guidance scale, wtwt≥0wt=C[13]
> For each paradigm, we list the policy names, their value ranges, and the common settings adopted. We use t∈ { 1, . . . , T }as a unified index representing the current generation step. For diffusion models, we focus on ODE-based diffusion sampling (denoted as Diffusion (ODE)) following recent works [7], [8], [14], [15]. We note that one should distinguish the generation step tfrom the policy parameter timestep κt. The latter arises in specific paradigms such as diffusion and rectified flow models, where it serves as a discretization point for numerical ODE solvers and implicitly controls the noise level at the t-th generation step [15]. This timestep parameter is conventionally denoted by ti[8], [15], [16]. To avoid confusion with generation step t, we use κtinstead. C, C ′are constants.

of expert scheduling and enables per-sample customization. However, training this policy network is challenging as end-to-end backpropagation through the entire multi-step generation process is computationally infeasible. To overcome this, we formulate the determination of the optimal generation policy as a Markov decision process (MDP). Within this formulation, the policy network naturally becomes an agent that observes the current generation state, adaptively decides the policy to maximize final quality, and can be trained through reinforce-ment learning ( e.g. , policy gradient). Another challenge in our problem lies in designing effec-tive reward signals. We find that straightforward approaches, such as employing standard metrics ( e.g ., FID) or pre-trained reward models [23], often lead to unintended outcomes. More specifically, although the expected value of rewards can be successfully maximized, the resulting policies usually guide the generation process to produce insufficiently high-fidelity and diverse images (see Figure 3). In other words, the policy network tends to “overfit” these rewards. Inspired by this phenomenon, we hypothesize that a potential solution is to consider a reward that is updated concurrently with the policy network and adjusted dynamically to resist overfitting .Therefore, we propose an adversarial reward model, which is formulated as a discriminator similar to that used in generative adversarial networks (GANs) [24]. When the policy network learns to maximize the reward, we refine the reward model simultaneously to better distinguish between real and gener-ated samples. In this way, the policy network is effectively prevented from overfitting a static objective, and we observe a balanced diversity and fidelity of the generated images. The training pipeline of AdaGen is illustrated in Figure 2. Beyond the core framework, we additionally introduce two enhancements to AdaGen. First , we propose an inference-time refinement strategy for further generation quality im-provement. Specifically, we find that the auxiliary networks in AdaGen ( e.g ., the adversarial reward model), though orig-inally discarded after training, can be repurposed as off-the-shelf perceptual evaluators. Leveraging their predictions, we design refinement mechanisms that guide sampling toward higher-quality outputs, without requiring additional training or external models (Table IX, Table X). Second , we enable con-trollable trade-offs between fidelity and diversity in AdaGen. Specifically, we introduce a fidelity-oriented policy network and blend its output with the original policy via a scalar λ. In the meantime, we blend our adversarial reward and a fidelity-centric reward using the same λ for the reward signal. This dual-interpolation design explicitly bridges the control param-eter λ with the fidelity-diversity spectrum, thereby offering a user-controllable modulation of generation behavior. The effectiveness of AdaGen is comprehensively validated across five benchmark datasets: ImageNet 256 ×256 [25], ImageNet 512 ×512 [25], MS-COCO [26], CC3M [27], and LAION-5B [28]. We demonstrate the generality of AdaGen by applying it to five representative generative models spanning four distinct paradigms: MaskGIT [1], DiT [11] (diffusion), SiT [13] (rectified flow), VAR [5] (autoregressive), and Sta-ble Diffusion [21]. AdaGen achieves significant computa-tional efficiency and performance improvements across diverse paradigms, delivering 17% to 54% improvements in generation performance or 1.6 × to 3.6 × reductions in inference cost when maintaining comparable generation performance. This paper is an extended version of our conference paper AdaNAT [29]. We make several important new contributions:  

> •

We extend the original AdaNAT to a more general and flexible framework, AdaGen. AdaGen extends the idea of learning adaptive generation policies beyond MaskGIT, encompassing major multi-step generative paradigms such as diffusion, autoregressive, and rectified flow mod-els (Section III-C), and achieves competitive performance across them. Meanwhile, we ensure more stable policy optimization as generation steps increase by proposing an action smoothing technique (Section III-E).  

> •

We introduce an off-the-shelf inference-time refinement strategy for further generation quality enhancement (Sec-tion III-F1). Specifically, we find that the auxiliary networks in AdaGen can be effectively repurposed as 3

perceptual evaluators. Guided by their predictions, we develop a refinement mechanism that enhances generation quality without requiring retraining or external models.  

> •

We enable explicit control over the fidelity-diversity trade-off in AdaGen (Section III-F2), by introducing a fidelity-centric policy network and blending its output with our original policy via a user-controllable scalar.  

> •

We conduct extensive experiments demonstrating Ada-Gen’s efficacy across four generative paradigms and large-scale settings, including 2B-parameter VAR [5] and Stable Diffusion [21] on LAION-5B [28] (Ta-bles III, IV, VIII). We also validate the feasibility of our newly proposed inference-time refinement and fidelity-diversity control mechanisms (Tables IX, X, Figure 13).  

> •

We provide more comprehensive analyses of AdaGen covering computational overhead, hyperparameter sensi-tivity, and extensive ablation studies on our key compo-nents (Tables XII, XIII, XIV, XV). II. R ELATED WORK 

Iterative Generation Models have now largely replaced traditional Generative Adversarial Networks (GANs) [24], [30], [31] as the dominant approach for high-quality image synthesis, due to their superior scalability [11], [20], [32], [33] and sample diversity [9], [32], [34]. Broadly speaking, these models can be categorized into two main families: discrete token-based and continuous-state approaches. Token-based models, inspired by advances in language modeling, represent images as sequences of discrete visual tokens and generate them either autoregressively [5], [19], [20], [35], predicting each token conditioned on previous ones, or through masked modeling strategies [1], [2], [17], [18], where subsets of tokens are iteratively predicted while others are held fixed. Continuous-state models, such as diffusion models, iteratively refine a noisy input in pixel or latent space by reversing a noise process [9], [11], [21], [32], while rectified flow models use deterministic velocity fields for faster, more stable sam-pling [13], [22]. Despite their differences, these frameworks share the goal of approximating complex data distributions through incremental refinements. Our work builds on this by introducing a lightweight policy network that adaptively predicts per-step generation policies, improving generative model output quality without tuning the model itself. 

Reinforcement Learning in Image Generation. The inte-gration of reinforcement learning (RL) for image generation began with early works like [36], which explored policy-based approaches to structured prediction tasks. Recently, ImageRe-ward [23] collected a large-scale human preference dataset to train a reward model, establishing a framework for quantifying image quality based on human aesthetic judgments. This pre-trained reward model has spurred research on using RL to fine-tune diffusion-based image generation models [37]–[39], where the reward signal guides the generative model toward synthesizing images with certain desired properties. Unlike these methods, which optimize the generative model itself through parameter updates, our work employs an RL agent to enhance a frozen generative model without modifying its internal parameters. This formulation shifts the focus from generator optimization to generation control, and obviates the need for costly retraining or fine-tuning of the base generator. 

Generative Adversarial Networks and RL. Several works have used ideas from reinforcement learning to train GANs [40]–[43] for text generation, information retrieval, point cloud completion, etc . Recently, SFT-PG [44] combines RL with a GAN objective for diffusion-based image genera-tion. Our work differs from these works in many important aspects. First, the research problem is orthogonal. We are interested in exploring a better generation policy given apre-trained generator backbone (which is kept unchanged throughout our method), while previous studies investigate the alternative “RL+GAN” approach to directly train or fine-tune the generator backbone itself. Second, our method is general and applicable to various iterative generation models, while previous works focus on improving specific generative paradigms, such as diffusion models. Third, compared to SFT-PG [44], we conduct comparative analyses on different reward designs and large-scale experiments while they mainly explored the GAN-based objective and proved their concept on relatively simple datasets ( e.g ., CIFAR [45] and CelebA [46]). 

Proper Scheduling for Generative Models. Due to the step-dependent nature of iterative models’ generation policies, designing proper sampling schedules has emerged as a critical factor for effective generation. Traditionally, these schedules are set heuristically, relying on expert intuition or extensive trial-and-error. For example, MaskGIT [1] compared several mask ratio schedules before settling on a cosine schedule, while MDT [12] crafted a power-cosine schedule for classifier-free guidance. However, such manual approaches are labor-intensive and may fail to yield the most suitable policies. To address this, recent works have explored ways to automatically obtain these schedules via differentiating through sample qual-ity [47], evolutionary search [48], numerical gradients [49], and predictor-based search [50]. Despite these advancements, these automatic scheduling methods exhibit certain limita-tions. First, they typically employ static objectives ( e.g ., stan-dard evaluation metrics [51], [52]), a practice which, as we identify in Section III-D, can be susceptible to overfitting issues. Second, these methods are often tailored to specific generative paradigms, predominantly diffusion models, leav-ing other iterative models underexplored. Third, the learned generation policies are globally shared, ignoring individual sample characteristics. In contrast, our proposed adversarial learning paradigm is designed to mitigate reward overfitting. Moreover, our framework is general and applicable to various iterative generation models, and our adaptive policy network furnishes sample-specific policies, enabling generated outputs that exhibit both enhanced quality and greater diversity. III. M ETHOD 

This section presents the AdaGen methodology. We begin by reviewing four generative paradigms and their policies in Section III-A, followed by our motivation in Section III-B. We then introduce our core MDP framework in Section III-C and reward design in Section III-D. Section III-E presents a 4

technique for stabilizing policy network training with larger generation steps, while Section III-F details additional en-hancements including inference-time refinement and a blend-ing mechanism for balancing fidelity and diversity. Details of the experiments in this section are available in Appendix A. 

A. Preliminaries 

We briefly review the four mainstream paradigms for gen-erative modeling presented in Table I. All paradigms aim to model a target data distribution p(x) through a parametric model pθ with learnable parameters θ.

Masked Generative Transformers (MaskGIT) [1], [2] learn to generate samples using a masked token modeling ob-jective [53]. Once trained, the model can predict masked tokens based on their surrounding unmasked context, enabling iterative and parallel token generation. During inference, the process begins with a fully masked token sequence x0. At each generation step t, the model takes the partially masked sequence xt as input, predicts all masked tokens in parallel, and then selectively re-masks tokens with low confidence: 

pθ (xt+1 |xt) = P 

> ˆxt+1

ppred  

> θ

( ˆ xt+1 |xt) · premask  

> θ

(xt+1 | ˆxt+1 ).This iterative process continues until all tokens are decoded. Typically, their generation policy consists of four compo-nents [1], [2]: the ratio of tokens to be masked for the next step 

mt, the temperature for token sampling τt and token masking 

ζt, and the classifier-free guidance scale wt.

Autoregressive Models [5], [6] generate data through se-quential conditional modeling, where each generation step depends on all previously generated content: pθ (x) =QTt=1 pθ (xt|x<t ). Here, x<t denotes all information gener-ated prior to step t, while xt represents the autoregressive data unit generated at step t, which is typically a single token. Recently, VAR [5] shows that treating “scale” as the autoregressive unit and performing next-scale prediction offers a more natural approach for visual generation. Typically, their generation policy consists of four compo-nents [5], [6]: the temperature τt for token sampling, the classifier-free guidance scale wt, the top-k filtering parameter 

kt, and the top-p nucleus sampling parameter ρt.

Diffusion Models [11], [54] learn to generate data by re-versing a gradual noising process. The noise process starts from real data x0, noise is progressively added to obtain 

xt1 via q(xt|x0) = N (xt; √¯αtx0, (1 − ¯αt)I), where ¯αt

are pre-defined hyperparameters. The model is trained to approximate the reverse, denoising process: pθ (xt−1|xt) = 

N (xt−1; μθ (xt, t ), σ 2 

> t

I), typically using the following loss function when parameterizing μθ as a noise-prediction net-work ϵθ : Lsimple (θ) = ∥ϵθ (xt) − ϵ∥22. To generate a sample, the process starts from pure noise xtmax ∼ N (0 , I) and iter-atively samples xt−1 ∼ pθ (xt−1|xt) to recover data. Recent work [7], [8], [14], [15] shows that ODE-based sampling for diffusion models greatly speeds up generation with little loss in quality. We follow this approach in our method.    

> 1Here we slightly overload the notation tto follow the conventional notation in diffusion models. This should be distinguished from the generation step t
> in our main text, which indexes the sequential generation iterations.

Typically, their generation policy consists of two compo-nents [8], [15]: the timestep κt for solving the ODE, and the classifier-free guidance scale wt, which modifies the predicted noise as ϵθ,w t (xt) = (1 + wt)ϵθ (xt) − wtϵuncond  

> θ

(xt).

Rectified Flow Models [13], [22] generate samples by progressively transforming a prior distribution, typically a standard normal, into the target data distribution through a continuous flow. The forward process is defined as a linear interpolation between a noise sample x1 ∼ N (0 , I) and a real data sample x0: xt = tx1 + (1 − t)x0, t ∈ [0 , 1] . The model learns a velocity field vθ that predicts how the sample evolves along this trajectory: min θ Et, x0,x1 vθ (xt, t ) − (x1 − x0) 22.Starting from x1 ∼ N (0 , I) (corresponding to t = 1 ), samples are generated by integrating the learned velocity field via an ordinary differential equation: dxt 

> dt

= vθ (xt, t ), t : 1 → 0.Similar to ODE-based diffusion sampling, the generation policy of rectified flow models also consists of two com-ponents [13]: the timestep κt for solving the ODE, and the classifier-free guidance scale wt for a modified velocity prediction function vθ,w t .

B. Motivation 

Existing Policy : η(t) pre-defined , static (1) 

AdaGen Policy : ηϕ(st) learnable , adaptive (2) 

Motivation I: Automatic Policy Acquisition. Modern gener-ative paradigms sequentially decomposes the complex genera-tion process into temporally discretized steps, which inherently introduces temporal dependency in their parameterization. Moreover, as discussed in Section III-A, these generative mod-els typically require more than one type of generation policy to control different aspects of the generation dynamics. This dual challenge of temporal dependency and policy diversity combinatorially amplifies configuration complexity. For exam-ple, even with a moderate T = 32 generation steps, MaskGIT would require 128 policy parameters to be configured, making it difficult for practitioners to fully exploit these models in realistic scenarios. Existing approaches mainly alleviate this problem by leveraging multiple pre-defined scheduling rules (collectively denoted as η(t), detailed in Table I), which can be either manually designed scheduling functions or simply fixed values across all generation steps. However, manual designs require substantial expert knowledge and effort, while over-simplified strategies may fail to capture the nuanced needs across generation steps. To address this issue, we propose to consider a learnable policy network ηϕ that is trained to produce the appropriate policy automatically. As a result, a significantly better generation policy can be acquired with minimal human effort once a proper learning algorithm for 

ηϕ is utilized (which will be discussed in Section III-C). 

Motivation II: Adaptive Policy Adjustment. Furthermore, it is noteworthy that Eq. (1) is a static formulation, i.e ., the generation of all samples shares the same set of scheduling functions. In contrast, we argue that every sample has its own characteristics, and ideally the generation process should be adaptively adjusted according to each individual sample. To attain this goal, we propose to dynamically determine 5                                                                       

> TABLE II UNIFIED MDP F ORMULATION FOR GENERATIVE MODELS
> Type State stAction atTransition Distribution P(st+1 |st,at)Reward R(st,at)
> MaskGIT  t, xt
>  mt, τ t, ζ t, w t
> 
> δ(t+ 1) , p θ(xt+1 |xt;at)
> 
> I(t=T)·r(x)
> Autoregressive  t, x1: t
>  τt, w t, k t, ρ t
> 
> δ(t+ 1) , p θ(x1: t+1 |x1: t;at)
> 
> Diffusion (ODE)  t, xκt
>  κt+1 , w t
> 
> δ(t+ 1) , δ  ODE (xκt, κ t+1 , ϵ θ,w t)
> Rectified Flow  t, xκt
>  κt+1 , w t
> 
> δ(t+ 1) , δ  ODE (xκt, κ t+1 , v θ,w t)
> Here, δ(·)is the Dirac delta function that is nonzero only at ·, representing a deterministic transition. ODE (·)encapsulates the numerical ODE solver step, while ϵθ,w tand
> vθ,w tare the noise and velocity prediction models respectively, guided by classifier-free guidance of scale wt(see Sec. III-A). The reward definition I(t=T)·r(x)indicates that rewards are only provided at the terminal step T, where r(·)(see Sec. III-D) evaluates the quality of the final generated image xextracted from the terminal state sT.

the policy for tth generation step conditioned on the current generation status st (detailed in Table II). In other words, st

provides necessary information on how the generated sample ‘looks like’ at tth step, based on which a tailored policy can be derived to enhance the generation quality. More evidence to support our idea can be found in Table XI and Figure 15. Driven by the discussions above, we propose to introduce a policy network ηϕ that directly learns to produce the appropriate policy in an adaptive manner, as shown in Eq. (2). In the following, we will introduce how to train ηϕ effectively. 

C. Unified MDP Formulation 

Formally, given a pre-trained generative model parameter-ized by θ and a policy network ηϕ to be trained, our objective is to maximize the expected quality of the generated images: maximize  

> ϕ

J(ϕ) = Ex∼pθ (x|ηϕ)[r(x)] , (3) where r(·) is a function quantifying image quality, as detailed in Section III-D, and pθ (x | ηϕ) denotes the distribution of the images generated by the generative model θ, under the generation policy specified by the policy network ηϕ.It is challenging to solve problem (3) with straightfor-ward gradient-based optimization for three reasons. First, the function r(·) that quantifies image quality is not necessarily differentiable. Second, certain generative paradigms, such as MaskGIT and autoregressive models, inherently involve dis-crete operations ( e.g ., token sampling) that block gradient flow during backpropagation. Third, even with a fully differentiable reward function and generation process, the iterative nature of modern generative models would render full-sequence gradient backpropagation prohibitively expensive. 

Unified MDP Formulation for Generative Models. To address this issue, we propose to reformulate the determination of the proper generation policy as a Markov decision process (MDP). Under this framework, the policy network ηϕ can be naturally defined as an agent that observes the generation status and takes actions to configure the policy, which can then be optimized via reinforcement learning. Our formulation, summarized in Table II, systematically characterizes diverse generative paradigms while preserving their distinctive char-acteristics. Formally, we define a T -horizon MDP structure 

(S, A, P, R ) where:  

> •

S denotes the state space. Each state st ∈ S comprises dual components essential for adaptive policy decisions: 1) the current generation step t, signaling the generation progress in the finite T -horizon trajectory, and 2) the intermediate generation result at step t. For MaskGIT, the intermediate result is the partially masked token sequence 

xt. For autoregressive models, the intermediate result is the partial generation x1: t. For diffusion and rectified flow models, the intermediate result is the partially denoised sample xκt at the ODE timestep κt. 

> •

A denotes the action space. Each action at ∈ A is defined as the specific generation policy required when transitioning from step t to step t + 1 , which are already elaborated in Section III-A.  

> •

P denotes the state transition probability function, which captures how states evolve under the influence of ac-tions. While the generation step is deterministically up-dated from t to t + 1 , the generation status evolves according to the underlying generative mechanism. For diffusion and rectified flow models, state transitions are deterministic and governed by numerical ODE solvers: 

xκt+1 = ODE (xκt , κ t+1 , ·), where · is either the noise prediction model ϵθ,w t or velocity field model vθ,w t ,guided by classifier-free guidance of scale wt. For MaskGIT and autoregressive models, transitions follow stochastic conditional distributions pθ (xt+1 |xt; at) or 

pθ (x1: t+1 |x1: t; at) learned by the generative model.  

> •

R defines the reward function, which guides the policy network’s behavior toward making better policy deci-sions. Consistent with our objective in Eq. (3) that focuses on the final outcome, the reward is defined to be non-zero only at the terminal state, t = T :

R(st, at) ≜ I(t = T ) · r(x), (4) where r(·) (Section III-D) measures the quality of the generated image x extracted from the terminal state 

sT , with the extraction process involving obtaining the generated sample from the state tuple sT and decoding to pixels when models operate in latent space [11], [21]. 

Learning the Adaptive Policy. Given our unified MDP formulation, we are able to formalize the policy network as a parametric agent ηϕ that determines the proper generation policy at based on the current generation status st:

at = ηϕ(st). (5) This formulation enables adaptive policy decisions tailored to each specific generation trajectory. To balance exploration and 6×𝑻 Generator generate sample 

fake images real images 

reward 

Real Data 

Distribution 

Frozen Train Policy Network Reward Model                           

> Fig. 2. Training pipeline of AdaGen with adversarial reward modeling. The process involves an adversarial interplay: the policy network is optimized to maximize a reward signal, while the reward model is concurrently trained to distinguish real from generated images. The reward signal is the probability of a sample being deemed real by the reward model. Notably, the pre-trained generative model stays frozen throughout the pipeline. (a) AdaNAT -FID
> (FID=2.56)
> (c) AdaNAT
> (FID=2.86)
> (b) AdaNAT -PRM
> (FID=33.1 )
> (a) AdaGen -FID ( FID =2.56 )(b) AdaGen -PRM ( FID =33 .1)(c) A daGen (FID =2.86 )
> fidelity diversity fidelity diversity fidelity diversity
> Fig. 3. Reward Design. (a) Using FID [55] as the reward. (b) Using a pre-trained reward model (PRM) [23]. (c) Our main approach with adversarial reward modeling.

Algorithm 1 Training Procedure for AdaGen 

Require: Policy network πϕ, reward model rψ , generative model θ, real data distribution pdata  

> 1:

for i = 0 , 1, 2, . . . do  

> 2:

# Policy network optimization  

> 3:

Sample images xfake ∼ pθ (x | πϕ) 

> 4:

Update ϕ with ∇ϕLPPO (ϕ) ▷ See Eq. (8)  

> 5:

# Reward model optimization  

> 6:

Sample images xreal ∼ pdata , xfake ∼ pθ (x | πϕ) 

> 7:

Update ψ with ∇ψ J(ϕ, ψ) ▷ See Eq. (10)  

> 8:

end for 

exploitation during policy learning, we implement the agent as a stochastic version of the deterministic policy network ηϕ:

πϕ(at | st) ≜ N  ηϕ(st), σ I, (6) where N (·) denotes a multivariate normal distribution, and 

σ is a hyperparameter controlling the degree of exploration. During inference, we simply use the mean of this distribution, which reduces to the deterministic policy ηϕ(st).We train πϕ to maximize the expected cumulative reward across the generation process: 

J(ϕ) = Eπϕ

" TX

> t=0

R(st, at)

#

= Eπϕ

R(sT , aT ), (7) where the simplification to R(sT , aT ) follows from our terminal-only reward design. For effective policy gradient estimation, we adopt the Proximal Policy Optimization (PPO) algorithm [56] with a clipped surrogate objective: 

LP P O (ϕ) = Et

h

min 



ρt(ϕ) ˆAt, clip (ρt(ϕ), 1 − ϵ, 1 + ϵ) ˆAt



− c Vϕ(st) − R(sT , aT )2i

, (8) where ρt(ϕ) = πϕ(at|st)  

> πϕold (at|st)

represents the probability ratio between new and old policies, Vϕ(st) is a learned state-value function that shares the parameters with the policy network, 

ˆAt is the advantage estimate, and ϵ, c are hyperparameters. The advantage estimate is calculated as: 

ˆAt = −Vϕ(st) + R(sT , aT ). (9) Further implementation details of our PPO optimization pro-cedure are provided in Appendix B. 

D. Reward Design 

One of the key aspects in training our reinforcement learn-ing agent is the design of the reward function. In this section, we start with two straightforward reward designs and discuss their challenges. Then, we propose an adversarial reward design that effectively addresses these challenges. 

1) Pre-defined Evaluation Metric. The most straightforward reward design is to employ commonly used evaluation metrics in image generation tasks such as Fréchet Inception Distance (FID). However, we observe two challenges in practice. First ,statistical metrics face challenges in providing sample-wise reward signals. Evaluation metrics in image generation tasks are usually statistical, i.e ., computed over a large number of samples. For example, the common practice in the ImageNet 256 ×256 benchmark is to evaluate the FID and IS score over 50K generated images [10], [11], which makes it challenging to attribute the reward to specific actions. In practice, we find this lack of informative feedback leads to failure in training the adaptive policy network, as detailed in Appendix A. 

Second , better metric scores do not necessarily translate into better visual quality. As shown in Figure 3a, even when the evaluation metric FID is successfully optimized ( e.g ., using a non-adaptive variant of AdaGen and achieving a low FID score of 2.56), the generated images may still suffer from poor visual quality. This underscores the limitations of directly adopting evaluation metrics as optimization objectives and motivates us to find better alternatives for the reward function. 

2) Pre-trained Reward Model. Another option is to adopt a pre-trained, off-the-shelf reward model [23] specialized at assessing the visual quality of images. This strategy mitigates the two challenges in option 1, as it provides per-image reward signals that align more closely with perceptual quality. However, we observe that the generated images in this case tend to converge on a similar style with relatively low diversity, 7

as shown in Figure 3b. Moreover, constructing such reward models typically requires large-scale human preference anno-tations and the training of additional deep networks [23], [57], which is generally costly. Therefore, access to a pre-trained image reward model cannot be assumed in all scenarios. 

3) Adversarial Reward Modeling. One shared issue with the aforementioned two designs is that, while the expected value of rewards can be effectively maximized, the resultant images exhibit unintended inferior quality or limited diversity. In other words, the policy network tends to “overfit” these rewards. Motivated by this observation, we hypothesize that a potential solution is to consider a reward that is updated concurrently with the policy network and adjusted dynamically to resist overfitting. To this end, we propose adversarial reward modeling, where we learn an adversarial reward model rψ

together with the policy network. Specifically, we formulate 

rψ as a discriminator akin to that in GANs [24] and establish a minimax game between the policy network and rψ :max  

> ϕ

min  

> ψ

J(ϕ, ψ) = Exfake ∼pθ (x|πϕ)

log rψ (xfake )

+ Exreal ∼pdata 

log  1 − rψ (xreal ) , (10) where pdata denotes the distribution of real images. As the policy tries to maximize the reward, the reward model is refined simultaneously to better distinguish between real and generated samples. Consequently, we effectively curb the policy network from overfitting a static objective, and more balanced diversity and fidelity of the generated images are also observed (see Figure 3c). Moreover, the adversarial reward model offers immediate, sample-wise reward signals, contrast-ing with the statistical metrics used in option 1; and it obviates the need for expensive human preference data required by the pre-training of reward models in option 2. We summarize the training procedure for AdaGen in Figure 2 and Algorithm 1. 

E. Stabilizing the Exploration of Action Space 

Our framework requires the policy network to operate in a high-dimensional action space, producing multiple policy pa-rameters across generation steps to maximize rewards. While Algorithm 1 effectively mitigates reward overfitting through the joint optimization of the reward and policy networks, we observe that training becomes increasingly unstable as the action space expands. As shown in Figure 4, increasing the number of generation steps from T = 8 to T = 32 in MaskGIT, which effectively expands the action space by 4 times, leads to greater instability without performance gains. 

Inspecting the Instability Issue. To investigate this issue, we examined the policy’s action sequences at T = 32 and observed erratic fluctuations (Figure 5, left). This raises the question of whether such fluctuations are truly necessary for effective generation. To test this, we conducted a controlled experiment in which the learned action sequences were re-placed with a simple linear interpolation between their initial and final values. Interestingly, this naive approach even slightly improved the performance, as shown in Figure 5 (right). This suggests that the high-frequency variations captured by the original policy network may be not only unnecessary but T=8 T=32 T=32 (Stabilized)           

> Fig. 4. Optimization process of AdaGen. When the generation steps increase from T= 8 to T= 32 , the optimization becomes unstable and yields worse performance. Our proposed action smoothing technique stabilizes convergence and achieves better performance ( T= 32 (Stabilized)). Inference Action Inference Action ( Interpolated )

FID=3.5 FID=3.4         

> Fig. 5. Action sequence analysis .Left : Learned action sequences at T=32 for three random samples exhibit erratic fluctuations. Right : A simple linear interpolation between the start and end points of the original learned action sequence yields slightly better performance. We visualize sampling temperature τtas an example of the action sequence.

FID=3.5 FID=2.3    

> Without Smoothing With Smoothing
> Fig. 6. Comparison of action sequences without (left) and with (right) action smoothing. We visualize sequences from both training (exploration) and inference. Action smoothing leads to more feasible exploration during training, and the inference action sequence is also much more reasonable.

also potentially harmful to performance. This observation is consistent with the characteristics of iterative, progressive generation paradigms: Since these models typically begin with high uncertainty and gradually reveal more information, it is less likely that the optimal policy would require highly non-monotonic behavior with irregular fluctuations. Given that these fluctuations appear unnecessary for effec-tive generation, we further investigate why they occur in the first place. The phenomenon may arise from the unstructured exploration process described in Eq. (6), where Gaussian noise with covariance σI is independently added to the policy network’s output at each time step. This approach generates highly unstructured exploration trajectories (Figure 6, left). However, as noted above, such action sequences are rarely suitable for progressive generation and thus may lead to inefficient exploration. Meanwhile, previous studies have also shown that this type of unstructured exploration tends to produce noisy, high-variance inference policy trajectories [58], [59], which aligns with the fluctuations observed in Figure 5. 8

Stabilizing Exploration via Action Smoothing. To enhance exploration stability, we introduce a simple yet powerful technique: action smoothing . The core idea is to smooth the policy network’s raw output sequence before execution. This process suppresses high-frequency oscillations, thereby mit-igating erratic, high-variance trajectories and fostering more stable and efficient policy optimization. Formally, we consider a transformation function f that maps the raw output sequence ˜a1: T sampled from the policy network 

πϕ to the final, smoothed action sequence a1: T :

a1: T = f (˜ a1: T ), (11) with two requirements imposed on the transformation f : 

> •

Low-pass filtering : The function should suppress high-frequency fluctuations to produce smoother action se-quences with fewer oscillations.  

> •

Causality : The transformation must satisfy the causality constraint, ensuring that the action output at at time t

must not depend on any future inputs ˜ai for i > t . This is essential to preserve the Markovian property of our MDP formulation (Section III-C). In theory, any causal low-pass filter from signal processing can serve as a valid choice for f . Among these, we find that the simplest single-pole low-pass IIR (Infinite Impulse Response) filter, commonly known as an Exponential Moving Average (EMA) filter [60], is already highly effective: 

at = βat−1 + (1 − β)˜ at, (12) where β ∈ [0 , 1] is a hyperparameter that controls the degree of smoothing. This straightforward approach effectively smooths the T = 32 action sequences (Figure 6), leading to more stable training and a clear performance improvement over the T = 8 

baseline (Figure 4). We present the ablation study on the action smoothing weight β in Table XIV. 

F. Improvements of AdaGen 1) Inference-time Refinement: Our framework uses an ad-versarial reward model rψ and a value network Vϕ during training to provide learning signals and improve sample effi-ciency. However, they are originally discarded after training and only the policy network is retained to generate outputs at inference time. In this section, we demonstrate that these two auxiliary networks can be repurposed at inference time to further enhance the generation performance. First, we leverage the adversarial reward model, which has been exposed to a large volume of both real and generator-produced images during training. We observe that its score can serve as an effective proxy for the quality of a generated image, as illustrated in Figure 7. Therefore, we use this score during inference to guide repeated sampling [61], [62], selecting the generation with the highest reward to enhance the final output. Beyond repeated sampling based on the final reward, we further propose a lookahead sampling mechanism for mid-process refinement when the transition function P (st+1 |st, at)

is non-deterministic ( e.g ., MaskGIT, see Table II for more details). Recall that the value network Vϕ is trained to predict the expected final reward from an intermediate generation state 0.91      

> 0.92
> 0.98
> 0.14
> 0.19
> 0.23
> 0.10
> 0.40
> 0.45
> 0.52
> 0.61
> 0.86
> Score 𝑟 𝝍 (𝒙 )
> low quality
> samples
> low
> scores
> medium quality
> samples
> high quality
> samples
> medium
> scores
> high
> scores
> Sample 𝒙
> ⋯ ⋯
> Fig. 7. Generated samples and their scores from our adversarial reward model . We showcase 12 samples and observe that the assigned scores, rψ(x),align well with their visual quality. This demonstrates that our adversarial reward model serves as an effective evaluator for perceptual quality.

st (see Eq. (8)). This property makes it well-suited for guiding local decision-making to improve sampling quality within a single generation process. Specifically, at each transition from state st to st+1 , when the transition is stochastic, we can sample K candidates (s(1) 

> t+1

, . . . , s(K) 

> t+1

), and then compute the predicted expected reward Vϕ(s(k)

> t+1

) for each k ∈ { 1, . . . , K }.We then select the next state with the highest expected reward: 

k∗ = arg max  

> k

Vϕ(s(k)

> t+1

), st+1 = s(k∗) 

> t+1

. (13) This lookahead mechanism steers the generation process to-ward more promising trajectories, enabling within-process refinement. We summarize our full inference-time refinement strategy in Algorithm 2. The algorithm includes both the repeated sampling and the optional lookahead sampling. 

2) Flexibly Trading Off Fidelity and Diversity: While our adversarial reward model is designed to achieve a strong, balanced trade-off of fidelity and diversity, certain applica-tions may demand prioritizing one metric over the other. For instance, a user might be willing to sacrifice some output variety in exchange for the highest possible image fidelity. To accommodate such requirements, we introduce a mechanism that offers explicit, granular control over the fidelity-diversity trade-off, governed by a user-adjustable parameter, λ.As illustrated in Figure 8, our strategy utilizes two policy networks. The first is our original policy network trained with adversarial reward modeling. We then introduce a secondary policy network that solely focuses on image fidelity. The final action ablend  

> t

is derived from a linear interpolation of the action at from our primary policy and the action a′ 

> t

from the fidelity-oriented policy: 

ablend  

> t

= (1 − λ) at + λ a′

> t

. (14) Here, λ ∈ [0 , 1] is a user-adjustable hyperparameter that allows a smooth transition from a balanced synthesis at λ = 0 to outputs of maximal fidelity as λ approaches 1. The training of the fidelity-oriented policy network is gov-erned by a dynamic objective. In each training step, we sample a blending factor λ ∼ U [0 , 1] and generate an image using 9

Algorithm 2 Inference-Time Refinement for AdaGen 

Require: Policy network ηϕ, adversarial reward model rψ ,value network Vϕ. Number of repeated sampling trials 

M . Number of lookahead branches K. 

> 1:

Initialize xbest ← null, rbest ← −∞  

> 2:

for i = 0 to M do ▷ Repeated Sampling  

> 3:

Initialize generation state s0 

> 4:

for t = 0 to T − 1 do  

> 5:

Obtain action: at ← ηϕ(st) 

> 6:

if Lookahead then ▷ Lookahead Sampling  

> 7:

Sample: {s(k)

> t+1

}Kk=1 ∼ P (st+1 |st, at) 

> 8:

Find optimal index: k∗ ← arg max k Vϕ(s(k)

> t+1

) 

> 9:

Select optimal state: st+1 ← s(k∗) 

> t+1
> 10:

else  

> 11:

Sample: st+1 ∼ P (st+1 |st, at) 

> 12:

end if  

> 13:

end for  

> 14:

Extract final image x from the terminal state sT 

> 15:

Compute reward: r ← rψ (x) 

> 16:

if r > r best then  

> 17:

rbest ← r 

> 18:

xbest ← x 

> 19:

end if  

> 20:

end for  

> 21:

return xbest Orig Policy 𝒔 ! 𝒂 !

New Polic y 𝒂 !  

> "
> (1−𝜆 )×
> 𝜆 ×

## 𝒂 !           

> #$%&'
> Original AdaGen
> Fig. 8. Illustration of our blended policy. We introduce a new, fidelity-oriented policy network upon our original framework and linearly interpolate its output a′
> twith the original policy network’s output atwith a blending parameter λto obtain the final action ablend
> t.

the interpolated policy. The fidelity-oriented policy network is then optimized with a blended reward signal: 

rblend = (1 − λ) r + λ r ′, (15) while the original policy network is kept frozen to maintain the integrity of the balanced policy. In this formulation, r

is our original adversarial reward, whereas r′ is a dedicated fidelity reward, for which we employ ImageReward [23]. The key to this design is the dual function of λ; it controls both the fidelity-oriented policy’s influence on the output and the weight of the fidelity-centric reward in its optimiza-tion objective. This coupling establishes a explicit mapping between the control parameter λ and the fidelity-diversity spectrum, thereby offering a user-controllable modulation of generation behavior. The effectiveness of this blending strategy is demonstrated in Figure 13. IV. E XPERIMENTS 

We conduct extensive experiments to demonstrate the effec-tiveness of AdaGen. Section IV-B presents class-conditional generation on ImageNet with different architectures and gen-eration steps. Section IV-C covers text-to-image generation results on MS-COCO, CC3M, and LAION-5B. Sections IV-D and IV-E examine AdaGen’s inference-time refinement ca-pabilities and flexibility in balancing fidelity and diversity. Section IV-F provides comprehensive analyses of AdaGen. 

A. Setups 

Datasets. For class-conditional image generation, we use Im-ageNet [25] at 256 ×256 and 512 ×512 resolutions, reporting FID-50K following [11]. For text-to-image generation, we use MS-COCO [26], CC3M [27], and LAION-5B [28], reporting FID-30K following [2], [10]. For large-scale text-to-image generation on LAION-5B, we follow common practice [2], [32] to report zero-shot FID-30K results on MS-COCO. 

Generative Models. We evaluate AdaGen across four rep-resentative generative paradigms. For MaskGIT, we utilize the publicly available implementation from [29] and evaluate MaskGIT-S with 13 layers, 512 dimensions, and approxi-mately 50M parameters, and MaskGIT-L with 25 layers, 768 dimensions, and approximately 200M parameters. For diffu-sion models, we adopt DiT-XL/2 [11] for class-conditional generation on ImageNet [25] and Stable Diffusion [21] for text-to-image generation on LAION-5B [28]. For rectified-flow models, we adopt SiT-XL [13]. For autoregressive mod-els, we employ VAR [5] with different model scales. Following recent trends in advanced samplers for diffusion and rectified-flow models [8], [15], [63], we adopt DPM-Solver [15] for DiT-XL and SiT-XL, building AdaGen upon it. For fair comparisons, we employ the same few-step samplers for the corresponding baselines. We also experiment with different generation steps. For models with more than 10 steps, we apply action smoothing with β = 0.8 to stabilize policy network exploration (see Section III-E for more details). 

Implementation Details. In practice, our adaptive policy network leverages off-the-shelf features from the backbone generative models rather than raw intermediate generation results (e.g., xt) as input, which we find more effective. The architecture consists of a convolutional layer, a multi-layer perceptron (MLP), and additional adaptive LayerNorm (AdaLN) layers to incorporate generation step information. To ensure output policy parameters remain within appropriate ranges, we introduce activation functions, e.g ., softplus to con-strain the classifier-free guidance scale to be within (0 , +∞),at the end of the policy network. The adversarial reward model architecture adopts a transformer-based discriminator following [64]. We present in-depth ablation studies for these architectural decisions in Table XV. Further implementation details can be found in Appendix B. 

B. Class-conditional Generation on ImageNet. 

Effectiveness of AdaGen on Different Generative Paradigms. Table III evaluates AdaGen on ImageNet 256 ×256 across six models spanning four iterative generation paradigms. For each paradigm except VAR with a fixed 10-step configuration, we test at 4, 8, and 16 inference steps. Our adaptive policy consistently outperforms the corresponding 10 

TABLE III EFFECTIVENESS OF ADA GEN ON DIFFERENT GENERATIVE PARADIGMS 

Model Type Policy T = 4 T = 8 T = 10 T = 16 

MaskGIT-S Mask. Baseline 7.65 5.01 - 4.88 

AdaGen 4.54 (-3.11) 3.71 (-1.30) - 3.36 (-1.52) 

MaskGIT-L Mask. Baseline 6.91 4.65 - 3.79 

AdaGen 3.63 (-3.28) 2.86 (-1.79) - 2.41 (-1.38) 

DiT-XL Diff. Baseline 9.71 5.18 - 3.31 

AdaGen 5.31 (-4.40) 2.82 (-2.36) - 2.19 (-1.12) 

SiT-XL Flow Baseline 9.33 4.90 - 2.99 

AdaGen 4.25 (-5.08) 2.72 (-2.18) - 2.12 (-0.87) 

VAR-d16 AR Baseline - - 3.30 -

AdaGen - - 2.62 (-0.68) -VAR-d30 AR Baseline - - 1.92 -

AdaGen - - 1.59 (-0.33) -

> We evaluate AdaGen on ImageNet 256 ×256 using six models spanning four representative paradigms and report FID-50K results. As VAR uses a fixed number of 10 generation steps [5], we report results under the same configuration. Mask.: MaskGIT, Diff.: diffusion, Flow: rectified-flow, AR: autoregressive.

3.45 x

2.96 x 2.96 x

1.66 x

3.61 x

3.08 x 3.12 x

2.84 x

2.84 x 2.98 x

1.92 x

1.64 x

Sampling Time / Image on CPU (s) 

MaskGIT DiT SiT VAR 

Sampling Compute / Image (TFLOPs) 

> FID -50K FID -50K FID -50K

Sampling Time / Image on GPU (s) 

Fig. 9. Efficiency of AdaGen . We vary the inference budget by adjusting the number of inference steps ( T = 4 , 8, 16 ) for MaskGIT-L, SiT-XL, and DiT-XL. For VAR, whose inference steps are fixed, we vary the model size (VAR-d16, VAR-d20, VAR-d30) instead. GPU time is measured on an A100 GPU with batch size 16. CPU time is measured on Xeon 8375 CPU with batch size 1. 

baselines across all paradigms and inference budgets, with performance gains becoming more pronounced as inference steps decrease. With the DiT-XL backbone, our policy reduces FID by 1.12 at 16 steps, 2.36 at 8 steps, and 4.40 at 4 steps, demonstrating strong advantages in computationally constrained scenarios. Figure 9 illustrates generation quality-efficiency trade-offs across different models. For most models, we vary inference steps to represent different computational budgets. For VAR, which utilizes fixed generation steps, we vary the model ca-pacity to represent different computational budgets. We report both theoretical inference cost measured by TFLOPs and practical GPU/CPU latency. Results demonstrate that AdaGen consistently improves the quality-efficiency frontier, achieving 1.6 × to 3.6 × speedup while maintaining comparable genera-tion quality. This enables reduced-budget inference to achieve competitive performance. 

System-level Comparison. Tables IV and V compare AdaGen-augmented models with strong baselines on Ima-geNet at 256 ×256 and 512 ×512 resolutions. Across all set-tings, AdaGen consistently improves both performance and efficiency. For instance, AdaGen-DiT-XL achieves an FID of 2.19 in 16 steps (4.1 TFLOPs), outperforming the baseline DiT-XL’s 2.29 FID with 50 steps (12.2 TFLOPs), resulting in a ∼ 3× reduction in inference cost. On VAR-d30, AdaGen improves the FID from 1.92 to 1.59 with negligible overhead. 11 

TABLE IV CLASS -CONDITIONAL IMAGE GENERATION ON IMAGE NET 256 ×256 Method Type Params Steps TFLOPs ↓ FID-50K ↓ IS ↑

VQVAE-2 [65] (NeurIPS’19 ) AR 13.5B 5120 - 31.1 ∼ 45 VQGAN [19] (CVPR’21 ) AR 1.4B 256 - 15.78 78.3 ADM-G [9] (NeurIPS’21 ) Diff. 554M 250 334.0 4.59 186.7 ADM-G, ADM-U [9] (NeurIPS’21 ) Diff. 608M 250 239.5 3.94 215.8 LDM [21] (CVPR’22 ) Diff. 400M 250 52.3 3.60 247.7 VQ-Diffusion [66] (CVPR’22 ) Diff. 554M 100 12.4 11.89 -ViT-VQGAN [67] (ICLR’22 ) AR 599M 1024 - 3.04 227.4 Draft-and-revise [68] (NeurIPS’22 ) Mask. 1.4B 72 - 3.41 224.6 U-ViT-H [10] (CVPR’23 ) Diff. 501M 50 13.6 2.29 263.8 DiT-XL [11] (ICCV’23 ) Diff. 675M 250 59.6 2.27 278.2 MDT [12] (ICCV’23 ) Mask. 676M 250 59.6 1.79 283.0 Simple-Diffusion [69] (ICML’23 ) Diff. 2B 512 - 2.44 256.3 RIN [70] (ICML’23 ) Diff. 410M 1000 334.0 3.42 182.0 VDM++ [71] (NeurIPS’23 ) Diff. 2B 512 - 2.12 267.7 SiT-XL [13] (ECCV’24 ) Flow 675M 250 59.6 2.06 270.3 MAGVIT-v2 [72] (ICLR’24 ) Mask. 307M 64 - 1.78 319.4 TiTok [4] (NeurIPS’24 ) Mask. 287M 64 4.3 1.97 -LlamaGen [6] (arxiv’24 ) AR 3.1B 256 - 2.18 263.3 MaskGIT [1] (CVPR’22 ) Mask. 227M 8 0.6 6.18 182.1 MaskGIT † [1] (CVPR’22 ) Mask. 227M 8 8.4 4.02 -DiT-XL [11] (ICCV’23 ) Diff. 675M 16 4.1 3.31 -Diff. 675M 50 12.2 2.29 275.3 SiT-XL [13] (ECCV’24 ) Diff. 675M 16 4.1 2.99 -Diff. 675M 50 12.2 2.18 287.0 Token-Critic [17] (ECCV’22 ) Mask. 422M 36 1.9 4.69 174.5 Token-Critic † [17] (ECCV’22 ) Mask. 422M 36 8.7 3.75 287.0 MAGE [18] (CVPR’23 ) Mask. 230M 20 1.0 6.93 -MaskGIT-FSQ [73] (ICLR’24 ) Mask. 225M 12 1.4 4.53 -USF [16] (ICLR’24 ) Diff. 554M 8 10.7 9.72 -RCG-L [74] (NeurIPS’24 ) Mask. 502M 20 4.4 2.15 253.4 VAR-d16 [5] (NeurIPS’24 ) AR 310M 10 0.5 3.30 274.4 VAR-d30 [5] (NeurIPS’24 ) AR 2.0B 10 2.0 1.92 323.1 

AdaGen-MaskGIT-L Mask. 206M 16 1.7 2.41 269.7 

AdaGen-DiT-XL Diff. 688M 16 4.1 2.19 279.8 

AdaGen-SiT-XL Flow 688M 16 4.1 2.12 291.4 

AdaGen-VAR-d16 AR 323M 10 0.5 2.62 278.4 

AdaGen-VAR-d30 AR 2.0B 10 2.0 1.59 324.3 

TFLOPs quantify the total computational cost of generating a single image. †: uses rejection sampling. 

TABLE V CLASS -CONDITIONAL IMAGE GENERATION ON IMAGE NET 512 ×512 Method Params Steps TFLOPs ↓ FID-50K ↓

VQGAN [19] 227M 1024 - 26.52 ADM-G [9] 559M 250 579.0 7.72 U-ViT-H [10] 501M 50 14.6 4.05 DiT-XL [11] 675M 250 60.0 3.04 RIN [70] 320M 1000 415.0 3.95 Simple-Diffusion [69] 2B 512 - 3.02 VDM++ [71] 2.4B 256 - 2.65 EDM2-XS [75] 125M 63 5.7 2.91 DiffiT [76] 561M 250 - 2.67 Large-DiT-3B-G [77] 3B 250 - 2.52 MaskDiT [78] 736M 79 - 2.50 DyDiT-XL [79] 678M 100 75.1 3.61 MaskGIT [1] 227M 12 3.3 7.32 MaskGIT † [1] 227M 12 13.1 4.46 Token-Critic [17] 422M 36 7.6 6.80 Token-Critic † [17] 422M 36 34.8 4.03 

AdaGen-MaskGIT-L 232M 16 2.0 2.82 232M 32 3.6 2.46 

> †

: uses rejection sampling. 

At 512 ×512 resolution, AdaGen-MaskGIT achieves 2.82 FID with 2.0 TFLOPs and 2.46 FID with 3.6 TFLOPs, outperform-ing baselines while remaining more efficient. 

Qualitative Results. Figure 10a and 10b present qualitative results on ImageNet at 512 ×512 and 256 ×256 resolutions, respectively. Our method generates high-fidelity images across diverse categories. In Figure 11, we provide a direct visual comparison between the VAR-d30 models with and without our adaptive policy. The results clearly illustrate that AdaGen consistently enhances visual quality, reduces artifacts, and preserves fine details across various image categories. We provide more qualitative results in Appendix C. 

C. Text-to-image Generation 

Text-to-image Generation on MS-COCO. As shown in Table VI, AdaGen-MaskGIT-S achieves a compelling perfor-mance on MS-COCO, reaching an FID score of 4.92 with 16 sampling steps. This marks a clear improvement over the MaskGIT-S baseline (5.78 FID) under the same computational cost. Moreover, our 8-step variant attains an FID of 5.29 with only 0.3 TFLOPs, outperforming both the 16-step MaskGIT-S baseline at 5.78 FID and 0.6 TFLOPs, and the 50-step U-ViT model, which achieves 5.48 FID with a cost of 2.2 TFLOPs. 

Text-to-image Generation on CC3M. As shown in Ta-ble VII, we apply AdaGen to the larger text-to-image model Muse [2] and evaluate its performance on the CC3M dataset. Our method demonstrates better efficiency and performance, achieving an FID-30K score of 6.66 with only 8 inference steps, already surpassing the Muse baseline at 16 steps while consuming roughly half the computation. With 16 steps, 12 A cat - porcupine - hybrid, animal          

> photography.
> (a) Class -conditional Generation on ImageNet 512 ×512 (b) Class -conditional Generation on ImageNet 256 ×256
> (c) Text -to -image Generation
> An architectural, unique pot made
> for houseplants.
> A cabin on a small hill surrounded
> by colorful trees.
> Interior design photo of an
> apartment with glass walls
> overlooking a vast hilly landscape.
> Walter White, cinematic, hyper
> realism, high detail, bright lights,
> lens flare, octane render, 8k.

Fig. 10. Qualitative results of AdaGen . The corresponding models are (a) AdaGen-MaskGIT-L ( T = 32 ), (b) AdaGen-VAR-d30 ( T = 10 ) and (c) AdaGen-Stable-Diffusion ( T = 32 ), respectively. Baseline AdaGen 

Fig. 11. Effectiveness of AdaGen on class-conditional image generation .We visualize the generated samples of VAR-d30 without and with AdaGen. TABLE VI TEXT -TO -IMAGE GENERATION ON MS-COCO Method Params Steps TFLOPs ↓ FID-30K ↓

VQ-Diffusion [66] 370M 100 206.7 13.86 Frido [80] 512M 200 - 8.97 Make-A-Scene [81] 4B 1536 - 7.55 U-Net [10] 53M 50 - 7.32 MaskGIT-S [1] 45M 16 0.6 5.78 U-ViT [10] 58M 16 0.9 5.91 50 2.2 5.48 

AdaGen-MaskGIT-S 57M 8 0.3 5.29 16 0.6 4.92 

AdaGen-Muse further improves to an FID of 6.36, clearly outperforming the corresponding baseline. 

TABLE VII TEXT -TO -IMAGE GENERATION ON CC3M Method Params Steps TFLOPs ↓ FID-30K ↓

VQGAN [19] 600M 256 12.6 28.86 ImageBART [82] 2.8B 256 7.4 22.61 LDM-4 [21] 645M 50 - 17.01 Draft-and-revise [68] 654M 72 - 9.65 Muse [2] 500M 8 2.8 7.67 16 5.4 7.01 

AdaGen-Muse 512M 8 2.8 6.66 16 5.4 6.36 

Large-scale Text-to-image Generation on LAION-5B. To evaluate the effectiveness of our approach in large-scale settings, we integrated AdaGen into Stable Diffusion and conducted zero-shot evaluation on MS-COCO, following [32], [34]. As reported in Table VIII, AdaGen-Stable Diffusion achieves 8.14 FID using 32 inference steps, outperforming the original model’s 9.03 FID under the same computational budget. It also surpasses the 50-step Stable Diffusion baseline at 8.59 FID while requiring roughly 1.5× less computation. These results demonstrate that our method remains effective when deployed in large-scale generative systems. 

Qualitative Results. Figure 10c showcases AdaGen-Stable Diffusion’s ability to generate high-fidelity, prompt-aligned images. Figure 12 contrasts the image generation quality of the standard Stable Diffusion baseline against our method. As demonstrated in the first two rows, AdaGen produces images with superior aesthetic quality. Furthermore, the last 13                                                      

> TABLE VIII ZERO -SHOT TEXT -TO -IMAGE GENERATION ON MSCOCO Method Params Steps TFLOPs ↓FID-30K ↓
> CogView [35] 4B 1088 182.2 27.1 LAFITE [83] 0.2B --26.94 CogView 2 [84] 6B 1088 117.0 24.00 DALL-E [19] 12B 256 309.8 17.89 GLIDE [85] 5B 250 77.0 12.24 Make-A-Scene [81] 4B 1536 -11.84 Corgi [86] 5.7B 64 43.3 10.88 DALL-E 2 [34] 6.5B 250 -10.39 Stable Diffusion [21] 1.4B 32 22.9 9.03 50 35.1 8.59 Simple Diffusion [69] 2B 512 151.3 8.30
> AdaGen-Stable Diffusion 1.4B 32 22.9 8.14

Realistic miniature of 

planets in the donut 

shape on space. 

Stock photo of the 

most delicious food 

in the world, studio 

lighting, 4k 

photorealistic .

Venice beach in the 

snow, photograph. 

Baseline AdaGen 

A photo of a Husky 

dog wearing a hat.       

> Fig. 12. Effectiveness of AdaGen on text-to-image generation . We visualize the generated samples of Stable Diffusion without and with AdaGen.

two rows highlight its improved image-text alignment. For the prompt “A photo of a Husky dog wearing a hat”, the baseline fails to generate the hat, an attribute that AdaGen successfully includes. Similarly, when prompted to create a “Realistic miniature of planets in the donut shape on space”, the baseline model struggles with the specified donut shapes, whereas AdaGen accurately renders the planets as donuts with convincing textures and appearance. We provide more qualitative results in Appendix C. 

D. Inference-time Refinement 

As introduced in Section III-F1, we repurpose the adver-sarial reward model rψ as a perceptual evaluator for re-peated sampling, while the value network Vϕ further enables                                               

> TABLE IX INFERENCE -TIME REFINEMENT OF ADA GEN
> Repeated Sampling? Lookahead Sampling? FID-50K ↓
> MaskGIT-L
> ✗✗2.28
> ✓✗2.07
> ✓✓1.94
> DiT-XL
> ✗✗2.19
> ✓✗2.06
> ✓✓-
> We apply inference-time refinement to MaskGIT-L ( T= 32 ) and DiT-XL ( T= 16 ). For DiT-XL, since the sampling process adopts an ODE solver [8] and thus the state transition is deterministic, we only apply repeated sampling. Here, repeated sampling is executed once, and Table X shows the results with more repeated sampling trials.
> TABLE X INFERENCE -TIME REFINEMENT OF ADA GEN WITH DIFFERENT NUMBERS OF REPEATED SAMPLING TRIALS
> # Repeat Trials 0123456MaskGIT-L 2.28 1.94 1.85 1.81 1.84 1.89 1.90 DiT-XL 2.19 2.06 2.02 1.99 1.93 1.91 1.92

lookahead refinement in models with stochastic transitions. We evaluate these strategies on MaskGIT-L with T = 32 

and DiT-XL with T = 16 . As shown in Table IX, repeated sampling consistently improves performance for both models. For MaskGIT-L, where transitions are stochastic, lookahead refinement (with K = 2 branches, which we find empirically sufficient) yields additional gains, reducing FID to 1.94. We further study the impact of the number of repeated sampling trials M , with results in Table X. Increasing M

leads to steady improvements—for example, in MaskGIT-L, FID drops from 2.28 to 1.81 with three sampling iterations (lookahead sampling is also enabled during generation). These results confirm the general effectiveness of our inference-time refinement. We also provide qualitative results in Appendix C. 

E. Flexibly Trading Off Fidelity and Diversity. 

Section III-F2 introduced a blending policy with parame-ter λ to control the fidelity-diversity trade-off. We evaluate its effectiveness on ImageNet 256 ×256 by varying λ ∈{0, 0.25 , 0.5, 0.75 , 1.0}, where larger values favor fidelity over diversity. Figure 13 illustrates this trade-off on two ImageNet classes with 4 ×4 visual grids, along with the corresponding FID scores which capture the fidelity-diversity balance. At 

λ = 0 , outputs are highly diverse but may occasionally contain visual artifacts. Increasing λ to 0.25 and 0.5 eliminates lower-quality samples while maintaining reasonable diversity. At 

λ = 0 .75 , the model yields consistently realistic and high-quality samples. At λ = 1.0, fidelity peaks with visually appealing but highly converged outputs and reduced diversity. This controllable trade-off allows practitioners to tailor the balance to their specific application needs. 

F. Discussions 

This section presents more analyses of AdaGen. If not mentioned otherwise, we use AdaGen-MaskGIT-S ( T = 4 )by default. 

Effectiveness of AdaGen. In Table XI, we evaluate the effectiveness of AdaGen using MaskGIT-S and DiT-XL with 14 Diversity Fidelity 

𝜆 = 0

FID = 1.59 

𝜆 = 0.25 

FID = 5.67 

𝜆 = 0.5

FID = 13 .52 

𝜆 = 1

FID = 20 .46 

𝜆 = 0.75 

FID = 18 .17 

Fig. 13. The fidelity-diversity trade-off of AdaGen . We visualize 4 ×4 samples from two ImageNet classes (brown bears and car wheels) across different 

λ values using AdaGen-VAR-d30. Each setting reports FID scores measuring the fidelity-diversity balance. Higher λ values prioritize fidelity over diversity. 

Fig. 14. Optimization curve of AdaGen with intermediate samples visualized. FID-5K scores are reported for efficiency. TABLE XI EFFECTIVENESS OF ADA GEN 

Generation Policy FID-50K ↓

Learnable? Adaptive? MaskGIT-S DiT-XL 

✗ ✗ 7.65 9.71 

✓ ✗ 5.40 (-2.25) 6.03 (-3.68) 

✓ ✓ 4.54 (-3.11) 5.31 (-4.40)  

> We use MaskGIT-S and DiT-XL ( T= 4 ) on ImageNet 256 ×256.

T = 4 . Introducing learnability yields substantial improve-ments: MaskGIT-S achieves a 2.25 FID reduction from 7.65 to 5.40, a 30% relative improvement, while DiT-XL obtains a 3.68 reduction from 9.71 to 6.03, a 38% gain. These results suggest the sub-optimality of the hand-crafted genera-tion policies from prior works [1], [11]. Making the policy both learnable and adaptive further improves performance. MaskGIT-S reaches an FID of 4.54 with an additional 16% improvement, while DiT-XL achieves 5.31 with a 12% gain. These consistent improvements demonstrate the effectiveness of learnable, adaptive policies. Figure 14 visualizes generation results during policy train-ing. Initially, the policy produced very blurry images with high FID scores. As training progresses, the policy gradually refines itself, yielding lower FID scores and improved image quality. Refine Level                   

> high
> low
> 99% 97%95%89 %71 %34 %9%
> 99% 98%91%76 %48 %19 %7%
> 99% 98%94%89 %79 %62 %28 %

Fig. 15. Visualizing the learned policies of AdaGen. We use AdaGen-MaskGIT-L ( T = 8 ) on ImageNet 256 ×256 as an example. The mask ratio 

mt (refine level), which controls the proportion of least-confident tokens to be refined at each step in MaskGIT (see [1] for more details), is visualized as an example. The policy network adaptively reduces mt for only minor refinements when the sample already reaches a decent quality; otherwise, it keeps adopting relatively higher mt for more adjustments. 

Finally, the policy converges to a robust generation strategy that consistently produces quality images with low FID scores, demonstrating the effectiveness of AdaGen. 

Visualizing the Learned Adaptive Policy. Figure 15 illus-trates our learned policies using AdaGen-MaskGIT-L with 

T = 8 as an example. We visualize the adaptive policy with mask ratio mt, which controls the proportion of least-confident tokens to be refined at each step. For samples with simple structures (first row), the refine level quickly drops once reasonable quality is achieved, limiting refinements. For harder structures (second row), the policy maintains higher refine levels for more adjustments. When the structure of the sample becomes complex (third row), the policy consistently applies relatively high refine levels to make more refinements until the end. These visualizations provide deeper insights into the adaptive mechanism of AdaGen. 

Computational Overhead of AdaGen. Table XII provides a detailed breakdown of inference cost, comparing our policy network’s computational requirements with various generative 15 

TABLE XV ABLATION STUDIES ON MODEL ARCHITECTURE AND COMPONENTS OF ADA GEN . O UR DEFAULT SETTING IS MARKED IN GRAY .Architecture # Params FID-50K ↓

Baseline / 7.65 Transformer-based 32.0M 4.54 

Conv-based 26.9M 5.86 (a) Discriminator architecture. The transformer-based architecture [64] significantly outperforms the baseline and Convolution-based architecture [87]. Output Activation FID-50K ↓

Activation 4.54 

Simple Clamping 6.29 (b) Output activation. Mapping the policy network’s output to the valid range via activation functions (see Appendix B) is more effective than clamping. Input Type FID-50K ↓

Features 4.54 

Raw Input 6.55 (c) Input type. Using off-the-shelf fea-tures from the generative model as the policy network’s input is more effective. Architecture Params ↓ FLOPs ↓ FID-50K ↓

Conv + MLP 12.0M 0.016G 4.54 Trans Block + MLP 16.7M 0.891G 4.72 Conv + Linear 6.3M 0.006G 5.03 Conv + Heavy MLP 24.6M 0.029G 4.49 

(d) Policy network architecture. The combination of a convolutional layer and an MLP block already achieves a satisfactory performance with low parameter count and computational cost. Step Cond. FID-50K ↓

✓ 4.54 

✗ 6.13 (e) Generation step conditioning. Incorporating the current generation step t into the policy network is critical for effective policy decisions. TABLE XII COMPUTATIONAL OVERHEAD OF ADA GEN 

Generator T Inference Cost (TFLOPs) Proportion Generator Policy Net. (Policy / Generator) MaskGIT-L 16 1.7 0.002 0.11% DiT-XL 16 4.1 0.003 0.07% SiT-XL 16 4.1 0.003 0.07% VAR-d16 10 0.5 0.002 0.40% VAR-d30 10 2.0 0.003 0.15% 

> We report the inference cost (in TFLOPs) for various generative models and our policy networks on ImageNet. The proportion column indicates the relative computational overhead introduced by our policy network compared to the base generator model.

TABLE XIII SENSITIVITY ANALYSIS OF PPO H YPERPARAMETERS . D EFAULT SETTING IS MARKED IN GRAY . A DA GEN CONSISTENTLY OUTPERFORMS THE 

BASELINE (L IGHT GRAY ) ACROSS VARIOUS HYPERPARAMETER CHOICES .                           

> ϵFID-50K ↓
> -7.65 0.05 5.01 0.1 4.86 0.2 4.54 0.3 4.45
> 0.4 4.68 (a) Effects of varying ϵ.
> cFID-50K ↓
> -7.65 0.2 4.63 0.4 4.59 0.5 4.54
> 0.6 4.66 0.8 4.73 (b) Effects of varying c.
> σFID-50K ↓
> -7.65 0.1 5.31 0.3 4.72 0.6 4.54
> 0.8 4.66 1.0 4.73 (c) Effects of varying σ.

models. Results show minimal overhead of only 0.07% -0.40% 

added to the original generator’s computational cost. This demonstrates the practicality of achieving notable performance gains with negligible computational overhead. 

Sensitivity Test on PPO Hyperparameters. Our policy network is optimized using the PPO algorithm [56], which introduces several hyperparameters defined in Eqs. (6) and (8). Table XIII shows that our method is able to achieve supe-rior performance across a variety of hyperparameter choices, demonstrating its robustness. 

Effect of Action Smoothing Weight. As discussed in Sec-tion III-E, we employ action smoothing to stabilize explo-ration, introducing smoothing hyperparameter β. Table XIV reports FID-50K scores for a variety of β values, with β of 0 representing no smoothing serving as the baseline. Across all 

β values, our action smoothing consistently outperforms the baseline and achieves the lowest FID of 3.36 with β of 0.8. 

TABLE XIV EFFECT OF ACTION SMOOTHING WEIGHT β

Smoothing Weight β 0 0.2 0.4 0.8 0.95 FID-50K ↓ 3.97 3.61 3.47 3.36 3.70    

> Action smoothing consistently improves the performance of the larger-step ( T= 16 )version of AdaGen-MaskGIT-S, with the best performance achieved at β= 0 .8.

As a result, we set β to 0.8 in our experiments. 

Ablation Study on the Reward Model Architecture. 

To evaluate the impact of the reward model’s architec-ture, we compare a convolutional discriminator [87] and a transformer-based discriminator [64]. As shown in Ta-ble XV(a), both variants significantly outperform the baseline, with the transformer-based design yielding superior results. Based on this observation, we adopt the transformer-based discriminator. 

Ablation Study on the Output Activation. At the end of our policy network, we introduce an activation function to smoothly constrain outputs within the desired range (detailed in Appendix B). Table XV(b) compares this approach with simple clamping that directly clips the output range to the desired bounds. Our smooth activation design yields an FID-50K of 4.54, significantly outperforming simple clamping with an FID of 6.29. This demonstrates the effectiveness of smooth constraining activation functions over hard clipping methods. 

Ablation Study on the Input Type. Table XV(c) compares using off-the-shelf features from the generative model ver-sus raw intermediate generation results as policy network input. Using features from pre-trained generative models yields substantial improvement with FID-50K of 4.54 versus 6.55 for raw inputs. We attribute this gain to the powerful feature-extraction capabilities of pre-trained generative mod-els, where representations encode semantically meaningful information [88]. 

Ablation Study on the Policy Network Architecture. Ta-ble XV(d) investigates different architectural choices. Our de-fault conv+MLP configuration achieves FID-50K of 4.54 with 12.0M parameters and 0.016G FLOPs. Replacing convolution with transformer blocks increases cost without improvement. 16 

Increasing MLP capacity achieves slight improvement but nearly doubles parameters, while reducing to one linear layer hurts performance with an FID of 5.03. 

Ablation Study on the Generation Step Conditioning. In our MDP formulation from Section III-C, the state space comprises both the intermediate generation result and the current generation step. To quantify the benefit of explicitly incorporating the generation step into the policy network, Table XV(e) compares the performance with and without step conditioning. Removing generation step conditioning yields significant degradation from FID of 4.54 to 6.13, underscoring its importance. One possible explanation is that effective pol-icy decisions are inherently time-sensitive: since the iterative generative process has a fixed number of generation steps and must terminate after a certain number of iterations, the policy network may need to understand where it stands in this progression to make appropriate interventions. V. C ONCLUSION 

We presented AdaGen, a unified framework that replaces manually designed scheduling functions in iterative generative models with a learnable policy network, enabling adaptive per-sample customization without requiring expert knowledge. Our key contributions include formulating policy selection as a Markov decision process and introducing an adversarial reward model to prevent overfitting. Comprehensive validation across multiple datasets and paradigms demonstrates signifi-cant improvements in generation quality and efficiency. By treating generation policy design as a data-driven optimiza-tion problem, AdaGen represents an encouraging step toward automated, adaptive generative modeling. ACKNOWLEDGMENTS 

This work is supported in part by the National Key R&D Program of China under Grant 2024YFB4708200, the National Natural Science Foundation of China under Grants U24B20173 and the Scientific Research Innovation Capability Support Project for Young Faculty under Grant ZYGXQNJSKYCXNLZCXM-I20. REFERENCES [1] H. Chang, H. Zhang, L. Jiang, C. Liu, and W. T. Freeman, “Maskgit: Masked generative image transformer,” in CVPR , 2022. [2] H. Chang, H. Zhang, J. Barber, A. Maschinot, J. Lezama, L. Jiang, M.-H. Yang, K. Murphy, W. T. Freeman, M. Rubinstein et al. , “Muse: Text-to-image generation via masked generative transformers,” in ICML ,2023. [3] V. Besnier and M. Chen, “A pytorch reproduction of masked generative image transformer,” arXiv preprint arXiv:2310.14400 , 2023. [4] Q. Yu, M. Weber, X. Deng, X. Shen, D. Cremers, and L.-C. Chen, “An image is worth 32 tokens for reconstruction and generation,” in NeurIPS ,2024. [5] K. Tian, Y. Jiang, Z. Yuan, B. Peng, and L. Wang, “Visual autoregressive modeling: Scalable image generation via next-scale prediction,” in 

NeurIPS , 2024. [6] P. Sun, Y. Jiang, S. Chen, S. Zhang, B. Peng, P. Luo, and Z. Yuan, “Autoregressive model beats diffusion: Llama for scalable image gener-ation,” arXiv preprint arXiv:2406.06525 , 2024. [7] J. Song, C. Meng, and S. Ermon, “Denoising diffusion implicit models,” in ICLR , 2021. [8] C. Lu, Y. Zhou, F. Bao, J. Chen, C. Li, and J. Zhu, “Dpm-solver++: Fast solver for guided sampling of diffusion probabilistic models,” arXiv preprint arXiv:2211.01095 , 2022. [9] P. Dhariwal and A. Nichol, “Diffusion models beat gans on image synthesis,” in NeurIPS , 2021. [10] F. Bao, C. Li, Y. Cao, and J. Zhu, “All are worth words: a vit backbone for score-based diffusion models,” in CVPR , 2023. [11] W. Peebles and S. Xie, “Scalable diffusion models with transformers,” in ICCV , 2023. [12] S. Gao, P. Zhou, M.-M. Cheng, and S. Yan, “Masked diffusion trans-former is a strong image synthesizer,” in ICCV , 2023. [13] N. Ma, M. Goldstein, M. S. Albergo, N. M. Boffi, E. Vanden-Eijnden, and S. Xie, “Sit: Exploring flow and diffusion-based generative models with scalable interpolant transformers,” in ECCV , 2024. [14] Y. Song, C. Durkan, I. Murray, and S. Ermon, “Maximum likelihood training of score-based diffusion models,” in NeurIPS , 2021. [15] C. Lu, Y. Zhou, F. Bao, J. Chen, C. Li, and J. Zhu, “Dpm-solver: A fast ode solver for diffusion probabilistic model sampling in around 10 steps,” in NeurIPS , 2022. [16] E. Liu, X. Ning, H. Yang, and Y. Wang, “A unified sampling framework for solver searching of diffusion probabilistic models,” in ICLR , 2024. [17] J. Lezama, H. Chang, L. Jiang, and I. Essa, “Improved masked image generation with token-critic,” in ECCV , 2022. [18] T. Li, H. Chang, S. Mishra, H. Zhang, D. Katabi, and D. Krishnan, “Mage: Masked generative encoder to unify representation learning and image synthesis,” in CVPR , 2023. [19] P. Esser, R. Rombach, and B. Ommer, “Taming transformers for high-resolution image synthesis,” in CVPR , 2021. [20] J. Yu, Y. Xu, J. Y. Koh, T. Luong, G. Baid, Z. Wang, V. Vasudevan, A. Ku, Y. Yang, B. K. Ayan et al. , “Scaling autoregressive models for content-rich text-to-image generation,” TMLR , 2022. [21] R. Rombach, A. Blattmann, D. Lorenz, P. Esser, and B. Ommer, “High-resolution image synthesis with latent diffusion models,” in CVPR , 2022. [22] X. Liu, C. Gong, and Q. Liu, “Flow straight and fast: Learning to generate and transfer data with rectified flow,” arXiv preprint arXiv:2209.03003 , 2022. [23] J. Xu, X. Liu, Y. Wu, Y. Tong, Q. Li, M. Ding, J. Tang, and Y. Dong, “Imagereward: Learning and evaluating human preferences for text-to-image generation,” in NeurIPS , 2023. [24] I. J. Goodfellow, J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley, S. Ozair, A. Courville, and Y. Bengio, “Generative adversarial nets,” in 

NeurIPS , 2014. [25] O. Russakovsky, J. Deng, H. Su, J. Krause, S. Satheesh, S. Ma, Z. Huang, A. Karpathy, A. Khosla, M. Bernstein et al. , “Imagenet large scale visual recognition challenge,” in IJCV , 2015. [26] T.-Y. Lin, M. Maire, S. Belongie, J. Hays, P. Perona, D. Ramanan, P. Dollár, and C. L. Zitnick, “Microsoft coco: Common objects in context,” in ECCV , 2014. [27] P. Sharma, N. Ding, S. Goodman, and R. Soricut, “Conceptual captions: A cleaned, hypernymed, image alt-text dataset for automatic image captioning,” in ACL , 2018. [28] C. Schuhmann, R. Beaumont, R. Vencu, C. Gordon, R. Wightman, M. Cherti, T. Coombes, A. Katta, C. Mullis, M. Wortsman et al. , “Laion-5b: An open large-scale dataset for training next generation image-text models,” in NeurIPS , 2022. [29] Z. Ni, Y. Wang, R. Zhou, R. Lu, J. Guo, J. Hu, Z. Liu, Y. Yao, and G. Huang, “Adanat: Exploring adaptive policy for token-based image generation.” in ECCV , 2024. [30] T. Karras, S. Laine, and T. Aila, “A style-based generator architecture for generative adversarial networks,” in CVPR , 2019. [31] ——, “Analyzing and improving the image quality of stylegan,” in 

CVPR , 2020. [32] C. Saharia, W. Chan, S. Saxena, L. Li, J. Whang, E. L. Denton, K. Ghasemipour, R. Gontijo Lopes, B. Karagol Ayan, T. Salimans 

et al. , “Photorealistic text-to-image diffusion models with deep language understanding,” in NeurIPS , 2022. [33] M. Kang, J.-Y. Zhu, R. Zhang, J. Park, E. Shechtman, S. Paris, and T. Park, “Scaling up gans for text-to-image synthesis,” in CVPR , 2023. [34] A. Ramesh, P. Dhariwal, A. Nichol, C. Chu, and M. Chen, “Hierarchical text-conditional image generation with clip latents,” arXiv preprint arXiv:2204.06125 , 2022. [35] M. Ding, Z. Yang, W. Hong, W. Zheng, C. Zhou, D. Yin, J. Lin, X. Zou, Z. Shao, H. Yang et al. , “Cogview: Mastering text-to-image generation via transformers,” in NeurIPS , 2021. [36] P. Bachman and D. Precup, “Data generation as sequential decision making,” in NeurIPS , 2015. 17 

[37] K. Black, M. Janner, Y. Du, I. Kostrikov, and S. Levine, “Training diffusion models with reinforcement learning,” in ICLR , 2024. [38] Y. Fan, O. Watkins, Y. Du, H. Liu, M. Ryu, C. Boutilier, P. Abbeel, M. Ghavamzadeh, K. Lee, and K. Lee, “Reinforcement learning for fine-tuning text-to-image diffusion models,” in NeurIPS , 2023. [39] Y. Zhang, E. Tzeng, Y. Du, and D. Kislyuk, “Large-scale reinforcement learning for diffusion models,” arXiv preprint arXiv:2401.12244 , 2024. [40] L. Yu, W. Zhang, J. Wang, and Y. Yu, “Seqgan: Sequence generative adversarial nets with policy gradient,” in AAAI , 2017. [41] J. Wang, L. Yu, W. Zhang, Y. Gong, Y. Xu, B. Wang, P. Zhang, and D. Zhang, “Irgan: A minimax game for unifying generative and discriminative information retrieval models,” in SIGIR , 2017. [42] X. Wang, W. Chen, Y.-F. Wang, and W. Y. Wang, “No metrics are per-fect: Adversarial reward learning for visual storytelling,” arXiv preprint arXiv:1804.09160 , 2018. [43] M. Sarmad, H. J. Lee, and Y. M. Kim, “Rl-gan-net: A reinforcement learning agent controlled gan network for real-time point cloud shape completion,” in CVPR , 2019. [44] Y. Fan and K. Lee, “Optimizing ddpm sampling with shortcut fine-tuning,” arXiv preprint arXiv:2301.13362 , 2023. [45] A. Krizhevsky, G. Hinton et al. , “Learning multiple layers of features from tiny images,” 2009. [46] Z. Liu, P. Luo, X. Wang, and X. Tang, “Deep learning face attributes in the wild,” in ICCV , 2015. [47] D. Watson, W. Chan, J. Ho, and M. Norouzi, “Learning fast samplers for diffusion models by differentiating through sample quality,” arXiv preprint arXiv:2202.05830 , 2022. [48] L. Li, H. Li, X. Zheng, J. Wu, X. Xiao, R. Wang, M. Zheng, X. Pan, F. Chao, and R. Ji, “Autodiffusion: Training-free optimization of time steps and architectures for automated diffusion model acceleration,” in 

ICCV , 2023. [49] Z. Ni, Y. Wang, R. Zhou, J. Guo, J. Hu, Z. Liu, S. Song, Y. Yao, and G. Huang, “Revisiting non-autoregressive transformers for efficient image synthesis,” in CVPR , 2024. [50] E. Liu, X. Ning, Z. Lin, H. Yang, and Y. Wang, “Oms-dpm: Optimizing the model schedule for diffusion probabilistic models,” in ICML , 2023. [51] M. Heusel, H. Ramsauer, T. Unterthiner, B. Nessler, and S. Hochreiter, “Gans trained by a two time-scale update rule converge to a local nash equilibrium,” in NeurIPS , 2017. [52] M. Bi´ nkowski, D. J. Sutherland, M. Arbel, and A. Gretton, “Demysti-fying mmd gans,” arXiv preprint arXiv:1801.01401 , 2018. [53] J. Devlin, M.-W. Chang, K. Lee, and K. Toutanova, “Bert: pre-training of deep bidirectional transformers for language understanding. google ai language,” in NAACL , 2019. [54] J. Ho, A. Jain, and P. Abbeel, “Denoising diffusion probabilistic models,” in NeurIPS , 2020. [55] T. Salimans, I. Goodfellow, W. Zaremba, V. Cheung, A. Radford, and X. Chen, “Improved techniques for training gans,” in NeurIPS , 2016. [56] J. Schulman, F. Wolski, P. Dhariwal, A. Radford, and O. Klimov, “Prox-imal policy optimization algorithms,” arXiv preprint arXiv:1707.06347 ,2017. [57] J. Xu, Y. Huang, J. Cheng, Y. Yang, J. Xu, Y. Wang, W. Duan, S. Yang, Q. Jin, S. Li et al. , “Visionreward: Fine-grained multi-dimensional human preference learning for image and video generation,” arXiv preprint arXiv:2412.21059 , 2024. [58] J. Kober and J. Peters, “Policy search for motor primitives in robotics,” in NeurIPS , 2008. [59] A. Raffin, J. Kober, and F. Stulp, “Smooth exploration for robotic reinforcement learning,” in CoRL , 2022. [60] R. A. Roberts and C. T. Mullis, Digital signal processing . Addison-Wesley Longman Publishing Co., Inc., 1987. [61] B. Brown, J. Juravsky, R. Ehrlich, R. Clark, Q. V. Le, C. Ré, and A. Mirhoseini, “Large language monkeys: Scaling inference compute with repeated sampling,” arXiv preprint arXiv:2407.21787 , 2024. [62] N. Ma, S. Tong, H. Jia, H. Hu, Y.-C. Su, M. Zhang, X. Yang, Y. Li, T. Jaakkola, X. Jia et al. , “Inference-time scaling for diffusion models beyond scaling denoising steps,” arXiv preprint arXiv:2501.09732 , 2025. [63] E. Xie, J. Chen, J. Chen, H. Cai, H. Tang, Y. Lin, Z. Zhang, M. Li, L. Zhu, Y. Lu, and S. Han, “Sana: Efficient high-resolution image synthe-sis with linear diffusion transformers,” arXiv preprint arXiv:2410.10629 ,2024. [64] A. Sauer, T. Karras, S. Laine, A. Geiger, and T. Aila, “Stylegan-t: Un-locking the power of gans for fast large-scale text-to-image synthesis,” in International conference on machine learning . PMLR, 2023, pp. 30 105–30 118. [65] A. Razavi, A. van den Oord, and O. Vinyals, “Generating diverse high-fidelity images with vq-vae-2,” 2019. [66] S. Gu, D. Chen, J. Bao, F. Wen, B. Zhang, D. Chen, L. Yuan, and B. Guo, “Vector quantized diffusion model for text-to-image synthesis,” in CVPR , 2022. [67] J. Yu, X. Li, J. Y. Koh, H. Zhang, R. Pang, J. Qin, A. Ku, Y. Xu, J. Baldridge, and Y. Wu, “Vector-quantized image modeling with im-proved vqgan,” arXiv preprint arXiv:2110.04627 , 2021. [68] D. Lee, C. Kim, S. Kim, M. Cho, and W. S. HAN, “Draft-and-revise: Effective image generation with contextual rq-transformer,” in NeurIPS ,2022. [69] E. Hoogeboom, J. Heek, and T. Salimans, “simple diffusion: End-to-end diffusion for high resolution images,” in ICML , 2023. [70] A. Jabri, D. Fleet, and T. Chen, “Scalable adaptive computation for iterative generation,” arXiv preprint arXiv:2212.11972 , 2022. [71] D. Kingma and R. Gao, “Understanding diffusion objectives as the elbo with simple data augmentation,” in NeurIPS , 2023. [72] L. Yu, J. Lezama, N. B. Gundavarapu, L. Versari, K. Sohn, D. Minnen, Y. Cheng, A. Gupta, X. Gu, A. G. Hauptmann et al. , “Language model beats diffusion–tokenizer is key to visual generation,” in ICLR , 2024. [73] F. Mentzer, D. Minnen, E. Agustsson, and M. Tschannen, “Finite scalar quantization: Vq-vae made simple,” in ICLR , 2023. [74] T. Li, D. Katabi, and K. He, “Return of unconditional generation: A self-supervised representation generation method,” in NeurIPS , 2024. [75] T. Karras, M. Aittala, J. Lehtinen, J. Hellsten, T. Aila, and S. Laine, “Analyzing and improving the training dynamics of diffusion models,” in CVPR , 2024. [76] A. Hatamizadeh, J. Song, G. Liu, J. Kautz, and A. Vahdat, “Diffit: Diffusion vision transformers for image generation,” in ECCV , 2024. [77] P. Gao, L. Zhuo, D. Liu, R. Du, X. Luo, L. Qiu, Y. Zhang, C. Lin, R. Huang, S. Geng et al. , “Lumina-t2x: Transforming text into any modality, resolution, and duration via flow-based large diffusion trans-formers,” arXiv preprint arXiv:2405.05945 , 2024. [78] H. Zheng, W. Nie, A. Vahdat, and A. Anandkumar, “Fast train-ing of diffusion models with masked transformers,” arXiv preprint arXiv:2306.09305 , 2023. [79] W. Zhao, Y. Han, J. Tang, K. Wang, Y. Song, G. Huang, F. Wang, and Y. You, “Dynamic diffusion transformer,” arXiv preprint arXiv:2410.03456 , 2024. [80] W.-C. Fan, Y.-C. Chen, D. Chen, Y. Cheng, L. Yuan, and Y.-C. F. Wang, “Frido: Feature pyramid diffusion for complex scene image synthesis,” in AAAI , 2023. [81] O. Gafni, A. Polyak, O. Ashual, S. Sheynin, D. Parikh, and Y. Taig-man, “Make-a-scene: Scene-based text-to-image generation with human priors,” in ECCV , 2022. [82] P. Esser, R. Rombach, A. Blattmann, and B. Ommer, “Imagebart: Bidi-rectional context with multinomial diffusion for autoregressive image synthesis,” in NeurIPS , 2021. [83] Y. Zhou, R. Zhang, C. Chen, C. Li, C. Tensmeyer, T. Yu, J. Gu, J. Xu, and T. Sun, “Lafite: Towards language-free training for text-to-image generation. arxiv,” arXiv preprint arXiv:2111.13792 , 2022. [84] M. Ding, W. Zheng, W. Hong, and J. Tang, “Cogview2: Faster and better text-to-image generation via hierarchical transformers,” in NeurIPS ,2022. [85] A. Nichol, P. Dhariwal, A. Ramesh, P. Shyam, P. Mishkin, B. McGrew, I. Sutskever, and M. Chen, “Glide: Towards photorealistic image gen-eration and editing with text-guided diffusion models,” arXiv preprint arXiv:2112.10741 , 2021. [86] Y. Zhou, B. Liu, Y. Zhu, X. Yang, C. Chen, and J. Xu, “Shifted diffusion for text-to-image generation,” in CVPR , 2023. [87] T. Karras, S. Laine, M. Aittala, J. Hellsten, J. Lehtinen, and T. Aila, “Analyzing and improving the image quality of stylegan,” in CVPR ,2019. [88] W. Zhao, Y. Rao, Z. Liu, B. Liu, J. Zhou, and J. Lu, “Unleashing text-to-image diffusion models for visual perception,” in ICCV , 2023. [89] D. P. Kingma and J. Ba, “Adam: A method for stochastic optimization,” in ICLR , 2015. 

APPENDIX 

APPENDIX AEXPERIMENT DETAILS OF SECTION III For the experiments in Section III-C, we use the MaskGIT-L model on ImageNet 256 ×256 (see Section IV-B for details). In the reward design part of Section III-D, we use T = 8 

inference steps. When implementing FID-based reward design, 18 

we find that the FID-based reward model is not able to provide a stable and effective reward signal for the adaptive policy network, leading to divergence:       

> FID-50K ↓
> Model TAdaptive Non-adaptive AdaGen-MaskGIT-L 855.4 (Fail) 2.56

As a result, we adopted a non-adaptive version of the policy network, where all samples share the same generation con-figuration. Empirically, the non-adaptive policy network can also be optimized effectively with a low FID. However, as discussed in Section III-D, this numerical superiority does not translate to a practical advantage in terms of sample quality. The FID reward-based policy network fails to produce images of satisfactory quality. In Section III-E, we use T = 8 and T = 32 inference steps for the MaskGIT-L model to study its behavior. For the case with T = 32 , the stabilized policy network adopts an action smoothing factor of β = 0 .8, as suggested by the ablation study in Table XIV. APPENDIX BIMPLEMENTATION DETAILS 

Proximal Policy Optimization (PPO) algorithm. The Prox-imal Policy Optimization (PPO) algorithm offers a balance between sample efficiency and ease of implementation. In this section, we elaborate in more detail on the PPO algorithm adopted in our paper. First, consider a surrogate objective: 

LCPI (ϕ) = Et

 πϕ(at|st)

πϕold (at|st) ˆAt



,

where πϕ and πϕold are the policy network before and after the update, respectively. The advantage estimator ˆAt is computed by: ˆAt = −Vϕ(st) + R(sT , aT ),

where Vϕ(st) is a learned state-value function. This objective function effectively maximizes the probability ratio ρt(ϕ) =   

> πϕ(at|st)
> πϕold (at|st)

when considering the advantage of taking action 

at in state st. However, directly maximizing LCPI (ϕ) usually leads to an excessively large policy update. Hence, we consider how to modify the objective, to penalize changes to the policy that move ρt(ϕ) away from 1. This gives rise to the clipped surrogate objective: 

LCLIP (ϕ) = Et

"

min 



ρt(ϕ) ˆAt, clip (ρt(ϕ), 1 − ϵ, 1 + ϵ) ˆAt

 #

where ϵ is a hyper-parameter that controls the range of the probability ratio. Finally, to learn the state-value function, we additionally add a term for value function estimation error to the objective following [56], which results in the objective in Eq. (8) in our main paper. 

Hyperparameter Details. We perform the optimization loop of AdaGen outlined in Algorithm 1 for 1000 iterations. In practice, we perform 5 gradient updates within each iteration of Algorithm 1 for both the policy network and the adversarial reward model to facilitate a more stable and efficient opti-mization process in the minimax game. For the optimization of the policy network, we use the Adam optimizer [89] with a learning rate of 1 × 10 −5 and betas (β1, β 2) = (0 .9, 0.999) .The clipping parameter in the PPO objective is set to ϵ = 0 .2,and the value function coefficient is c = 0 .5. The exploration-exploitation hyperparameter σ is set to 0.6. The batch size for policy updates is 4096. For the adversarial reward model, we use the Adam optimizer [89] with a learning rate of 1 × 10 −4

and betas (β1, β 2) = (0 .5, 0.999) . The default batch size is 1024. For experiments on ImageNet 512 ×512 [25] and LAION-5B [28], we reduce the batch size to 512 to fit the memory constraints. The results on CC3M are based on a publicly available Muse model from github 2.

Implementation Details of Policy Network. We introduce activation functions at the end of the policy network to map the raw, unbounded output to the valid range of each generation policy. Table XV details the specific activation function we used for each generation policy. Before applying the activation, we also employ the heuristics listed in Table I as initialization for more effective policy optimization. APPENDIX CADDITIONAL QUALITATIVE RESULTS 

A. Qualitative Results of Inference-time Refinement 

Figure 16 presents a qualitative comparison between images generated by AdaGen-MaskGIT ( T = 32 ) and their refined counterparts obtained through our inference-time refinement strategy. The top row displays the initial outputs of AdaGen-MaskGIT, while the bottom row illustrates the enhanced visual quality achieved after applying our proposed refinement techniques. These visual results corroborate the quantitative improvements reported in Section IV-D, demonstrating the capability of our refinement strategy to generate images with higher visual quality. AdaGen -MaskGIT (FID=2.28)   

> AdaGen -MaskGIT, w/ Refine (FID=1.81)
> Fig. 16. Generated samples without vs . with refinement of AdaGen.

B. More Generated Samples 

Here we present more generated samples of AdaGen on ImageNet 256 ×256, ImageNet 512 ×512, and text-to-image tasks. Figure 17 shows the generated samples of AdaGen-VAR-d30 ( T = 10 ) on ImageNet 256 ×256. Figure 18 shows 

> 2https://github.com/baaivision/MUSE-Pytorch

19 

TABLE XV SUMMARY OF ACTIVATION FUNCTIONS FOR DIFFERENT GENERATION POLICIES IN ADA GEN . T HE POLICY NETWORK PRODUCES AN UNBOUNDED OUTPUT x, WHICH IS THEN PASSED THROUGH AN ACTIVATION FUNCTION TO ENSURE THE RESULTING POLICY VALUE LIES WITHIN ITS VALID RANGE .Type Generation Policy Range Activation Function MaskGIT Mask ratio, mt mt ∈ [0 , 1] sigmoid (x)

Sampling temperature, τt τt ≥ 0 softplus (x)

Mask temperature, ζt ζt ≥ 0 softplus (x)

Guidance scale, wt wt ≥ 0 softplus (x)

Autoregressive Sampling temperature, τt τt ≥ 0 softplus (x)

Guidance scale, wt wt ≥ 0 softplus (x)

Top-k parameter, kt kt ∈ N+ round (1 + softplus (x)) 

Top-p parameter, ρt ρt ∈ [0 , 1] sigmoid (x)

Diffusion (ODE) Timestep, κt κt ∈ { 0, . . . , κ max } round (κmax · sigmoid (x)) 

Guidance scale, wt wt ≥ 0 softplus (x)

Rectified Flow Timestep, κt κt ∈ [0 , 1] sigmoid (x)

Guidance scale, wt wt ≥ 0 softplus (x)

the generated samples of AdaGen-MaskGIT-L ( T = 32 ) on ImageNet 512 ×512. Figure 19 shows the generated samples of AdaGen-Stable-Diffusion ( T = 32 ) for text-to-image gen-eration. 20 

Fig. 17. Generated samples of AdaGen-VAR-d30 ( T = 10 ) on ImageNet 256 ×256. 21 

Fig. 18. Generated samples of AdaGen-MaskGIT-L ( T = 32 ) on ImageNet 512 ×512. 22 Fallout 5 set in Budapest, 

game screenshot, 4k, highly 

detailed. 

A sandwich , high resolution 

food photography, studio 

lighting. 

Floating futuristic city in the 

skies, ArtStation concept art, 

highly detailed. 

Matt Berry as Batman, highly 

detailed face. 

Sculpture made of recycled 

materials, detailed. 

Photo of candy corn pizza. A beautiful painting of a little 

cottage. 

A retro -futuristic racecar 

concept, steampunk, studio 

lighting, award -winning car 

magazine photography. 

The lobby of a class a 

downtown office building. 

Warehouses lining a street, 

with an autumn mountain 

directly behind, lens 

compressed, photography. 

A room, sunlight, glass walls, 

water levels, trees, shrubs, 

flowers, warm and calm .

Warrior character portrait 

revenant old male hobbit 

fantasy .

Photo of a sakura tree, high 

detail, cinematic, Unreal 

Engine, realistic, 8k, beautiful. 

A cyborg dog with a beautiful, 

intricate, detailed, elegant, 

ornate exoskeleton. 

An isometric photo of a real 

food product. 

Dessert photography.