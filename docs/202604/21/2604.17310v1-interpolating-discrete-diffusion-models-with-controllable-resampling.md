---
title: Interpolating Discrete Diffusion Models with Controllable Resampling
title_zh: 具有可控重采样的插值离散扩散模型
authors: "Marcel Kollovieh, Sirine Ayadi, Stephan Günnemann"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17310v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于文本生成的插值离散扩散模型
tldr: 针对离散扩散模型中掩码模型不可逆错误和均匀模型采样质量低的问题，本文提出插值离散扩散模型（IDDM）。IDDM引入可控重采样机制，通过在当前状态、先验重采样和目标状态间进行插值，减少了对中间潜状态的依赖，有效缓解了误差累积并增强了Token纠错能力。在分子图和文本生成任务中，IDDM表现出极具竞争力的性能。
selection_source: fresh_fetch
motivation: 现有的离散扩散模型在处理早期去掩码错误和中间状态依赖方面存在局限，导致生成质量受限。
method: 提出IDDM模型，通过可控重采样机制在生成过程中插值不同状态转换，实现训练与推理的完全解耦。
result: 在分子图生成和文本生成任务的基准测试中，IDDM达到了与当前最先进模型相当的竞争性能。
conclusion: IDDM通过减少对中间状态的依赖和引入重采样，为离散扩散模型提供了一种更稳健且高效的生成方案。
---

## 摘要
离散扩散模型在文本和图等多个领域中构成了一类强大的生成模型。然而，现有方法面临着根本性的局限。掩码扩散模型由于早期去掩码而产生不可逆的错误，而均匀扩散模型虽然支持自我修正，但由于对中间潜状态的强烈依赖，往往导致样本质量较低。我们提出了 IDDM，一种插值离散扩散模型，它通过减少对中间潜状态的依赖来改进扩散过程。IDDM 的核心是一种可控重采样机制，该机制将部分概率质量重置为边缘分布，从而减轻误差累积并实现更有效的标记修正。IDDM 定义了一个生成过程，其转移在保持当前状态、从先验分布重采样以及向目标状态翻转之间进行插值，同时强制执行边缘一致性，并实现了训练与推理的完全解耦。我们在分子图生成和文本生成任务上将该模型与最先进的离散扩散模型进行了对比，证明了其具有竞争力的性能。

## Abstract
Discrete diffusion models form a powerful class of generative models across diverse domains, including text and graphs. However, existing approaches face fundamental limitations. Masked diffusion models suffer from irreversible errors due to early unmasking, while uniform diffusion models, despite enabling self-correction, often yield low-quality samples due to their strong reliance on intermediate latent states. We introduce IDDM, an Interpolating Discrete Diffusion Model, that improves diffusion by reducing dependence on intermediate latent states. Central to IDDM is a controllable resampling mechanism that partially resets probability mass to the marginal distribution, mitigating error accumulation and enabling more effective token corrections. IDDM specifies a generative process whose transitions interpolate between staying at the current state, resampling from a prior, and flipping toward the target state, while enforcing marginal consistency and fully decoupling training from inference. We benchmark our model against state-of-the-art discrete diffusion models across molecular graph generation as well as text generation tasks, demonstrating competitive performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Interpolating Discrete Diffusion Models with Controllable Resampling》（具有可控重采样的插值离散扩散模型）的结构化总结：

### 1. 核心问题与整体含义
该论文针对**离散扩散模型（Discrete Diffusion Models）**在生成任务（如文本和分子图）中的两个核心局限性提出了改进方案：
*   **掩码扩散模型（Masked/Absorbing Diffusion）的不可逆性**：一旦某个标记（Token）被取消掩码，它在后续步骤中就无法更改，导致早期错误会持续累积。
*   **均匀扩散模型（Uniform Diffusion）的低质量**：虽然允许自我修正，但由于过度依赖中间潜状态（Latent States），往往导致生成样本质量不佳。
*   **研究动机**：开发一种能够减少对中间状态依赖、支持推理时灵活调整重采样频率、且能有效纠正早期错误的通用离散生成框架。

### 2. 方法论
论文提出了 **IDDM（Interpolating Discrete Diffusion Model）**，其核心思想是直接定义生成过程（逆向过程）的转移概率，而非从预定义的马尔可夫前向过程推导。
*   **核心机制（三种动作）**：在每一个生成步骤中，模型在三个动作之间进行插值：
    1.  **保持（Stay）**：保留当前状态。
    2.  **翻转（Flip）**：向模型预测的目标状态（$x_0$）转换。
    3.  **重采样（Resample）**：从先验分布（Prior）中重新采样。
*   **关键技术细节**：
    *   **边缘一致性约束**：通过数学约束确保每一时刻的边缘分布符合先验分布与数据分布之间的插值。
    *   **遗忘参数（$\lambda$）**：引入超参数 $\lambda \in [0, 1]$。当 $\lambda=0$ 时，模型类似于吸收态扩散；当 $\lambda=1$ 时，模型完全丢弃当前状态进行重置。这允许在推理时动态权衡“状态保持”与“随机探索”。
    *   **训练与推理脱钩**：模型训练时只需学习预测原始数据 $x_0$，而推理时的重采样频率（$\lambda$）和时间步长分布（$\rho$）可以自由调节，无需重新训练。

### 3. 实验设计
*   **数据集**：
    *   **分子图生成**：QM9（小分子）、GuacaMol 和 MOSES（大分子）。
    *   **文本生成**：LM1B（十亿词语数据集）和 OpenWebText（OWT）。
*   **Benchmark 与对比方法**：
    *   **图生成**：对比了 DiGress、DisCo、Cometh 和 DeFoG 等最先进的图扩散/流匹配模型。
    *   **文本生成**：对比了 D3PM、SEDD、MDLM、Duo 等离散扩散模型以及自回归（Autoregressive）基准。
*   **评估指标**：分子合法性（Validity）、独特性（Unique）、FCD（分子相似度距离）、困惑度（Perplexity）、生成困惑度（Gen PPL）和熵（Entropy）。

### 4. 资源与算力
*   **模型规模**：LM1B 实验使用了 1.39 亿参数的 Transformer，OpenWebText 使用了 1.69 亿参数。
*   **训练细节**：LM1B 训练了 100 万步，OWT 训练了 15 万步。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长（通常此类规模的模型需要多块 A100 或 H100 级别的显卡训练数日）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的分子图到复杂的长文本生成，实验设计较为全面。
*   **消融实验**：详细探讨了遗忘参数 $\lambda$、时间扭曲参数 $\rho$ 以及采样步数 $T$ 对结果的影响。
*   **客观性**：通过在多个标准基准上与 SOTA 模型对比，并展示了在减少采样步数（如仅用 10% 步数）时的性能表现，证明了方法的鲁棒性和效率。实验设计遵循了学术界的标准划分和评估流程，具有较高的公平性。

### 6. 主要结论与发现
*   **性能领先**：IDDM 在分子图生成任务中达到了 SOTA 水平，特别是在 FCD 指标上表现优异。
*   **灵活的纠错能力**：通过调节 $\lambda$，IDDM 能够比传统的吸收态模型更有效地修正生成过程中的早期错误。
*   **推理效率**：在减少采样步数的情况下，IDDM 的性能下降比同类模型更缓慢，显示出极高的采样效率。
*   **理论贡献**：证明了 IDDM 对应的实际上是一个非马尔可夫的前向过程。

### 7. 优点
*   **高度可控性**：推理时可调的 $\lambda$ 参数为用户提供了在“确定性生成”和“多样性探索”之间平衡的手段。
*   **训练简单**：无需复杂的马尔可夫链推导，训练目标清晰（预测 $x_0$）。
*   **通用性强**：同一套框架可无缝应用于图结构数据和序列文本数据。

### 8. 不足与局限
*   **文本生成的微小差距**：虽然在均匀扩散模型中表现出色，但在文本生成的困惑度（PPL）指标上，仍略逊于专门优化的吸收态模型（如 MDLM）。
*   **先验分布依赖**：目前主要探索了均匀先验和边缘先验，对于更复杂的先验分布（如基于掩码的先验）的潜力挖掘尚不充分。
*   **超参数敏感性**：虽然 $\lambda$ 提供了灵活性，但也增加了推理时寻找最优超参数组合的负担。

（完）
