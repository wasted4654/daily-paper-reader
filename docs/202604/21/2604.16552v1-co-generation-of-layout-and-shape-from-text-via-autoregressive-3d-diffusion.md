---
title: Co-generation of Layout and Shape from Text via Autoregressive 3D Diffusion
title_zh: 基于自回归 3D 扩散的文本驱动布局与形状协同生成
authors: "Zhenggang Tang, Yuehao Wang, Yuchen Fan, Jun-Kun Chen, Yu-Ying Yeh, Kihyuk Sohn, Zhangyang Wang, Qixing Huang, Alexander Schwing, Rakesh Ranjan, Dilin Wang, Zhicheng Yan"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16552v1"
tags: ["query:ardm"]
score: 10.0
evidence: 用于场景生成的 3D 自回归扩散模型 (3D-ARD+)
tldr: 本研究针对现有文本生成场景方法在布局与物体生成上难以兼顾且一致性差的问题，提出了3D-ARD+模型。该模型采用自回归3D扩散范式，通过多模态Token序列统一了布局与形状的生成。它先在场景空间生成粗粒度潜变量确定位置，再在物体空间生成细粒度潜变量确定几何与外观。基于230K大规模室内场景数据集，该模型实现了复杂空间布局与语义指令的高度一致性。
selection_source: fresh_fetch
motivation: 现有方法通常无法同时生成高质量的场景布局和物体形状，且难以处理包含复杂空间关系和细节描述的文本指令。
method: 提出一种自回归3D扩散模型3D-ARD+，通过两步走策略分别在场景空间和物体空间生成粗、细粒度的3D潜变量。
result: 在23万个室内场景的大规模数据集上验证了7B参数模型，展示了其根据复杂指令生成语义一致且布局合理的3D场景的能力。
conclusion: 该研究通过结合自回归与扩散生成的优势，为实现交互式、高保真度的文本到3D场景生成提供了全新的范式。
---

## 摘要
最近的文本到场景生成方法显著减少了创建 3D 场景所需的人工。然而，这些方法的研究重点通常要么是生成场景布局，要么是生成物体，很少有方法能同时生成两者。即使在 LLM 的帮助下，生成的场景布局往往也较为简单。此外，当文本输入包含关于物体形状、外观和空间排列的复杂描述时，生成的场景往往与其不一致。我们提出了一种顺序文本到场景生成的新范式，并为交互式场景创建设计了一种新颖的生成模型。其核心是一个 3D 自回归扩散模型 3D-ARD+，它将多模态标记序列上的自回归生成与下一个物体 3D 潜变量的扩散生成统一起来。为了生成下一个物体，该模型首先以当前文本指令和已合成的 3D 场景为条件，通过一个自回归步骤在场景空间中生成粗粒度的 3D 潜变量；随后在较小的物体空间中生成 3D 潜变量，并将其解码为细粒度的物体几何形状和外观。我们整理了一个包含 23 万个室内场景及配对文本指令的大型数据集用于训练。我们在挑战性场景中对 7B 规模的 3D-ARD+ 进行了评估，结果表明该模型能够遵循文本指令中规定的复杂空间布局和语义来生成并放置物体。

## Abstract
Recent text-to-scene generation approaches largely reduced the manual efforts required to create 3D scenes. However, their focus is either to generate a scene layout or to generate objects, and few generate both. The generated scene layout is often simple even with LLM's help. Moreover, the generated scene is often inconsistent with the text input that contains non-trivial descriptions of the shape, appearance, and spatial arrangement of the objects. We present a new paradigm of sequential text-to-scene generation and propose a novel generative model for interactive scene creation. At the core is a 3D Autoregressive Diffusion model 3D-ARD+, which unifies the autoregressive generation over a multimodal token sequence and diffusion generation of next-object 3D latents. To generate the next object, the model uses one autoregressive step to generate the coarse-grained 3D latents in the scene space, conditioned on both the current seen text instructions and already synthesized 3D scene. It then uses a second step to generate the 3D latents in the smaller object space, which can be decoded into fine-grained object geometry and appearance. We curate a large dataset of 230K indoor scenes with paired text instructions for training. We evaluate 7B 3D-ARD+, on challenging scenes, and showcase the model can generate and place objects following non-trivial spatial layout and semantics prescribed by the text instructions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **3D-ARD+** 的新型 3D 场景生成模型，旨在通过文本指令实现场景布局与物体形状的协同生成。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：现有的文本驱动 3D 场景生成方法存在“脱节”现象：要么侧重于生成宏观布局（通常过于简单），要么侧重于生成单个物体。当文本包含复杂的空间关系（如“将台灯放在右侧床头柜的后方角落”）或精细的形状描述时，现有模型难以保证生成结果与文本的高度一致性。
*   **研究动机**：为了实现交互式的场景创建，需要一种能够理解顺序指令、同时处理空间排列（布局）和几何细节（形状）的统一模型。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出**自回归 3D 扩散（3D Autoregressive Diffusion）**范式。将场景生成视为一个序列过程，每一步根据当前指令和已生成的场景，预测下一个物体的 3D 潜变量。
*   **关键技术细节**：
    *   **多模态 Token 序列**：模型输入包含三种 Token：**文本 Token**（指令）、**3D 理解 Token**（已存在物体的占用体信息）和 **3D 生成 Token**（当前步骤要生成的物体）。
    *   **两阶段生成（3D-ARD+）**：
        1.  **场景空间生成（粗粒度）**：在 64³ 的全局场景体素中确定物体的位置和大致轮廓。
        2.  **物体空间生成（细粒度）**：在物体局部的 64³ 空间内生成高分辨率潜变量，捕捉精细几何和外观。
    *   **广义因果注意力机制**：在 Transformer 架构中，文本 Token 遵循标准因果掩码，而 3D Token 之间允许无限制的相互注意，以增强空间理解。
    *   **算法流程**：采用 **Rectified Flow（整流流）** 匹配目标进行训练。推理时，通过 3D VAE 解码器将潜变量转为体素，再利用 3D 高斯泼溅（3DGS）解码器生成最终的精细资产。

### 3. 实验设计
*   **数据集**：
    *   **230K 室内场景**：涵盖卧室、客厅、厨房等，每个场景配有由 VLM 生成的步骤化组装指令。
    *   **1M 单物体资产**：用于增强物体多样性。
*   **Benchmark 与对比方法**：
    *   在 50 组极具挑战性的未见指令集上进行评估。
    *   **对比基线**：包括 **TRELLIS-text XL**（最先进的单物体生成模型）、**MIDI**（多实例扩散模型）、**LLM-layout+TRELLIS**（利用大模型预测布局再填充物体）等。
*   **评估指标**：KID/KDD（视觉质量）、CLIP-text/image（语义一致性）、P-FID/P-IS（3D 点云质量）。

### 4. 资源与算力
*   **模型规模**：7B 参数（基于 Bagel 多模态模型微调）。
*   **算力投入**：使用了 **128 张 Nvidia H100 GPU**。
*   **训练时长**：训练了约 **120K 步**，耗时约 **一周**。
*   **推理配置**：使用 Euler 采样器，50 个采样步。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量对比（5 项核心指标）和定性视觉对比。
*   **消融实验**：
    *   对比了 3D-ARD（仅粗粒度）与 3D-ARD+（带细粒度细化）的效果。
    *   验证了物体级训练数据的重要性。
    *   探讨了 3D Token 的 Patch Size（分块大小）对生成质量的影响（发现 Patch Size=1 优于 2）。
*   **充分性评价**：实验设计较为全面，涵盖了从单物体到多物体复杂组合的多种场景，对比基线涵盖了当前主流的不同技术路线，具有较强的说服力。

### 6. 主要结论与发现
*   **协同生成的优越性**：3D-ARD+ 在处理复杂空间关系（如堆叠、靠左、特定角落放置）方面显著优于“LLM 布局预测 + 独立物体生成”的组合。
*   **细化阶段的必要性**：增加物体空间的细化步骤能有效消除粗粒度生成带来的纹理伪影和几何空洞。
*   **自回归的灵活性**：模型支持交互式生成，用户可以随时给定一个初始物体，让模型续写后续场景。

### 7. 优点（亮点）
*   **范式创新**：将自回归预测与扩散生成完美结合，解决了 3D 场景生成中“全局布局”与“局部细节”难以兼顾的痛点。
*   **高质量数据管线**：开发了一套自动化的 VLM 数据标注流程，将静态 3D 场景转化为动态的“组装指令集”，这对社区具有重要价值。
*   **语义对齐度高**：能够精准理解非平凡的空间方位词，生成的场景在逻辑和物理位置上更合理。

### 8. 不足与局限
*   **物理冲突风险**：尽管布局合理，但有时仍会出现物体间的轻微重叠或缝隙，缺乏显式的物理约束。
*   **初始缩放偏差**：如果第一个生成的物体（如沙发）占据了过大空间，可能会导致后续物体无法按指令放入场景。
*   **推理效率**：由于采用了 7B 规模的 Transformer 架构，生成复杂场景的时间成本（比 TRELLIS 等模型慢 5-10 倍）较高，实时交互性受限。
*   **实验覆盖**：目前主要集中在室内场景，室外或超大规模场景的泛化能力尚未验证。

（完）
