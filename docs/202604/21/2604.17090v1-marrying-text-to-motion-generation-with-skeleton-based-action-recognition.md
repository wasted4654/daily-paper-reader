---
title: Marrying Text-to-Motion Generation with Skeleton-Based Action Recognition
title_zh: 将文本生成动作与基于骨架的动作识别相结合
authors: "Jidong Kuang, Hongsong Wang, Jie Gui"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17090v1"
tags: ["query:ardm"]
score: 9.0
evidence: 基于坐标的自回归运动扩散模型 (CoAMD)
tldr: 本文旨在统一动作识别与运动生成这两个长期分离的研究领域。作者提出了CoAMD（基于坐标的自回归运动扩散模型），利用骨骼坐标进行从粗到细的运动合成。其核心组件MAR（多模态动作识别器）为生成过程提供梯度语义引导。该模型在动作识别、文本生成运动、检索和编辑四个任务上表现卓越，在13个基准测试中均达到SOTA水平，展示了统一建模的强大潜力。
selection_source: fresh_fetch
motivation: 针对现有研究将动作识别与运动生成割裂的问题，探索两者在语义理解上的内在关联并实现统一建模。
method: 提出CoAMD模型，利用多模态动作识别器（MAR）为自回归扩散过程提供梯度语义引导，实现基于坐标的运动生成。
result: 在动作识别、文本生成运动等四个任务的13个基准测试中均取得了最先进的性能表现。
conclusion: 实验证明了将运动理解与生成任务统一在同一框架下的有效性，为人体运动建模提供了更具通用性的方案。
---

## 摘要
人体动作识别和动作生成是人体中心计算机视觉中两个活跃的研究课题，两者都旨在将动作与文本语义对齐。然而，大多数现有工作分别研究这两个问题，未能揭示它们之间的联系，即动作生成需要语义理解。本研究通过利用骨架坐标进行动作理解和生成，探讨了统一的动作识别与动作生成。我们提出了基于坐标的自回归动作扩散模型（CoAMD），该模型以由粗到精的方式合成动作。作为 CoAMD 的核心组件，我们设计了一个多模态动作识别器（MAR），为动作生成提供基于梯度的语义引导。此外，我们通过在绝对坐标上评估基准模型，建立了一个严谨的基准测试。我们的模型可应用于四个重要任务，包括基于骨架的动作识别、文本生成动作、文本-动作检索和动作编辑。在这些任务的 13 个基准测试上进行的广泛实验表明，我们的方法达到了最先进的性能，突显了其在人体动作建模方面的有效性和通用性。

## Abstract
Human action recognition and motion generation are two active research problems in human-centric computer vision, both aiming to align motion with textual semantics. However, most existing works study these two problems separately, without uncovering the links between them, namely that motion generation requires semantic comprehension. This work investigates unified action recognition and motion generation by leveraging skeleton coordinates for both motion understanding and generation. We propose Coordinates-based Autoregressive Motion Diffusion (CoAMD), which synthesizes motion in a coarse-to-fine manner. As a core component of CoAMD, we design a Multi-modal Action Recognizer (MAR) that provides gradient-based semantic guidance for motion generation. Furthermore, we establish a rigorous benchmark by evaluating baselines on absolute coordinates. Our model can be applied to four important tasks, including skeleton-based action recognition, text-to-motion generation, text-motion retrieval, and motion editing. Extensive experiments on 13 benchmarks across these tasks demonstrate that our approach achieves state-of-the-art performance, highlighting its effectiveness and versatility for human motion modeling. Code is available at https://github.com/jidongkuang/CoAMD.

---

## 论文详细总结（自动生成）

这篇论文《Marrying Text-to-Motion Generation with Skeleton-Based Action Recognition》由东南大学的研究团队提出，旨在打破人体动作识别（理解）与动作生成（合成）之间的壁垒。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长期以来，人体动作识别（Action Recognition）和文本生成动作（Text-to-Motion）被视为两个独立的任务。识别侧重于从动作到语义的映射，而生成侧重于从语义到动作的映射。
*   **研究动机**：作者认为生成任务本质上需要深层的语义理解，而识别任务提供的结构化知识可以引导生成过程。
*   **整体含义**：本文提出了一个统一的框架，通过**绝对坐标**作为共同的表征基础，利用一个强大的“识别器”作为“导师”，在生成过程中提供实时的语义引导，从而实现双向的任务互促。

### 2. 方法论
*   **核心思想**：提出**基于坐标的自回归运动扩散模型 (CoAMD)**，并引入**多模态动作识别器 (MAR)** 提供梯度引导。
*   **关键技术细节**：
    1.  **多模态动作表征**：将人体骨架分解为三个流：**关节 (Joints)**、**骨骼 (Bones)** 和 **运动动态 (Motion)**。这种分解能更好地捕捉人体运动的运动学结构和时序变化。
    2.  **多模态动作识别器 (MAR)**：基于 Transformer 架构，利用对比学习（InfoNCE Loss）在细粒度文本描述和高层动作类别上进行预训练。
    3.  **自回归扩散生成 (CoAMD)**：
        *   使用多模态自编码器（AE）将动作映射到潜空间。
        *   采用掩码自回归 Transformer 处理上下文。
        *   利用扩散模型（流匹配 Flow-matching 策略）填充掩码部分的动作。
    4.  **采样中的语义引导**：在推理阶段，MAR 计算当前生成动作与目标文本的对齐分数，通过计算该分数的梯度来修正扩散模型的采样轨迹，确保生成的动作在语义上高度准确。

### 3. 实验设计
*   **数据集**：
    *   **生成与检索**：HumanML3D、KIT-ML。
    *   **识别与泛化**：NTU-RGB+D 60、NTU-RGB+D 120。
*   **Benchmark 与对比方法**：
    *   作者建立了一个严谨的基准，将所有基准方法（如 MDM, MotionDiffuse, MoMask, MARDM, ACMDM 等）统一在**绝对坐标**下重新评估，解决了以往评估标准不一致的问题。
    *   对比了 13 个基准测试，涵盖生成、识别、检索和编辑四个任务。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$，潜空间维度为 512，Transformer 为 6 层。文中未明确提及总训练时长，但给出了详细的超参数设置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个不同的基准测试，实验组数非常多。
*   **消融实验**：详细探讨了多模态表征（J+B+M）的贡献、语义引导机制的有效性以及自回归架构的必要性。
*   **客观性与公平性**：
    *   通过重新实现基准方法并统一坐标系，确保了对比的公平性。
    *   引入了**用户研究 (User Study)**，邀请 20 位参与者对动作质量和语义一致性进行主观评价，增强了实验的说服力。

### 6. 主要结论与发现
*   **统一建模的优越性**：将识别器集成到生成循环中，能显著提升生成动作的 FID（真实度）和语义对齐度。
*   **多模态表征的重要性**：相比单一的关节坐标，加入骨骼和运动流能显著提升识别准确率（尤其在少样本类别上）和生成质量。
*   **迭代引导的必要性**：在自回归采样的每一步进行梯度引导，比单次修正或重排序（Re-ranking）更有效，能防止语义漂移。

### 7. 优点
*   **框架统一**：成功将判别式任务（识别）与生成式任务（扩散）结合，实现了“以理解促生成”。
*   **评估严谨**：纠正了社区在坐标系使用上的混乱，建立了更公平的绝对坐标评估标准。
*   **多任务通用**：一个模型同时在识别、生成、检索、编辑四个领域达到 SOTA，展示了极强的通用性。

### 8. 不足与局限
*   **物理约束缺失**：虽然生成的动作语义准确且真实，但模型并未显式引入物理定律（如重力、碰撞检测），在某些极端动作下可能存在物理不合理性。
*   **计算开销**：在采样过程中需要计算 MAR 的梯度，这会增加推理时的计算负担和耗时。
*   **场景限制**：目前主要针对单人动作，尚未扩展到复杂的多人交互场景。

（完）
