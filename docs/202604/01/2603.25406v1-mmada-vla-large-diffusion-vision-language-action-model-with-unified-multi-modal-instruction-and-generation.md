---
title: "MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation"
title_zh: MMaDA-VLA：具有统一多模态指令与生成的扩散视觉-语言-动作大模型
authors: "Yang Liu, Pengxiang Ding, Tengyue Jiang, Xudong Wang, Wenxuan Song, Minghui Lin, Han Zhao, Hongyin Zhang, Zifeng Zhuang, Wei Zhao, Siteng Huang, Jinkui Shi, Donglin Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25406v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于机器人控制的原生离散扩散公式
tldr: 针对现有视觉-语言-动作（VLA）模型在架构开销、时间一致性及环境动力学建模方面的不足，本文提出MMaDA-VLA。这是一种原生预训练的大型离散扩散VLA模型，将语言、图像和机器人控制统一到离散标记空间中。通过掩码标记去噪，模型能并行生成未来目标观测和动作块，在无需额外世界模型的情况下实现了长程一致性和动作接地。实验证明其在LIBERO和CALVIN基准测试中均达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有VLA模型面临架构冗余、长程误差累积以及难以在不增加额外模块的情况下捕捉环境动力学的问题。
method: 采用原生离散扩散公式将多模态数据统一到同一标记空间，利用掩码去噪并行生成未来视觉目标和动作块。
result: "在LIBERO模拟基准上实现了98.0%的平均成功率，并在CALVIN任务中达到了4.78的平均完成长度。"
conclusion: MMaDA-VLA通过统一多模态理解与生成，显著增强了机器人操作的全局一致性和对未来环境变化的感知能力。
---

## 摘要
视觉-语言-动作 (VLA) 模型旨在根据视觉观测和自然语言指令控制机器人进行操作。然而，现有的分层和自回归范式通常会引入架构开销，面临时间不一致性和长时程误差累积的问题，并且缺乏在不添加额外模块的情况下捕捉环境动力学的机制。为此，我们提出了 MMaDA-VLA，这是一种全原生的预训练扩散 VLA 大模型，它在单一框架中统一了多模态理解与生成。我们的核心思想是一种原生离散扩散公式，它将语言、图像和连续机器人控制嵌入到一个离散标记空间中，并通过掩码标记去噪训练单一骨干网络，以并行地联合生成未来目标观测和动作块。迭代去噪实现了全局、无序的细化，在提高长时程一致性的同时，将动作锚定在预测的未来视觉结果中，而无需辅助世界模型。在仿真基准测试和现实世界任务中的实验表明，该模型达到了最先进的性能，在 LIBERO 上实现了 98.0% 的平均成功率，在 CALVIN 上实现了 4.78 的平均长度。

## Abstract
Vision-Language-Action (VLA) models aim to control robots for manipulation from visual observations and natural-language instructions. However, existing hierarchical and autoregressive paradigms often introduce architectural overhead, suffer from temporal inconsistency and long-horizon error accumulation, and lack a mechanism to capture environment dynamics without extra modules. To this end, we present MMaDA-VLA, a fully native pre-trained large diffusion VLA model that unifies multi-modal understanding and generation in a single framework. Our key idea is a native discrete diffusion formulation that embeds language, images, and continuous robot controls into one discrete token space and trains a single backbone with masked token denoising to jointly generate a future goal observation and an action chunk in parallel. Iterative denoising enables global, order-free refinement, improving long-horizon consistency while grounding actions in predicted future visual outcomes without auxiliary world models. Experiments across simulation benchmarks and real-world tasks show state-of-the-art performance, achieving 98.0% average success on LIBERO and 4.78 average length on CALVIN.

---

## 论文详细总结（自动生成）

### MMaDA-VLA 论文深度总结

#### 1. 核心问题与研究动机
现有的视觉-语言-动作（VLA）模型主要面临三大挑战：
*   **架构冗余与信息损失**：分层架构（如在 VLM 上加策略头）增加了训练成本，且模块间的信息传递可能导致保真度下降。
*   **时间不一致性**：自回归生成动作序列时，容易产生长程误差累积，缺乏全局轨迹规划能力。
*   **环境动力学缺失**：大多数模型缺乏预测未来视觉状态的机制，难以在没有额外“世界模型”模块的情况下理解环境演变。

**MMaDA-VLA 的核心含义**：提出一种全原生的预训练大型离散扩散 VLA 模型，通过单一框架统一多模态理解与生成，利用并行去噪同时预测未来目标观测和动作块。

#### 2. 方法论：核心思想与技术细节
*   **统一离散标记空间**：将语言（文本分词器）、图像（MAGVIT-v2 量化器）和连续机器人动作（离散化为 256 个 bin）全部映射到统一的离散 Token 空间。
*   **原生离散扩散公式**：采用掩码标记去噪（Masked Token Denoising）作为统一训练目标。在训练时，随机掩盖目标观测和动作序列，让模型学习重建。
*   **并行生成架构**：模型在推理时通过迭代去噪，**并行**生成“未来目标图像”和“动作块（Action Chunk）”。这种设计允许动作生成参考预测的未来视觉结果，从而隐式建模环境动力学。
*   **混合注意力机制（Hybrid Attention）**：
    *   **模态内**：采用双向全注意力，允许 Token 间全局信息交换，尊重动作维度的无序性。
    *   **模态间**：采用因果注意力，确保信息流向的合理性。
*   **推理加速**：引入训练无关的缓存框架（KV Cache），针对指令部分进行长程缓存，针对生成部分进行选择性刷新，以满足实时控制需求。

#### 3. 实验设计与基准对比
*   **仿真基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，测试多任务和终身学习能力。
    *   **CALVIN**：采用 ABC → D 设置，测试长程任务（连续 5 个子任务）和环境泛化性。
*   **现实世界任务**：使用 AgileX Piper 6-DoF 机械臂，涵盖简单取放、精准堆叠、复杂抽屉存储、长程桌面整理四类任务。
*   **对比方法**：对比了 OpenVLA、RT-1、π0、UniVLA、DreamVLA、LLaDA-VLA 等数十种 SOTA 模型，涵盖了离散动作、连续动作及带世界模型的 VLA 变体。

#### 4. 资源与算力开销
*   **算力资源**：使用了 **8 个 Nvidia H800 GPU 节点**（每个节点 8 张 80GB 显存显卡，共 64 张 GPU）。
*   **训练时长**：预训练阶段在 6100 万步的数据集上耗时约 **30 小时**。
*   **模型规模**：基于 MMaDA-8B-Base 骨干网络。

#### 5. 实验数量与充分性
*   **数据规模**：预训练使用了 6100 万步的大规模跨化身机器人操作数据集（包括 DROID、Bridge V2 等 20 多个来源）。
*   **实验覆盖**：
    *   仿真实验：在 LIBERO 和 CALVIN 上进行了详尽的成功率和平均长度统计。
    *   消融实验：针对“世界模型（视觉预测）”、“并行去噪”、“注意力机制类型”以及“预训练作用”进行了对比。
    *   现实实验：每类任务进行 30 次测试。
*   **评价**：实验设计较为充分，涵盖了从仿真到现实、从短程到长程、从单一任务到多任务的多个维度，对比基线包含了最新的研究成果，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：MMaDA-VLA 在 LIBERO 上达到 98.0% 平均成功率，在 CALVIN 上达到 4.78 的平均完成长度，均刷新了 SOTA 记录。
*   **视觉预测的价值**：消融实验证明，加入未来目标观测预测（世界模型属性）能显著提升性能（CALVIN 平均长度提升 0.48）。
*   **并行优于串行**：并行生成目标图像和动作比先生成图像再生成动作的效果更好，因为并行去噪允许动作预测利用图像生成的中间特征。
*   **预训练至关重要**：大规模预训练显著增强了模型的泛化能力和执行精度。

#### 7. 优点与亮点
*   **架构简洁统一**：无需额外的策略头或独立的世界模型模块，降低了系统复杂性。
*   **全局一致性强**：离散扩散的无序细化特性避免了自回归模型的顺序偏差，提高了长程任务的稳定性。
*   **实时性优化**：通过 KV Cache 优化，在保持扩散模型生成质量的同时，提升了推理速度。
*   **开源贡献**：提供了完整的预训练和微调流程，对社区具有参考价值。

#### 8. 不足与局限
*   **视觉生成质量有限**：生成的未来目标图像在精细细节（如夹具几何形状、微小物体）上存在模糊或失真，这受限于 Token 数量的压缩。
*   **现实环境挑战**：在处理极高精度的任务（如窄口杯子堆叠）或复杂交互（如抽屉未完全拉开）时仍存在失败概率。
*   **计算成本**：尽管有推理加速，但扩散模型的多次迭代去噪相比单次前向传播的自回归模型，在计算资源消耗上仍有一定压力。

（完）
