---
title: "$R^2$-dLLM: Accelerating Diffusion Large Language Models via Spatio-Temporal Redundancy Reduction"
title_zh: $R^2$-dLLM：通过时空冗余削减加速扩散大语言模型
authors: "Zhenbang Du, Kejing Xia, Xinrui Zhong, Yonggan Fu, Nicolai Oswald, Binfei Ji, Brucek Khailany, Pavlo Molchanov, Yingyan Lin"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.18995v1"
tags: ["query:ardm"]
score: 8.0
evidence: 讨论扩散大语言模型 (dLLMs) 作为自回归生成的替代方案
tldr: "扩散大语言模型（dLLMs）虽支持并行生成，但推理延迟高。本文发现其低效源于空间（置信度簇）和时间（稳定预测的重复掩码）冗余。为此提出 $R^2$-dLLM 框架，在推理端引入聚合局部置信度与固定稳定标记的策略，在训练端通过冗余感知微调优化解码轨迹。实验显示该方法在保持生成质量的同时，将解码步数减少了高达 75%，显著提升了推理效率。"
selection_source: fresh_fetch
motivation: 扩散大语言模型在解码过程中存在显著的空间和时间冗余，严重限制了其推理速度和实际部署。
method: 提出 $R^2$-dLLM 框架，通过推理时的免训练解码规则和训练时的冗余感知监督微调来消除时空冗余。
result: "实验证明该方法在不同模型和任务中，能在保持生成质量的同时将解码步数最多减少 75%。"
conclusion: 解码冗余是扩散大语言模型的主要瓶颈，通过显式减少冗余可以获得实质性的推理加速。
---

## 摘要
扩散大语言模型 (dLLMs) 通过实现并行 Token 预测，已成为自回归生成的一种极具前景的替代方案。然而，实际的 dLLM 解码仍面临高推理延迟的问题，这限制了其部署。在这项工作中，我们观察到这种低效的很大一部分源于解码过程中反复出现的冗余，包括由置信度簇和位置歧义引起的空间冗余，以及由重复掩码已稳定的预测结果引起的时间冗余。受这些模式的启发，我们提出了 $R^2$-dLLM，这是一个从推理和训练两个角度减少解码冗余的统一框架。在推理阶段，我们引入了无需训练的解码规则，这些规则聚合了局部置信度和 Token 预测，并确定了时间上稳定的 Token，以避免冗余的解码步骤。我们进一步提出了一种冗余感知的有监督微调流水线，使模型与高效的解码轨迹对齐，并减少对人工调节阈值的依赖。实验表明，与现有的解码策略相比，$R^2$-dLLM 在不同模型和任务中始终能减少高达 75% 的解码步数，同时保持具有竞争力的生成质量。这些结果验证了解码冗余是 dLLM 的核心瓶颈，显式地减少冗余可以带来显著的实际效率提升。

## Abstract
Diffusion Large Language Models (dLLMs) have emerged as a promising alternative to autoregressive generation by enabling parallel token prediction. However, practical dLLM decoding still suffers from high inference latency, which limits deployment. In this work, we observe that a substantial part of this inefficiency comes from recurring redundancy in the decoding process, including spatial redundancy caused by confidence clusters and positional ambiguity, and temporal redundancy caused by repeatedly remasking predictions that have already stabilized. Motivated by these patterns, we propose $R^2$-dLLM, a unified framework for reducing decoding redundancy from both inference and training perspectives. At inference time, we introduce training-free decoding rules that aggregate local confidence and token predictions, and finalize temporally stable tokens to avoid redundant decoding steps. We further propose a redundancy-aware supervised fine-tuning pipeline that aligns the model with efficient decoding trajectories and reduces reliance on manually tuned thresholds. Experiments demonstrate that $R^2$-dLLM consistently reduces the number of decoding steps by up to 75% compared to existing decoding strategies, while maintaining competitive generation quality across different models and tasks. These results validate that decoding redundancy is a central bottleneck in dLLMs, and that explicitly reducing it yields substantial practical efficiency gains.

---

## 论文详细总结（自动生成）

这篇论文提出了 **$R^2$-dLLM**，旨在通过减少时空冗余来加速扩散大语言模型（dLLMs）的推理过程。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：扩散大语言模型（dLLMs）通过并行预测 Token，被视为自回归（AR）模型的有力竞争者。然而，尽管具备并行潜力，dLLM 在实际推理中往往需要大量的迭代步数（NFE），导致延迟较高。
*   **核心问题**：作者发现 dLLM 的低效源于解码轨迹中的**时空冗余**：
    *   **空间冗余**：包括“置信度簇”（相邻 Token 同时达到高置信度但被分开处理）和“Token 簇”（同一 Token 在相邻位置重复出现导致的定位歧义）。
    *   **时间冗余**：许多 Token 在解码早期就已稳定，但现有的策略仍会对其进行重复的掩码和重新解码。
*   **研究意义**：通过识别并消除这些冗余，可以在不损失生成质量的前提下，显著提升 dLLM 的推理速度。

### 2. 方法论：核心思想与关键技术
$R^2$-dLLM 是一个结合了“推理端免训练规则”和“训练端冗余感知微调”的统一框架：
*   **推理端（免训练策略）**：
    *   **置信度簇聚合**：在局部窗口内计算平均置信度，若超过阈值 $\tau_s$，则同时解码窗口内的所有 Token。
    *   **Token 簇聚合**：当同一 Token 在相邻位置重复预测时，仅保留置信度最高的位置并直接解码，解决位置歧义。
    *   **多步一致性检查**：若某个位置的预测 Token 在连续 $m$ 步内保持不变且置信度达标，则提前固定该 Token，避免重复计算。
*   **训练端（冗余感知监督微调 - SFT）**：
    *   **数据集构建**：利用预训练模型生成多个候选响应，根据上述规则触发的次数计算“冗余分数” ($R_{total}$)，选择冗余度最低且结果正确的轨迹作为训练数据。
    *   **半自回归微调**：采用分块掩码策略进行 SFT，使模型学习更高效、更确定的解码行为，减少对推理时人工调参的依赖。

### 3. 实验设计
*   **数据集/场景**：涵盖数学推理（**GSM8K**, **MATH**）和代码生成（**HumanEval**, **MBPP**）四大基准任务。
*   **评估模型**：LLaDA-Instruct-8B、Dream-v0-Instruct-7B 以及强化学习优化后的 LLaDA-1.5。
*   **对比方法（Baselines）**：
    *   Vanilla dLLM（Top-1 采样）。
    *   Fast-dLLM（基于固定阈值的并行解码）。
    *   LocalLeap（局部确定性传播）。
    *   dParallel（基于蒸馏的并行解码）。
*   **评估指标**：推理延迟（Latency）、函数评估次数（NFE）、任务准确率（ACC）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 台 NVIDIA H200 (141 GB) GPU** 进行训练和推理测量。
*   **训练细节**：
    *   LLaDA 系列模型训练 6 个 Epoch，Dream 模型训练 3 个 Epoch。
    *   使用了 LoRA 微调技术（Rank=32, Alpha=32）。
    *   训练数据集规模：LLaDA 约 5.2万条，Dream 约 9.1万条。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流模型和 4 个数据集上进行了详尽测试，并提供了消融实验。
*   **消融实验**：分别验证了空间聚合、时间一致性检查等各组件的贡献；分析了不同位置选择策略（如最高置信度 vs 随机）的影响；探讨了阈值 $\tau_s, \tau_t$ 的敏感性。
*   **充分性评价**：实验设计较为全面，不仅对比了 SOTA 方法，还验证了在长文本生成下的鲁棒性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **效率提升**：$R^2$-dLLM 相比 Vanilla 模式实现了高达 **7.7倍** 的延迟加速，相比 Fast-dLLM 减少了约 **21%~40%** 的 NFE。
*   **质量保持**：在大幅减少解码步数的同时，模型在数学和代码任务上的准确率保持稳定，甚至在某些任务（如 MBPP）上有所提升。
*   **冗余本质**：实验证明，解码冗余确实是 dLLM 的核心瓶颈，通过 SFT 引导模型学习低冗余轨迹是行之有效的。

### 7. 优点（亮点）
*   **视角独特**：首次从时空冗余的角度统一分析 dLLM 的低效问题。
*   **双重优化**：结合了推理时的启发式规则和训练时的分布对齐，既能即插即用，又能通过微调进一步强化。
*   **泛化性强**：在不同架构（LLaDA, Dream）和不同优化阶段（指令微调, 强化学习微调）的模型上均表现出一致的增益。

### 8. 不足与局限
*   **阈值依赖**：推理端的免训练策略仍依赖于超参数 $\tau_s$ 和 $\tau_t$ 的调节，不同任务可能需要不同的最优阈值。
*   **训练成本**：虽然 SFT 提升了性能，但需要先通过生成和打分构建低冗余数据集，增加了额外的数据准备开销。
*   **应用范围**：目前主要集中在逻辑性较强的数学和代码任务，在开放域对话或长篇文学创作等对多样性要求极高的场景下，过度减少冗余是否会影响生成灵活性尚待验证。

（完）
