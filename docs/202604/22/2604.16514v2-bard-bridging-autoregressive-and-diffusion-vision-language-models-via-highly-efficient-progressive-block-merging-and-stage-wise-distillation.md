---
title: "BARD: Bridging AutoRegressive and Diffusion Vision-Language Models Via Highly Efficient Progressive Block Merging and Stage-Wise Distillation"
title_zh: BARD：通过高效渐进式块合并与阶段式蒸馏连接自回归与扩散视觉语言模型
authors: "Baoyou Chen, Hanchen Xia, Peng Tu, Haojun Shi, Shan Mu, Weihao Yuan, Siyu Zhu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.16514v2"
tags: ["query:ardm"]
score: 9.0
evidence: 桥接自回归与扩散视觉语言模型
tldr: 针对自回归视觉语言模型推理速度慢的问题，本文提出BARD框架，通过渐进式块合并和阶段式扩散蒸馏，将预训练的自回归模型转换为高效的扩散模型。该方法在保持多模态能力的同时，显著提升了解码速度，在4B和8B规模上达到了开源扩散视觉语言模型的领先水平，并实现最高3倍的吞吐量提升。
selection_source: fresh_fetch
motivation: 自回归模型逐令牌解码导致推理效率低下，而直接将预训练自回归模型转换为大块扩散模型会造成严重的性能下降。
method: 采用渐进式监督块合并逐步扩大解码块，并结合阶段式扩散内蒸馏、混合噪声调度器及内存友好训练来恢复性能。
result: BARD-VL在4.4M数据下使Qwen3-VL具备高效扩散能力，在多个规模上刷新了开源扩散视觉语言模型的最优性能，且速度提升达3倍。
conclusion: 通过在扩散域内进行蒸馏而非跨域蒸馏，可以有效地将自回归模型的能力迁移至高效的扩散解码架构中。
---

## 摘要
自回归视觉语言模型（VLM）提供了强大的多模态能力，但其逐标记（token-by-token）解码方式带来了根本性的推理瓶颈。扩散视觉语言模型（dVLM）提供了一种更具并行性的解码范式，然而，将预训练的自回归 VLM 直接转换为大块（large-block）扩散 VLM 往往会导致严重的质量下降。在这项工作中，我们提出了 BARD，这是一个简单且有效的桥接框架，可将预训练的自回归 VLM 转换为具有相同架构且解码高效的 dVLM。我们的方法结合了渐进式有监督块合并（逐渐增大解码块大小）以及来自固定小块扩散锚点的阶段式 dVLM 内部蒸馏，以恢复在大块设置下损失的性能。我们还引入了混合噪声调度器以提高鲁棒性和去噪过程中的标记修正，并采用内存友好型训练以实现对长多模态序列的高效训练。一个关键的经验发现是，直接的“自回归到扩散”蒸馏对齐效果较差，甚至可能损害性能，而扩散机制内部的蒸馏则始终

## Abstract
Autoregressive vision-language models (VLMs) deliver strong multimodal capability, but their token-by-token decoding imposes a fundamental inference bottleneck. Diffusion VLMs offer a more parallel decoding paradigm, yet directly converting a pretrained autoregressive VLM into a large-block diffusion VLM (dVLM) often leads to substantial quality degradation. In this work, we present BARD, a simple and effective bridging framework that converts a pretrained autoregressive VLM into a same-architecture, decoding-efficient dVLM. Our approach combines progressive supervised block merging, which gradually enlarges the decoding block size, with stage-wise intra-dVLM distillation from a fixed small-block diffusion anchor to recover performance lost at larger blocks. We further incorporate a mixed noise scheduler to improve robustness and token revision during denoising, and memory-friendly training to enable efficient training on long multimodal sequences. A key empirical finding is that direct autoregressive-to-diffusion distillation is poorly aligned and can even hurt performance, whereas distillation within the diffusion regime is consistently effective. Experimental results show that, with $\leq 4.4M$ data, BARD-VL transfers strong multimodal capability from Qwen3-VL to a large-block dVLM. Remarkably, BARD-VL establishes a new SOTA among comparable-scale open dVLMs on our evaluation suite at both 4B and 8B scales. At the same time, BARD-VL achieves up to \textbf{3$\times$} decoding throughput speedup compared to the source model.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **BARD** 的框架，旨在将高性能的自回归（Autoregressive, AR）视觉语言模型（VLM）高效地转换为具有相同架构但解码速度更快的扩散（Diffusion）视觉语言模型（dVLM）。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自回归 VLM 虽然能力强大，但其“逐标记”的顺序解码方式在推理时存在严重的效率瓶颈。
*   **研究动机**：虽然扩散模型支持并行解码（块解码），但直接将预训练的自回归模型转换为大块解码的扩散模型通常会导致显著的性能下降。现有扩散 VLM 在多模态理解能力上仍普遍弱于同规模的自回归模型。
*   **整体含义**：BARD 旨在通过一种平滑的转换机制，在保留自回归模型强大能力的同时，实现数倍的推理加速。

### 2. 方法论
BARD 框架包含以下核心技术组件：
*   **渐进式有监督块合并（Progressive Supervised Block Merging）**：不直接训练大块扩散模型，而是从块大小 $B=4$ 开始，逐步增加到 8、16、32。这种渐进式策略让模型平滑地适应从因果解码到并行去噪的转变。
*   **阶段式 dVLM 蒸馏（Stage-wise dVLM Distillation）**：
    *   **核心发现**：直接用自回归模型作为教师进行蒸馏效果较差，因为 AR（预测下一标记）与扩散（同位置去噪）的逻辑不一致。
    *   **方案**：先将 AR 模型转换为一个小块扩散锚点模型（Anchor），然后以此锚点为教师，对后续更大块的扩散学生模型进行蒸馏。
*   **混合噪声调度器（Mixed-Noise Scheduler）**：在训练中同时引入“掩码（Mask）”和“可见标记损坏（Uniform Corruption）”，使模型不仅能填充缺失信息，还能修正已生成的错误标记（Token Revision）。
*   **内存友好型训练（Memory-Friendly Training）**：采用打包序列布局 $[Q, x_1, x_t]$，避免重复存储冗长的视觉特征 $Q$，显著降低了多模态扩散训练的显存开销。

### 3. 实验设计
*   **源模型**：以 Qwen3-VL（4B 和 8B）作为预训练基座。
*   **数据集**：使用 LLaVA-OneVision-1.5 和 FineVision 约 4.4M 的指令微调数据。
*   **Benchmark**：在 7 个主流基准上评估，涵盖：
    *   **多模态推理**：MMMU, MMMU-Pro, MME。
    *   **通用 VQA**：RealWorldQA, MMStar。
    *   **文档理解**：AI2D, ChartQA。
*   **对比方法**：与 LLaDA-V, Dream-VL, LaviDa, SDAR-VL, MMaDA, Dimple-VL 等开源扩散 VLM 以及源自回归模型进行对比。

### 4. 资源与算力
*   **训练算力**：使用 **8 台 NVIDIA H200 GPU** 进行训练。
*   **训练配置**：全局 Batch Size 为 256，语言模型学习率 $2 \times 10^{-5}$，视觉编码器学习率 $2 \times 10^{-6}$。
*   **评估算力**：使用 4 台 NVIDIA RTX 4090 GPU 进行指标测试。
*   **数据效率**：仅需不到 4.4M 的样本即可完成转换。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2B、4B、8B 三个参数规模。
*   **消融实验**：非常充分，分别验证了数据规模（110K 到 4.4M）、块合并策略（直接训练 vs 渐进式）、蒸馏来源（AR 教师 vs 扩散教师）以及噪声类型（Mask vs Uniform vs Mixture）的影响。
*   **客观性**：使用了统一的开源评估工具 VLMEvalKit，并与 SOTA 模型在相同基准下对比，实验设计严谨且具有说服力。

### 6. 主要结论与发现
*   **性能对齐**：BARD-VL 成功消除了扩散模型与自回归模型之间的性能差距。在 4B 和 8B 规模上，BARD-VL 在多数指标上甚至略优于其源自回归模型（Qwen3-VL）。
*   **效率提升**：相比于源模型，BARD-VL 实现了高达 **3 倍** 的解码吞吐量加速。
*   **蒸馏关键点**：在扩散域内部进行蒸馏（Diffusion-to-Diffusion）比跨域蒸馏（AR-to-Diffusion）更有效，能显著恢复大块解码带来的性能损失。

### 7. 优点
*   **高效转换**：提供了一套成熟的从 AR 到扩散模型的“后训练”方案，无需从头预训练。
*   **架构兼容**：保持了与原模型相同的架构，便于现有 VLM 生态的迁移。
*   **推理优势**：在长文本和结构化输出（如文档提取）场景下，并行解码的优势极其明显。
*   **训练优化**：内存友好的打包设计解决了多模态长序列训练的痛点。

### 8. 不足与局限
*   **模型覆盖**：目前主要基于 Qwen3-VL 进行了验证，尚未在更多样化的基座模型（如 Llama 或 InternVL 系列）上广泛测试。
*   **领域局限**：虽然在通用理解上表现优异，但在极长视频理解或极复杂推理任务上的表现仍有待进一步探索。
*   **依赖高质量数据**：转换过程仍依赖于数百万量级的优质指令微调数据。

（完）
