---
title: "DepCap: Adaptive Block-Wise Parallel Decoding for Efficient Diffusion LM Inference"
title_zh: DepCap：用于高效扩散语言模型推理的自适应分块并行解码
authors: "Xiang Xia, Wuyang Zhang, Jiazheng Liu, Cheng Yan, Yanyong Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15750v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型作为自回归生成的替代方案
tldr: 扩散语言模型（DLM）在并行解码方面具有潜力，但现有分块解码方法受限于固定调度或局部信号。本文提出 DepCap，一个无需训练的自适应分块并行解码框架。它利用跨步信号动态确定块边界，并识别无冲突令牌子集进行安全并行解码。DepCap 在保持生成质量的同时，显著提升了推理速度，在多个基准测试中实现了高达 5.63 倍的加速。
selection_source: fresh_fetch
motivation: 现有的分块 DLM 解码方法依赖固定调度或局部信号，限制了生成质量与推理速度之间的权衡。
method: 提出 DepCap 框架，通过上一个解码块的影响力自适应划分块边界，并利用令牌级冲突信号实现块内安全并行解码。
result: 在推理和代码生成任务中，DepCap 在不损失性能的前提下，实现了最高 5.63 倍的推理加速。
conclusion: DepCap 是一种即插即用的高效 DLM 推理方案，通过自适应分块策略有效平衡了生成效率与质量。
---

## 摘要
扩散语言模型（DLM）因其在并行解码和全序列全局优化方面的潜力，已成为自回归语言生成的一种极具前景的替代方案。为了释放这一潜力，DLM 推理必须仔细权衡生成质量和解码速度。最近的分块 DLM 解码方法通过按块顺序执行基于扩散的解码，改善了这种权衡。然而，现有方法通常依赖固定的分块调度或当前步骤的局部信号来确定块边界，并使用保守的基于置信度的并行解码来避免冲突，这限制了质量与速度之间的权衡。在本文中，我们认为分块 DLM 推理的两个核心决策需要更合适的信号：用于确定块边界的跨步信号，以及用于并行解码的词元级冲突信号。基于这一观点，我们提出了 DepCap，这是一个用于高效分块 DLM 推理的免训练框架。具体而言，DepCap 将跨步信号实例化为上一个已解码块的影响力，并利用它自适应地确定下一个块的延伸范围，同时识别每个块内无冲突的词元子集以进行安全的并行解码，从而在质量损失微乎其微的情况下实现显著的推理加速。DepCap 是一种即插即用的方法，适用于各种 DLM，并与现有的分块 DLM KV 缓存策略兼容。信息论分析进一步表明，上一个块对候选块的累积影响在词元之间近似具有加性，这为所提出的分块准则提供了支持。实验结果表明，DepCap 在多个 DLM 骨干网络以及推理和编码基准测试中均实现了优异的速度-质量权衡，在没有显著性能下降的情况下实现了高达 5.63 倍的加速。

## Abstract
Diffusion language models (DLMs) have emerged as a promising alternative to autoregressive language generation due to their potential for parallel decoding and global refinement of the entire sequence. To unlock this potential, DLM inference must carefully balance generation quality and decoding speed. Recent block-wise DLM decoding methods improve this trade-off by performing diffusion-based decoding sequentially in blocks. However, existing methods typically rely on fixed block schedules or current-step local signals to determine block boundaries, and use conservative confidence-based parallel decoding to avoid conflicts, limiting the quality-speed trade-off. In this paper, we argue that block-wise DLM inference requires more suitable signals for its two core decisions: cross-step signals for determining block boundaries, and token-level conflict signals for parallel decoding. Based on this view, we propose DepCap, a training-free framework for efficient block-wise DLM inference. Specifically, DepCap instantiates the cross-step signal as the influence of the last decoded block and uses it to adaptively determine how far the next block should extend, while identifying a conflict-free subset of tokens for safe parallel decoding within each block, enabling substantial inference acceleration with negligible quality degradation. DepCap is a plug-and-play method applicable to various DLMs, and compatible with existing KV-cache strategies for block-wise DLM. An information-theoretic analysis further suggests that the cumulative last-block influence on a candidate block is approximately additive across tokens, supporting the proposed block-partitioning criterion. Experimental results show that DepCap achieves favorable speed-quality trade-offs across multiple DLM backbones and reasoning and coding benchmarks, with up to 5.63$\times$ speedup without significant performance degradation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **DepCap**，一种旨在提升扩散语言模型（DLM）推理效率的免训练框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
扩散语言模型（DLM）通过并行更新词元和利用双向上下文，展现出超越自回归模型的潜力。然而，DLM 推理面临**生成质量与解码速度之间的权衡**：
*   **现有挑战**：目前的分块解码方法（Block-wise decoding）通常依赖固定的块大小，或者仅根据当前步骤的局部信号（如语义结构或熵）来确定块边界。
*   **局限性**：这种做法忽略了多步去噪过程中的动态变化，且在块内并行解码时，简单的“基于置信度”策略容易导致词元冲突，从而损害生成质量。
*   **研究目标**：寻找更合适的信号（跨步信号和词元冲突信号）来指导块划分和块内并行解码，以在不牺牲质量的前提下实现大幅加速。

### 2. 方法论：核心思想与关键技术
DepCap 包含两个核心模块，均无需重新训练模型：
*   **DepGA-Block（依赖引导的自适应块划分）**：
    *   **核心思想**：利用“上一个已解码块”对未来位置的影响力来决定下一个块的边界。
    *   **技术细节**：计算解码上一个块前后，未来位置预测分布的 KL 散度（称为“上一个块的影响力” $I_k$）。结合预测不确定性（熵 $H_k$），定义依赖分数 $S_k = I_k - \lambda H_k$。当不确定性开始主导（分数小于 0）时，即为块边界。
*   **CAP-Decoding（冲突感知并行解码）**：
    *   **核心思想**：显式检测并过滤块内可能产生冲突的词元，构建一个“无冲突子集”进行并行解码。
    *   **技术细节**：通过计算词元间的冲突分数（基于成对的对数概率），识别相互干扰的位置。算法分为两阶段：首先优先处理高置信度词元，然后通过贪心策略补充其他不冲突的候选词元。
*   **理论支持**：通过信息论分析证明，上一个块对候选块的累积影响在词元间近似具有加性，为该划分准则提供了数学依据。

### 3. 实验设计
*   **模型骨干（Backbones）**：LLaDA-8B-Instruct、Dream-v0-base-7B、LLaDA-1.5。
*   **基准测试（Benchmarks）**：
    *   数学推理：GSM8K (5-shot)、Math-500 (4-shot)。
    *   代码生成：MBPP (3-shot)、HumanEval (0-shot)。
*   **对比方法**：Vanilla（逐个解码）、Fast-dLLM（固定块大小）、AdaBlock（基于语义结构的自适应块）。
*   **评估指标**：任务准确率（Acc）、每秒词元数（TPS）、函数调用次数（NFE）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单台 **NVIDIA A100 40GB GPU** 上完成。
*   **软件框架**：使用 `lm-eval` 库进行公平评估。
*   **训练时长**：由于该方法是**免训练（Training-free）**的，因此不涉及模型训练时长，仅涉及推理评估时间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流 DLM 家族、4 个核心基准测试。
*   **消融实验**：分别验证了 DepGA-Block 和 CAP-Decoding 的独立贡献。
*   **兼容性测试**：验证了该方法与现有 KV-cache 策略（如 Prefix Cache, Dual Cache）结合后的表现。
*   **超参数分析**：对生成长度、最大块大小、不确定性权重 $\lambda$、冲突阈值 $\gamma$ 等进行了详细的敏感度分析。
*   **评价**：实验设计全面，对比了固定与动态策略，覆盖了推理和代码等复杂任务，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显著加速**：在 LLaDA 家族模型上，DepCap 平均实现了 **3.57×** 的加速；在 MBPP 基准测试中，LLaDA-1.5 甚至达到了 **5.63×** 的加速。
*   **质量保持**：在大幅提升吞吐量的同时，生成质量（准确率）几乎没有下降，甚至在某些任务中因更合理的块划分而有所提升。
*   **即插即用**：证明了该方法可以无缝集成到现有的分块 DLM 推理流程中，且与缓存技术高度兼容。

### 7. 优点
*   **免训练且通用**：不需要昂贵的微调过程，适用于各种基于掩码的扩散语言模型。
*   **动态自适应**：相比固定块大小，能根据生成内容的复杂程度动态调整解码步长。
*   **冲突处理**：引入冲突感知机制，解决了并行解码中常见的“词元打架”问题，提升了鲁棒性。
*   **理论与实践结合**：提供了信息论视角的解释，增强了算法的可解释性。

### 8. 不足与局限
*   **理论简化**：信息论分析基于局部重叠假设，尚未建立严格的性能下界或形式化保证。
*   **应用范围**：目前主要针对分块解码（Block-wise）场景，对于滑动窗口解码（Sliding-window）等其他变体的适用性有待进一步探索。
*   **超参数依赖**：虽然实验证明其相对稳健，但在极端不同的模型或任务下，可能仍需微调 $\lambda$ 和 $\gamma$ 以获得最优平衡。

（完）
