---
title: Scaling Properties of Continuous Diffusion Spoken Language Models
title_zh: 连续扩散口语语言模型的缩放特性
authors: "Jason Ramapuram, Eeshan Gunesh Dhekane, Amitis Shidani, Dan Busbridge, Bogdan Mazoure, Zijin Gu, Russ Webb, Tatiana Likhomanenko, Navdeep Jaitly"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24416v1"
tags: ["query:ardm"]
score: 8.0
evidence: 连续扩散与离散自回归口语语言模型的对比
tldr: 本研究探讨了连续扩散语音语言模型（CD SLM）的扩展特性，旨在解决离散自回归模型在处理连续语音时的瓶颈。通过引入音素JSD指标评估语言质量，研究发现CD SLM在验证损失和语言质量上遵循缩放法则。实验将模型扩展至160亿参数并使用千万小时数据，实现了高质量的多语种、多情感语音生成，尽管长文本连贯性仍具挑战。
selection_source: fresh_fetch
motivation: 针对离散自回归语音模型在处理连续语音信号时存在的性能瓶颈和计算需求，探索连续扩散模型作为替代方案的可行性。
method: 引入音素詹森-香农散度（pJSD）作为语言质量评估指标，并研究连续扩散语音语言模型在不同计算规模下的缩放规律。
result: 实验证明CD SLM遵循缩放法则，且在计算规模增加时，损失函数对数据量和模型大小的选择变得不敏感，有利于快速推理。
conclusion: 扩展至16B参数的CD SLM能生成极具表现力的多语种语音，但实现长篇内容的逻辑连贯性仍是未来研究的重大挑战。
---

## 摘要
纯语音口语语言模型（SLM）在性能上落后于文本和文本-语音模型，最近的离散自回归（AR）SLM 表明，要达到文本模型的水平需要巨大的计算和数据需求。由于为 AR 离散化连续语音会产生瓶颈，我们探讨了连续扩散（CD）SLM 是否更具可行性。为了量化 SLM 的语言质量，我们引入了音素 Jensen-Shannon 散度（pJSD）指标。我们的分析表明，CD SLM 镜像了 AR 的行为，在验证损失和 pJSD 上表现出缩放法则，并显示出随着计算规模扩大，最优的 token 与参数比例会下降。然而，对于后者，损失对数据和模型大小的选择变得不敏感，显示出快速推理的潜力。将 CD SLM 扩展到 160 亿参数并使用数千万小时的对话数据，可以生成具有情感、韵律、多说话者和多语言的语音，尽管实现长篇连贯性仍然是一个重大挑战。

## Abstract
Speech-only spoken language models (SLMs) lag behind text and text-speech models in performance, with recent discrete autoregressive (AR) SLMs indicating significant computational and data demands to match text models. Since discretizing continuous speech for AR creates bottlenecks, we explore whether continuous diffusion (CD) SLM is more viable. To quantify the SLMs linguistic quality, we introduce the phoneme Jensen-Shannon divergence (pJSD) metric. Our analysis reveals CD SLMs, mirroring AR behavior, exhibit scaling laws for validation loss and pJSD, and show optimal token-to-parameter ratios decreasing as compute scales. However, for the latter, loss becomes insensitive to choice of data and model sizes, showing potential for fast inference. Scaling CD SLMs to 16B parameters with tens of millions of hours of conversational data enables generation of emotive, prosodic, multi-speaker, multilingual speech, though achieving long-form coherence remains a significant challenge.

---

## 论文详细总结（自动生成）

这篇论文由 Apple 研究团队撰写，深入探讨了连续扩散（Continuous Diffusion, CD）在口语语言模型（SLM）中的缩放特性（Scaling Laws），旨在挑战目前主流的离散自回归（AR）建模范式。

### 1. 论文的核心问题与整体含义
*   **研究动机**：目前的口语语言模型主要依赖将连续语音离散化为 Token，然后使用自回归（AR）模型进行训练。然而，离散化过程会产生信息瓶颈，且 AR 模型在处理长序列语音时计算压力巨大。
*   **核心问题**：连续扩散模型（CD）是否能作为离散 AR 模型的替代方案？它在语音建模上是否遵循类似于文本大模型的缩放法则（Scaling Laws）？其语言生成质量（Languageness）如何量化？

### 2. 论文提出的方法论
*   **核心思想**：直接在连续的语音特征（Log-mel filterbanks）上应用扩散模型，避免离散化损失，并研究其在不同计算量、模型大小和数据量下的性能演变。
*   **关键技术细节**：
    *   **语音表示**：采用 80 维 Log-mel 滤波器组（80Hz 采样率），认为其比神经编码器更具物理可解释性且无压缩伪影。
    *   **架构**：使用多模态扩散 Transformer（MM-DiT），将语音上下文和生成目标作为两个流处理，通过注意力机制交互。
    *   **pJSD 指标**：提出“音素 Jensen-Shannon 散度”（phoneme JSD），通过比较生成语音与真实语音的音素 n-gram 分布差异，量化模型的语言能力。
    *   **无分类器引导（CFG）**：在推理时使用全零信号作为无条件输入，以增强生成效果。
    *   **缩放公式**：采用改进的参数化缩放表面公式 $L(N, D) = E + (A/N^\alpha + B/D^\beta)^\gamma$，其中引入 $\gamma$ 指数以提高拟合稳定性。

### 3. 实验设计
*   **数据集**：使用自建的 **SpeechCrawl** 数据集，包含从公开渠道收集的数千万小时对话语音。缩放实验主要针对其中的 700 万小时纯英文数据。
*   **Benchmark 与对比**：
    *   **语言质量**：pJSD（1-gram 到 5-gram）。
    *   **感知质量**：DNSMOS（P.808, P.835）、NISQA MOS、Meta Audiobox Aesthetics（内容享受度、理解度、制作质量等）。
    *   **对比对象**：不同规模的 CD SLM 模型（0.6M 到 11.5B 参数）以及最终扩展的 16B 模型。

### 4. 资源与算力
*   **算力规模**：论文分析了 10 个计算预算等级，从 $10^{18}$ 到 $10^{21}$ FLOPs。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号（如 H100/A100）或具体卡数，但提到训练数据量高达数千万小时，且模型规模达 16B，暗示了极大规模的计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 组不同计算预算的实验，每组包含多个 (N, D) 组合，且每个设置至少运行 3 个随机种子以确保统计显著性。
*   **消融实验**：针对训练时长、时间补丁大小（Patch size）、噪声调度（Noise schedule）和扩散步数进行了系统性的消融研究。
*   **充分性评价**：实验设计非常严谨，覆盖了从微型模型到超大规模模型的全过程，拟合误差（MRE）极低，结论具有较强的说服力。

### 6. 论文的主要结论与发现
*   **遵循缩放法则**：验证损失（Validation Loss）和语言质量指标（pJSD）均严格遵循幂律缩放。
*   **Token-参数比**：随着计算量增加，最优的“Token 与参数比例”反而下降，这表明 CD SLM 在大规模下具有更高的数据效率。
*   **推理前沿（Inference Frontier）**：在高计算量下，IsoFLOP 曲线变得平坦，这意味着开发者可以在不显著损失性能的情况下，灵活选择更小的模型或更少的数据，有利于快速推理。
*   **感知质量饱和**：MOS 等感知指标不遵循缩放法则，通常在很小的计算量下就达到饱和（接近真实数据水平）。
*   **16B 模型表现**：虽然能生成极具情感和韵律的多语种语音，但**长篇内容的逻辑连贯性**仍然是一个尚未解决的重大挑战。

### 7. 优点
*   **开创性**：首次对连续扩散语音语言模型进行了系统的缩放特性分析。
*   **新指标**：pJSD 为无法直接计算似然值的生成模型提供了一种有效的语言能力评估手段。
*   **工程指导**：发现的高计算量下 IsoFLOP 曲线平坦化特性，为实际部署提供了重要的灵活性指导。

### 8. 不足与局限
*   **长程连贯性缺失**：即使扩展到 16B 参数，模型依然无法像文本 LLM 那样保持长期的语义逻辑，说明单纯靠缩放（Scaling）可能无法解决语音建模的底层表示问题。
*   **感知指标局限**：现有的自动感知指标（如 DNSMOS）无法准确反映模型在高算力下的微小改进。
*   **硬件透明度**：缺乏具体的硬件资源消耗说明，外部研究者难以评估复现成本。
*   **表示瓶颈**：研究暗示 Log-mel 特征可能存在结构性下限，未来可能需要更强大的语音表示或文本-语音联合建模。

（完）
