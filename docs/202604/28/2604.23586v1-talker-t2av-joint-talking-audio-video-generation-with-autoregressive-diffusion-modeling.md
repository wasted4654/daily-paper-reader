---
title: "Talker-T2AV: Joint Talking Audio-Video Generation with Autoregressive Diffusion Modeling"
title_zh: Talker-T2AV：基于自回归扩散建模的联合说话音视频生成
authors: "Zhen Ye, Xu Tan, Aoxiong Yin, Hongzhan Lin, Guangyan Zhang, Peiwen Sun, Yiming Li, Chi-Min Chan, Wei Ye, Shikun Zhang, Wei Xue"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23586v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于音视频联合生成的自回归扩散框架
tldr: 针对现有联合音视频生成模型在处理说话人头像时存在的语义与细节过度耦合、效率低下的问题，本文提出了Talker-T2AV框架。该框架采用自回归扩散建模，通过共享的语言模型骨干网络在统一的补丁级Token空间内进行高层跨模态推理，并利用两个轻量化扩散Transformer头分别进行模态特定的低层细节解码。实验证明，该方法在口型同步、音视频质量及跨模态一致性上均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的联合生成模型将音视频的语义与底层细节完全耦合，导致说话人合成中不同模态的渲染过程相互干扰且效率低下。
method: 提出一种自回归扩散框架，利用共享骨干网络进行高层语义建模，并配合模态特定的扩散解码器生成音视频潜变量。
result: 在说话人头像基准测试中，该模型在口型同步精度、视频质量和音频质量方面均超越了双分支基准模型。
conclusion: Talker-T2AV通过解耦高层语义推理与底层细节生成，实现了比级联流水线更强的跨模态一致性和更高的生成质量。
---

## 摘要
联合音视频生成模型已经表明，统一生成比级联方法具有更强的跨模态一致性。然而，现有模型在整个去噪过程中通过普遍的注意力机制耦合模态，以完全纠缠的方式处理高层语义和底层细节。这对于说话人脸合成来说并非最优：虽然音频和面部动作在语义上相关，但它们的底层实现（声学信号和视觉纹理）遵循不同的渲染过程。在所有层级上强制进行联合建模会导致不必要的纠缠并降低效率。我们提出了 Talker-T2AV，这是一个自回归扩散框架，其中高层跨模态建模发生在共享主干网络中，而底层细化则使用模态特定的解码器。一个共享的自回归语言模型在统一的补丁级标记空间中对音频和视频进行联合推理。两个轻量级的扩散 Transformer 头部将隐藏状态解码为帧级音频和视频潜变量。在说话肖像基准测试上的实验表明，Talker-T2AV 在口型同步准确度、视频质量和音频质量方面优于双分支基线模型，实现了比级联流水线更强的跨模态一致性。

## Abstract
Joint audio-video generation models have shown that unified generation yields stronger cross-modal coherence than cascaded approaches. However, existing models couple modalities throughout denoising via pervasive attention, treating high-level semantics and low-level details in a fully entangled manner. This is suboptimal for talking head synthesis: while audio and facial motion are semantically correlated, their low-level realizations (acoustic signals and visual textures) follow distinct rendering processes. Enforcing joint modeling across all levels causes unnecessary entanglement and reduces efficiency. We propose Talker-T2AV, an autoregressive diffusion framework where high-level cross-modal modeling occurs in a shared backbone, while low-level refinement uses modality-specific decoders. A shared autoregressive language model jointly reasons over audio and video in a unified patch-level token space. Two lightweight diffusion transformer heads decode the hidden states into frame-level audio and video latents. Experiments on talking portrait benchmarks show Talker-T2AV outperforms dual-branch baselines in lip-sync accuracy, video quality, and audio quality, achieving stronger cross-modal consistency than cascaded pipelines.

---

## 论文详细总结（自动生成）

以下是对论文《Talker-T2AV: Joint Talking Audio-Video Generation with Autoregressive Diffusion Modeling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：联合音视频生成（如 Sora、Kling 等）已证明比传统的级联流水线（先生成音频再驱动视频）具有更强的跨模态一致性。
*   **核心问题**：
    1.  **过度纠缠**：现有的联合扩散模型（如双分支 DiT）在整个去噪过程中通过注意力机制将音频和视频完全耦合。作者认为，虽然语义和时序高度相关，但底层信号（声学波形与视觉纹理）的渲染过程截然不同，全过程耦合会导致建模效率低下。
    2.  **固定长度限制**：非自回归扩散模型通常只能生成固定时长（如 5s）的内容，难以适应不同语速和长文本输入。
*   **整体含义**：本文旨在通过“高层语义联合建模 + 底层细节模态特定渲染”的解耦设计，提升说话人头像生成的质量、同步性和灵活性。

### 2. 方法论
Talker-T2AV 采用**自回归扩散（Autoregressive Diffusion）**框架，将生成过程分为两个阶段：

*   **核心思想**：利用共享的自回归骨干网络进行高层时序规划，再利用独立的扩散头进行底层信号细化。
*   **关键技术细节**：
    *   **时序对齐的潜空间**：使用 **LIA-X** 提取 25Hz 的视频运动潜变量，使用 **WhisperX-VAE** 提取同频率的音频潜变量，确保两者在时间轴上天然对齐。
    *   **第一阶段：跨模态建模（AR Backbone）**：基于 Qwen3-0.6B 构建自回归语言模型。输入包括文本前缀和“音视频补丁 Token”。补丁 Token 是通过将音频和视频的特征向量进行**逐元素相加（Element-wise Summation）**融合而成的。
    *   **第二阶段：模态特定细化（Diffusion Heads）**：骨干网络输出的隐藏状态 $h_i$ 作为条件，输入到两个独立的轻量级扩散 Transformer（DiT）头中。这两个头分别通过**流匹配（Flow Matching）**算法解码出具体的音频波形和视频帧。
    *   **停止预测器**：在骨干网络末端连接一个 MLP，用于预测生成何时结束，从而支持变长输出。
*   **算法流程**：文本 $\rightarrow$ AR 骨干网络（生成共享隐藏状态序列）$\rightarrow$ 独立扩散头（并行去噪生成音视频潜变量）$\rightarrow$ 各自的解码器（还原波形和图像）。

### 3. 实验设计
*   **数据集**：
    *   **联合生成（T2AV）**：收集了约 100 万个说话人头像音视频剪辑（带字幕）。
    *   **纯语音（TTS）**：使用 Emilia 数据集进行多任务混合训练，以增强语音质量。
    *   **测试集**：DH-FaceVid-1K（中文）、HDTF 和 Hallo3（英文）。
    *   **配音任务（V2A）**：使用 Chem 基准数据集。
*   **Benchmark 与对比方法**：
    *   **联合生成基准**：MoVA, Ovi, LTX-2, UniVerse-1, UniAVGen。
    *   **音频驱动视频基准**：FLOAT, EchoMimic, Sonic, Ditto, AniPortrait。
    *   **视频配音基准**：InstructDub, ProDubber 等。
*   **评估指标**：WER/CER（字错率）、UTMOS（语音自然度）、FID/FVD（图像/视频质量）、SyncNet Confidence/Distance（口型同步性）。

### 4. 资源与算力
*   **模型规模**：骨干网络基于 Qwen3-0.6B，扩散头为 8 层 Transformer。
*   **训练细节**：
    *   **总步数**：200,000 步。
    *   **全局 Batch Size**：256。
    *   **优化器**：AdamW，使用 bfloat16 混合精度。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量（如 A100 或 H100 的具体枚数），但从参数量和训练步数推断，这属于中大规模的训练任务。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了中文和英文两个语种的独立测试。
    *   对比了联合生成、音频驱动视频、视频配音三个不同的任务场景。
    *   **消融实验**：详细探讨了 Token 排列方式（相加、交错、延迟）对性能的影响。
*   **充分性评价**：实验设计较为充分。通过多任务对比证明了该模型不仅在联合生成上领先，且在单模态驱动任务中也能匹配甚至超越专用模型。消融实验解释了“逐元素相加”设计的优越性，逻辑自洽。

### 6. 主要结论与发现
*   **性能领先**：Talker-T2AV 在口型同步准确度（SyncNet 分数）和视频保真度（FVD）上显著优于现有的双分支扩散模型。
*   **解耦的必要性**：实验证明，在语义层面联合建模而在渲染层面保持独立，能有效避免模态间的干扰，提升语音的清晰度（WER 显著降低）。
*   **多功能性**：由于采用了逐元素相加的融合设计，模型无需微调即可直接用于音频驱动视频（A2V）和视频配音（V2A）任务。
*   **变长生成**：自回归机制成功解决了扩散模型在处理长文本时内容被压缩或截断的问题。

### 7. 优点
*   **架构创新**：巧妙地结合了自回归模型（处理长程时序和语义）与扩散模型（处理底层信号质量）的优势。
*   **高效融合**：逐元素相加的设计不仅简化了跨模态对齐，还赋予了模型极强的任务通用性。
*   **同步性强**：通过在潜空间强制时序对齐，实现了极高的口型同步精度。
*   **实用性高**：支持变长输出，更符合实际应用中对话长度不一的需求。

### 8. 不足与局限
*   **误差累积**：由于骨干网络在连续潜空间进行自回归预测，长序列生成时可能会出现预测误差的累积，导致后期质量下降。
*   **视觉上限**：视频质量受限于预训练的 LIA-X 自动编码器的表达能力，对于极端头部姿态的还原可能仍有提升空间。
*   **推理开销**：自回归过程是逐补丁进行的，相比于一次性生成全序列的非自回归模型，在推理速度上可能存在劣势（尽管论文提到了补丁压缩来缓解）。
*   **数据依赖**：虽然使用了 1M 数据，但对于构建通用的“世界模型”级别的音视频生成，数据规模仍有继续扩张的空间。

（完）
