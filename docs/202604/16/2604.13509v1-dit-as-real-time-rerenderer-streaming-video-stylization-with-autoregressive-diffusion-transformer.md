---
title: "DiT as Real-Time Rerenderer: Streaming Video Stylization with Autoregressive Diffusion Transformer"
title_zh: DiT 作为实时重渲染器：基于自回归扩散 Transformer 的流式视频风格化
authors: "Hengye Lyu, Zisu Li, Yue Hong, Yueting Weng, Jiaxin Shi, Hanwang Zhang, Chen Liang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13509v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于流式视频风格化的自回归扩散Transformer
tldr: 针对现有视频风格化方法在长视频处理中稳定性差、计算开销大且难以实时应用的问题，本文提出了RTR-DiT框架。该方法基于扩散Transformer（DiT），通过自回归蒸馏技术将双向教师模型转化为高效的少步生成模型，并引入参考保持的KV缓存策略。RTR-DiT不仅实现了长视频的稳定风格化，还支持文本和参考图引导的实时风格切换，在视觉质量和处理速度上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有的视频风格化方法在处理长视频时难以兼顾时空一致性与实时性，且多步去噪过程计算成本过高。
method: 提出一种基于DiT的自回归流式框架，通过Self Forcing和分布匹配蒸馏实现少步生成，并利用参考保持的KV缓存策略确保长视频的一致性。
result: 实验证明RTR-DiT在文本和参考图引导的风格化任务中均优于现有模型，并能实现实时的长视频处理与交互式风格切换。
conclusion: RTR-DiT为实时、长时且一致的视频风格化提供了一种高效的自回归扩散Transformer解决方案。
---

## 摘要
近期视频生成模型的进展显著加速了视频生成及相关的下游任务。其中，视频风格化在沉浸式应用和艺术创作等领域具有重要的研究价值，吸引了广泛关注。然而，现有的基于扩散模型的视频风格化方法在处理长视频时难以保持稳定性和一致性，且其高昂的计算成本和多步去噪过程使其难以应用于实际场景。在这项工作中，我们提出了 RTR-DiT（DiT 作为实时重渲染器），这是一个基于扩散 Transformer（Diffusion Transformer）构建的流式视频风格化框架。我们首先在一个精心策划的视频风格化数据集上微调了一个双向教师模型，支持文本引导和参考图引导的视频风格化任务，随后通过结合 Self Forcing 和分布匹配蒸馏（Distribution Matching Distillation）的后训练，将其蒸馏为一个少步自回归模型。此外，我们提出了一种保留参考信息的 KV 缓存（KV cache）更新策略，不仅能够实现长视频的稳定一致处理，还支持文本提示词与参考图像之间的实时切换。实验结果表明，RTR-DiT 在文本引导和参考图引导的视频风格化任务中，无论是在定量指标还是视觉质量方面均优于现有方法，并在实时长视频风格化和交互式风格切换应用中展现了卓越的性能。

## Abstract
Recent advances in video generation models has significantly accelerated video generation and related downstream tasks. Among these, video stylization holds important research value in areas such as immersive applications and artistic creation, attracting widespread attention. However, existing diffusion-based video stylization methods struggle to maintain stability and consistency when processing long videos, and their high computational cost and multi-step denoising make them difficult to apply in practical scenarios. In this work, we propose RTR-DiT (DiT as Real-Time Rerenderer), a steaming video stylization framework built upon Diffusion Transformer. We first fine-tune a bidirectional teacher model on a curated video stylization dataset, supporting both text-guided and reference-guided video stylization tasks, and subsequently distill it into a few-step autoregressive model via post-training with Self Forcing and Distribution Matching Distillation. Furthermore, we propose a reference-preserving KV cache update strategy that not only enables stable and consistent processing of long videos, but also supports real-time switching between text prompts and reference images. Experimental results show that RTR-DiT outperforms existing methods in both text-guided and reference-guided video stylization tasks, in terms of quantitative metrics and visual quality, and demonstrates excellent performance in real-time long video stylization and interactive style-switching applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RTR-DiT**，一个基于扩散 Transformer（DiT）的实时视频风格化框架，旨在解决长视频风格化中的一致性、实时性和交互性难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视频风格化方法（如基于 LDM 或双向 DiT 的模型）存在三大痛点：
    1.  **时空不一致**：处理长视频时容易出现闪烁或风格漂移。
    2.  **高延迟**：多步迭代去噪导致无法满足实时应用需求。
    3.  **非流式架构**：双向注意力机制依赖全局帧，无法进行流式（Streaming）处理。
*   **核心目标**：开发一个能够像“重渲染器”一样，在保持原始视频结构的同时，实时、稳定地将视频转换为指定风格（通过文本或参考图引导）的系统。

### 2. 方法论
RTR-DiT 的核心思想是将强大的预训练 DiT 模型转化为一个少步、自回归的流式生成器。
*   **数据构建**：利用 Qwen3-VL 生成提示词，并使用 Kling（可灵）生成高质量风格化视频对，构建了一个包含 5000 个视频的训练集。
*   **模型架构**：
    *   **双向教师模型**：首先微调一个双向时间建模的 DiT 作为教师。
    *   **自回归转换**：在 3D 注意力机制中引入**因果掩码（Causal Mask）**，使当前帧仅依赖于过去帧，实现流式推理。
*   **关键技术细节**：
    *   **Self Forcing**：在训练中让模型基于自身生成的历史帧进行预测，缩小训练与推理之间的分布差距，减少长视频推理中的误差累积。
    *   **分布匹配蒸馏（DMD）**：将多步去噪过程蒸馏为 **2 步（2-step）** 生成，极大提升了推理速度。
    *   **参考保持（Reference-preserving）KV 缓存策略**：在处理长视频时，将参考图像的 KV 缓存永久锚定在序列前端，而视频帧缓存则采用滚动更新。这确保了长视频在任何时刻都能保持与参考风格的一致性。
    *   **交互式风格切换**：通过重新初始化交叉注意力 KV 缓存并保留 3D 注意力缓存，实现文本/参考图风格的平滑实时切换。

### 3. 实验设计
*   **数据集**：从 Pexels 收集了 50 个涵盖人、动物、自然场景的视频作为 Benchmark。
*   **任务场景**：
    *   **TV2V（文本引导）**：使用水墨、我的世界、皮克斯等 10 种风格提示词。
    *   **RV2V（参考图引导）**：使用特定风格图像作为引导。
*   **对比方法**：
    *   **TV2V 对标**：Rerender-A-Video, FRESCO, TokenWarping。
    *   **RV2V 对标**：VACE, StyleMaster, 以及商业模型 **Runway Gen-4 Aleph**。
*   **评估指标**：CLIP-T/CSD-Score（一致性）、CLIP-F（时间稳定性）、VBench 的审美（AQ）与成像质量（IQ）、推理耗时。

### 4. 资源与算力
*   **训练细节**：
    *   第一阶段：微调双向教师模型（10,000 步）。
    *   第二阶段：初始化少步学生模型（10,000 步）。
    *   第三阶段：后训练蒸馏（5,000 步）。
*   **硬件环境**：测试在单张 **NVIDIA H20 GPU** 上完成。
*   **效率**：生成 5 秒视频仅需约 **0.12 分钟**（约 7.2 秒），远快于其他开源方法（通常需 3-28 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了定量对比、定性视觉对比、消融实验以及长视频（约 1 分钟）实测。
*   **消融实验**：验证了“后训练（Post-training）”对稳定性的提升，以及“参考保持 KV 缓存”对防止风格漂移的必要性。
*   **客观性**：对比了当前最先进的开源模型和顶尖商业模型（Runway），实验设计较为公平，且展示了在真实移动端（智能手机+服务器）的部署应用。

### 6. 主要结论与发现
*   **性能领先**：RTR-DiT 在文本遵循度（CLIP-T）和时间稳定性（CLIP-F）上均优于现有开源方法。
*   **商业级质量**：在参考图引导任务中，其视觉质量和风格保真度接近甚至在某些指标上超过了商业模型 Gen-4。
*   **实时交互**：证明了自回归 DiT 配合 KV 缓存优化可以实现长达数分钟的稳定视频渲染，并支持动态风格切换。

### 7. 优点（亮点）
*   **首创性**：提出了首个基于 DiT 的实时流式视频风格化框架。
*   **稳定性**：通过参考保持 KV 缓存解决了长视频处理中常见的“风格丢失”问题。
*   **极速推理**：2 步生成配合自回归架构，使其具备了在 VR/AR 等沉浸式场景中应用的潜力。
*   **交互友好**：支持在视频播放过程中实时更改风格引导条件。

### 8. 不足与局限
*   **数据依赖**：训练数据高度依赖于其他商业大模型（如 Kling）的生成结果，可能存在模型偏差的传递。
*   **算力门槛**：虽然推理快，但训练过程涉及多阶段蒸馏和复杂的后训练，对算力有一定要求。
*   **极端运动处理**：虽然在常规场景表现优异，但在极高动态或复杂遮挡的视频中，自回归模型仍可能面临潜在的伪影或结构扭曲风险（文中未深入探讨极端失败案例）。

（完）
