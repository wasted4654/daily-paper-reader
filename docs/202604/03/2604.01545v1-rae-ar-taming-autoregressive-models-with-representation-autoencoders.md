---
title: "RAE-AR: Taming Autoregressive Models with Representation Autoencoders"
title_zh: RAE-AR：利用表示自编码器驯服自回归模型
authors: "Hu Yu, Hang Xu, Jie Huang, Zeyue Xue, Haoyang Huang, Nan Duan, Feng Zhao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01545v1"
tags: ["query:ardm"]
score: 8.0
evidence: 将表示自编码器集成到连续自回归模型中
tldr: 本研究探讨了将表示自编码器（RAE）应用于连续自回归（AR）模型的挑战。针对高维表示带来的分布建模复杂和训练-推理偏差（曝光偏差）问题，提出了分布归一化简化令牌建模，并引入高斯噪声注入增强鲁棒性。实验证明，RAE-AR 在生成性能上可媲美传统的 VAE，为视觉理解与生成任务的架构统一奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决高维表示自编码器在连续自回归生成模型中面临的分布建模困难和严重的曝光偏差问题。
method: 通过分布归一化简化令牌分布，并结合训练过程中的高斯噪声注入来缓解推理阶段的误差累积。
result: 实验结果显示，改进后的 RAE-AR 在自回归模型上的表现达到了与传统 VAE 相当的水平。
conclusion: 该研究证明了表示自编码器在生成建模中的潜力，推动了视觉理解与生成任务在统一架构下的融合。
---

## 摘要
生成模型的潜空间长期以来由 VAE 编码器主导。来自预训练表示编码器（如 DINO、SigLIP、MAE）的潜变量此前被认为不适用于生成建模。最近，RAE 方法带来了希望，并揭示了表示自编码器也能达到与 VAE 编码器相当的竞争性能。然而，将表示自编码器集成到连续自回归（AR）模型中的研究在很大程度上仍未被探索。在这项工作中，我们研究了在 AR 范式中使用高维表示自编码器的挑战，并将其命名为 RAE-AR。我们关注 AR 模型的独特属性，并确定了两个主要障碍：复杂的逐 token 分布建模以及由高维性放大的训练-推理差距（曝光偏差）。为了解决这些问题，我们引入了通过分布归一化实现的 token 简化，以降低建模难度并提高收敛性。此外，我们通过在训练期间引入高斯噪声注入来增强预测鲁棒性，从而减轻曝光偏差。我们的实验结果表明，这些改进显著缩小了性能差距，使表示自编码器在 AR 模型上能够取得与传统 VAE 相当的结果。这项工作为视觉理解与生成建模之间更统一的架构铺平了道路。

## Abstract
The latent space of generative modeling is long dominated by the VAE encoder. The latents from the pretrained representation encoders (e.g., DINO, SigLIP, MAE) are previously considered inappropriate for generative modeling. Recently, RAE method lights the hope and reveals that the representation autoencoder can also achieve competitive performance as the VAE encoder. However, the integration of representation autoencoder into continuous autoregressive (AR) models, remains largely unexplored. In this work, we investigate the challenges of employing high-dimensional representation autoencoders within the AR paradigm, denoted as \textit{RAE-AR}. We focus on the unique properties of AR models and identify two primary hurdles: complex token-wise distribution modeling and the high-dimensionality amplified training-inference gap (exposure bias). To address these, we introduce token simplification via distribution normalization to ease modeling difficulty and improve convergence. Furthermore, we enhance prediction robustness by incorporating Gaussian noise injection during training to mitigate exposure bias. Our empirical results demonstrate that these modifications substantially bridge the performance gap, enabling representation autoencoder to achieve results comparable to traditional VAEs on AR models. This work paves the way for a more unified architecture across visual understanding and generative modeling.

---

## 论文详细总结（自动生成）

这篇论文《RAE-AR: Taming Autoregressive Models with Representation Autoencoders》探讨了如何将预训练的视觉表示编码器（如 DINOv2、SigLIP）有效地集成到连续自回归（AR）生成模型中。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：生成模型（如扩散模型和自回归模型）的潜空间长期被重建导向的 VAE（变分自编码器）统治。虽然语义表示编码器（如 DINO、MAE）在视觉理解任务中表现卓越，但在生成任务中，直接使用这些高维语义潜变量往往导致生成质量大幅下降。
*   **核心问题**：为什么在扩散模型中已经取得成功的表示自编码器（RAE），在连续自回归（AR）模型中表现极差？
*   **研究动机**：旨在解决高维语义表示在 AR 范式下存在的“逐令牌（token-wise）分布建模困难”和“曝光偏差（Exposure Bias）”问题，从而实现视觉理解与生成的架构统一。

### 2. 方法论
论文提出了 **RAE-AR** 框架，核心思想是通过简化分布和增强鲁棒性来“驯服”高维表示：
*   **令牌分布归一化（Token Normalization）**：
    *   **发现**：语义编码器的 token 间方差（Token Variance）极大，增加了 AR 模型预测下一 token 的难度。
    *   **做法**：对每个 token 的通道维度进行归一化，使其符合零均值和单位方差（$\mu=0, \sigma^2=1$）。这显著降低了训练损失并加速了收敛。
*   **高斯噪声注入（Noise Perturbation）**：
    *   **发现**：AR 模型在推理时依赖之前生成的 token，高维空间放大了训练与推理之间的差距（即曝光偏差）。
    *   **做法**：在训练阶段向 ground-truth token 注入高斯噪声：$\tilde{E} = E + \alpha \cdot \Delta$。这迫使模型学习纠偏能力，增强了推理时的鲁棒性。
*   **连续 AR 架构**：采用类似 MAR 的架构，使用扩散模型作为损失函数（MLP 降噪器）来处理连续值的 token 预测。

### 3. 实验设计
*   **数据集**：主要在 ImageNet (256x256) 上进行验证。
*   **基准模型（Benchmarks）**：
    *   **重建类**：标准 VAE、VA-VAE。
    *   **表示类（Baseline）**：DINOv2-B、SigLIP2-B、MAE-B。
*   **评估指标**：
    *   **重建**：rFID、PSNR、LPIPS、SSIM。
    *   **生成**：gFID、Inception Score (IS)、Precision、Recall。

### 4. 资源与算力
*   **模型配置**：AR 模型宽度设置为 1280，解码器使用 ViT-XL。
*   **训练细节**：模型训练了 300 个 epoch。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量，但从 300 epoch 的 ImageNet 训练规模来看，通常需要大规模 GPU 集群（如 8-64 张 A100/H100）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 5 种不同的潜空间表示。
    *   针对三种主流表示编码器（DINOv2, SigLIP2, MAE）分别进行了消融实验。
    *   探讨了噪声水平（$\alpha$）对性能的影响曲线。
    *   验证了方法在“因果自回归（Raster-scan）”和“掩码自回归（Mask-based）”两种范式下的通用性。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还有定性的视觉对比，且通过跨范式验证证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **语义表示的局限**：直接将 DINOv2 或 MAE 用于 AR 模型会导致 FID 极高（如 MAE 达到 67.6），生成图像结构扭曲。
*   **关键障碍**：高维性导致的曝光偏差是性能下降的主因，而非重建能力不足。
*   **RAE-AR 的效能**：通过归一化和噪声注入，SigLIP2 的生成 FID 从 34.0 降至 6.0，达到了与传统 VAE（FID 3.2-5.9）可比的水平。
*   **编码器差异**：在语义编码器中，DINOv2 最适合生成，而 MAE 最差。

### 7. 优点
*   **简洁有效**：提出的归一化和噪声注入方法不需要复杂的架构改动，易于实现。
*   **架构统一**：证明了无需专门的 VAE，利用现成的视觉理解编码器也能实现高质量生成，推动了多模态大模型的统一。
*   **深入的诊断分析**：通过可视化 token 的均值/方差图，直观地解释了建模困难的原因。

### 8. 不足与局限
*   **细节还原度**：尽管生成指标提升明显，但 RAE 在重建细节（如文字、人脸微小特征）上仍略逊于专门优化的 VAE。
*   **颜色偏移**：实验观察到 DINOv2 在重建时容易出现明显的颜色偏差。
*   **超参数敏感性**：噪声水平 $\alpha$ 需要针对不同的编码器进行微调，缺乏一种自适应的噪声确定机制。
*   **算力成本**：高维表示（如 768 维）相比传统 VAE（16 维）显著增加了 AR 模型的计算负担。

（完）
