---
title: "X-Cache: Cross-Chunk Block Caching for Few-Step Autoregressive World Models Inference"
title_zh: X-Cache：用于少步自回归世界模型推理的跨分块块级缓存
authors: "Yixiao Zeng, Jianlei Zheng, Chaoda Zheng, Shijia Chen, Mingdian Liu, Tongping Liu, Tengwei Luo, Yu Zhang, Boyang Wang, Linkun Xu, Siyuan Lu, Bo Tian, Xianming Liu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20289v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于自动驾驶世界模型的自回归视频扩散
tldr: 针对自动驾驶世界模型推理成本高的问题，本文提出 X-Cache，一种无需训练的跨块（Cross-Chunk）缓存加速方法。不同于传统的跨去噪步缓存，X-Cache 利用连续生成块之间的冗余，通过结构和动作感知的双指标门控机制决定模块复用。同时，它通过强制全量计算 KV 更新块来防止误差累积。在 X-world 模型上实现了 2.6 倍加速，且几乎不损失生成质量。
selection_source: fresh_fetch
motivation: 现有的扩散模型缓存技术无法应用于少步迭代且具有自回归特性的实时交互式驾驶世界模型。
method: 提出跨连续生成块的残差缓存机制，利用双指标门控进行块级复用决策，并定期强制全量计算以切断 KV 缓存中的误差传播。
result: "在多摄像头驾驶世界模型 X-world 上，该方法实现了 71% 的模块跳过率和 2.6 倍的推理加速，且保持了高保真度。"
conclusion: X-Cache 为实时、闭环的自回归视频扩散模型提供了一种高效且无损的推理加速方案。
---

## 摘要
实时世界模拟正成为自动驾驶系统可扩展评估和在线强化学习的关键基础设施。近期基于自回归视频扩散构建的驾驶世界模型实现了高保真、可控的多摄像头生成，但其推理成本仍是交互式部署的瓶颈。然而，现有的扩散缓存方法是为具有多个去噪步骤的离线视频生成设计的，无法迁移到此场景。少步蒸馏模型已无步间冗余可供这些方法复用，而序列级并行化技术则需要闭环交互式生成无法提供的未来条件。我们提出了 X-Cache，一种无需训练的加速方法，它沿着不同的轴进行缓存：跨连续生成分块（chunks）而非跨去噪步骤。X-Cache 维护跨分块持久化的逐块残差缓存，并对感知结构和动作的块输入指纹应用双指标门控机制，以独立决定每个块应重新计算还是复用其缓存残差。为了防止近似误差永久污染自回归 KV 缓存，X-Cache 识别 KV 更新分块（即向持久缓存写入干净键和值的正向传播过程），并在这些分块上无条件强制进行完整计算，从而切断误差传播。我们在 X-world 上实现了 X-Cache，这是一个基于多块因果 DiT 构建的生产级多摄像头动作条件驾驶世界模型，具有少步去噪和滚动 KV 缓存。X-Cache 实现了 71% 的块跳过率和 2.6 倍的实际运行加速，同时保持了极低的性能退化。

## Abstract
Real-time world simulation is becoming a key infrastructure for scalable evaluation and online reinforcement learning of autonomous driving systems. Recent driving world models built on autoregressive video diffusion achieve high-fidelity, controllable multi-camera generation, but their inference cost remains a bottleneck for interactive deployment. However, existing diffusion caching methods are designed for offline video generation with multiple denoising steps, and do not transfer to this scenario. Few-step distilled models have no inter-step redundancy left for these methods to reuse, and sequence-level parallelization techniques require future conditioning that closed-loop interactive generation does not provide. We present X-Cache, a training-free acceleration method that caches along a different axis: across consecutive generation chunks rather than across denoising steps. X-Cache maintains per-block residual caches that persist across chunks, and applies a dual-metric gating mechanism over a structure- and action-aware block-input fingerprint to independently decide whether each block should recompute or reuse its cached residual. To prevent approximation errors from permanently contaminating the autoregressive KV cache, X-Cache identifies KV update chunks (the forward passes that write clean keys and values into the persistent cache) and unconditionally forces full computation on these chunks, cutting off error propagation. We implement X-Cache on X-world, a production multi-camera action-conditioned driving world model built on multi-block causal DiT with few-step denoising and rolling KV cache. X-Cache achieves 71% block skip rate with 2.6x wall-clock speedup while maintaining minimum degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《X-Cache: Cross-Chunk Block Caching for Few-Step Autoregressive World Models Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）视频扩散模型在自动驾驶世界模拟中表现优异，但其推理成本极高，难以满足实时交互需求。
*   **研究背景**：
    *   现有的扩散模型加速方法（如 DeepCache）主要利用**跨去噪步（Cross-step）**的冗余。
    *   然而，现代高性能模型通常经过**少步（Few-step）蒸馏**（如仅 4 步去噪），步间冗余极低，导致传统方法失效。
    *   此外，交互式模拟要求模型实时响应动作指令，无法使用需要预知未来的序列级并行技术。
*   **核心发现**：在自动驾驶场景中，物理世界演变相对平滑，连续生成的**视频分块（Chunks）**之间存在极强的空间和结构冗余。

### 2. 方法论
X-Cache 是一种**无需训练**的块级缓存方法，其核心思想是沿着“跨分块”轴而非“跨步”轴进行缓存复用。
*   **跨分块残差缓存**：在相同的去噪步索引下，缓存 DiT（Diffusion Transformer）块的输出残差。如果当前块输入与前一分块对应位置的输入足够相似，则直接复用缓存残差。
*   **双指标门控机制（Dual-Metric Gating）**：
    *   **指纹提取（Fingerprint）**：对块输入进行 3D 空间下采样，并加入全局均值通道和**动作条件通道**（使模型能感知转向、刹车等突发动作）。
    *   **相似度判定**：结合余弦相似度（全局方向）和最大 Token 偏差（局部离群值）进行判定。
*   **自适应阈值**：为每个块和去噪步维护一个基于指数移动平均（EMA）的动态阈值，自动调节不同位置的复用强度。
*   **安全机制（防止误差累积）**：
    *   **KV 更新帧保护**：自回归模型依赖持久的 KV Cache。X-Cache 识别并强制对写入 KV Cache 的帧进行全量计算，从源头切断近似误差的永久传播。
    *   **锚点块与步-0 保护**：强制计算起始块和起始去噪步，确保条件信号（如动作、文本）能有效注入。

### 3. 实验设计
*   **实验模型**：X-World（小鹏汽车开发的生产级多摄像头动作条件驾驶世界模型，基于 WAN 2.2 构建）。
*   **场景设置**：
    *   **城市街道**：高密度交通、行人和复杂背景。
    *   **高速公路**：快速移动但背景相对简单。
    *   **U 型转弯**：极端的航向变化，用于压力测试跨分块冗余的假设。
*   **Benchmark**：以全量计算（Full-compute）的自回归推理作为基准。
*   **评估指标**：跳过率（Skip Rate）、DiT 墙钟耗时、加速比、PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件平台**：使用了阿里巴巴平头哥开发的 **真武（Zhenwu）810E** AI 加速器（文中称为 PPU），配备 96GB HBM2e 显存。
*   **精度**：所有 DiT 前向传播均在 **BF16** 精度下执行。
*   **算力细节**：实验在单卡 PPU 上完成，未提及具体的训练时长（因为该方法是无需训练的推理加速）。

### 5. 实验数量与充分性
*   **实验规模**：测试集包含 13 个长序列剪辑（7 个城市、3 个高速、3 个掉头），每个剪辑生成 264 帧（约 22 秒）。
*   **消融实验**：针对 KV 更新保护、步-0 保护、前置锚点块、自适应阈值下限等关键组件进行了详细的消融测试。
*   **充分性评价**：实验设计较为客观，涵盖了自动驾驶中最具挑战性的运动场景（如掉头），并对比了像素级、结构级和感知级三个维度的保真度。

### 6. 主要结论与发现
*   **加速效果**：X-Cache 实现了约 **71% 的块跳过率**，带来 **2.6 倍至 2.7 倍** 的 DiT 墙钟加速。
*   **质量保持**：生成质量损失几乎不可察觉（SSIM > 0.999，LPIPS < 4e-4，PSNR 保持在 51-55dB 之间）。
*   **鲁棒性**：即使在掉头等剧烈运动场景下，通过自适应阈值和安全机制，模型依然能保持高保真度，且没有出现误差漂移。

### 7. 优点
*   **维度创新**：首次提出在少步自回归模型中利用“跨分块”冗余，填补了现有缓存方法的空白。
*   **动作感知**：通过将动作向量引入指纹，解决了缓存方法难以响应实时控制指令的顽疾。
*   **误差控制**：巧妙地利用 KV 更新周期来重置误差，确保了长序列生成的稳定性。
*   **即插即用**：无需重新训练或微调模型，具有极强的工程实用性。

### 8. 不足与局限
*   **场景覆盖**：实验主要基于内部数据集，尚未在夜间、极端天气或更长的时间跨度（如数分钟）上验证。
*   **帕累托前沿未完全探索**：目前的参数设置偏向保守（追求无损），尚未完全探索通过牺牲少量质量来换取更高加速比（如 3-4 倍）的极限。
*   **模型依赖**：虽然理论上通用，但目前仅在 X-World 这一种架构上进行了验证，对于其他非 DiT 架构或不同蒸馏方案的适用性有待观察。

（完）
