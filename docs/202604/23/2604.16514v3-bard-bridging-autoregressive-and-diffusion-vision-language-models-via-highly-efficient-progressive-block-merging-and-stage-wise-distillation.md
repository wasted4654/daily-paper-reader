---
title: "BARD: Bridging AutoRegressive and Diffusion Vision-Language Models Via Highly Efficient Progressive Block Merging and Stage-Wise Distillation"
title_zh: BARD：通过高效渐进式块合并与分阶段蒸馏桥接自回归与扩散视觉语言模型
authors: "Baoyou Chen, Hanchen Xia, Peng Tu, Haojun Shi, Shan Mu, Weihao Yuan, Siyu Zhu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.16514v3"
tags: ["query:ardm"]
score: 10.0
evidence: 将预训练的自回归VLM转换为扩散VLM
tldr: 针对自回归多模态模型推理速度慢的问题，本文提出BARD框架，通过渐进式块合并和阶段式扩散蒸馏，将预训练的自回归模型高效转换为同架构的扩散式模型。该方法利用扩散域内的蒸馏克服了直接转换带来的性能损失，在保持强大性能的同时实现了高达3倍的推理加速，在4B和8B规模上均达到开源扩散多模态模型的领先水平。
selection_source: fresh_fetch
motivation: 自回归模型逐token解码导致推理效率低下，而直接将其转换为大块并行解码的扩散模型往往伴随严重的性能下降。
method: 采用渐进式监督块合并逐渐增大解码块，并结合阶段式扩散内蒸馏、混合噪声调度及内存友好训练来恢复大块解码下的性能。
result: BARD-VL在4B和8B规模上刷新了开源扩散多模态模型的性能记录，且推理吞吐量较原始自回归模型提升了3倍。
conclusion: 实验证明扩散域内的蒸馏比直接的自回归到扩散蒸馏更有效，为构建高效且高性能的并行解码多模态模型提供了新路径。
---

## 摘要
自回归视觉语言模型（VLM）展现了强大的多模态能力，但其逐标记（token-by-token）解码方式构成了根本性的推理瓶颈。扩散视觉语言模型（dVLM）提供了一种更具并行性的解码范式，然而，将预训练的自回归 VLM 直接转换为大块（large-block）扩散 VLM 往往会导致严重的质量下降。在这项工作中，我们提出了 BARD，这是一个简单且有效的桥接框架，可将预训练的自回归 VLM 转换为同架构且解码高效的 dVLM。我们的方法结合了渐进式有监督块合并（逐渐扩大解码块大小）以及来自固定小块扩散锚点的分阶段 dVLM 内部蒸馏，以恢复在大块设置下损失的性能。我们还引入了混合噪声调度器以提高鲁棒性和去噪过程中的标记修正，并采用内存友好型训练以实现对长多模态序列的高效训练。一个关键的经验发现是，直接从自回归到扩散的蒸馏对齐效果较差，甚至可能损害性能，而扩散机制内部的蒸馏则始终有效。实验结果表明，仅需不超过 440 万条数据，BARD-VL 就能将 Qwen3-VL 强大的多模态能力迁移到大块 dVLM 中。值得注意的是，在 4B 和 8B 规模下，BARD-VL 在我们的评估套件中均在同等规模的开源 dVLM 中达到了新的 SOTA（最先进水平）。同时，与源模型相比，BARD-VL 实现了高达 3 倍的解码吞吐量加速。代码可在以下地址获取：https://github.com/fudan-generative-vision/Bard-VL。

## Abstract
Autoregressive vision-language models (VLMs) deliver strong multimodal capability, but their token-by-token decoding imposes a fundamental inference bottleneck. Diffusion VLMs offer a more parallel decoding paradigm, yet directly converting a pretrained autoregressive VLM into a large-block diffusion VLM (dVLM) often leads to substantial quality degradation. In this work, we present BARD, a simple and effective bridging framework that converts a pretrained autoregressive VLM into a same-architecture, decoding-efficient dVLM. Our approach combines progressive supervised block merging, which gradually enlarges the decoding block size, with stage-wise intra-dVLM distillation from a fixed small-block diffusion anchor to recover performance lost at larger blocks. We further incorporate a mixed noise scheduler to improve robustness and token revision during denoising, and memory-friendly training to enable efficient training on long multimodal sequences. A key empirical finding is that direct autoregressive-to-diffusion distillation is poorly aligned and can even hurt performance, whereas distillation within the diffusion regime is consistently effective. Experimental results show that, with $\leq$ 4.4M data, BARD-VL transfers strong multimodal capability from Qwen3-VL to a large-block dVLM. Remarkably, BARD-VL establishes a new SOTA among comparable-scale open dVLMs on our evaluation suite at both 4B and 8B scales. At the same time, BARD-VL achieves up to 3$\times$ decoding throughput speedup compared to the source model. Code is available at: $\href{https://github.com/fudan-generative-vision/Bard-VL}{this~https~URL}$.

---

## 论文详细总结（自动生成）

这是一份关于论文《BARD: Bridging AutoRegressive and Diffusion Vision-Language Models Via Highly Efficient Progressive Block Merging and Stage-Wise Distillation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）视觉语言模型（VLM）虽然性能强大，但其逐标记（token-by-token）的解码方式导致推理速度慢，存在严重的推理瓶颈。
*   **研究背景**：扩散式视觉语言模型（dVLM）支持并行解码，能显著提升效率。然而，直接将预训练的自回归模型转换为大块（large-block）并行解码的扩散模型时，往往会出现严重的性能退化。
*   **整体含义**：本文提出了 **BARD** 框架，旨在通过一种平滑的转换机制，将强大的预训练自回归 VLM 转化为同架构、高效率且性能无损的扩散 VLM。

### 2. 方法论：核心思想与关键技术
BARD 的核心思想是“渐进式演进”和“扩散域内蒸馏”，主要包含以下四个关键组件：
*   **渐进式有监督块合并（Progressive Supervised Block Merging）**：
    *   不直接从自回归跳转到大块扩散解码，而是设定一个块大小序列（如 4→8→16→32）。
    *   首先将 AR 模型转换为一个小块（B=4）的扩散锚点模型，然后逐步合并相邻块，使模型平滑适应更高的并行度。
*   **阶段式 dVLM 蒸馏（Stage-wise dVLM Distillation）**：
    *   **核心发现**：直接用 AR 模型作为教师去蒸馏扩散学生模型效果较差，因为两者的 Logits（预测概率分布）在因果掩码和扩散噪声下是不对齐的。
    *   **方案**：使用固定的小块扩散模型作为“锚点教师”，对正在训练的大块扩散学生模型进行蒸馏，有效恢复了因块增大而损失的性能。
*   **混合噪声调度器（Mixed-Noise Scheduler）**：
    *   在训练中同时引入“掩码（Mask）”和“随机可见标记替换（Uniform Corruption）”。
    *   这使得模型不仅学会填充缺失信息，还学会了修正已生成的错误标记，增强了推理时的迭代修正能力。
*   **内存友好型训练（Memory-Friendly Training）**：
    *   采用打包序列布局 `[Q, x1, xt]`（多模态上下文、干净响应、噪声响应），避免了重复存储冗余的视觉特征，显著降低了长序列训练的显存开销。

### 3. 实验设计
*   **数据集**：使用 LLaVA-OneVision-1.5 和 FineVision 数据集进行监督微调，数据量最高达 4.4M 条。
*   **基准测试（Benchmarks）**：涵盖 7 个主流多模态评测集，包括 MMMU（综合推理）、MMMU-Pro、MME（感知与认知）、RealWorldQA、MMStar、AI2D（图表）和 ChartQA。
*   **对比方法**：
    *   **自回归基准**：Qwen3-VL (2B/4B/8B), InternVL3.5。
    *   **扩散式基准**：LLaDA-V, Dream-VL, LaViDa, SDAR-VL, MMaDA, Dimple-VL 等。

### 4. 资源与算力
*   **硬件环境**：使用 **8 台 NVIDIA H200 GPU** 进行模型训练。
*   **训练细节**：
    *   全局 Batch Size 为 256。
    *   语言模型学习率 2e-5，视觉编码器学习率 2e-6。
    *   使用了 AdamW 优化器和余弦退火学习率调度。
*   **推理测试**：在 4 台 NVIDIA RTX 4090 GPU 上运行指标评估。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2B、4B、8B 三个参数规模的模型。
*   **消融实验**：非常充分，分别验证了：
    1.  **数据规模的影响**：从 110K 到 4.4M 数据的性能提升曲线。
    2.  **渐进式合并的必要性**：对比了直接训练大块模型与渐进式训练的差异。
    3.  **蒸馏策略对比**：对比了“无蒸馏”、“AR 蒸馏”和“扩散锚点蒸馏”的效果。
    4.  **噪声类型**：对比了纯掩码、纯替换和混合噪声的效果。
*   **客观性**：使用了统一的 VLMEvalKit 工具包进行评估，确保了与现有 SOTA 方法对比的公平性。

### 6. 主要结论与发现
*   **性能突破**：BARD-VL 在 4B 和 8B 规模上均刷新了开源扩散 VLM 的记录，甚至在多个指标上超越了其原始的自回归源模型（Qwen3-VL）。
*   **效率提升**：相比自回归模型，BARD-VL 实现了高达 **3 倍** 的解码吞吐量加速。
*   **蒸馏洞察**：证实了在扩散模型内部进行“同域蒸馏”远比跨域（AR 到 Diffusion）蒸馏更有效。
*   **数据效率**：仅需少量数据（如 110K）即可获得极具竞争力的扩散 VLM。

### 7. 优点
*   **平滑转换**：通过渐进式块合并解决了扩散模型训练不稳定的难题。
*   **架构通用**：该框架不改变模型架构，可广泛应用于各种预训练自回归 VLM。
*   **修正能力**：混合噪声设计赋予了模型在生成过程中自我纠错的能力。
*   **训练高效**：内存友好的打包设计解决了多模态扩散模型训练中视觉标记过多的显存瓶颈。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 Qwen3-VL 系列，虽然证明了有效性，但尚未在更多样化的基座（如 Llama 或 InternLM 系列）上进行大规模验证。
*   **推理开销**：虽然并行度提高，但扩散模型通常需要多次迭代（Step），在极短序列生成上，其优势可能不如长序列生成明显。
*   **应用限制**：目前主要侧重于多模态理解任务，对于极长文本生成或纯对话任务的泛化能力讨论较少。

（完）
