---
title: Normalizing Flows with Iterative Denoising
title_zh: 结合迭代去噪的归一化流
authors: "Tianrong Chen, Jiatao Gu, David Berthelot, Joshua Susskind, Shuangfei Zhai"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20041v1"
tags: ["query:ardm"]
score: 8.0
evidence: 自回归生成后接迭代去噪过程
tldr: 本研究提出了iTARFlow，旨在提升标准化流（NF）在图像生成任务中的表现。不同于扩散模型，iTARFlow在训练时保持端到端的似然目标，而在采样阶段结合了自回归生成与受扩散模型启发的迭代去噪过程。实验证明，该方法在ImageNet不同分辨率（64到256像素）上均取得了极具竞争力的性能，为标准化流作为强大的生成模型提供了新的可能，并深入分析了其生成的伪影。
selection_source: fresh_fetch
motivation: 旨在通过改进标准化流模型，使其在图像建模任务中达到与扩散模型相媲美的生成效果。
method: 提出iTARFlow，在训练中采用端到端似然目标，采样时结合自回归生成与迭代去噪技术。
result: 在ImageNet 64、128和256分辨率下均实现了具有竞争力的生成性能。
conclusion: iTARFlow证明了标准化流在高质量图像生成领域的巨大潜力，是扩散模型之外的一种强力替代方案。
---

## 摘要
归一化流（NFs）是一类经典的基于似然的方法，近期重新受到了关注。诸如 TARFlow 等最新研究表明，NFs 能够在图像建模任务上取得极具前景的性能，使其成为扩散模型等其他方法的可行替代方案。在这项工作中，我们通过引入迭代 TARFlow（iTARFlow），进一步提升了归一化流生成模型的发展水平。与扩散模型不同，iTARFlow 在训练期间保持了完全端到端的、基于似然的优化目标。在采样过程中，它先执行自回归生成，随后进行受扩散式方法启发的迭代去噪过程。通过广泛的实验，我们展示了 iTARFlow 在 64、128 和 256 像素的 ImageNet 分辨率下均取得了具有竞争力的性能，证明了其作为强大生成模型的潜力，并推进了归一化流的前沿。此外，我们分析了 iTARFlow 产生的特征性伪影，提供了可能为未来改进带来启发的见解。代码可在 https://github.com/apple/ml-itarflow 获取。

## Abstract
Normalizing Flows (NFs) are a classical family of likelihood-based methods that have received revived attention. Recent efforts such as TARFlow have shown that   NFs are capable of achieving promising performance on image modeling tasks, making them viable alternatives to other methods such as diffusion models.   In this work, we further advance the state of Normalizing Flow generative models by introducing iterative TARFlow (iTARFlow). Unlike diffusion models, iTARFlow maintains a fully end-to-end, likelihood-based objective during training. During sampling, it performs autoregressive generation followed by an iterative denoising procedure inspired by diffusion-style methods. Through extensive experiments, we show that iTARFlow achieves competitive performance across ImageNet resolutions of 64, 128, and 256 pixels, demonstrating its potential as a strong generative model and advancing the frontier of Normalizing Flows. In addition, we analyze the characteristic artifacts produced by iTARFlow, offering insights that may shed light on future improvements. Code is available at https://github.com/apple/ml-itarflow.

---

## 论文详细总结（自动生成）

这篇论文由苹果公司（Apple）的研究团队提出，介绍了一种名为 **iTARFlow**（Iterative TARFlow）的新型生成模型。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持归一化流（Normalizing Flows, NFs）精确似然评估和连续表示优势的同时，解决其在图像生成中的“噪声困境”（Noise Dilemma）。
*   **研究背景**：
    *   **扩散模型**：视觉质量高，但推理慢且缩放定律（Scaling Laws）不如语言模型明确。
    *   **离散自回归模型**：缩放性好，但量化过程会造成信息瓶颈。
    *   **归一化流（如 TARFlow）**：虽然能处理连续空间，但面临矛盾：训练噪声小时，局部纹理丰富但全局结构差；训练噪声大时，全局结构好但细节模糊（受 Tweedie 定理限制）。

### 2. 方法论
iTARFlow 的核心思想是将**自回归生成**与**多尺度迭代去噪**相结合。
*   **核心思想**：在训练时，让模型学习一系列噪声水平 $t \in [t_{min}, t_{max}]$ 下的似然分布。在采样时，先自回归地生成一个高噪声样本，再利用似然函数导出的得分（Score）进行并行迭代去噪。
*   **关键技术细节**：
    *   **多噪声训练**：优化目标为 $\log p_\theta(x_t, t)$，其中 $x_t = x + t\epsilon$。通过在不同噪声尺度上摊销（Amortize）似然目标，使模型具备处理不同程度噪声的能力。
    *   **两阶段采样流程**：
        1.  **自回归阶段**：从高斯先验出发，按块（Patch-by-patch）顺序生成一个处于最高噪声水平 $t_{max}$ 的图像 $x_{t_{max}}$。
        2.  **并行去噪阶段**：利用自动微分计算得分函数 $\nabla_x \log p_\theta(x_t, t)$，通过求解概率 ODE（类似于 Flow Matching 或 DDIM）将噪声图像细化为清晰图像。此过程是全图像并行的。
    *   **分类器自由引导（CFG）**：仅在自回归阶段的第一层应用，以平衡生成质量与多样性。

### 3. 实验设计
*   **数据集**：ImageNet 64x64, 128x128, 256x256。
*   **Benchmark**：主要使用 FID（Fréchet Inception Distance）衡量生成质量。
*   **对比方法**：
    *   **扩散模型**：EDM, ADM, DiT, SiT 等。
    *   **自回归模型**：MAR, GIVT, Llama-Gen 等。
    *   **归一化流**：TARFlow, STARFlow, JetFormer, FARMER 等。
*   **场景**：涵盖了像素空间（Pixel-space）生成和潜空间（Latent-space）生成。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率提及**：文中提到 iTARFlow 的训练效率高于 DiT（例如在潜空间实验中，iTARFlow 在 600 epoch 时收敛，而 DiT 需要 1400 epoch）。但同时也指出，在迭代去噪阶段，由于需要对整个网络进行自动微分，**GPU 显存消耗较大**。

### 5. 实验数量与充分性
*   **实验规模**：在三个分辨率上进行了广泛测试，并提供了从 Small 到 Extra Large 不同规模的模型变体。
*   **消融实验**：针对最大噪声水平 $t_{max}$ 的选择、去噪步数（发现 5-10 步即可达到优异效果）、Patch 大小以及 CFG 尺度进行了详细的消融研究。
*   **客观性**：实验对比了当前最先进的各类模型，并诚实地报告了失败案例（如背景崩溃和特定位置的模糊），体现了学术客观性。

### 6. 主要结论与发现
*   **性能突破**：iTARFlow 显著优于之前的 TARFlow 变体，缩小了归一化流与顶级扩散模型之间的性能差距。
*   **解决噪声困境**：通过多尺度训练和迭代去噪，模型能同时获得良好的全局结构和精细的局部纹理。
*   **兼容性**：iTARFlow 的得分估计能力与专门的扩散模型（如 DiT）相当，且能与预训练的扩散后端无缝结合。

### 7. 优点
*   **理论完备**：基于精确似然框架，避免了离散化带来的信息损失。
*   **采样灵活**：结合了自回归的结构化生成和扩散式的并行细化。
*   **训练高效**：相比纯扩散模型，收敛速度更快。
*   **可扩展性**：Transformer 架构使其能够受益于模型规模的扩大。

### 8. 不足与局限
*   **显存压力**：推理时的自动微分操作对显存要求极高，限制了其在高分辨率下的应用。
*   **生成伪影**：在 ImageNet-256 像素空间实验中，存在“背景变黑”和“右下角模糊”（由于第一个自回归 Token 缺乏上下文导致误差累积）的现象。
*   **超参数敏感**：$t_{max}$ 等关键超参数目前仍需经验性调节，缺乏统一的理论指导。

（完）
