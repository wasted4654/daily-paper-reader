---
title: "DARE: Diffusion Large Language Models Alignment and Reinforcement Executor"
title_zh: DARE：扩散大语言模型对齐与强化执行器
authors: "Jingyi Yang, Yuxian Jiang, Xuhao Hu, Shuang Cheng, Biqing Qi, Jing Shao"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04215v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散大语言模型作为自回归模型的替代方案
tldr: 扩散大语言模型（dLLM）作为自回归模型的替代方案正迅速兴起，但其后训练生态系统（如强化学习、评估脚本等）高度碎片化，阻碍了研究迭代。本文提出DARE，一个统一的dLLM对齐与强化执行框架。它集成了监督微调、偏好优化及特定强化学习算法，支持多种扩散模型家族，为dLLM的开发、对比和部署提供了可复用的研究基石。
selection_source: fresh_fetch
motivation: 扩散大语言模型的研究生态因模型家族和后训练流程的碎片化，导致研究迭代缓慢且难以进行公平比较。
method: 提出DARE框架，在统一执行栈下整合了针对掩码和块扩散模型的SFT、PEFT、偏好优化及强化学习功能。
result: 在LLaDA、Dream等多个代表性模型家族上实现了广泛的算法覆盖、可复现的基准评估以及实际的计算加速。
conclusion: DARE为当前及未来扩散大语言模型的后训练方法开发、性能对比和实际部署提供了一个标准化的研究平台。
---

## 摘要
扩散大语言模型 (dLLMs) 正在成为主流自回归模型的一个极具吸引力的替代方案，它们通过迭代去噪和并行生成动态取代了严格的顺序标记生成。然而，它们的开源生态系统在不同模型家族之间，特别是在后训练流水线方面，仍然处于碎片化状态。强化学习目标、Rollout 实现和评估脚本通常作为特定论文的代码库发布。这种碎片化减缓了研究迭代速度，增加了复现的工程负担，并使得算法之间的公平比较变得困难。我们提出了 DARE (dLLMs Alignment and Reinforcement Executor)，这是一个用于 dLLMs 后训练和评估的开源框架。DARE 构建于 verl 和 OpenCompass 之上，在统一的执行栈下整合了监督微调、参数高效微调、偏好优化以及针对 dLLM 的强化学习，同时支持掩码扩散和块扩散语言模型。在包括 LLaDA、Dream、SDAR 和 LLaDA2.x 在内的代表性模型家族中，DARE 提供了广泛的算法覆盖、可复现的基准评估和实际的加速效果。广泛的实证结果表明，DARE 可作为开发、比较和部署当前及新兴 dLLMs 后训练方法的可重用研究基石。

## Abstract
Diffusion large language models (dLLMs) are emerging as a compelling alternative to dominant autoregressive models, replacing strictly sequential token generation with iterative denoising and parallel generation dynamics. However, their open-source ecosystem remains fragmented across model families and, in particular, across post-training pipelines, where reinforcement learning objectives, rollout implementations and evaluation scripts are often released as paper-specific codebases. This fragmentation slows research iteration, raises the engineering burden of reproduction, and makes fair comparison across algorithms difficult. We present \textbf{DARE} (\textbf{d}LLMs \textbf{A}lignment and \textbf{R}einforcement \textbf{E}xecutor), an open framework for post-training and evaluating dLLMs. Built on top of verl~\cite{sheng2024hybridflow} and OpenCompass~\cite{2023opencompass}, DARE unifies supervised fine-tuning, parameter-efficient fine-tuning, preference optimization, and dLLM-specific reinforcement learning under a shared execution stack for both masked and block diffusion language models. Across representative model families including LLaDA, Dream, SDAR, and LLaDA2.x, DARE provides broad algorithmic coverage, reproducible benchmark evaluation, and practical acceleration. Extensive empirical results position that DARE serves as a reusable research substrate for developing, comparing, and deploying post-training methods for current and emerging dLLMs.

---

## 论文详细总结（自动生成）

### 论文总结：DARE (Diffusion Large Language Models Alignment and Reinforcement Executor)

#### 1. 核心问题与整体含义（研究动机和背景）
扩散大语言模型（dLLMs）通过迭代去噪和并行生成，展现出超越传统自回归（AR）模型的潜力。然而，dLLM 的研究生态目前高度**碎片化**：
*   **代码库孤立**：大多数后训练（Post-training）方法（如强化学习 RL）随论文发布独立代码，缺乏统一接口。
*   **工程负担重**：dLLM 的生成逻辑（非线性、双向上下文）与现有针对自回归模型设计的 RL 框架（如基于左向右生成的框架）不兼容。
*   **比较不公平**：由于 Rollout 实现、奖励接口和评估脚本的差异，难以在相同基准下公平对比不同算法。
**DARE 的意义**在于提供了一个统一的执行框架，将多种 dLLM 家族、后训练算法和评估工具整合在同一个执行栈中，降低了研究门槛并提升了实验的可复现性。

#### 2. 核心方法论
DARE 构建于分布式训练框架 `verl` 和评估平台 `OpenCompass` 之上，其核心思想是**抽象化与解耦**：
*   **统一抽象架构**：将后训练流程抽象为 **Worker**（执行角色，如 Rollout、Actor、Critic）、**Dataflow**（数据流转，如 Prompt、轨迹、Log-prob）和 **Workflow**（优化循环）。
*   **多模型家族支持**：
    *   **掩码扩散模型 (MDLM)**：如 LLaDA、Dream，侧重于全序列迭代去噪。
    *   **块扩散模型 (BDLM)**：如 SDAR、LLaDA2.x，结合了块内扩散与块间自回归。
*   **系统级优化**：
    *   **训练侧**：使用 `FlashAttention` 的变长函数（varlen）减少 Padding 开销；针对 BDLM 集成 `FlexAttention`。
    *   **生成侧（Rollout）**：将 Rollout 与训练的 Attention 后端解耦。MDLM 使用 `Fast-dLLM` 结合 KV-Cache 加速；BDLM 集成 `LMDeploy` 和 `SGLang`。
*   **算法集成**：统一实现了 SFT、PEFT 以及多种 dLLM 特有的 RL 算法（如 VRPO、D1、Coupled-GRPO、CJ-GRPO、SPG、BGPO、EBPO 等）。

#### 3. 实验设计
*   **数据集与任务场景**：
    *   **数学推理**：GSM8K、MATH。
    *   **代码生成**：HumanEval、MBPP（使用从 DeepCoder 过滤的 16K 题目训练）。
    *   **逻辑规划**：Countdown（倒计时游戏）、Sudoku（数独）。
    *   **通用能力**：MMLU、GPQA、ARC-C、HellaSwag 等。
*   **Benchmark**：使用 OpenCompass 扩展的 dLLM 感知评估基准。
*   **对比方法**：在 LLaDA-8B 和 Dream-7B 等基座上，横向对比了上述 7 种以上的 RL 算法。

#### 4. 资源与算力
*   **算力说明**：文中**未明确给出**具体的 GPU 型号（如 A100 或 H100）及总卡数。
*   **效率提升**：
    *   对于 MDLM，通过后端解耦实现了约 **4 倍** 的端到端 RL 流水线加速。
    *   对于 BDLM，通过集成高效推理后端实现了超过 **14 倍** 的加速。
    *   SFT 迭代延迟从 22.1s 降至 10.8s（约 2 倍加速）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流 dLLM 模型家族（LLaDA, Dream, SDAR, LLaDA2.x），并在 3 大类核心任务（数学、代码、规划）上进行了详尽的 RL 算法对比。
*   **充分性**：实验不仅提供了最终分数，还展示了训练过程中的奖励曲线（Reward Curves）。
*   **客观性**：通过在统一的 DARE 框架下运行，消除了因底层实现不同导致的“算法增益假象”，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **无“万能”算法**：没有一种 RL 算法在所有任务和模型上都保持领先。例如，CJ-GRPO 在数学上表现优异，而 VRPO 在代码任务上更强。
*   **稳定性差异**：基于 ELBO（证据下界）的方法（如 SPG、BGPO）对采样噪声更敏感，容易出现奖励崩溃；而基于单步去噪优化的方法（如 D1、Coupled-GRPO）通常更稳定。
*   **模型敏感性**：同一算法在不同基座模型（如 LLaDA vs Dream）上的表现排名会发生变化，强调了统一测试框架的必要性。

#### 7. 优点
*   **高度集成**：首个统一了 MDLM 和 BDLM 两大扩散模型流派的后训练框架。
*   **系统优化深入**：针对扩散模型特有的迭代生成逻辑做了深度的算子级和后端级优化，显著提升了 RL 训练效率。
*   **科研友好**：通过解耦算法逻辑与执行栈，使研究者能像“插拔插件”一样测试新算法。

#### 8. 不足与局限
*   **模态限制**：目前仅专注于文本（dLLM），尚未扩展到多模态（如视觉-语言扩散模型）。
*   **超参数敏感性**：虽然框架统一，但不同算法对超参数（如蒙特卡洛采样数）的依赖依然严重，文中未给出通用的调优指南。
*   **硬件细节缺失**：缺少具体的算力消耗记录，不便于开发者预估实际部署成本。

（完）
