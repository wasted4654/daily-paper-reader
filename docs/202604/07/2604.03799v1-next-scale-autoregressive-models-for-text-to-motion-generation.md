---
title: Next-Scale Autoregressive Models for Text-to-Motion Generation
title_zh: 用于文本到动作生成的下一尺度自回归模型
authors: "Zhiwei Zheng, Shibo Jin, Lingjie Liu, Mingmin Zhao"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03799v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于动作生成的下一尺度自回归框架
tldr: 本研究针对文本生成动作任务，指出传统自回归模型在处理时间结构上的局限性，提出了MoScale框架。MoScale采用“下一尺度”预测机制，通过从粗到细的时间分辨率层级化生成动作，并引入跨尺度细化与尺度内双向重预测来增强鲁棒性。该方法在保持高训练效率的同时，实现了SOTA性能，并展现出卓越的扩展能力和零样本泛化能力，适用于多种动作生成与编辑任务。
selection_source: fresh_fetch
motivation: 传统的自回归模型采用逐标记预测，难以有效捕捉动作生成中复杂的长程时间结构和全局语义。
method: 提出了MoScale框架，通过层级化的“下一尺度”预测，结合跨尺度细化和尺度内时间重预测来逐步优化动作细节。
result: 该方法在文本生成动作基准测试中取得了SOTA性能，且在模型规模扩展和零样本任务中表现优异。
conclusion: MoScale证明了层级化尺度预测在动作建模中的优越性，为高效、高质量的动作生成提供了新的技术路径。
---

## 摘要
自回归 (AR) 模型具有稳定且高效的训练优势，但标准的下一标记 (next-token) 预测与文本条件动作生成所需的时间结构并不完全匹配。我们引入了 MoScale，这是一个下一尺度 (next-scale) AR 框架，它能够以从粗到细的时间分辨率分层生成动作。通过在最粗尺度上提供全局语义并逐步进行细化，MoScale 建立了一个更适合长程动作结构的因果层次体系。为了提高在有限文本-动作数据下的鲁棒性，我们进一步结合了跨尺度分层细化（用于改进每个尺度的初始预测）和尺度内时间细化（用于选择性的双向重新预测）。MoScale 以高训练效率实现了最先进的 (SOTA) 文本到动作生成性能，能随模型规模有效扩展，并可零样本泛化到多种动作生成和编辑任务。

## Abstract
Autoregressive (AR) models offer stable and efficient training, but standard next-token prediction is not well aligned with the temporal structure required for text-conditioned motion generation. We introduce MoScale, a next-scale AR framework that generates motion hierarchically from coarse to fine temporal resolutions. By providing global semantics at the coarsest scale and refining them progressively, MoScale establishes a causal hierarchy better suited for long-range motion structure. To improve robustness under limited text-motion data, we further incorporate cross-scale hierarchical refinement for improving per-scale initial predictions and in-scale temporal refinement for selective bidirectional re-prediction. MoScale achieves SOTA text-to-motion performance with high training efficiency, scales effectively with model size, and generalizes zero-shot to diverse motion generation and editing tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next-Scale Autoregressive Models for Text-to-Motion Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的文本到动作（Text-to-Motion）生成模型在捕捉**全局语义结构**（如“做两次开合跳”中的次数，或“转身、捡东西、再转身”的序列逻辑）方面表现不佳。
*   **研究动机**：
    *   **传统自回归（AR）模型**采用“下一标记预测（Next-token）”，由于人体动作具有高度的短期可预测性，模型倾向于走“捷径”，只学习局部动态而忽略了长程语义。
    *   **扩散模型和掩码 Transformer**缺乏因果顺序，在有限的数据集上难以从无序的去噪过程中推断出复杂的全局结构。
*   **整体含义**：本文提出 **MoScale**，将自回归机制从“预测下一个标记”转变为“**预测下一个尺度（Next-scale）**”，通过从粗到细的时间分辨率层级化地构建动作，从而强制模型先确定全局框架，再填充局部细节。

### 2. 方法论
MoScale 的核心思想是建立一个基于时间分辨率的因果层级体系：
*   **层级化动作表示（Residual VQ-VAE）**：使用残差向量量化技术将动作编码为 $K$ 个尺度。最粗尺度捕捉全局轨迹和姿态，后续尺度逐步添加残差细节。
*   **下一尺度自回归预测**：模型不再逐帧预测，而是以尺度为单位进行预测。给定文本和前 $k-1$ 个尺度的信息，预测第 $k$ 个尺度的所有标记。
*   **跨尺度层级细化（Cross-scale Hierarchical Refinement）**：在训练时对粗尺度预测引入随机扰动（Corrupt），强制细尺度模型学习如何纠正前序阶段的误差，从而缓解自回归模型常见的“曝光偏差”问题。
*   **尺度内时间细化（In-scale Temporal Refinement）**：在每个尺度内部，利用双向注意力机制进行“掩码-再预测（Mask-and-repredict）”，通过多次迭代优化该尺度下的动作一致性。

### 3. 实验设计
*   **数据集**：使用了两个主流基准数据集：**HumanML3D**（约 1.45 万个动作）和 **KIT-ML**（约 3.9 千个动作）。
*   **评估指标**：R-Precision（文本-动作匹配度）、FID（生成质量）、MM-Dist（多模态距离）、Diversity（多样性）。
*   **对比方法**：
    *   **自回归类**：TM2T, T2M-GPT, AttT2M, ParCo。
    *   **扩散模型类**：MDM, MLD, MotionDiffuse, ReMoDiffuse。
    *   **掩码 Transformer 类**：MMM, MoMask, MoMask++。

### 4. 资源与算力
*   **训练时长**：论文图 3 显示，MoScale 在 HumanML3D 上的训练时间约为 **20-30 小时**（取决于模型大小，分为 Tiny, Small, Medium, Large 四个版本）。
*   **算力设备**：文中**未明确说明**具体的 GPU 型号和数量，但强调了其训练效率高于 ParCo 和 AttT2M 等改进型自回归模型。
*   **推理速度**：推理耗时在 0.08s 到 0.28s 之间，比扩散模型（约 7s）快得多。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个标准数据集上进行了完整的定量对比。
    *   针对**文本复杂度**（低、中、高）进行了分级评估，证明了 MoScale 在处理复杂指令时的优势。
    *   进行了详尽的**消融实验**，涵盖了层级细化、时间细化、扰动率（$\gamma_{max}$）、Transformer 深度、迭代次数等变量。
    *   包含**用户主观研究**（20 人参与），对比了文本对齐度和动作真实感。
*   **充分性评价**：实验设计非常充分且客观。通过引入视觉语言模型（VLM）对生成动作进行细粒度评分，进一步增强了实验的说服力。

### 6. 主要结论与发现
*   **性能领先**：MoScale 在所有主要指标上均达到了 SOTA 水平，尤其是在 R-Precision 上显著超过了之前的模型。
*   **层级预测的价值**：实验证明“从粗到细”的预测顺序比无序预测（如掩码模型）更能有效地对齐复杂文本语义。
*   **扩展性**：模型性能随参数量增加而稳步提升，展现出良好的 Scaling Law 特性。
*   **零样本能力**：MoScale 无需额外训练即可直接应用于动作编辑、补全（Inpainting）和长动作生成。

### 7. 优点
*   **创新的架构设计**：通过“下一尺度”预测巧妙解决了自回归模型在动作生成中的“局部捷径”问题。
*   **鲁棒性强**：跨尺度细化机制有效解决了层级生成中的误差累积。
*   **多功能性**：一个模型统一了生成、编辑和预测等多种任务，且保持了极高的推理效率。
*   **语义对齐度高**：在处理包含数字、顺序动作的复杂文本时表现远超同类方法。

### 8. 不足与局限
*   **推理延迟略增**：虽然比扩散模型快，但由于引入了尺度内迭代细化，其推理速度略慢于最简单的掩码模型（如 MoMask）。
*   **离散化限制**：依赖于 VQ-VAE 的离散代码本，重建质量受限于代码本的表达能力（虽然文中提到其生成与重建的差距已显著缩小）。
*   **数据依赖**：尽管在小数据集上表现良好，但对于极其罕见或超出训练分布的动作，仍可能存在泛化风险。

（完）
