---
title: Coordinate-Based Dual-Constrained Autoregressive Motion Generation
title_zh: 基于坐标的双重约束自回归运动生成
authors: "Kang Ding, Hongsong Wang, Jie Gui, Liang Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08088v1"
tags: ["query:ardm"]
score: 8.0
evidence: 将自回归范式与运动生成相结合，作为扩散模型的替代方案
tldr: 本文针对文本生成动作任务中扩散模型的误差放大和自回归模型的模式崩溃问题，提出了CDAMD框架。该框架直接采用运动坐标作为输入，结合自回归范式与扩散启发的MLP来提升动作保真度。通过引入双约束因果掩码，将运动标记作为先验与文本编码结合以引导生成。此外，研究还建立了坐标驱动的动作合成与编辑新基准。实验证明，CDAMD在保真度和语义一致性上均达到了领先水平。
selection_source: fresh_fetch
motivation: 旨在解决扩散模型在噪声预测中的误差放大问题，以及自回归模型因运动离散化导致的模式崩溃问题。
method: 提出基于坐标输入的自回归框架CDAMD，利用扩散启发的MLP增强保真度，并采用双约束因果掩码结合运动先验与文本编码。
result: 在文本生成动作和动作编辑的新基准测试中，该方法在动作保真度和语义一致性方面均取得了当前最优性能。
conclusion: CDAMD通过结合坐标表示与改进的自回归机制，为实现灵活、高保真且语义准确的动作生成提供了有效的技术路径。
---

## 摘要
文本到运动生成近年来在研究界引起了越来越多的关注，在动画、虚拟现实、机器人技术和人机交互等领域具有潜在的应用前景。扩散模型和自回归模型是文本到运动生成的两个流行且并行的研究方向。然而，扩散模型在噪声预测过程中经常面临误差放大问题，而自回归模型则由于运动离散化而表现出模式崩溃。为了解决这些局限性，我们提出了一种灵活、高保真且语义忠实的文本到运动框架，命名为基于坐标的双重约束自回归运动生成（CDAMD）。CDAMD 以运动坐标作为输入，遵循自回归范式，并利用受扩散模型启发的多层感知器来增强预测运动的保真度。此外，本文引入了双重约束因果掩码来引导自回归生成，其中运动标记作为先验并与文本编码拼接。由于目前关于基于坐标的运动合成研究较少，我们为文本到运动生成和运动编辑建立了新的基准。实验结果表明，我们的方法在这些基准上的保真度和语义一致性方面均达到了最先进的性能。

## Abstract
Text-to-motion generation has attracted increasing attention in the research community recently, with potential applications in animation, virtual reality, robotics, and human-computer interaction. Diffusion and autoregressive models are two popular and parallel research directions for text-to-motion generation. However, diffusion models often suffer from error amplification during noise prediction, while autoregressive models exhibit mode collapse due to motion discretization. To address these limitations, we propose a flexible, high-fidelity, and semantically faithful text-to-motion framework, named Coordinate-based Dual-constrained Autoregressive Motion Generation (CDAMD). With motion coordinates as input, CDAMD follows the autoregressive paradigm and leverages diffusion-inspired multi-layer perceptrons to enhance the fidelity of predicted motions. Furthermore, a Dual-Constrained Causal Mask is introduced to guide autoregressive generation, where motion tokens act as priors and are concatenated with textual encodings. Since there is limited work on coordinate-based motion synthesis, we establish new benchmarks for both text-to-motion generation and motion editing. Experimental results demonstrate that our approach achieves state-of-the-art performance in terms of both fidelity and semantic consistency on these benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Coordinate-Based Dual-Constrained Autoregressive Motion Generation》（CDAMD）的结构化总结：

### 1. 研究动机与背景
*   **核心问题**：现有的文本生成运动（Text-to-Motion）方法主要分为扩散模型和自回归模型。扩散模型在噪声预测中容易产生误差累积；自回归模型则因运动离散化（Tokenization）导致精细细节丢失，进而引发模式崩溃（Mode Collapse）。
*   **表示冗余**：传统方法多采用关节位置与旋转角度的混合表示，存在数据冗余且不符合用户直觉（用户更倾向于指定坐标）。
*   **研究目标**：提出一种基于坐标输入的、结合自回归范式与扩散优化机制的框架，以实现高保真度、语义一致且易于编辑的运动生成。

### 2. 方法论
*   **核心思想**：采用纯坐标表示，通过自回归 Transformer 生成潜在表示，并利用扩散启发的多层感知器（Diffusion MLPs）进行精细化修正。
*   **关键技术细节**：
    *   **双重运动编码器**：同时训练一个确定性自编码器（AE）获取连续潜在空间，以及一个残差矢量量化自编码器（RVQ-VAE）获取离散运动标记（Tokens）作为先验。
    *   **双约束因果掩码（DCCA）**：在 Transformer 中引入一种新的注意力掩码机制，同时约束“时间因果性”（当前帧仅看过去）和“条件因果性”（生成位置对文本和运动先验的可见性），确保生成过程的严格自回归一致性。
    *   **混合条件引导**：将 CLIP 文本嵌入与 RVQ-VAE 提取的运动标记拼接，弥补单一文本描述在捕捉运动动力学细节上的不足。
    *   **扩散 MLP 细化**：在自回归解码后，使用轻量级的 Diffusion MLPs（基于 SiT 或 DDPM 架构）对潜在序列进行迭代去噪，提升运动的真实感。

### 3. 实验设计
*   **数据集**：使用了两大主流基准数据集 **HumanML3D** 和 **KIT-ML**。
*   **Benchmark 建立**：由于现有工作多基于旋转角，作者重新建立了基于“绝对关节坐标”的评估基准，并重新实现/训练了多个 SOTA 模型进行对比。
*   **对比方法**：包括 MDM、MotionDiffuse、ReMoDiffuse、MotionLCM、MMM、MoMask、BAMM、MARDM 等 10 余种代表性模型。
*   **评估指标**：R-Precision（Top 1/2/3）、FID（保真度）、Matching Score（语义匹配）、Diversity（多样性）以及 CLIP-score。

### 4. 资源与算力
*   **训练参数**：HumanML3D 批大小为 64，KIT-ML 为 16；最大运动长度 196 帧。
*   **训练时长**：AE 和 RVQ-VAE 预训练 50 轮，Transformer 主网络训练 500 轮。
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 A100 或 V100）及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个数据集上进行了完整的文本生成运动对比。
    *   针对运动编辑任务（补全、外推、前缀/后缀生成）进行了 4 类专项实验。
    *   进行了详尽的消融实验，验证了运动先验（CMP）、双约束掩码（DCCA）以及不同量化层数的影响。
    *   包含 20 人参与的人类主观评价实验（User Study）。
*   **充分性评价**：实验设计较为全面，通过重新实现基准模型确保了在坐标维度下的公平对比，消融实验逻辑清晰，客观地展示了模型在保真度与多样性之间的权衡。

### 6. 主要结论与发现
*   **性能领先**：CDAMD 在 HumanML3D 上的 FID 达到 0.046，显著优于 MoMask（0.047）和 BAMM（0.060），同时在 R-Precision 上保持领先。
*   **坐标优势**：证明了直接使用绝对坐标进行运动生成不仅可行，且在语义对齐和编辑灵活性上具有优势。
*   **掩码作用**：双约束因果掩码比传统的双向掩码能产生更符合物理规律和文本语义的运动分布。

### 7. 优点
*   **架构创新**：巧妙结合了自回归的序列建模能力与扩散模型的精细修正能力。
*   **控制灵活**：基于坐标的输入使得运动编辑（如 Inpainting）更加直观，符合实际应用需求。
*   **语义忠实**：通过引入离散运动标记作为先验，有效解决了纯文本引导下动作细节模糊的问题。

### 8. 不足与局限
*   **推理延迟**：由于采用了自回归预测加扩散迭代细化的两阶段过程，其推理速度可能慢于单步生成模型。
*   **量化依赖**：生成的质量在很大程度上取决于 RVQ-VAE 离散标记的质量，若量化过程丢失过多信息，后续修正也难以完全弥补。
*   **极端案例失败**：在处理极其复杂的空间指令（如“右腿抬起平衡在左腿上”但生成了交替站立）时，仍存在语义理解偏差。

（完）
