---
title: Accelerating Speculative Decoding with Block Diffusion Draft Trees
title_zh: 利用块扩散草稿树加速投机解码
authors: "Liran Ringel, Yaniv Romano"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12989v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于自回归语言模型的扩散草稿器
tldr: 投机采样通过轻量级草稿模型加速大模型推理。本文提出 DDTree，基于高性能的块扩散草稿模型 DFlash，通过构建草稿树而非单一序列来提高接受率。DDTree 利用最佳优先堆算法从扩散模型的分布中筛选高概率路径，并在单次验证中并行处理，显著提升了推理速度，达到了投机采样的领先水平。
selection_source: fresh_fetch
motivation: 现有的块扩散草稿模型虽然生成速度快，但每轮仅验证单一路径，限制了平均接受长度和加速潜力。
method: 提出 DDTree 方法，利用最佳优先堆算法从扩散模型的每位置分布中构建草稿树，并通过祖先注意力掩码实现单次并行验证。
result: DDTree 在 DFlash 的基础上进一步提升了投机采样的性能，超越了 EAGLE-3 等强力自回归草稿模型。
conclusion: DDTree 证明了将扩散草稿模型与树状验证结合能有效提升大语言模型的推理效率，是当前投机采样的前沿方案。
---

## 摘要
投机解码通过使用轻量级草稿模型预测多个未来词元，并由目标模型进行并行验证，从而加速自回归语言模型。DFlash 表明，块扩散草稿模型可以在单次前向传播中生成整个草稿块，并取得最先进的投机解码性能，优于 EAGLE-3 等强力自回归草稿模型。然而，原始的 DFlash 每轮仍仅验证单条草稿轨迹，这可能限制了其接受长度。我们提出了 DDTree（扩散草稿树），这是一种直接从块扩散草稿模型的逐位置分布中构建草稿树的方法。在固定节点预算下，DDTree 使用简单的最佳优先堆算法，根据草稿模型输出定义的代理指标，选择最可能与目标模型匹配的后续序列。生成的树通过仅祖先注意力掩码，在目标模型的单次前向传播中得到高效验证。由于 DDTree 构建在领先的投机解码草稿模型 DFlash 之上，这些提升使 DDTree 跻身投机解码的领先方法之列。

## Abstract
Speculative decoding accelerates autoregressive language models by using a lightweight drafter to propose multiple future tokens, which the target model then verifies in parallel. DFlash shows that a block diffusion drafter can generate an entire draft block in a single forward pass and achieve state-of-the-art speculative decoding performance, outperforming strong autoregressive drafters such as EAGLE-3. Vanilla DFlash, however, still verifies only a single drafted trajectory per round, potentially limiting its acceptance length. We introduce DDTree (Diffusion Draft Tree), a method that constructs a draft tree directly from the per-position distributions of a block diffusion drafter. Under a fixed node budget, DDTree uses a simple best-first heap algorithm to select the continuations that are most likely to match the target model according to a surrogate defined by the draft model's output. The resulting tree is verified efficiently in a single target model forward pass using an ancestor-only attention mask. Because DDTree builds on DFlash, a leading draft model for speculative decoding, these gains place DDTree among the leading approaches to speculative decoding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Accelerating Speculative Decoding with Block Diffusion Draft Trees》（利用块扩散草稿树加速投机解码）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：投机解码（Speculative Decoding）通过轻量级草稿模型预测未来词元来加速大语言模型（LLM）推理。现有的先进方法 **DFlash** 利用“块扩散”（Block Diffusion）在单次前向传播中生成整个草稿块，性能优于传统的自回归草稿器。然而，原始 DFlash 每轮仅验证一条草稿路径，未能充分利用扩散模型输出的丰富概率分布，限制了平均接受长度。
*   **研究动机**：如何利用块扩散草稿器产生的逐位置分布，在不显著增加验证开销的前提下，筛选出最有可能被目标模型接受的多条候选路径（即构建“草稿树”），从而进一步提升加速比。

### 2. 方法论：DDTree (Diffusion Draft Tree)
*   **核心思想**：直接从块扩散草稿器单次生成的逐位置边缘分布中，构建一个在给定节点预算 $B$ 下最优的草稿树。
*   **关键技术细节**：
    *   **代理目标函数**：由于目标模型的真实分布未知，DDTree 定义了一个基于草稿模型输出的“因子化分布”作为代理指标，旨在最大化该分布下的期望接受长度。
    *   **最佳优先堆算法（Best-first Heap Algorithm）**：
        1.  草稿器单次运行得到未来 $L$ 个位置的词元概率分布。
        2.  使用最大堆（Max-heap）维护候选前缀，初始为概率最高的第一个词元。
        3.  每次从堆中弹出概率最高的前缀，并将其“子节点”（下一位置的最优词元）和“兄弟节点”（当前位置的次优词元）推入堆中。
        4.  重复此过程直到达到节点预算 $B$，该算法被证明能在线性对数时间内找到代理目标下的最优树结构。
    *   **树状验证**：将构建的树展平，利用“树状注意力掩码”（Tree Attention Mask）在目标模型的一次前向传播中并行验证所有路径。

### 3. 实验设计
*   **目标模型**：Qwen3-4B、Qwen3-8B、Qwen3-Coder-30B-A3B-Instruct。
*   **草稿模型**：对应的 DFlash 检查点。
*   **数据集（Benchmark）**：涵盖 10 个主流评测集，包括：
    *   **推理/数学**：MATH-500, GSM8K, AIME 2024, AIME 2025。
    *   **代码**：HumanEval, MBPP, LiveCodeBench, SWE-bench Lite。
    *   **通用/对话**：MT-Bench, Alpaca。
*   **对比方法**：自回归解码（Baseline）、原始 DFlash（单路径验证）。
*   **评测指标**：端到端加速比（Speedup）、平均接受长度（$\tau$）。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 台 NVIDIA H200 GPU** 上运行。
*   **实现细节**：使用 Hugging Face Transformers 库实现；推理采用 bfloat16 精度；目标模型使用标准 PyTorch 缩放点积注意力（因为 FlashAttention-2 当时不支持自定义树状掩码）。
*   **训练时长**：文中未提及草稿模型的训练时长，主要关注推理阶段的性能表现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个数据集、3 种不同规模的目标模型、2 种解码温度（0.0 和 1.0），共计 **60 组核心对比实验**。
*   **消融与分析**：
    *   **预算权衡实验**：测试了从 16 到 1024 个节点的不同预算对加速比的影响。
    *   **分布分析**：对比了 DDTree 与 DFlash 的接受长度分布直方图。
*   **充分性评价**：实验设计非常充分，覆盖了从逻辑推理到代码生成的多种场景，且在所有测试设置下 DDTree 均表现出一致的提升，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：DDTree 在所有测试用例中均优于原始 DFlash。在 Qwen3-30B 模型上，相对于自回归解码实现了高达 **8.2倍** 的加速。
*   **接受长度**：通过构建草稿树，DDTree 显著增加了每轮验证被接受的词元数量，尤其是在数学和代码等逻辑性强的任务中。
*   **预算平衡**：实验发现节点预算并非越大越好，通常在 256 到 512 个节点时达到加速比峰值，过大的预算会导致验证开销超过收益。

### 7. 优点
*   **理论完备性**：提出了明确的代理优化目标，并证明了所提堆算法在构建最优树上的有效性。
*   **极低开销**：相比于自回归草稿树（如 EAGLE-2）需要多次草稿器前向传播，DDTree 仅需 **单次** 扩散草稿器运行，极大降低了草稿阶段的延迟。
*   **通用性**：作为 DFlash 的增强插件，能够直接应用于现有的块扩散草稿模型。

### 8. 不足与局限
*   **算力库兼容性**：由于树状注意力的特殊性，无法直接利用 FlashAttention-2 的硬件加速，这在一定程度上削弱了目标模型验证阶段的效率。
*   **代理目标偏差**：算法优化的是草稿模型的因子化分布，而非目标模型的真实分布。如果草稿模型与目标模型分布偏差较大，树的质量会受限。
*   **硬件依赖**：最优节点预算高度依赖于具体的 GPU 算力和内存带宽，用户在不同平台上可能需要重新调优预算参数。

（完）
