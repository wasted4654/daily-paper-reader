---
title: On the Trainability of Masked Diffusion Language Models via Blockwise Locality
title_zh: 论通过分块局部性提升掩码扩散语言模型的可训练性
authors: "Yuxiang Wang, Yu Xiang, Baojian Zhou, Qifang Zhao, Keyue Jiang, Yanghua Xiao, Xiaoxiao Xu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24832v1"
tags: ["query:ardm"]
score: 9.0
evidence: 比较掩码扩散语言模型与自回归大语言模型
tldr: 掩码扩散语言模型（MDM）虽具潜力但训练稳定性较差。本文通过线性回归、路径寻找和数独任务对比了分块MDM与自回归模型，发现标准随机掩码MDM在有序生成任务中表现不佳。为此，研究者提出了Jigsaw和Scatter两种具备局部感知能力的模型，通过在块内引入自回归偏置并保留块级迭代优化，显著提升了训练稳定性和生成性能。
selection_source: fresh_fetch
motivation: 针对掩码扩散语言模型在处理具有强顺序依赖或结构化生成任务时存在的训练不稳定和性能瓶颈问题。
method: 提出了Jigsaw和Scatter两种局部感知分块模型，在块内强制执行自回归局部性，同时在块间保留扩散模型的迭代优化机制。
result: Jigsaw在线性回归任务中达到了与自回归模型相当的稳定性，而Scatter在路径寻找任务中保留了扩散模型的规划优势。
conclusion: 标准随机掩码MDM在有序生成任务中并非最优，引入局部归纳偏置是提升扩散语言模型训练稳定性和性能的关键。
---

## 摘要
掩码扩散语言模型 (MDMs) 最近已成为标准自回归大语言模型 (AR-LLMs) 的一种有前景的替代方案，但其优化过程可能明显不够稳定。我们研究了分块 MDMs，并在三个强调结构化生成不同方面的受控任务上将其与 AR-LLMs 进行了比较：上下文线性回归、图路径寻找和数独求解。我们发现，标准的随机掩码 MDMs 无法可靠地学习线性回归，在图路径寻找任务中表现出高方差的训练动态，但在数独任务上优于 AR-LLMs。为了缓解这些不稳定性，我们提出了两种具有局部感知能力的分块模型，即 Jigsaw 和 Scatter。它们通过在块内强制执行自回归局部性，同时在块级别保留迭代细化，从而注入了从左到右的归纳偏置。实验表明，Jigsaw 在线性回归上的稳定性与 AR-LLM 相当，且在数独上保持了强劲表现，而 Scatter 则保留了扩散模型在路径寻找上的规划优势。我们的结果表明，标准的随机掩码 MDMs（即使是分块变体）对于有序生成而言，可能并非扩散语言模型的最优实例化方式，这激励了对随机掩码之外的模型的研究。

## Abstract
Masked diffusion language models (MDMs) have recently emerged as a promising alternative to standard autoregressive large language models (AR-LLMs), yet their optimization can be substantially less stable. We study blockwise MDMs and compare them with AR-LLMs on three controlled tasks that stress different aspects of structured generation: in-context linear regression, graph path-finding, and Sudoku solving. We find that standard random-masking MDMs fail to reliably learn linear regression, exhibit high variance training dynamics on graph path-finding, while outperforming AR-LLMs on Sudoku. To mitigate these instabilities, we propose two locality aware blockwise models, namely Jigsaw and Scatter, that inject left-to-right inductive bias by enforcing autoregressive locality within blocks while preserving iterative refinement at the block level. Empirically, Jigsaw matches AR-LLM stability on linear regression and remains strong on Sudoku, while Scatter retains diffusion's planning advantage on path-finding. Our results indicate that standard random-masking MDMs, even with blockwise variants, may be a suboptimal instantiation of diffusion LMs for ordered generation, motivating models beyond random masking.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了掩码扩散语言模型（MDM）在训练稳定性上的挑战，并提出了通过引入局部性归纳偏置来优化模型的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然掩码扩散模型（MDM）在并行生成和迭代优化方面展现出潜力，但其训练过程往往比传统的自回归模型（AR-LLM）更不稳定。
*   **核心矛盾**：AR模型具备强烈的“从左到右”局部归纳偏置，擅长处理顺序依赖任务；而MDM采用全局随机掩码，擅长全局规划（如数独），但在需要精确局部绑定（如线性回归）的任务中表现糟糕。
*   **研究目标**：探索如何结合自回归的局部性偏置与扩散模型的全局规划能力，提升MDM的可训练性和泛化性。

### 2. 方法论：Jigsaw 与 Scatter
论文在分块扩散（Blockwise Diffusion）的基础上提出了两种改进架构：
*   **Scatter（同步并行自回归）**：
    *   **核心思想**：将序列分为多个块，在每个宏观步骤中，同步生成所有块的第 $j$ 个标记。
    *   **技术细节**：设计了“Scatter因果掩码”，确保标记只能关注其他块中偏移量更小的标记，从而在保持块内自回归局部性的同时，实现跨块的并行去噪。
*   **Jigsaw（熵引导动态规划）**：
    *   **核心思想**：采用“先易后难”的策略，动态决定块的生成顺序。
    *   **流程**：
        1.  **探测（Probe）**：计算所有未生成块的预测熵（不确定性）。
        2.  **选择**：挑选熵最小（最确定）的块。
        3.  **求解（Solve）**：对该块进行局部自回归解码，并将其作为后续生成的固定锚点。
*   **训练策略**：使用随机独立掩码（Random Independence Mask），强制模型仅依赖已确定的上下文进行推理，避免无效的全局相关性干扰。

### 3. 实验设计
*   **实验场景**：
    1.  **上下文线性回归 (ICL)**：测试模型提取潜在数学算子并进行精确特征绑定的能力。
    2.  **星形图路径寻找**：测试逆向规划能力（逻辑依赖方向与生成方向相反）。
    3.  **数独求解**：测试全局约束满足能力。
    4.  **自然语言建模 (LM1B)**：在真实语言数据集上验证方法的可迁移性。
*   **对比基准 (Benchmarks)**：
    *   **AR基准**：基于LLaMA/Qwen架构的自回归模型。
    *   **扩散基准**：LLaDA、Dream等标准掩码扩散模型。
    *   **分块基准**：SDAR（分块自回归扩散）。
*   **评估指标**：均方误差 (MSE)、序列准确率、困惑度 (PPL)。

### 4. 资源与算力
*   **算力统计**：论文通过 PyTorch profiling 详细测量了不同任务达到特定性能阈值所需的累积训练 FLOPs（计算成本）。
*   **硬件说明**：文中未明确给出具体的 GPU 型号和数量（如 A100 数量），但强调了在相同参数规模（Small ~4M, Medium ~30M, Large ~60M）下进行公平对比。
*   **训练时长**：线性回归任务训练了 1M 到 2M 个步骤。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到真实语言建模的多个维度，并进行了跨模型规模（Small/Medium/Large）的扩展性实验。
*   **消融实验**：非常充分。包括块大小 ($S$) 的影响、坐标嵌入的必要性、掩码调度策略、以及数据多样性（单轮 vs 多轮训练）对“结构性顿悟”的影响。
*   **客观性**：通过参数对齐（Parameter Parity）确保了 AR 和 MDM 在模型容量上的公平竞争。

### 6. 主要结论与发现
*   **范式反转**：在数独和路径寻找等规划任务中，MDM 远优于 AR；但在顺序绑定任务（线性回归）中，标准 MDM 经常优化失败。
*   **局部性的重要性**：引入局部自回归偏置（Jigsaw）能显著提升 MDM 在有序生成任务中的稳定性，使其达到与 AR 相当的水平。
*   **Jigsaw 的局限**：在路径寻找任务中，Jigsaw 的块内自回归限制了逆向逻辑链的传递，导致其表现不如 Scatter 或标准 MDM。
*   **随机掩码的缺陷**：对于具有强各向异性（顺序依赖）的任务，各向同性的随机掩码是次优的，会导致训练方差巨大。

### 7. 优点
*   **深入的归纳偏置分析**：不仅提出了模型，还从概率分解的角度解释了为什么不同任务需要不同的生成顺序。
*   **创新的规划机制**：Jigsaw 的熵引导策略为非单调文本生成提供了一个可解释且高效的框架。
*   **任务设计的针对性**：三个受控任务精准地隔离了规划、绑定和约束满足三个核心能力。

### 8. 不足与局限
*   **推理延迟**：虽然 Jigsaw 和 Scatter 优化了训练，但其推理复杂度仍为 $O(L)$ 或受限于块大小，在极长序列下的推理速度优势尚不明显。
*   **超参数敏感性**：块大小 $S$ 对性能影响较大，不同任务需要手动调整以寻找“金发姑娘区”（最优平衡点）。
*   **语言模型规模**：在 LM1B 上的实验规模较小（最高 350M 参数），尚未在大规模参数（如 7B+）和更复杂的多轮对话任务上验证。

（完）
