---
title: "Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM"
title_zh: Fast-dVLM：通过从自回归 VLM 直接转换实现的高效块扩散 VLM
authors: "Chengyue Wu, Shiyi Lan, Yonggan Fu, Sensen Gao, Jin Wang, Jincheng Yu, Jose M. Alvarez, Pavlo Molchanov, Ping Luo, Song Han, Ligeng Zhu, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06832v1"
tags: ["query:ardm"]
score: 9.0
evidence: 从自回归VLM直接转换为块扩散VLM
tldr: 针对自回归视觉语言模型（VLM）推理速度受限的问题，本文提出 Fast-dVLM。该模型通过将预训练的自回归 VLM 直接转换为块扩散（Block-Diffusion）模型，实现了 KV 缓存兼容的并行解码。通过引入块大小退火和因果上下文注意力等优化技术，Fast-dVLM 在保持与自回归模型相当的生成质量的同时，实现了超过 6 倍的推理加速，显著提升了边缘设备上的推理效率。
selection_source: fresh_fetch
motivation: 自回归解码在边缘设备上受限于内存带宽，导致硬件并行度利用不足，难以满足机器人和自动驾驶等实时场景的需求。
method: 提出一种直接将全量自回归 VLM 转换为块扩散模型的方法，并结合块大小退火、因果上下文注意力和视觉高效拼接等技术进行优化。
result: 在 11 个多模态基准测试中，Fast-dVLM 的生成质量与自回归基准相当，且在集成 SGLang 和 FP8 量化后实现了 6 倍以上的端到端推理加速。
conclusion: 直接从预训练 VLM 转换块扩散模型是一种高效且保质的加速方案，为实时多模态 AI 应用提供了有力支持。
---

## 摘要
视觉语言模型 (VLMs) 主要依赖自回归解码，这种方式一次生成一个 token，从根本上限制了推理吞吐量。这种限制在机器人和自动驾驶等物理 AI 场景中尤为突出，在这些场景中，VLM 以 batch size 为 1 部署在边缘设备上，使得自回归 (AR) 解码受限于内存带宽，导致硬件并行性利用不足。虽然块级离散扩散在并行文本生成方面展现出潜力，但由于需要同时处理连续视觉表示和离散文本 token，并保持预训练的多模态能力，将其扩展到 VLM 仍具挑战性。我们提出了 Fast-dVLM，这是一种基于块扩散的 VLM，支持与 KV 缓存兼容的并行解码和投机块解码，以实现推理加速。我们系统地比较了两种 AR 到扩散的转换策略：一种是两阶段方法，即在多模态训练之前先通过纯文本扩散微调来适配 LLM 主干网络；另一种是直接方法，即在一个阶段内转换完整的 AR VLM。在相当的训练预算下，直接转换通过利用已经实现多模态对齐的 VLM，证明了其效率显著更高；因此，我们将其作为推荐方案。我们引入了一系列多模态扩散适配技术，包括块大小退火、因果上下文注意力、自动截断掩码和视觉高效拼接，这些技术共同实现了 VLM 环境下的有效块扩散。在 11 个多模态基准测试上的广泛实验表明，Fast-dVLM 在生成质量上与其自回归对应模型相当。通过 SGLang 集成和 FP8 量化，Fast-dVLM 相比 AR 基准实现了超过 6 倍的端到端推理加速。

## Abstract
Vision-language models (VLMs) predominantly rely on autoregressive decoding, which generates tokens one at a time and fundamentally limits inference throughput. This limitation is especially acute in physical AI scenarios such as robotics and autonomous driving, where VLMs are deployed on edge devices at batch size one, making AR decoding memory-bandwidth-bound and leaving hardware parallelism underutilized. While block-wise discrete diffusion has shown promise for parallel text generation, extending it to VLMs remains challenging due to the need to jointly handle continuous visual representations and discrete text tokens while preserving pretrained multimodal capabilities. We present Fast-dVLM, a block-diffusion-based VLM that enables KV-cache-compatible parallel decoding and speculative block decoding for inference acceleration. We systematically compare two AR-to-diffusion conversion strategies: a two-stage approach that first adapts the LLM backbone with text-only diffusion fine-tuning before multimodal training, and a direct approach that converts the full AR VLM in one stage. Under comparable training budgets, direct conversion proves substantially more efficient by leveraging the already multimodally aligned VLM; we therefore adopt it as our recommended recipe. We introduce a suite of multimodal diffusion adaptations, block size annealing, causal context attention, auto-truncation masking, and vision efficient concatenation, that collectively enable effective block diffusion in the VLM setting. Extensive experiments across 11 multimodal benchmarks show Fast-dVLM matches its autoregressive counterpart in generation quality. With SGLang integration and FP8 quantization, Fast-dVLM achieves over 6x end-to-end inference speedup over the AR baseline.

---

## 论文详细总结（自动生成）

以下是对论文《Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉语言模型（VLM）主要依赖自回归（AR）解码，即逐个生成 token。在机器人、自动驾驶等物理 AI 场景中，模型通常以 Batch Size=1 部署在边缘设备上，这使得推理受限于内存带宽，导致硬件并行计算能力被大量闲置。
*   **核心目标**：引入“块级离散扩散”（Block-wise Discrete Diffusion）机制，使 VLM 能够并行生成多个 token，同时保持与 KV 缓存（KV-cache）的兼容性，从而在不损失精度的前提下大幅提升推理吞吐量。

### 2. 方法论
*   **核心思想**：将序列划分为固定大小的块（Block），块与块之间保持自回归顺序，而块内部的 token 则通过离散扩散模型并行去噪生成。
*   **关键技术细节**：
    *   **直接转换策略（Direct Conversion）**：放弃先转换 LLM 再进行多模态训练的两阶段法，直接从预训练好的 AR VLM 开始进行多模态扩散微调。实验证明这能更好地继承已有的多模态对齐能力。
    *   **块大小退火（Block-size Annealing）**：训练时从小块开始，逐渐增加到目标块大小（如 32），帮助模型学习从细粒度到粗粒度的去噪。
    *   **因果上下文注意力（Causal Context Attention）**：在块内去噪时，对之前的上下文使用因果掩码，以保留预训练模型的顺序推理能力，并支持自回归验证。
    *   **视觉高效拼接（Vision-efficient Concatenation）**：由于视觉 token 不需要被掩码去噪，因此仅将其放入“干净流”中，避免在“噪声流”中重复计算，节省约 15% 的显存。
    *   **自投机块解码（Self-speculative Block Decoding）**：利用模型自身的扩散模式作为“草稿器”并行生成块，再利用其因果模式作为“验证器”进行快速校验。
*   **训练损失**：结合了扩散损失（$\mathcal{L}_{diff}$）和传统的自回归交叉熵损失（$\mathcal{L}_{AR}$），权重各占 0.5。

### 3. 实验设计
*   **基准模型**：以 Qwen2.5-VL-3B 为主干网络。
*   **数据集**：涵盖 11 个多模态基准测试，包括：
    *   **短回答任务**：AI2D, ChartQA, DocVQA, GQA, MMBench, MMMU, POPE, RealWorldQA, SEEDBench2+, TextVQA。
    *   **长回答/推理任务**：MMMU-Pro-V（考察思维链推理）。
*   **对比对象**：
    *   **AR 基准**：VILA-1.5, MiniCPM-V-2, Intern-VL-2.5, Qwen2.5-VL。
    *   **扩散 VLM 基准**：LaViDa, Dimple, LLaDA-V 等。

### 4. 资源与算力
*   **训练算力**：使用了 **64 张 NVIDIA H100 GPU**（8 个节点）。
*   **训练配置**：采用 DeepSpeed ZeRO Stage-2，BF16 混合精度，训练 1 个 epoch（约 200 万个多模态样本）。
*   **推理测试**：在单张 NVIDIA H100 上进行吞吐量（TPS）测量。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个主流榜单上进行了全面测试，样本量充足。
*   **消融实验**：针对转换策略（两阶段 vs 直接）、因果上下文、块大小退火、自动截断掩码等核心组件均做了对比实验。
*   **客观性**：实验对比了多种 SOTA 自回归模型和最新的扩散类 VLM，评价指标涵盖了精度（Accuracy）、吞吐量（TPS）和每步生成 token 数（Tokens/NFE），论证较为严谨。

### 6. 主要结论与发现
*   **转换效率**：直接从预训练 VLM 转换比从 LLM 开始转换更高效，在相同预算下精度更高。
*   **性能表现**：Fast-dVLM 在短回答任务上几乎达到了与 AR 基准相同的精度（74.0 vs 74.0），在长推理任务上差距极小。
*   **加速效果**：通过集成 SGLang 推理引擎和 FP8 量化，Fast-dVLM 实现了 **6.18 倍** 的端到端推理加速，吞吐量从 56.7 tok/s 提升至 350.3 tok/s。

### 7. 优点
*   **推理极速**：解决了扩散模型推理慢的顽疾，通过块级设计实现了真正的并行化。
*   **架构优雅**：兼容 KV 缓存，且无需额外的草稿模型（Draft Model），利用自身权重即可实现投机解码。
*   **实用性强**：针对物理 AI 场景（Batch Size=1）进行了专门优化，并提供了系统级的集成方案（SGLang）。

### 8. 不足与局限
*   **长文本挑战**：在需要极长思维链推理的任务（如 MMMU-Pro-V）中，虽然差距在缩小，但仍略逊于纯自回归模型（24.6 vs 26.3）。
*   **算子优化**：论文提到的“二次投机解码”变体虽然理论上减少了前向传播次数，但由于缺乏针对性的 CUDA 内核优化，实际运行速度反而不如线性变体。
*   **训练成本**：虽然是微调，但仍需要 64 张 H100 级别的算力支持，对普通开发者门槛较高。

（完）
