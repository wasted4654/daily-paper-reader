---
title: "$S^3$: Stratified Scaling Search for Test-Time in Diffusion Language Models"
title_zh: $S^3$：扩散语言模型推理阶段的分层缩放搜索
authors: "Ahsan Bilal, Muhammad Ahmed Mohsin, Muhammad Umer, Asad Aali, Muhammad Usman Khanzada, Muhammad Usman Rafique, Zihao He, Emily Fox, Dean F. Hougen"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06260v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型的测试时缩放
tldr: 本研究针对扩散语言模型（DLM）的推理侧扩展展开探索，指出传统的Best-of-K采样受限于基础分布的偏差。为此，作者提出了分层缩放搜索（S³），这是一种验证器引导的搜索方法。通过在去噪过程中动态分配计算资源，对候选轨迹进行扩展、评估和重采样，从而在保持多样性的同时逼近高质量输出分布。实验证明，S³在数学推理等任务上显著提升了模型性能，为DLM的推理侧扩展提供了有效路径。
selection_source: fresh_fetch
motivation: 传统的Best-of-K采样受限于扩散模型基础分布与高质量输出之间的不一致，难以通过增加推理计算量实现性能的持续提升。
method: 提出S³方法，在去噪的每个步骤中利用轻量级验证器对多个候选轨迹进行评估和选择性重采样，实现计算资源的动态优化分配。
result: 在LLaDA-8B-Instruct模型上的实验显示，S³在MATH-500和GSM8K等多个基准测试中均取得显著进步，尤其在数学推理任务上表现突出。
conclusion: 针对去噪轨迹的经典搜索算法是实现扩散语言模型推理侧扩展的一种实用且有效的机制。
---

## 摘要
推理阶段缩放（Test-time scaling）研究在不进行额外训练的情况下，固定的扩散语言模型（DLM）在给予更多推理计算资源时是否能生成更好的输出。然而，朴素的 best-of-$K$ 采样存在根本性的局限，因为它反复从同一个基础扩散分布中抽取样本，而该分布的高概率区域往往与高质量输出不一致。我们提出了 $S^3$（分层缩放搜索），这是一种经典的验证器引导搜索方法，它通过在去噪过程中（而非仅在最终输出阶段）重新分配计算资源来改进生成质量。在每个去噪步骤中，$S^3$ 扩展多个候选轨迹，使用轻量级的无参考验证器对其进行评估，并在保持搜索前沿多样性的同时，选择性地对有潜力的候选轨迹进行重采样。该过程有效地近似了一个奖励倾斜（reward-tilted）的采样分布，在偏向高质量输出的同时，保持与模型先验的锚定。使用 LLaDA-8B-Instruct 在 MATH-500、GSM8K、ARC-Challenge 和 TruthfulQA 上的实验表明，$S^3$ 在各个基准测试中一致地提升了性能，在数学推理任务上取得了最大的增益，同时保持底层模型和解码方案不变。这些结果表明，对去噪轨迹进行经典搜索为扩散语言模型中的推理阶段缩放提供了一种实用的机制。

## Abstract
Test-time scaling investigates whether a fixed diffusion language model (DLM) can generate better outputs when given more inference compute, without additional training. However, naive best-of-$K$ sampling is fundamentally limited because it repeatedly draws from the same base diffusion distribution, whose high-probability regions are often misaligned with high-quality outputs. We propose $S^3$ (Stratified Scaling Search), a classical verifier-guided search method that improves generation by reallocating compute during the denoising process rather than only at the final output stage. At each denoising step, $S^3$ expands multiple candidate trajectories, evaluates them with a lightweight reference-free verifier, and selectively resamples promising candidates while preserving diversity within the search frontier. This procedure effectively approximates a reward-tilted sampling distribution that favors higher-quality outputs while remaining anchored to the model prior. Experiments with LLaDA-8B-Instruct on MATH-500, GSM8K, ARC-Challenge, and TruthfulQA demonstrate that $S^3$ consistently improves performance across benchmarks, achieving the largest gains on mathematical reasoning tasks while leaving the underlying model and decoding schedule unchanged. These results show that classical search over denoising trajectories provides a practical mechanism for test-time scaling in DLMs.

---

## 论文详细总结（自动生成）

以下是对论文《$S^3$: Stratified Scaling Search for Test-Time in Diffusion Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在不增加额外训练的前提下，如何通过增加推理阶段的计算量（Test-time compute）来提升扩散语言模型（DLM）的输出质量？
*   **研究动机**：
    *   **密度-质量不匹配（Density-Quality Mismatch）**：扩散模型的基础分布 $p_0$ 的高概率区域往往与高质量输出（由验证器奖励定义）不一致。
    *   **Best-of-K 的局限性**：传统的 Best-of-K 采样只是从同一分布中重复抽样，其性能提升随 $K$ 的增加仅呈对数增长，效率低下。
    *   **DLM 的结构优势**：与自回归模型不同，扩散模型通过迭代去噪生成，这为在中间步骤进行轨迹搜索和资源重新分配提供了天然的结构机会。

### 2. 方法论（$S^3$: Stratified Scaling Search）
*   **核心思想**：将推理计算资源从“生成后的筛选”转移到“去噪过程中的搜索”。通过在去噪轨迹上进行粒子搜索（Particle Search），使采样分布向高奖励的 Gibbs 分布偏移。
*   **关键技术细节**：
    1.  **轨迹扩展（Expansion）**：维持 $N$ 个并行粒子（部分去噪状态）。在每个去噪步骤，将每个粒子扩展出 $b$ 个候选后继状态。
    2.  **前瞻性评分（Look-ahead Scoring）**：由于中间状态无法直接评分，算法利用模型的“一步清洁预测”（One-step clean prediction）生成最终输出的估计值 $\hat{x}_0$，并使用验证器对其评分。
    3.  **奖励倾斜（Reward Tilting）**：利用 Gibbs 分布原理，根据验证器分数计算权重 $w \propto \exp(\lambda \cdot score)$，使搜索偏向高分区域。
    4.  **分层重采样（SSP Resampling）**：采用 **Srinivasan 采样过程（SSP）** 进行低方差的随机重采样，从 $N \times b$ 个候选者中选回 $N$ 个粒子。SSP 相比 Top-K 剪枝能更好地保持种群的多样性，防止模式崩溃。
    5.  **最终决策**：在去噪结束时，对剩余粒子进行多数投票（Majority Voting），并以模型负对数似然（NLL）作为平局决胜标准。

### 3. 实验设计
*   **基础模型**：LLaDA-8B-Instruct（一种先进的扩散大语言模型）。
*   **数据集/场景**：
    *   **数学推理**：MATH-500, GSM8K。
    *   **科学/常识推理**：ARC-Challenge。
    *   **事实准确性**：TruthfulQA。
*   **对比方法（Baselines）**：
    *   **Baseline Diffusion**：标准的单轨迹去噪解码。
    *   **Best-of-K (BoK)**：独立采样 $K$ 条完整轨迹并筛选。
    *   **消融变体**：仅前瞻搜索（无倾斜）、仅奖励倾斜（无搜索结构）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。
*   **计算成本度量**：论文使用 **NFE（函数评估次数）** 作为衡量推理计算量的核心指标。$S^3$ 的计算成本约为 $T \times N \times b$（$T$ 为去噪步数），通过调整 $N$ 和 $b$ 来控制计算预算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四个主流基准测试，并针对不同的计算预算（NFE 从 10 到 100+）绘制了缩放曲线。
*   **充分性**：
    *   **消融实验**：详细分解了“前瞻搜索”和“Gibbs 倾斜”各自的贡献，证明了两者结合的必要性。
    *   **块长度分析**：研究了不同去噪步长（Block Length）对搜索效果的影响。
    *   **客观性**：实验在相同的计算预算下对比了 $S^3$ 与 BoK，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：$S^3$ 在所有基准上均优于基准模型。在 MATH-500 上准确率从 25.6% 提升至 30.2%（+4.6%），在 GSM8K 上从 68.16% 提升至 70.21%。
*   **搜索优于采样**：在相同的计算预算下，$S^3$ 的帕累托前沿（Pareto Frontier）通常优于 Best-of-K，尤其是在需要多步推理的任务中。
*   **置信度演变**：$S^3$ 在去噪过程中能维持更高的平均 Token 置信度，表明验证器引导稳定了生成动力学。

### 7. 优点与亮点
*   **无需重训练**：作为一种纯推理侧的方法，它可以直接应用于任何现有的扩散语言模型。
*   **理论完备**：将搜索过程建立在 Gibbs 分布和 Doob h-transform 的数学框架之上，而非纯启发式搜索。
*   **轻量级验证器**：设计了一套无需地面真值（Ground-truth-free）的复合验证器（涵盖结构、算术一致性、可达性等），计算开销极低。

### 8. 不足与局限
*   **验证器依赖**：搜索效果高度依赖于验证器的质量。如果验证器信号存在偏差或噪声，可能会引导模型走向错误的轨迹。
*   **推理延迟**：虽然 NFE 相同，但由于需要进行粒子扩展和评分，其实际墙钟时间（Wall-clock time）高于简单的并行采样。
*   **任务局限**：在 ARC-Challenge 等多选题任务上，由于验证器信号较弱，$S^3$ 相比 BoK 的优势不明显。
*   **前瞻误差**：中间步骤的“一步清洁预测”可能不准确，尤其是在去噪早期，这会限制搜索的引导效果。

（完）
