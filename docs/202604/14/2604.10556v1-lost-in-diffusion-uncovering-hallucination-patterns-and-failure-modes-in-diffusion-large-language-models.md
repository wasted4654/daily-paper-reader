---
title: "Lost in Diffusion: Uncovering Hallucination Patterns and Failure Modes in Diffusion Large Language Models"
title_zh: 迷失在扩散中：揭示扩散大语言模型中的幻觉模式与失效模式
authors: "Zhengnan Guo, Fei Tan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10556v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散大语言模型与自回归模型在幻觉模式上的对比研究
tldr: 本研究针对扩散大语言模型（dLLMs）的幻觉问题开展了首次受控对比研究。通过在相同架构、规模和预训练权重下与自回归模型对比，发现 dLLMs 具有更高的幻觉倾向，并揭示了推理计算中非顺序解码的细化潜力。研究识别出扩散模型特有的失效模式，如提前终止和去噪不全，为提升非自回归模型的可靠性提供了重要见解。
selection_source: fresh_fetch
motivation: 尽管扩散大语言模型在性能上已接近自回归模型，但其在生成忠实度及幻觉方面的表现尚缺乏深入系统的研究。
method: 通过控制变量法，在相同的模型架构和预训练权重基础上，对比分析了 dLLMs 与自回归模型在幻觉模式上的差异。
result: 实验发现 dLLMs 比自回归模型更容易产生幻觉，并识别出提前终止、去噪不完全和上下文侵入等扩散过程特有的失效模式。
conclusion: 扩散大语言模型独特的幻觉机制对其可靠性构成了严峻挑战，未来需针对其非自回归生成特性进行专门的优化。
---

## 摘要
尽管扩散大语言模型（dLLMs）已成为一种可与自回归（AR）模型相媲美且极具前景的非自回归范式，但其忠实度（特别是幻觉问题）在很大程度上仍未得到充分探索。为了填补这一空白，我们提出了首个受控对比研究，旨在评估 dLLMs 中的幻觉模式。我们的结果表明，在控制架构、规模和预训练权重的情况下，目前的 dLLMs 比对应的 AR 模型表现出更高的幻觉倾向。此外，对推理时计算的分析揭示了不同的动态特性：准自回归生成会遭遇早期饱和，而非顺序解码则释放了持续优化的潜力。最后，我们识别出了扩散过程特有的不同失效模式，包括过早终止、去噪不完全和上下文侵入。我们的研究结果强调，尽管 dLLMs 缩小了在通用任务上的性能差距，但其独特的幻觉机制对模型可靠性构成了严峻挑战。我们的代码可在 https://github.com/ZeroLoss-Lab/Lost-in-Diffusion 获取。

## Abstract
While Diffusion Large Language Models (dLLMs) have emerged as a promising non-autoregressive paradigm comparable to autoregressive (AR) models, their faithfulness, specifically regarding hallucination, remains largely underexplored. To bridge this gap, we present the first controlled comparative study to evaluate hallucination patterns in dLLMs. Our results demonstrate that current dLLMs exhibit a higher propensity for hallucination than AR counterparts controlled for architecture, scale, and pre-training weights. Furthermore, an analysis of inference-time compute reveals divergent dynamics: while quasi-autoregressive generation suffers from early saturation, non-sequential decoding unlocks potential for continuous refinement. Finally, we identify distinct failure modes unique to the diffusion process, including premature termination, incomplete denoising, and context intrusion. Our findings underscore that although dLLMs have narrowed the performance gap on general tasks, their distinct hallucination mechanisms pose a critical challenge to model reliability. Our code is available at https://github.com/ZeroLoss-Lab/Lost-in-Diffusion

---

## 论文详细总结（自动生成）

这篇论文《Lost in Diffusion: Uncovering Hallucination Patterns and Failure Modes in Diffusion Large Language Models》对扩散大语言模型（dLLMs）的幻觉问题进行了深入的实证研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着扩散大语言模型（如 LLaDA, Dream 等）在性能上逐渐追赶自回归模型（AR-LLMs），其可靠性和忠实度（即幻觉问题）成为了一个亟待解决的空白领域。
*   **核心问题**：扩散机制（并行去噪、双向可见性）究竟是减轻了还是加剧了幻觉？
*   **整体含义**：这是首个系统性对比 dLLMs 与 AR-LLMs 幻觉模式的研究，揭示了扩散模型在生成事实性内容时的独特缺陷。

### 2. 方法论：核心思想与关键技术
*   **成对对比框架（Pairwise Comparison）**：为了消除模型规模和预训练数据的干扰，研究设计了两组对照：
    *   **架构对齐**：LLaDA-8B vs. LLaMA-3-8B（相似架构与规模）。
    *   **参数对齐**：Dream-7B vs. Qwen2.5-7B（Dream 直接从 Qwen 权重初始化，最大限度控制知识差异）。
*   **推理设置**：采用标准扩散推理（Canonical Diffusion），去噪步数 $T$ 等于序列长度 $L$，温度设为 0 以确保可重复性。
*   **评估协议**：适配了 **HalluLens** 基准测试，利用 Gemini-2.5-Flash 作为评估引擎，涵盖精确知识召回、长文本事实一致性和知识边界检测。

### 3. 实验设计
*   **数据集与任务**：
    *   **PreciseWikiQA**：基于 Wikipedia 的精确实体召回。
    *   **LongWiki**：长篇事实生成（1024 tokens），评估原子命题的准确性。
    *   **NonExistentRefusal**：识别虚假实体（如虚构的药物名称），测试模型拒绝回答的能力。
*   **对比方法**：对比了主流的 dLLMs（LLaDA, Dream, Fast-dLLM v2）及其对应的 AR 基准模型。
*   **推理计算分析**：研究了去噪步数（128 到 1024 步）对幻觉率的影响。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长），因为其主要分析对象是已发布的开源模型。实验部分主要涉及推理评估，使用了 Gemini-2.5-Flash API 作为自动化法官。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大类幻觉任务，使用了 2 对核心基准模型及多款指令微调模型。
*   **充分性与公平性**：
    *   实验设计非常严谨，特别是“参数对齐”组（Dream vs. Qwen）极大地排除了预训练知识差异的干扰。
    *   引入了**人工校验**（每项任务抽样 100-300 例），证明了自动化评估与人类判断的高度一致性（Kappa 值 0.68-0.81）。
    *   分析了推理步数对性能的动态影响，实验维度较为全面。

### 6. 主要结论与发现
*   **幻觉倾向更高**：在相同条件下，dLLMs 的幻觉率普遍高于 AR 模型。
*   **推理计算的二元性**：
    *   **准自回归（如 LLaDA）**：受限于线性噪声调度，性能在早期步数即饱和，无法通过增加计算量提升质量。
    *   **非顺序解码（如 Dream）**：能够通过增加推理步数实现持续的去噪细化，降低幻觉。
*   **三大独特失效模式**：
    1.  **提前终止（Premature Termination）**：由于前后文语法不匹配，模型被迫插入结束符或断裂结构。
    2.  **去噪不全（Incomplete Denoising）**：在处理稀有实体时，序列中残留噪声或无意义符号。
    3.  **上下文侵入（Context Intrusion）**：未来位置的随机高频词（如数字、代码）反向“劫持”了当前的生成轨迹。

### 7. 优点：亮点
*   **首次系统性研究**：填补了扩散模型在幻觉评估领域的空白。
*   **失效模式分类**：深入分析了扩散机制导致的非线性错误，而非简单地报告分数。
*   **解码策略洞察**：揭示了最小熵解码（Minimum Entropy Decoding）在推理时间扩展（Inference-time Scaling）上的潜力。

### 8. 不足与局限
*   **加速推理覆盖不足**：主要测试了标准扩散过程，未深入探讨块状扩散（Block-diffusion）等加速方案对幻觉的影响。
*   **指令微调的干扰**：指令微调后的 dLLM 表现出极高的方差（如 Dream-7B-Instruct 的过度拒绝倾向），使得难以纯粹评估生成机制的影响。
*   **规模限制**：实验主要集中在 7B-8B 规模，尚未验证在千亿级参数模型中这些模式是否依然存在。

（完）
