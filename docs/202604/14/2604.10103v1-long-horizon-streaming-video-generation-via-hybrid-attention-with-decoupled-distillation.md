---
title: Long-Horizon Streaming Video Generation via Hybrid Attention with Decoupled Distillation
title_zh: 通过解耦蒸馏的混合注意力实现长程流式视频生成
authors: "Ruibin Li, Tao Yang, Fangzhou Ai, Tianhe Wu, Shilei Wen, Bingyue Peng, Lei Zhang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10103v1"
tags: ["query:ardm"]
score: 9.0
evidence: 将扩散模型蒸馏为自回归模型用于视频生成
tldr: 本研究针对流式视频生成中滑动窗口注意力导致的长程信息丢失和计算开销大等问题，提出了Hybrid Forcing框架。该框架通过引入轻量级线性时间注意力保留远端历史，并结合块稀疏注意力优化局部计算效率。配合解耦蒸馏策略，该方法在保持长程一致性的同时显著提升了生成速度，实现了在单张H100 GPU上以29.5 FPS实时生成无限制长度的高质量视频。
selection_source: fresh_fetch
motivation: 现有的流式视频生成方法受限于滑动窗口，难以兼顾长程历史信息的保留与实时部署所需的计算效率。
method: 提出Hybrid Forcing框架，利用线性时间注意力捕捉长程依赖，结合块稀疏注意力减少局部冗余计算，并采用先稠密后混合的解耦蒸馏策略。
result: 该模型在长短视频生成基准上均达到SOTA，并能在单张H100上实现832x480分辨率、29.5 FPS的实时无限制视频生成。
conclusion: Hybrid Forcing通过创新的混合注意力设计与蒸馏方案，成功解决了流式视频生成中的效率与长程一致性瓶颈。
---

## 摘要
流式视频生成 (SVG) 将预训练的双向视频扩散模型蒸馏为配备滑动窗口注意力 (SWA) 的自回归模型。然而，SWA 在长视频生成过程中不可避免地会丢失远期历史信息，且其计算开销仍是实时部署面临的关键挑战。在这项工作中，我们提出了 Hybrid Forcing，通过混合注意力设计共同优化时间信息的保留和计算效率。首先，我们引入了轻量级的线性时间注意力，以保留滑动窗口之外的长程依赖关系。具体而言，我们维护一个紧凑的键值状态来增量吸收被剔除的 token，以极小的内存和计算开销保留时间上下文。其次，我们在局部滑动窗口中加入块稀疏注意力，以减少短程建模中的冗余计算，将计算能力重新分配给更关键的依赖项。最后，我们引入了针对混合注意力设计定制的解耦蒸馏策略。在密集注意力下进行少步初始蒸馏，随后激活所提出的线性时间注意力和块稀疏注意力的蒸馏以进行流式建模，从而确保优化过程的稳定。在短视频和长视频生成基准上的广泛实验表明，Hybrid Forcing 始终保持着最先进的性能。值得注意的是，在不使用量化或模型压缩的情况下，我们的模型在单张 NVIDIA H100 GPU 上实现了 29.5 FPS 的实时、无限制 832x480 视频生成。源代码和训练好的模型可在 https://github.com/leeruibin/hybrid-forcing 获取。

## Abstract
Streaming video generation (SVG) distills a pretrained bidirectional video diffusion model into an autoregressive model equipped with sliding window attention (SWA). However, SWA inevitably loses distant history during long video generation, and its computational overhead remains a critical challenge to real-time deployment. In this work, we propose Hybrid Forcing, which jointly optimizes temporal information retention and computational efficiency through a hybrid attention design. First, we introduce lightweight linear temporal attention to preserve long-range dependencies beyond the sliding window. In particular, we maintain a compact key-value state to incrementally absorb evicted tokens, retaining temporal context with negligible memory and computational overhead. Second, we incorporate block-sparse attention into the local sliding window to reduce redundant computation within short-range modeling, reallocating computational capacity toward more critical dependencies. Finally, we introduce a decoupled distillation strategy tailored to the hybrid attention design. A few-step initial distillation is performed under dense attention, then the distillation of our proposed linear temporal and block-sparse attention is activated for streaming modeling, ensuring stable optimization. Extensive experiments on both short- and long-form video generation benchmarks demonstrate that Hybrid Forcing consistently achieves state-of-the-art performance. Notably, our model achieves real-time, unbounded 832x480 video generation at 29.5 FPS on a single NVIDIA H100 GPU without quantization or model compression. The source code and trained models are available at https://github.com/leeruibin/hybrid-forcing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Long-Horizon Streaming Video Generation via Hybrid Attention with Decoupled Distillation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
流式视频生成（Streaming Video Generation, SVG）旨在将预训练的双向视频扩散模型转化为自回归模型，以实现长视频的逐块生成。
*   **研究动机**：现有的 SVG 方法主要依赖滑动窗口注意力（SWA）。这种机制存在两个致命缺陷：
    1.  **长程信息丢失**：随着窗口滑动，早期的帧被移出缓存，导致模型失去远期上下文，产生严重的“时间漂移”和不连贯。
    2.  **计算开销大**：SWA 的二次方复杂度限制了实时部署的可能性。
*   **核心目标**：在保持长程时间一致性的同时，实现超高帧率（实时）的无限制长度视频生成。

### 2. 方法论：Hybrid Forcing 框架
论文提出了 **Hybrid Forcing** 框架，通过“混合注意力”与“解耦蒸馏”协同工作：
*   **线性时间注意力（Linear Temporal Attention）**：
    *   **核心思想**：将滑动窗口剔除的旧 token 压缩进一个紧凑的、固定大小的线性状态矩阵（KV State）。
    *   **技术细节**：利用线性注意力的循环特性，增量式地吸收历史信息。这使得模型能以极低的内存开销（不随视频长度增长）访问无限远的历史背景。
*   **块稀疏滑动窗口注意力（Block-sparse SWA）**：
    *   **核心思想**：在局部窗口内，利用视频注意力图的稀疏性，仅计算最重要的块之间的交互。
    *   **技术细节**：基于块重要性得分保留 Top-K 激活块，显著降低了局部建模的计算冗余。
*   **解耦蒸馏策略（Decoupled Distillation）**：
    *   **流程**：分为两个阶段。第一阶段在全密集注意力下进行少步蒸馏，旨在让模型学习稳健的语义表示；第二阶段激活线性与稀疏注意力模块，专注于长程结构化建模。这种分离避免了初期噪声状态对模型优化的干扰。
*   **稳定性增强技术**：
    *   **相对 RoPE 约束**：将时间旋转位置编码（RoPE）限制在预训练模型的范围内，解决长视频生成时的外推失效问题。
    *   **教师引导正则化损失**：引入辅助损失函数，强制学生模型的首步生成结果接近教师模型的完整轨迹，有效缓解了长程生成中的“模式崩溃”（如相机异常漂移）。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练**：使用 LLM 增强后的 VidProM 数据集（百万级文本-视频对）。
    *   **评估**：采用 **VBench**（短视频）和 **VBench-Long**（长视频，生成 30 秒视频进行量化评估）。
*   **Benchmark 与对比方法**：
    *   **扩散模型**：Wan2.1 (1.3B)。
    *   **多步自回归模型**：SkyReels-V2, MAGI-1, Pyramid Flow 等。
    *   **少步流式模型（主要对手）**：CausVid, Self-Forcing, Rolling Forcing, LongLive, MemFlow, InfRoPE。

### 4. 资源与算力
*   **硬件环境**：所有推理性能测试均在单张 **NVIDIA H100 GPU** 上完成。
*   **训练细节**：
    *   基于 Wan2.1-T2V-1.3B 预训练模型。
    *   第一阶段蒸馏 1000 步，第二阶段（混合注意力）蒸馏 1000 步。
    *   全局 Batch Size 为 32，使用 AdamW 优化器。
*   **推理表现**：在 832×480 分辨率下达到了 **29.5 FPS**，且未进行任何量化或模型压缩。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的量化对比，涵盖了 VBench 的 16 个维度（如审美质量、运动平滑度、时间一致性等）。
*   **消融实验**：针对混合注意力、相对 RoPE、正则化损失和解耦蒸馏四个核心组件分别做了消融，验证了每个模块对性能（尤其是 Dynamic 和 Consistency 指标）的贡献。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了最新的 SOTA 模型，还展示了长达 **10 分钟** 的生成案例，证明了其在极端长程场景下的稳定性。

### 6. 主要结论与发现
*   **性能领先**：Hybrid Forcing 在短视频和长视频基准上均超越了现有的流式生成模型，尤其在“动态程度”和“语义一致性”上提升显著。
*   **效率突破**：通过混合注意力，该方法比次优的流式模型提速约 35%，首次在 1.3B 规模模型上实现了接近 30 FPS 的实时生成。
*   **长程稳定性**：线性注意力与相对 RoPE 的结合彻底解决了自回归生成中的漂移问题，支持无限制长度的视频合成。

### 7. 优点（亮点）
*   **架构创新**：巧妙地结合了线性注意力的“无限记忆”和稀疏注意力的“局部高效”，解决了 SVG 的核心矛盾。
*   **训练策略**：解耦蒸馏解决了复杂架构在蒸馏过程中的收敛难点。
*   **实用性极强**：无需量化即可实时生成，且代码开源，具有很高的工业应用价值。

### 8. 不足与局限
*   **性能上限受限**：作为一种蒸馏方法，其最终生成的视频质量上限受限于教师模型（Wan2.1）和初始的 ODE 检查点。
*   **硬件依赖**：虽然在 H100 上表现优异，但在低端 GPU 上的实时性尚未得到充分验证。
*   **评估维度**：虽然 VBench 指标全面，但对于极长视频（如数小时）的逻辑连贯性（如复杂剧情）仍缺乏标准化的量化评估手段。

（完）
