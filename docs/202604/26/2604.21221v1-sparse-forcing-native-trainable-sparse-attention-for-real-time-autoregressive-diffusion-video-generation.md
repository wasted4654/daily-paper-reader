---
title: "Sparse Forcing: Native Trainable Sparse Attention for Real-time Autoregressive Diffusion Video Generation"
title_zh: Sparse Forcing：用于实时自回归扩散视频生成的原生可训练稀疏注意力
authors: "Boxun Xu, Yuming Du, Zichang Liu, Siyu Yang, Ziyang Jiang, Siqi Yan, Rajasi Saha, Albert Pumarola, Wenchen Wang, Peng Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21221v1"
tags: ["query:ardm"]
score: 10.0
evidence: 为自回归扩散视频生成提出稀疏注意力机制
tldr: 针对自回归视频扩散模型在长视频生成中的质量与延迟挑战，本文提出 Sparse Forcing 范式。该方法基于注意力集中于持久显著块的观察，设计了可训练的原生稀疏机制来动态压缩和更新 KV 缓存，并结合高效的 PBSA GPU 内核。实验证明，该方法在显著提升长视频生成质量的同时，大幅降低了显存占用并加快了推理速度，为实时长视频生成提供了高效方案。
selection_source: fresh_fetch
motivation: 发现自回归扩散模型中的注意力天然集中在少数持久的显著视觉块上，且在滑动窗口内呈现局部块稀疏模式。
method: 提出一种可训练的稀疏机制来动态选择和保留关键视觉块，并开发了 PBSA 高效内核以加速稀疏注意力计算。
result: "在长视频生成上 VBench 评分显著提升，推理速度最高提升 1.27 倍，且 KV 缓存峰值占用降低了 42%。"
conclusion: Sparse Forcing 通过原生可训练稀疏性，实现了高质量、低延迟且显存高效的实时长视频自回归生成。
---

## 摘要
我们介绍了 Sparse Forcing，这是一种用于自回归视频扩散模型的训练与推理范式，旨在提高长程生成质量的同时降低解码延迟。Sparse Forcing 的灵感源于自回归扩散推演中的一个经验观察：注意力集中在显著视觉块的一个持久子集上，在 KV 缓存中形成隐式的时空记忆，并在滑动窗口内表现出局部结构化的块稀疏模式。基于这一观察，我们提出了一种可训练的原生稀疏机制，该机制学习压缩、保留和更新这些持久块，同时将每个局部窗口内的计算限制在动态选择的局部邻域内。为了使该方法在训练和推理中都能大规模应用，我们进一步提出了持久块稀疏注意力（PBSA），这是一种高效的 GPU 内核，可加速稀疏注意力和内存更新，从而实现低延迟、内存高效的解码。实验表明，在 5 秒文本生成视频任务中，Sparse Forcing 相比 Self-Forcing 将 VBench 评分提高了 0.26，同时实现了 1.11-1.17 倍的解码加速，并降低了 42% 的峰值 KV 缓存占用。在更长程的推演中，这种增益更加显著，在 20 秒和 1 分钟的生成任务中，VBench 评分分别提高了 0.68 和 2.74，加速比分别达到 1.22 倍和 1.27 倍。

## Abstract
We introduce Sparse Forcing, a training-and-inference paradigm for autoregressive video diffusion models that improves long-horizon generation quality while reducing decoding latency. Sparse Forcing is motivated by an empirical observation in autoregressive diffusion rollouts: attention concentrates on a persistent subset of salient visual blocks, forming an implicit spatiotemporal memory in the KV cache, and exhibits a locally structured block-sparse pattern within sliding windows. Building on this observation, we propose a trainable native sparsity mechanism that learns to compress, preserve, and update these persistent blocks while restricting computation within each local window to a dynamically selected local neighborhood. To make the approach practical at scale for both training and inference, we further propose Persistent Block-Sparse Attention (PBSA), an efficient GPU kernel that accelerates sparse attention and memory updates for low-latency, memory-efficient decoding. Experiments show that Sparse Forcing improves the VBench score by +0.26 over Self-Forcing on 5-second text-to-video generation while delivering a 1.11-1.17x decoding speedup and 42% lower peak KV-cache footprint. The gains are more pronounced on longer-horizon rollouts, delivering improved visual quality with +0.68 and +2.74 VBench improvements, and 1.22x and 1.27x speedups on 20-second and 1-minute generations, respectively.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Sparse Forcing** 的新颖范式，旨在解决自回归（AR）视频扩散模型在生成长视频时面临的计算效率低下和质量退化问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归视频扩散模型在生成长视频时面临两大挑战：
    1.  **计算瓶颈**：随着视频长度增加，KV 缓存（Key-Value Cache）呈线性增长，注意力计算呈现二次方复杂度，导致显存溢出和高延迟。
    2.  **误差累积（曝光偏差）**：模型在推演过程中依赖自身生成的（不完美的）历史帧，导致长程生成时出现色彩漂移、物体变形等质量退化。
*   **研究动机**：作者观察到自回归推演中注意力具有**持久性（Persistency）**和**局部块稀疏性（Local Block Sparsity）**。即注意力往往集中在少数关键的视觉块（锚点）上，而局部窗口内的注意力也呈现结构化的稀疏模式。

### 2. 方法论
Sparse Forcing 包含一套完整的训练与推理机制，核心思想是将注意力机制从“全量密集”转变为“原生可训练稀疏”。
*   **核心思想**：将 KV 缓存分解为**持久内存（Persistent Memory）**和**局部窗口（Local Window）**。
*   **关键技术细节**：
    1.  **隐式时空记忆分解**：维护一个固定容量的持久块集合（捕捉全局语义和主体一致性）和一个滑动局部窗口（捕捉短期运动细节）。
    2.  **块压缩与评分**：将潜在张量划分为时空块，通过池化生成压缩代表向量。利用这些向量进行粗粒度评分，动态决定哪些块进入持久内存，哪些在局部窗口中被屏蔽。
    3.  **PBSA 内核（Persistent Block-Sparse Attention）**：基于 ThunderKittens 开发的定制化 GPU 内核，支持不规则的稀疏模式和持久块的动态更新，实现了端到端的加速。
    4.  **原生可训练稀疏性**：在训练阶段就引入这种稀疏注意力模式，通过分布匹配蒸馏（DMD）损失函数，使模型适应稀疏的上下文环境，从而减少推演时的分布偏移。

### 3. 实验设计
*   **基础模型**：以 Wan2.1-T2V-1.3B 为骨干网络。
*   **数据集**：使用经过过滤和 LLM 扩展的 VidProM 数据集进行训练。
*   **Benchmark**：使用 **VBench** 综合评估套件，涵盖 16 个维度（语义一致性、感知质量等）。
*   **对比方法**：
    *   **双向模型**：LTX-Video, Wan2.1。
    *   **自回归模型**：SkyReels-V2, MAGI-1, CausVid, Self-Forcing（主要 Baseline）。
*   **场景**：涵盖 5 秒（短程）、20 秒（中程）及 1 分钟（长程）视频生成。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：训练了 1200 个 step，batch size 为 64。
*   **推理效率**：在 H100 上进行了内核性能测试，对比了 FlashAttention-2。

### 5. 实验数量与充分性
*   **实验规模**：评估了 4,730 个生成的视频（946 个提示词 × 每个提示词 5 个样本）。
*   **消融实验**：针对持久内存（P）、局部块稀疏注意力（BSA）以及持续预训练（Cont. Pretrain）分别做了消融，验证了各组件对画质和速度的贡献。
*   **内核测试**：在不同稀疏度（Top-K）、序列长度和内存比例下进行了详尽的性能基准测试。
*   **评价**：实验设计较为充分，不仅有客观的 VBench 指标，还有定性的视觉对比和内核级的耗时分析，客观地展示了该方法在长视频生成上的优势。

### 6. 主要结论与发现
*   **质量提升**：在 1 分钟长视频生成中，VBench 评分比 Self-Forcing 显著提升了 **2.74**，有效缓解了长程漂移。
*   **效率飞跃**：相比全量注意力，实现了 **42% 的 KV 缓存峰值降幅**，解码速度提升了 **1.11x 至 1.27x**。
*   **内核优势**：定制的 PBSA 内核在特定稀疏配置下比 FlashAttention-2 快 **1.16x 至 11.11x**。
*   **训练必要性**：实验证明，仅在推理时使用稀疏性（Training-free）会导致语义重写，必须通过训练使模型适应稀疏注意力。

### 7. 优点
*   **软硬结合**：不仅提出了算法层面的稀疏策略，还开发了底层高效内核，确保了理论优势能转化为实际推理加速。
*   **长程稳定性**：通过“持久内存”机制，解决了自回归模型常见的“遗忘”和“漂移”问题，使分钟级视频生成成为可能。
*   **原生性**：稀疏性是可训练的，这比单纯的推理侧剪枝具有更好的鲁棒性和生成质量。

### 8. 不足与局限
*   **模型覆盖面**：主要在 Wan2.1-1.3B 这一种架构上进行了验证，尚未展示在更大规模（如 10B+ 参数）或其他架构（如 U-Net）上的泛化能力。
*   **分辨率限制**：实验主要集中在特定分辨率（如 896x512），更高分辨率下的表现有待观察。
*   **运动伪影**：在某些配置下，由于稀疏采样，可能会出现轻微的时间闪烁或运动平滑度下降（VBench 中的相关指标略有波动）。

（完）
