---
title: "Breaking Block Boundaries: Anchor-based History-stable Decoding for Diffusion Large Language Models"
title_zh: 突破块边界：扩散大语言模型的基于锚点的历史稳定解码
authors: "Shun Zou, Yong Wang, Zehui Chen, Lin Chen, Chongyang Tao, Feng Zhao, Xiangxiang Chu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08964v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散大语言模型的半自回归解码研究
tldr: 针对扩散大语言模型（dLLM）在半自回归解码中受限于块边界、导致稳定标记解码延迟的问题，本文提出了锚点历史稳定解码（AHD）。AHD 是一种无需训练、即插即用的动态策略，通过动态锚点实时监测标记稳定性，并在标记稳定后提前开启跨块解码。实验证明，AHD 在显著提升推理效率的同时增强了模型在多模态任务上的性能，有效解决了现有加速策略带来的性能退化问题。
selection_source: fresh_fetch
motivation: 现有的半自回归解码受限于固定的块边界，导致许多已趋于稳定的跨块标记无法被及时解码，限制了推理效率。
method: 提出 AHD 策略，利用动态锚点实时监控标记的收敛趋势，一旦标记达到稳定状态即触发提前跨块解码。
result: "实验显示 AHD 在减少 80% 解码步骤的同时，将 BBH 基准测试的性能提升了 3.67%，实现了效率与效果的双重提升。"
conclusion: AHD 成功打破了 dLLM 解码的块限制，为扩散模型提供了一种高效且能保持甚至提升性能的通用解码方案。
---

## 摘要
扩散大语言模型 (dLLMs) 近期已成为自回归大语言模型 (ARMs) 的一种极具前景的替代方案。由于其卓越的性能，半自回归 (Semi-AR) 解码被广泛应用于基础 dLLMs 和先进的解码策略中。然而，我们的观察表明，Semi-AR 解码受到固有的块限制，导致许多跨块稳定标记 (stable tokens) 的解码被不必要地延迟。为了应对这一挑战，我们系统地研究了稳定标记的识别，并提出了三个关键发现：(1) 朴素的前瞻解码 (lookahead decoding) 是不可靠的；(2) 标记稳定性与收敛趋势密切相关；(3) 历史信息是孤立的。基于这些见解，我们提出了基于锚点的历史稳定解码 (AHD)，这是一种无需训练、即插即用的动态解码策略。具体而言，AHD 通过动态锚点实时监测标记的稳定性趋势。一旦标记达到稳定状态，它就会启动早期跨块解码，以提高效率和性能。在语言、视觉-语言和音频-语言领域的广泛实验表明，AHD 同时提升了性能和推理效率。值得注意的是，AHD 有效地扭转了现有先进解码加速策略中常见的性能下降现象。例如，在 BBH 基准测试中，我们的方法在减少 80% 解码步骤的同时，将性能提升了 3.67%。

## Abstract
Diffusion Large Language Models (dLLMs) have recently become a promising alternative to autoregressive large language models (ARMs). Semi-autoregressive (Semi-AR) decoding is widely employed in base dLLMs and advanced decoding strategies due to its superior performance. However, our observations reveal that Semi-AR decoding suffers from inherent block constraints, which cause the decoding of many cross-block stable tokens to be unnecessarily delayed. To address this challenge, we systematically investigate the identification of stable tokens and present three key findings: (1) naive lookahead decoding is unreliable, (2) token stability closely correlates with convergence trend, and (3) historical information is isolated. Building on these insights, we propose Anchor-based History-stable Decoding (AHD), a training-free, plug-and-play dynamic decoding strategy. Specifically, AHD monitors the stability trend of tokens in real time through dynamic anchors. Once a token reaches stability, it initiates early cross-block decoding to enhance efficiency and performance. Extensive experiments across language, vision-language, and audio-language domains demonstrate that AHD simultaneously improves both performance and inference efficiency. Notably, AHD effectively reverses the performance degradation typically observed in existing advanced decoding acceleration strategies. For instance, on the BBH benchmark, our approach reduces decoding steps by 80% while improving performance by 3.67%.

---

## 论文详细总结（自动生成）

以下是对论文《Breaking Block Boundaries: Anchor-based History-stable Decoding for Diffusion Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
扩散大语言模型（dLLM）通常采用**半自回归（Semi-AR）解码**，即按块（Block）顺序生成文本。研究人员发现这种机制存在**块边界延迟（Block-Boundary Delay）**：许多位于后续块的标记（Tokens）其实在当前块解码时就已经趋于稳定（即预测结果不再变化），但受限于块边界，必须等待当前块完全解码后才能被释放。
此外，研究发现稳定标记具有“辐射效应”，能加速邻近标记的收敛。因此，如何准确识别并提前释放这些“跨块稳定标记”，是提升 dLLM 推理效率和生成质量的关键。

### 2. 方法论：核心思想与技术细节
论文提出了**基于锚点的历史稳定解码（AHD）**，这是一种无需训练、即插即用的动态解码策略。
*   **核心思想**：打破传统解码中各步骤相互孤立的状态，利用历史轨迹信息来判断标记的绝对稳定性。
*   **关键技术细节**：
    *   **动态锚点（Dynamic Anchor）**：将当前解码步骤 $t$ 的概率分布作为参考锚点。
    *   **历史缓冲区（Historical Buffer）**：维护每个位置过去 $H$ 步的预测分布。
    *   **锚点 KL 散度（Anchored KL Divergence）**：计算当前锚点分布与历史分布之间的 KL 散度，以衡量一致性。
    *   **指数加权一致性得分（ACS）**：对历史步骤的 KL 散度进行指数衰减加权求和。如果 ACS 低于设定阈值 $\epsilon$，则认为该标记已进入“绝对稳定趋势”。
    *   **提前解锁**：一旦识别出未来块中的稳定标记，立即将其从掩码状态中释放，实现跨块并行解码。

### 3. 实验设计
*   **数据集与场景**：
    *   **语言领域**：HumanEval、MBPP（代码）；BBH、MMLU-Pro、TruthfulQA（通用能力）；Math、Asdiv（数学推理）。
    *   **视觉-语言领域**：MathVista、MATH-Vision、ScienceQA、GQA、MME。
    *   **音频-语言领域**：VoiceBench（含 AlpacaEval、OpenBookQA 等 5 个任务）。
*   **基准模型（Backbones）**：LLaDA-8B/1.5、MMaDA-8B、DIFFA。
*   **对比方法**：朴素解码（Vanilla）、PC-sampler、Fast-dLLM、KLASS、Saber 等主流加速策略。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 台 NVIDIA H20 GPU** 上进行。
*   **训练时长**：由于 AHD 是**无需训练（Training-free）**的方法，因此不涉及模型训练时长，仅涉及推理阶段的计算开销。文中指出，ACS 计算带来的额外 GPU 内存和时间开销相对于 dLLM 本身的推理成本几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大模态（文本、图像、音频）、十余个主流基准测试。
*   **消融实验**：针对序列长度（最高至 2048）、块长度（16 到 128）、历史缓冲区长度 $H$、稳定性阈值 $\epsilon$ 以及温度系数进行了详尽的消融研究。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了推理速度（解码步骤减少量），还重点关注了性能指标。结果显示 AHD 在不同任务、不同模型规模和不同模态下均表现出一致的优越性。

### 6. 主要结论与发现
*   **性能与效率双增**：AHD 不仅显著减少了解码步骤（如在 BBH 上减少 80%），还提升了模型性能（如 BBH 提升 3.67%）。
*   **扭转性能退化**：现有的加速策略（如 Saber、Fast-dLLM）通常以牺牲精度换取速度，而 AHD 成功逆转了这一趋势，实现了“跑得更快且更准”。
*   **跨模态通用性**：AHD 在视觉和音频理解任务中同样有效，证明了其作为通用解码插件的潜力。

### 7. 优点与亮点
*   **突破性视角**：首次系统分析并量化了 Semi-AR 解码中的块边界延迟问题。
*   **鲁棒性强**：通过历史轨迹而非单步置信度来判断稳定性，有效过滤了局部波动导致的误判。
*   **即插即用**：无需对原模型进行任何微调或重新训练，适配性极高。
*   **理论支撑**：提供了关于锚点历史一致性如何控制分布和语义稳定性的数学证明。

### 8. 不足与局限
*   **超参数敏感性**：虽然 AHD 表现稳健，但针对不同任务（如数学 vs 代码），最优的阈值 $\epsilon$ 和缓冲区长度 $H$ 仍存在细微差异，可能需要少量的人工调优。
*   **模型规模覆盖**：实验主要集中在 8B 规模的模型上，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **长序列优化空间**：虽然验证了长序列的有效性，但在极长序列下，ACS 的向量化计算仍有进一步优化的空间以压榨极致性能。

（完）
