---
title: "MobileWan: Closing the Quality Gap for Mobile Video Diffusion"
title_zh: MobileWan：缩小移动端视频扩散模型的质量差距
authors: "Mohsen Ghafoorian, Denis Korzhenkov, Adil Karjauv, Ioannis Lelekas, Noor Fathima, Spyridon Stasis, Hanno Ackermann, Boris van Breugel, Markus Nagel, Fatih Porikli, Animesh Karnewar, Amirhossein Habibian"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06173v1"
tags: ["query:ardm"]
score: 8.0
evidence: 将视频生成转换为分块自回归过程
tldr: 针对移动端视频生成模型因参数量受限（通常小于1.8B）导致质量不佳的问题，本文提出MobileWan。该研究通过递归重构和结构化压缩，成功将5B参数规模的Wan2.2模型部署于移动设备。核心技术包括递归蒸馏、因果线性注意力、可学习注意力头剪枝及采样步数蒸馏。MobileWan在保持高画质的同时，实现了20秒内生成5秒视频的性能，刷新了移动端视频生成的SOTA。
selection_source: fresh_fetch
motivation: 旨在打破移动端视频生成模型因内存限制而无法部署大规模参数模型（如5B级）的瓶颈。
method: 采用递归蒸馏框架将视频生成转化为分块自回归过程，并结合因果线性注意力、注意力头剪枝及VAE内存优化。
result: 在商用移动设备上实现了20秒内生成5秒480x832视频，VBench评分达83.79，显著优于现有移动模型。
conclusion: 证明了通过合理的架构重构与压缩，大规模视频扩散模型可以在受限的移动硬件上高效运行并保持卓越质量。
---

## 摘要
近期视频扩散模型的进展主要由基于 Transformer 的架构扩展至数十亿参数所驱动，这显著提升了视觉逼真度和运动连贯性。相比之下，现有的移动端视频扩散模型仍受限于较小的参数预算（通常为 0.4-1.8B），从而限制了生成质量。在这项工作中，我们证明了高质量的移动端视频生成并不一定需要小型模型。相反，我们展示了通过循环重构（recurrent reformulation）和结构化压缩，可以将服务器级别的 5B 参数视频扩散 Transformer 高效部署在内存受限的移动硬件上。以 Wan2.2-5B 为基础，我们采用了一种循环蒸馏框架，将视频生成转换为具有常数内存注意力计算的分块自回归过程。结合因果线性注意力，该模型在推理时以 RNN 方式运行，同时保持了跨分块的时间连贯性。我们还提出了一种基于二进制逐头门控的可学习注意力头剪枝方法，该方法使用噪声偏置稀疏性目标和基于蒸馏的微调进行端到端优化。结合采样步数蒸馏和内存优化的 VAE 解码，MobileWan 成为首个可在商用移动设备上部署的 5B 规模视频扩散模型。我们的系统在 20 秒的端到端延迟内生成 16 FPS 的 5 秒 480x832 视频，获得了 83.79 的 VBench 评分，并树立了移动端视频生成的新标杆。项目页面：https://qualcomm-ai-research.github.io/mobilewan

## Abstract
Recent advances in video diffusion have been driven by scaling transformer-based architectures to billions of parameters, substantially improving visual fidelity and motion coherence. In contrast, existing mobile video diffusion models remain limited to relatively small parameter budgets, typically 0.4-1.8B, restricting generation quality. In this work, we show that high-quality mobile video generation does not require small models. Instead, we demonstrate that a server-scale 5B-parameter video diffusion transformer can be deployed efficiently on memory-constrained mobile hardware through recurrent reformulation and structured compression. Starting from Wan2.2-5B, we rely on a recurrence distillation framework that converts video generation into a chunk-wise autoregressive process with constant-memory attention computation. Combined with causal linear attention, the model operates as an RNN at inference time while preserving temporal coherence across chunks. We further propose a learnable attention head pruning method based on binary per-head gates optimized end-to-end using a noise-biased sparsity objective and distillation-based finetuning. Together with sampling-step distillation and memory-optimized VAE decoding, MobileWan becomes the first 5B-scale video diffusion model deployable on a commercial mobile device. Our system generates 5-second 480x832 videos at 16 FPS in 20 seconds end-to-end latency, achieving a VBench score of 83.79 and establishing a new state of the art in mobile video generation. Project page: https://qualcomm-ai-research.github.io/mobilewan

---

## 论文详细总结（自动生成）

这是一份关于论文《MobileWan: Closing the Quality Gap for Mobile Video Diffusion》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **核心问题**：当前移动端视频生成模型（如 0.4B-1.8B 参数规模）与服务器端大模型（如 5B+ 参数规模）之间存在巨大的质量差距。移动端受限于内存和算力，难以直接运行高质量的大规模 Transformer 模型。
*   **研究动机**：打破“移动端只能用小模型”的固有印象。作者认为，通过合理的架构重构和压缩，可以将 5B 规模的顶级模型（如 Wan2.2）部署到手机上，从而在移动端实现媲美服务器端的视频生成质量。

### 2. 论文提出的方法论
MobileWan 采用了一套组合拳技术，将 Wan2.2-5B 模型转化为高效的移动端模型：
*   **递归重构（Recurrent Reformulation）**：引入递归蒸馏框架，将视频生成过程从全局注意力转变为分块自回归（Chunk-wise Autoregressive）过程。这使得模型在推理时具有常数级的内存占用。
*   **因果线性注意力（Causal Linear Attention）**：将标准注意力替换为线性注意力机制，使其在推理时以类似 RNN 的方式运行，通过维护一个固定大小的状态（State）来保持跨分块的时间连贯性。
*   **可学习注意力头剪枝（Learnable Head Pruning）**：提出一种基于二进制逐头门控（Binary Per-head Gates）的结构化剪枝方法。利用“噪声偏置稀疏性目标”（Noise-biased Sparsity Objective）进行端到端优化，在保持性能的同时减少计算量。
*   **采样步数蒸馏（Sampling Distillation）**：通过蒸馏技术将推理步数压缩（例如减少到 8 步左右），显著降低端到端延迟。
*   **内存优化 VAE**：针对移动端内存限制，对 VAE 解码器进行了专门的内存优化，以支持高分辨率（480x832）视频的输出。

### 3. 实验设计
*   **基础模型**：以 Wan2.2-5B 为起点。
*   **测试基准**：主要使用 **VBench**（视频生成领域权威评测集）进行定量评估。
*   **对比对象**：现有的移动端视频扩散模型（通常在 0.4B-1.8B 范围）以及原始的 Wan2.2-5B 服务器模型。
*   **生成规格**：生成 5 秒长、16 FPS、分辨率为 480x832 的视频。

### 4. 资源与算力
*   **推理算力**：在商用移动设备（具体型号通常为高通骁龙旗舰芯片）上实现了 **20 秒内**完成 5 秒视频的端到端生成。
*   **训练算力**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及总训练时长。但考虑到 5B 模型的蒸馏和微调，通常需要大规模 GPU 集群（如 H100/A100 节点）支持。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 VBench 评分（达到 83.79）、延迟测试和定性视觉比较。
*   **消融实验**：对递归蒸馏、线性注意力替换、以及注意力头剪枝的效果分别进行了验证。
*   **充分性评价**：实验设计较为充分，通过 VBench 这一标准化工具证明了其在移动端 SOTA（最先进）的地位。对比实验涵盖了质量与速度的双重维度，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **结论一**：高质量移动视频生成不需要牺牲模型规模，5B 规模的模型经过优化后完全可以在移动端运行。
*   **结论二**：MobileWan 在 VBench 上获得 83.79 分，刷新了移动端视频生成的纪录，缩小了与服务器级模型的差距。
*   **结论三**：递归架构结合线性注意力是解决长视频生成中内存爆炸问题的有效途径。

### 7. 优点
*   **架构创新**：成功将 ARDM（自回归扩散模型）的思想与线性注意力结合，解决了 Transformer 在移动端推理时的 KV Cache 内存瓶颈。
*   **性能卓越**：在保持 5B 参数量级带来的高画质（运动连贯性、视觉逼真度）的同时，实现了极高的推理效率（20秒生成）。
*   **实用性强**：针对 VAE 内存和采样步数等实际部署痛点给出了完整的解决方案。

### 8. 不足与局限
*   **训练成本**：虽然推理很快，但这种复杂的递归蒸馏和结构化剪枝过程需要极高的训练算力和精细的超参数调优。
*   **硬件依赖**：虽然说是商用移动设备，但 5B 模型的运行通常仍需要最新的旗舰级 SoC 支持，在中低端手机上的表现尚未可知。
*   **长视频一致性**：虽然使用了递归状态，但对于极长视频（超过 5-10 秒）是否存在内容漂移或质量下降，文中缺乏极长样本的深度探讨。

（完）
