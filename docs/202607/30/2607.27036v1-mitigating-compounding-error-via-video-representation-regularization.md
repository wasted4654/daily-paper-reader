---
title: Mitigating Compounding Error via Video Representation Regularization
title_zh: 通过视频表示正则化缓解复合误差
authors: "Taiye Chen, Qi Zhang, Yisen Wang"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27036v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于自动驾驶的视频扩散世界模型及自回归生成误差研究
tldr: 针对视频扩散世界模型在长程自回归生成中存在的复合误差问题，本文研究发现该现象与隐藏层表示的维度坍缩（有效秩下降）密切相关。研究指出单纯增加训练数据无法解决漂移问题，并提出了一种轻量级的视频表示正则化方法。该方法通过稳定潜空间表示，显著抑制了迭代误差累积，在VBench指标上大幅提升了长视频生成的鲁棒性和画面质量。
selection_source: fresh_fetch
motivation: 视频世界模型在长程自回归推理时面临严重的复合误差和质量退化，且其内在机制及数据规模化的局限性尚不明确。
method: 提出一种轻量级的视频表示正则化约束，通过抑制隐藏层表示的维度坍缩来稳定模型内部动态并减少误差累积。
result: 在VBench测试中，该方法将审美质量从38.65提升至55.56，成像质量从44.37提升至72.08，显著优于现有方法。
conclusion: 本文首次揭示了自回归视频漂移与内部表示退化的关联，并证明了表示正则化是提升长视频生成稳定性的有效手段。
---

## 摘要
基于视频扩散的世界模型能够为机器人、自动驾驶和模拟任务实现长程自回归视频生成，但滑动窗口自回归推理面临严重的误差累积问题，导致视频帧质量随时间推移而下降。尽管这一现象已被广泛观察到，但复合误差的底层机制以及如何实现稳定的长程生成在很大程度上仍未得到解决。在本文中，我们研究了视频世界模型的内部表示动力学，并发现复合误差与隐藏表示的维度坍缩（dimensional collapse）紧密耦合。具体而言，模型表示的有效秩（erank）在生成漂移开始时急剧下降，揭示了表示退化与长程展开（rollout）不稳定性之间的强关联。此外，我们发现单纯的训练数据缩放无法增强模型对误差漂移的抵抗力，这与主流的缩放范式相悖。为了解决这一问题，我们提出了视频表示正则化，这是一种轻量级的训练约束，能够稳定潜表示并抑制迭代误差累积。与 Diffusion Forcing 相比，我们的方法在 VBench 的审美质量和成像质量指标上分别从 38.65 提升至 55.56，以及从 44.37 提升至 72.08。我们的工作首次建立了自回归视频漂移与模型内部表示之间的联系，采用 erank 作为误差累积的量化指标，揭示了视频世界模型中违反直觉的缩放限制，并提出了一种简单而有效的正则化策略，以提高长视频生成的鲁棒性。

## Abstract
Video diffusion-based world models enable long autoregressive video generation for robotics, autonomous driving and simulation tasks, yet sliding-window autoregressive inference suffers from severe error accumulation that degrades frame quality over time. Although this phenomenon has been widely observed, the underlying mechanism of compounding error and how to achieve stable long-horizon generation remain largely unresolved. In this paper, we investigate the internal representation dynamics of video world models and discover that compounding error is tightly coupled with dimensional collapse of hidden representations. Specifically, the effective rank of model representations sharply decreases at the onset of generation drift, revealing a strong connection between representational degradation and long-term rollout instability. Furthermore, we find that pure training data scaling fails to boost model resistance to error drift, contradicting mainstream scaling paradigms. To address this problem, we propose video representation regularization, a lightweight training constraint that stabilizes latent representations and suppresses iterative error accumulation. Compared with Diffusion Forcing, our method achieves improvements from 38.65 to 55.56 and from 44.37 to 72.08 on the Aesthetic Quality and Imaging Quality metrics of VBench. Our work establishes the first connection between autoregressive video drifting and model internal representations, adopts erank as a quantitative metric for error accumulation, reveals counterintuitive scaling limitations for video world models, and presents a simple yet effective regularization strategy to improve long video generation robustness.

---

## 论文详细总结（自动生成）

这篇论文由北京大学的研究团队提出，旨在解决视频扩散世界模型在长程自回归生成中的“复合误差”问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频扩散模型在进行长程自回归生成（如自动驾驶、机器人模拟）时，由于采用滑动窗口推理，前一阶段生成的微小误差会不断累积，导致视频质量在几十帧后迅速退化（出现噪声、过曝或语义崩溃），即“复合误差”（Compounding Error）或“漂移”现象。
*   **研究动机**：尽管该现象被广泛观察到，但其底层机制不明。作者试图探索模型内部表示与生成崩溃之间的关系，并挑战“增加训练数据即可解决误差累积”的传统认知。

### 2. 方法论
*   **核心思想**：将视频质量的崩溃归因于模型内部隐藏层表示的“维度坍缩”（Dimensional Collapse）。通过引入**有效秩（Effective Rank, erank）**来量化隐藏状态的表达能力，发现 erank 的急剧下降与视觉崩溃高度同步。
*   **关键技术：视频表示正则化（VRR）**：
    *   在训练阶段，除了标准的扩散损失（Diffusion Forcing Loss）外，在 DiT（Diffusion Transformer）的隐藏层中加入一个轻量级的正则化项 $L_{reg}$。
    *   **正则化函数**：主要采用 **SigReg**（约束向量服从各向同性高斯分布）或 **Uniformity Loss**（约束向量服从均匀分布），以防止奇异值分布过于集中，从而维持高有效秩。
    *   **作用位置**：实验发现对 DiT 的首层、中间层和末层（如第 0, 7, 15 层）进行正则化效果最佳。

### 3. 实验设计
*   **数据集与场景**：主要使用 **MineRL (Minecraft)** 游戏数据集，包含 18,000 条高质量游戏序列，每条长达 1,200 帧。
*   **Benchmark**：使用 **VBench** 评估体系，重点关注**审美质量（Aesthetic Quality）**和**成像质量（Imaging Quality）**，以量化长程生成的稳定性。
*   **对比方法**：
    *   **Diffusion Forcing**：基础自回归扩散模型。
    *   **Frame Anchor**：通过插入干净帧作为锚点的常用缓解策略。
    *   **SVI (Stable Video Infinity)**：通过误差回收（Error Recycling）增强鲁棒性的方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到了训练步数（如 16,000 步到 40,000 步），窗口大小设置为 20 帧。虽然未标明硬件，但从 DiT 模型规模和视频生成任务来看，通常需要多块 A100 或 H100 级别的显卡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了长达 1,100 帧的超长程生成测试（远超常规视频模型的几十帧）。
    *   **消融实验**：对比了 5 种不同的正则化方法（SigReg, Uniformity, Barlow Twins, VICReg, Erank 直接正则化）以及不同层级的正则化组合。
    *   **数据规模实验**：分析了从 4k 步到 40k 步训练过程中模型表现的变化。
*   **充分性**：实验设计较为客观，通过定量指标（VBench）和定性可视化（视频帧对比）双重验证，证明了方法在特定领域（Minecraft）的有效性。

### 6. 主要结论与发现
*   **表示与误差耦合**：首次证明了自回归漂移与内部表示坍缩紧密相关，erank 可作为误差累积的有效量化指标。
*   **数据缩放悖论**：发现单纯增加训练数据量（Scaling Data）不仅不能缓解复合误差，反而可能导致模型学习到“捷径”（如直接复制相邻帧），加速表示坍缩。
*   **VRR 的有效性**：VRR 能显著提升长程生成的鲁棒性，在 VBench 审美质量上从 38.65 提升至 55.56，成像质量从 44.37 提升至 72.08。

### 7. 优点与亮点
*   **理论联系实际**：将抽象的流形表示理论（维度坍缩）应用到具体的视频生成稳定性问题中，解释力强。
*   **轻量且通用**：VRR 只是一个额外的损失函数项，不改变模型架构，推理时不增加额外开销。
*   **挑战权威**：提出了关于数据规模化局限性的反直觉发现，对工业界大规模训练视频模型具有参考价值。

### 8. 不足与局限
*   **场景单一性**：实验主要集中在 Minecraft 这一合成环境数据集上，尚未在复杂的真实世界场景（如 Waymo 自动驾驶数据集）中进行大规模验证。
*   **机制解释尚浅**：虽然发现了数据增加导致表示退化的现象，但对于“为什么更多数据会导致模型学习捷径”的深层诱因探讨尚处于假设阶段。
*   **超参数敏感性**：正则化项的权重 $\lambda$ 和作用层数的选择可能需要针对不同架构进行精细调优。

（完）
