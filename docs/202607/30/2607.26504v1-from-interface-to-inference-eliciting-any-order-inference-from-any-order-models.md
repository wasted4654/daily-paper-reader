---
title: "From Interface to Inference: Eliciting Any-Order Inference from Any-Order Models"
title_zh: 从接口到推理：从任意顺序模型中激发任意顺序推理
authors: "Seunggeun Kim, Jaeyeon Kim, Taekyun Lee, Yuyuan Chen, Yilun Du, Sham Kakade, Sitan Chen"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26504v1"
tags: ["query:ardm"]
score: 8.0
evidence: 掩码扩散模型作为自回归模型在任意顺序推理中的替代方案
tldr: 针对代码生成等非因果推理任务，传统自回归模型缺乏原生任意顺序推理能力。本文指出掩码扩散模型虽有任意顺序接口，但因位置不确定性存在推理鸿沟。为此，作者提出了基于插入的FlexMDM和潜空间LatentMDM，分别通过放宽位置约束和在语义段上搜索生成顺序，实现了真正的任意顺序推理，并在Python编程和数学推理任务中验证了其有效性。
selection_source: fresh_fetch
motivation: 尽管掩码扩散模型具备任意顺序的训练接口，但由于固定画布下的位置不确定性，导致其在实际推理中难以有效执行非因果生成。
method: 提出了FlexMDM（基于插入的掩码扩散）和LatentMDM（潜空间掩码扩散），分别通过动态插入和粗粒度语义段预测来优化推理过程。
result: 实验表明，7B规模的FlexMDM在Python代码任务和125M规模的LatentMDM在GSM8K任务上均展现出优异的任意顺序推理能力和性能提升。
conclusion: 通过解决位置不确定性问题，掩码扩散模型可以实现高效的任意顺序推理，为非因果离散推理任务提供了新的范式。
---

## 摘要
许多离散推理任务（如代码生成）本质上是非因果的：程序员在宏观结构和局部细节之间穿梭，我们将这一过程称为任意顺序推理（any-order inference）。对于缺乏原生任意顺序接口的自回归语言模型，诸如填空（infilling）和下一编辑预测（next-edit prediction）等非因果能力需要手工设计的机制。我们能否设计出原生支持任意顺序推理的模型？掩码扩散模型（Masked diffusion models）最近成为备受关注的候选方案，因为它们的任意顺序训练目标自然地提供了一个任意顺序预测接口。然而，这一接口并不会自动产生任意顺序推理。我们证明了这种“接口-推理差距”源于位置不确定性：固定画布（fixed-canvas）的词元级（token-level）模型可能知道应该出现什么语义组件，但不知道该将其放在何处。鉴于此，我们提出了两种互补的方法：(1) 基于插入的掩码扩散，基于 FlexMDM (Kim et al, 2025)，通过插入操作放宽了对固定位置的限制，从而实现了跨非连续区域的生成；(2) 潜空间掩码扩散将预测转向更粗粒度的语义分段，从而实现了对潜在生成顺序的搜索。在实验上，我们针对 Python 编程训练了一个 7B 的 FlexMDM，针对 GSM8K 训练了一个 125M 的 LatentMDM，并证明这两种方法都能诱导出独特的任意顺序推理行为，并提升下游任务性能。我们在 https://github.com/SeunggeunKimkr/genuine-any-order 发布了代码库。

## Abstract
Many discrete reasoning tasks, such as code generation, are inherently non-causal: programmers move between high-level structure and local details, a process we call any-order inference. For autoregressive language models, which lack a native any-order interface, non-causal abilities such as infilling and next-edit prediction require hand-designed mechanisms. Can we instead design models that natively support any-order inference? Masked diffusion models have recently emerged as compelling candidates, as their any-order training objective naturally offers an any-order prediction interface. This interface, however, does not automatically yield any-order inference. We demonstrate that this interface-inference gap stems from positional uncertainty: fixed-canvas, token-level models may know what semantic component should appear without knowing where to place it. In light of this, we propose two complementary approaches: (1) Insertion-based masked diffusion, building on FlexMDM (Kim et al, 2025), relaxes fixed-position commitments via insertions, enabling generation across non-contiguous regions. (2) Latent-space masked diffusion shifts prediction to coarser semantic segments, enabling search over latent generation orders. Empirically, we train a 7B FlexMDM for Python coding and a 125M LatentMDM for GSM8K and show that both approaches induce distinct any-order inference behaviors and improve downstream performance. We release our codebase at https://github.com/SeunggeunKimkr/genuine-any-order.

---

## 论文详细总结（自动生成）

这篇论文对掩码扩散模型（Masked Diffusion Models, MDMs）在非线性推理任务中的表现进行了深入探讨，并提出了改进方案。以下是结构化的总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：人类在编写代码或解决复杂问题时，往往不是严格从左到右进行的，而是在宏观结构和局部细节间反复跳转（即“任意顺序推理”）。
*   **核心矛盾**：虽然掩码扩散模型在训练上具备“任意顺序预测”的接口，但在实际推理中，它们往往会陷入“因果坍缩”（Causal Collapse），即生成顺序退化为类似自回归的从左到右模式。
*   **核心发现**：论文指出这种差距源于**位置不确定性（Positional Uncertainty）**。在固定画布的词元级模型中，模型可能确信要生成某个语义组件（如 `return False`），但由于不确定其精确位置，导致概率分布分散，从而在置信度排序中被局部确定的词元击败，迫使推理回归线性顺序。

### 2. 方法论：核心思想与关键技术
论文提出了两种互补的方案来打破位置不确定性：

*   **FlexMDM（基于插入的掩码扩散）**：
    *   **核心思想**：放宽“固定画布”限制。允许模型在推理过程中动态插入新的掩码词元。
    *   **技术细节**：在训练中引入删除操作，使模型学习预测两个词元间应插入的词元数量（使用泊松分布建模）。
    *   **推理机制**：通过“插入进度坐标”进行时间重参数化，允许在推理时调整插入强度（插入温度），使模型能先构建框架再填充细节。
*   **LatentMDM（潜空间掩码扩散）**：
    *   **核心思想**：放宽“词元级预测”限制。将预测对象从单个词元提升到语义段（如一行代码）。
    *   **技术细节**：使用轻量级编码器将语义段映射为连续潜向量，在潜空间进行掩码扩散预测，再由自回归解码器还原为词元。
    *   **推理机制**：基于段级别的置信度（Token对数似然的均值）来决定生成顺序，从而在更高层级的语义空间搜索生成路径。

### 3. 实验设计
*   **数据集与场景**：
    *   **代码生成**：使用 HumanEval(+)、MBPP(+) 和 LiveCodeBench 评估 Python 代码生成能力。
    *   **数学推理**：使用 TinyGSM（将 GSM8K 转化为结构化 Python 代码）评估数学逻辑推理。
*   **Benchmark 与对比方法**：
    *   **FlexMDM 对比**：以 7B 规模的 Dream-Coder 为基准。
    *   **LatentMDM 对比**：对比了自回归模型（AR）、标准 MDM、潜空间流模型（S-FLM）和 DUO 等。
*   **评估指标**：除了常规的 Pass@K，还引入了基于抽象语法树（AST）的结构指标：CBC（提交前覆盖率）、RUB（返回未完成块）和 OBW（开放块宽度），用以量化“任意顺序”的真实程度。

### 4. 资源与算力
*   **FlexMDM**：基于 Dream-Coder 7B 进行全参数微调。使用了约 **260 万条** Python 数据，在 **16 台 H100 GPU** 上训练了 **3 天**（约 1150 GPU 小时）。
*   **LatentMDM**：从头训练 125M 参数模型。在 **4 台 80GB A100 GPU** 上训练了 **3 天**（50 万步迭代）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 125M 到 7B 不同量级的模型，并在代码和数学两个典型非因果推理领域进行了验证。
*   **消融实验**：针对插入温度（Insertion Temperature）、段选择策略（Scoring rules）以及并行解码规模（k）进行了详细的消融研究。
*   **客观性**：通过 AST 树分析证明了模型确实产生了非线性生成行为，而非仅仅是性能指标的提升，实验设计较为严谨且对比公平（如在相同推理耗时下对比）。

### 6. 主要结论与发现
*   **打破坍缩**：FlexMDM 和 LatentMDM 均能显著缓解因果坍缩，诱导出更接近人类的非连续生成行为。
*   **性能提升**：FlexMDM 在 HumanEval+ 的 Pass@16 上超过了基准模型；LatentMDM 在相同推理预算下，准确率比自回归模型高出 5.5% 以上。
*   **位置确定性验证**：实验证实，将接口移至潜空间或引入插入机制，确实能让模型在位置不确定时仍能做出高置信度的语义决策。

### 7. 优点
*   **理论深刻**：成功诊断出 MDM 无法实现任意顺序推理的根本原因——位置不确定性，并给出了形式化定义。
*   **指标创新**：提出的 CBC、RUB 等树结构指标为评估生成模型的推理轨迹提供了新工具。
*   **实用性**：FlexMDM 展示了如何通过微调现有大模型来获取插入能力，具有较强的工程参考价值。

### 8. 不足与局限
*   **领域局限**：实验主要集中在 Python 代码和数学题上，这些任务具有极强的结构性，尚不清楚在纯自然语言创作（如小说）中是否有同样效果。
*   **训练成本**：LatentMDM 目前需要从头训练，且依赖于预定义的语义分段（如按行切分），这在处理非结构化文本时可能存在挑战。
*   **复杂度**：引入插入机制和潜空间增加了推理算法的复杂性，对推理引擎的优化提出了更高要求。

（完）
