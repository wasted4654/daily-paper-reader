---
title: Rethinking the Generation Order of Block Diffusion Language Models
title_zh: 重新审视块扩散语言模型的生成顺序
authors: "Kai Syun Hou, James Kwok"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24306v1"
tags: ["query:ardm"]
score: 9.0
evidence: 块扩散语言模型的并行自回归解码
tldr: 本研究探讨了块扩散语言模型（BDLM）的生成顺序，发现其天然更契合自左向右的解码方式。针对现有采样方法多为早期掩码扩散模型设计的局限，作者提出了并行自回归解码（PARD）。这是一种无需训练的采样方法，在保持自左向右去掩码结构的同时，实现了Token的并行提交。实验证明，PARD在生成质量上优于现有并行采样器，且在保持高质量的同时显著提升了生成速度。
selection_source: fresh_fetch
motivation: 现有的扩散模型采样方法主要针对早期掩码扩散模型设计，未能充分利用块扩散语言模型天然契合自左向右生成顺序的特性。
method: 提出并行自回归解码（PARD），一种无需训练的采样方法，通过在保持自左向右结构的同时允许并行Token提交来优化生成。
result: 实验表明，PARD在生成质量上一致优于现有的并行采样器，并能在极小质量损失下实现比纯自回归解码更快的速度。
conclusion: 重新思考扩散模型的生成顺序对提升BDLM的采样效率和质量至关重要，PARD为高效并行解码提供了新思路。
---

## 摘要
扩散语言模型支持灵活的任意顺序生成，但现有的采样方法大多是为早期的掩码扩散模型 (MDMs) 设计的。在这项工作中，我们研究了近期出现的块扩散语言模型 (BDLMs) 的采样问题。我们通过实验和分析表明，与 MDMs 相比，这些模型在本质上与自左向右解码更加契合。基于这一观察，我们提出了并行自回归解码 (PARD)，这是一种简单的无需训练的采样方法，它在保留自左向右去掩码结构的同时，允许并行提交 token。大量实验表明，PARD 在生成质量上始终优于现有的并行采样器，同时在仅有微小质量差距的情况下，相比纯自回归 (AR) 解码实现了显著的加速。

## Abstract
Diffusion language models enable flexible arbitrary-order generation, but existing sampling methods are mostly designed for early masked diffusion models (MDMs). In this work, we study sampling for recent block diffusion language models (BDLMs). We show empirically and analytically that these models are naturally more aligned with left-to-right decoding than MDMs. Based on this observation, we propose Parallel Autoregressive Decoding (PARD), a simple training-free sampling method that preserves left-to-right unmasking structure while allowing parallel token commitment. Extensive experiments show that PARD consistently outperforms existing parallel samplers in generation quality, while achieving substantial speedups over pure AR decoding with only a small quality gap.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking the Generation Order of Block Diffusion Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：扩散语言模型（DLMs）因其灵活的生成顺序和并行解码潜力，成为自回归模型（ARMs）的有力竞争者。近期，**块扩散语言模型（BDLMs）**（如 SDAR、Fast-dLLM）通过在块内应用扩散、块间保持自回归的方式，显著提升了生成质量和推理效率。
*   **核心问题**：现有的采样算法（如基于置信度、熵或边际的采样）大多是为早期的全序列掩码扩散模型（MDMs）设计的。作者发现，BDLMs 在训练机制上与 MDMs 有本质不同，这导致现有的“任意顺序”采样策略在 BDLMs 上并非最优。
*   **研究动机**：探索 BDLMs 是否存在天然的“自回归偏置（AR Bias）”，并据此设计一种既能保持并行效率又能对齐该偏置的新型采样方法。

### 2. 方法论：并行自回归解码（PARD）
*   **核心思想**：PARD 是一种**无需训练**的采样策略。它不再从整个掩码集合中挑选置信度最高的若干 Token，而是**严格遵循自左向右的顺序，仅接受最长的一个连续前缀**（该前缀中的每个 Token 必须满足预设的可靠性标准，如置信度阈值）。
*   **关键技术细节**：
    *   **AR 偏置分析**：作者通过数学证明（Proposition 1 & 2）指出，BDLM 在训练时观察到的上下文比 MDM 更接近自回归模式（即左侧全已知，右侧全掩码），且 BDLM 通常从预训练的 AR 模型微调而来，继承了强烈的自左向右偏好。
    *   **算法流程**：
        1.  在每个去噪步骤，模型预测当前块内所有掩码位置的概率分布。
        2.  应用筛选准则（如置信度阈值 $\tau_c$）。
        3.  **前缀约束**：从左向右扫描掩码位置，一旦遇到不满足阈值的 Token 即停止，仅提交该位置之前的连续合格 Token。
        4.  若最左侧 Token 都不合格，则退化为仅提交最左侧的一个 Token（确保进度）。
    *   **兼容性**：PARD 可作为插件应用于 Confidence、Margin 或 Entropy 等多种现有采样准则。

### 3. 实验设计
*   **数据集/场景**：涵盖代码生成（HumanEval, MBPP）、数学推理（GSM8K, MATH）、指令遵循（IFEval）和通用推理（BigBenchHard）。
*   **Benchmark 模型**：
    *   **Fast-dLLM v2 7B**
    *   **SDAR 8B**
    *   **LLaDA2.1-Mini 16B**
*   **对比方法**：
    *   **串行采样**：Confidence, Margin, Entropy, Uncode, 以及纯 AR 采样。
    *   **并行采样**：KLASS（KL引导）、EB-Sampler（熵约束）、APD（自适应并行解码）、Hierarchy（层级解码）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA A6000 GPU** 上完成。
*   **训练细节**：该方法为 **Training-free（无需训练）**，直接在公开的预训练模型检查点上进行推理评估。
*   **推理配置**：使用了 bfloat16 精度，Batch Size 为 4。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流 BDLM 模型上进行了跨 6 个基准测试的全面评估。
    *   进行了详尽的“速度-质量权衡（Speed-Quality Trade-off）”曲线分析。
    *   包含消融实验（对比固定长度前缀与自适应前缀）。
    *   包含随机采样（Top-p）下的鲁棒性测试。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最新的并行采样器，还通过数学证明解释了现象背后的本质原因，并提供了定性的案例分析（Case Study）来展示 PARD 如何避免任意顺序采样带来的逻辑错误。

### 6. 主要结论与发现
*   **BDLM 天然契合 AR 顺序**：实验显示，在 BDLM 上，简单的纯 AR 采样效果优于复杂的任意顺序采样；而在从零训练的 MDM（如 LLaDA）上结论相反。
*   **PARD 的优越性**：PARD 在所有测试模型和任务中一致优于现有的并行采样器。
*   **性能平衡**：PARD 能够达到纯 AR 采样 99% 以上的精度，同时实现 **1.3x 到 3.6x 的加速**。
*   **代码任务提升显著**：在代码生成任务中，保持 AR 顺序对逻辑正确性至关重要，PARD 的提升尤为明显。

### 7. 优点：亮点总结
*   **简单高效**：无需重新训练模型，仅通过修改解码逻辑即可获得显著的性能提升。
*   **理论支撑**：通过概率论证明了 BDLM 训练分布与 AR 模式的对齐程度，为启发式采样提供了坚实的理论基础。
*   **通用性强**：可以无缝集成到现有的各种扩散模型采样准则中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 到 16B 规模的模型，尚未在百亿（100B+）级别的超大规模扩散模型上验证。
*   **训练阶段未优化**：PARD 仅针对推理端，未探讨如果在训练阶段引入采样感知的优化是否能进一步提升效果。
*   **依赖模型偏置**：该方法主要针对具有 AR 偏置的 BDLM，对于完全从零训练且无块结构的 MDM，其优势可能不明显。

（完）
