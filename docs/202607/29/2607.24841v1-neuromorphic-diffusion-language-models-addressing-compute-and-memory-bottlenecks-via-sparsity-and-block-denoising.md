---
title: "Neuromorphic Diffusion Language Models: Addressing Compute and Memory Bottlenecks via Sparsity and Block Denoising"
title_zh: 神经形态扩散语言模型：通过稀疏性与分块去噪解决计算与内存瓶颈
authors: "Dengyu Wu, Clement Ruah, Jiechen Chen, Bipin Rajendran, Osvaldo Simeone"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.24841v1"
tags: ["query:ardm"]
score: 8.0
evidence: 类脑扩散语言模型与块去噪
tldr: 针对自回归大语言模型在推理时因频繁访问参数导致的低效率和高能耗问题，本文提出了神经形态掩码扩散语言模型（N-MDLM）。该模型创新性地结合了块扩散技术与基于脉冲的神经形态计算，通过块并行生成提高 token 吞吐量，并利用脉冲诱导的稀疏性减少有效参数传输与计算开销。实验证明，N-MDLM 在翻译任务中显著提升了能量效率和吞吐量，有效解决了计算和内存瓶颈。
selection_source: fresh_fetch
motivation: 旨在解决自回归模型推理时因逐个生成 token 导致的低运算强度、高能耗以及内存访问瓶颈。
method: 提出 N-MDLM 架构，将块扩散的并行生成能力与脉冲神经网络的稀疏计算特性相结合，并开发了 token 级 Roofline 模型进行效率分析。
result: 在翻译任务中，N-MDLM 显著提升了能效和吞吐量，即使在传统扩散模型无法超越自回归模型的计算受限平台上也表现出色。
conclusion: 通过协同利用稀疏性和块并行生成，N-MDLM 为实现高效、低功耗的大语言模型推理提供了一种极具潜力的技术路径。
---

## 摘要
自回归（AR）大语言模型（LLMs）在推理时本质上是低效的，因为每个生成的 token 都需要访问全量模型参数，导致低操作强度和高能耗。掩码扩散语言模型（MDLMs）通过允许每次参数访问生成多个 token，部分解决了内存受限场景下的这一局限性。为了在具有大量片上内存的现代平台上进一步提高推理效率，本研究提出了神经形态 MDLMs（N-MDLMs），它将分块扩散与基于脉冲的神经形态计算相结合，以共同提高吞吐量和能效。虽然分块扩散通过每次参数访问产生多个 token 来增加 token 吞吐量，但脉冲诱导的稀疏性通过跳过非活动通道减少了有效的参数传输和计算量。为了分析稀疏性与扩散的协同效应，我们开发了一个受 Roofline 模型启发的 token 级模型，该模型捕捉了分块并行生成和脉冲稀疏性对解码效率的综合影响。在翻译任务上的实验结果表明，得益于脉冲诱导的稀疏性，N-MDLMs 即使在 MDLMs 无法优于 AR-LLMs 的计算受限平台上，也能在能效和吞吐量方面取得显著提升。

## Abstract
Autoregressive (AR) large language models (LLMs) are inherently inefficient at inference time because each generated token requires accessing the full set of model parameters, leading to low operational intensity and high energy consumption. Masked diffusion language models (MDLMs) partially address this limitation for memory-bound settings by allowing multiple tokens to be generated per parameter access. In order to further enhance inference efficiency on modern platforms with extensive in-chip memory, this work proposes neuromorphic MDLMs (N-MDLMs), which integrate block diffusion with spike-based neuromorphic computation to jointly improve throughput and energy efficiency. While block diffusion increases token throughput by producing multiple tokens per parameter access, spike-induced sparsity reduces effective parameter traffic and computations by skipping inactive channels. To analyze the synergistic effect of sparsity and diffusion, we develop a token-level roofline-inspired model that captures the combined impact of block-parallel generation and spike sparsity on decoding efficiency. Experimental results on translation tasks show that, thanks to spike-induced sparsity, N-MDLMs achieve substantial improvements in energy efficiency and throughput even in compute-bound platforms for which MDLMs would fail to improve over AR-LLMs.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**神经形态掩码扩散语言模型（N-MDLM）**的新型架构，旨在通过结合分块并行生成与脉冲神经网络（SNN）的稀疏计算特性，解决大语言模型（LLM）推理中的计算与内存瓶颈。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的自回归（AR）大语言模型在推理时存在严重的效率瓶颈。每生成一个 token 都需要访问全量模型参数，导致**运算强度低**且**能效差**。
*   **研究背景**：
    *   **掩码扩散模型（MDLM）**虽然能通过“一次参数访问生成多个 token（分块生成）”来缓解内存受限问题，但在现代具有大容量片上内存的硬件（如神经形态芯片或专用加速器）上，系统往往转为**计算受限**，此时 MDLM 的优势会消失。
    *   **神经形态计算**利用脉冲（Spike）驱动的稀疏性，可以显著降低计算量和参数传输量。
*   **研究目标**：将扩散模型的并行性与神经形态计算的稀疏性结合，实现吞吐量与能效的双重提升。

### 2. 方法论
*   **核心思想**：在分块扩散（Block Diffusion）的去噪过程中引入脉冲神经元动力学，利用事件驱动（Event-driven）的特性跳过无效计算。
*   **关键技术细节**：
    *   **架构设计**：采用编码器-解码器结构。编码器处理上下文，解码器通过 $S$ 个去噪步骤迭代细化一个包含 $B$ 个 token 的掩码块。
    *   **模型转换**：通过基于量化的转换方法，将预训练的 MDLM 转换为 N-MDLM。使用**整合-发放（IF）神经元模型**，将激活值转换为时间轴上的三值脉冲（-1, 0, 1）。
    *   **Roofline 分析框架**：开发了一个 token 级的分析模型，量化了块大小 $B$ 和脉冲稀疏度 $\sigma$ 对计算负载（$N_{ops}$）和内存传输（$N_{data}$）的共同影响。
    *   **计算逻辑**：只有当脉冲发生时才触发突触运算，从而在计算受限和内存受限两种场景下都能通过稀疏性获益。

### 3. 实验设计
*   **数据集/场景**：WMT 14 德语-英语（DE-EN）翻译任务。
*   **Benchmark（对比方法）**：
    *   **AR-LLM**：标准的自回归大语言模型。
    *   **MDLM**：传统的分块掩码扩散语言模型。
    *   **N-AR-LLM**：引入脉冲稀疏性的自回归模型。
*   **硬件场景模拟**：
    *   **OCMS（片外内存系统）**：模拟传统 GPU，内存带宽是主要瓶颈。
    *   **ICMS（片上内存系统）**：模拟神经形态硬件或 AI 加速器，计算能力更容易成为瓶颈。

### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA DGX Spark GPU** 上进行模拟。
*   **模型规模**：约 **2.5 亿（250M）参数**（编码器 28 层，解码器 4 层）。
*   **训练细节**：从预训练模型转换后，使用 Adam 优化器进行了 1000 步的微调，batch size 为 16，学习率为 $3 \times 10^{-5}$。
*   **时间步长**：脉冲编码的时间步长 $T=8$。

### 5. 实验数量与充分性
*   **实验规模**：论文针对不同的块大小（$B=1, 2, 4, 8$）和不同的稀疏度超参数（$K$ 值）进行了多组对比实验。
*   **充分性评价**：
    *   **客观性**：通过 Roofline 模型从理论上推导吞吐量，并结合实验测得的真实稀疏度进行验证，逻辑严密。
    *   **公平性**：对比了相同架构下的 AR 和扩散版本，并考虑了两种截然不同的硬件架构（OCMS vs ICMS），实验设计较为全面。
    *   **局限性**：实验主要集中在翻译任务上，尚未在更大规模（如数十亿参数）的通用 LLM 上验证。

### 6. 主要结论与发现
*   **协同效应**：N-MDLM 结合了扩散模型的“分块并行”和脉冲神经网络的“稀疏性”。在 ICMS（计算受限）场景下，普通 MDLM 无法超越 AR 模型，但 N-MDLM 凭借稀疏性显著提升了吞吐量。
*   **能效提升**：N-MDLM 在保持与 AR 模型相当的翻译质量（BLEU 分数仅轻微下降）的同时，大幅降低了每个 token 的能耗。
*   **瓶颈转移**：增加块大小 $B$ 可以摊销内存访问成本，而提高稀疏度则能缓解计算瓶颈。两者结合使模型能更好地适应不同类型的硬件限制。

### 7. 优点
*   **理论创新**：首次将神经形态计算与扩散语言模型结合，并提出了专门的 Roofline 分析框架。
*   **硬件友好**：针对未来“存算一体”或“高带宽片上内存”硬件趋势提供了明确的优化路径。
*   **效率平衡**：在不显著损失模型精度的情况下，实现了吞吐量和能效的量级提升。

### 8. 不足与局限
*   **任务单一**：目前仅在翻译任务上验证，缺乏在长文本生成或逻辑推理等更复杂任务上的表现数据。
*   **硬件模拟**：实验结果基于 GPU 上的模拟计算，而非在真实的神经形态芯片（如 Loihi 或 NorthPole）上直接运行，实际部署中的硬件开销（如脉冲分发开销）可能未完全体现。
*   **转换损失**：虽然微调可以弥补，但从连续值到离散脉冲的转换仍存在一定的精度风险。

（完）
