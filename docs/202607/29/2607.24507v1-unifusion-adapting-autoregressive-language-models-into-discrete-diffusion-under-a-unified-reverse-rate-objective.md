---
title: "UNIFUSION: Adapting Autoregressive Language Models into Discrete Diffusion under a Unified Reverse-Rate Objective"
title_zh: UNIFUSION：在统一反向速率目标下将自回归语言模型适配为离散扩散模型
authors: "Xiaoyi Jiang, Jingyuan Li, Yixuan Jiang, Wei Liu, Yi Zhu, Zuoqiang Shi, Pipi Hu"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24507v1"
tags: ["query:ardm"]
score: 9.0
evidence: 将自回归语言模型适配到离散扩散模型
tldr: 本研究提出了 UNIFUSION，旨在将预训练的自回归（AR）语言模型直接适配为离散扩散模型。通过建立统一的逆向速率目标函数，该方法解决了不同扩散核之间目标函数不一致的问题。UNIFUSION 支持在掩码和均匀噪声核之间切换，并通过对 GPT2 进行持续预训练，在生成困惑度与熵的权衡以及多项下游任务（如 WinoGrande 和 BBH）上显著优于现有的离散扩散模型。
selection_source: fresh_fetch
motivation: 现有的离散扩散模型在不同损坏核下目标函数和参数化方式不统一，导致难以将预训练自回归模型高效适配到均匀噪声扩散模型中。
method: 提出一种基于统一逆向速率目标的广义 KL 散度框架，并推导出共享的 x0 接口，通过持续预训练将 GPT2 适配为均匀噪声扩散模型。
result: 实验表明 UNIFUSION 在生成困惑度与熵的平衡上表现优异，并在 WinoGrande、SIQA 和 BBH 等推理任务中达到了同规模扩散模型的领先水平。
conclusion: 该研究通过统一的数学框架简化了自回归模型向离散扩散模型的迁移过程，证明了均匀噪声扩散在语言建模和下游任务中的潜力。
---

## 摘要
现有方法主要将预训练的自回归（AR）语言模型适配到掩码扩散（masked diffusion），而我们直接将其适配到均匀噪声扩散（uniform-noise diffusion），在这种模式下，采样过程中的每个 token 都保持可编辑状态。然而，由于现有的离散扩散模型（DLM）使用不同的目标函数和预测参数化方式，跨损坏核（corruption kernels）适配 AR 检查点仍然具有挑战性。我们通过将 SEDD、MDLM/GIDD、M

## Abstract
Existing methods mainly adapt pretrained autoregressive (AR) language models to masked diffusion, whereas we directly adapt them to uniform-noise diffusion, where every token remains editable during sampling. However, adapting AR checkpoints across corruption kernels remains challenging because existing DLMs use different objectives and prediction parameterizations. We establish connections among SEDD, MDLM/GIDD, M2S, and Neural CTMC by expressing their conditional losses as a single generalized Kullback--Leibler objective over model reverse rates. We further derive conversions from clean-token predictions to concrete-score, posterior-mean, and exit-rate/jump parameterizations, yielding a shared \(x_0\) interface that supports switching between mask and uniform kernels. Building on these connections, we propose \ours{}, a simple continual pre-training approach for directly adapting pretrained GPT2 checkpoints to uniform-noise diffusion. Through systematic evaluation of 124M- and 355M-parameter models, we show that \ours{} steadily improves the trade-off between generative perplexity (GenPPL) and unigram entropy as the sampling budget increases from 16 to 256 steps. At 256 steps, \ours{}-S and \ours{}-M achieve GenPPL/entropy pairs of \(97.783/5.2626\) and \(71.516/5.6669\), respectively; no evaluated model at the same scale simultaneously outperforms \ours{} on both metrics. At both scales, \ours{} also achieves the highest WinoGrande, SIQA, and BBH accuracy among the compared diffusion models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **UNIFUSION** 的新框架，旨在将预训练的自回归（AR）语言模型（如 GPT-2）直接适配为离散扩散模型（Discrete Diffusion），特别是针对“均匀噪声”扩散核。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自回归模型（AR）虽然强大，但生成顺序固定且不可修改。离散扩散模型（DLM）允许在采样过程中对序列进行全局编辑，但从头训练 DLM 会浪费 AR 模型中已有的语言知识。
*   **核心挑战**：
    1.  **参数化不统一**：现有的 DLM 方法（如 SEDD, MDLM, GIDD, M2S, Neural CTMC）使用完全不同的预测目标和损失函数，难以互相转换。
    2.  **核适配困难**：现有的适配研究（如 DiffuGPT）大多局限于“掩码（Masking）”损坏核，而“均匀噪声（Uniform Noise）”核（将 token 随机替换为词表中的其他项）能让所有位置在采样中始终保持可编辑，但缺乏直接从 AR 适配的方法。
*   **整体含义**：论文试图建立一套统一的数学框架，证明不同扩散方法的本质一致性，并据此实现 AR 到均匀扩散模型的高效迁移。

### 2. 方法论：核心思想与关键技术
*   **统一反向速率目标（Unified Reverse-Rate Objective）**：
    *   论文在数学上证明了 SEDD、MDLM/GIDD、M2S 和 Neural CTMC 的条件损失函数，在“反向速率空间（Reverse-Rate Space）”中都可以表示为同一个**广义 Kullback–Leibler (KL) 散度**。
    *   这意味着这些方法虽然预测的东西不同（分数、干净 token、后验均值等），但它们在优化同一个反向马尔可夫链过程。
*   **共享的 $x_0$ 接口**：
    *   推导出了从“干净 token 预测（$x_0$）”到其他四种参数化方式（Concrete Score, Posterior Mean, Exit Rate/Jump）的显式转换公式。
    *   这使得 AR 模型的输出（本质上是 $x_0$ 的预测）可以作为所有离散扩散核的通用初始化接口。
*   **Unifusion 适配流程**：
    1.  **初始化**：将 GPT-2 的输出层平移一位，用于初始化扩散模型的 $x_0$ 预测器。
    2.  **注意力退火**：在持续预训练初期，将因果注意力（Causal Attention）逐渐退火为双向注意力（Bidirectional Attention），使模型适应扩散任务的全局上下文。
    3.  **时间条件注入**：学习扩散时间 $t$ 的条件参数。
    4.  **均匀噪声训练**：使用线性均匀噪声调度进行微调，无需经过中间的掩码扩散阶段。

### 3. 实验设计
*   **数据集**：主要使用 FineWeb 语料库（30B token 子集）进行持续预训练。
*   **模型规模**：124M (Small) 和 355M (Medium) 参数规模。
*   **对比方法（Baselines）**：
    *   **自回归模型**：GPT-2。
    *   **离散扩散模型**：DiffuGPT, SEDD (mask/uniform), GIDD, Neural-CTMC, M2S, DOU。
*   **评估指标**：
    *   **生成质量**：生成困惑度（GenPPL）与一元语法熵（Unigram Entropy）的 Pareto 前沿。
    *   **下游任务**：HellaSwag, WinoGrande, SIQA, PIQA, BBH（零样本准确率）。

### 4. 资源与算力
*   **算力配置**：使用了 **24 张 NVIDIA H100 (80GB) GPU**。
*   **训练时长**：
    *   **Unifusion-S (124M)**：训练 240,000 步，处理约 106.2B tokens。
    *   **Unifusion-M (355M)**：训练 255,000 步，处理约 225.6B tokens。
    *   **Unifusion-Gemma (4.6B)**：作为扩展实验，训练了 16,400 步（约 9.67B tokens）。
*   **并行技术**：使用了 DeepSpeed ZeRO-2/3 优化。

### 5. 实验数量与充分性
*   **实验组数**：
    *   对比了 4 种不同参数化目标的训练轨迹。
    *   对比了 4 种不同的转换路径（如 AR->Mask, AR->Uniform 等）。
    *   在 5 个不同的随机种子下进行了统计评估。
*   **充分性**：实验覆盖了从理论验证（数值模拟）到大规模语言模型适配的全过程。通过 GenPPL-Entropy 曲线展示了模型在“流畅度”与“多样性”之间的权衡，相比单一指标更具说服力。

### 6. 主要结论与发现
*   **理论一致性**：证明了现有主流离散扩散损失函数在速率空间是等价的，消除了方法选择上的困惑。
*   **适配效率**：直接从 AR 适配到均匀扩散模型是可行的，且效果优于经过掩码扩散的“两步走”方案。
*   **性能领先**：在相同的采样步数（如 256 步）下，Unifusion 在 GenPPL 和熵的平衡上达到了最优（Pareto 领先）。
*   **下游任务增强**：Unifusion 在 WinoGrande、SIQA 和 BBH 等推理任务上的表现不仅超过了其他扩散模型，甚至在某些任务上超过了其 AR 初始模型。

### 7. 优点
*   **理论贡献突出**：统一了离散扩散模型的数学表达，为后续研究提供了清晰的理论框架。
*   **灵活性高**：提出的 $x_0$ 接口允许模型在不改变核心架构的情况下，灵活切换不同的噪声核。
*   **平衡性好**：有效解决了扩散模型容易产生重复、低熵文本的问题，生成结果更具多样性。

### 8. 不足与局限
*   **采样延迟**：尽管比 AR 模型快（并行生成），但达到高质量生成仍需 64-256 步迭代，推理成本高于单步生成模型。
*   **模型规模限制**：主要实验集中在 124M 和 355M 规模，虽然有 4.6B 的初步尝试，但在超大规模参数（如 70B+）上的适配表现和算力需求尚待验证。
*   **任务覆盖**：主要关注通用语言生成和基础推理，对于长文本摘要或复杂对话任务的适配效果未深入探讨。

（完）
