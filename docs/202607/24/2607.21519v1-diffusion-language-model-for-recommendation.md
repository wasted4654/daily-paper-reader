---
title: Diffusion Language Model for Recommendation
title_zh: 用于推荐的扩散语言模型
authors: "Chengyi Liu, Yongqi Zhou, Junwei Pan, Zhixiang Feng, Chengguo Yin, Haijie Gu, Jie Jiang, Yinghao Liu, Yujuan Ding, Qing Li, Wenqi Fan"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21519v1"
tags: ["query:ardm"]
score: 8.0
evidence: 离散扩散语言模型作为自回归范式的替代方案
tldr: 针对现有大语言模型推荐系统在自回归生成中存在的结构依赖建模不足及误差累积问题，本文提出DLMRec，一种专为推荐设计的离散扩散语言模型。该模型通过协作感知分词器捕捉多步协作信号，利用课程驱动训练策略优化去噪过程，并引入稳定性感知投票机制提升生成的一致性。DLMRec为生成式推荐提供了一种非自回归的替代方案，有效增强了对复杂用户偏好的建模能力。
selection_source: fresh_fetch
motivation: 传统的自回归语言模型在推荐中难以捕捉项间的结构依赖，且存在单向解码导致的误差累积问题。
method: 提出DLMRec模型，结合协作感知分词器、课程驱动训练策略和稳定性感知投票机制，实现离散扩散生成。
result: 该方法通过非自回归的去噪过程，能够更准确地恢复用户偏好并生成高质量的推荐结果。
conclusion: 离散扩散语言模型在处理推荐任务时展现出比自回归模型更强的双向上下文建模能力和生成鲁棒性。
---

## 摘要
大语言模型（LLM）赋能的推荐系统已成为生成式推荐中一种极具前景的范式，利用其强大的语义推理和生成能力来建模复杂且多样的用户偏好。然而，大多数现有方法依赖于自回归范式，这对于推荐任务而言并非最优。其“预测下一个 token”的目标强调顺序，而非用户偏好背后的结构性项间依赖关系。此外，受前缀限制的生成限制了双向上下文，并采用从左到右的解码方式，导致早期错误在没有修正的情况下不断累积。受扩散语言模型成功的启发，我们提出了 DLMRec，这是一种专为推荐定制的离散扩散语言模型，为自回归生成提供了一个极具吸引力的替代方案。具体而言，DLMRec 引入了三个关键组件，将扩散语言建模与推荐相结合：首先，协作感知随机分词器将多跳协作信号编码为与扩散建模兼容的具有表现力的离散 token；其次，课程驱动的训练策略通过渐进式的项级和 token 级学习，使去噪过程与偏好恢复保持一致；第三，稳定性感知投票机制聚合迭代预测，以提高生成的连贯性和鲁棒性。

## Abstract
Large language model (LLM)-empowered recommender systems have emerged as a promising paradigm for generative recommendation, leveraging their strong semantic reasoning and generative capacity to model complex, diverse user preferences. However, most existing approaches rely on an autoregressive paradigm that is suboptimal for recommendation. The next-token objective emphasizes sequential order rather than the structural inter-item dependencies underlying user preferences. In addition, prefix-constrained generation restricts bidirectional context and commits to left-to-right decoding, causing early errors to accumulate without correction. Inspired by the success of diffusion language models, we propose \textbf{DLMRec}, a discrete diffusion language model tailored for recommendation that offers a compelling alternative to autoregressive generation. Specifically, DLMRec introduces three key components to bridge diffusion language modeling with recommendation. First, a collaborative-aware stochastic tokenizer encodes multi-hop collaborative signals into expressive discrete tokens compatible with diffusion modeling. Second, a curriculum-driven training strategy aligns the denoising process with preference recovery through progressive item- and token-level learning. Third, a stability-aware voting mechanism aggregates iterative predictions to improve generation consistency and robustness.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DLMRec** 的新型推荐框架，旨在利用**离散扩散语言模型（Discrete Diffusion Language Model）**替代传统的自回归（Autoregressive）模型，以解决生成式推荐中的结构建模不足和误差累积问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的推荐系统大多采用**自回归范式**（即预测下一个 token）。这种范式在推荐场景中存在三大缺陷：
    1.  **目标不匹配**：自回归强调严格的从左到右顺序，而推荐更依赖于项与项之间的结构化依赖关系，交互顺序往往包含噪声。
    2.  **上下文受限**：前缀约束的生成限制了模型利用双向上下文的能力。
    3.  **误差累积**：一旦某个步骤预测错误，错误会随解码过程传播且无法修正。
*   **研究背景**：扩散模型在图像和文本生成领域展现出强大的双向建模和迭代优化能力，论文试图探索其作为推荐系统生成骨干网的潜力。

### 2. 方法论与关键技术
DLMRec 核心思想是将推荐任务建模为离散状态下的去噪过程，包含以下三个关键组件：
*   **协作感知随机分词器 (CAST)**：
    *   利用 GNN（如 LightGCN）提取用户/项的多跳协作嵌入。
    *   将不同跳数的语义解耦并映射到多个子码本（Codebooks）中。
    *   引入**随机分配机制**：根据相似度概率性地选择 token，以捕捉图结构中的不确定性和模糊性。
*   **课程驱动的训练策略**：
    *   **阶段 1（项级对齐）**：通过掩码项及其文本描述，让模型学习协作 token 与语义文本之间的对应关系。
    *   **阶段 2（Token 级偏好去噪）**：在 token 级别进行掩码，并引入**偏好感知损失函数**（对比学习），通过可微的软量化技术，将预测结果与真实目标项及负采样项进行对比，强化判别能力。
*   **稳定性感知投票机制 (Inference)**：
    *   在推理阶段，模型进行多次迭代优化。
    *   **稳定性检测**：识别在迭代中保持不变的“稳定 token”并提前固定；对“不稳定 token”继续优化。
    *   **投票聚合**：汇总所有迭代步骤的对数概率，通过投票产生最终推荐，提高鲁棒性。

### 3. 实验设计
*   **数据集**：LastFM（音乐）、MovieLens-1M（电影）、Amazon-Beauty（美妆）。
*   **Benchmark（基准测试）**：采用全排名设置下的 Recall@K 和 NDCG@K（K=10, 20）。
*   **对比方法**（共 12 种）：
    *   **传统 GNN**：LightGCN, SGL, LTGNN。
    *   **序列推荐**：SASRec, BERT4Rec。
    *   **扩散推荐**：DiffRec, CDRec, LLaDaRec。
    *   **LLM 推荐**：TIGER, LLaRa, CoLLM, TokenRec。

### 4. 资源与算力
*   **模型骨干**：使用了 **LLaDA-8B-Base** 作为扩散语言模型的基础。
*   **微调技术**：采用了 **LoRA** 参数高效微调。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到 8B 参数规模及扩散模型的迭代特性，其算力需求通常高于同规模的自回归模型微调。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的标准数据集上进行了测试，对比了 4 大类共 12 种 SOTA 方法。
*   **消融实验**：针对 CAST 分词器、项级/Token 级掩码、偏好损失等组件分别做了消融，验证了各模块的必要性。
*   **参数敏感性**：详细分析了码本大小（K, L）和扩散采样步数（S）对性能的影响。
*   **效率分析**：对比了不同采样步数下的推理延迟。
*   **评价**：实验设计全面、客观，通过多维度对比证明了扩散模型在推荐中的优越性。

### 6. 主要结论与发现
*   **扩散胜过自回归**：DLMRec 在所有数据集上均显著优于基于 Llama-3 的自回归推荐模型（如 LLaRa）。
*   **双向建模优势**：实验证明，扩散模型的双向上下文建模和迭代修正能力能更有效地捕捉复杂的用户偏好。
*   **少量迭代即可达标**：在推理时，仅需 2-3 步迭代即可达到最优性能，平衡了效果与效率。
*   **协作语义的重要性**：CAST 分词器通过引入图结构信息，显著提升了离散 token 的表达能力。

### 7. 优点与亮点
*   **范式创新**：首次将大规模离散扩散语言模型（8B 规模）系统性地应用于推荐系统，打破了自回归模型的垄断。
*   **分词器设计**：CAST 巧妙地解决了图结构数据与离散语言模型 token 之间的对齐问题，且考虑了随机性。
*   **推理优化**：稳定性感知投票机制有效缓解了扩散模型生成过程中的不确定性。

### 8. 不足与局限
*   **推理延迟**：尽管只需 2-3 步，但扩散模型的迭代推理天生比单次前向传播（或某些轻量级 LLM 方案）慢。
*   **训练复杂性**：多阶段课程学习和复杂的掩码策略增加了模型训练和调参的难度。
*   **长序列挑战**：论文主要关注短/中长度的交互序列，对于极长序列的扩散效率和显存占用未做深入探讨。
*   **算力透明度**：缺乏具体的硬件资源消耗报告，不便于复现者评估成本。

（完）
