---
title: "Ms. Forcing: Efficient Streaming Video Generation with Multi-Scale Patchification and Attention"
title_zh: Ms. Forcing：基于多尺度分块与注意力的流式视频生成
authors: "Zekun Li, Xiaoyan Cong, Hongyu Li, Zhiyang Dou, Chuan Guo, Abhay Mittal, Sizhe An, Srinath Sridhar"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20940v1"
tags: ["query:ardm"]
score: 8.0
evidence: 流式视频扩散模型中的嵌套自回归与去噪循环
tldr: 针对流式视频生成中传统方法计算冗余的问题，本文提出 Ms. Forcing 框架。该框架通过多尺度分块（MSP）根据噪声水平动态调整空间粒度，并结合多尺度自注意力（MSSA）优化计算效率。此外，引入 H-DMD 减少训练与推理的不一致性。实验表明，该方法在保持高质量生成的同时，推理速度提升至 22.84 FPS，显著优于现有方法。
selection_source: fresh_fetch
motivation: 传统的流式视频生成方法在处理不同噪声水平的状态时采用统一的空间粒度，导致了显著的计算冗余。
method: 提出多尺度分块（MSP）和多尺度自注意力（MSSA）机制，并结合同质噪声水平 DMD（H-DMD）优化训练过程。
result: "在 H200 GPU 上实现了 22.84 FPS 的生成速度，比 Rolling Forcing 快 39.6%，且在 VBench 各项指标上均有提升。"
conclusion: Ms. Forcing 通过自适应空间粒度有效降低了流式视频生成的计算开销，实现了高效且高质量的长视频实时生成。
---

## 摘要
流式视频扩散模型在交互式和动态世界模拟方面取得了实质性进展，但传统逐帧生成的嵌套自回归和去噪循环阻碍了实时部署。最近的滚动窗口方法在不同噪声水平下对多个连续帧进行流水线去噪，提高了吞吐量和长时稳定性。然而，这些方法以相同的细空间粒度对每个状态进行标记化，在联合去噪窗口中留下了大量与噪声相关的冗余。我们提出了 Ms.Forcing，这是一种高效的流式视频生成范式，可根据每个状态的噪声水平调整空间粒度。其多尺度分块（MSP）为噪声较大的状态分配较粗的图块，将活动窗口的 token 数量减少了 45%；而多尺度自注意力（MSSA）则将可见的非汇点（non-sink）键和值的密度与每个查询尺度相匹配，从而进一步降低注意力成本。由于这两种调度方案都由窗口位置固定，Ms.Forcing 保留了静态且硬件友好的计算图。我们进一步引入了同质噪声水平 DMD（H-DMD），它利用共享相同源噪声水平的清晰预测来组装每个生成视频，从而减少了 DMD 训练序列与推理时展开（rollouts）之间的不匹配。多尺度设计有助于抵消通过重叠窗口进行反向传播带来的额外训练成本。定量和定性实验表明，Ms.Forcing 在单块 H200 GPU 上达到了 22.84 FPS，比 Rolling Forcing 快 39.6%，同时在短视频和长视频生成设置中都显著提高了 VBench 评分。

## Abstract
Streaming video diffusion models have made substantial progress toward interactive and dynamic world simulation, but the nested autoregressive and denoising loops of conventional next-frame generation hinder real-time deployment. Recent rolling-window methods pipeline denoising across multiple consecutive frames at different noise levels, improving throughput and long-horizon stability. However, they tokenize every state at the same fine spatial granularity, leaving substantial noise-dependent redundancy in the joint denoising window. We propose Ms.Forcing, an efficient streaming video generation paradigm that adapts spatial granularity to each state's noise level. Its Multi-Scale Patchification (MSP) assigns coarser patches to noisier states, reducing the active-window token count by 45%, while Multi-Scale Self-Attention (MSSA) matches the density of visible non-sink keys and values to each query scale to further reduce attention cost. Because both schedules are fixed by window position, Ms.Forcing retains a static, hardware-friendly computation graph. We further introduce Homogeneous-Noise-Level DMD (H-DMD), which assembles each fake video from clean predictions sharing the same source noise level, thereby reducing the mismatch between DMD training sequences and inference-time rollouts. The multi-scale design helps offset the additional training cost of backpropagating through overlapping windows. We include both quantitative and qualitative experiments to show that Ms.Forcing reaches 22.84 FPS on a single H200 GPU, 39.6% faster than Rolling Forcing, while significantly improving VBench scores in both short video and long video generation setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Ms. Forcing: Efficient Streaming Video Generation with Multi-Scale Patchification and Attention》的深度学术总结：

### 1. 核心问题与整体含义
该论文针对**流式视频生成（Streaming Video Generation）**中的效率与稳定性问题展开研究。
*   **研究背景**：传统的自回归视频扩散模型采用“嵌套循环”（外层自回归生成帧，内层进行多步去噪），导致推理延迟极高，难以实现实时交互。
*   **核心痛点**：最近的“滚动窗口（Rolling Forcing）”方法虽然通过流水线化去噪步骤提高了吞吐量，但它对窗口内所有帧（无论噪声水平高低）都采用统一的细粒度空间分块（Patchification），造成了巨大的计算冗余。
*   **整体含义**：论文提出 **Ms. Forcing** 框架，核心逻辑是“噪声越大，处理越粗糙”。通过动态调整空间粒度，在保证视觉质量的同时大幅提升生成速度，实现了长视频的实时流式生成。

### 2. 方法论
Ms. Forcing 引入了三大核心技术：
*   **多尺度分块 (Multi-Scale Patchification, MSP)**：
    *   基于扩散模型“从粗到精”的特性，为窗口内噪声水平较高的帧分配更大的 Patch（如 $2p$ 或 $4p$），为接近清晰的帧分配原始 Patch（$p$）。
    *   这使得活动窗口内的 Token 数量减少了 **45%**。
*   **多尺度自注意力 (Multi-Scale Self-Attention, MSSA)**：
    *   匹配非汇点（non-sink）键值对（KV）的密度与查询（Query）的尺度。即：当查询是粗粒度时，其看到的上下文也进行空间下采样。
    *   在 MSP 基础上进一步减少了 **12.9%** 的注意力计算开销。
*   **同质噪声水平 DMD (Homogeneous-Noise-Level DMD, H-DMD)**：
    *   **改进分布匹配蒸馏（DMD）**：传统方法在训练时拼接不同噪声水平的预测帧，导致训练与推理（推理时只输出头部的清晰帧）不一致。
    *   **做法**：在每次更新时采样一个固定的窗口位置，收集所有连续窗口中对应该噪声水平的预测结果，组装成“同质”视频进行蒸馏，显著降低了长视频生成的质量漂移。

### 3. 实验设计
*   **基础模型**：Wan2.1-T2V-1.3B（分辨率 832×480）。
*   **数据集/场景**：
    *   **短视频**：使用 VBench 官方的 946 个提示词生成 5 秒视频。
    *   **长视频**：使用 MovieGen 基准的 128 个提示词生成 60 秒视频。
*   **对比方法**：包括 SkyReels-V2、CausVid、Self Forcing、LongLive 以及直接基准 Rolling Forcing。
*   **评估指标**：VBench 总分、质量分、语义分、FPS（吞吐量）以及长视频的质量漂移（Drift）。

### 4. 资源与算力
*   **推理算力**：在单块 **NVIDIA H200 GPU** 上进行测试。
*   **训练细节**：
    *   使用 16k 个 ODE 解对进行初始化，另用 6k 个解对预热新增的模块（LoRA、多尺度嵌入层等）。
    *   DMD 阶段训练 1400 步，Batch Size 为 64。
    *   **注意**：论文未明确给出总训练时长（小时/天），但提到了优化器参数（AdamW）和学习率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 5 秒短视频到 60 秒长视频的定量评估，甚至展示了超过 2 分钟的定性生成效果。
*   **消融实验**：非常充分。论文通过“累加式”消融（Rolling Forcing → +H-DMD → +MSP → +MSSA）清晰地展示了每个组件对速度提升和质量改善的贡献。
*   **客观性**：引入了“零样本探测（Zero-shot Probes）”实验，证明了在不重新训练的情况下，改变空间粒度对 LPIPS 指标影响较小，从而为 MSP 的可行性提供了理论支撑。

### 6. 主要结论与发现
*   **实时性突破**：Ms. Forcing 在 832×480 分辨率下达到了 **22.84 FPS**，比 Rolling Forcing 快了 **39.6%**。
*   **质量提升**：在 VBench 评分上优于所有对比的流式生成模型，尤其在语义一致性和长视频稳定性上表现突出。
*   **稳定性**：将 60 秒长视频的质量漂移从 2.227 降低至 1.700，有效抑制了长时生成的“曝光漂移”和伪影问题。

### 7. 优点
*   **计算效率极高**：巧妙利用噪声水平与空间频率的对应关系，通过减少 Token 数量实现硬件友好的加速。
*   **静态计算图**：多尺度调度由窗口位置固定，不需要动态路由，方便在现有推理框架中部署。
*   **训练推理一致性**：H-DMD 解决了蒸馏过程中的核心矛盾，使模型在长程生成中更加稳健。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1.3B 参数模型上，尚未在百亿级（如 10B+）模型上验证其扩展性。
*   **缓存更新开销**：论文提到 KV-cache 的每步更新仍存在显著延迟，抵消了一部分由 MSP/MSSA 带来的 DiT 计算加速，端到端吞吐量仍有优化空间。
*   **分辨率上限**：目前仅验证到 480P 级别，对于 1080P 或 4K 的超高清流式生成效果尚不明确。

（完）
