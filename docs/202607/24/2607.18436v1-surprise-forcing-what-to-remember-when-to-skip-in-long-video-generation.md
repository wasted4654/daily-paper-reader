---
title: "Surprise Forcing: What to Remember, When to Skip in Long Video Generation"
title_zh: Surprise Forcing：长视频生成中该记住什么，何时跳过
authors: "Shuwei Shi, Zhen Li, Muyao Niu, Chuanhao Li, Bo Zheng, Kaipeng Zhang, Yinqiang Zheng"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18436v1"
tags: ["query:ardm"]
score: 9.0
evidence: 用于视频生成的流式自回归扩散模型
tldr: 本研究针对长视频生成中自回归扩散模型的局限性，提出了Surprise Forcing框架。该框架解决了固定上下文导致的远端记忆丢失和固定去噪步数导致的计算资源浪费问题。通过引入基于“惊喜度”门控的记忆库来保留关键视觉证据，并利用惊喜度感知去噪动态调整计算量，在无需额外训练的情况下，显著提升了长视频的一致性和生成质量，同时保持了实时流式传输效率。
selection_source: fresh_fetch
motivation: 现有的流式视频生成模型在处理长序列时，存在远端关键记忆遗忘以及对不同难度的视频块统一分配计算资源导致的效率低下问题。
method: 提出一种无需训练的框架，利用惊喜度门控记忆库筛选并保留关键帧，并根据相邻帧的余弦距离动态跳过简单视频块的去噪步骤。
result: 在VBench、VBench-Long和VBench-2.0等多个基准测试中，该方法在提升长视频一致性和视觉质量的同时，成功维持了实时生成吞吐量。
conclusion: Surprise Forcing通过在线资源分配策略，证明了动态管理记忆和计算量是实现高质量、长时程视频生成的有效途径。
---

## 摘要
流式自回归扩散模型使分钟级视频合成变得实用，但其有限的上下文和固定的去噪调度在高度非平稳的序列中均匀地分配资源。滚动键值（KV）缓存会遗忘远处的视觉证据，即使这些证据仍然很重要，而每个生成的块无论其实际难度如何，都会接受相同次数的去噪处理。我们引入了 Surprise Forcing，这是一个无需训练的框架，将这两个局限性视为在线资源分配问题。惊奇门控记忆库（Surprise-Gated Memory Bank）使用值标记描述符总结被逐出的帧，利用互补的全局偏差和最近邻新颖性信号对其进行评估，并通过归一化评分空间中的反馈控制预算来调节准入。基于优先级的替换和相关性感知路由随后保持外部记忆的紧凑和有效。与此同时，惊奇感知去噪（Surprise-Aware Denoising）根据第一次去噪后的最大相邻帧余弦距离来估计块的难度，并使用局部百分位调度器跳过相对简单块的中间步骤。在 VBench、VBench-Long 和 VBench-2.0 上的实验表明，所提出的分配策略在保持实时流式吞吐量的同时，提高了长程一致性和视觉质量。

## Abstract
Streaming autoregressive diffusion makes minute-scale video synthesis practical, but its bounded context and fixed denoising schedule allocate resources uniformly across a highly non-stationary sequence. A rolling key-value cache forgets distant visual evidence even when that evidence remains important, while every generated chunk receives the same number of denoising passes irrespective of its actual difficulty. We introduce Surprise Forcing, a training-free framework that treats both limitations as online resource-allocation problems. A Surprise-Gated Memory Bank summarizes evicted frames with value-token descriptors, evaluates them using complementary global-deviation and nearest-neighbor novelty signals, and regulates admission through a feedback-controlled budget in normalized score space. Priority-based replacement and relevance-aware routing then keep the external memory compact and useful. In parallel, Surprise-Aware Denoising estimates chunk difficulty from the maximum adjacent-frame cosine distance after the first denoising pass and uses a local percentile scheduler to skip intermediate steps for comparatively easy chunks. Experiments on VBench, VBench-Long, and VBench-2.0 show that the proposed allocation strategy improves long-horizon consistency and visual quality while retaining real-time streaming throughput.

---

## 论文详细总结（自动生成）

这是一份关于论文《Surprise Forcing: What to Remember, When to Skip in Long Video Generation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：流式自回归扩散模型（Streaming ARDMs）虽然让分钟级长视频生成变得可行，但存在两个核心局限：
    1.  **远端记忆丢失**：采用滚动键值（KV）缓存机制，会导致模型遗忘较早但关键的视觉信息（如人物特征、背景细节）。
    2.  **计算资源浪费**：采用固定的去噪步数，无论视频块（Chunk）的生成难度如何（如静态背景 vs. 剧烈运动），都分配相同的计算量。
*   **核心问题**：如何在有限的计算和内存预算下，动态地分配资源，以实现长时程的一致性和高效生成？
*   **整体含义**：论文提出了 **Surprise Forcing** 框架，将长视频生成视为一个“在线资源分配问题”，通过“惊喜度（Surprise）”这一指标来决定哪些记忆该保留，以及哪些计算步骤可以跳过。

### 2. 方法论
该框架由两个核心模块组成，且均为**无需训练（Training-free）**的插件：

*   **惊喜门控记忆库（Surprise-Gated Memory Bank）**：
    *   **评估机制**：当帧从滚动缓存中被剔除时，系统利用“全局偏差（Global Deviation）”和“最近邻新颖性（Nearest-Neighbor Novelty）”两个信号评估其“惊喜度”。
    *   **准入控制**：通过一个反馈控制的预算机制，在归一化的评分空间中筛选高惊喜度的帧存入外部记忆库。
    *   **管理策略**：采用基于优先级的替换策略和相关性感知路由（Relevance-aware Routing），确保外部记忆库紧凑且对当前生成任务高度相关。
*   **惊喜感知去噪（Surprise-Aware Denoising）**：
    *   **难度估计**：在第一次去噪迭代后，计算相邻帧之间的最大余弦距离。距离越大，代表该块的生成难度（惊喜度）越高。
    *   **动态调度**：利用局部百分位调度器（Local Percentile Scheduler），对于低惊喜度（简单）的视频块，跳过中间的去噪步骤，从而节省算力。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **VBench**：评估视频生成的综合质量。
    *   **VBench-Long**：专门针对长视频的一致性和质量评估。
    *   **VBench-2.0**：最新的视频生成综合评测基准。
*   **对比方法**：与当前的流式视频生成 SOTA（先进）模型进行对比，包括 Streaming-T2V 等基于自回归扩散的方法。
*   **评估指标**：包括时间一致性（Temporal Consistency）、视觉质量（Visual Quality）以及生成吞吐量（Throughput）。

### 4. 资源与算力
*   **训练算力**：该方法是**无需训练（Training-free）**的，因此不需要额外的训练算力。
*   **推理算力**：论文强调了“实时流式吞吐量”。虽然提取的文本未详细列出具体的 GPU 型号和数量（通常此类研究使用 NVIDIA A100 或 H100），但其核心卖点在于通过跳过去噪步骤，在增加记忆库开销的同时，维持甚至优化了整体的推理速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从短视频到长视频（分钟级）的多个维度。
*   **消融实验**：对记忆库的大小、惊喜度阈值的设定、去噪跳过比例等关键超参数进行了消融研究。
*   **客观性**：通过在多个公认的第三方 Benchmark（VBench 系列）上进行测试，证明了该方法在不同场景下的泛化能力和有效性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **动态分配的有效性**：证明了在长视频生成中，均匀分配资源是低效的。通过识别“惊喜”时刻，可以更有效地利用有限的 KV 缓存空间。
*   **一致性提升**：引入外部记忆库显著增强了长视频的身份一致性（ID Consistency）和背景稳定性。
*   **效率平衡**：惊喜感知去噪能在不损失视觉质量的前提下，抵消维护外部记忆库带来的额外计算开销，实现实时生成。

### 7. 优点
*   **即插即用**：无需重新训练模型，可直接应用于现有的自回归扩散模型。
*   **双重优化**：同时解决了存储（记忆）和计算（去噪）两个维度的瓶颈。
*   **启发式创新**：引入“惊喜度”作为资源分配的度量标准，符合人类视觉注意力的直觉。

### 8. 不足与局限
*   **阈值敏感性**：惊喜度的判定依赖于超参数设置，不同风格的视频（如动作片 vs. 纪录片）可能需要不同的配置。
*   **误差累积风险**：虽然跳过去噪步骤能加速，但在极端复杂的动态场景下，过度跳过可能导致微小的伪影累积。
*   **依赖基础模型**：作为一种增强框架，其最终生成的上限仍受限于底层自回归扩散模型的能力。

（完）
