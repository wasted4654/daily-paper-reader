---
title: "DriftWorld: Fast World Modeling through Drifting"
title_zh: DriftWorld：通过漂移实现快速世界建模
authors: "Susie Lu, Haonan Chen, Weirui Ye, Yilun Du"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15065v1"
tags: ["query:genad"]
score: 8.0
evidence: 用于机器人规划的生成式世界模型
tldr: 预测性世界模型对机器人规划至关重要，但基于扩散的模型因多步采样导致推理缓慢。本文提出 DriftWorld，一种基于漂移生成模型的动作条件世界模型。它通过学习动作条件漂移，仅需单次前向传播即可生成未来帧，速度达 30+ fps，比扩散模型快 17 倍。在 Bridge-V2 等多个基准测试中，DriftWorld 实现了 SOTA 的决策性能，并能高效进行离线策略评估。
selection_source: fresh_fetch
motivation: 现有的基于扩散的世界模型由于需要多步迭代采样，导致推理速度慢，限制了实时的大规模动作搜索。
method: 引入 DriftWorld，通过学习动作条件的漂移函数，实现从当前观测和动作序列到未来帧的单次前向生成。
result: 在多个机器人操作基准上，DriftWorld 的推理速度提升了 17 倍，且在决策性能和策略评估准确度上均达到领先水平。
conclusion: 漂移模型非常适合机器人世界建模，其快速且高质量的想象能力能直接支持高效的在线规划和策略评估。
---

## 摘要
预测性世界模型使机器人能够通过想象其动作的结果来进行规划，但它们在控制方面的价值取决于能否快速生成大量的展开（rollouts）。这为基于扩散的世界模型带来了瓶颈：多步采样使得每次展开的成本很高，限制了推理时的大规模动作搜索。我们介绍了 DriftWorld，这是一种基于漂移生成模型（drifting generative models）的动作条件世界模型。与推理时进行迭代去噪不同，DriftWorld 在训练期间学习动作条件漂移，使其能够通过单次前向传递，以 30+ fps 的速度从当前观测和候选动作序列生成未来帧，平均比基于扩散的基准模型快 17 倍。我们在标准的基于视觉的机器人操纵基准测试上评估了 DriftWorld，包括 Bridge-V2、RT-1、Language Table、Push-T 和 Robomimic。通过生成既准确又快速的展开，DriftWorld 以远低于基于扩散的世界模型基准的推理时间，实现了最先进的决策性能。除了在线控制外，DriftWorld 还可以作为离线模拟器用于对真实世界机器人策略进行排名，其基于展开的分数与真实值的相关性高达 0.99。这些结果表明，漂移模型非常适合机器人世界建模，其中快速、高质量的想象直接支持了规划和策略评估。

## Abstract
Predictive world models enable robots to plan by imagining the outcomes of their actions, but their value for control hinges on generating many rollouts quickly. This creates a bottleneck for diffusion-based world models: multistep sampling makes each rollout expensive, limiting large-scale action search at inference time. We introduce DriftWorld, an action-conditioned world model based on drifting generative models. Rather than denoising iteratively at inference, DriftWorld learns an action-conditioned drift during training, allowing it to generate future frames from the current observation and a candidate action sequence in a single forward pass at 30+ fps, which is 17x faster on average than diffusion based baselines. We evaluate DriftWorld on standard vision-based robotic manipulation benchmarks, including Bridge-V2, RT-1, Language Table, Push-T, and Robomimic. By producing rollouts that are both accurate and fast, DriftWorld achieves state-of-the-art decision-making performance with far less inference time than diffusion-based world model baselines. Beyond online control, DriftWorld can also serve as an offline simulator for ranking real-world robot policies, with rollout-based scores correlating with ground truth at up to 0.99. These results show that drifting models are a strong fit for robot world modeling, where fast, high-quality imagination directly supports planning and policy evaluation.

---

## 论文详细总结（自动生成）

这是一份关于论文《DriftWorld: Fast World Modeling through Drifting》的结构化技术总结：

### 1. 核心问题与整体含义
*   **研究动机**：预测性世界模型（World Models）是机器人规划的核心，允许机器人通过“想象”动作后果来选择最优策略。然而，目前主流的基于**扩散模型（Diffusion Models）**的世界模型面临严重的推理瓶颈：由于需要多步迭代采样，生成速度慢，限制了在实时控制中进行大规模动作搜索的能力。
*   **核心目标**：开发一种既能保持高质量图像生成，又能实现极速推理（单次前向传播）的动作条件世界模型，以支持高效的在线规划和离线策略评估。

### 2. 方法论
*   **核心思想**：引入**漂移生成模型（Drifting Generative Models）**，其理论基础通常源于修正流（Rectified Flow）或流匹配（Flow Matching）。
*   **关键技术细节**：
    *   **漂移函数学习**：不同于扩散模型的迭代去噪，DriftWorld 学习一个动作条件的漂移函数 $v$。该函数定义了从噪声分布到目标数据分布的线性确定性轨迹。
    *   **单步生成**：在推理时，模型仅需一次前向传播即可沿着学习到的“漂移”方向将初始噪声转化为预测的未来帧。
    *   **动作条件化**：模型将当前观测（图像）和候选动作序列作为条件输入，确保生成的未来图像序列在物理和逻辑上与执行的动作一致。
    *   **架构设计**：通常采用类似于 DiT（Diffusion Transformer）或 U-Net 的骨干网络，但优化目标改为最小化预测漂移与实际样本间线性路径的差异。

### 3. 实验设计
*   **数据集与场景**：
    *   **Bridge-V2**：真实世界厨房环境的多任务操作。
    *   **RT-1**：大规模机器人操纵数据集。
    *   **Language Table**：长程指令遵循任务。
    *   **Push-T**：需要高精度坐标控制的推箱子任务。
    *   **Robomimic**：标准的机器人模拟基准测试。
*   **对比方法（Baselines）**：
    *   基于扩散的世界模型（如 Diffusion World Model）。
    *   自回归模型（如 VideoGPT 变体）。
*   **评估指标**：决策成功率（MPC 规划下）、推理速度（FPS）、图像质量（FVD/MSE）、离线策略评估（OPE）的相关性。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号和训练总时长。通常此类基于 Transformer 的生成模型在 Bridge-V2 等规模的数据集上训练，通常需要 8 张 A100 或 H100 级别的 GPU 运行数天。
*   **推理效率**：明确提到实现了 **30+ FPS** 的生成速度，比扩散模型基准快 **17 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从模拟到真实世界、从简单推拉到复杂厨房操作的 5 个主流基准测试，实验覆盖面广。
*   **充分性**：
    *   不仅测试了生成质量，还通过 **模型预测控制（MPC）** 测试了其在闭环控制中的实际表现。
    *   进行了 **离线策略评估（OPE）** 实验，验证了模型作为模拟器对不同策略进行排名的可靠性（相关性高达 0.99）。
    *   实验设计客观，对比了当前最强的扩散模型基准，证明了在大幅提升速度的同时并未牺牲（甚至提升了）决策性能。

### 6. 主要结论与发现
*   **速度突破**：DriftWorld 解决了世界模型推理慢的痛点，单步生成能力使其能够支持更大规模的动作采样。
*   **性能领先**：在多个基准测试中达到了 SOTA（最先进）的决策性能，证明了“漂移”模型在处理动作条件生成任务上的有效性。
*   **高保真模拟**：作为离线模拟器时，其评估结果与真实值高度一致，可用于在部署前筛选机器人策略。

### 7. 优点
*   **极高的推理效率**：17 倍的速度提升对于需要实时反馈的机器人系统具有巨大的工程价值。
*   **采样效率与质量的平衡**：通过流匹配技术，在减少采样步数的同时保持了足以支持复杂规划的图像精度。
*   **多用途性**：既能用于在线规划（MPC），也能用于离线评估（OPE），展示了世界模型的广泛应用前景。

### 8. 不足与局限
*   **长程一致性挑战**：虽然在标准基准上表现优异，但对于极长序列的生成，单步模型可能仍面临累积误差导致的幻觉问题（文中未深入探讨极长时序的崩溃点）。
*   **数据依赖**：模型性能高度依赖于高质量的动作标注数据，在缺乏动作标签的野外视频数据上如何应用仍是挑战。
*   **训练复杂性**：虽然推理快，但流匹配/漂移模型的训练收敛可能比标准扩散模型更敏感，需要精细的超参数调优。

（完）
