---
title: "Self Gradient Forcing: Native Long Video Extrapolation"
title_zh: 自梯度强制：原生长视频外推
authors: "Junhao Zhuang, Shiyi Zhang, Yuxuan Bian, Yaowei Li, Yawen Luo, Yijun Liu, Weiyang Jin, Songchun Zhang, Xianglong He, Xuying Zhang, Haoran Li, Haoyang Huang, Zeyue Xue, Nan Duan"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20368v1"
tags: ["query:ardm"]
score: 9.0
evidence: 自回归视频扩散方法与Self-Forcing训练
tldr: 针对自回归视频扩散模型中历史上下文梯度缺失导致的生成偏差问题，本文提出 Self Gradient Forcing (SGF) 训练策略。通过两阶段训练（无梯度生成记录与并行梯度重建），SGF 恢复了未来帧对历史 KV 缓存编码的监督信号。该方法显著提升了长视频外推的身份一致性与时空稳定性，仅需 5 秒训练窗口即可实现数分钟的高质量视频生成。
selection_source: fresh_fetch
motivation: 现有的自回归视频生成方法将历史生成结果视为冻结状态，导致未来帧的损失无法指导历史上下文的高效编码。
method: 提出两阶段训练策略，先通过无梯度推理记录自生成上下文，再通过并行重建恢复未来帧对历史 KV 缓存的梯度监督。
result: SGF 在长视频外推任务中表现优异，在主体身份一致性、背景布局和时间稳定性方面均优于 Self Forcing。
conclusion: SGF 证明了通过恢复历史上下文的梯度监督，可以使模型在极短的训练窗口下具备极强的长视频外推能力。
---

## 摘要
最近的自回归视频扩散方法越来越多地基于自强制（Self Forcing），即学生模型是在其自身展开（rollout）生成的历史数据上进行训练，而非真实视频上下文。这减少了曝光偏差，但历史键值（KV）缓存仍仅作为冻结的展开状态被后续帧使用。因此，未来的损失函数无法监督早期生成的潜变量（latents）应如何被写入更有效的键和值，以供后续视频潜变量生成使用。我们将此称为历史上下文-梯度间隙。我们提出了自梯度强制（Self Gradient Forcing, SGF），这是一种两阶段训练策略，在无需通过完整串行展开进行反向传播的情况下，恢复了这一缺失的监督信号。第一阶段执行与推理一致的无梯度自回归展开，并在采样的去噪退出步骤中，记录自生成的上下文以及输入模型的噪声潜变量。第二阶段针对记录的退出步骤执行并行上下文-梯度重建。生成的上下文被用作停止梯度的干净潜变量输入，同时模型重新计算上下文 KV 表示以及未来到上下文的因果注意力。因此，SGF 在原生自回归训练目标中提供了缺失的记忆写入监督，利用未来视频潜变量的损失来训练模型将上下文编码为更有效的因果记忆。在不同初始化条件下的广泛长程逐帧和逐块实验中，SGF 实现了比自强制更强的原生长视频外推能力，特别是在主体身份、背景/布局一致性以及时间稳定性方面。值得注意的是，仅使用 5 秒的训练窗口，SGF 即可外推至持续数分钟的视频。代码和模型将发布，以推动自回归视频生成的研究。

## Abstract
Recent autoregressive video diffusion methods are increasingly built upon Self Forcing, where the student is trained on histories produced by its own rollout rather than ground-truth video contexts. This reduces exposure bias, but the historical key-value cache is still used by future frames only as frozen rollout state. As a result, future losses cannot supervise how earlier generated latents should be written into more useful keys and values for later video-latent generation. We call this the historical context-gradient gap. We propose Self Gradient Forcing (SGF), a two-pass training strategy that restores this missing supervision signal without backpropagating through the full serial rollout. Pass 1 performs a no-gradient autoregressive rollout matching inference and, at a sampled denoising exit step, records both the self-generated context and the noisy latents fed to the model. Pass 2 performs parallel context-gradient reconstruction for the recorded exit step. The generated context is used as stop-gradient clean-latent input, while the model recomputes the context KV representations and future-to-context causal attention. Thus, SGF provides the missing memory-writing supervision within the native autoregressive training objective, using losses on future video latents to train the model to encode context into more effective causal memory. Across extensive long-horizon frame-wise and chunk-wise experiments under different initializations, SGF achieves stronger native long-video extrapolation than Self Forcing, especially in subject identity, background/layout consistency, and temporal stability. Remarkably, using only a 5-second training window, SGF can extrapolate to videos lasting several minutes. Code and models will be released to advance research on autoregressive video generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self Gradient Forcing: Native Long Video Extrapolation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）视频扩散模型在生成长视频时，通常面临“曝光偏差”和“时空不一致”的问题。
*   **研究背景**：现有的改进方案如“自强制（Self Forcing, SF）”让模型在自身生成的历史数据上训练，而非仅依赖真实数据（Teacher Forcing）。然而，SF 存在一个关键缺陷——**历史上下文-梯度间隙（Historical Context-Gradient Gap）**。
*   **动机**：在 SF 训练中，历史帧的键值（KV）缓存被视为“冻结”的状态。这意味着未来帧的损失函数（Loss）无法指导模型如何将历史信息编码为更有效的记忆。论文旨在通过恢复这种梯度监督，提升模型对长程上下文的编码能力。

### 2. 论文提出的方法论：Self Gradient Forcing (SGF)
SGF 提出了一种**两阶段（Two-pass）训练策略**，在不进行全序列反向传播（BPTT）的情况下恢复梯度流：
*   **第一阶段：无梯度自回归展开（No-gradient Rollout）**
    *   模拟推理过程，模型基于初始帧自回归生成后续帧。
    *   在随机采样的去噪步骤中，记录模型自生成的上下文（Context）以及输入到模型中的噪声潜变量。
*   **第二阶段：并行上下文-梯度重建（Parallel Reconstruction）**
    *   将第一阶段记录的自生成上下文作为输入，但此时**不冻结**其编码过程。
    *   模型重新计算这些上下文的 KV 表示，并计算未来帧对这些上下文的因果注意力。
    *   **关键点**：未来帧的生成损失现在可以通过注意力机制回传到编码上下文的参数上。这迫使模型学习“如何为了未来更好地存储过去”。

### 3. 实验设计
*   **实验场景**：涵盖了逐帧（Frame-wise）和逐块（Chunk-wise）的长视频外推任务。
*   **对比基准（Baselines）**：
    *   **Teacher Forcing (TF)**：传统训练方式，仅使用真实数据。
    *   **Self Forcing (SF)**：在自生成历史数据上训练，但梯度在历史处截断。
*   **评估指标**：侧重于主体身份一致性（Subject Identity）、背景/布局一致性（Background/Layout Consistency）以及时间稳定性（Temporal Stability）。

### 4. 资源与算力
*   **训练窗口**：论文强调 SGF 非常高效，仅需 **5 秒** 的短视频训练窗口。
*   **算力细节**：摘要和元数据中未明确提及具体的 GPU 型号（如 H100/A100）或具体数量。但从其“两阶段”描述来看，其显存开销高于 SF，但远低于完整序列的 BPTT。

### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的实验，包括不同的模型初始化条件、逐帧与逐块生成的对比。
*   **消融实验**：验证了梯度恢复对记忆编码的贡献。
*   **充分性评价**：实验设计较为全面，通过将 5 秒训练的模型外推至数分钟视频，极具说服力地证明了该方法在处理“训练-测试长度不一致”问题上的优越性。

### 6. 论文的主要结论与发现
*   **梯度监督至关重要**：仅仅让模型看到自生成的历史是不够的，必须让未来的损失能够“告诉”模型如何优化历史的 KV 编码。
*   **极强的外推能力**：SGF 显著提升了长视频的稳定性。即使训练数据很短，模型也能维持数分钟的视频逻辑连贯，不会出现身份坍塌或背景漂移。
*   **原生支持**：该方法不需要在推理时增加复杂的插件，而是通过改进训练目标使模型获得原生的长视频生成能力。

### 7. 优点
*   **理论深刻**：精准捕捉到了 AR 扩散模型中“KV 缓存冻结”导致的监督缺失问题。
*   **效率平衡**：通过两阶段设计，巧妙避开了长序列反向传播带来的显存爆炸问题，同时保留了关键的梯度路径。
*   **效果显著**：在保持身份一致性和背景稳定性方面表现优异，解决了长视频生成的痛点。

### 8. 不足与局限
*   **训练成本增加**：相比于 SF，SGF 需要额外的推理步骤（第一阶段）和重新计算步骤（第二阶段），训练耗时会增加。
*   **自回归固有局限**：虽然外推能力增强，但作为自回归模型，生成极长视频时的推理速度仍然受限（需逐帧/块生成）。
*   **数据依赖**：虽然训练窗口短，但模型性能仍可能受限于初始预训练模型的质量。

（完）
