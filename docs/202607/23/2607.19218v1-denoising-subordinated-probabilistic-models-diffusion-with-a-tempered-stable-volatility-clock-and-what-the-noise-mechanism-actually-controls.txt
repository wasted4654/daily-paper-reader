Title: Denoising Subordinated Probabilistic Models: Diffusion with a Tempered-Stable Volatility Clock, and What the Noise Mechanism Actually Controls

URL Source: https://arxiv.org/pdf/2607.19218v1

Published Time: Wed, 22 Jul 2026 01:12:35 GMT

Number of Pages: 14

Markdown Content:
# Denoising Subordinated Probabilistic Models: Diffusion with a Tempered-Stable Volatility Clock, and What the Noise Mechanism Actually Controls 

Junchi Shen, Helin Zhao ∗

July 2026 

Abstract 

Heavy-tailed extensions of denoising diffusion models replace the Gaussian noise by a Gaus-sian variance mixture εi = √Aizi: denoising L´ evy probabilistic models (DLPM) take the mixing variables Ai i.i.d. across coordinates, Student-t EDM takes a single A shared by all coordinates. Neither extreme has dynamics , yet temporal dependence of the noise amplitude— volatility clustering—is the defining stylized fact of the financial data these models target. We introduce the Denoising Subordinated Probabilistic Model (DSPM), whose mixing vector is a stationary AR(1) chain driven by tempered-stable subordinator increments—the discrete-time Barndorff-Nielsen–Shephard stochastic volatility process—running along the data axis. Conditionally on the chain, the entire DDPM machinery survives verbatim, and we prove closed-form identities linking the chain parameters to the excess kurtosis and the squared-noise autocorrelation, yielding an exactly identified, analytically invertible calibration with a falsifiable feasibility bound ρr2 (1) < K/ (3( K + 2)). DDPM, DLPM-type stable noise, and shared-scale Student-t-type noise are boundary cases of one memory parameter. We then prove a result that delimits the entire design space: if the denoiser is conditioned on A (as sufficiency demands), the mixing law is a nuisance —in the exact-denoiser limit the generated distribution is invariant to it, and interventions on A have no effect. Controlled experiments (identical denoiser, schedule, budget; only the law of A or the conditioning bit varies) confirm both halves: conditioned models match the data’s clustering regardless of the mixing law, and a designed ×8 volatility shock moves the generated amplitude envelope by less than 13% against a naive prediction of +183%; blind models transmit the noise structure exactly as calibrated—lag-one squared-return autocorrelation 0 .151 against the calibrated 0 .161 with the predicted geometric decay, while the i.i.d. variant’s clustering collapses to 0 .008. The mixing law controls what structure the noise offers ; the conditioning bit controls whether it transmits .Finally we close the loop (v2): coupling the chain to the data through a variational volatility encoder (with a log-normal volatility prior, for closed-form KL) restores control—provided the objective retains the stochastic-volatility likelihood whose log-determinant term the simplified denoising loss discards; naive β-weighted couplings collapse or leak, and both failure modes are documented. With it, the same ×8 intervention scales the envelope by 3 .07 (naive scal-ing: 2 .83), the encoder tracks the true latent volatility of held-out paths (per-path correlation 0.57; 0 .76 pooled), and the learned prior memory moves from its moment-calibrated initial-ization toward the true persistence. The cost is substantial and stated plainly: the coupled model’s unconditional kurtosis falls below even the Gaussian baseline—fidelity and control are demonstrated separately, not yet jointly. 

> ∗

Working draft. All derivations and every number in this paper are reproduced by the accompanying scripts 

01 sampler check.py –07 blind.py (fixed seeds, single laptop). 

1arXiv:2607.19218v1 [q-fin.MF] 21 Jul 2026 1 Introduction 

Score-based and denoising diffusion models (Sohl-Dickstein et al., 2015; Ho et al., 2020; Song et al., 2021) corrupt data with Gaussian noise and learn to invert the corruption. For financial time series the Gaussian choice discards the domain’s structure. Fifty years of empirical finance have fixed a short list of stylized facts of asset returns (Cont, 2001): (i) heavy-tailed marginals with tail index ζ ≈ 3 (Gopikrishnan et al., 1999); (ii) negligible linear autocorrelation; (iii) strong, slowly decaying autocorrelation of squared returns— volatility clustering ; (iv) aggregational Gaussianity. Recent work addresses fact (i) inside the diffusion framework by replacing the Gaussian. Through the classical variance-mixture representation ε = √AZ , the published proposals sit at two extremes of one axis. Denoising L´ evy probabilistic models (DLPM, Shariatian et al., 2024) and the L´ evy-Itˆ o model (Yoon et al., 2023) use α-stable noise: mixing variables i.i.d. across coordinates, 

no memory . Student-t EDM (Pandey et al., 2025) uses multivariate Student-t noise: one inverse-gamma mixing variable per sample, infinite memory . Neither can represent fact (iii) in its noise mechanism, and financial econometrics identified the missing object decades ago: the mixing vari-able is the flow of economic time (Clark, 1973), it is persistent , and its canonical continuous-time model is an Ornstein–Uhlenbeck process driven by a (tempered) L´ evy subordinator—Barndorff-Nielsen–Shephard (BNS) stochastic volatility (Barndorff-Nielsen and Shephard, 2001), embedded in the time-changed L´ evy consensus of Carr et al. (2003), with exponential tempering (Koponen, 1995; Carr et al., 2002) curing the infinite variance of pure stable laws (Mantegna and Stanley, 1994). This paper makes two contributions that pull in deliberately opposite directions, and we believe the tension is the point. 

Constructive contribution. We define the DSPM (Section 3): a DDPM whose per-coordinate noise variance is a stationary tempered-stable AR(1) chain along the data axis. Conditionally on the chain the Gaussian posterior, the closed-form ELBO and ancestral sampling hold verbatim (Propositions 1, 2)—the DDPM derivations never use independence of the mixing variables. The excess kurtosis and squared-noise autocorrelation of the induced noise are closed-form in the chain parameters (Proposition 3); the calibration map is exactly identified with an analytic inverse and a falsifiable feasibility bound (Corollary 1); and DDPM, DLPM-type and t-EDM-type noise are boundary cases of the single memory parameter φ (Proposition 4). The noise prior coincides with the “diffusing diffusivity” models of statistical physics (Section 5). 

Delimiting contribution. We then prove (Proposition 5) that when the denoiser is conditioned on A—the statistically natural choice, since A is known at both training and sampling time—the mixing law is a nuisance parameter : in the exact-denoiser limit the generated distribution does not depend on it at all, and interventions on A provably do nothing. The experiments (Section 4) confirm both the theorem and its boundary with unusual sharpness: conditioned models reproduce clustering equally well whatever the law of A (even pure DDPM), a ×8 engineered volatility shock leaves the generated envelope essentially unchanged, while blind models (denoiser not shown 

A) transmit the mechanism’s structure quantitatively—the calibrated lag-one autocorrelation appears in the samples to within 0 .01, with the geometric decay fingerprint of Proposition 3, and vanishes for the i.i.d. variant. The practical moral for the growing heavy-tailed-diffusion literature (Anonymous, 2026): the mixing law controls what the noise offers; the conditioning choice controls whether it transmits ; claimed benefits of heavy-tailed noise in the conditioned regime must be inductive-bias effects, not mechanism effects—and in our measurements, across 2three training seeds, they are at most marginal even for tail fidelity and absent for dependence structure. 

Coupling contribution (v2). The invariance theorem names its own escape route: break the independence of x0 and A. We do so by variational inference—an encoder posits each path’s own latent volatility, qψ(A | x0), trained jointly with the denoiser (Section 3.5). The exercise surfaces a second delimiting fact: the simplified denoising loss omits the log-determinant of the noise covariance—exactly the term through which a Gaussian likelihood fits a variance—and the whitened residual it retains is nearly scale-free in A, leaving the encoder only weak indirect gradi-ent pathways; empirically these do not suffice (both failure modes are documented in Remark 2). Restoring the term (equivalently, adding the exact variational objective of the classical stochastic-volatility state-space model) turns the encoder into a volatility filter and unlocks what v1 provably lacks: interventions on A now steer generation quantitatively, and the prior’s memory parameter, learned by empirical Bayes, moves in the direction of the ground-truth persistence that moment calibration underestimates (Section 4.6). 

# 2 Background 

DDPM. For x0 ∈ Rd and a schedule β1, . . . , β T ∈ (0 , 1), DDPM (Ho et al., 2020) sets q(xt |

xt−1) = N (√1 − βt xt−1, β tI), giving q(xt | x0) = N (√¯αtx0, (1 − ¯αt)I) with αt = 1 − βt, ¯ αt =Q 

> s≤t

αs; training minimizes the reweighted ELBO in the ε-parametrization. 

Variance mixtures. If A > 0 is random and Z ∼ N (0 , 1) independent, ε = √AZ has 

Eeiuε = Ee−u2A/ 2: one-sided α-stable A gives symmetric 2 α-stable ε (DLPM); inverse-gamma 

A gives Student-t (t-EDM); lognormal A gives Clark’s (1973) model. The mixing variable is the instantaneous variance; its temporal dependence is volatility clustering. 

Tempered stable subordinators. An increment η ∼ TS( α, θ, δ ), 0 < α < 1, θ > 0, δ > 0, is the exponential tilting of the one-sided stable law: 

E e−uη = exp  −δ[( θ + u)α − θα], κn(η) = δ α Γ( n−α)Γ(1 −α) θα−n; (1) in particular κ1 = δαθ α−1, κ2 = δα (1 − α)θα−2. All moments are finite for θ > 0; θ → 0 recovers the stable law Ee−uη = e−δu α

.

# 3 The model 

3.1 Definition 

Definition 1 (DSPM) . Fix 0 < α < 1, θ > 0, 0 ≤ φ < 1 and a DDPM schedule {βt}. The 

variance chain A = ( A1, . . . , A d) is the stationary solution of 

Ai = φA i−1 + ηi, ηi iid 

∼ TS( α, θ, δ ), δ = (1 − φ)θ1−α

α (⇒ EAi = 1) , (2) 

and, writing DA = diag( A), the forward process conditionally on A is 

q(xt | xt−1, A ) = N p 1 − βt xt−1, β tDA

, q(xt | x0, A ) = N √¯αtx0, (1 − ¯αt)DA

. (3) 3The index i is calendar time of the generated path , not the diffusion step: the terminal prior 

xT ≈ D1/2 

> A

z is a discrete BNS stochastic-volatility path. Recursion (2) is the exact skeleton of the BNS variance process dV s = −λV sds + dL λs with φ = e−λ∆; fast exact simulation is standard (Sabino and Cufaro Petroni, 2022). Generation is a latent-variable model: draw A from (2), then run the learned reverse diffusion given A. In training, x0 (data) and A are drawn independently —a fact that Proposition 5 will make consequential. 

3.2 Conditional Gaussian structure 

Proposition 1 (Posterior) . Conditionally on A, for t ≥ 2,

q(xt−1 | xt, x 0, A ) = N  ˜μt(xt, x 0), ˜βtDA

, ˜βt = 1 − ¯αt−1

1 − ¯αt

βt, (4) 

with the unchanged DDPM mean ˜μt =   

> √¯αt−1βt
> 1−¯αt

x0 +   

> √αt(1 −¯αt−1)1−¯αt

xt.Proof. Every covariance in (3) is a multiple of the same diagonal DA, so the computation factorizes over coordinates. In coordinate i with variance factor a = Ai: posterior precision αt 

> βta

+ 1(1 − ¯αt−1)a =   

> 1−¯αt
> βt(1 −¯αt−1)a

, i.e. variance ˜βta; in the mean, a multiplies prior and likelihood variances alike and cancels. Only conditioning on the whole vector A is used—its dependence structure across i is irrelevant. 

Proposition 2 (ELBO) . With pϕ(xt−1 | xt, A ) = N (μϕ, ˜βtDA), μϕ = 1√αt

 xt− βt√1− ¯αt D1/2 

> A

ˆzϕ(xt, t, A ),the per-step KL of the variational bound is 

KL  q(xt−1 | xt, x 0, A ) ∥ pϕ

 = β2

> t

2 ˜βtαt(1 − ¯αt) z − ˆzϕ 2, (5) 

where z is the whitened noise in (3) : the DA-Mahalanobis norm collapses to the Euclidean norm of the whitened residual, and E∥z − ˆzϕ∥2 is the standard reweighted ELBO, exactly as in DDPM. Proof. Equal covariances give KL = ∥˜μt−μϕ∥2

> D−1
> A

/(2 ˜βt); substituting x0 = ( xt−√1 − ¯αtD1/2 

> A

z)/√¯αt

leaves ˜ μt − μϕ = βt√αt(1 − ¯αt) D1/2 

> A

(ˆ zϕ − z), and D1/2 

> A

cancels against D−1 

> A

.Training and sampling are therefore verbatim DDPM with D1/2 

> A

scalings; we condition the denoiser on A and whiten its input, ˆ zϕ

 xt/p¯αt + (1 − ¯αt)A, √A, t  (coordinate-wise operations). What does not survive is the DLPM two-draw training acceleration, which needs additive closure of stable laws plus independence; with a correlated chain one simulates the full O(d) scalar recursion (2) per example—cheap in practice ( ≈ 10% of wall-clock per training step at our scale: 111s vs. 100s per 6 ,000 steps). 

3.3 Closed-form stylized facts, exact calibration 

Let ri = √Aizi, z ∼ N (0 , I ) ⊥ A: the terminal noise, i.e. what the mechanism offers before any learning. 4Proposition 3 (Moment identities) . Under Definition 1, with v := Var( Ai) = 1 − αθ(1 + φ) : (i) 

Cov( Ai, A i+h) = vφ h; (ii) Eri = 0 , Er2 

> i

= 1 , Cov( ri, r i+h) = 0 , and excess kurtosis 

K = 3 v = 3(1 − α)

θ(1 + φ) ; (6) 

(iii) the squared-noise autocorrelation is 

ρr2 (h) = v φ h

3v + 2 , h ≥ 1. (7) 

Proof. (i) Ai = P 

> k≥0

φkηi−k gives EA = κ1(η)/(1 − φ) = 1 by (2), Var A = κ2(η)/(1 − φ2) = v

after substituting (1), and the AR(1) autocovariance vφ h. (ii) Er4 = EA2Ez4 = 3( v + 1), hence (6); oddness in z kills linear autocorrelation. (iii) Cov( r2 

> i

, r 2

> i+h

) = Cov( Ai, A i+h) = vφ h and Var( r2 

> i

) = 3( v + 1) − 1 = 3 v + 2. 

Corollary 1 (Exact calibration; feasibility) . For targets K∗ > 0, ρ∗ 

> 1

∈ (0 , 1) and fixed α, the map 

(φ, θ ) 7 → (K, ρ 1) is exactly identified with analytic inverse 

φ = 3ρ∗

> 1

(K∗ + 2) 

K∗ , θ = 3(1 − α)

K∗(1 + φ) , (8) 

feasible iff ρ∗ 

> 1

< K ∗/(3( K∗ + 2)) . The bound is a falsifiable prediction of the model class ( ρ1 < 1/3

always); daily equity indices ( K ≳ 8, ρ1 ≈ 0.2) satisfy it. 

The activity index α is left free by (8): it shapes the variance marginal beyond its first two moments (we fix α = 0 .7). For θ > 0 all moments of r are finite— semi-heavy tails, the CGMY class (Carr et al., 2002); as θ ↓ 0 the noise approaches symmetric 2 α-stable, so small calibrated 

θ yields apparent power-law behaviour over the observable range before tempering takes over, matching the empirical situation (Mantegna and Stanley, 1994; Gopikrishnan et al., 1999). 

Proposition 4 (Boundary cases) . (i) θ → ∞ : v → 0, A → 1 in L2—DDPM. (ii) φ = 0 ,

θ → 0 at fixed δ: ri i.i.d. symmetric 2α-stable—the DLPM/LIM noise family. (iii) φ → 1 with increments rescaled to hold (EA, v ) fixed: Cov( Ai, A j ) → v for all i, j —one frozen scale per path, 

ρr2 (h) → v/ (3 v + 2) lag-independent: the correlation structure of t-EDM (whose inverse-gamma marginal yields Student-t). Proof. (i) is (6); (ii) is (1) with Eeiur = Ee−u2A/ 2 = e−δ(u2/2) α

; (iii) is (7) with φh → 1. A single parameter φ ∈ [0 , 1] thus interpolates between the two published extremes, and (8) pins it to a measurable quantity—the decay rate of volatility clustering. 

3.4 The nuisance-invariance theorem 

The results above describe what the noise offers . The next result determines when the generated distribution can accept the offer. Recall that in training x0 ∼ q and A are independent. 

Proposition 5 (Nuisance invariance) . Suppose the reverse chain uses the exact conditional ker-nels: it is initialized at q(xT | A) and transitions by q(xt−1 | xt, A ) = R q(xt−1 | xt, x 0, A ) q(dx 0 |

xt, A ). Then for almost every A the chain terminates at 

p(x0 | A) = q(x0 | A) = q(x0). (9) 5Consequently, in the exact-denoiser limit: (i) interventions on A (scenario designs, distributional changes at sampling time within the support) leave the generated law unchanged; (ii) the law of A

does not affect the generated distribution at all. The mixing law acts only through what the limit does not cover: finite-T prior mismatch, the approximation and estimation error of the learned denoiser, and the optimization path. Proof. Conditionally on A, ( x0, . . . , x T ) is a Markov chain with a well-defined time reversal; composing the exact reverse kernels from the exact terminal marginal reproduces the joint law of the forward chain given A, hence the marginal q(x0 | A). Independence of x0 and A gives 

q(x0 | A) = q(x0). (i) and (ii) follow because the terminal law does not depend on the conditioning value or on the law of A.

Remark 1. The proposition formalizes, and sharpens to an exact statement, the recent skepticism about heavy-tailed noise in diffusion models (Anonymous, 2026): in the conditioned regime there is no mechanism route by which the mixing law can shape the learned distribution; any measured benefit is an inductive-bias effect (preconditioning of the loss, tail coverage of the terminal prior at finite T ). Conversely, a model whose denoiser is not shown A cannot whiten it away, and the noise structure transmits. Both predictions are tested in Section 4. 

3.5 Coupling the chain to the data (v2) 

Proposition 5 rests on x0 ⊥ A. The coupled model replaces the independent draw of A by amortized inference. An encoder maps x0 to a Gaussian mean-field posterior over a coarse log-volatility chain μc ∈ Rdc (dc = 16 for d = 64), which is linearly interpolated to full resolution and exponentiated, A = exp(up( μc)). The coarse bottleneck is not a convenience: it structurally prevents the latent from copying per-coordinate detail of x0 (sign and fine amplitude), reserving it for what it is meant to carry—a smooth volatility profile. The prior on μc is the station-ary Gaussian AR(1) with memory a and stationary variance s2 (log-normal OU volatility, i.e. Clark, 1973), mean fixed to −s2/2 so EA = 1; ( a, s 2) are learned (empirical Bayes), initialized at the closed-form calibration (8) translated to the log-normal family: under the log-normal chain, Var( A) = es2

− 1 = K/ 3 and ρr2 (1) = ( es2af − 1) /(K + 2), giving s2 = log(1 + K/ 3), fine-scale memory af = log  1 + ρ1(K + 2) /s 2, and coarse memory a = a d/d c 

> f

. We flag the substitution plainly: v2 as implemented runs on Clark’s log-normal clock, not on the tempered-stable chain of Definition 1 —the choice buys the closed-form Gaussian KL in the objective below, and it means the tempered-stable machinery of Sections 3.1–3.3 enters v2 only through the calibration logic and the initialization; carrying the TS prior itself into v2 requires Monte-Carlo KL and is future work. The constructive contribution (TS family, calibration, limits) and the coupling contribution therefore currently live on two adjacent members of the same variance-mixture class, not on one model. The objective is 

L = E z − ˆzϕ 2

| {z }

> denoising

+ Eqψ

h 12d

P

> i

 x20,i /A i + log Ai

i

+ 1 

> d

KL  qψ(μc | x0) pa,s (μc)| {z }

> SV-ELBO

, (10) the second group being the exact variational objective of the classical stochastic-volatility state-space model x0,i = √Ai εi with closed-form Gaussian KL. 

Remark 2 (Why the simplified loss cannot train the encoder) . The ε-parametrized denoising term in (10) is the Euclidean norm of the whitened residual (Proposition 2): whitening cancels the noise covariance, so the term is nearly scale-free in A and carries almost no gradient toward matching 

6A to the path’s realized variance. In the exact ELBO that information lives in the log-determinant 

> 12

P 

> i

log Ai of the Gaussian likelihood—the term every simplified diffusion loss drops. We verified both failure modes this analysis predicts: training (10) without the SV term at KL weight β = 1 

collapses the posterior ( KL → 0.007 nats per latent; interventions remain ineffective), while 

β = 0 .1 makes the latent copy x0 (reconstruction MSE drops to 0.02 ; unconditional generation collapses to excess kurtosis 0.18 ). With the SV term, no β-tuning is needed. 

Generation is unchanged (sample the coarse prior chain, interpolate, run the reverse diffusion given A); but because training pairs are now coupled, pθ(x0 | A) genuinely depends on A, and designed variance paths become control inputs rather than nuisance. 

# 4 Experiments 

Setup common to all runs: paths of length d = 64; T = 100, linear β ∈ [10 −3, 0.2]; denoiser = six dilated Conv1d residual blocks with FiLM time conditioning ( ≈ 0.5M parameters, receptive field 69 ≥ d); 6 ,000 Adam steps, batch 256; Apple-Silicon laptop, minutes per model. Every headline comparison (Tables 2 and 4) reports mean ± sd over three training seeds with the dataset held fixed; the sampler and identity checks (Sections 4.1–4.2) have their own replication protocols, and the blind and receptive-field ablations are single-run (flagged where cited). Ground truth for Sections 4.3–4.5: 28 ,000 paths (20 ,000 train, 8 ,000 test) of GARCH(1,1)-t (ω = 0 .05 , a = 0 .10 , b =0.85 , ν = 6), with pooled test statistics: excess kurtosis 6 .31, Hill index 3 .69 (top 2 .5% of |r|), 

ρr2 (1) = 0 .159, ρr2 (5) = 0 .110. Training-set estimates ( ˆK, ˆρ1) = (6 .27 , 0.161) calibrate the DSPM by (8): φ = 0 .638, θ = 0 .0876; the i.i.d.-TS variant is kurtosis-matched ( φ = 0, θ = 0 .144), the shared-scale variant is a kurtosis-matched inverse-gamma (equivalent Student-t with ν ≈ 4.96). 

4.1 Sampler 

We sample TS( α, θ, δ ) by Kanter’s one-sided stable representation (Kanter, 1975; Devroye, 1986) with exponential-tilting rejection, splitting into n = ⌈δθ α⌉ pieces to keep acceptance ≥ e−1

(Baeumer and Meerschaert, 2010). With 2 × 10 6 draws, the empirical Laplace transform matches (1) to ≤ 2.1 × 10 −4 uniformly on u ∈ [0 .05 , 4] across untempered ( α ∈ { 0.5, 0.7}) and tempered cases including the splitting regime δθ α ≈ 4.9 ( n = 5); sample mean and variance match κ1, κ 2 to three digits (Figure 1). 

4.2 Identities and calibration recovery 

Table 1 verifies Proposition 3 on a ( θ, φ ) grid; Figure 2 shows the exact ACF curve, the recovery ex-periment, and one noise path. At targets ( K∗, ρ ∗

> 1

) = (8 , 0.20), (8) gives ( φ∗, θ ∗) = (0 .750 , 0.0643); re-estimating from finite panels (2 ,000 paths, d = 64) and re-inverting yields ˆ φ = 0 .756 ± 0.085, ˆθ = 0 .066 ± 0.007 over 200 replications: unbiased, exactly identified. 

Remark 3 (Estimator bias under heavy tails) . Averaging per-path short-window ACF estimates underestimates ρr2 by up to a factor 3 at these parameters—the downward bias of sample auto-correlations of heavy-tailed squares (Mikosch and St˘ aric˘ a, 2000). All ACFs here use the pooled (global-mean, global-variance) estimator, applied identically to data and models; with it, Monte Carlo matches (7) to the third digit. Calibrations of (8) to per-asset estimates should pool or bias-correct first. 

70 1 2 3 4              

> u
> 0.6
> 0.7
> 0.8
> 0.9
> 1.0
> E[e u ]
> TS Laplace transform (=0.7, =1.0, =0.3)
> theory
> empirical (2M draws)
> 10 110 010 110 2
> x
> 10 6
> 10 5
> 10 4
> 10 3
> 10 2
> 10 1
> 10 0
> 10 1
> density
> exponential tempering of the tail
> stable (untempered)
> tempered stable

Figure 1: Sampler validation. Left: empirical vs. exact Laplace transform, TS(0 .7, 1.0, 0.3). Right: tempering suppresses the far tail while preserving the stable body. Table 1: Proposition 3 vs. Monte Carlo (8 ,000 paths, d = 64, α = 0 .7, pooled estimators). 

θ φ K th. K MC ρ1 th. ρ1 MC 0.06 0.75 8.57 8.14 0.203 0.202 0.15 0.50 4.00 4.38 0.111 0.110 0.40 0.90 1.18 1.17 0.112 0.113 0.80 0.30 0.87 0.84 0.030 0.034 

4.3 Conditioned models: the invariance in action 

Four models differing only in the law of A (Table 2, Figure 3); all denoisers receive √A and whitened inputs; all metrics are pooled over 4 ,000 generated paths, reported as mean ± sd over three training seeds. Two findings. (a) Dependence structure: all four seed-averaged models land within 0 .017 of the data’s ρr2 (1) = 0 .159 and within 0 .011 of its extremogram, regard-less of whether their noise offers no clustering ( A ≡ 1, i.i.d.), constant clustering (shared-IG) or calibrated clustering (DSPM); the between-model spread is comparable to the between-seed spread. With a receptive-field-restricted denoiser (dilations (1 , 1), RF = 13 ≪ 64; single run) the picture is unchanged: small-RF DDPM reaches ρ1 = 0 .131, small-RF DSPM 0 .139. (We note the restriction is suggestive rather than a strict information bound: composing T reverse steps extends the effective receptive field well beyond a single pass, so a small-RF sampler can still build long-range dependence iteratively.) Clustering in the conditioned regime is learned , not transmitted—consistent with Proposition 5. The decay shape confirms the source: conditioned-DSPM ρr2 (5) = 0 .118 ± 0.012 tracks the data’s 0 .110 (learned), not the mechanism’s theoretical 0.027 (see Section 4.5). (b) Marginals: here seed variance forces us to retract the sharper single-seed reading. Our first run showed DDPM clearly worst on every tail metric; across three seeds the kurtosis differences dissolve (DDPM 4 .59 ± 0.53 against heavy-tailed variants’ 4 .3–5 .2, all overlapping) and W1 retains only a directional, within-noise ordering (iid-TS 0 .031 ± 0.018 and DSPM 0 .038 ± 0.013 vs. DDPM 0 .055 ± 0.028). The one tail statistic that is simultaneously accurate and stable is DSPM’s Hill index, 3 .70 ±0.04 against the data’s 3 .69 (DDPM: 3 .85 ±0.16). At this scale, then, the inductive-bias channel that Proposition 5 leaves open is at most marginal 

in our measurements—the invariance is, if anything, more complete than the theorem strictly 85 10 15 20                  

> lag h
> 0.00
> 0.05
> 0.10
> 0.15
> 0.20
> ACF of  r2
> r2(h) = vh/(3 v+ 2)
> theory
> Monte Carlo
> 0.5 0.6 0.7 0.8 0.9
> 0.05
> 0.06
> 0.07
> 0.08
> recovery over 200 replications
> truth
> 050 100 150 200 250
> data time i
> 2
> 1
> 0
> 1
> 2
> sample path: riand Ai

Figure 2: Left: exact ACF (7) vs. Monte Carlo. Middle: recovered ( ˆ φ, ˆθ) over 200 replications (star = truth). Right: noise path ri = √Aizi with latent √Ai—clustering before any learning. Table 2: Conditioned four-way comparison: mean ± sd over three training seeds (4 ,000 generated paths per run; pooled estimators; extremogram = P (|ri+h| > u | | ri| > u ) at the 95% quantile u). ex.kurt Hill ρr2 (1) W1 xgram 1 xgram 5

data (test) 6.31 3.69 0.159 – 0.149 0.128 DDPM ( A ≡ 1) 4.59 ±.53 3.85 ±.16 .142 ±.012 .055 ±.028 .138 ±.010 .122 ±.008 iid-TS (DLPM-like) 4.75 ±.11 3.82 ±.03 .149 ±.001 .031 ±.018 .142 ±.002 .123 ±.005 shared-IG (t-EDM-like) 4.33 ±.44 3.88 ±.09 .142 ±.004 .049 ±.023 .142 ±.001 .124 ±.006 DSPM (TS-OU chain) 5.23 ±.71 3.70 ±.04 .154 ±.011 .038 ±.013 .146 ±.005 .129 ±.005 requires. 

4.4 Interventions on A do nothing 

Proposition 5(i) predicts that designing the variance chain at sampling time cannot steer a well-trained conditioned model. We feed the trained DSPM denoiser three scenarios: the unconditional chain; a volatility shock Ai = 1 + 7 φ i−i0 for i ≥ i0 = 24 (peak ×8, the chain’s own conditional-mean decay); and a flat calm regime A ≡ 0.25. Naive √A-scaling predicts envelope changes of +183% at the peak and −50% in the calm regime. Measured (Figure 4, one run shown; ratios averaged over three training seeds): the envelope ratio at the shock peak is 0 .88 ± 0.03 against the naive 2 .83—a small dip with the wrong sign for the naive prediction—and the calm-regime ratio is 1.06 ± 0.01 against the naive 0 .50. The trained network cancels A almost exactly, confirming that in v1 the latent is nuisance machinery, not a control variable; scenario control requires coupling 

A to the data (Section 3.5). 

4.5 Blind models: the mechanism transmits, as calibrated 

Finally we flip the single design bit the theorem identifies: the denoiser is blind —raw xt input, no √A channel—while the sampler machinery is unchanged. (This is an ablation isolating the con-ditioning bit, not a faithful DLPM re-implementation: pairing a marginal denoiser with A-scaled updates is deliberately crude, and inflates marginals. Single training run; the effect sizes here are an order of magnitude above the seed variability of Table 2.) Table 3: the blind DSPM’s samples show ρr2 (1) = 0 .151 against the calibrated 0.161, with ρr2 (5) = 0 .019 against the mechanism’s theoretical vφ 5/(3 v + 2) = 0 .027—the geometric fingerprint of (7), clearly distinct from the data’s 910 5 0 5 10                  

> r
> 10 6
> 10 5
> 10 4
> 10 3
> 10 2
> 10 1
> 10 0
> density
> pooled marginal (log scale)
> data
> DDPM (Gaussian)
> iid-TS (DLPM-like)
> shared-IG (t-EDM-like)
> DSPM (TS-OU chain)
> 510 15 20
> lag h
> 0.00
> 0.02
> 0.04
> 0.06
> 0.08
> 0.10
> 0.12
> 0.14
> 0.16
> pooled ACF of  r2
> volatility clustering
> data
> DDPM (Gaussian)
> iid-TS (DLPM-like)
> shared-IG (t-EDM-like)
> DSPM (TS-OU chain)
> 010 20 30 40 50 60
> data time i
> DDPM
> DSPM
> data
> sample paths

Figure 3: Conditioned comparison. Left: pooled marginals (log scale). Middle: pooled ACF of squared returns. Right: sample paths. 0 10 20 30 40 50 60  

> data time i
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> |ri|

envelopes are invariant to interventions on A            

> unconditional
> shock ×8 at i0
> calm A= 0.25
> naive A-scaling
> 010 20 30 40 50 60
> data time i

paths generated under the shock scenario 

Figure 4: Invariance under interventions. Left: amplitude envelopes under the three designed A

scenarios coincide, far from the naive √A-scaling (dotted). Right: paths generated under the ×8shock scenario show no burst at i0 (dashed line). 0.110 and from the conditioned model’s learned 0 .118. The blind iid variant’s clustering collapses to 0 .008 and its extremogram to the 5% base rate: an i.i.d. mechanism has nothing to transmit. Meanwhile both blind models’ kurtosis explodes ( ≈ 70): without whitening, the heavy injected noise cannot be cleanly removed. Together with Sections 4.3–4.4 this completes the causal picture: 

the law of A determines what structure the noise offers (Proposition 3, quantitatively visible in transmission); the conditioning choice determines whether it transmits (Proposition 5). 

4.6 The coupled model: control restored, filter validated 

We train v2 (Section 3.5) with the same denoiser, schedule, budget and data as v1, and re-run the intervention of Section 4.4 with the identical designed variance paths on both models (Figure 5, Table 4; all ratios and metrics mean ± sd over three training seeds). Three results. 

(a) Control. The ×8 shock, which moves the v1 envelope by a factor 0 .88 ±0.03 (i.e. nothing, with the wrong sign), scales the v2 envelope at the shock point by 3.00 ± 0.14 —bracketing the naive √A prediction of 2 .83—and the response decays along the designed path (1 .98 → 1.51 →

1.07 → 0.90 over four steps in the run shown). The calm scenario A ≡ 0.25 scales the envelope to 0 .52 ± 0.01 times baseline (naive: 0.50; v1: 1.06 ± 0.01). The same experiment, the same intervention, opposite outcome, stable across seeds: the difference is one premise of Proposition 5. 10 Table 3: Conditioning ablation. “Offered” = the noise mechanism’s own values from Proposition 3 at the calibrated parameters. ex.kurt Hill ρr2 (1) ρr2 (5) xgram 1

data (test) 6.31 3.69 0.159 0.110 0.149 offered by DSPM noise 6.27 – 0.161 0.027 –conditioned DSPM 4.26 3.76 0.141 0.118 0.140 blind DSPM 71.7 1.80 0.151 0.019 0.346 blind iid-TS 68.9 1.68 0.008 0.003 0.066 

(b) Filtering. On held-out paths the encoder’s posterior mean of A attains a mean per-path 

correlation of 0 .567 ± 0.001 with the true GARCH variance—known exactly because the data is synthetic—and 0 .762 ± 0.001 pooled. The per-path figure is the honest measure of within-path tracking; the pooled figure is inflated by cross-path differences in overall volatility level, which are easier to infer than dynamics. A 0 .57 within-path correlation makes the encoder a serviceable but unremarkable volatility smoother; its value here is that it is validated against ground truth rather than a proxy, and that it exists at all where the simplified objective produced none. 

(c) Memory, learned rather than calibrated. Empirical Bayes moves the prior’s memory from its moment-calibrated initialization (0 .638 at fine scale) to 0 .803 ± 0.000 (identical to three decimals across seeds), in the direction of the ground-truth persistence a + b = 0 .95. We state the comparison’s limits: the three numbers live in three related but distinct parametrizations— the TS chain’s φ, the coarse log-normal a re-expressed at fine scale through the interpolation, and GARCH’s a + b—so the claim is directional, not metric. The direction itself is the classical errors-in-variables prediction: calibrating persistence to the autocorrelation of squared returns , a noisy proxy of latent variance, attenuates it, and likelihood-based inference partially undoes the attenuation. The measured cost closes the ledger, and it is not small: v2’s unconditional excess kurtosis of 2 .86 ± 0.12 falls short not only of the target 6 .31 and of v1’s 5 .23 ± 0.71, but of the Gaussian 

baseline’s 4 .59 ± 0.53—a gap far outside seed noise; its ρr2 (1) = 0 .114 ± 0.003 is likewise below all four conditioned models (0 .142–0 .154). The paper’s opening argument was that Gaussian noise discards the domain’s structure—and the one model here that achieves control currently reproduces that structure worst, because the coarse bottleneck and the learned log-normal prior smooth away variance extremes. The honest summary is that v1 delivers fidelity without control and v2 delivers control without fidelity; obtaining both in one model is the open engineering problem (finer latent with a residual fine-scale variance component; tempered-stable prior via Monte-Carlo KL), not an achievement of this paper. 

# 5 Physics correspondence 

The DSPM noise prior is a known object in statistical physics, and the correspondences are exact, not analogies. Superstatistics (Beck and Cohen, 2003): r = √AZ with slowly varying 

A is a superstatistical ensemble; χ2-distributed intensity gives Student-t (t-EDM), our chain gives a self-decomposable tempered mixing law. Diffusing diffusivity : equations (2)–(3) are a discrete diffusing-diffusivity model (Chubynsky and Slater, 2014)—a Brownian particle whose diffusion coefficient is itself a stationary process—the accepted mechanism for “Brownian yet non-11 Table 4: v1 vs. v2 under identical interventions and identical unconditional metrics. “Shock ratio” = envelope at the shock peak over baseline (naive √A scaling: 2 .83); “calm ratio” anal-ogous (naive: 0.50). The three memory-column entries are expressed in related but distinct parametrizations (TS-chain φ; coarse log-normal a at fine scale; GARCH a + b)—see the caveat in Section 4.6(c). shock calm filter fine-scale unconditional ratio ratio corr memory a ex.kurt ρr2 (1) W1

target / naive 2.83 0.50 1 0.95 6.31 0.159 –v1 (uncoupled) .88 ±.03 1.06 ±.01 – 0.638 (calib.) 5.23 ±.71 .154 ±.011 .038 ±.013 v2 (coupled) 3.00 ±.14 .52 ±.01 .57 ±.00 0.803 (learned) 2.86 ±.12 .114 ±.003 .071 ±.006 0 10 20 30 40 50 60                    

> data time i
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> 2.00
> |ri|
> v2 responds to interventions; v1 cannot
> v2 unconditional
> v2 shock ×8
> v2 calm A= 0.25
> v1 shock (flat)
> naive A
> 010 20 30 40 50 60
> data time i
> 0
> 1
> 2
> 3
> 4
> volatility filtering (path corr 0.72)
> |ri| (observed)
> true i(GARCH)
> inferred Ai
> 10 110 010 1
> true 2
> i
> 10 1
> 10 0
> 10 1
> inferred  [Ai]
> pooled corr 0.76

Figure 5: v2. Left: under identical designed interventions, the v2 envelope responds along the designed decay while v1 stays flat. Middle: inferred √Ai vs. the true GARCH σi on a held-out path. Right: pooled inferred variance vs. truth. Gaussian” transport; the subordination analysis of Chechkin et al. (2017) transfers verbatim and predicts re-Gaussianization of aggregated noise over horizons ≫ (1 − φ)−1, i.e. stylized fact (iv). 

Subordination : the chain is a random clock rate in the sense of Clark (1973) and of continuous-time random walks (Montroll and Weiss, 1965); DSPM is a neural generative model driven by a discretized time-changed Brownian motion, the minimal adequate description of returns according to Carr et al. (2003). These statements constrain the noise mechanism only; by Proposition 5 they imply nothing about the learned model in the conditioned regime—a caution the physics framing makes easy to forget. 

# 6 Related work 

Heavy-tailed diffusion: DLPM (Shariatian et al., 2024), LIM (Yoon et al., 2023) (i.i.d. stable mixing; no memory); t-EDM (Pandey et al., 2025) (shared scale; infinite memory); fractional generative diffusion (Nobis et al., 2023) (Gaussian long memory); jump-diffusion forward processes (Baule, 2026); GBM-based financial diffusion (Kim et al., 2025), where heteroscedasticity is a function of the observed price level rather than a latent process with calibratable memory. None places a dynamically correlated latent variance inside the noise, and none states the invariance of Section 3.4. We are careful about that result’s blast radius: it constrains mechanism claims only in the conditioned regime (denoiser shown the mixing variables, drawn independently of the data). Models that operate blind with respect to their mixing variables—as published DLPM 12 effectively does—fall on the transmission side of the dichotomy, where the mechanism does act; for them the relevant finding here is different, namely that an i.i.d. mixing structure has no temporal dependence to transmit. On the classical side DSPM is the neural counterpart of BNS (Barndorff-Nielsen and Shephard, 2001) and time-changed L´ evy models (Carr et al., 2003); simulation follows Kanter (1975); Baeumer and Meerschaert (2010); Sabino and Cufaro Petroni (2022). 

# 7 Limitations and outlook 

(1) Single-factor memory. One OU factor forces geometric ACF decay; the calibrated φ =0.638 cannot match the GARCH truth’s persistence a + b = 0 .95 at long lags (visible in Table 3: offered ρr2 (5) = 0 .027 vs. data 0 .110). Superpositions of OU factors (supOU) fix this within the same propositions; consistently, v2’s learned memory (0 .803) moved toward but not onto the truth. (2) The v2 trade-off is not yet optimized. The coarse bottleneck ( dc = 16) that blocks the copy-cheat also smooths away variance extremes, costing unconditional kurtosis (2 .69 vs. v1’s 4 .26); and the log-normal prior was chosen for its closed-form KL, not its tails. A finer latent with a residual fine-scale variance component, and the tempered-stable prior of Definition 1 with Monte-Carlo KL, are the natural v2.1. (3) The blind sampler is crude. The blind rows of Table 3 isolate the conditioning bit; they are not a fair evaluation of DLPM, whose reverse process is derived for the marginal noise. (4) Synthetic ground truth. GARCH-t was chosen so the target stylized facts are known and controllable; the economically decisive test—pricing path-dependent structures under generated measures, where post-breach volatility persistence carries P&L—is left for the next iteration, on real index data. (5) Scale. d = 64, 0 .5M parameters, minutes of training: deliberately small, fully reproducible; conclusions about large-scale behaviour require care, though the invariance theorem itself is scale-free. 

Reproducibility. dspm core.py (sampler, chain, identities, calibration), dspm net.py (denoiser, sampler), scripts 01 –09 (validation, calibration, v1 training, evaluation, invariance, blind ablation, v2 training and evaluation); fixed seeds throughout. 

# References        

> B. Baeumer and M. M. Meerschaert. Tempered stable L´ evy motion and transient super-diffusion. J. Com-put. Appl. Math. , 233:2438–2448, 2010. O. E. Barndorff-Nielsen and N. Shephard. Non-Gaussian Ornstein–Uhlenbeck-based models and some of their uses in financial economics. J. R. Stat. Soc. B , 63(2):167–241, 2001. R. Baule. Score-based generative models with jump-diffusion forward processes. Working paper, 2026. C. Beck and E. G. D. Cohen. Superstatistics. Physica A , 322:267–275, 2003. T. Bollerslev. Generalized autoregressive conditional heteroskedasticity. J. Econometrics , 31:307–327, 1986. P. Carr, H. Geman, D. B. Madan, and M. Yor. The fine structure of asset returns: an empirical investigation.
> J. Business , 75(2):305–332, 2002. P. Carr, H. Geman, D. B. Madan, and M. Yor. Stochastic volatility for L´ evy processes. Math. Finance ,13(3):345–382, 2003. A. V. Chechkin, F. Seno, R. Metzler, and I. M. Sokolov. Brownian yet non-Gaussian diffusion: from superstatistics to subordination of diffusing diffusivities. Phys. Rev. X , 7:021002, 2017.

13 M. V. Chubynsky and G. W. Slater. Diffusing diffusivity: a model for anomalous, yet Brownian, diffusion. 

Phys. Rev. Lett. , 113:098302, 2014. P. K. Clark. A subordinated stochastic process model with finite variance for speculative prices. Econo-metrica , 41(1):135–155, 1973. R. Cont. Empirical properties of asset returns: stylized facts and statistical issues. Quant. Finance , 1:223– 236, 2001. L. Devroye. Non-Uniform Random Variate Generation . Springer, 1986. R. F. Engle. Autoregressive conditional heteroscedasticity with estimates of the variance of United Kingdom inflation. Econometrica , 50(4):987–1007, 1982. P. Gopikrishnan, V. Plerou, L. A. N. Amaral, M. Meyer, and H. E. Stanley. Scaling of the distribution of fluctuations of financial market indices. Phys. Rev. E , 60:5305–5316, 1999. J. Ho, A. Jain, and P. Abbeel. Denoising diffusion probabilistic models. NeurIPS , 33:6840–6851, 2020. M. Kanter. Stable densities under change of scale and total variation inequalities. Ann. Probab. , 3(4):697– 707, 1975. Kim et al. Diffusion-based generative models for financial time series via geometric Brownian motion. arXiv:2507.19003, 2025. I. Koponen. Analytic approach to the problem of convergence of truncated L´ evy flights towards the Gaussian stochastic process. Phys. Rev. E , 52:1197–1199, 1995. R. N. Mantegna and H. E. Stanley. Stochastic process with ultraslow convergence to a Gaussian: the truncated L´ evy flight. Phys. Rev. Lett. , 73:2946–2949, 1994. T. Mikosch and C. St˘ aric˘ a. Limit theory for the sample autocorrelations and extremes of a GARCH(1,1) process. Ann. Statist. , 28(5):1427–1451, 2000. E. W. Montroll and G. H. Weiss. Random walks on lattices. II. J. Math. Phys. , 6:167–181, 1965. G. Nobis et al. Generative fractional diffusion models. arXiv:2310.17638, 2023. K. Pandey et al. Heavy-tailed diffusion models. ICLR , 2025. arXiv:2410.14171. P. Sabino and N. Cufaro Petroni. Fast simulation of tempered stable Ornstein–Uhlenbeck processes. Com-put. Statist. , 37:2517–2551, 2022. D. Shariatian, U. Simsekli, and A. Durmus. Denoising L´ evy probabilistic models. arXiv:2407.18609, 2024. J. Sohl-Dickstein, E. Weiss, N. Maheswaranathan, and S. Ganguli. Deep unsupervised learning using nonequilibrium thermodynamics. ICML , 2015. Y. Song, J. Sohl-Dickstein, D. P. Kingma, A. Kumar, S. Ermon, and B. Poole. Score-based generative modeling through stochastic differential equations. ICLR , 2021. E. B. Yoon, K. Park, S. Kim, and S. Lim. Score-based generative models with L´ evy processes. NeurIPS ,36, 2023. Do heavy tails help diffusion? A statistical-estimation perspective. Working paper, 2026. 

14