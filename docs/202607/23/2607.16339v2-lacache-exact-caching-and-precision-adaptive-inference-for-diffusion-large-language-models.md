---
title: "LaCache: Exact Caching and Precision-Adaptive Inference for Diffusion Large Language Models"
title_zh: LaCache：扩散大语言模型的精确缓存与精度自适应推理
authors: "Xingru Chen, Zelang Liang, Yongjia Ma, Jiqing Zhan, Shuling Yang, Lian Wen, Kun Zhan"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.16339v2"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散大语言模型中的半自回归解码
tldr: 扩散大语言模型（DLLM）在去噪过程中存在严重的算子级冗余计算。本文提出LaCache，一个无需训练的加速框架。它通过无损状态备忘录（LSM）缓存Embedding、RoPE和FlashAttention的中间结果，并结合针对FFN层的自适应FP8量化，有效减少了重复计算和内存带宽瓶颈。实验显示LaCache可实现1.3倍至40.2倍的端到端加速，且不损失模型精度。
selection_source: fresh_fetch
motivation: 针对扩散大语言模型在半自回归解码过程中，由于重复计算不变的序列前缀和掩码后缀而导致的算子级冗余问题。
method: 提出LaCache框架，利用无损缓存机制存储中间状态，并结合基于步长感知的FFN层FP8量化策略来优化推理。
result: LaCache在保持任务准确度的同时，实现了1.3倍的独立加速，结合其他方法后最高可达40.2倍的端到端加速。
conclusion: LaCache通过消除冗余计算和优化内存带宽，为扩散大语言模型提供了一种高效、无损且易于集成的推理加速方案。
---

## 摘要
基于扩散的大语言模型（DLLMs）通过文本生成中的半自回归（SAR）解码实现并行生成。然而，当前方法存在严重的算子级冗余：它们在去噪步骤中重新计算整个序列，忽略了前缀和掩码后缀在块内保持不变的事实。我们提出了 LaCache，这是一个无需训练的加速框架，通过无损缓存和混合精度来缓解这种冗余。具体而言，LaCache 采用无损状态备忘录（LSM），通过缓存三类中间结果：(i) 用于嵌入输出的 EmbedCache，(ii) 用于逐 token 注意力前状态的 RoPECache，以及 (iii) 用于 FlashAttention 中在线 softmax 统计量的 FACache。这些缓存允许模型在不改变输出的情况下，跳过对未变化 token 的冗余计算。为了进一步缓解内存带宽瓶颈，LaCache 为 FFN 层集成了一种按组 FP8 量化策略，该策略针对扩散过程中与步骤相关的激活分布进行了定制。实验表明，LaCache 仅凭自身就能比原始 DLLM 实现约 1.3 倍的端到端加速。当与现有的加速方法结合时，LaCache 在保持相当的任务准确性的同时，实现了高达 40.2 倍的端到端加速。

## Abstract
Diffusion-based Large Language Models(DLLMs) enable parallel generation via Semi-Autoregressive (SAR) decoding in text generation. However, current methods suffer from severe operator-level redundancy: they recompute the entire sequence during denoising steps, ignoring that the prefix and masked suffix remain invariant within a block. We propose LaCache, a training-free acceleration framework that alleviates this redundancy through lossless caching and mixed precision. Specifically, LaCache employs Lossless State Memoization (LSM) by caching three types of intermediate results: (i) EmbedCache for embedding outputs, (ii) RoPECache for token-wise pre-attention states, and (iii) FACache for the online softmax statistics within FlashAttention. These caches allow the model to skip redundant computation on unchanged tokens without altering the output. To further alleviate memory-bandwidth bottlenecks, LaCache inegrates a per-group FP8 quantization strategy for FFN layers, tailored to step-dependent activation distributions across the diffusion process. Experiments demonstrate that LaCache alone achieves approximately 1.3X end-to-end speedup over vanilla DLLM. When combined with existing acceleration methods, LaCache reaches up to 40.2X end-to-end speedup while maintaining comparable task accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **LaCache**，一个旨在加速扩散大语言模型（Diffusion Large Language Models, DLLMs）推理的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散大语言模型（如 LLaDA）通常采用**半自回归（SAR）解码**，即在每个生成块（Block）内进行多次迭代去噪。作者发现，在同一个块的多次去噪步骤中，序列的前缀（Prompt）和掩码后缀（Masked Suffix）是保持不变的，但现有模型在每一步都会重新计算整个序列。
*   **研究动机**：这种“算子级冗余”导致了巨大的计算浪费。由于 DLLM 使用双向注意力机制，传统的 KV Cache 无法直接应用。因此，需要一种新的机制来消除这些重复计算，同时不损害生成质量。

### 2. 方法论
LaCache 结合了**无损状态备忘录（LSM）**和**精度自适应推理**：
*   **无损状态备忘录 (LSM)**：在每个 SAR 块的第一步计算并缓存不变 token 的中间状态，后续步骤直接复用。
    *   **EmbedCache**：缓存 Embedding 层的输出。
    *   **RoPECache**：缓存第一层 Transformer 的预注意力状态（包括 Norm、QKV 投影和旋转位置编码 RoPE）。
    *   **FACache (FlashAttention Cache)**：这是核心创新。它缓存 FlashAttention 的在线 Softmax 统计量（行最大值、累加器等）。对于不涉及当前生成块的计算分片（Tiles），直接跳过计算；对于涉及的分片，利用缓存值进行初始化。
*   **混合精度策略**：
    *   针对 FFN 层采用**按组（Per-group）FP8 量化**。
    *   **算子融合**：将量化操作与 AddNorm 和 SwiGLU 融合，减少内存访问开销。
    *   **自适应性**：根据扩散步骤中激活值的分布特性（前几层较尖锐，后几层较平滑）调整量化粒度。

### 3. 实验设计
*   **模型对象**：LLaDA (Base), LLaDA-Instruct, LLaDA-1.5。
*   **数据集/场景**：
    *   **推理任务**：GSM8K, MATH。
    *   **代码生成**：HumanEval, MBPP。
    *   **通用能力**：ARC, PIQA, Hellaswag, Winogrande, GPQA。
*   **Benchmark 与对比方法**：
    *   **Vanilla**：原始 LLaDA 模型。
    *   **+Parallel (Fast-dLLM)**：并行解码策略。
    *   **+DPad**：后缀丢弃（Suffix Dropout）策略。
    *   **LaCache**：作为独立插件或与上述方法结合。

### 4. 资源与算力
*   **硬件平台**：论文提到实验是在“一种设计的 GPU”（designed GPU）上进行的，并在附录中提到在另一种设计的 GPU 上也进行了验证。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号（推测为特定厂商的加速卡）、具体数量或总训练时长。由于 LaCache 是**无需训练（Training-free）**的方法，其核心开销在于推理时的显存占用（用于缓存）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础模型到指令微调模型的多个版本，并在 10 多个主流 Benchmark 上进行了测试。
*   **消融实验**：作者分别验证了纯缓存策略（w/o FP8）的效果、不同量化粒度（Per-tensor vs Per-token vs Per-group）的准确度对比，以及在长文本场景下的表现。
*   **充分性评价**：实验设计较为全面，不仅关注速度提升，还严格监控了准确率的变化。对比了多种现有的加速方案，证明了 LaCache 的**可组合性（Composability）**，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **加速效果显著**：LaCache 独立使用时可提供约 1.3 倍的加速；当与 Parallel 和 DPad 结合时，在标准序列上实现 3-12 倍加速，在长文本场景下最高实现 **40.2 倍**的端到端加速。
*   **精度几乎无损**：通过无损缓存和精细的 FP8 量化，模型在所有任务上的准确率与 Vanilla 版本持平，甚至在某些代码任务上因生成长度增加而略有提升。
*   **瓶颈转移**：通过消除第一层的冗余，推理瓶颈从计算密集型部分部分转移到了采样器和后续层。

### 7. 优点
*   **无损性**：LSM 机制在数学上与原始计算等价，保证了推理结果的绝对一致性。
*   **无需训练**：作为一种即插即用的推理框架，不需要对模型进行重训练或微调。
*   **高兼容性**：可以与现有的调度级优化（如并行解码、上下文剪枝）完美结合，产生叠加加速效应。

### 8. 不足与局限
*   **缓存层数受限**：由于双向注意力的特性，无损缓存目前主要局限于**第一层**。若要在更深层实现缓存，可能需要引入近似方法，这会导致精度下降。
*   **硬件依赖**：FP8 混合精度策略高度依赖支持 FP8 硬件加速的新型 GPU 架构，在旧硬件上无法发挥性能。
*   **显存开销**：虽然减少了计算量，但维护 EmbedCache、RoPECache 和 FACache 需要额外的显存空间，对于显存受限的设备可能存在压力。

（完）
