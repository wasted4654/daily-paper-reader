---
title: "Seeing the End at Step Zero: Accelerating Diffusion MLLMs via MLP Sparsity-Aware Truncation"
title_zh: 零步见终：通过 MLP 稀疏性感知截断加速扩散多模态大语言模型
authors: "Qicheng Zhao, Qi Sun, Zheyu Yan"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14557v1"
tags: ["query:ardm"]
score: 8.0
evidence: 加速扩散多模态大语言模型（DMLLM）
tldr: 针对扩散多模态大语言模型（DMLLM）因固定生成长度导致的推理效率低下问题，本文提出Seer框架。研究发现模型在去噪第一步即可通过MLP激活稀疏性显现语义边界。Seer利用信噪比准则在首步检测并截断冗余后缀，结合混合执行策略优化批处理吞吐量。实验显示Seer在保持甚至提升性能的同时，将吞吐量提升高达31倍，为DMLLM加速提供了即插即用的高效方案。
selection_source: fresh_fetch
motivation: 扩散多模态大语言模型在推理时因无法预知输出长度而产生大量冗余计算，严重限制了推理效率。
method: 提出Seer框架，通过监测首步去噪中MLP激活稀疏性的突变来识别语义边界，并采用信噪比准则进行一次性截断。
result: 在9个基准测试中，Seer实现了最高31倍的吞吐量提升，且在DocVQA等复杂任务上因减少噪声泄露而提升了准确率。
conclusion: Seer是一种无需训练、即插即用的加速方案，有效解决了DMLLM的填充浪费问题，实现了效率与性能的双赢。
---

## 摘要
扩散多模态大语言模型 (DMLLMs) 在多模态推理方面表现出色，但其推理效率受到固定长度生成约束的严重制约。由于实际输出长度未知，输出序列通常被填充至预设的最大长度，导致在不必要的 [EOS] 标记上产生大量冗余计算。在本文中，我们发现 DMLLMs 在第一个去噪步骤中，通过 MLP 激活稀疏性的显著转变，隐式地揭示了其有效的语义边界。基于这一观察，我们提出了 Seer，这是一个无需训练的框架，它利用基于信噪比 (SNR) 的准则来检测该边界，并对后续所有计算执行冗余后缀的一次性截断。为了在批量服务中保持这些理论收益，Seer 结合了一种混合执行策略，在无缝适配动态序列长度的同时最大化吞吐量。实验结果表明，Seer 有效消除了填充浪费，使吞吐量提升高达约 31 倍。在 9 个基准测试中，Seer 稳健地保持了整体性能，甚至通过减轻噪声泄漏提高了复杂视觉任务的准确性（例如，DocVQA 分数从 63.52 提升至 63.66），为 DMLLM 加速提供了一种高效、即插即用的解决方案。

## Abstract
Diffusion Multimodal Large Language Models (DMLLMs) are highly effective for multimodal reasoning, yet their inference efficiency is significantly hindered by fixed-length generation constraints. Since the actual output length is unknown, output sequences are padded to a predefined maximum length, resulting in substantial redundant computation over unnecessary [EOS] tokens. In this work, we discover that DMLLMs implicitly reveal their valid semantic boundary at the very first denoising step through a distinct shift in MLP activation sparsity. Leveraging this observation, we propose Seer, a training-free framework that detects this boundary using a Signal-to-Noise Ratio (SNR)-based criterion and performs one-shot truncation of the redundant suffix for all subsequent computations. To preserve these theoretical gains during batched serving, Seer incorporates a hybrid execution strategy that maximizes throughput while seamlessly accommodating dynamic sequence lengths. Experimental results demonstrate that Seer effectively eliminates padding waste, accelerating throughput by up to $\sim$31$\times$. Across 9 benchmarks, Seer robustly maintains overall performance and even improves accuracy on complex visual tasks by mitigating noise leakage (e.g., DocVQA score increases from 63.52 to 63.66), offering a highly efficient, plug-and-play solution for DMLLM acceleration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Seer** 的框架，旨在解决扩散多模态大语言模型（DMLLM）在推理过程中的效率瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题：** 扩散多模态大语言模型（如 LaViDa, MMaDA）采用全局迭代去噪的生成范式，必须在预定义的固定长度窗口内处理所有 Token。
*   **研究动机（填充之咒）：** 由于实际输出长度不可知，模型通常被强制生成大量冗余的填充 Token（如 `[EOS]`）。在多模态场景下，这些冗余 Token 会与高维视觉特征进行密集的双向注意力计算，导致巨大的计算浪费。
*   **噪声泄露：** 冗余的后缀 Token 往往缺乏语义支撑，会弥散性地关注无关背景，并通过双向注意力机制将噪声反馈给有效文本，损害推理质量。

### 2. 核心方法论
*   **核心发现（Step-0 稀疏性）：** 作者发现 DMLLM 在去噪的第一步（Step-0）的早期层（第 3-6 层）中，其 MLP 激活稀疏性会显现出明显的“语义跳跃”（Semantic Jump）和“填充平台”（Padding Plateau）。这表明模型在生成之初就已经“知道”了语义边界。
*   **SNR 感知边界检测：** 提出一种基于信噪比（SNR）的准则。通过计算局部窗口内的激活稀疏度变化（信号）与波动（噪声）之比，稳健地识别语义边界 $i^*$，实现一次性宏观截断。
*   **混合执行路由（Hybrid Execution Routing）：** 为了在批量推理（Batch Serving）中保持加速效果，Seer 设计了三条路径：
    *   **静态图路径（Static-Graph）：** 针对长度集中的批次，使用预捕获的 CUDA Graph。
    *   **桶装变长图路径（Bucket-Varlen-Graph）：** 针对中等差异批次，进行序列重排（Repacking）以消除填充。
    *   **变长急切路径（Bucket-Varlen-Eager）：** 针对极端离群值或超短序列。
*   **设备驻留延迟遮蔽：** 使用专门的 Triton 内核在 GPU 显存内直接完成张量切片和重排，避免了 CPU-GPU 同步带来的延迟。

### 3. 实验设计
*   **评估模型：** LaViDa-LLaDA, LaViDa-Dream, MMaDA。
*   **数据集/基准测试：** 涵盖 9 个主流基准，包括 MME, MMMU, MMBench, InfoVQA, ChartQA, ScienceQA, DocVQA, GQA, MathVista。
*   **对比方法：** 
    *   视觉 Token 压缩方法：D3ToM, RedVTP, VisionZip, MMTok, DivPrune, SparseVLM。
    *   扩散语言模型加速方法：Focus, Daedal。

### 4. 资源与算力
*   **硬件环境：** 所有实验均在 **NVIDIA A100 GPU** 上进行。
*   **配置：** 批量服务实验使用 4 张 A100 模拟高并发场景；基准测试和消融实验使用单张 A100。
*   **训练时长：** 该方法为 **Training-free（无需训练）**，因此不涉及训练时长，属于即插即用的推理加速方案。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 种不同的模型架构和 9 个数据集上进行了详尽测试，实验覆盖面广。
*   **消融实验：** 针对超参数（跳跃阈值 $\tau_{jump}$、容差率 $\gamma$、填充浪费阈值 $\tau_{pad}$）、前瞻窗口大小、系统组件开销等进行了深入分析。
*   **客观性：** 实验不仅对比了吞吐量和延迟，还通过帕累托前沿（Pareto Frontier）分析了精度与速度的权衡，并探讨了与 KV Cache 的兼容性，实验设计严谨、客观。

### 6. 主要结论与发现
*   **大幅加速：** Seer 在保持精度的前提下，将吞吐量提升了高达 **31 倍**（在 MMaDA + InfoVQA 任务上）。
*   **精度提升：** 在某些复杂视觉任务（如 DocVQA）中，截断冗余后缀反而提升了准确率（从 63.52 升至 63.66），验证了其“表示净化”作用。
*   **系统友好：** 混合路由策略成功解决了批量推理中的“长尾效应”（Straggler effect），使理论上的计算量减少转化为实际的墙钟时间加速。

### 7. 优点
*   **无需训练：** 纯推理侧优化，不改变模型权重，部署成本极低。
*   **首创性观察：** 首次揭示了扩散模型在 Step-0 即可通过 MLP 稀疏性预测边界的现象。
*   **软硬结合：** 不仅有算法层面的边界检测，还有系统层面的 Triton 内核优化和路由策略，确保了实际落地效果。
*   **双重收益：** 同时提升了推理效率和模型对视觉噪声的鲁棒性。

### 8. 不足与局限
*   **架构限制：** 该方法目前主要针对基于 Transformer 的扩散多模态模型，对于其他架构（如纯自回归模型或非 Transformer 架构）的适用性尚未探讨。
*   **超参数敏感性：** 虽然论文给出了帕累托最优配置，但在极端不同的任务分布下，可能仍需微调边界检测的阈值。
*   **过截断风险：** 尽管实验显示过截断率很低（约 0.8%-3.8%），但在对生成长度极其敏感的任务中，仍存在丢失有效信息的微小风险。

（完）
