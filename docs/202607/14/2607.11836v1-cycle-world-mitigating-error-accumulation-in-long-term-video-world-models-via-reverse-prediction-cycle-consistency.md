---
title: "Cycle-World: Mitigating Error Accumulation in Long-term Video World Models via Reverse-Prediction Cycle Consistency"
title_zh: Cycle-World：通过逆向预测循环一致性缓解长期视频世界模型中的误差累积
authors: "Zihan Su, Teng Hu, Jiangning Zhang, Ruiyan Wang, Ran Yi, Lizhuang Ma, Dacheng Tao"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11836v1"
tags: ["query:ardm"]
score: 10.0
evidence: 解决了长视频生成中自回归扩散模型的误差累积问题
tldr: 针对自回归扩散模型在长视频生成中存在的误差累积和生成漂移问题，本文提出了 Cycle-World 框架。该方法通过在训练和推理阶段引入时间可逆性约束，利用反向预测模型来纠正前向生成过程。实验证明，该方法能有效抑制结构崩溃和视觉退化，在 60 秒长视频合成中实现了卓越的时间一致性和生成质量。
selection_source: fresh_fetch
motivation: 自回归视频生成模型在长序列合成时，微小的预测偏差会随时间累积，导致严重的生成漂移和视觉质量下降。
method: 提出一种循环一致性框架，在训练时通过反向预测模型嵌入因果约束，在推理时利用该模型作为修正器进行梯度引导。
result: 在 VBench 基准测试中，Cycle-World 显著减轻了误差漂移，在 60 秒长视频生成任务上达到了最先进的质量和一致性。
conclusion: 通过引入时间可逆性约束，可以有效解决长程视频世界模型中的误差累积问题，提升生成视频的长期稳定性。
---

## 摘要
自回归扩散模型实现了高质量的视频生成，但其序列特性本质上受到误差累积的影响。在长时程视频合成中，微小的预测偏差会随时间累积，不可避免地导致无约束的生成漂移、结构崩溃和严重的视觉退化。为了解决这一问题，我们提出了 Cycle-World，这是一个旨在实现稳定且时间一致的长视频生成的创新框架。我们的方法通过在训练和推理阶段强制执行严格的时间可逆性来应对误差漂移。在理论上，我们证明了前向生成漂移可以被循环一致性目标严格限制。在训练期间，我们集成了一个高效的逆向预测模型，将因果约束隐式嵌入到前向生成器中，迫使其生成紧密贴合自然视频流形的、可逆的序列。在推理时，我们将这个冻结的逆向模型重新用作运行时校正器。通过基于梯度的循环引导，它迭代地优化生成的潜表示，在累积误差进入历史上下文之前主动对其进行抑制。在 VBench 基准测试上的广泛实验表明，Cycle-World 的双阶段协同作用显著缓解了误差漂移，在 60 秒合成中实现了最先进的整体生成质量和长时程时间一致性。

## Abstract
Autoregressive diffusion models have enabled high-quality video generation, yet their sequential nature inherently suffers from error accumulation. In long-horizon video synthesis, minor prediction deviations compound over time, inevitably leading to unconstrained generative drift, structural collapse, and severe visual degradation. To address this, we propose Cycle-World, a novel framework designed for stable and temporally consistent long-video generation. Our approach tackles error drift by enforcing strict temporal reversibility across both the training and inference phases. Theoretically, we demonstrate that forward generative drift can be strictly bottlenecked by a cycle-consistency objective. During training, we integrate an efficient reverse-prediction model to implicitly embed causal constraints into the forward generator, compelling it to produce reversible sequences that tightly adhere to the natural video manifold. At inference time, we repurpose this frozen reverse model as a runtime corrector. Through gradient-based cycle guidance, it iteratively refines the generated latent representations, actively suppressing accumulated errors before they are committed to the historical context. Extensive experiments on the VBench benchmark demonstrate that Cycle-World's dual-phase synergy significantly mitigates error drift, achieving state-of-the-art overall generation quality and long-horizon temporal consistency in 60-second synthesis.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Cycle-World**，一个旨在解决自回归视频生成模型中“误差累积”问题的创新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自回归（Autoregressive, AR）扩散模型在生成长视频时，每一帧的微小预测偏差会随时间不断累积。这种“误差漂移”会导致视频在长时程（如 60 秒）生成中出现结构崩溃、视觉质量退化以及逻辑不连贯。
*   **研究背景**：现有的视频世界模型虽然在短视频上表现优异，但由于缺乏对长期生成稳定性的约束，无法维持长时程的物理一致性。

### 2. 方法论：循环一致性框架
Cycle-World 的核心思想是利用**时间可逆性（Temporal Reversibility）**来约束生成过程。
*   **核心思想**：如果一个视频序列是符合自然物理规律的，那么从未来帧应该能够准确推测出过去帧。通过引入“逆向预测”，为前向生成建立闭环反馈。
*   **关键技术细节**：
    *   **训练阶段（隐式约束）**：同时训练一个前向生成器 $G_f$ 和一个高效的逆向预测模型 $G_r$。引入循环一致性损失（Cycle-consistency Loss），要求 $G_r$ 能够从 $G_f$ 生成的后续帧中还原出初始参考帧。这迫使生成器产生位于“自然视频流形”上的可逆序列。
    *   **推理阶段（显式校正 - Cycle Guidance）**：将冻结的逆向模型作为“运行时校正器”。在生成当前块（Chunk）时，利用逆向模型计算当前生成内容与历史上下文的偏差，并通过**基于梯度的循环引导**迭代优化当前的潜表示（Latent）。在误差进入历史缓存之前，主动将其抑制。

### 3. 实验设计
*   **数据集与场景**：论文主要针对长时程视频合成任务，生成时长达 60 秒的视频。
*   **Benchmark**：使用了 **VBench**（视频生成领域权威基准测试），重点评估时间一致性、视觉质量和生成稳定性。
*   **对比方法**：对比了当前主流的自回归视频扩散模型（如基于 DiT 架构的视频生成模型），验证其在缓解漂移方面的优越性。

### 4. 资源与算力
*   **算力说明**：提供的摘要和元数据中**未明确说明**具体的 GPU 型号、数量及总训练时长。通常此类长视频模型训练需要大规模 GPU 集群（如 H100 或 A100 阵列），但文中重点强调了逆向预测模型的“高效性（Efficient）”，暗示其计算开销在可控范围内。

### 5. 实验数量与充分性
*   **实验规模**：论文在 VBench 上进行了广泛实验，并专门针对 60 秒的长视频合成进行了长程稳定性测试。
*   **充分性评价**：实验设计较为充分，不仅涵盖了标准的质量指标，还通过消融实验验证了“训练阶段约束”与“推理阶段引导”的双重协同作用。通过 60 秒的超长生成挑战，客观地证明了该方法在处理极端误差累积时的鲁棒性。

### 6. 主要结论与发现
*   **误差可控性**：理论上证明了前向生成漂移可以被循环一致性目标严格限制（Bottlenecked）。
*   **双阶段协同**：仅靠训练约束或仅靠推理引导都无法达到最优，两者的协同作用是实现长时程稳定的关键。
*   **性能提升**：Cycle-World 在保持高视觉质量的同时，显著提升了长视频的时间一致性，达到了 SOTA（最先进）水平。

### 7. 优点与亮点
*   **理论严谨**：将循环一致性引入视频扩散模型，为解决自回归模型的固有缺陷提供了坚实的数学逻辑。
*   **即插即用潜力**：推理阶段的循环引导（Cycle Guidance）可以作为一种通用的优化手段，应用于其他自回归生成框架。
*   **长程稳定性**：成功突破了长视频生成中常见的“结构崩溃”瓶颈，使 60 秒级别的连贯生成成为可能。

### 8. 不足与局限
*   **推理开销**：推理阶段需要进行梯度回传和迭代优化（Cycle Guidance），这会不可避免地增加视频生成的推理耗时。
*   **逆向模型依赖**：系统的效果高度依赖于逆向预测模型的准确性，如果逆向模型本身存在偏差，可能会引入新的伪影。
*   **场景复杂性限制**：虽然在 VBench 上表现优异，但在极高动态或非线性极强的物理场景（如剧烈爆炸、复杂流体）中，时间可逆性的假设可能面临挑战。

（完）
