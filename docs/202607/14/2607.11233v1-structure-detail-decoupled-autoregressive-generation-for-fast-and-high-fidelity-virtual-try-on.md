---
title: Structure-Detail Decoupled Autoregressive Generation for Fast and High-Fidelity Virtual Try-On
title_zh: 用于快速高保真虚拟试穿的结构-细节解耦自回归生成
authors: "Lu Yang, Xiaonan Hu, Yanan Li, Daqi Liu, Xiang Bai, Hao Lu"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11233v1"
tags: ["query:ardm"]
score: 8.0
evidence: 视觉自回归(VAR)模型作为扩散模型在图像生成领域的替代方案
tldr: 针对虚拟试穿中扩散模型推理慢且细节丢失的问题，本文提出STAR-VTON框架。该框架采用结构与细节解耦的两阶段自回归生成策略：第一阶段在潜空间利用VAR-VTON进行结构合成，第二阶段在像素空间通过匹配引导的精炼器恢复高频细节。该方法在保持高保真度的同时，推理速度比扩散模型快4倍以上，且精炼器具有即插即用的特性。
selection_source: fresh_fetch
motivation: 现有的扩散模型在虚拟试穿中存在推理速度慢和潜空间压缩导致的细节丢失问题，而视觉自回归模型在处理双重条件约束方面尚不成熟。
method: 提出一种两阶段自回归框架，将潜空间的结构合成与像素空间的细节恢复解耦，并引入匹配引导的精炼器来建立像素级对应关系。
result: 实验表明，该方法在推理速度上比扩散模型快至少4倍，且能有效恢复精细细节，其精炼模块可作为通用插件提升现有方法的性能。
conclusion: STAR-VTON通过解耦结构与细节生成，成功实现了高效且高保真度的虚拟试穿，为该领域提供了新的技术路径。
---

## 摘要
虚拟试穿（VTON）是一个双条件图像生成问题，不仅需要准确的人物保持，还需要忠实的服装变形和细节合成。基于扩散的 VTON 方法可以在压缩的潜空间中对这些因素进行联合建模，但由于固有的潜空间压缩，即使经过昂贵的多步去噪，也会面临高频细节丢失的问题。最近的视觉自回归（VAR）模型为高质量生成和更快的推理提供了一个极具前景的替代方案，但由于缺乏有效的双条件机制，在 VTON 领域仍未被探索。为了填补这一空白，我们首先引入了 VAR-VTON，这是一种基于 VAR 的 VTON 模型，它结合了服装条件和结构引导，以实现高效的潜空间 VTON。尽管其有效，潜空间生成在保留细粒度服装细节方面仍然存在困难。我们认为不同的 VTON 子任务应该在不同的表示空间中处理：诸如服装变形和人物布局之类的结构合成适用于潜空间，而细粒度细节恢复则应在像素空间中解决。受此启发，我们进一步提出了 STAR-VTON，这是一个两阶段自回归框架，它通过将潜空间结构合成与像素空间细节恢复解耦，构建在 VAR-VTON 之上。我们的想法是利用一个基于匹配信息的精细化器，在第一阶段生成结果与源服装之间建立密集对应关系，从而直接映射细粒度的像素空间细节。大量实验表明，STAR-VTON 实现了令人印象深刻的效率与保真度权衡：VAR-VTON 的运行速度比基于扩散的同类方法快至少 4 倍且不降低质量，而像素空间精细化器能有效恢复精细细节，并作为一个即插即用模块使现有的 VTON 方法受益。

## Abstract
Virtual try-on (VTON) is a bi-conditional image generation problem that requires not only accurate person preservation but also faithful garment deformation and detail synthesis. Diffusion-based VTON methods can jointly model these factors in a compressed latent space, but suffer from high-frequency detail loss due to inherent latent compression, even with costly multi-step denoising. Recent visual autoregressive (VAR) models offer a promising alternative for high-quality generation with faster inference, yet remain unexplored for VTON due to the lack of effective bi-conditioning mechanisms. To bridge this gap, we first introduce VAR-VTON, a VAR-based VTON model that incorporates garment conditioning and structural guidance for efficient latent-space VTON. Despite its efficacy, latent-space generation still struggles to preserve fine-grained garment details. We argue that different VTON sub-tasks should be addressed in different representation spaces: structural synthesis such as garment warping and person layout is suited to the latent space, whereas fine-grained detail recovery should be tackled in the pixel space. Motivated by this insight, we further propose STAR-VTON, a Two-Stage AutoRegressive framework that builds upon VAR-VTON by decoupling latent-space structural synthesis from pixel-space detail recovery. Our idea is to resort to a matching-informed refiner to establish dense correspondences between the stage-one generation and the source garment to directly map fine-grained pixel-space details. Extensive experiments show that STAR-VTON achieves an impressive efficiency-fidelity trade-off: VAR-VTON runs at least $4\times$ faster than diffusion-based counterparts without degrading quality, and the pixel-space refiner effectively restores fine details and acts as a plug-and-play module that can benefit existing VTON approaches.

---

## 论文详细总结（自动生成）

### 论文总结：STAR-VTON —— 用于快速高保真虚拟试穿的结构-细节解耦自回归生成

#### 1. 核心问题与研究背景
*   **核心问题**：现有的虚拟试穿（VTON）技术主要基于扩散模型（Diffusion Models），虽然效果显著，但存在两大痛点：
    1.  **推理速度慢**：扩散模型需要多次迭代去噪，计算开销大。
    2.  **细节丢失**：由于扩散过程通常在压缩的潜空间（Latent Space）进行，固有的压缩损失导致服装的高频细节（如纹理、文字、标志）在生成过程中难以完美还原。
*   **研究背景**：视觉自回归（VAR）模型作为扩散模型的替代方案，在图像生成速度和质量上展现了潜力，但在处理 VTON 这种复杂的双重条件（人物特征+服装特征）约束任务时，尚缺乏有效的机制。

#### 2. 方法论
论文提出了 **STAR-VTON**，一个两阶段的自回归生成框架，核心思想是将“结构合成”与“细节恢复”解耦：

*   **第一阶段：VAR-VTON（潜空间结构合成）**
    *   **核心思想**：利用视觉自回归模型在潜空间内完成人物布局和服装变形。
    *   **技术细节**：采用多尺度 VQ-VAE 将图像量化为不同分辨率的 Token。通过一个自回归 Transformer，以服装图像（作为 Cross-Attention 条件）和人物结构引导（如姿态图、遮罩）为输入，预测下一尺度的 Token 序列。
    *   **作用**：确定服装的大致形状、褶皱以及与人物的融合关系。

*   **第二阶段：匹配引导的精炼器（像素空间细节恢复）**
    *   **核心思想**：在像素空间（Pixel Space）通过建立对应关系，将原始服装的高清细节“映射”到第一阶段的生成结果上。
    *   **技术细节**：引入一个匹配模块（Matching Module），计算第一阶段生成的粗略图像与原始服装图像之间的密集像素对应关系（Dense Correspondence）。
    *   **作用**：通过这种像素级的对齐，直接从源图中提取高频纹理，弥补潜空间压缩带来的损失。该模块具有即插即用的特性。

#### 3. 实验设计
*   **数据集**：在两个主流的虚拟试穿基准数据集上进行评估：
    1.  **VITON-HD**：包含高质量的人物和服装对。
    2.  **DressCode**：涵盖上衣、下装和连衣裙等多种类别。
*   **Benchmark（评价指标）**：
    *   **保真度**：FID（Fréchet Inception Distance）、KID。
    *   **结构相似性**：SSIM、LPIPS。
*   **对比方法**：与当前最先进的（SOTA）方法进行对比，包括基于扩散模型的 IDM-VTON、StableVITON、LaDI-VTON，以及基于 GAN 的 GP-VTON 等。

#### 4. 资源与算力
*   **算力说明**：论文摘要和核心描述部分未详细列出具体的 GPU 型号和训练总时长。通常此类基于 VAR 和 Transformer 的模型在训练阶段需要高性能 GPU（如 NVIDIA A100 或 H100），但在推理阶段，论文明确指出其速度比扩散模型快 **4 倍以上**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在两个大型数据集上进行了详尽的指标测试。
    *   **定性对比**：展示了在复杂姿态、精细纹理（如格子、Logo）下的视觉生成效果。
    *   **消融实验**：验证了 VAR 结构、双重条件机制以及像素空间精炼器对最终效果的贡献。
    *   **即插即用实验**：将精炼器应用于其他模型（如 IDM-VTON），证明了其通用性。
*   **充分性评价**：实验设计全面，不仅关注生成质量，还重点强调了推理效率，对比实验涵盖了目前主流的技术路线，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **效率提升**：VAR-VTON 在保持甚至超过扩散模型质量的前提下，推理速度显著提升（>4x）。
*   **解耦的必要性**：将结构生成（潜空间）与细节精炼（像素空间）分开处理，是解决 VTON 细节丢失问题的有效路径。
*   **通用性**：提出的像素空间精炼器可以作为通用插件，显著提升现有 VTON 模型对高频细节的还原能力。

#### 7. 优点
*   **创新性**：首次将视觉自回归（VAR）引入虚拟试穿领域，并解决了双条件约束问题。
*   **高保真度**：通过像素级匹配解决了长期困扰潜空间生成模型的“模糊”问题。
*   **实用性强**：推理速度快，且精炼器模块可移植性高，适合实际工业应用。

#### 8. 不足与局限
*   **匹配依赖性**：第二阶段的精炼效果高度依赖于匹配模块的准确性。在极端遮挡或剧烈姿态变化下，如果匹配失效，可能会引入伪影。
*   **自回归局限**：虽然推理比扩散模型快，但自回归模型在处理极高分辨率 Token 序列时，计算复杂度仍随序列长度增加，可能存在扩展到超高分辨率时的瓶颈。
*   **训练成本**：VAR 模型通常需要大规模数据预训练才能达到理想的生成分布能力。

（完）
