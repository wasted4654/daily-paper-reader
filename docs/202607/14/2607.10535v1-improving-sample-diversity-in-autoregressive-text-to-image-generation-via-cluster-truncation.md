---
title: Improving Sample Diversity in Autoregressive Text-to-Image Generation via Cluster Truncation
title_zh: 通过聚类截断提高自回归文本生成图像中的样本多样性
authors: "Trang Nguyen, Shuang Wu, Runyan Tan, Phillip Howard"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10535v1"
tags: ["query:ardm"]
score: 8.0
evidence: 自回归图像生成作为扩散模型在多样性方面的替代方案研究
tldr: 本研究针对自回归（AR）文本生成图像模型中的样本多样性问题进行了系统研究。虽然扩散模型在质量上领先但存在多样性坍塌，AR模型因其序列生成特性在提升多样性方面具有潜力。作者发现AR模型的高熵和视觉Token冗余限制了传统解码策略的效果，为此提出了一种名为“p-less cluster”的聚类级别截断采样方法。实验证明，该方法在保持图像质量和提示词对齐的同时，显著提升了多种AR模型的多样性。
selection_source: fresh_fetch
motivation: 旨在解决扩散模型多样性不足的问题，并探索自回归模型在图像生成中实现更好质量与多样性平衡的潜力。
method: 提出了一种名为“p-less cluster”的新型解码策略，在聚类级别而非Token级别进行基于熵的截断采样。
result: 实验表明，该方法在四个自回归模型和两个数据集上均能显著提升样本多样性，且不损失图像质量和对齐度。
conclusion: 聚类级别的截断采样是提升自回归图像生成模型多样性的有效手段，克服了视觉Token冗余带来的局限性。
---

## 摘要
虽然扩散模型在文本生成图像（T2I）领域达到了最先进的图像质量，但最近的研究表明它们存在样本多样性崩溃的问题。在这项工作中，我们研究了自回归（AR）图像生成模型是否能够推进图像质量与样本多样性之间的帕累托前沿。随着质量和效率的最新进展，AR 模型已成为基于扩散的图像生成的有力替代方案。除了支持交错图文生成等新用例之外，它们的顺序生成过程使其能够兼容广泛的基于 token 的解码策略，这些策略最初是为提高文本生成的多样性而开发的。受 AR 范式中更好的多样性-质量权衡潜力的启发，我们对 AR 图像生成模型中的样本多样性进行了首次系统研究。我们展示了 AR 图像生成的两个关键属性——持续的高 token 级熵和视觉 token 空间中的大量冗余——限制了现有 token 级解码方法在增强多样性方面的有效性。因此，我们提出了 $p$-less cluster，这是一种新的解码策略，它在聚类级别而非 token 级别执行基于熵的截断采样。我们在四个自回归 T2I 模型和两个数据集上评估了我们的方法和基准解码方法，使用了一套涵盖图像质量、提示词对齐和多样性的综合指标。我们的结果表明，在大多数评估的自回归 T2I 模型和数据集中，$p$-less cluster 在保持图像质量和提示词对齐的同时，释放了最大的多样性。

## Abstract
While diffusion models achieve state-of-the-art image quality for text-to-image (T2I) generation, recent work has demonstrated that they suffer from sample diversity collapse. In this work, we investigate whether autoregressive (AR) image generation models can push the Pareto frontier between image quality and sample diversity. With recent advances in quality and efficiency, AR models have emerged as a viable alternative to diffusion-based image generation. Beyond enabling new use cases such as interleaved image-text generation, their sequential generation process makes them compatible with a wide range of token-based decoding strategies originally developed to improve diversity in text generation. Motivated by the potential of a better diversity-quality tradeoff in the AR paradigm, we present the first systematic study of sample diversity in AR image generation models. We show that two key properties of AR image generation, persistently high token-level entropy and substantial redundancy in visual token spaces, limit the effectiveness of existing token-level decoding methods for diversity enhancement. We therefore propose $p$-less cluster, a new decoding strategy that performs entropy-based truncation sampling at cluster level rather than at token level. We evaluate our approach and baseline decoding methods across four autoregressive T2I models and two datasets using a comprehensive suite of metrics spanning image quality, prompt alignment, and diversity. Our results show that $p$-less cluster unlocks the greatest diversity across most evaluated autoregressive T2I models and datasets while maintaining image quality and prompt alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sample Diversity in Autoregressive Text-to-Image Generation via Cluster Truncation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然扩散模型（Diffusion Models）在图像生成质量上表现优异，但存在严重的“多样性坍塌（Diversity Collapse）”问题，即生成的图像往往趋向于某些特定模式。
*   **研究背景**：自回归（AR）模型作为扩散模型的有力竞争者，其序列生成的特性允许使用文本生成中的解码策略（如 Top-p 采样）来调节多样性。
*   **核心发现**：作者发现直接将文本解码策略应用于视觉 Token 效果不佳，原因有二：
    1.  **持续的高熵**：视觉 Token 在生成过程中始终保持高熵（不确定性），不像文本生成那样随上下文增加而降低。
    2.  **视觉冗余**：视觉 Token 空间存在大量冗余，许多不同的 Token 在语义上代表相似的视觉特征。
*   **整体含义**：本研究旨在通过改进 AR 模型的解码策略，在不牺牲图像质量和提示词对齐度的情况下，显著提升生成样本的多样性。

### 2. 论文提出的方法论：$p$-less cluster
*   **核心思想**：放弃在单个 Token 级别进行截断，转而在“聚类（Cluster）”级别进行基于熵的截断采样。
*   **关键技术细节**：
    1.  **聚类化（Clustering）**：预先对视觉码本（Codebook）中的 Token 嵌入向量进行聚类（如使用 K-means）。
    2.  **概率聚合**：在推理的每一步，将预测分布中属于同一聚类的所有 Token 概率求和，得到聚类级别的概率分布。
    3.  **聚类截断**：应用类似于 Nucleus Sampling (Top-p) 的逻辑，但对象是聚类。保留累积概率达到阈值 $p$ 的前 $N$ 个聚类。
    4.  **Token 采样**：在选定的聚类内部，根据原始的 Token 概率分布进行重新归一化并采样。
*   **算法流程**：通过这种方式，模型能够过滤掉那些语义上无关的“噪声聚类”，同时保留语义相关的多个视觉表达路径，从而避免了传统 Top-p 采样因视觉冗余而过早截断潜在多样化路径的问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **MS-COCO**：标准的文本生成图像基准。
    *   **MJHQ-30K**：高质量、高分辨率的图像数据集，用于评估模型在复杂场景下的表现。
*   **评估模型（Benchmark）**：选取了四个具有代表性的 AR 模型：**LlamaGen**、**Show-o**、**Chameleon** 和 **Janus**。
*   **对比方法**：
    *   **基础采样**：Top-k 采样、Top-p 采样、温度缩放（Temperature Scaling）。
    *   **引导策略**：无分类器引导（Classifier-Free Guidance, CFG）。
*   **评估指标**：
    *   **质量**：FID（Fréchet Inception Distance）。
    *   **对齐度**：CLIP Score。
    *   **多样性**：V-Recall、V-Precision、V-Self-Similarity（基于视觉特征的召回率、精确率和自相似度）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的训练算力（如 GPU 型号、数量或总时长），因为该研究主要集中在**推理阶段（Inference-time）**的解码策略改进，使用的是预训练好的开源模型。
*   **推理开销**：作者提到聚类操作是预先完成的，推理时的额外计算开销主要在于聚类概率求和，相对于模型本身的计算量而言几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种不同架构和规模的 AR 模型。
    *   在 2 个主流数据集上进行了详尽测试。
    *   进行了消融实验，探讨了聚类数量（$K$）和截断阈值（$p$）对结果的影响。
*   **充分性评价**：实验设计较为充分。通过跨模型、跨数据集的对比，证明了该方法的普适性。同时，引入了专门针对多样性的量化指标（V-Recall 等），使得结论比仅看 FID 更有说服力。

### 6. 主要结论与发现
*   **多样性提升**：$p$-less cluster 在所有测试模型上均显著提升了样本多样性，且在多样性-质量的帕累托前沿（Pareto frontier）上优于传统的 Top-p 和 Top-k 采样。
*   **AR 模型的潜力**：AR 模型在多样性方面具有比扩散模型更好的天然属性，而合理的解码策略能进一步释放这种潜力。
*   **视觉 Token 特性**：证实了视觉 Token 的高冗余性是限制传统 NLP 解码策略在图像领域发挥作用的主要障碍。

### 7. 优点
*   **创新性**：敏锐捕捉到了视觉 Token 与文本 Token 在分布特性上的本质差异，提出了聚类截断这一新颖视角。
*   **即插即用**：该方法不需要重新训练模型，仅需在推理阶段修改采样逻辑，具有极高的实用价值。
*   **效果显著**：在保持 CLIP Score（对齐度）几乎不变的情况下，大幅改善了生成结果的丰富度。

### 8. 不足与局限
*   **聚类依赖性**：方法的有效性可能依赖于预训练模型码本（Codebook）的质量以及聚类算法的选择。
*   **超参数敏感性**：虽然论文给出了建议值，但在不同任务下可能仍需微调聚类数 $K$ 和阈值 $p$。
*   **实验覆盖**：虽然测试了多个 AR 模型，但未与最新的流匹配（Flow Matching）模型在多样性上进行直接的大规模量化对比。
*   **语义一致性风险**：在极端追求多样性时，虽然聚类截断比 Token 截断更稳健，但仍可能出现部分图像局部伪影或语义漂移。

（完）
