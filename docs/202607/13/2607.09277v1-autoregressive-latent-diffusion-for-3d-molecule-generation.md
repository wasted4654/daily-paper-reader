---
title: Autoregressive latent diffusion for 3D molecule generation
title_zh: 用于三维分子生成的自回归潜扩散模型
authors: "Federico Ottomano, Gaopeng Ren, Yingzhen Li, Kim E. Jelfs, Alex M. Ganose"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09277v1"
tags: ["query:ardm"]
score: 10.0
evidence: 用于3D分子生成的潜空间自回归扩散框架
tldr: 本研究针对3D分子生成中扩散模型需预设分子大小及自回归模型难以平衡生成质量的问题，提出了KRONOS框架。该框架在预训练自编码器的潜空间内进行自回归扩散，同时建模分子图拓扑与几何结构。通过引入“中间填充”（FIM）混合训练策略，KRONOS在单一模型中实现了无条件生成与片段条件生成。实验证明其在QM9和GEOM-Drugs数据集上表现优异，兼具自回归的灵活性与扩散模型的高质量。
selection_source: fresh_fetch
motivation: 现有的3D分子生成扩散模型通常需要预设分子大小，而自回归方法在平衡无条件生成与条件生成方面存在挑战。
method: 提出KRONOS框架，在预训练自编码器的潜空间中结合自回归与扩散机制，并采用“中间填充”策略进行混合训练。
result: 在QM9和GEOM-Drugs数据集上，KRONOS在自回归方法中取得了领先的无条件生成性能，并与扩散模型相当。
conclusion: KRONOS证明了可以在单一架构中同时支持高质量的无条件生成和灵活的片段条件生成。
---

## 摘要
三维（3D）分子生成目前由扩散模型主导，这类模型虽然生成质量很高，但通常需要预先指定分子大小。最近的自回归方法已大幅缩小了性能差距，同时自然地支持变长生成以及基于部分分子上下文的条件生成。然而，平衡无条件生成和上下文条件生成仍然具有挑战性。我们提出了 KRONOS，这是一个潜自回归扩散框架，它在预训练自编码器的潜空间中生成分子，同时对分子图拓扑和几何结构进行联合建模，并保留了自回归生成的灵活性。我们进一步引入了一种受“中间填充”（FIM）范式启发的混合训练策略，使得在单个从左到右的自回归模型中能够同时实现无条件生成和片段条件分子生成。在 QM9 和 GEOM-Drugs 数据集上的实验表明，KRONOS 在自回归方法中实现了领先的无条件生成性能，同时与扩散模型相比也具有竞争力。此外，片段条件生成的实现对无条件生成性能的影响微乎其微，证明了两种生成范式可以在单一架构中得到支持。

## Abstract
Three-dimensional (3D) molecule generation has been dominated by diffusion models, which achieve strong generation quality but typically require the molecular size to be specified a priori. Recent autoregressive approaches have substantially narrowed the performance gap while naturally supporting variable-length generation and conditioning on partial molecular context. However, balancing unconditional and context-conditioned generation remains challenging. We introduce KRONOS, a latent autoregressive diffusion framework that generates molecules in the latent space of a pre-trained autoencoder, jointly modeling molecular graph topology and geometry, while retaining the flexibility of autoregressive generation. We further introduce a mixed training strategy inspired by Fill-in-the Middle (FIM) paradigm, enabling both unconditional and fragment-conditioned molecular generation within a single left-to-right autoregressive model. Experiments on QM9 and GEOM-Drugs demonstrate that KRONOS achieves leading unconditional generation performance among autoregressive methods, while remaining competitive with diffusion models. Moreover, fragment-conditioned generation is achieved with negligible impact on unconditional generation performance, demonstrating that both generation paradigms can be supported within a single architecture.

---

## 论文详细总结（自动生成）

以下是对论文《Autoregressive latent diffusion for 3D molecule generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
三维（3D）分子生成是药物发现和材料设计的核心任务。目前该领域主要由**扩散模型**主导，虽然生成质量高，但通常需要预设分子大小（原子数）。**自回归（AR）模型**虽然能自然支持变长生成和局部结构条件生成，但在生成质量上一直落后于扩散模型，且难以在单一架构中平衡“无条件生成”与“基于片段的条件生成”。
论文提出了 **KRONOS** 框架，旨在结合自回归的灵活性与扩散模型的高生成质量，通过在潜空间（Latent Space）进行建模，解决3D分子生成中的尺寸预测和复杂条件约束问题。

### 2. 方法论：核心思想与关键技术
KRONOS 的核心是将自回归建模与潜扩散机制相结合：
*   **潜空间自回归（Latent AR）：** 使用预训练的统一自编码器（UAE）将分子的拓扑和几何信息压缩为连续的潜向量序列。模型按“原子”顺序逐个生成潜向量。
*   **扩散损失函数（Diffusion Loss）：** 每一个自回归步骤 $p(z_i | z_{<i})$ 并不直接预测离散类别，而是通过一个扩散过程来参数化。即在给定历史上下文 $z_{<i}$ 的情况下，通过去噪目标来生成当前步的连续潜向量 $z_i$。
*   **停止预测机制（Stop Classifier）：** 由于潜空间没有天然的“终止符”，模型额外训练了一个轻量级的 MLP 分类器，根据当前上下文预测序列是否结束，从而实现变长生成。
*   **混合训练策略（FIM 启发）：** 借鉴自然语言处理中的“中间填充”（Fill-in-the-Middle, FIM）范式，通过随机将分子分解为片段（基于 BRICS 算法），并在训练中混合“从左到右生成”和“给定片段补全”的任务，使模型在不改变架构的情况下支持片段条件生成。
*   **片段局部编码：** 为了支持推理时的片段条件生成，修改了 UAE 编码器，使其在编码时忽略片段间的边缘，确保潜表征仅依赖于局部片段信息。

### 3. 实验设计
*   **数据集：** 使用了 3D 分子生成的标准基准数据集 **QM9**（约 13 万小分子）和 **GEOM-Drugs**（约 30 万药物样分子，包含多构象）。
*   **对比方法（Baselines）：**
    *   **扩散/流匹配模型：** EDM, MiDi, JODO, ADiT, SemlaFlow。
    *   **自回归模型：** G-SchNet, Symphony, QUETZAL, NEAT。
*   **评估指标：** 3D 有效性（Validity）、原子/分子稳定性（Stability）、几何分布差异（MMD，包括键长、键角、二面角），以及基于量子化学工具 GFN2-xTB 的能量和 RMSD 评估。

### 4. 资源与算力
论文在致谢部分提到了使用了 **Isambard-AI 国家 AI 研究资源**（由布里斯托大学运行）。
*   **具体算力：** 文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **超参数参考：** 附录中提到 QM9 训练了 10,000 个 epoch，GEOM-Drugs 训练了 1,000 个 epoch，Batch Size 分别为 2048 和 512。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了无条件生成、片段条件生成（约 1.5 万个分子-片段对）、2D 拓扑评估以及量子化学稳定性评估。
*   **消融实验：** 针对模型规模（Transformer 层数、隐藏维度）和片段条件概率 $p_f$ 进行了详细的消融研究。
*   **充分性评价：** 实验设计非常充分。不仅对比了传统的启发式指标（如稳定性），还引入了基于物理能量（xTB）的评估，这在 3D 分子生成领域被认为是更客观、更具物理意义的评价方式。

### 6. 主要结论与发现
*   **性能领先：** KRONOS 在 QM9 和 GEOM-Drugs 上均取得了自回归方法中的最佳性能，且在多个指标上达到或超越了最先进的扩散模型。
*   **多任务统一：** 证明了通过 FIM 训练策略，单一模型可以同时胜任无条件生成和片段补全任务，且片段训练对无条件生成的质量几乎没有负面影响。
*   **几何精确性：** 经过 xTB 优化验证，KRONOS 生成的构象在能量和几何结构上非常接近物理真实状态，优于大多数现有基准。

### 7. 优点与亮点
*   **潜空间建模的优势：** 避开了直接处理离散原子类型和连续坐标的异构数据难题，将任务统一为连续潜向量的生成。
*   **灵活的条件生成：** 相比于需要修改架构或使用复杂采样技巧的扩散模型，KRONOS 的片段补全能力更加自然且高效。
*   **物理一致性：** 引入半经验量子化学评估，增强了模型在实际药物设计应用中的可信度。

### 8. 不足与局限
*   **依赖预训练自编码器：** 生成质量的上限受限于 UAE 的重建精度。如果 UAE 无法完美捕捉某些复杂结构，生成模型也无法弥补。
*   **非完全置换不变性：** 尽管使用了片段分解，但其本质仍是序列模型，不具备像 NEAT 等模型那样的完全置换不变性（Permutation Invariance）。
*   **片段化策略限制：** 依赖于固定的 BRICS 分解规则，这可能无法覆盖所有化学上有意义的分割方式，未来可能需要更动态的片段学习方法。

（完）
