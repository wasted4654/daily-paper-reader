---
title: "Induction in Both Directions: A Mechanistic Analysis of In-Context Learning in Masked Diffusion Language Models"
title_zh: 双向归纳：掩码扩散语言模型中上下文学习的机制分析
authors: "Andy Catruna, Emilian Radoi"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15893v2"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型与自回归模型的机制分析对比
tldr: 本研究深入探讨了掩码扩散语言模型（DLM）实现上下文学习的核心机制——归纳电路。通过与自回归（AR）模型对比，研究发现DLM利用双向归纳电路，结合前向和后向标记头将局部上下文写入残差流，从而在双向语境下实现更强的归纳能力。此外，研究还提供了因果证据，证明DLM能通过计算掩码标记的全局比例来隐式获取时间步信息，而非依赖显式嵌入。
selection_source: fresh_fetch
motivation: 旨在揭示扩散语言模型在处理上下文学习时，其内部归纳机制与传统自回归模型的异同及工作原理。
method: 通过对比具有匹配架构的自回归模型与吸收掩码扩散模型，对两者的归纳电路进行机械论分析和因果干预。
result: DLM发展出了对称的双向归纳电路，在双向语境可见时表现优于AR模型，并能利用掩码比例作为隐式时间步。
conclusion: DLM的性能优势源于其对双向上下文的访问能力，这一发现为理解非自回归生成模型的内部机理提供了重要见解。
---

## 摘要
虽然自回归 (AR) Transformer 的内部机制已得到广泛研究，但对于扩散语言模型 (DLM) 的了解却少得多，这是一种通过迭代去噪生成文本的新兴替代方案。在这项工作中，我们研究了 DLM 如何实现归纳 (induction)，这是上下文学习背后的一种机制，模型通过该机制寻找重复的上下文并复制其后的标记 (token)。我们的分析比较了架构匹配的仅注意力 AR 模型和吸收掩码 (absorbing-mask) DLM。我们发现 DLM 学习了一种双向归纳电路，其中前向标记 (previous-token) 和后向标记 (next-token) 头将局部上下文写入残差流，随后的归纳头利用这些信息从匹配的源位置寻找并复制答案。该电路具有方向对称性，无论源出现在过去还是未来都能发挥作用。当仅左侧上下文可见时（与 AR 模型所见一致），DLM 在归纳能力上并不优于其对应的 AR 模型。然而，我们观察到当被掩码标记的两侧都可见时，它具有更强的归纳能力，这表明其优势在于双向上下文访问，而非更强的一侧机制。除了归纳之外，我们还提供了因果证据，表明 DLM 会计算被掩码标记的全局比例，并将其用作隐式时间步长，即使没有给定显式的时间步嵌入。

## Abstract
While the internal mechanisms of autoregressive (AR) transformers have been studied extensively, much less is known about diffusion language models (DLMs), an emerging alternative that generates text by iterative denoising. In this work, we study how DLMs implement induction, a mechanism behind in-context learning in which the model finds a repeated context and copies the token that followed it. Our analysis compares attention-only AR models and absorbing-mask DLMs with matched architectures. We find that DLMs learn a bidirectional induction circuit, where previous-token and next-token heads write local context into the residual stream and later induction heads use it to find and copy the answer from the matching source position. The circuit is direction-symmetric, working whether the source appears in the past or in the future. When only left context is visible, matching what an AR model sees, the DLM does not outperform its AR counterpart in induction capabilities. However, we observe it has stronger induction when both sides of the masked token are visible, pointing to bidirectional context access rather than a stronger one-sided mechanism. Beyond induction, we provide causal evidence that DLMs compute the global fraction of masked tokens and use it as an implicit timestep, even though they are given no explicit timestep embedding.

---

## 论文详细总结（自动生成）

这篇论文对掩码扩散语言模型（DLM）的上下文学习机制进行了深入的机械论分析（Mechanistic Analysis），并将其与传统的自回归（AR）模型进行了对比。以下是结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然自回归 Transformer 的“归纳头”（Induction Heads）机制已被广泛研究，但新兴的扩散语言模型（DLM）如何实现上下文学习（ICL）尚不明确。
*   **核心问题**：DLM 是否像 AR 模型一样通过特定的电路实现归纳？其双向注意力和迭代去噪的特性如何改变这一机制？
*   **整体含义**：通过揭示 DLM 的内部电路，为理解非自回归生成模型的涌现能力提供了理论基础，并证明了 DLM 在双向语境下的独特优势。

### 2. 方法论
*   **核心思想**：通过训练架构完全匹配的 AR 和 DLM 模型（仅含注意力层，深度 L=1, 2, 3），在受控环境下对比两者的归纳行为和内部电路。
*   **关键技术细节**：
    *   **吸收掩码扩散（Absorbing-mask DLM）**：使用 `[MASK]` 标记逐步替换原始标记的离散扩散过程。
    *   **电路定位（Circuit Localization）**：利用**均值消融（Mean Ablation）**识别对归纳得分贡献最大的注意力头。
    *   **权重分解（Weight Decomposition）**：将注意力得分分解为 QK（查询-键）项和 OV（值-输出）项，分析模型如何匹配上下文并复制标记。
    *   **线性探测（Linear Probing）**：在残差流中训练线性分类器，检测模型是否编码了全局掩码比例。
    *   **因果干预**：通过路径修补（Path Patching）改变残差流中的特定方向，验证其对预测熵的影响。

### 3. 实验设计
*   **数据集**：使用 **FineWeb-Edu** 进行预训练（250k 步）。
*   **评估场景**：**随机标记重复序列**（Random-token repeated sequences）。通过 512 长度的序列（前 256 位随机，后 256 位重复）来隔离语义干扰，纯粹测试模型的归纳能力。
*   **Benchmark 与对比方法**：
    *   **AR 模型**：作为基准，仅能访问左侧上下文。
    *   **DLM 变体**：测试不同可见窗口（仅左侧、仅右侧、双向）下的归纳表现。
    *   **方向性测试**：对比“前向归纳”（从过去复制）和“反向归纳”（从未来复制）。

### 4. 资源与算力
*   **模型规模**：小型 Transformer（$d_{model}=512$，16 个头，$d_{head}=32$），深度为 1-3 层。
*   **训练细节**：Batch size 128，学习率 $10^{-3}$，250k 步训练。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号和数量，但基于模型规模和训练步数推断，该实验在单张或少量消费级 GPU（如 A100 或 RTX 4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：针对每种架构和深度训练了 3 个独立随机种子，确保结果的可重复性。
*   **消融实验**：进行了详尽的逐头消融、冲突提示词（Conflict Prompts）实验以及不同掩码比例下的鲁棒性测试。
*   **客观性**：通过“折叠层归一化”（Folded LayerNorm）技术消除了非线性干扰，使权重分解更具说服力。实验设计严谨，对比实验在相同数据分布和架构下进行，具有高度的公平性。

### 6 主要结论与发现
*   **双向归纳电路**：DLM 学习了一种对称的电路。第 0 层头负责写入“前一个标记”和“后一个标记”的特征，第 1 层归纳头利用这些特征在双向语境中定位并复制答案。
*   **性能来源**：DLM 的归纳优势并非源于更强的单向机制，而是源于其能同时利用左右两侧的上下文。在仅有左侧上下文时，DLM 表现不如 AR 模型。
*   **隐式时间步**：DLM 内部能自动计算全局掩码比例（即腐蚀程度），并将其作为隐式时间步长来调节预测的确定性，即使训练时未提供显式的时间步嵌入。
*   **相位转变**：归纳能力在训练早期（约 8k-24k 步）突然涌现，且 DLM 的涌现速度通常快于匹配的 AR 模型。

### 7. 优点
*   **机制透明化**：首次清晰地描绘了扩散模型实现 ICL 的底层电路图。
*   **因果严谨性**：不仅观察相关性，还通过消融和修补实验提供了因果证据。
*   **发现隐式特征**：揭示了模型如何自发学习全局统计量（掩码率）来辅助局部预测。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于极小的注意力模型（最多 3 层），未包含 MLP 层，尚不确定结论是否能完美推广到具有数千亿参数的大规模 DLM。
*   **任务单一性**：主要依赖合成的随机标记任务，对自然语言中复杂语义归纳的覆盖有限。
*   **扩散类型局限**：仅研究了“吸收掩码”扩散，对于“均匀噪声”或其他类型的离散扩散模型，其机制可能有所不同。

（完）
