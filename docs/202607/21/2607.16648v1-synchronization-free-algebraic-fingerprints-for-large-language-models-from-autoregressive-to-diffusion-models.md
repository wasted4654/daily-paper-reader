---
title: "Synchronization-Free Algebraic Fingerprints for Large Language Models: From Autoregressive to Diffusion Models"
title_zh: 大语言模型的免同步代数指纹：从自回归模型到扩散模型
authors: "Jaroslaw Janas, Josef Pieprzyk, Pawel Morawiecki"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16648v1"
tags: ["query:ardm"]
score: 8.0
evidence: 自回归与扩散模型的数字水印技术
tldr: 针对大语言模型生成内容的归属识别问题，本文提出一种无需同步的代数指纹水印方案。该方法利用相邻 Token 对生成二进制同余约束，结合密码学哈希与 Reed-Solomon 多项式嵌入秘密身份。由于每个约束仅依赖局部 Token 对，该方案天然具有抗插入、删除和重排序的鲁棒性。实验证明，即使在高损坏率下，该方法也能以较低冗余实现可靠的身份恢复，适用于自回归及扩散模型。
selection_source: fresh_fetch
motivation: 现有的水印方案在面对文本编辑、改写或 Token 同步失效时表现脆弱，亟需一种对结构变化具有鲁棒性的归属识别方法。
method: 通过相邻 Token 对的哈希值确定 Reed-Solomon 多项式的评估点，并将多项式值的奇偶性作为水印位嵌入到后续 Token 中。
result: 该方案在无需全局同步的情况下，能够有效抵抗 Token 的增删改操作，并支持在不同长度的文本中灵活嵌入秘密身份。
conclusion: 本文提出的代数指纹框架为大模型内容安全提供了强鲁棒性、低冗余的解决方案，且具有良好的数学理论支撑。
---

## 摘要
大语言模型（LLM）对可靠的水印方法产生了紧迫需求，这些方法能够实现生成文本的归属识别，同时对编辑和改写保持鲁棒性。我们提出了一种新型的免同步水印方案，其中每个水印由一对相邻 token 生成的单个二进制同余式组成。对于每一对 token，加密哈希决定了代表秘密身份的 Reed-Solomon 多项式的求值点，而多项式求值的奇偶性则决定了嵌入到该对第二个 token 中的水印位。由于每个同余式都是自包含的，且仅依赖于局部 token 对，因此所提出的结构天然能够抵抗插入、删除和 token 重排序。我们从代数角度分析了恢复问题，讨论了适用于不同身份长度的几种解码算法，并将水印损坏建模为二进制对称信道。分析表明，即使在 token 损坏率较高的情况下，可靠的恢复也仅需要很小的冗余。与现有的基于块的水印方案不同，该方法避免了同步问题，同时为嵌入短秘密身份和长秘密身份提供了一个灵活的框架。

## Abstract
Large Language Models (LLMs) have created an urgent need for reliable watermarking methods that enable attribution of generated text while remaining robust to editing and paraphrasing. We propose a novel synchronization-free watermarking scheme in which every watermark consists of a single binary congruence generated from a pair of neighbouring tokens. For each token pair, a cryptographic hash determines an evaluation point of a Reed--Solomon polynomial representing the secret identity, while the parity of the polynomial evaluation determines the watermark bit embedded into the second token of the pair. Since each congruence is self-contained and depends only on the local token pair, the proposed construction is naturally resistant to insertions, deletions, and token reordering. We analyse the recovery problem from an algebraic perspective, discuss several decoding algorithms suitable for different identity sizes, and model watermark corruption as a Binary Symmetric Channel. The analysis shows that reliable recovery requires only a small redundancy even for relatively high token corruption rates. Unlike existing block-based watermarking schemes, the proposed method avoids synchronization problems while providing a flexible framework for embedding both short and long secret identities.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对大语言模型（LLM）的创新水印方案，旨在解决现有水印技术在面对文本编辑（如增删改、重排序）时容易失效的“同步性”难题。

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 生成内容的普及，如何准确识别 AI 生成文本并追踪其来源（归属识别）变得至关重要。现有的水印方案（如基于固定块或序列位置的方法）高度依赖 **同步性**：一旦用户对文本进行微小的编辑（如删除一个词或调换句子顺序），水印位的排列就会被打乱，导致解码失败。
**研究动机**：开发一种“免同步”的水印技术，使每一个水印位都仅依赖于局部的 Token 对，从而在文本遭受严重篡改后依然能可靠地提取身份信息。

### 2. 方法论：核心思想与技术细节
该方案将水印嵌入过程视为一种代数指纹的生成与恢复过程：
*   **身份代数化**：将 $n$ 位的秘密身份 $S$ 表示为一个有限域 $\mathbb{F}_q$ 上的 Reed-Solomon 多项式 $f_S(x)$。
*   **局部嵌入逻辑**：
    1.  **确定评估点**：对于文本中相邻的 Token 对 $(t_i, t_{i+1})$，利用密钥 $K$ 和第一个 Token $t_i$ 的哈希值计算出一个代数评估点 $\alpha_i = H(K, id(t_i)) \pmod q$。
    2.  **生成水印位**：计算多项式在点 $\alpha_i$ 处的奇偶性 $w_i = f_S(\alpha_i) \pmod 2$。
    3.  **偏置采样**：在生成第二个 Token $t_{i+1}$ 时，根据 $w_i$ 的值（0 或 1）对 Token 池进行偏置采样（类似绿名单/红名单机制）。
*   **免同步恢复**：检测者只需收集文本中所有现存的 Token 对，重新计算评估点和观察到的奇偶位。由于每个约束是自包含的，恢复过程不依赖 Token 的绝对位置。
*   **分段技术（Fragmentation）**：为了处理长身份并降低计算复杂度，将长身份切分为多个短片段，通过哈希值随机选择片段索引进行嵌入。
*   **扩散模型扩展**：针对非自回归的扩散模型，提出了 **Basic、Refined 和 Sliding** 三种“提交（Commit）”算法，通过迭代优化使文本局部满足代数约束。

### 3. 实验设计
论文的评估侧重于**数学建模与概率分析**，而非单纯的工程实验：
*   **信道建模**：将文本编辑（插入、删除、替换）统一建模为**二进制对称信道（BSC）**，通过交叉概率 $p$ 来衡量损坏程度。
*   **基准对比**：对比了不同恢复算法（穷举搜索 vs. 中途相遇法）的复杂度，并与传统的基于块的水印逻辑进行了鲁棒性对比分析。
*   **场景模拟**：模拟了 32 位、64 位和 128 位身份在不同错误率（$p=0.001$ 到 $0.3$）下的恢复成功率。
*   **扩散模型模拟**：在不同序列长度（32 到 1024 Token）下模拟三种提交算法的收敛速度。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 训练时长或型号，因为该方案主要应用于推理阶段的采样偏置，不涉及模型重训练。
*   **计算开销分析**：详细讨论了恢复算法的内存需求。例如，使用“中途相遇法”恢复 60 位身份约需 9-17 GB 内存，这在普通工作站上即可完成。

### 5. 实验数量与充分性
*   **理论验证充分**：论文通过大量的概率推导（如定理 1-4）证明了方案的有效性，并给出了精确的二项分布和泊松分布近似公式。
*   **数值模拟**：提供了多组表格（Table 2-4, 6）展示不同参数下的冗余需求和收敛迭代次数。
*   **局限性**：缺乏在真实大规模语料库（如 C4, WikiText）上对文本质量（困惑度 Perplexity）影响的实测数据，实验更偏向于信息论和编码理论的验证。

### 6. 主要结论与发现
*   **极低冗余需求**：即使在 10% 的 Token 损坏率下，恢复一个 32 位身份仅需约 41 个 Token 对，冗余极小。
*   **鲁棒性极强**：由于不依赖同步，该方案对句子重排序和随机增删具有天然的免疫力。
*   **扩散模型可行性**：证明了扩散模型可以通过局部一致性约束在 $O(\log N)$ 或 $O(N)$ 步内嵌入水印。
*   **算法权衡**：中途相遇法能有效处理 60 位以内的身份；更长的身份需通过分段技术解决。

### 7. 优点
*   **数学优雅性**：将水印问题转化为 Reed-Solomon 码的奇偶校验问题，提供了严谨的成功率保证。
*   **真正的免同步**：彻底解决了文本改写导致水印失效的痛点。
*   **灵活性**：既支持短身份的快速识别，也支持通过分段嵌入长达 128 位以上的复杂指纹。

### 8. 不足与局限
*   **文本质量评估缺失**：未量化偏置采样对文本流畅度和语义准确性的具体影响。
*   **哈希碰撞风险**：虽然概率极低，但在极短文本中，重复的 Token 可能会导致有效观察点不足。
*   **计算瓶颈**：对于未经分段的长身份，恢复算法的计算复杂度随位数呈指数增长。
*   **应用限制**：主要针对 Token 级别的嵌入，对于大幅度的语义重写（完全改变用词）的抵抗力仍需进一步实测。

（完）
