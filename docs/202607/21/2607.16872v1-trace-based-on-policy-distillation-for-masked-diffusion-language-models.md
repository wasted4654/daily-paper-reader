---
title: Trace-Based On-Policy Distillation for Masked Diffusion Language Models
title_zh: 基于轨迹的掩码扩散语言模型同策略蒸馏
authors: "Haolin Ren, Ziyang Huang, Chenhao Yuan, Jun Zhao, Kang Liu"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16872v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散大语言模型作为自回归序列生成的替代方案
tldr: 针对扩散大语言模型（dLLM）在推理任务中微调困难的问题，本文提出了一种基于轨迹的同策略蒸馏框架（TOPD）。该方法通过让目标模型在自身的去噪轨迹上学习教师模型的知识，利用逆向KL散度进行优化，避免了强化学习中复杂的奖励估计。实验表明，TOPD在数学推理任务上达到了与强化学习相当的性能，且训练效率提升了约96倍，为dLLM的后训练提供了一种高效的新路径。
selection_source: fresh_fetch
motivation: 传统的监督微调存在离策略偏差，而强化学习依赖稀疏奖励，限制了扩散大语言模型在复杂推理任务中的表现。
method: 提出TOPD框架，通过采样目标模型的同策略去噪轨迹，并利用教师模型在对应中间状态下的分布进行逆向KL散度指导。
result: 在数学推理基准测试中，TOPD以极低的计算成本实现了与强化学习相当的精度，训练速度提升约96倍。
conclusion: TOPD证明了同策略蒸馏是提升扩散模型推理能力的一种高效且强大的替代强化学习的方案。
---

## 摘要
扩散大语言模型 (dLLMs) 是自回归生成的一种极具前景的替代方案。然而，针对 dLLMs 的面向推理的后训练仍然具有挑战性。dLLMs 的有监督微调 (SFT) 需要密集但通常是异策略 (off-policy) 的掩码状态，而强化学习 (RL) 则依赖于稀疏奖励或价值建模。本文提出了基于轨迹的同策略蒸馏 (TOPD)，这是一种教师监督框架，可在无需奖励估计的情况下将推理能力迁移到目标 dLLM。其核心思想是在 dLLM 自身的去噪轨迹上对其进行监督，重点关注形成最终响应的轨迹对齐 Token 决策。具体而言，TOPD 从目标 dLLM 中采样同策略扩散轨迹，从教师模型获取对应部分去噪状态下的教师 Token 分布，并使用 Token 级的逆向库尔贝克-莱布勒 (Reverse-KL) 目标函数更新目标 dLLM。这种设计在保留密集教师监督的同时，使训练与模型自身的去噪状态保持一致。在数学推理基准测试中，TOPD 使 SDAR-4B-Chat 的 MATH500 准确率达到了与其强化学习训练的对应模型 TraDo-4B-Instruct 相当的水平，在静态评估下提升了 +5.7，在动态评估下提升了 +4.5。与强化学习训练的对应模型相比，TOPD 实现这一目标所需的展开 (rollout) 轮数减少了 4 倍，相当于估计 96.0 倍的达到目标准确率的模型计算加速。

## Abstract
Diffusion large language models (dLLMs) are a promising alternative to autoregressive generation. However, reasoning-oriented post-training for dLLMs remains challenging. Supervised fine-tuning (SFT) for dLLMs requires dense but often off-policy masked states, while reinforcement learning (RL) relies on sparse rewards or value modeling. This paper proposes \textbf{trace-based on-policy distillation (TOPD)}, a teacher-supervised framework that transfers reasoning ability to a target dLLM without reward estimation. The key idea is to supervise a dLLM on its own denoising trajectory, focusing on the trace-aligned token decisions that form the final response. Specifically, TOPD samples on-policy diffusion trajectories from the target dLLM, obtains teacher token distributions from a teacher model on the corresponding partially denoised states, and updates the target dLLM with a token-level Reverse Kullback-Leibler (Reverse-KL) objective. This design preserves dense teacher supervision while aligning training with the model's own denoising states. On mathematical reasoning benchmarks, TOPD enables SDAR-4B-Chat to match the MATH500 accuracy of its RL-trained counterpart TraDo-4B-Instruct, with gains of +5.7 under static evaluation and +4.5 under dynamic evaluation. Compared with the RL-trained counterpart, TOPD achieves this with 4$\times$ fewer rollout rounds, corresponding to an estimated 96.0$\times$ to-accuracy model-compute speedup.

---

## 论文详细总结（自动生成）

### 论文总结：基于轨迹的同策略蒸馏（TOPD）

#### 1. 核心问题与整体含义
*   **研究背景**：扩散大语言模型（dLLM）作为自回归模型的替代方案，在非自回归生成领域展现了潜力。然而，如何通过后训练（Post-training）提升其推理能力仍是难题。
*   **核心痛点**：
    *   **SFT（监督微调）**：通常是离策略的，训练状态与推理时的去噪轨迹不一致，导致“暴露偏差”。
    *   **RL（强化学习）**：依赖稀疏奖励，信用分配（Credit Assignment）困难，且需要维护价值模型，计算开销巨大。
*   **研究目标**：提出一种高效的蒸馏框架，在不依赖奖励估计的情况下，将强教师模型的推理能力迁移到学生 dLLM 中。

#### 2. 方法论
*   **核心思想**：**基于轨迹的同策略蒸馏（TOPD）**。让学生模型在自己生成的去噪轨迹上学习，并由教师模型提供密集的 Token 级指导。
*   **关键技术细节**：
    1.  **同策略采样（On-Policy Sampling）**：从学生模型当前的策略中采样完整的去噪轨迹，而非使用预定义的静态掩码状态。
    2.  **轨迹对齐决策选择（Trace-Aligned Selection）**：在扩散过程中，并非所有预测都是最终结果。TOPD 仅针对那些最终保留在输出响应中的 Token 决策进行监督，过滤掉中间被修正的无效步骤。
    3.  **教师分布匹配**：在学生经历的每一个中间去噪状态下，调用教师模型获取其 Token 概率分布。
    4.  **逆向 KL 散度（Reverse-KL）优化**：使用 Reverse-KL 目标函数最小化学生与教师分布的差异。相比 Forward-KL，它能让学生模型更专注于教师偏好的高概率模式，在噪声状态下表现更稳健。

#### 3. 实验设计
*   **数据集**：MATH500、AIME2024、GSM8K（主要聚焦数学推理）。
*   **模型设置**：
    *   **学生模型**：SDAR-4B-Chat（块注意力扩散模型）、LLaDA-8B-Instruct（全注意力扩散模型）。
    *   **教师模型**：TraDo-8B-Instruct、ESPO-trained LLaDA-8B。
*   **对比方法**：
    *   **SFT**：半自回归监督微调。
    *   **RL 方法**：TraceRL（基于轨迹的强化学习）、ESPO（针对扩散模型的偏好优化）。
*   **评估指标**：静态评估（Static）与动态评估（Dynamic）下的准确率。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100-80G 或 A800 GPU**。
*   **训练时长**：
    *   SDAR-4B 模型的训练在 30 个 Rollout 步内即可达到高性能，总耗时约 **5 小时**。
    *   相比 RL 方法（如 TraceRL），达到相同精度所需的 Rollout 轮数减少了 4 倍。
*   **效率提升**：论文估算在达到相同准确率的情况下，TOPD 相比 RL 方法实现了约 **96.0 倍的计算加速**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种主流 dLLM 架构（Block-attention 和 Full-attention）。
    *   进行了详尽的**消融实验**，包括：同策略 vs 异策略、轨迹对齐 vs 随机掩码、以及不同散度函数（Forward-KL, Reverse-KL, JSD）的对比。
*   **充分性评价**：实验设计客观且公平。通过在多个推理基准上与最先进的 RL 流程进行对比，充分证明了该方法在效率和性能上的双重优势。

#### 6. 主要结论与发现
*   **性能卓越**：TOPD 在 MATH500 上的表现匹配或超过了经过复杂 RL 训练的模型。
*   **效率极高**：通过密集教师监督替代稀疏奖励，极大地缩短了收敛时间并降低了算力需求。
*   **轨迹的重要性**：实验证明，随机掩码会破坏推理的因果依赖，而基于轨迹的监督能显著提升模型在动态解码环境下的鲁棒性。

#### 7. 优点
*   **算法简洁**：无需奖励模型、价值模型或复杂的 PPO 策略裁剪机制，易于实现。
*   **解决暴露偏差**：通过同策略采样，确保训练状态与推理状态分布一致。
*   **计算友好**：单次 Rollout 仅需一次教师前向传播和一次学生更新，适合资源有限的团队。

#### 8. 不足与局限
*   **教师依赖性**：学生模型的最终表现高度依赖于教师模型的质量，若教师模型在某些领域存在偏差，学生也会继承。
*   **任务局限**：目前主要在数学推理任务上验证，尚未在代码生成、多轮对话或长文本创作等更广泛的领域进行大规模测试。
*   **规模效应**：实验主要在 4B-8B 规模模型上进行，对于百亿或千亿级参数模型的扩展性仍需进一步观察。

（完）
