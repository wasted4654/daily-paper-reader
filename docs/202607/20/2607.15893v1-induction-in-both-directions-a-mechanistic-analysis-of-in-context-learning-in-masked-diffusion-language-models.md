---
title: "Induction in Both Directions: A Mechanistic Analysis of In-Context Learning in Masked Diffusion Language Models"
title_zh: 双向归纳：掩码扩散语言模型中上下文学习的机制分析
authors: "Andy Catruna, Emilian Radoi"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15893v1"
tags: ["query:ardm"]
score: 9.0
evidence: 扩散语言模型与自回归Transformer的机制分析
tldr: 本研究深入探讨了扩散语言模型（DLM）实现上下文学习的内部机制，重点分析了其归纳（Induction）能力。通过对比自回归模型与吸收掩码DLM，研究者发现DLM构建了双向归纳电路，能够同时利用过去和未来的上下文进行预测。此外，研究还揭示了DLM在没有显式时间步嵌入的情况下，能通过计算全局掩码比例作为隐式时间步。这一发现为理解非自回归生成模型的机理提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在揭示新兴的扩散语言模型在处理上下文学习时，其内部归纳机制与传统自回归模型的异同。
method: 采用机械论分析方法，对比研究了架构匹配的自回归模型与吸收掩码扩散模型在归纳任务中的表现。
result: 实验证明DLM具备对称的双向归纳电路，且在双向上下文可见时表现更强，并能利用掩码比例作为隐式时间步。
conclusion: DLM通过双向上下文访问和隐式时间步感知，实现了不同于自回归模型的、更具对称性的上下文学习机制。
---

## 摘要
虽然自回归（AR）Transformer 的内部机制已得到广泛研究，但对于扩散语言模型（DLM）的了解却少得多，这是一种通过迭代去噪生成文本的新兴替代方案。在这项工作中，我们研究了 DLM 如何实现归纳（induction），这是上下文学习背后的一种机制，即模型寻找重复的上下文并复制其后的 token。我们的分析对比了架构匹配的仅注意力 AR 模型和吸收掩码（absorbing-mask）DLM。我们发现 DLM 学习到了一个双向归纳电路，其中前一个 token（previous-token）和后一个 token（next-token）头将局部上下文写入残差流，随后的归纳头利用这些信息从匹配的源位置寻找并复制答案。该电路具有方向对称性，无论源出现在过去还是未来都能发挥作用。当只有左侧上下文可见时（与 AR 模型所见一致），DLM 在归纳能力上并不优于其对应的 AR 模型。然而，我们观察到当被掩码 token 的两侧都可见时，它具有更强的归纳能力，这表明其优势在于双向上下文访问，而非更强的一侧机制。除了归纳之外，我们还提供了因果证据，表明 DLM 会计算被掩码 token 的全局比例，并将其作为隐式时间步长使用，即使没有给定显式的时间步嵌入。

## Abstract
While the internal mechanisms of autoregressive (AR) transformers have been studied extensively, much less is known about diffusion language models (DLMs), an emerging alternative that generates text by iterative denoising. In this work, we study how DLMs implement induction, a mechanism behind in-context learning in which the model finds a repeated context and copies the token that followed it. Our analysis compares attention-only AR models and absorbing-mask DLMs with matched architectures. We find that DLMs learn a bidirectional induction circuit, where previous-token and next-token heads write local context into the residual stream and later induction heads use it to find and copy the answer from the matching source position. The circuit is direction-symmetric, working whether the source appears in the past or in the future. When only left context is visible, matching what an AR model sees, the DLM does not outperform its AR counterpart in induction capabilities. However, we observe it has stronger induction when both sides of the masked token are visible, pointing to bidirectional context access rather than a stronger one-sided mechanism. Beyond induction, we provide causal evidence that DLMs compute the global fraction of masked tokens and use it as an implicit timestep, even though they are given no explicit timestep embedding.

---

## 论文详细总结（自动生成）

这篇论文对掩码扩散语言模型（DLM）的上下文学习机制进行了深入的机械论分析（Mechanistic Analysis），并将其与传统的自回归（AR）模型进行了对比。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：自回归（AR）Transformer 的内部机制（如归纳头 Induction Heads）已被广泛研究，但扩散语言模型（DLM）作为一种通过迭代去噪生成文本的新兴架构，其内部如何实现上下文学习（ICL）尚不明确。
*   **核心问题**：DLM 是否像 AR 模型一样通过“归纳”机制来复制重复出现的上下文？其双向注意力的特性如何改变这一过程？

### 2. 方法论
*   **核心思想**：通过训练架构完全匹配（仅注意力机制、层数相同）的 AR 模型和吸收掩码（Absorbing-mask）DLM，在受控环境下对比两者的归纳行为。
*   **关键技术细节**：
    *   **双向归纳电路**：识别出 DLM 中的两阶段电路。第 0 层（Layer 0）的“邻近头”（Neighbour heads）负责将当前掩码位置左侧（Previous-token）和右侧（Next-token）的上下文信息写入残差流；第 1 层（Layer 1）的“归纳头”利用这些信息匹配源位置并复制答案。
    *   **机械论分析工具**：使用**均值消融（Mean Ablation）**验证各头的因果作用；通过 **QK/OV 分解**分析权重如何实现匹配和复制；利用**线性探测（Linear Probing）**检测残差流中的信息。
    *   **隐式时间步**：研究 DLM 如何在没有显式时间步嵌入的情况下，感知当前的去噪进度。

### 3. 实验设计
*   **数据集/场景**：使用随机 Token 组成的重复序列（前 256 个 Token 随机生成并重复一次，总长 512）。这种设计排除了语义和频率干扰，迫使模型必须依赖上下文。
*   **Benchmark**：对比了深度为 1、2、3 层的 AR 模型与 DLM。
*   **对比方法**：
    *   **前向归纳（Forward Induction）**：源在过去，掩码在未来（AR 和 DLM 均适用）。
    *   **反向归纳（Reverse Induction）**：源在未来，掩码在过去（仅 DLM 适用）。
    *   **上下文限制实验**：限制 DLM 可见的局部窗口大小，观察其在仅有左侧上下文时与 AR 模型的表现差异。

### 4. 资源与算力
*   **模型配置**：$d_{model}=512$，16 个注意力头，$d_{head}=32$，序列长度 512。
*   **训练细节**：在 FineWeb-Edu 数据集上训练 250k 步，Batch Size 为 128，使用 AdamW 优化器。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但考虑到模型规模较小（1-3 层注意力模型），所需的算力资源相对较低，属于典型的机械论解释性研究的实验规模。

### 5. 实验数量与充分性
*   **实验规模**：针对每种架构和深度训练了 3 个独立随机种子，以确保结果的可重复性。
*   **充分性**：实验涵盖了行为分析（归纳得分）、因果消融（证明电路必要性）、权重分解（解释计算逻辑）以及冲突提示词实验（验证双向路径的独立性）。
*   **客观性**：通过折叠 LayerNorm 后的模型进行分析，确保了残差流分解的数学严谨性，实验设计较为客观、公平。

### 6 主要结论与发现
*   **双向对称性**：DLM 学习到了对称的归纳电路，处理“过去”和“未来”的重复信息能力几乎一致。
*   **性能来源**：DLM 在归纳任务上的优势并非源于更强的单向机制，而是源于其能同时访问双向上下文。在仅限左侧上下文时，DLM 表现不如 AR 模型。
*   **隐式时间步感知**：DLM 内部会计算全局掩码比例（Mask Fraction），并将其作为隐式时间步来调节预测的熵（确定性）。通过干预实验证明，这一特征具有因果影响力。
*   **相位变化**：归纳能力在训练过程中是突然涌现的（约 8k-24k 步），且 DLM 的涌现通常早于 AR 模型。

### 7. 优点
*   **因果证据充分**：不仅观察到了相关性，还通过消融和路径干预提供了电路功能的因果证明。
*   **揭示了隐式机制**：首次证明了 DLM 可以在没有显式输入的情况下，通过注意力机制自发计算并利用全局掩码率。
*   **受控对比严谨**：通过匹配架构消除了模型容量差异带来的干扰。

### 8. 不足与局限
*   **模型规模较小**：实验基于小型（最多 3 层）且仅含注意力的模型，尚不确定这些结论在包含 MLP 层的大规模 DLM（如 LLaDA, DiffusionGemma）中是否完全成立。
*   **任务单一**：主要关注合成的随机 Token 任务，在自然语言语义理解中的归纳机制可能更复杂。
*   **架构限制**：仅研究了吸收掩码 DLM，对于高斯噪声或其他类型的扩散模型（如 Uniform-noise）可能不适用。

（完）
