---
title: Hierarchical Denoising For Multi-Step Visual Reasoning
title_zh: 多步视觉推理的分层去噪
authors: "Zezhong Qian, Xiaowei Chi, Chak-Wing Mak, Tianze Zhou, Ruibin Yuan, Yuhan Rui, Hengzhe Sun, Zhuoqun Wu, Yuming Li, Siyuan Qian, Sirui Han, Shanghang Zhang"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15278v1"
tags: ["query:ardm"]
score: 9.0
evidence: 将分层潜变量集成到因果视频生成中，用于流式自回归扩散
tldr: 针对视频模型在多步视觉推理中缺乏逻辑一致性且推理延迟高的问题，本文提出HDR框架。该框架通过层次化潜变量结构实现由粗到精的推理，结合稀疏层次注意力机制（SHAP），在保持低延迟流式输出的同时显著提升了全局规划能力。在涵盖迷宫、汉诺塔等六类任务的新基准测试中，HDR大幅超越了自回归扩散模型，并在机器人实验中展现了物理交互潜力。
selection_source: fresh_fetch
motivation: 现有的视频生成模型在处理复杂多步推理时，难以兼顾逻辑一致性、全局规划能力与低延迟推理。
method: 提出HDR框架，将视频潜变量组织为树状层次结构，通过粗层进行全局规划并逐步细化，并利用SHAP减少计算开销。
result: "在多步推理基准测试中，HDR将成功率从34.22%提升至60.29%，推理速度比双向扩散模型快54.2倍。"
conclusion: HDR成功实现了高效且逻辑一致的多步视觉推理，为视频基础模型在复杂任务规划和物理世界建模方面提供了新方案。
---

## 摘要
视频模型正在演变为视觉基础模型，但仍缺乏类人的多步推理能力。流式自回归扩散模型虽然高效，但在推理能力上受限；而双向扩散模型虽然支持全局修正，但由于密集的帧级去噪，推理成本很高。这两种范式在处理复杂推理任务时，都难以同时实现逻辑一致性和低延迟流式输出。我们提出了 HDR（分层去噪视觉推理），这是一个将分层潜变量集成到因果视频生成中以进行多步推理的统一框架。HDR 将视频潜变量组织成树状层级结构，在流式输出前实现从粗到细的推理。粗去噪层保留不确定的假设以进行全局规划，而细去噪层则逐步将其细化为具体的视觉状态。一种稀疏分层注意力模式（SHAP）进一步降低了时间注意力成本。我们引入了一个包含分布外案例的等级分层多步视频推理基准，涵盖六个任务：迷宫导航、汉诺塔、一笔画、数字推盘、推箱子和倒水。与流式自回归扩散基准相比，HDR 将成功率从 34.22 提高到 60.29（相对提升 76.2%），并将平均进度从 76.00 提高到 89.56，展示了更一致的推理轨迹。HDR 保持了每个潜变量 0.70 秒的低延迟流式传输，推理速度比双向扩散快 54.2 倍。在仅使用 2% 训练数据的情况下，它仍能保留 82.9% 的全量数据性能，而双向扩散仅为 52.0%。真实世界的机器人实验进一步证明了 HDR 在物理交互和世界建模方面的潜力。项目演示：https://hierarchical-diffusion-reasoning.github.io/。

## Abstract
Video models are evolving into vision foundation models, yet they still lack human-like multi-step reasoning. Streaming autoregressive diffusion models are efficient but limited in reasoning, while bidirectional diffusion enables global revision with high inference costs due to dense frame-level denoising. Both paradigms struggle to achieve logical consistency and low-latency streaming for complex reasoning tasks. We propose HDR (Hierarchical Denoising for Visual Reasoning), a unified framework that integrates hierarchical latents into causal video generation for multi-step reasoning. HDR organizes video latents into a tree-structured hierarchy, enabling coarse-to-fine reasoning before streaming output. Coarse denoising layers preserve uncertain hypotheses for global planning, while finer layers progressively refine them into concrete visual states. A sparse hierarchical attention pattern (SHAP) further reduces temporal attention costs. We introduce a level-stratified multi-step video reasoning benchmark with out-of-distribution cases, covering six tasks: maze navigation, Tower of Hanoi, one-line drawing, sliding puzzle, Sokoban, and water pouring. Compared with streaming autoregressive diffusion baselines, HDR improves success from 34.22 to 60.29 (76.2% relative gain) and increases average progress from 76.00 to 89.56, demonstrating more consistent reasoning trajectories. HDR maintains low-latency streaming at 0.70 seconds per latent, achieving 54.2 times faster inference than bidirectional diffusion. It also retains 82.9% of full-data performance with only 2% training data, compared with 52.0% for bidirectional diffusion. Real-world robot experiments further demonstrate HDR's potential for physical interaction and world modeling. Project demo: https://hierarchical-diffusion-reasoning.github.io/.

---

## 论文详细总结（自动生成）

这篇论文提出了 **HDR (Hierarchical Denoising for Visual Reasoning)**，旨在解决视频生成模型在处理复杂多步视觉推理任务时，难以兼顾“逻辑一致性”与“低延迟推理”的难题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频生成模型在执行多步视觉推理（如解迷宫、拼图）时存在瓶颈。
    *   **流式自回归扩散模型 (Streaming AR Diffusion)**：虽然推理速度快（低延迟），但缺乏全局规划能力，容易在长序列中产生逻辑漂移或步入死胡同。
    *   **双向扩散模型 (Bidirectional Diffusion)**：虽然能通过全局去噪保持一致性，但计算成本极高，无法实现流式输出，推理延迟随视频长度剧增。
*   **研究动机**：人类在解决复杂问题时通常先进行粗略的全局规划，再细化具体步骤。论文希望模仿这种“由粗到精”的思维方式，构建一个既能全局规划又能高效流式输出的视觉推理框架。

### 2. 方法论
HDR 框架的核心是将视频潜变量组织为**树状层次结构**，并结合**分层去噪**机制：
*   **分层潜变量结构**：将视频序列表示为多层树状结构。顶层（粗层）代表抽象的全局状态或关键帧，底层（细层）代表具体的视觉细节。
*   **由粗到精的去噪 (Coarse-to-Fine Denoising)**：
    1.  首先对高层潜变量进行去噪，这些层负责保留不确定的假设并进行全局路径规划。
    2.  随后，高层的信息作为条件引导低层潜变量的去噪，逐步将抽象规划细化为具体的视觉状态。
*   **稀疏分层注意力机制 (SHAP)**：为了降低计算开销，设计了 SHAP 模式。它允许节点仅与其父节点、同级节点及前序上下文交互，将传统自注意力的复杂度显著降低，从而支持长序列的流式生成。
*   **因果推理集成**：将上述机制集成到自回归框架中，使得模型在生成当前帧时已经具备了对未来步骤的“粗略预见”。

### 3. 实验设计
*   **数据集与 Benchmark**：引入了一个全新的**等级分层多步视频推理基准**，包含 6 类具有挑战性的逻辑任务：
    *   迷宫导航 (Maze)、汉诺塔 (Tower of Hanoi)、一笔画 (One-line drawing)、数字推盘 (Sliding puzzle)、推箱子 (Sokoban)、倒水问题 (Water pouring)。
*   **对比方法**：
    *   **Streaming AR Diffusion**（流式自回归扩散基准）。
    *   **Bidirectional Diffusion**（双向全局扩散模型）。
*   **评估指标**：成功率 (Success Rate, SR)、平均进度 (Average Progress, AP)、推理延迟 (Latency)。

### 4. 资源与算力
*   **算力说明**：论文摘要和提取文本中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **推理效率数据**：提到了推理性能指标，即每个潜变量的流式传输延迟为 **0.70 秒**，推理速度比双向扩散模型快 **54.2 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种不同的逻辑推理任务，并设置了分布外 (OOD) 案例来测试泛化性。
*   **消融实验**：进行了充分的消融研究，验证了分层结构和 SHAP 机制的有效性。
*   **数据效率实验**：特别测试了在极少量数据（仅 2% 训练集）下的表现，HDR 仍能保持 82.9% 的性能，证明了其强大的学习效率。
*   **真实世界验证**：通过机器人抓取和移动实验，验证了模型在物理世界建模和交互中的潜力。
*   **评价**：实验设计全面，涵盖了从模拟逻辑到物理交互的多个维度，对比基准清晰，结果具有统计说服力。

### 6. 主要结论与发现
*   **性能飞跃**：HDR 将多步推理的成功率从 34.22% 提升至 **60.29%**（相对提升 76.2%）。
*   **逻辑一致性**：平均进度从 76.00 提升至 89.56，表明 HDR 生成的推理轨迹更少中断，逻辑更连贯。
*   **高效性**：在保持高成功率的同时，实现了极低的流式延迟，解决了全局规划与实时输出的矛盾。
*   **小样本能力**：相比传统模型，HDR 对训练数据的依赖更低，展现出更强的结构化先验。

### 7. 优点
*   **架构创新**：巧妙地将树状分层结构引入扩散模型，模拟了人类“先规划后执行”的认知模式。
*   **平衡性极佳**：在推理精度（逻辑性）和推理速度（实用性）之间找到了极好的平衡点。
*   **通用性**：不仅适用于逻辑游戏，还能扩展到机器人控制等物理世界任务。
*   **计算优化**：SHAP 机制有效解决了长视频生成的内存和计算瓶颈。

### 8. 不足与局限
*   **任务类型限制**：目前的基准测试主要集中在具有明确规则的逻辑任务上，对于高度非结构化或开放域的视觉推理（如复杂社交互动推理）尚待验证。
*   **分层深度敏感性**：树状结构的层数和分支因子可能需要针对不同复杂度的任务进行手动调优，缺乏完全自动化的结构演化机制。
*   **误差累积风险**：虽然引入了分层修正，但作为自回归框架，在极长序列中仍可能存在顶层规划失误导致底层全面崩溃的风险。

（完）
