---
title: "Sangam: Efficiently Serving Diffusion LLMs with the AR Stack"
title_zh: Sangam：利用 AR 技术栈高效服务扩散大语言模型
authors: "Nitin Kedia, Saurabh Agarwal, Myungjin Lee, Aditya Akella"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04206v1"
tags: ["query:ardm"]
score: 8.0
evidence: 使用自回归堆栈机制服务扩散语言模型
tldr: 扩散语言模型（dLLM）因双向注意力和迭代去噪特性，难以直接应用自回归推理优化技术。本文提出 Sangam 推理系统，通过引入赤字令牌预算调度器实现摊销后的无停顿调度，并采用混合服务策略动态平衡预填充与解码负载。实验表明，Sangam 在不同负载下均能显著降低延迟，有效解决了 dLLM 推理中的预填充-解码干扰和资源分配问题，提升了服务效率。
selection_source: fresh_fetch
motivation: 扩散语言模型的双向注意力和重复预填充结构使得传统的自回归推理优化机制无法直接应用，导致推理效率受限。
method: 提出 Sangam 系统，采用赤字令牌预算调度器优先保障解码任务，并结合混合服务策略将溢出的预填充任务分配至解码节点。
result: "在不同负载下，Sangam 相比现有方案可降低 8-20% 的平均延迟，有效缓解了预填充与解码之间的资源竞争。"
conclusion: 通过针对性地优化调度算法和资源分配策略，可以显著提升扩散语言模型在实际生产环境中的推理服务性能。
---

## 摘要
扩散大语言模型 (dLLMs) 通过对掩码响应进行迭代去噪来生成文本，并且每次模型调用可以确定多个输出位置。由于确定一个位置会改变所有其他位置的 KV 激活值，其双向注意力机制阻止了精确的自回归式 KV 缓存。诸如 Fast-dLLM 和 dKV-Cache 等近似缓存技术会重复刷新 KV 激活值并在中间的解码过程中重用它们，从而引发了重复的预填充/解码（prefill/decode）结构。这使得自回归 (AR) 服务机制与 dLLMs 相关，但无法直接应用。dLLM 的解码是块大小而非标记大小的，预填充会循环发生，且双向注意力机制排除了用于无停顿同机服务的分块预填充机制。我们提出了 Sangam，一个用于缓存式 dLLM 推理的服务系统。Sangam 引入了一种赤字标记预算调度器，优先准入正在进行的解码，仅在累积的标记预算允许时才准入完整的不可分割预填充，并将未使用的预算向后结转。这实现了摊销后的无停顿调度。解耦服务避免了预填充与解码之间的干扰，但面临预填充/解码资源划分问题。Sangam 采用混合服务策略，将溢出的预填充任务分配到解码工作节点上以缓解预填充资源配置不足，并使用相同的赤字预算调度器来保护这些节点的解码任务不受溢出影响。我们表明，与 AR 服务类似，dLLM 服务的设计空间受预填充-解码干扰和预填充/解码划分的支配。同机服务在解码密集型工作负载上最有效，在 LLaDA-8B ShareGPT 上相比混合执行将平均延迟降低了 9-20%；而混合执行在预填充密集型工作负载上最有效，在 Dream-7B arXiv 上相比同机执行将平均延迟降低了 8-20%。Sangam 已在 https://github.com/UT-InfraAI/sangam 开源。

## Abstract
Diffusion language models (dLLMs) generate text by iteratively denoising a masked response and can commit multiple output positions per model invocation. Their bidirectional attention prevents exact autoregressive-style KV caching, since committing one position shifts the KV activations of all others. Approximate caching techniques such as Fast-dLLM and dKV-Cache refresh KV activations repeatedly and reuse them across intervening decodes, inducing a repeated prefill/decode structure. This makes AR serving mechanisms relevant to dLLMs, but not directly applicable. dLLM decodes are block-sized rather than token-sized, prefills recur, and bidirectional attention precludes the chunked prefill mechanism used for stall-free colocated serving. We present Sangam, a serving system for cached dLLM inference. Sangam introduces a deficit token-budget scheduler that admits in-flight decodes first, admits whole indivisible prefills only when the accumulated token budget allows, and carries unused budget forward. This achieves amortized stall-free scheduling. Disaggregated serving avoids prefill-decode interference but suffers from prefill/decode resource partitioning problem. Sangam adopts a hybrid serving strategy, overflowing prefills onto decode workers to relieve prefill under-provisioning, and uses the same deficit-budget scheduler to protect those workers' decodes from the overflow. We show that like AR serving, dLLM serving design space is governed by prefill-decode interference and prefill/decode partitioning. Colocated serving is most effective on decode-heavy workloads, cutting mean latency by 9-20% over hybrid execution on LLaDA-8B ShareGPT; while hybrid execution is most effective on prefill-heavy workloads, cutting mean latency by 8-20% over colocated execution on Dream-7B arXiv. Sangam is available at https://github.com/UT-InfraAI/sangam.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Sangam**，这是一个专门为扩散大语言模型（dLLM）设计的推理服务系统。它通过借鉴并改造自回归（AR）模型的技术栈，解决了 dLLM 在并发请求下的推理效率问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散大语言模型（如 LLaDA, Dream）通过迭代去噪生成文本，虽然单请求速度快，但在多并发请求的服务场景下效率低下。
*   **研究动机**：
    *   **KV 缓存失效**：dLLM 使用双向注意力机制，任何位置的更新都会改变全局 KV 激活值，导致传统的 AR 式 KV 缓存无法直接应用。
    *   **重复预填充**：虽然近期有近似缓存技术（如 Fast-dLLM），但它们引入了“预填充-多次解码-再预填充”的循环结构，增加了计算负担。
    *   **现有技术不匹配**：AR 模型的优化手段（如分块预填充 Chunked Prefill）依赖因果掩码，在 dLLM 的双向注意力下失效，导致预填充任务不可分割，容易造成严重的解码停顿（Stall）。

### 2. 方法论：核心思想与关键技术
*   **赤字令牌预算调度器（Deficit Token-Budget Scheduler）**：
    *   **核心思想**：借鉴“赤字轮询”算法，在无法对预填充进行物理分块的情况下，通过时间维度的延迟来实现“摊销后的无停顿调度”。
    *   **机制**：为每个迭代设置令牌预算 $\tau$。优先处理在途的解码任务。只有当剩余预算加上之前累积的“赤字”（未使用的预算）足以容纳一个完整的预填充任务时，才准入该任务。
*   **混合服务策略（Hybrid Serving）**：
    *   **核心思想**：结合“同机服务（Colocated）”和“解耦服务（Disaggregated）”的优点。
    *   **机制**：设置专门的预填充节点池和解码节点池。当预填充池过载时，将任务“溢出”到解码节点（此时解码节点变为同机模式）。
    *   **保护机制**：在解码节点上同样运行赤字预算调度器，确保溢出的预填充任务不会过度干扰原有的解码任务。
*   **两级架构**：中央调度器负责全局负载均衡和路由，工作节点本地调度器负责微批次（Micro-batch）的构建。

### 3. 实验设计
*   **数据集/场景**：
    *   **ShareGPT**：模拟对话场景（短 Prompt，中等长度输出）。
    *   **arXiv Summarization**：模拟长文本摘要场景（长 Prompt，短输出）。
*   **Benchmark 与对比方法**：
    *   **Fast-dLLM**：作为基准（原始实现通常仅支持 Batch Size 1）。
    *   **Colocated**：所有 GPU 均运行预填充和解码。
    *   **Static Disaggregated**：固定的预填充/解码节点划分（如 5P3D）。
    *   **Sangam (Hybrid)**：本文提出的动态溢出混合模式。
*   **模型**：LLaDA-8B 和 Dream-7B。

### 4. 资源与算力
*   **硬件环境**：使用了一个 AWS p5.48xlarge 节点，包含 **8 块 NVIDIA H100 (80GB) GPU**。
*   **互联**：GPU 之间通过 NVLink 连接。
*   **软件栈**：基于 PyTorch、HuggingFace Transformers、PagedAttention 和 FlashInfer 内核实现。
*   **训练/测试时长**：文中未明确说明具体实验总时长，但提到每个 QPS 点位运行了 4000 个请求以获取统计数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种不同架构的模型（MHA 和 GQA）、两种截然不同的工作负载（解码密集型 vs 预填充密集型）、以及多种 QPS（每秒请求数）负载水平。
*   **消融实验**：对令牌预算 $\tau$ 的取值、预填充/解码节点比例、溢出阈值 $\theta$ 等关键超参数进行了详细的敏感性分析。
*   **评价**：实验设计较为充分，对比了多种服务模式，并深入分析了延迟（Mean/P99）和排队等待时间的分布，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **解码特性**：dLLM 的解码是块状的（Block-sized），计算量远大于 AR 模型，因此在 Batch Size 较小时即可达到 GPU 饱和。
*   **模式选择**：
    *   在**解码密集型**（如 ShareGPT）负载下，**同机服务**效果最好，平均延迟降低 9-20%。
    *   在**预填充密集型**（如 arXiv）负载下，**混合执行**最有效，平均延迟降低 8-20%。
*   **调度器作用**：赤字预算调度器能有效平衡吞吐量与尾部延迟，在不分块的情况下缓解了预填充对解码的干扰。

### 7. 优点
*   **创新性**：首次系统性地将 AR 推理优化技术迁移并适配到 dLLM 领域。
*   **实用性**：提出的“赤字预算”概念巧妙地解决了双向注意力模型无法分块预填充的痛点。
*   **灵活性**：混合服务架构能够自适应不同的工作负载，比静态的解耦方案更具鲁棒性。

### 8. 不足与局限
*   **算力规模限制**：实验主要集中在单节点（8 GPU）环境，虽然讨论了 KV 传输开销，但在更大规模的分布式集群（跨节点）中的表现仍需进一步验证。
*   **依赖近似缓存**：系统的增益在很大程度上依赖于 Fast-dLLM 等近似缓存算法的有效性，若未来出现不需要重复预填充的 dLLM 架构，该系统的部分设计可能需要调整。
*   **超参数敏感**：预算 $\tau$ 和溢出阈值 $\theta$ 需要根据具体模型和硬件进行调优，缺乏一种完全自动化的动态配置机制。

（完）
