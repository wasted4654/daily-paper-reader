---
title: "Flex-Forcing: Towards a Unified Autoregressive and Bidirectional Video Diffusion Model"
title_zh: Flex-Forcing：迈向统一的自回归与双向视频扩散模型
authors: "Xinyin Ma, Julius Berner, Chao Liu, Arash Vahdat, Weili Nie, Xinchao Wang"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03509v1"
tags: ["query:ardm"]
score: 9.0
evidence: 统一的自回归和双向视频扩散模型
tldr: 针对视频扩散模型中双向生成（全局一致但慢）与自回归生成（高效但长期一致性差）的权衡问题，本文提出 Flex-Forcing 统一框架。通过在时间轴和去噪步骤上引入灵活的分块机制，该模型能兼顾全局结构规划与局部高效合成，支持任意顺序和步数的生成，在提升视频质量和长视频稳定性的同时显著加快了推理速度。
selection_source: fresh_fetch
motivation: 现有的视频生成模型受限于固定的推理范式，难以在生成效率、全局一致性与推理灵活性之间取得平衡。
method: 提出一种在时间维度和去噪步数上共同定义的灵活分块机制，实现跨块的双向推理与块内的自回归生成。
result: 在多个基准测试中，Flex-Forcing 在视频质量、长视频稳定性和推理速度上均优于传统的固定推理调度基准。
conclusion: Flex-Forcing 成功统一了自回归与双向视频扩散模型，为灵活且高效的长视频生成提供了新的范式。
---

## 摘要
近期大规模生成模型的进展显著推动了视频生成的发展，但现有方法仍受限于僵化的推理范式。双向扩散模型在全局连贯性和视觉保真度方面表现出色，但推理速度较慢；而自回归模型虽然能提供高效的流式生成，却以牺牲长期一致性和面临曝光偏差为代价。我们提出了 Flex-Forcing，这是一个统一的训练与推理框架，使视频扩散模型能够在双向和自回归生成模式下无缝运行。其核心思想是一种在时间轴和去噪步骤上共同定义的灵活分块机制。该设计允许模型：(1) 根据不同的设备预算进行灵活分块；(2) 在块之间进行双向推理以进行全局结构规划，同时在每个块内自回归地生成帧，以实现高效且细粒度的合成；(3) 在没有严格因果约束的情况下，执行任意顺序、任意时间步的自回归生成。在多个视频生成基准测试上的广泛实验表明，与具有僵化推理方案的强基线模型相比，Flex-Forcing 在视频质量和长视频稳定性方面均表现更优，同时提供了更快的推理速度。

## Abstract
Recent progress in large-scale generative models has substantially advanced video generation, yet existing methods remain constrained by a rigid inference paradigm. Bidirectional diffusion models excel at global coherence and visual fidelity but suffer from slow inference, while autoregressive models offer efficient and streaming generation at the cost of long-range consistency and exposure bias. We introduce Flex-Forcing, a unified training and inference framework that enables a video diffusion model to seamlessly operate under both bidirectional and autoregressive generation regimes. The core idea is a flexible chunking mechanism jointly defined over the temporal axis and denoising steps. This design allows the model to (1) perform flexible chunking according to different device budgets, (2) perform bidirectional inference across chunks for global structure planning, while generating frames autoregressively within each chunk for efficient and fine-grained synthesis, and (3) perform any-order, any-timestep autoregressive generation without the strict causal constraint. Extensive experiments on multiple video generation benchmarks demonstrate that Flex-Forcing achieves consistently better video quality, long-video stability than strong baselines with a rigid inference schedule, while offering faster inference.

---

## 论文详细总结（自动生成）

以下是对论文《Flex-Forcing: Towards a Unified Autoregressive and Bidirectional Video Diffusion Model》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频生成扩散模型在推理范式上存在“两难”困境：
    *   **双向扩散模型（Bidirectional）**：虽然能保证全局连贯性和高视觉保真度，但推理速度慢，且难以处理超长视频。
    *   **自回归模型（Autoregressive）**：支持流式生成，效率高，但容易产生“曝光偏差”（Exposure Bias），导致长视频在后期出现内容漂移或不一致。
*   **研究动机**：打破僵化的推理模式，寻求一种既能保持全局结构一致性，又能实现高效局部合成的统一框架。

### 2. 论文提出的方法论：Flex-Forcing
*   **核心思想**：引入一种在**时间轴（Temporal Axis）**和**去噪步骤（Denoising Steps）**上共同定义的**灵活分块机制（Flexible Chunking）**。
*   **关键技术细节**：
    *   **跨块双向推理**：在视频的宏观结构上，模型采用双向推理进行全局规划，确保长程一致性。
    *   **块内自回归生成**：在每个时间块内部，采用自回归方式生成帧，以实现细粒度的高效合成。
    *   **灵活调度**：该机制允许根据设备的计算预算（如显存大小）动态调整分块大小。
    *   **非因果约束**：支持任意顺序、任意时间步的生成，打破了传统自回归模型必须严格遵守时间顺序的限制。
*   **训练策略**：通过 Flex-Forcing 训练框架，使模型能够适应不同的掩码模式（Masking Patterns）和噪声水平，从而在推理时无缝切换双向和自回归模式。

### 3. 实验设计
*   **数据集/场景**：论文在多个主流视频生成基准测试上进行了验证（注：摘要中提到“多个基准测试”，通常包含 UCF-101、SkyTimelapse 或 Taichi-HD 等，但具体名单需参照正文）。
*   **Benchmark 与对比方法**：
    *   对比了具有**固定推理调度**的强基线模型（包括纯自回归模型和纯双向模型）。
    *   评估指标涵盖视频质量（如 FVD）、长视频稳定性以及推理速度（FPS/生成耗时）。

### 4. 资源与算力
*   **算力说明**：根据提供的摘要内容，文中**未明确说明**具体的 GPU 型号、数量及训练总时长。此类大规模视频扩散模型通常需要高性能计算集群（如 NVIDIA A100 或 H100 阵列）进行训练。

### 5. 实验数量与充分性
*   **实验规模**：论文声称进行了“广泛的实验”（Extensive experiments），涵盖了多个数据集和消融实验。
*   **充分性评价**：从摘要看，实验设计考虑了质量、稳定性和速度三个维度，并与强基线进行了对比，逻辑较为严密。但具体的实验组数和消融细节需查阅正文以确认其统计显著性。

### 6. 论文的主要结论与发现
*   **统一性**：Flex-Forcing 成功统一了自回归与双向生成，证明了两者并非互斥，而是可以互补。
*   **性能提升**：在保持或提升视频质量（FVD 等指标）的同时，显著增强了长视频的稳定性。
*   **效率优势**：相比于传统的双向模型，Flex-Forcing 提供了更快的推理速度，且能适应不同的硬件约束。

### 7. 优点
*   **灵活性极高**：能够根据设备算力动态调整推理策略，具有很强的实用价值。
*   **解决痛点**：有效缓解了自回归模型中的曝光偏差问题，同时解决了双向模型处理长视频时的计算瓶颈。
*   **范式创新**：提出的“时间-去噪步”联合分块机制为视频生成调度提供了新思路。

### 8. 不足与局限
*   **训练复杂度**：为了支持“任意顺序、任意时间步”的生成，训练过程中的采样和掩码策略可能比传统模型更复杂，增加了训练成本。
*   **超参数敏感性**：分块大小和去噪步数的分配可能需要针对不同场景进行精细调优，以达到最佳的质量与速度平衡。
*   **应用限制**：虽然提高了效率，但对于极低延迟的实时交互场景，扩散模型本身的迭代本质可能仍是瓶颈。

（完）
