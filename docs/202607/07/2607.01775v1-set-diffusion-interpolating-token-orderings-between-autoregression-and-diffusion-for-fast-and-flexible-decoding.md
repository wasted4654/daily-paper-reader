---
title: "Set Diffusion: Interpolating Token Orderings Between Autoregression and Diffusion for Fast and Flexible Decoding"
title_zh: Set Diffusion：在自回归与扩散之间插值 Token 排序，实现快速且灵活的解码
authors: "Marianne Arriola, Volodymyr Kuleshov"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01775v1"
tags: ["query:ardm"]
score: 10.0
evidence: 在自回归和扩散之间插值令牌排序
tldr: 本研究提出Set Diffusion，一种结合自回归与扩散模型优势的新型语言模型。针对离散扩散模型在固定长度生成和KV缓存支持上的局限，该模型通过对灵活位置和长度的Token集合进行似然分解，并采用支持KV缓存更新的集合因果扩散架构，实现了任意顺序的解码。Set Diffusion在数学推理、摘要生成等任务中展现了优于传统扩散模型的速度与质量平衡，并提供了更强的文本填充能力。
selection_source: fresh_fetch
motivation: 传统的离散扩散模型受限于固定生成长度且不支持KV缓存，而块扩散模型则因固定的顺序块限制了解码的灵活性和并行度。
method: 提出一种基于灵活Token集合分解的似然参数化方法，并结合支持每步推理后更新KV缓存的集合因果扩散架构。
result: 该模型在数学推理、摘要和无条件生成任务上实现了更佳的速度与质量权衡，且在文本填充性能上优于块扩散模型。
conclusion: Set Diffusion通过插值自回归与扩散解码，为快速、灵活且支持任意顺序生成的语言建模提供了高效的新方案。
---

## 摘要
离散扩散模型在质量上相对于自回归（AR）模型已稳步提升。然而，这些模型通常受限于固定长度的生成，且不支持键值（KV）缓存。块扩散（Block diffusion）通过自左向右生成 token 块，在一定程度上桥接了扩散与自回归，但其固定大小的顺序块限制了解码的灵活性和并行性。在本文中，我们提出了一类新型语言模型——集合扩散（set diffusion），它由以下部分组成：(i) 一种在位置灵活、长度灵活的 token 集合上进行分解的似然参数化方案；(ii) 一种支持在每步推理后更新 KV 缓存的集合因果扩散架构。通过在 token 集合而非固定大小的块上进行分解，token 可以按任意顺序的集合进行解码（包括滑动窗口集合），从而实现更快的推理并支持任意顺序解码。与之前的扩散语言模型相比，集合扩散在数学推理、摘要生成和无条件生成任务上实现了更好的速度-质量权衡，同时提供了比块扩散更强的填充（infilling）性能。我们在项目页面提供了代码、模型权重和博客文章：https://m-arriola.com/setdlms/

## Abstract
Discrete diffusion models have steadily improved in quality relative to autoregressive (AR) models. However, these models are normally constrained to fixed-length generation and do not support key-value (KV) caching. Block diffusion partially bridges diffusion and AR by generating token blocks left-to-right, but its fixed-size sequential blocks limit decoding flexibility and parallelism. Here, we present a new class of language models, set diffusion, comprised of (i) a likelihood parameterization that factorizes over flexible-position, flexible-length token sets and (ii) a set-causal diffusion architecture that supports KV cache updates after every inference step. By factorizing over token sets instead of fixed-size blocks, tokens can be decoded in arbitrarily-ordered sets, including sliding-window sets, enabling faster inference and support for any-order decoding. Set diffusion achieves better speed-quality tradeoffs on mathematical reasoning, summarization, and unconditional generation compared to prior diffusion language models while offering stronger infilling performance than block diffusion. We provide the code, along with the model weights and blog post on the project page: https://m-arriola.com/setdlms/

---

## 论文详细总结（自动生成）

这是一份关于论文《Set Diffusion: Interpolating Token Orderings Between Autoregression and Diffusion for Fast and Flexible Decoding》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：离散扩散模型（Discrete Diffusion）在生成质量上正逐步追赶自回归（AR）模型，但面临两个主要瓶颈：
    1.  **缺乏灵活性**：通常受限于固定长度生成，且由于依赖双向上下文，无法支持键值（KV）缓存，导致长文本生成效率低下。
    2.  **结构僵化**：现有的“块扩散”（Block Diffusion）虽然结合了 AR 和扩散，但其固定的左到右块结构限制了并行性，且不支持任意位置的文本填充（Infilling）或编辑。
*   **研究动机**：作者观察到，扩散模型与自回归模型之间的插值不应仅限于改变块大小，更应通过改变 **Token 集合的生成顺序** 来实现。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **Set Diffusion**，将序列分解为多个位置灵活、长度灵活的 Token 集合（Sets）。通过定义这些集合上的自回归概率分布，模型可以根据不同的生成策略（如滑动窗口、随机顺序）进行解码。
*   **关键技术细节**：
    *   **似然参数化**：似然函数在 Token 集合上进行因子分解：$p_\theta(x) = \sum_{\sigma} \prod_{n=1}^N \pi(\sigma_n | x_{<\sigma_n}) \cdot p_\theta(x_{\sigma_n} | x_{<\sigma_n})$。其中 $\pi$ 是选择下一个生成位置的策略。
    *   **集合因果架构（Set-Causal Architecture）**：采用 Transformer 架构，但使用特殊的注意力掩码，使得 Token 集合仅能关注已生成的集合。这种设计允许在**每一步推理后**更新 KV 缓存，而非像块扩散那样必须等整个块完成。
    *   **位置偏移调度（Position-Offset Schedules）**：引入一种基于位置的揭示时间（Reveal-time）分布。通过调整窗口宽度 $w$，可以在完全随机顺序（对应 MDLM）和严格左到右顺序（对应 AR）之间平滑插值。
    *   **高效训练算法**：通过将“干净”的上下文和“加噪”的查询 Token 拼接在一次前向传播中，实现对所有 Token 集合似然的高效计算，降低了梯度方差。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：GSM8K（评估 zero-shot pass@1 准确率）。
    *   **文本摘要**：CNN/DailyMail（评估 ROUGE 分数）。
    *   **文本填充**：ROCStories（评估在给定上下文时填充中间句子的能力）。
    *   **无条件生成与似然估计**：OpenWebText (OWT) 和 One Billion Words (LM1B)。
*   **对比方法（Baselines）**：
    *   **自回归模型**：标准 AR Transformer。
    *   **扩散模型**：MDLM、SEDD、UDLM、D3PM。
    *   **混合模型**：BD3LM（块扩散）、FlexMDM、AR-Diffusion。

### 4. 资源与算力
*   **硬件使用**：
    *   **推理测试**：使用单张 **NVIDIA H100 80GB GPU** 测量吞吐量（tokens/sec）。
    *   **无条件生成**：在 **4 张 RTX A6000 48GB GPU** 上进行。
*   **训练细节**：
    *   模型参数量约为 110M（OWT/LM1B 任务）至 1.7B（GSM8K 蒸馏任务）。
    *   训练步数：OWT 任务约 300K 步，LM1B 任务约 1M 步。
    *   使用了 AdamW 优化器和指数移动平均（EMA）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础语言建模（PPL）到下游应用（推理、摘要、填充）的 5 个主要数据集。
*   **消融实验**：专门针对“高效训练”和“KV 缓存”的影响进行了消融研究，验证了集合因果掩码对性能和速度的提升。
*   **客观性**：通过控制“预期推理预测预算”（Expected Inference Prediction Budget）来公平对比 Set Diffusion 与块扩散模型，确保在相同的并行度下比较质量。

### 6. 主要结论与发现
*   **性能领先**：在 GSM8K 数学推理上，Set Diffusion 达到了扩散模型中最优的速度-质量权衡（Pareto Frontier）。
*   **填充能力强**：在 ROCStories 填充任务中，Set Diffusion 的 ROUGE 分数显著超过块扩散模型，且解码速度快约 25%。
*   **KV 缓存优势**：相比于不支持 KV 缓存的 MDLM，Set Diffusion 在长序列生成上的吞吐量提升了数倍。
*   **插值特性**：证明了通过调整位置偏移调度的参数，Set Diffusion 可以完美还原 AR 模型或 MDLM，实现了真正的架构统一。

### 7. 优点
*   **极高的灵活性**：支持变长生成、任意顺序解码和滑动窗口解码。
*   **推理效率高**：首次在扩散模型中实现了步进式的 KV 缓存更新，大幅降低了计算冗余。
*   **训练稳定**：提出的 Token-efficient 训练目标比传统的随机掩码训练具有更低的梯度方差，收敛更快。

### 8. 不足与局限
*   **调度调优**：位置偏移调度的参数（如窗口大小 $w$ 和形状 $k$）目前仍需根据硬件并行能力手动调整，尚未实现完全自动化的最优调度学习。
*   **绝对质量差距**：虽然缩小了差距，但在极高性能要求的任务上，其准确率与最顶尖的纯自回归模型（如经过大规模指令微调的模型）相比仍有细微空间。
*   **应用限制**：目前的实验主要集中在文本领域，尚未在多模态（如图像、视频）生成任务中验证其泛化性。

（完）
