---
title: "Self Gradient Forcing: Native Long Video Extrapolation"
title_zh: 自梯度强制：原生长视频外推
authors: "Junhao Zhuang, Shiyi Zhang, Yuxuan Bian, Yaowei Li, Yawen Luo, Yijun Liu, Weiyang Jin, Songchun Zhang, Xianglong He, Xuying Zhang, Haoran Li, Haoyang Huang, Zeyue Xue, Nan Duan"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20368v2"
tags: ["query:ardm"]
score: 10.0
evidence: 自回归视频扩散方法及用于长视频外推的自梯度强制技术
tldr: 本研究针对自回归视频扩散模型中的“历史上下文梯度缺失”问题，提出了Self Gradient Forcing (SGF) 训练策略。传统方法在推理时将历史KV缓存视为冻结状态，导致未来帧的损失无法优化历史帧的编码方式。SGF通过两阶段训练，在不进行全序列反向传播的情况下，恢复了未来帧对历史记忆写入的监督信号。实验表明，SGF显著提升了长视频生成的主体一致性与时间稳定性，仅需5秒训练即可实现数分钟的视频外推。
selection_source: fresh_fetch
motivation: 解决自回归视频生成中，未来帧的损失无法指导历史帧如何编码为更有效的KV缓存（上下文梯度缺失）的问题。
method: 提出两阶段训练策略，通过第一阶段记录生成上下文，第二阶段重新计算KV表示并回传梯度，实现对记忆写入过程的监督。
result: SGF在主体一致性、背景布局和时间稳定性方面均优于Self Forcing，仅需5秒训练窗口即可外推至数分钟的视频。
conclusion: SGF通过在原生自回归目标中引入记忆写入监督，显著增强了视频扩散模型的长程外推能力与生成质量。
---

## 摘要
最近的自回归视频扩散方法越来越多地基于 Self Forcing（自强制），即学生模型在自身展开（rollout）生成的历史数据上进行训练，而非真实视频上下文。这减少了曝光偏差，但历史键值（KV）缓存仍仅作为冻结的展开状态被后续帧使用。因此，未来的损失无法监督早期生成的潜变量应如何被写入更有效的键和值，以供后续视频潜变量生成使用。我们将此称为历史上下文梯度间隙。我们提出了自梯度强制（Self Gradient Forcing, SGF），这是一种两阶段训练策略，在无需通过完整串行展开进行反向传播的情况下，恢复了这一缺失的监督信号。第一阶段执行与推理一致的无梯度自回归展开，并在采样的去噪退出步记录自生成的上下文以及输入模型的噪声潜变量。第二阶段为记录的退出步执行并行的上下文梯度重建。生成的上下文被用作停止梯度的清洁潜变量输入，同时模型重新计算上下文 KV 表示以及未来到上下文的因果注意力。因此，SGF 在原生自回归训练目标中提供了缺失的记忆写入监督，利用未来视频潜变量的损失来训练模型将上下文编码为更有效的因果记忆。在不同初始化下的广泛长程逐帧和逐块实验中，SGF 实现了比 Self Forcing 更强的原生长视频外推能力，特别是在主体身份、背景/布局一致性以及时间稳定性方面。值得注意的是，仅使用 5 秒的训练窗口，SGF 即可外推至持续数分钟的视频。代码和模型将发布，以推动自回归视频生成的研究。

## Abstract
Recent autoregressive video diffusion methods are increasingly built upon Self Forcing, where the student is trained on histories produced by its own rollout rather than ground-truth video contexts. This reduces exposure bias, but the historical key-value cache is still used by future frames only as frozen rollout state. As a result, future losses cannot supervise how earlier generated latents should be written into more useful keys and values for later video-latent generation. We call this the historical context-gradient gap. We propose Self Gradient Forcing (SGF), a two-pass training strategy that restores this missing supervision signal without backpropagating through the full serial rollout. Pass 1 performs a no-gradient autoregressive rollout matching inference and, at a sampled denoising exit step, records both the self-generated context and the noisy latents fed to the model. Pass 2 performs parallel context-gradient reconstruction for the recorded exit step. The generated context is used as stop-gradient clean-latent input, while the model recomputes the context KV representations and future-to-context causal attention. Thus, SGF provides the missing memory-writing supervision within the native autoregressive training objective, using losses on future video latents to train the model to encode context into more effective causal memory. Across extensive long-horizon frame-wise and chunk-wise experiments under different initializations, SGF achieves stronger native long-video extrapolation than Self Forcing, especially in subject identity, background/layout consistency, and temporal stability. Remarkably, using only a 5-second training window, SGF can extrapolate to videos lasting several minutes. Code and models will be released to advance research on autoregressive video generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self Gradient Forcing: Native Long Video Extrapolation》（自梯度强制：原生长视频外推）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：自回归（AR）视频扩散模型在生成长视频时，通常面临“曝光偏差”问题（训练时使用真值，推理时使用预测值）。为了缓解这一问题，业界引入了**Self Forcing（自强制）**技术，即让模型在自身生成的历史数据上进行训练。
*   **核心问题**：作者发现，即便使用了 Self Forcing，现有的训练方式仍存在**“历史上下文梯度间隙”**。在训练过程中，历史帧的键值（KV）缓存被视为“冻结”的状态。这意味着，模型在学习生成未来帧时，其损失函数无法反馈并指导模型“应该如何将历史帧编码为更有效的记忆（KV 缓存）”。
*   **研究动机**：通过引入一种机制，让未来帧的生成质量能够反向监督历史帧的编码过程，从而提升长视频生成的一致性和稳定性。

### 2. 论文提出的方法论：Self Gradient Forcing (SGF)
SGF 采用了一种**两阶段（Two-pass）训练策略**，旨在不进行全序列反向传播（避免内存爆炸）的前提下，恢复缺失的监督信号：
*   **第一阶段（无梯度展开）**：模拟推理过程。模型进行自回归生成，不计算梯度。在此过程中，记录下生成的上下文潜变量（Context Latents）以及送入模型的噪声潜变量。
*   **第二阶段（并行梯度重建）**：
    *   将第一阶段记录的上下文作为输入（应用 `stop-gradient` 确保其作为“清洁”输入）。
    *   **重新计算**这些上下文的 KV 表示。
    *   计算未来帧到这些新生成的 KV 缓存的因果注意力。
*   **核心逻辑**：通过重新计算 KV 过程，未来帧的去噪损失（Loss）可以通过注意力机制回传到模型的编码权重上。这迫使模型学习如何将历史信息转化为对未来生成最有用的“因果记忆”。

### 3. 实验设计
*   **实验场景**：涵盖了长程逐帧（Frame-wise）和逐块（Chunk-wise）的视频生成实验。
*   **对比基准（Baselines）**：主要对比了传统的 **Teacher Forcing**（使用真值作为上下文）和 **Self Forcing**（使用自身生成值但冻结 KV 缓存）。
*   **评估维度**：主体身份一致性（Subject Identity）、背景/布局一致性（Background/Layout Consistency）以及时间稳定性（Temporal Stability）。
*   **外推能力测试**：验证模型在仅经过短时间窗口训练后，向未来外推数分钟视频的能力。

### 4. 资源与算力
*   **训练窗口**：论文强调了一个显著的效率指标——仅需 **5 秒** 的训练窗口（Training Window）。
*   **算力细节**：摘要中未明确给出具体的 GPU 型号（如 A100 或 H100）及具体的总卡时数。这通常在论文的“Implementation Details”章节中说明，但根据其两阶段训练的特性，其显存占用会高于标准训练，但远低于全序列 BPTT（随时间反向传播）。

### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的对比实验，涵盖了不同的初始化条件和生成策略（逐帧 vs 逐块）。
*   **消融实验**：通过对比 Self Forcing 和 SGF，清晰地展示了“梯度回传至 KV 编码过程”对长程一致性的贡献。
*   **客观性**：实验结果显示 SGF 在长达数分钟的视频外推中表现优异，证明了该方法在处理长程依赖方面的稳健性。

### 6. 主要结论与发现
*   **记忆写入监督至关重要**：证明了“如何编码记忆”比“记忆本身是什么”同样重要。
*   **外推性能飞跃**：SGF 显著增强了原生长视频的外推能力，能够维持数分钟的视频主体不漂移、背景不坍塌。
*   **训练效率**：通过两阶段设计，成功在有限的计算资源下实现了长程梯度的有效利用。

### 7. 优点
*   **理论深刻**：精准识别了 AR 视频扩散模型中被忽视的“梯度间隙”问题。
*   **架构通用**：SGF 是一种训练策略，可以广泛应用于各种基于 Transformer 的自回归视频生成模型。
*   **效果显著**：在极短的训练窗口下实现了极长的时间外推，具有很强的实用价值。

### 8. 不足与局限
*   **计算开销增加**：相比于 Self Forcing，SGF 需要在第二阶段重新计算 KV 缓存，这增加了训练时的计算量和耗时。
*   **实现复杂度**：两阶段训练逻辑（记录状态、并行重建）比标准的单向训练更复杂，对工程实现有一定要求。
*   **推理与训练的微小差异**：虽然 SGF 模拟了推理，但在第二阶段使用了 `stop-gradient` 的清洁潜变量，这与完全真实的推理环境仍存在细微的分布差异。

（完）
