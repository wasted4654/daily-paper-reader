---
title: "Mean-to-Score Discrete Diffusion: Posterior-Mean Denoisers for Score Entropy"
title_zh: 均值到分数离散扩散：用于分数熵的后验均值去噪器
authors: "Jingyuan Li, Xiaoyi Jiang, Yixuan Jiang, Wei Liu, Yi Zhu, Zuoqiang Shi, Pipi Hu"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21372v1"
tags: ["query:ardm"]
score: 8.0
evidence: 用于序列生成和分数熵的离散扩散模型
tldr: 本研究针对离散扩散模型（SEDD）中得分比率不满足贝叶斯可实现性的问题，提出了 Mean-to-Score (M2S) 方法。该方法通过预测干净标记的后验均值，并利用精确的线性映射将其转换为得分，确保了得分始终位于有效的桥多胞形内。实验表明，M2S 在 CIFAR-10 和 OpenWebText 任务上均显著提升了生成质量和采样效率，解决了传统 SEDD 在有限步采样中可能出现的负权重问题。
selection_source: fresh_fetch
motivation: 现有的离散扩散模型无法保证预测的得分比率与有效的后验分布相兼容，导致采样过程中出现负权重并限制了生成性能。
method: 提出 M2S 框架，通过预测概率单纯形上的后验均值并利用核相关的线性映射将其转化为得分，从而强制满足贝叶斯约束。
result: 在 OpenWebText 任务中，170M 参数的 M2S 模型在 128 步采样下的生成困惑度从 183.6 降至 143.3，且在 CIFAR-10 上也取得了更优的 BPD 和 FID 指标。
conclusion: M2S 通过引入后验均值参数化，有效解决了离散扩散模型中的得分一致性问题，显著提升了模型的生成质量和采样鲁棒性。
---

## 摘要
分数熵离散扩散 (SEDD) 使用无约束的正分数比率来参数化离散逆过程。虽然正性保证了非负的逆向跳跃率，但它并不能确保贝叶斯可实现性：噪声状态下的比率不一定是由前向核下的任何干净标记后验共同诱导的。分数熵损失具有正确的总体最优值，但在偏离最优值时并不强制执行此约束。在训练好的纯均匀 SEDD 检查点中，大约四分之一的完整分数向量

## Abstract
Score Entropy Discrete Diffusion (SEDD) parameterizes discrete reverse processes with unconstrained positive score ratios. While positivity guarantees nonnegative reverse jump rates, it does not ensure Bayes realizability: ratios at a noisy state need not be jointly induced by any clean-token posterior under the forward kernel. The score-entropy loss has the correct population optimum but does not enforce this constraint away from it. In a trained pure-uniform SEDD checkpoint, roughly one quarter of complete score vectors violate the coordinate box, while more than half lie inside it yet remain materially incompatible with any valid posterior. Such violations can produce negative pre-normalization weights in finite-step sampling. Projecting raw scores onto the bridge polytope removes all observed negative weights and improves external generative PPL from $203.6$ to $175.1$ without changing the sampler. We introduce \emph{mean-to-score} (M2S), which predicts a clean-token posterior mean and converts it to the score through an exact kernel-dependent linear map. The construction applies to any known coordinate-wise continuous-time Markov chain (CTMC) satisfying a mild support condition. For uniform corruption, it maps the probability simplex onto the bridge polytope; for absorbing-mask corruption, the resulting objective recovers MD4 exactly. In a controlled 28.4M-parameter CIFAR-10 comparison, M2S lowers test BPD from $3.173$ to $3.129$ and FID-50k from $\CifarSEDDFID$ to $\CifarMtwoSFID$. A 170M-parameter M2S model trained on about 262B OpenWebText token slots outperforms the evaluated pure-uniform SEDD, GIDD, and Neural CTMC checkpoints at every tested sampling budget, reaching generative PPL $143.3$ at 128 steps versus $183.6$ for the strongest pure-uniform baseline.

---

## 论文详细总结（自动生成）

以下是对论文《Mean-to-Score Discrete Diffusion: Posterior-Mean Denoisers for Score Entropy》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的离散扩散模型（如 SEDD）通过预测“分数比率”（Score Ratios）来参数化逆向过程。虽然这些模型保证了分数比率为正，但无法保证**贝叶斯可实现性（Bayes Realizability）**。
*   **核心矛盾**：在离散状态空间中，一个有效的得分向量必须能够由某个合法的干净标记后验分布（位于概率单纯形上）通过前向核函数诱导得出。作者发现，训练好的 SEDD 模型输出的得分向量中，有超过一半虽然坐标值为正，但逻辑上无法对应任何合法的后验分布。这种不一致性会导致在有限步采样时出现负权重，从而损害生成质量。

### 2. 方法论：Mean-to-Score (M2S)
*   **核心思想**：不再让神经网络直接预测得分比率，而是预测干净标记的**后验均值（Posterior Mean）** $\mu$，然后通过一个精确的、依赖于前向核的线性映射（称为“桥” $B_t$）将其转换为得分。
*   **关键技术细节**：
    *   **参数化重构**：网络输出经过 Softmax 处理，确保其位于概率单纯形 $\Delta^{K-1}$ 内。
    *   **线性映射（Bridge）**：对于均匀腐蚀过程，得分 $s$ 与后验 $\mu$ 的关系为：$s = 1 + (\rho_t - 1)\mu_{x_i} + (\rho_t^{-1} - 1)\mu_y$。这种映射确保了得分始终落在“桥多胞形”（Bridge Polytope）内。
    *   **目标函数**：推导了一种新的 M2S 损失函数，证明了在均匀核下，该损失的唯一最小值即为真实的干净标记后验。
    *   **统一性**：该方法具有普适性，在吸收掩码（Absorbing-mask）场景下，M2S 自动退化为已有的 MD4 目标函数。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像生成**：MNIST（256阶灰度）、CIFAR-10。
    *   **语言建模**：OpenWebText（OWT）。
*   **对比方法（Benchmarks）**：
    *   **纯均匀扩散基准**：SEDD、GIDD、Neural CTMC。
    *   **掩码/混合扩散基准**：MDLM、MD4。
*   **评估指标**：FID（图像质量）、BPD（比特每维度，衡量似然）、GenPPL（使用 Gemma2-9B 评估的生成困惑度）。

### 4. 资源与算力
*   **算力投入**：
    *   **OpenWebText**：使用了 **24 台 NVIDIA H100 GPU** 进行训练。
    *   **CIFAR-10**：匹配了 MD4 的训练预算，总计处理了 5.12 亿张图像（约 10,240 个 epoch）。
*   **模型规模**：CIFAR-10 实验使用 28.4M 参数模型；OpenWebText 实验使用 170M 参数的 Transformer 架构。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的 MNIST 到大规模文本生成（262B tokens）的多个维度。
*   **充分性**：
    *   **消融与对比**：在相同架构、参数量、优化器和采样器下与 SEDD 进行了严格的“苹果对苹果”对比。
    *   **采样步数分析**：测试了从 16 步到 128 步的不同采样预算，证明了 M2S 在低预算下的显著优势。
    *   **干预实验**：通过对现有 SEDD 检查点进行后验投影修复（Projection Repair），进一步验证了贝叶斯可实现性对性能的直接影响。
*   **客观性**：实验设计严谨，通过固定随机种子和采样路径来隔离参数化方式带来的改进。

### 6. 主要结论与发现
*   **性能提升**：在 OpenWebText 上，M2S 将 128 步采样的 GenPPL 从 183.6（最强基准）降至 143.3。在 CIFAR-10 上，FID 从 42.83 显著降至 28.09。
*   **采样鲁棒性**：M2S 彻底消除了采样过程中的负权重问题，使得模型在步数较少时依然能保持较高的生成质量。
*   **理论验证**：证明了强制执行贝叶斯可实现性不仅是理论上的完善，更是提升离散扩散模型经验性能的关键。

### 7. 优点
*   **理论深刻**：识别并定义了离散得分向量的“贝叶斯可实现性”这一结构化要求。
*   **即插即用**：M2S 并不改变前向过程或基础架构，仅通过改变输出层的解释方式和损失函数即可实现显著增益。
*   **修复能力**：提出的投影方法甚至可以用来改善已经训练好的其他得分模型。

### 8. 不足与局限
*   **计算开销**：虽然映射是线性的且复杂度为 $O(K)$，但在超大规模词表（如超过 10 万词）下，计算所有候选得分的开销仍需关注（尽管文中通过公式简化了计算）。
*   **前向核依赖**：M2S 的线性映射严格依赖于已知的前向 CTMC 核函数，对于核函数未知或极其复杂的场景可能难以应用。
*   **模型规模限制**：实验主要集中在 170M 参数规模，尚未在百亿级（10B+）参数的大语言模型上验证其扩展性。

（完）
