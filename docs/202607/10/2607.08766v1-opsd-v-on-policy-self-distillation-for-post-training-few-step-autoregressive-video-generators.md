---
title: "OPSD-V: On-Policy Self-Distillation for Post-Training Few-Step Autoregressive Video Generators"
title_zh: OPSD-V：针对后训练少步自回归视频生成器的同策略自蒸馏
authors: "Hongyu Liu, Chun Wang, Feng Gao, Xuanhua He, Yue Ma, Ziyu Wan, Yong Zhang, Xiaoming Wei, Qifeng Chen"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08766v1"
tags: ["query:ardm"]
score: 10.0
evidence: 自回归视频扩散模型的策略内自蒸馏
tldr: 针对少步自回归视频生成模型在长视频生成中存在的误差累积和运动减弱问题，本文提出了OPSD-V，一种策略内自蒸馏范式。该方法在不改变推理路径的前提下，引入真实长视频作为时间上下文，通过教师模型在学生访问的状态下提供密集的去噪校正目标，从而优化自回归缓存动态。实验证明，OPSD-V在Self-Forcing和LongLive等模型上显著提升了长视频的视觉质量和运动表现。
selection_source: fresh_fetch
motivation: 现有的少步自回归视频生成器在长序列生成时容易出现误差累积和运动动力学减弱的问题。
method: 提出一种策略内自蒸馏方法，利用真实视频上下文引导教师模型为学生的自回归推理路径提供密集的去噪校正监督。
result: 在多个代表性模型上应用后，显著提升了长视频的视觉质量、运动一致性及VBenchLong评分，并获得了更高的用户偏好。
conclusion: OPSD-V有效地缓解了长视频生成的退化问题，是提升少步自回归视频生成模型性能的有效后训练方案。
---

## 摘要
我们提出了 OPSD-V，一种用于后训练少步自回归 (AR) 视频扩散模型的同策略自蒸馏范式。现有的少步 AR 视频生成器能够以低延迟生成长视频，但在长程自回归展开过程中仍面临误差累积和运动动力学减弱的问题。OPSD-V 在保持原始少步推理路径的同时，减少了长程退化。其核心思想是在训练期间引入真实的视频长数据作为时间上下文，并利用其提供密集的轨迹级监督。具体而言，学生模型遵循与推理时完全一致的展开过程，根据其自身先前生成的 KV 缓存来生成每个视频块。与此同时，教师模型在学生模型访问的相同去噪状态下进行评估，但使用更清晰且符合 AR 一致性的时间缓存，其中旧的历史记录可以被真实视频上下文替换。这在同策略 AR 缓存动态下提供了密集的去噪级校正目标，且无需更改采样器、去噪步数或推理时的缓存机制。我们将 OPSD-V 应用于具有代表性的少步 AR 视频模型，包括 Self-Forcing 和 LongLive。实验结果表明，该方法在视觉质量、运动动力学和 VBenchLong 评分方面均有持续提升。一项包含 10 名参与者、对比 20 对视频的用户研究显示，在 66.0% 的总体偏好判断中（排除平局后为 82.5%），OPSD-V 优于基础模型。

## Abstract
We propose OPSD-V, an on-policy self-distillation paradigm for post-training few-step autoregressive (AR) video diffusion models. Existing few-step AR video generators can produce long videos with low latency, but still suffer from error accumulation and weakened motion dynamics during long autoregressive rollout. OPSD-V reduces long-horizon degradation while preserving the original few-step inference path. The key idea is to introduce real long-video data as temporal context during training and use it to provide dense trajectory-level supervision. Specifically, the student follows the exact inference-time rollout, generating each chunk conditioned on its own previously generated KV cache. In parallel, the teacher is evaluated at the same student-visited denoising states, but uses a cleaner AR-consistent temporal cache in which older history can be replaced by real-video context. This provides dense denoising-level corrective targets under on-policy AR cache dynamics, without changing the sampler, number of denoising steps, or inference-time cache mechanism. We apply OPSD-V to representative few-step AR video models, including Self-Forcing and LongLive. Experiments show consistent improvements in visual quality, motion dynamics, and VBenchLong scores. A user study with 10 participants comparing 20 video pairs shows that OPSD-V is preferred over the base models in 66.0% of overall-preference judgments (82.5% excluding ties).

---

## 论文详细总结（自动生成）

这是一份关于论文《OPSD-V: On-Policy Self-Distillation for Post-Training Few-Step Autoregressive Video Generators》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：当前的少步自回归（AR）视频生成模型（如 Self-Forcing, LongLive）虽然能以极低的延迟生成长视频，但在长程生成过程中面临严重的**误差累积（Error Accumulation）**和**运动动力学减弱（Motion Fading）**问题。
*   **核心问题**：由于训练（通常基于短片段）与推理（长程自回归展开）之间存在分布偏移，模型在生成后续视频块时，会基于之前生成的带有细微误差的缓存（KV Cache）进行预测，导致视频质量随时间推移而迅速退化。
*   **整体含义**：本文提出了一种名为 **OPSD-V** 的后训练（Post-training）范式，旨在不改变推理效率的前提下，通过“同策略自蒸馏”技术修复长视频生成的退化问题。

### 2. 论文提出的方法论
*   **核心思想**：采用**同策略（On-Policy）**的思路，让学生模型在训练时完全模拟推理时的行为，而教师模型则利用真实的视频上下文（Ground Truth Context）来纠正学生模型的偏差。
*   **关键技术细节**：
    *   **学生模型（Student）**：遵循真实的推理路径，根据自身先前生成的、可能带有误差的 KV 缓存来生成当前的视频块。
    *   **教师模型（Teacher）**：在与学生模型相同的去噪状态下进行评估，但其时间缓存（Temporal Cache）中的旧历史记录被替换为**真实的视频长数据上下文**。
    *   **密集轨迹监督**：教师模型为学生模型提供每一采样步的去噪校正目标。通过这种方式，学生模型学会了如何在存在自身生成误差的情况下，依然向“正确且具有运动一致性”的方向演进。
    *   **无需更改推理机制**：该方法仅在后训练阶段使用，推理时不需要更改采样器、去噪步数或缓存机制。

### 3. 实验设计
*   **基础模型（Base Models）**：选择了具有代表性的少步自回归视频模型，包括 **Self-Forcing** 和 **LongLive**。
*   **评估指标**：
    *   **VBenchLong**：专门用于评估长视频生成质量的基准。
    *   **视觉质量与运动动力学**：通过定性观察和定量评分衡量。
    *   **用户研究**：邀请 10 名参与者对 20 对视频进行盲测对比。
*   **对比对象**：主要是应用 OPSD-V 前后的基础模型性能对比。

### 4. 资源与算力
*   **算力说明**：论文摘要及提供的片段中**未明确说明**具体的 GPU 型号、数量或训练总时长。通常此类视频扩散模型的后训练需要中大规模的 GPU 集群（如 A100 或 H100），但具体数值需参考完整论文的实验设置章节。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流的少步 AR 模型上进行了验证，证明了方法的通用性。
    *   进行了包含 200 次判断（10 人 × 20 对）的用户偏好研究。
*   **充分性评价**：实验设计较为充分，涵盖了自动评分（VBenchLong）和主观评价。通过在不同架构的模型上应用该方法，展示了其作为一种“范式”的有效性。不过，若能涵盖更多样化的数据集（如不同风格、不同分辨率）会更具说服力。

### 6. 论文的主要结论与发现
*   **性能提升**：OPSD-V 在视觉质量、运动一致性方面显著优于原始模型。
*   **VBenchLong 评分**：应用该方法后，模型在长视频基准测试中获得了持续的评分提升。
*   **用户偏好**：在用户研究中，OPSD-V 的胜率极高。在排除平局的情况下，82.5% 的判断倾向于 OPSD-V 生成的结果。
*   **有效性验证**：证明了引入真实视频上下文作为“锚点”进行同策略蒸馏，是解决自回归漂移问题的有效途径。

### 7. 优点
*   **非侵入性**：不需要在推理阶段增加计算开销或改变模型架构，对部署非常友好。
*   **解决痛点**：直接针对自回归视频生成中最核心的“运动衰减”和“质量退化”问题。
*   **训练效率**：作为一种后训练（Post-training）手段，它比从头训练长视频模型更节省资源。

### 8. 不足与局限
*   **数据依赖**：该方法高度依赖于高质量的**真实长视频数据**作为教师模型的上下文，如果缺乏此类数据，提升效果可能受限。
*   **训练复杂度**：虽然推理不增压，但训练时需要同时运行学生和教师模型，且涉及长序列的自回归展开，显存压力和训练耗时可能较高。
*   **泛化边界**：目前主要在少步（Few-step）模型上验证，对于多步（如 50 步以上）的标准扩散模型，其蒸馏效率和效果尚待进一步验证。

（完）
