---
title: Reinforcing the Generation Order of Multimodal Masked Diffusion Models
title_zh: 强化多模态掩码扩散模型的生成顺序
authors: "Yidong Ouyang, Zhe Wang, Sourav Bhabesh, Dmitriy Bespalov"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08056v1"
tags: ["query:ardm"]
score: 8.0
evidence: 优化多模态掩码扩散模型的生成顺序
tldr: "本研究探讨了多模态掩码扩散模型中生成顺序的优化问题。针对文本生成图像和多模态理解任务中 Logits 无法有效指导生成顺序的挑战，作者提出了一种基于群体相对策略优化（GRPO）训练的可学习控制模块。该方法通过动态调整 Token 生成顺序，显著提升了模型对细粒度空间关系的捕捉能力及多模态推理性能。实验表明，该框架在 GenEval 和 VLMEvalKit 基准测试中分别实现了 4.08% 和 4.85% 的性能提升，证明了优化生成顺序在多模态任务中的重要性。"
selection_source: fresh_fetch
motivation: 现有扩散语言模型在多模态任务中仅依赖 Logits 难以确定最优生成顺序，限制了其在空间关系捕捉和复杂推理方面的表现。
method: 引入一个通过群体相对策略优化（GRPO）训练的可学习控制模块，用于动态决定多模态掩码扩散模型的生成顺序。
result: "在 GenEval 图像对齐基准上提升了 4.08%，在 VLMEvalKit 多模态理解基准上提升了 4.85%，显著增强了细粒度空间关系捕捉能力。"
conclusion: 优化生成顺序是提升多模态扩散模型性能的关键，通过强化学习训练的控制模块能有效增强模型的生成质量与理解深度。
---

## 摘要
扩散语言模型（DLMs）近期在自然语言生成任务中取得了显著进展。最近的研究表明，自适应 Token 生成顺序可以显著提高数学推理和代码合成应用的性能。在这项工作中，我们研究了文本到图像合成和多模态理解中生成顺序的优化。我们首先确定，与数独谜题等语言生成中的结构化问题不同，仅靠模型 Logits 不足以确定文本到图像生成和多模态理解中的最优生成序列。为了应对这一挑战，我们引入了一个通过群体相对策略优化（GRPO）训练的可学习控制模块，用于确定生成顺序。我们的结果表明，学习该控制模块显著提升了 DLMs 的文本到图像对齐和多模态理解能力。特别是，它增强了模型捕捉生成图像中细粒度空间关系的能力，同时也加强了在多模态推理和理解任务上的表现。我们在以物体为核心的文本到图像对齐基准测试 GenEval 上评估了我们的框架，实现了 4.08% 的相对提升。此外，在 VLMEvalKit 上的实验证实了多模态理解方面 4.85% 的相对提升，突显了我们方法的广泛有效性。

## Abstract
Diffusion Language Models (DLMs) have recently achieved substantial progress in natural language generation tasks. Recent research demonstrates that adaptive token generation ordering can significantly improve performance in mathematical reasoning and code synthesis applications. In this work, we investigate the optimization of generation order for both text-to-image synthesis and multimodal understanding. We first establish that, unlike structured problems in language generation such as Sudoku puzzles, model logits alone are insufficient for determining optimal generation sequences in text-to-image generation and multimodal understanding. To address this challenge, we introduce a learnable control module trained via Group Relative Policy Optimization (GRPO) to determine the generation order. Our results demonstrate that learning this control block substantially improves both text-to-image alignment and multimodal understanding in DLMs. In particular, it enhances the model's ability to capture fine-grained spatial relationships in generated images while also strengthening performance on multimodal reasoning and comprehension tasks. We evaluate our framework on GenEval, an object-focused benchmark for text-to-image alignment, where it achieves 4.08% relative improvements. In addition, experiments on VLMEvalKit confirm 4.85% relative improvements in multimodal understanding, highlighting the broad effectiveness of our approach.

---

## 论文详细总结（自动生成）

这篇论文针对多模态掩码扩散模型（MDMs）中的生成顺序优化问题进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在掩码扩散模型中，Token 的生成顺序（即哪些位置先被取消掩码并预测）对最终生成质量至关重要。
*   **研究背景**：虽然在纯文本任务（如数独、逻辑推理）中，基于模型置信度（Logits）的启发式策略（如 Top-K 或 Top-K Margin）能显著提升性能，但作者发现这些方法在**文本生成图像**和**多模态理解**任务中失效了。
*   **动机**：多模态任务涉及复杂的空间关系和非线性依赖，仅靠 Logits 无法反映 Token 的重要性。因此，需要一种能够学习并自适应调整生成顺序的机制。

### 2. 方法论
*   **核心思想**：引入一个可学习的**控制模块（Control Block）**，通过强化学习（GRPO）来动态决定每一步采样中哪些 Token 应该被优先生成。
*   **关键技术细节**：
    *   **取消掩码策略模块（UPM）**：该模块接收 Transformer 最后一层的隐藏状态、当前步数和掩码指示符，为每个 Token 输出一个排名分数。
    *   **Plackett-Luce 策略**：根据排名分数，使用不放回抽样的方式选择前 K 个 Token 进行取消掩码。
    *   **训练算法（GRPO）**：采用群体相对策略优化（Group Relative Policy Optimization）。它通过采样一组输出并计算组内相对奖励来估计优势函数（Advantage），无需额外的价值函数（Value Function）网络，从而降低计算开销。
    *   **奖励函数设计**：
        *   **图像生成**：结合了基于规则的奖励（如物体计数、位置准确性）和基于模型的奖励（PickScore，用于衡量人类偏好和对齐度）。
        *   **多模态理解**：奖励由正确性奖励（是否答对）和格式奖励（是否包含思维链 <think> 标签）加权组成。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本生成图像**：使用 **GenEval** 基准，涵盖单物体、双物体、计数、颜色、位置和颜色属性六个子任务。
    *   **多模态理解**：使用 **VLMEvalKit** 框架，涵盖 GQA、MMMU、MMB、SEED、MathVista_MINI 和 COCO_VAL 六个主流数据集。
*   **对比方法**：主要对比了基座模型 MMaDA-COT 配合 **Top-K** 策略和 **Top-K Margin** 策略的表现。

### 4. 资源与算力
*   **基座模型**：MMaDA-8B-MixCoT。
*   **算力消耗**：
    *   **图像生成训练**：使用了 **24 台 NVIDIA A100 GPU**，训练 5000 步，每步采样 24 个样本。
    *   **多模态理解训练**：使用了 **8 台 NVIDIA A100 GPU**，训练 5000 步，每步采样 12 个样本。
*   **精度**：图像任务使用 bfloat16，理解任务使用 float16。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个大类任务（生成与理解）共 12 个子基准上进行了测试。
*   **充分性**：实验涵盖了从基础物体识别到复杂空间推理的多个维度。
*   **消融与分析**：提供了生成轨迹的可视化分析，展示了学习后的生成顺序如何逐步构建图像或推理逻辑。
*   **公平性**：对训练集进行了严格的去重处理（防止 GenEval 测试集泄露），确保了评估的客观性。

### 6. 主要结论与发现
*   **学习优于启发式**：在多模态场景下，可学习的控制模块显著优于基于 Logits 的启发式策略。
*   **性能提升**：在 GenEval 上实现了 **4.08%** 的相对提升，在 VLMEvalKit 上实现了 **4.85%** 的相对提升。
*   **空间与推理增强**：该方法显著增强了模型处理空间关系（如“左边”、“右边”）和多物体组合的能力，并能引导模型生成更详细的思维链（CoT）过程。

### 7. 优点
*   **任务自适应**：通过强化学习让模型自己发现最优的生成路径，而非人工指定。
*   **通用性强**：同一套框架（GRPO + 控制模块）可同时应用于生成式任务和判别式理解任务。
*   **效率优化**：利用 GRPO 避免了 PPO 中复杂的 Critic 网络训练，适合大规模扩散模型的后训练。

### 8. 不足与局限
*   **训练成本**：尽管 GRPO 较 PPO 更高效，但对扩散模型进行全轨迹的强化学习训练仍需要大量的 A100 算力支持。
*   **奖励函数依赖**：模型性能高度依赖于奖励函数（如 PickScore）的质量，如果奖励模型存在偏差，生成顺序可能会过拟合于错误的偏好。
*   **推理开销**：在推理阶段增加了一个额外的控制模块计算步骤，虽然开销较小，但仍略微增加了延迟。

（完）
