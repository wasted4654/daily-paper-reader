---
title: "ARDY: Autoregressive Diffusion with Hybrid Representation for Interactive Human Motion Generation"
title_zh: ARDY：基于混合表示的自回归扩散模型，用于交互式人体运动生成
authors: "Kaifeng Zhao, Mathis Petrovich, Haotian Zhang, Tingwu Wang, Siyu Tang, Davis Rempe"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08741v1"
tags: ["query:ardm"]
score: 10.0
evidence: 用于动作生成的混合表示自回归扩散模型
tldr: ARDY是一个用于交互式人体动作生成的流式框架，旨在解决现有离线方法推理慢和在线方法控制力弱的问题。它结合了显式根节点特征与潜空间身体嵌入的混合表示，并采用两阶段自回归Transformer去噪器。该方法支持在线文本提示和长程运动学约束，在保持高保真度的同时实现了实时生成，适用于动画、仿真和机器人等领域。
selection_source: fresh_fetch
motivation: 现有的动作生成方法难以在保证实时性的同时，实现对复杂文本语义和长程运动学约束的精确控制。
method: 提出一种结合显式轨迹与潜空间嵌入的混合表示法，并利用支持可变历史上下文的两阶段自回归Transformer进行去噪生成。
result: 在HumanML3D和Bones Rigplay数据集上的实验证明，ARDY在动作质量和约束遵循方面表现优异，并支持多种交互式控制。
conclusion: ARDY成功平衡了生成效率与可控性，为实时交互式人体动作生成提供了一个高效且通用的解决方案。
---

## 摘要
在交互式应用中实时生成逼真的 3D 人体运动是动画、模拟和类人机器人的关键。虽然最近的离线运动生成方法通过文本和运动学约束提供了精确控制，但它们缺乏交互式场景所需的推理速度。相反，现有的在线方法虽然能够实现实时合成，但由于上下文窗口有限，往往会牺牲可控性，或者难以处理复杂的文本语义和长程目标。在这项工作中，我们介绍了 ARDY，这是一个流式生成框架，通过实现由在线文本提示和灵活运动学约束控制的高保真运动生成，弥补了这一差距。ARDY 采用了一种混合表示，将显式的根节点特征与潜空间身体嵌入相结合，平衡了精确的轨迹控制与高效的生成学习。我们提出了一种两阶段自回归 Transformer 去噪器，其特点是具有可变的历时上下文，并支持以灵活的长程运动学约束为条件。通过在大规模运动捕捉数据集上进行训练，并直接以从真实姿态中采样的文本标签和运动学约束为条件，ARDY 原生地学习了支持在线提示和灵活长程目标的可控生成。在 HumanML3D 基准测试和大规模、高保真 Bones Rigplay 数据集上的广泛评估表明，ARDY 具有极高的运动质量和约束遵循能力，验证了我们关键架构决策的有效性。最后，我们通过一个交互式演示展示了该方法的实际通用性，该演示具有动态文本控制、多样化的关键帧姿态约束、路径跟随以及通过鼠标和键盘进行的交互式运动控制。补充视频结果、代码和模型发布可在 https://research.nvidia.com/labs/sil/projects/ardy/ 找到。

## Abstract
Generating realistic 3D human motions in real-time within interactive applications is key for animation, simulation, and humanoid robotics. While recent offline motion generation approaches offer precise control via text and kinematic constraints, they lack the inference speed required for interactive settings. Conversely, existing online methods enable real-time synthesis but often sacrifice controllability or struggle with complex text semantics and long-horizon goals due to limited context windows. In this work, we introduce ARDY, a streaming generation framework that bridges this gap by enabling high-fidelity motion generation controllable via online text prompts and flexible kinematic constraints. ARDY employs a hybrid representation that combines explicit root features with a latent body embedding, balancing precise trajectory control with efficient generative learning. We propose a two-stage autoregressive transformer denoiser that features variable history context and supports conditioning on flexible, long-horizon kinematic constraints. By training on a large-scale motion capture dataset and being directly conditioned on text labels and kinematic constraints sampled from ground truth poses, ARDY natively learns controllable generation that supports online prompting and flexible long-horizon goals. Extensive evaluations on the HumanML3D benchmark and the large-scale, high-fidelity Bones Rigplay dataset demonstrate ARDY's high motion quality and constraint adherence, validating the efficacy of our key architectural decisions. Finally, we demonstrate the method's practical versatility through an interactive demo featuring dynamic text control, diverse keyframe pose constraints, path following, and interactive locomotion control via mouse and keyboard. Supplementary video results, code, and model releases can be found at https://research.nvidia.com/labs/sil/projects/ardy/.

---

## 论文详细总结（自动生成）

### ARDY：基于混合表示的自回归扩散模型用于交互式人体运动生成

#### 1. 论文的核心问题与整体含义
在动画、仿真和机器人领域，实时生成高质量且可控的人体动作至关重要。现有的方法存在两极分化：
*   **离线模型（Offline）**：如传统的扩散模型，虽然支持复杂的文本和运动学约束（如关键帧），但推理速度慢，无法满足实时交互需求。
*   **在线模型（Online/Streaming）**：虽然速度快，但通常控制力较弱，且由于上下文窗口较短，难以处理复杂的长程语义（如“先走再跳再坐下”）和远期目标。

**ARDY** 的核心目标是**弥补实时性与高可控性之间的鸿沟**。它提出了一个流式生成框架，既能响应实时的文本指令和运动学约束（如鼠标点击路径、关键帧），又能保持高保真的动作质量。

#### 2. 方法论
ARDY 的核心思想结合了**混合表示法**和**两阶段自回归去噪架构**：

*   **混合运动表示（Hybrid Representation）**：
    *   **显式根节点（Explicit Root）**：使用全局坐标表示根节点的位移和朝向。这允许直接通过覆盖（Overwriting）特征来实现精确的轨迹控制，避免了局部速度累积产生的误差。
    *   **潜空间身体（Latent Body）**：使用预训练的 Tokenizer（分词器）将高维的身体关节特征压缩为紧凑的潜空间向量，提高生成效率。
*   **运动分词器（Motion Tokenizer）**：采用 FSQ（有限标量量化）技术的因果 Transformer 编码器-解码器。解码时将全局根节点转为局部表示，以显著减少“滑步”现象。
*   **两阶段自回归去噪器（Two-Stage Denoiser）**：
    *   **第一阶段**：根节点 Transformer 预测清洁的全局轨迹。
    *   **第二阶段**：身体 Transformer 以第一阶段的预测结果为条件，预测清洁的身体潜变量。
    *   这种设计确保了根节点运动（全局位移）与身体动作（局部姿态）的深度耦合。
*   **灵活约束处理**：将运动学约束（如关节位置、旋转）表示为掩码序列（Masked Sequence），作为条件输入。支持可变长度的历史上下文（最高 8 秒）和远期目标（Future Goals）。

#### 3. 实验设计
*   **数据集**：
    *   **HumanML3D**：标准的公开基准数据集（约 30 小时动作）。
    *   **Bones Rigplay**：大规模、高保真的专有数据集（约 700 小时，31.5 万个剪辑），包含丰富的文本描述和复杂的动作类型。
*   **对比方法**：
    *   **离线 SOTA**：MaskControl（基于掩码的生成）。
    *   **在线/自回归 SOTA**：DiP（自回归扩散）、CAMDM、DartControl 等。
*   **评估指标**：FID（动作质量）、R-Precision（文本匹配度）、Foot Skating（滑步率）、Joint Error（约束精度）以及人类主观偏好研究。

#### 4. 资源与算力
*   **训练算力**：
    *   **分词器**：使用 1 张 NVIDIA A100-SXM4-80GB GPU，训练 400 万步。
    *   **去噪器**：使用 4 张 NVIDIA A100-SXM4-80GB GPU，训练 100 万步。
*   **推理算力**：在 **RTX 4090** 上进行实时演示。
    *   4 步扩散模型平均延迟为 **33 ms**。
    *   10 步扩散模型平均延迟为 **63 ms**。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验：
*   **消融实验**：对比了混合表示 vs. 纯显式表示、全局 vs. 局部解码、单阶段 vs. 两阶段架构。
*   **超参数分析**：探讨了生成窗口长度（4 到 40 帧）、扩散步数（1 到 100 步）、Tokenizer 补丁大小及潜空间容量对结果的影响。
*   **基准测试**：在公开数据集上验证了其作为通用生成器的能力，在专有大数据集上验证了其在生产环境下的潜力。
*   **主观评价**：通过 240 组人类成对比较，证明了 ARDY 在动作质量和目标达成度上显著优于 DiP。
实验设计客观、公平，覆盖了从底层架构到高层应用的各个维度。

#### 6. 主要结论与发现
*   **混合表示是关键**：将根节点显式化能极大提升空间约束的精度，而身体潜变量化则保证了生成的流畅度。
*   **两阶段架构优于单阶段**：先预测根节点再预测身体，能有效降低生成难度，提升控制精度。
*   **长程上下文的重要性**：支持长达 8 秒的历史上下文，解决了自回归模型在处理复杂连续动作时容易“遗忘”或“重复”的问题。
*   **实时性与质量的平衡**：仅需 4 步扩散即可达到可接受的生成质量，满足 30FPS 以上的交互需求。

#### 7. 优点
*   **全能控制**：原生支持文本、根节点轨迹、路径点、全身体关键帧、稀疏关节位置及旋转等多种约束。
*   **长程规划**：能够根据远在生成窗口之外的目标（如 10 秒后的终点）来调整当前的起步动作。
*   **工业级潜力**：在大规模数据集上表现稳健，推理速度极快，且提供了延迟感知的重规划机制。

#### 8. 不足与局限
*   **内存开销**：自回归过程中显式存储所有历史帧作为上下文，在处理极长序列时可能存在效率瓶颈。
*   **物理真实性**：虽然引入了滑步损失，但 ARDY 本质上仍是运动学模型，缺乏物理动力学约束，偶尔会出现抖动或穿模。
*   **扩散步数限制**：虽然 4 步很快，但相比于非扩散的自回归模型（如 GPT 类），计算开销依然较大。

（完）
