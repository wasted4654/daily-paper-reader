---
title: Constrained Decoding for Diffusion Language Models via Efficient Inference over Finite Automata
title_zh: 基于有限自动机高效推理的扩散语言模型约束解码
authors: "Meihua Dang, Stefano Ermon"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07026v1"
tags: ["query:ardm"]
score: 8.0
evidence: 扩散语言模型与自回归模型的受限解码对比
tldr: 针对扩散语言模型（Diffusion LMs）非线性生成的特性，本文提出了一种基于有限自动机（FA）的高效约束解码算法。该方法将FA视为概率图模型，实现了对约束均值场后验分布的精确采样，确保生成结果严格符合JSON等结构化要求。通过引入算术电路深度缩减技术，将采样深度从线性降至对数级。在函数调用、规划和数学推理等任务上，该方法在保持极低推理开销的同时，大幅提升了模型的生成准确率和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的约束解码方法依赖自回归模型的顺序生成特性，难以处理扩散模型在每一步去噪中同时采样多个位置的并行生成模式。
method: 提出一种基于有限自动机的精确推理算法，通过将约束表示为图模型并结合深度缩减技术，实现对均值场后验分布的高效采样。
result: "在Dream-7B和LLaDA-8B上的实验表明，该方法在函数调用等任务中显著提升了准确率（如BFCL任务从22.3%提升至69.0%），且额外时间开销不足5%。"
conclusion: 该算法为扩散语言模型提供了严谨且高效的结构化输出保障，使其在处理复杂格式约束任务时具备了与自回归模型竞争的能力。
---

## 摘要
约束解码对于大语言模型（LLM）的服务至关重要，它能确保生成的输出遵循特定的结构，例如符合 JSON 模式格式的函数调用。现有系统是为自回归模型设计的，并假设采用从左到右的生成方式，在每一步中屏蔽掉无效的下一个标记。然而，扩散语言模型打破了这一假设：它们在每个去噪步骤中，从完全分解的平均场分布中同时对多个位置进行采样。在本文中，我们提出了一种精确且易于处理的算法，用于在任何可由有限自动机表示的约束下，从受限的平均场后验分布中进行采样。通过将有限自动机视为概率图模型，我们获得了受限分布的易处理表示，从而实现了高效推理。该方法通过构造保证了约束的满足，支持贪婪解码和基于采样的解码，并且与任意重掩码调度下的并行和分块解码兼容。应用算术电路理论中的深度缩减技术，我们将采样深度从序列长度的线性复杂度进一步降低到对数复杂度。在 Dream-7B 和 LLaDA-8B 上的实证评估显示，在包括函数调用（xLAM、BFCL）、规划（数独、倒计时）、text-to-SQL（Spider）和数学推理（GSM-Symbolic）在内的各种任务中，准确率均有显著提升，且相对于无约束解码，推理开销极小。例如，在 BFCL-Live 上，我们的方法将 Dream-7B 的贪婪解码准确率从 63.9% 提高到 71.5%，随机采样准确率从 22.3% 提高到 69.0%（此时无约束基线已崩溃），而实际运行时间开销低于 5%。

## Abstract
Constrained decoding is essential for serving LLMs, ensuring that generated outputs follow specific structures such as JSON schema-formatted function calls. Existing systems are designed for autoregressive models and assume left-to-right generation, masking out invalid next tokens at each step. Diffusion language models, however, break this assumption: they sample multiple positions simultaneously from a fully-factorized mean-field distribution at each denoising step. In this paper, we present an exact and tractable algorithm for sampling from the constrained mean-field posterior under any constraint expressible as a finite automaton. Viewing finite automata as graphical models, we obtain tractable representations of the constrained distribution that enable efficient inference. The approach guarantees constraint satisfaction by construction, supports both greedy and sampling-based decoding, and is compatible with parallel and block-wise decoding under arbitrary remasking schedules. Applying depth-reduction techniques from arithmetic circuit theory, we further reduce sampling depth from linear to logarithmic in the sequence length. Empirical evaluations on Dream-7B and LLaDA-8B show substantial accuracy gains across various tasks including function calling (xLAM, BFCL), planning (Sudoku, Countdown), text-to-SQL (Spider), and math reasoning (GSM-Symbolic), with little inference overhead relative to unconstrained decoding. For example, on BFCL-Live, our approach improves Dream-7B's greedy decoding accuracy from 63.9% to 71.5%, and stochastic sampling accuracy from 22.3% to 69.0%, where the unconstrained baseline collapses, with under 5% wall-clock overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Constrained Decoding for Diffusion Language Models via Efficient Inference over Finite Automata》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**扩散语言模型（dLLMs）**中实现高效且精确的**约束解码**（如确保输出符合 JSON 格式或 SQL 语法）。
*   **研究动机**：
    *   **自回归模型的局限性**：现有的约束解码工具（如 Outlines, XGrammar）专为自回归模型设计，依赖“从左到右”逐个预测标记并屏蔽无效 token。
    *   **扩散模型的挑战**：扩散模型在去噪过程中会同时对序列中的多个位置进行采样（均值场分布）。由于约束通常是全局性的（例如括号匹配），独立屏蔽每个位置的 token 无法保证最终序列的合法性。
    *   **性能鸿沟**：无约束的扩散模型在随机采样时极易崩溃，导致结构化任务准确率大幅下降。

### 2. 方法论
*   **核心思想**：将约束建模为**有限自动机（FA）**，并将其视为一种**链式概率图模型**。通过在每一步去噪过程中从“均值场预测”与“自动机约束”的乘积分布中进行精确采样，确保生成序列始终满足约束。
*   **关键技术细节**：
    *   **图模型表示**：将有限自动机编码为隐马尔可夫模型（HMM）结构，其中隐变量表示自动机的边，观测变量表示 token。
    *   **受限后验采样**：在每个扩散步骤，计算 $p_{\theta}(x_0 | x_t, C) \propto p_{\theta}(x_0 | x_t) \cdot \mathbb{1}[x_0 \in C]$。由于 $p_{\theta}$ 是完全分解的，而 $C$ 是链式结构的，两者的乘积仍是一个可处理的链式图模型。
    *   **对数深度树采样（Log-depth Tree Sampling）**：为了克服传统前向-后向算法 $O(L)$ 的线性时间复杂度，借鉴算术电路理论，将计算图重构为二叉树结构。利用条件独立性，将采样深度降低至 $O(\log L)$，极大提升了 GPU 的并行利用率。
    *   **重掩码置信度**：利用约束分布下的边缘概率作为置信度信号，指导扩散模型选择哪些位置进行保留或重新掩码。

### 3. 实验设计
*   **实验模型**：Dream-7B 和 LLaDA-8B（包括 Base 和 Instruct 版本）。
*   **任务与数据集**：
    *   **函数调用**：xLAM, BFCL (Live & Non-live)，支持 JSON 和 Python 格式。
    *   **规划任务**：4x4 数独（Sudoku）、倒计时（Countdown）。
    *   **结构化生成**：Spider (Text-to-SQL)。
    *   **数学推理**：GSM-Symbolic（约束符号表达式格式）。
*   **对比基线**：无约束的原始模型（Base）在贪婪解码（Greedy）和随机采样（Sampling）下的表现。

### 4. 资源与算力
*   **硬件设备**：论文在性能测试部分明确提到了使用 **NVIDIA RTX A6000 GPU**。
*   **算力开销**：
    *   引入约束后的实际运行时间（Wall-clock time）开销极低，相对于无约束基线仅增加 **< 5%**。
    *   通过树状采样算法，在 Batch Size 增加时，吞吐量几乎与无约束解码持平。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 类任务、6 个主流数据集，进行了数百次自动化评估。
*   **充分性评价**：
    *   **多维度对比**：不仅对比了准确率，还对比了约束满足率（CS）、推理延迟、吞吐量。
    *   **消融实验**：对比了不同的重掩码策略（均值场置信度 vs. 约束边缘概率）以及不同去噪步数（16 到 256 步）对性能的影响。
    *   **客观性**：使用了 BFCL 等标准化评测集，并对比了同尺寸的自回归模型（如 Llama-3.1-8B），实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能飞跃**：在随机采样模式下，该方法挽救了扩散模型的表现。例如在 BFCL-Live 任务中，Dream-7B 的准确率从 22.3% 提升至 69.0%。
*   **格式鲁棒性**：扩散模型往往具备语义能力但缺乏格式控制。通过约束解码，模型在 Python 格式下的表现能迅速追平 JSON 格式，证明了改进主要源于结构引导。
*   **高效并行**：对数深度采样算法成功抵消了复杂约束带来的计算延迟，使得扩散模型在受限场景下具备了实际部署的竞争力。

### 7. 优点
*   **理论严谨**：实现了从受限均值场后验分布中的**精确采样**，而非近似或拒绝采样。
*   **100% 保证**：通过构造保证了输出 100% 符合有限自动机定义的语法。
*   **极低开销**：利用算术电路优化，解决了扩散模型多步迭代带来的累积延迟问题。
*   **通用性强**：兼容多种扩散模型架构（如 LLaDA, Dream）和不同的掩码调度策略。

### 8. 不足与局限
*   **约束表达力限制**：目前仅支持**有限自动机（FA）**可表达的约束（正则语言），尚未覆盖更复杂的上下文无关文法（CFG，如嵌套层级极深的复杂代码），尽管论文提到可以通过 NFA 模拟部分场景。
*   **序列长度固定**：实验主要针对固定长度或最大长度内的生成，对于长度动态变化极大的任务可能需要额外处理。
*   **安全风险**：论文指出，虽然提高了可靠性，但也可能被用来生成语法正确但具有恶意意图的工具调用或 SQL 注入语句。

（完）
