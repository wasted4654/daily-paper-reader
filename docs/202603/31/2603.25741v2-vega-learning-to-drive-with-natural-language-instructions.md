---
title: "Vega: Learning to Drive with Natural Language Instructions"
title_zh: Vega：利用自然语言指令学习驾驶
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v2"
tags: ["query:genad"]
score: 9.0
evidence: 基于扩散范式的指令驱动自动驾驶生成与规划
tldr: 本研究针对现有自动驾驶模型缺乏遵循多样化指令能力的局限，提出了Vega模型。通过构建包含10万个场景的InstructScene指令驾驶数据集，Vega结合了自回归（处理视觉与语言）与扩散模型（生成预测与轨迹）的优势。该模型实现了视觉、语言、世界建模与动作的统一，不仅提升了规划性能，还赋予了系统强大的指令遵循能力，为个性化智能驾驶奠定了基础。
selection_source: fresh_fetch
motivation: 现有自动驾驶系统多将语言用于场景描述，缺乏遵循用户多样化指令以实现个性化驾驶的灵活性。
method: 提出了Vega模型，利用自回归范式处理视听输入，并结合扩散范式进行世界建模与轨迹生成，同时构建了大规模指令驾驶数据集InstructScene。
result: 实验表明Vega在规划性能上表现优异，并展现出极强的自然语言指令遵循能力。
conclusion: 该研究通过整合多模态交互与生成式架构，为开发更智能、更具个性化的自动驾驶系统提供了有效路径。
---

## 摘要
视觉-语言-动作模型重塑了自动驾驶，将语言纳入决策过程。然而，大多数现有流程仅利用语言模态进行场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。为解决这一问题，我们首先构建了一个大规模驾驶数据集（InstructScene），包含约 100,000 个场景，并标注了多样化的驾驶指令及相应的轨迹。随后，我们提出了一种统一的视觉-语言-世界-动作模型 Vega，用于基于指令的生成与规划。我们采用自回归范式处理视觉输入（视觉）和语言指令（语言），并采用扩散范式生成未来预测（世界建模）和轨迹（动作）。我们通过联合注意力机制实现模态间的交互，并为不同模态使用独立的投影层以增强能力。大量实验表明，我们的方法不仅实现了卓越的规划性能，还展现出强大的指令遵循能力，为更智能、更个性化的驾驶系统铺平了道路。

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

以下是对论文《Vega: Learning to Drive with Natural Language Instructions》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）驾驶模型虽然能进行场景描述或逻辑推理，但缺乏遵循多样化、开放式用户指令的能力（例如“超车以赶上绿灯”或“靠边停车”）。
*   **研究动机**：
    *   **从模仿到指令的转变**：传统模型倾向于模仿平均的专家策略，无法实现个性化驾驶。
    *   **监督信号稀疏**：直接将高维视觉输入映射到低维动作（轨迹）存在信息不对称，导致模型难以学习复杂的因果关系。
    *   **缺乏统一框架**：需要一个能同时理解指令、预测世界演变并规划动作的统一模型。

### 2. 方法论
*   **核心思想**：提出 **Vega**，一个统一的“视觉-语言-世界-动作”模型。通过引入“世界建模”（预测未来图像）作为密集的像素级监督信号，增强模型对指令和动作之间因果关系的理解。
*   **关键技术细节**：
    *   **混合架构**：采用**自回归（AR）**范式处理视觉和语言理解，采用**扩散（Diffusion）**范式生成未来图像预测和动作轨迹。
    *   **混合变换器（MoT）**：不同于传统的混合专家（MoE），MoT 为不同模态（视觉、语言、动作、生成）分配独立的 Transformer 参数层，有效解耦模态并提升模型容量。
    *   **因果注意力机制**：通过阻塞式下三角掩码确保推理路径符合“指令 -> 动作 -> 视觉结果”的逻辑。
    *   **InstructScene 数据集**：基于 NAVSIM 构建，包含约 10 万个场景。利用强大的 VLM（如 Qwen2.5-VL）结合基于规则的方法，自动生成描述驾驶意图的自然语言指令。
*   **算法流程**：输入历史图像、历史动作和当前指令 -> AR 模块编码理解 -> 扩散模块在统一序列中去噪生成动作轨迹 -> 进一步生成对应的未来场景图像。

### 3. 实验设计
*   **数据集与场景**：
    *   **NAVSIM v1**：包含 8.5 万个训练场景，评估指标为 PDMS（综合得分）。
    *   **NAVSIM v2**：引入反应式交通流，评估指标为 EPDMS（扩展得分）。
*   **Benchmark 与对比方法**：
    *   对比了传统的 BEV 方法（如 UniAD, TransFuser, Hydra-MDP）和最新的 VLA 方法（如 AutoVLA, DriveVLA-W0）。
    *   使用了 **Best-of-N (N=6)** 策略进行性能增强对比。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 H20 GPU**。
*   **训练细节**：
    *   训练步数：200,000 步。
    *   学习率：2e-5，带有 2500 步预热。
    *   Batch Size：每张显卡为 1。
    *   模型规模：基于 Qwen2.5-7B 构建，隐藏层维度 3584，深度 28 层。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 Benchmark（NAVSIM v1/v2）上进行了全面测试。
    *   **消融实验**：针对“未来帧预测任务”、“图像-动作交织序列长度”、“独立动作专家模块”以及“VLA 基准对比”进行了多组实验。
*   **充分性与客观性**：实验设计较为充分，涵盖了从规划性能到指令遵循能力的定性与定量分析。通过对比 Best-of-N 策略，确保了与 SOTA 方法在同等条件下的公平竞争。

### 6. 主要结论与发现
*   **性能领先**：Vega 在 NAVSIM v2 上达到了 86.9 EPDMS（Best-of-N 下为 89.4），在多个子项指标（如红绿灯合规性、车道保持）上超越了现有 SOTA。
*   **世界建模的增益**：实验证明，加入未来图像预测任务能显著提升规划性能，因为像素级监督帮助模型理解了环境动力学。
*   **指令遵循能力**：Vega 能够根据不同的指令（如“加速赶超” vs “保持跟车”）生成截然不同的轨迹和对应的未来预测图像，展现了极强的灵活性。

### 7. 优点
*   **架构统一性**：成功将理解、预测和规划整合进一个 Transformer 框架，避免了模块化流水线的误差累积。
*   **密集监督**：利用生成式任务解决动作监督稀疏的问题，思路新颖且有效。
*   **高质量数据集**：构建并开源了 InstructScene，填补了大规模指令驾驶数据的空白。

### 8. 不足与局限
*   **NAVSIM v1 偏差**：在 v1 版本上的表现略逊于部分 VLA 方法，论文指出这是由于 v1 的评估指标过于偏向保守策略，而 Vega 学习到了更多样化的驾驶行为。
*   **传感器限制**：目前主要基于单路前视摄像头，虽然易于扩展，但在处理侧方或后方复杂交互时可能存在感知盲区。
*   **推理延迟**：由于采用了扩散模型进行生成，尽管使用了集成式 Transformer，其推理速度（尤其是生成图像时）可能仍难以满足极高频率的实时闭环控制需求。

（完）
