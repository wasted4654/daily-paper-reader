---
title: Encoding Predictability and Legibility for Style-Conditioned Diffusion Policy
title_zh: 为风格条件扩散策略编码可预测性与可读性
authors: "Adrien Jacquet Crétides, Mouad Abrini, Hamed Rahimi, Mohamed Chetouani"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16368v1"
tags: ["query:genad"]
score: 8.0
evidence: 用于协作环境中轨迹生成的样式条件扩散策略
tldr: 本研究针对人机协作中机器人动作的“可读性”与“效率”之间的权衡挑战，提出了风格条件扩散策略（SCDP）。该框架通过在预训练的扩散模型基础上增加轻量级场景编码器和条件预测器，实现在歧义环境下生成更具表达力的可读动作，而在目标明确时回归高效路径。这种模块化方法无需重新训练基础策略，有效提升了协作安全性与系统效率。
selection_source: fresh_fetch
motivation: 旨在解决协作机器人中为了提高动作可读性而导致的运动冗余，以及在目标明确时过度表达所带来的效率损失问题。
method: 提出一种模块化框架，通过冻结基础扩散策略并引入歧义检测模块，动态引导轨迹生成向可读性或效率风格偏移。
result: 在操纵和导航任务的评估中，该方法在歧义场景下显著增强了动作可读性，同时在非歧义场景下保持了最优的运动效率。
conclusion: SCDP证明了通过后训练调节扩散过程，可以在无需重新训练基础模型的情况下，实现机器人行为风格在表达力与效率之间的灵活平衡。
---

## 摘要
在人机协作中，平衡效率与透明运动是一个核心挑战，因为高度表现力的动作往往会产生不必要的时间和能量消耗。在协作环境中，可读性使人类观察者能够更好地理解机器人的动作，从而提高安全性和信任度。然而，这些行为会导致次优且夸张的轨迹，在机器人目标已经很明显的低歧义场景中，这些轨迹是多余的。为了解决这一权衡问题，我们提出了风格条件扩散策略（SCDP），这是一个模块化框架，可根据环境配置将预训练扩散模型的轨迹生成约束为可读性或效率。我们的方法利用了一种训练后流水线，该流水线冻结基础策略，并训练一个轻量级场景编码器和条件预测器来调节扩散过程。在推理时，歧义检测模块会激活相应的条件，仅针对歧义目标优先考虑表现力动作，否则恢复到高效路径。我们在操纵和导航任务上评估了 SCDP，结果表明，它在歧义设置中增强了可读性，同时在不需要可读性时保持了最佳效率，且无需重新训练基础策略。

## Abstract
Striking a balance between efficiency and transparent motion is a core challenge in human-robot collaboration, as highly expressive movements often incur unnecessary time and energy costs. In collaborative environments, legibility allows a human observer a better understanding of the robot's actions, increasing safety and trust. However, these behaviors result in sub-optimal and exaggerated trajectories that are redundant in low-ambiguity scenarios where the robot's goal is already obvious. To address this trade-off, we propose Style-Conditioned Diffusion Policy (SCDP), a modular framework that constrains the trajectory generation of a pre-trained diffusion model toward either legibility or efficiency based on the environment's configuration. Our method utilizes a post-training pipeline that freezes the base policy and trains a lightweight scene encoder and conditioning predictor to modulate the diffusion process. At inference time, an ambiguity detection module activates the appropriate conditioning, prioritizing expressive motion only for ambiguous goals and reverting to efficient paths otherwise. We evaluate SCDP on manipulation and navigation tasks, and results show that it enhances legibility in ambiguous settings while preserving optimal efficiency when legibility is unnecessary, all without retraining the base policy.

---

## 论文详细总结（自动生成）

以下是对论文《Encoding Predictability and Legibility for Style-Conditioned Diffusion Policy》的结构化深入总结：

### 1. 核心问题与研究动机
在人机协作（HRI）中，机器人动作的**可读性（Legibility）**至关重要，它能让渡人类直观理解机器人的意图，增强安全感。然而，高可读性动作通常意味着夸张、非最优的轨迹，会牺牲**效率（Efficiency）**。
*   **研究动机**：现有的方法（如 Legibility Diffuser）往往在所有场景下都生成夸张动作，但在目标明确、无歧义的简单场景中，这种夸张是多余且低效的。
*   **核心问题**：如何根据环境的动态歧义程度，在“意图表达”与“运动效率”之间取得自动平衡，且无需对基础策略进行大规模重训。

### 2. 方法论
论文提出了 **SCDP（风格条件扩散策略）**，其核心思想是采用模块化架构对预训练的扩散模型进行后处理调节。
*   **基础模型**：使用基于 U-Net 的扩散策略（Diffusion Policy），输入为机器人状态和目标位置。
*   **场景编码器（Scene Encoder）**：一个轻量级 MLP，将目标物体与干扰物体的相对位置、距离编码为潜在上下文向量 $c$。
*   **风格预测器（Conditioning Predictors）**：包含两个独立的 MLP（可读性预测器和可预测性预测器）。它们通过 **FiLM（特征线性调制）** 技术生成缩放因子 $\gamma$ 和偏移量 $\beta$，作用于冻结的基础 U-Net 瓶颈层，从而引导轨迹生成。
*   **歧义检测模块（Ambiguity Detection）**：引入“**歧义椭圆（Ellipse of Ambiguity）**”几何准则。如果干扰目标落在机器人与目标之间的特定椭圆区域内，则判定为有歧义，激活“可读性”风格；否则激活“效率/可预测性”风格。
*   **训练流程**：首先冻结基础扩散策略的权重，仅利用少量特定风格（最可读或最高效）的演示数据训练场景编码器和预测器。

### 3. 实验设计
*   **实验场景**：
    1.  **Block Reach（方块抓取）**：Franka Emika Panda 机械臂在有干扰块的情况下触碰目标块。
    2.  **Navigation（导航）**：Turtlebot 机器人在房间内向两个潜在目标之一移动。
*   **基准模型（Benchmarks）**：
    *   **Diffusion Policy**：原始的扩散策略模型。
    *   **Legibility Diffuser**：基于分类器自由引导（Classifier-free guidance）的现有可读性增强方法。
*   **评估指标**：
    *   **脱离得分（Detachment Score）**：衡量轨迹偏离干扰目标的程度（可读性指标）。
    *   **轨迹效率（Efficiency）**：路径长度的倒数。
    *   **自适应透明度得分（Adaptive Transparency Score）**：结合歧义权重的综合评分，用于衡量模型在不同场景下的适应能力。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX A3000 GPU**。
*   **训练时长**：
    *   基础扩散策略：100 轮（Epochs）。
    *   场景编码器：50 轮（基于 5000 个随机配置预训练）。
    *   风格预测器：300 轮（基于原始数据集中前 20% 的特定风格子集进行微调）。
*   **推理效率**：平均总推理时间约为 **5 秒**，与基础模型相当，额外模块带来的延迟可忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中针对每种任务进行了 **100 次** 独立推理测试。
*   **覆盖范围**：涵盖了空间有歧义和无歧义两种典型配置。
*   **真实部署**：在真实的 Franka Emika Panda 机器人上进行了部署验证，结合 YOLO 进行目标检测，证明了算法的 Sim-to-Real 迁移能力。
*   **客观性**：实验对比了标准模型和最先进的可读性扩散模型，并使用了综合加权指标，评价较为客观。但实验场景相对单一（仅 2 个目标），未测试多目标复杂环境。

### 6. 主要结论与发现
*   **自适应性**：SCDP 在有歧义时能生成类似 Legibility Diffuser 的表达性动作，而在无歧义时能显著提升效率（得分从 0.32 提升至 0.76）。
*   **性能保持**：引入风格调节模块后，任务成功率保持在 **98% 以上**，说明风格约束不会破坏基础策略的底层操作能力。
*   **模块化优势**：通过冻结基础模型并训练轻量级 MLP，实现了高效的风格迁移，无需昂贵的全量数据重训。

### 7. 优点
*   **灵活性高**：模块化设计允许在不改变核心策略的情况下，通过更换预测器来学习安全、节能等其他风格。
*   **环境感知**：相比于手动调节引导权重的现有方法，SCDP 能根据场景几何特征自动切换行为模式。
*   **数据效率**：仅需少量（20%）的风格化数据即可完成微调。

### 8. 不足与局限
*   **性能上限受限**：由于是基于演示数据的模仿学习，模型生成轨迹的可读性上限受限于训练集中最优秀的样本，无法像基于优化的方法那样生成超越数据的极端夸张动作。
*   **歧义检测过于简化**：目前的“歧义椭圆”是基于几何位置的启发式代理指标，可能无法完全捕捉人类复杂的心理推理过程。
*   **扩展性挑战**：实验仅针对两个潜在目标，在目标数量增加或环境动态变化（如障碍物移动）时的表现尚待验证。

（完）
