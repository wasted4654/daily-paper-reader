---
title: "TRACE: Trajectory Recovery with State Propagation Diffusion for Urban Mobility"
title_zh: TRACE：面向城市移动性的状态传播扩散轨迹恢复模型
authors: "Jinming Wang, Hai Wang, Hongkai Wen, Geyong Min, Man Luo"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19474v1"
tags: ["query:genad"]
score: 8.0
evidence: 用于城市移动轨迹恢复的状态传播扩散模型
tldr: "针对城市移动性中GPS轨迹稀疏且不完整的问题，本文提出了一种名为TRACE的新型扩散模型。该模型核心是状态传播扩散模型（SPDM），通过引入记忆机制在去噪过程中保留并利用中间结果，从而有效恢复复杂的轨迹片段。在多个真实数据集上的实验表明，TRACE在保持较低推理开销的同时，精度提升超过26%，为位置服务和智慧城市应用提供了高质量的数据基础。"
selection_source: fresh_fetch
motivation: 现实中的GPS轨迹常因采样率低和基础设施覆盖有限而呈现稀疏且不均匀的特征，难以直接用于高质量的城市位置服务。
method: 提出状态传播扩散模型（SPDM），通过创新的记忆机制在去噪步骤间传递状态，增强对难以恢复轨迹段的重建能力。
result: "在多个真实世界数据集上，TRACE相比现有最先进方法实现了超过26%的精度提升，且推理开销较小。"
conclusion: TRACE通过结合扩散模型与状态传播机制，显著提升了轨迹恢复的质量，为数据驱动的城市应用奠定了坚实基础。
---

## 摘要
高质量的 GPS 轨迹对于基于位置的网络服务和智慧城市应用（包括导航、网约车和配送）至关重要。然而，由于数据采集过程中的低采样率和有限的基础设施覆盖，现实世界的轨迹通常是稀疏的，且位置点分布不均。鉴于其复杂且不规则的时空模式，将这些轨迹恢复为密集且连续的形式虽然至关重要，但极具挑战性。在本文中，我们提出了一种名为 TRACE 的新型轨迹恢复扩散模型，该模型能够从稀疏且不完整的输入中重建密集且连续的轨迹。TRACE 的核心是我们提出的一种状态传播扩散模型（SPDM），它集成了一种新颖的记忆机制，使得 TRACE 在去噪过程中能够保留并利用前几步的中间结果，从而有效地重建那些难以恢复的轨迹段。在多个真实世界数据集上的广泛实验表明，TRACE 的性能优于现有最先进的方法，在没有显著推理开销的情况下，准确率提升了 26% 以上。我们的工作巩固了移动和网络连接位置服务的基础，提升了数据驱动型城市应用的质量和公平性。代码可在以下网址获取：https://github.com/JinmingWang/TRACE。

## Abstract
High-quality GPS trajectories are essential for location-based web services and smart city applications, including navigation, ride-sharing and delivery. However, due to low sampling rates and limited infrastructure coverage during data collection, real-world trajectories are often sparse and feature unevenly distributed location points. Recovering these trajectories into dense and continuous forms is essential but challenging, given their complex and irregular spatio-temporal patterns. In this paper, we introduce a novel diffusion model for trajectory recovery named TRACE, which reconstruct dense and continuous trajectories from sparse and incomplete inputs. At the core of TRACE, we propose a State Propagation Diffusion Model (SPDM), which integrates a novel memory mechanism, so that during the denoising process, TRACE can retain and leverage intermediate results from previous steps to effectively reconstruct those hard-to-recover trajectory segments. Extensive experiments on multiple real-world datasets show that TRACE outperforms the state-of-the-art, offering $>$26\% accuracy improvement without significant inference overhead. Our work strengthens the foundation for mobile and web-connected location services, advancing the quality and fairness of data-driven urban applications. Code is available at: https://github.com/JinmingWang/TRACE

---

## 论文详细总结（自动生成）

以下是对论文《TRACE: Trajectory Recovery with State Propagation Diffusion for Urban Mobility》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从现实世界中**稀疏且不规则**（采样率低、分布不均）的 GPS 轨迹中恢复出高精度、连续且密集的轨迹。
*   **研究动机**：
    *   高质量轨迹是导航、网约车、物流配送等智慧城市应用的基础。
    *   现有方法存在局限：RNN 易产生长距离误差累积；Transformer 在时空分布极度倾斜时表现不佳；传统生成模型（VAE/GAN）易出现过度平滑或模式崩溃。
    *   **扩散模型的痛点**：标准扩散模型在去噪过程中，每一步都是孤立处理输入，忽略了前一步推断出的中间特征，导致在处理长距离、高不确定性的轨迹缺口时效果有限。

### 2. 方法论：TRACE 框架
TRACE 的核心思想是将轨迹恢复视为**条件生成任务**，并引入**状态传播机制**。
*   **条件聚合阶段**：
    *   将观测时间点 $S$ 与查询时间点 $Q$ 合并，构建统一的时间序列。
    *   引入多维先验知识：包括二进制掩码（标识待恢复位置）、线性插值轨迹（提供几何先验）以及多模态上下文（用户 ID、日期、运单信息等）。
*   **状态传播扩散模型 (SPDM)**：
    *   **核心创新**：设计了一个“有记忆”的去噪过程。通过**状态传播管线**，将每一步去噪产生的多尺度时空特征（隐藏状态）传递给下一步。
    *   **网络架构**：采用改进的 **UNet** 作为去噪骨干，并结合 **MCGRU**（多尺度卷积门控循环单元）。MCGRU 负责跨扩散步骤更新和累积知识，使模型能集中处理难恢复的片段。
*   **训练算法**：
    *   由于引入了步骤间的依赖，TRACE 放弃了传统扩散模型的随机采样 $t$ 的训练方式，转而采用**顺序训练范式**。
    *   提出 **2步联合损失函数**（平衡计算开销与性能）和**动态批次管理技术**（确保模型在所有去噪阶段的泛化能力）。

### 3. 实验设计
*   **数据集**：
    1.  **西安出租车数据集**：密集且平滑的城市轨迹。
    2.  **成都出租车数据集**：同上。
    3.  **物流数据集**：末端配送员在居住区的移动轨迹，具有极强的不规则采样特征。
*   **评估指标**：均方误差 (MSE)、平均绝对误差 (MAE)、归一化动态时间规整 (NDTW)。
*   **对比方法 (Benchmarks)**：
    *   **RNN 类**：DeepMove。
    *   **Transformer 类**：AttnMove。
    *   **扩散模型类**：PriSTI（时空插值）、DT+RP（DiffTraj 结合 RePaint 图像修复思路）。
    *   **消融版本**：TRACE w/o SPDM（去除状态传播的标准扩散模型）。

### 4. 资源与算力
*   **硬件设备**：论文明确提到实验在一台配备 **NVIDIA GeForce RTX 3070 Ti Laptop GPU** 的笔记本电脑上运行。
*   **推理效率**：在 100 个样本的批处理下，结合 DDIM 加速技术，11 步去噪仅需约 3.58 秒。
*   **训练细节**：文中未详细说明总训练时长，但强调了其参数量（6.28M）相对较小，具有较高的参数效率。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同性质的数据集上进行了测试，涵盖了 50% 到 70% 的不同丢包率（稀疏度）。
*   **消融实验**：详细对比了有无 SPDM、不同的训练步数（2-step vs 3-step）、不同的批次管理策略。
*   **鲁棒性分析**：专门针对“时空不规则性”进行了定量分析，验证了模型在极端不均匀分布下的稳定性。
*   **评价**：实验设计较为全面，对比了不同架构的 SOTA 方法，且通过案例研究（Case Study）展示了在物流场景下的实际应用价值，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **精度大幅提升**：TRACE 在所有指标上均优于基准模型，相比标准扩散模型，恢复精度提升了 **26.65%**。
*   **处理不规则性能力强**：SPDM 机制能有效利用历史去噪状态，解决长距离、非线性轨迹段的恢复难题。
*   **高效性**：SPDM 仅增加了约 12% 的参数量和极小的推理延迟，但显著提高了生成质量的上限。
*   **实际价值**：在物流案例中，使用 TRACE 恢复的轨迹计算出的移动速度和距离准确度从 60% 提升至 83%。

### 7. 优点
*   **创新性记忆机制**：首次将状态传播引入轨迹恢复扩散模型，解决了扩散步骤间信息孤立的问题。
*   **端到端处理不规则性**：无需依赖复杂的路网地图匹配（Map-matching），在自由空间轨迹恢复上表现卓越。
*   **训练策略优化**：针对有状态扩散模型设计的动态批次管理解决了训练收敛难的问题。

### 8. 不足与局限
*   **计算开销**：尽管有加速，但扩散模型固有的多步迭代特性使其推理速度仍慢于 RNN 或 Transformer 等单次前向传播模型。
*   **路网约束缺失**：论文主要关注自由空间恢复，未显式引入城市路网拓扑约束（虽然这增加了通用性，但在已知路网的场景下可能损失部分物理一致性）。
*   **长序列限制**：实验主要针对长度为 512 的序列，对于超长轨迹的显存占用和计算压力未做深入探讨。

（完）
