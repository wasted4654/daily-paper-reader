---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:diffpl"]
score: 9.0
evidence: 用于导航和运动规划的统一图像空间扩散策略
tldr: 针对机器人视觉运动规划中目标指定复杂及模型资源消耗大的问题，本文提出 DiffusionAnything。这是一个统一的图像空间扩散策略，通过多尺度特征调制，仅需 5 分钟自监督数据即可实现米级导航与厘米级抓取前运动。该模型仅依赖 RGB 输入，具备低显存占用和高推理频率，在未知场景中展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言动作模型计算资源需求高且在陌生场景中泛化能力差，难以实现高效且通用的端到端运动规划。
method: 提出一种统一的扩散策略，结合多尺度 FiLM 条件机制、轨迹对齐深度预测及自监督注意力机制，实现跨尺度的动作推理。
result: 模型仅需 2.0 GB 显存即可在 10 Hz 频率下运行，并在全新场景中实现了稳健的零样本泛化表现。
conclusion: 该研究证明了通过轻量级扩散模型和自监督学习，可以实现高效、统一且易于机载部署的机器人导航与操作控制。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源、大量的训练数据，且在陌生场景中无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖大规模、高算力需求的视觉-语言-动作（VLA）模型的情况下，实现机器人**米级导航**与**厘米级精细操作（抓取前运动）**的统一端到端规划。
*   **研究背景**：传统的机器人系统通常将导航和操作分离，导致架构冗余、延迟高且切换僵硬。虽然现有的 VLA 模型（如 RT-2）试图统一任务，但其计算成本极高，且在面对新场景时往往缺乏零样本（Zero-shot）泛化能力。

### 2. 方法论
核心思想是利用一个轻量级的**上下文感知扩散策略（Context-Aware Diffusion Policy）**，通过图像空间进行轨迹预测。
*   **多尺度 FiLM 条件化**：在 UNet 骨干网络的四个尺度上应用特征线性调制（FiLM）。上下文向量 $c$ 包含：任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图。这使得单一模型能根据任务动态调整关注区域（如导航关注地面，抓取关注物体）。
*   **轨迹对齐深度推理**：模型不生成全图深度，而是仅预测生成的轨迹路径点上的深度值。这种设计显著降低了计算开销，同时保证了避障和精确接近所需的度量 3D 信息。
*   **自监督目标学习**：利用 AnyTraverse 框架生成自监督的通行性（Traversability）和注意力图，无需人工演示或昂贵的视觉语言模型。
*   **双模式目标指定**：支持自主目标选择（基于场景上下文）或通过参考图像特征匹配进行用户指定的目标导航。

### 3. 实验设计
*   **实验场景**：包括走廊导航、室内避障以及桌面物体的抓取前接近。
*   **机器人平台**：Unitree G1 人形机器人（配备 RGB 摄像头，具备 7 自由度手臂和双足行走能力）。
*   **对比基准（Benchmark）**：
    *   **导航**：对比了 **NoMaD**（基于大规模数据集训练的视觉导航模型）。
    *   **操作**：对比了 NVIDIA 的 **GR00T (n1.5/n1.6)** 模型。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   主模型：使用 20 分钟视频数据训练 50 轮，耗时约 **2 小时**。
    *   新任务适配：仅需 5 分钟新数据，微调 10 轮，耗时 **15-20 分钟**。
*   **推理性能**：显存占用仅 **2.0 GB**，推理频率达 **10 Hz**，非常适合机载部署。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了探索导航、目标导向导航和抓取前规划三大类任务。
*   **充分性评价**：
    *   **客观性**：在全新的、未见过的场景中进行了零样本泛化测试，验证了模型的鲁棒性。
    *   **对比性**：通过与 NoMaD 和 GR00T 的对比，证明了该模型在小样本训练下的优越性。
    *   **消融/验证**：展示了不同任务下注意力机制的自动切换（图 4）。
    *   **局限性**：虽然实验涵盖了关键任务，但样本量（如抓取前规划的测试次数）相对较小，且主要集中在室内环境。

### 6. 主要结论与发现
*   **跨尺度统一**：单一模型通过上下文调制，成功将导航精度（约 30cm 误差）提升至抓取精度（约 4.7cm 误差），精度提升达 6 倍。
*   **零样本泛化**：在导航任务中实现了 100% 的避障成功率，在抓取前规划中实现了 70.6% 的成功率，表现优于在特定分布上训练的 GR00T 模型。
*   **高效性**：证明了通过结构化的几何推理（通行性、空间注意力）而非纯端到端的语义记忆，可以大幅减少对训练数据和算力的依赖。

### 7. 优点
*   **轻量化与实时性**：2GB 显存和 10Hz 频率使其能在人形机器人上本地运行。
*   **极低的数据需求**：每个任务仅需 5 分钟自监督数据，极大地降低了机器人技能获取的门槛。
*   **多任务融合**：巧妙地解决了导航与操作在空间尺度上的矛盾，实现了平滑的任务切换。

### 8. 不足与局限
*   **依赖外部监督源**：模型训练依赖于 AnyTraverse 和深度估计模型（如 Depth Pro）生成的伪标签，若这些源模型出错，会影响策略质量。
*   **缺乏力反馈**：目前仅限于运动规划（抓取前），未涉及实际抓取时的接触力控制。
*   **环境限制**：主要在受控的室内环境测试，在极端光照或高度动态的室外场景下的表现尚待验证。

（完）
