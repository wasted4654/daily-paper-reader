---
title: Temporally Decoupled Diffusion Planning for Autonomous Driving
title_zh: 自动驾驶的时间解耦扩散规划
authors: "Xiang Li, Bikun Wang, John Zhang, Jianjun Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25462v1"
tags: ["query:genad"]
score: 10.0
evidence: 基于扩散模型的自动驾驶运动规划
tldr: 针对自动驾驶中长短期规划需求不同的挑战，本文提出时间解耦扩散模型（TDDM）。该模型通过“噪声即掩码”范式将轨迹划分为具有独立噪声水平的片段，利用远期目标引导近期路径生成。通过引入TD-AdaLN架构和非对称时间无分类器引导，TDDM在nuPlan基准测试中表现优异，特别是在复杂场景下展现出更强的规划能力。
selection_source: fresh_fetch
motivation: 现有的扩散模型将轨迹视为整体，忽略了近期规划受动力学约束而远期规划受导航目标约束的时间异构性。
method: 提出TDDM模型，采用噪声即掩码范式、TD-AdaLN架构以及非对称时间无分类器引导，实现轨迹片段的独立建模与相互引导。
result: 在nuPlan基准测试中达到或超过了现有最先进水平，尤其在极具挑战性的Test14-hard子集中表现卓越。
conclusion: 时间解耦的扩散规划方法能有效平衡即时安全与长期目标，显著提升了自动驾驶在复杂动态环境下的规划性能。
---

## 摘要
动态城市环境中的运动规划需要平衡即时安全与长期目标。虽然扩散模型能有效捕捉多模态决策，但现有方法将轨迹视为统一实体，忽略了异构的时间依赖性，即短期规划受瞬时动力学约束，而长期规划受导航目标约束。为解决这一问题，我们提出了时间解耦扩散模型 (TDDM)，通过“噪声即掩码”范式重新构建轨迹生成。通过将轨迹划分为具有独立噪声水平的分段，我们将高噪声隐式地视为信息缺失，将弱噪声视为上下文线索。这促使模型利用保存较好的时间上下文中的内部相关性，来重建受损的短期状态。在架构上，我们引入了时间解耦自适应层归一化 (TD-AdaLN) 以注入特定分段的时间步。在推理过程中，我们的非对称时间无分类器引导 (Asymmetric Temporal Classifier-Free Guidance) 利用弱噪声的远期先验来引导即时路径生成。在 nuPlan 基准测试上的评估表明，TDDM 达到或超过了最先进的基准水平，特别是在极具挑战性的 Test14-hard 子集中表现出色。

## Abstract
Motion planning in dynamic urban environments requires balancing immediate safety with long-term goals. While diffusion models effectively capture multi-modal decision-making, existing approaches treat trajectories as monolithic entities, overlooking heterogeneous temporal dependencies where near-term plans are constrained by instantaneous dynamics and far-term plans by navigational goals. To address this, we propose Temporally Decoupled Diffusion Model (TDDM), which reformulates trajectory generation via a noise-as-mask paradigm. By partitioning trajectories into segments with independent noise levels, we implicitly treat high noise as information voids and weak noise as contextual cues. This compels the model to reconstruct corrupted near-term states by leveraging internal correlations with better-preserved temporal contexts. Architecturally, we introduce a Temporally Decoupled Adaptive Layer Normalization (TD-AdaLN) to inject segment-specific timesteps. During inference, our Asymmetric Temporal Classifier-Free Guidance utilizes weakly noised far-term priors to guide immediate path generation. Evaluations on the nuPlan benchmark show TDDM approaches or exceeds state-of-the-art baselines, particularly excelling in the challenging Test14-hard subset.

---

## 论文详细总结（自动生成）

以下是对论文《Temporally Decoupled Diffusion Planning for Autonomous Driving》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的基于扩散模型的运动规划方法通常将整个轨迹视为一个不可分割的整体（Monolithic），忽略了轨迹在时间维度上的异构依赖性。
*   **研究动机**：在自动驾驶中，**短期规划**（Near-term）高度依赖于车辆当前的精确状态和瞬时环境动力学（如避障），而**长期规划**（Far-term）则更侧重于实现导航目标。现有模型统一加噪/去噪的方式限制了模型平衡短期安全性和长期一致性的能力。

### 2. 方法论
*   **核心思想**：提出**时间解耦扩散模型（TDDM）**，采用“噪声即掩码（Noise-as-mask）”范式，将轨迹划分为多个独立加噪的时间片段，通过建模片段间的相关性来生成更具前瞻性的路径。
*   **关键技术细节**：
    *   **轨迹令牌化（Trajectory Tokenization）**：将轨迹划分为 $N$ 个时间片段（Tokens），并聚类为宏组（如近期和远期）。
    *   **独立加噪训练**：在训练时，为不同片段分配独立的扩散时间步（Timesteps），迫使模型利用保留较好的远期上下文（弱噪声）来重建受损严重的近期状态（高噪声）。
    *   **TD-AdaLN 架构**：在 Diffusion Transformer (DiT) 基础上设计了“时间解耦自适应层归一化”模块，支持为每个时间片段注入独立的扩散时间步信息。
    *   **非对称时间无分类器引导（Asymmetric Temporal CFG）**：在推理阶段，通过并行运行“全序列扩散”和“以远期先验为条件的补全任务”，利用远期目标引导近期路径的生成。

### 3. 实验设计
*   **数据集与场景**：使用大规模自动驾驶数据集 **nuPlan**（约1500小时真实驾驶数据）。
*   **基准测试（Benchmarks）**：
    *   **Val14**：1118个常规验证场景。
    *   **Test14-hard**：272个极具挑战性的长尾、高风险场景。
    *   **Test14-random**：200多个随机采样场景。
*   **对比方法**：包括规则/混合方法（IDM, PDM-Closed/Hybrid）、学习型方法（UrbanDriver, GameFormer, PlanTF, PLUTO）以及同类扩散规划器（Diffusion Planner）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA 3080 GPU**。
*   **训练细节**：在 nuPlan 的 20 万个场景子集上训练了 **500 个 Epoch**。
*   **效率**：推理时采用 DPM-Solver++，仅需 2 个去噪步骤，输出 8 秒（10Hz）的轨迹。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了闭环非反应式（Non-reactive）和反应式（Reactive）两种评估模式。
*   **消融实验**：进行了 6 组核心组件消融（验证了轨迹令牌化、TD-AdaLN、独立加噪和非对称 CFG 的有效性），并针对令牌数量（Token Num）和引导权重（CFG Scale）进行了参数敏感性分析。
*   **充分性评价**：实验设计较为全面，通过 Test14-hard 专项测试证明了模型在复杂场景下的鲁棒性。对比实验涵盖了从经典规则到最新生成式模型的多种基线，结果具有客观性。

### 6. 主要结论与发现
*   **性能卓越**：TDDM 在 nuPlan 多个基准上达到或超过了 SOTA 水平，尤其在 **Test14-hard** 场景中表现突出（得分 77.95，显著高于 Diffusion Planner 的 75.67）。
*   **高数据效率**：仅使用 20 万个场景训练，其表现就优于使用 100 万个场景训练的同类模型。
*   **解耦优势**：证明了显式建模时间结构对于处理复杂城市环境中的运动规划至关重要，非对称引导能有效避免规划的近视和不一致问题。

### 7. 优点（亮点）
*   **创新范式**：首次将视频生成领域的“时间解耦”思想引入自动驾驶轨迹规划，解决了扩散模型处理长时序轨迹时的单调性问题。
*   **架构适配**：TD-AdaLN 的设计巧妙地解决了 Transformer 难以同时处理多个扩散时间步的技术障碍。
*   **鲁棒性强**：在窄路转弯、障碍物绕行等挑战性场景中，比传统扩散模型表现出更好的决策果断性和安全性。

### 8. 不足与局限
*   **反应式评估略逊**：在完全反应式（Reactive）闭环仿真中的得分虽具竞争力，但仍略低于部分顶尖方法（如 PDM-Closed）。
*   **依赖锚点**：目前仍依赖于预定义的轨迹锚点集（Anchor-based），限制了轨迹生成的绝对自由度。
*   **交互建模**：对智能体之间复杂博弈关系的显式建模仍有提升空间，未来可能需要结合博弈论原则进一步优化。

（完）
