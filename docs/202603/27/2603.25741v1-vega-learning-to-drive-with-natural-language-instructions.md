---
title: "Vega: Learning to Drive with Natural Language Instructions"
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v1"
tags: ["query:genad"]
score: 9.0
evidence: 端到端模型中使用扩散范式生成未来轨迹
tldr: 本研究针对现有自动驾驶模型缺乏遵循多样化指令以实现个性化驾驶的问题，提出了Vega模型。首先构建了包含10万个场景的大规模指令驾驶数据集InstructScene。Vega采用统一的视觉-语言-世界-动作架构，结合自回归范式处理多模态输入，并利用扩散模型生成未来预测和行驶轨迹。实验证明，Vega在规划性能和指令遵循能力上均表现优异，为智能化和个性化驾驶系统奠定了基础。
selection_source: fresh_fetch
motivation: 现有自动驾驶模型多将语言用于场景描述，缺乏根据用户多样化指令进行个性化驾驶的能力。
method: 提出了Vega模型，结合自回归范式处理视觉与语言输入，并利用扩散模型进行世界建模与轨迹规划。
result: 实验结果显示，Vega在规划性能上达到领先水平，并展现出极强的自然语言指令遵循能力。
conclusion: 该研究通过构建大规模指令数据集和创新的多模态架构，显著提升了自动驾驶系统的智能性与个性化水平。
---

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Vega: Learning to Drive with Natural Language Instructions》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自动驾驶视觉-语言-动作（VLA）模型大多仅将语言用于场景描述或逻辑推理，缺乏遵循多样化、个性化用户指令（如“靠边停车”、“超车以赶上绿灯”）的能力。
*   **研究背景**：传统的模仿学习（Imitation Learning）通常学习平均化的专家策略，难以处理开放式、灵活的自然语言指令。此外，高维视觉输入与低维动作输出之间存在巨大的信息鸿沟，导致模型难以精准映射指令到具体动作。

### 2. 方法论
*   **核心思想**：提出一个统一的**视觉-语言-世界-动作（Vision-Language-World-Action）**模型——**Vega**。通过引入“世界建模”（未来图像生成）作为密集的像素级监督信号，增强模型对环境动力学和指令因果关系的理解。
*   **关键技术细节**：
    *   **数据集构建 (InstructScene)**：基于 NAVSIM 构建了包含约 10 万个场景的数据集，利用强大的 VLM（如 Qwen2.5-VL）结合基于规则的方法，自动生成多样化的驾驶指令及对应轨迹。
    *   **混合架构**：采用集成 Transformer 架构，结合了**自回归（AR）**和**扩散（Diffusion）**范式。
        *   **自回归部分**：用于处理视觉观测和语言指令的理解（基于 Qwen2.5 骨干网络）。
        *   **扩散部分**：用于生成未来图像预测（世界建模）和规划行驶轨迹（动作）。
    *   **混合 Transformer (MoT)**：为了有效解耦不同模态并提升容量，模型为视觉理解、文本理解、图像生成和动作规划分别设置了独立的投影层和参数块。
    *   **因果建模**：通过因果注意力机制，确保模型按照“感知 -> 指令理解 -> 动作决策 -> 结果预测”的逻辑链条进行推理。

### 3. 实验设计
*   **数据集与场景**：主要在 **NAVSIM v1** 和 **NAVSIM v2** 两个基准上进行评估。NAVSIM v2 引入了更具挑战性的反应式交通流模拟。
*   **Benchmark 评价指标**：
    *   **PDMS / EPDMS**：综合评分，包含无碰撞率（NC）、可行驶区域合规性（DAC）、舒适度（Comf）、时间碰撞距离（TTC）等。
*   **对比方法**：对比了 TransFuser、HydraMDP++、DriveVLA-W0、DiffusionDrive 等 SOTA（先进）方法，涵盖了基于 BEV 的方法和基于 VLA 的方法。

### 4. 资源与算力
*   **算力投入**：论文明确指出，模型在 **8 台 H20 GPU** 上进行了训练。
*   **训练时长**：总计训练了 **200,000 个步数 (steps)**，采用单阶段训练范式，学习率为 2e-5，包含 2500 步预热。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在两个版本的 NAVSIM 上进行了全面对比，结果显示 Vega 在 NAVSIM v2 上达到了 SOTA 水平（EPDMS 86.9/89.4）。
    *   **消融实验**：针对“未来帧预测任务的影响”、“图像-动作交织序列的长度”、“独立动作模块的必要性”等关键设计进行了验证。
*   **充分性评价**：实验设计较为充分，不仅有定量指标，还提供了丰富的定性可视化分析（如不同指令下的轨迹差异、未来图像生成的连贯性），证明了模型确实在“遵循指令”而非仅仅是模仿轨迹。

### 6. 主要结论与发现
*   **世界建模的增益**：引入未来图像生成任务能显著提升规划性能，因为它提供了比稀疏动作信号更丰富的监督。
*   **指令遵循能力**：Vega 能够根据不同的指令（如“加速赶超” vs “保持平稳”）生成截然不同的合理轨迹和对应的未来视觉预测。
*   **架构优势**：MoT 架构比单一的 VLM 或纯扩散模型在处理多模态驾驶任务时更具效率和性能优势。

### 7. 优点与亮点
*   **统一性**：将理解、生成、预测和规划整合进一个 Transformer 框架，实现了真正的端到端指令驱动驾驶。
*   **数据集贡献**：InstructScene 数据集的发布为社区研究“指令驾驶”提供了重要资源。
*   **闭环一致性**：通过同时预测动作和未来图像，确保了模型决策与环境演变在逻辑上的一致性。

### 8. 不足与局限
*   **NAVSIM v1 表现**：在 NAVSIM v1 上的表现略逊于某些特定优化的 VLA 模型，作者认为这与 v1 指标偏向保守策略有关。
*   **传感器局限**：目前主要依赖前视摄像头图像，尚未充分利用环视或 LiDAR 等多传感器融合（虽然架构上支持扩展）。
*   **推理延迟**：由于涉及扩散模型的多次去噪迭代，实时推理性能（Latency）在文中未做极详细的量化对比，可能存在计算开销较大的风险。

（完）
