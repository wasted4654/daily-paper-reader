---
title: "TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization"
title_zh: TIGFlow-GRPO：通过交互感知流匹配与奖励驱动优化的轨迹预测
authors: "Xuepeng Jing, Wenhuan Lu, Hao Meng, Zhizhi Yu, Jianguo Wei"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24936v1"
tags: ["query:genad"]
score: 8.0
evidence: 自动驾驶环境下的生成式轨迹预测
tldr: 针对自动驾驶和人群监控中的轨迹预测问题，本文提出 TIGFlow-GRPO 框架。该框架结合了条件流匹配（CFM）与轨迹交互图（TIG）来捕捉复杂的时空交互，并引入基于奖励驱动的 GRPO 优化阶段。通过将确定性流推导转化为随机 SDE 采样，并结合社会规范与地图约束的复合奖励进行后训练，显著提升了预测轨迹的社会合规性和物理可行性。
selection_source: fresh_fetch
motivation: 现有的流匹配轨迹预测方法主要依赖监督学习，难以充分反映复杂的社会规范和场景物理约束。
method: 提出两阶段框架，首先利用轨迹交互图增强上下文编码，随后通过随机微分方程采样与组相对策略优化（GRPO）进行行为对齐。
result: 在 ETH/UCY 和 SDD 数据集上的实验表明，该方法在提高预测准确性的同时，增强了长时稳定性和环境适应性。
conclusion: 该研究成功将流匹配生成模型与强化学习对齐技术相结合，为动态环境下的行为感知轨迹建模提供了有效方案。
---

## 摘要
行人轨迹预测对于在视觉复杂环境中运行的智能多媒体系统（如自动驾驶和人群监控）至关重要。尽管条件流匹配（CFM）在根据时空观测建模轨迹分布方面表现出强大的能力，但现有方法仍主要侧重于监督拟合，这可能导致生成的轨迹无法充分反映社交规范和场景约束。为了解决这一问题，我们提出了 TIGFlow-GRPO，这是一个将基于流的轨迹生成与行为规则对齐的两阶段生成框架。在第一阶段，我们构建了一个基于 CFM 的预测器，并配备了轨迹交互图（TIG）模块，以建模细粒度的视觉空间交互并增强上下文编码。该阶段更有效地捕捉了智能体与智能体以及智能体与场景之间的关系，为后续对齐提供了更具信息量的条件特征。在第二阶段，我们进行 Flow-GRPO 后训练，其中确定性流展开被重新表述为随机的 ODE 到 SDE 采样以实现轨迹探索，并且通过复合奖励结合了视角感知的社交合规性与地图感知的物理可行性。通过评估 SDE 展开探索的轨迹，GRPO 逐步引导多模态预测走向行为上合理的未来。在 ETH/UCY 和 SDD 数据集上的实验表明，TIGFlow-GRPO 在提高预测准确性和长时稳定性的同时，生成的轨迹更符合社交规范且更具物理可行性。这些结果表明，所提出的框架为在动态多媒体环境中将基于流的轨迹建模与行为感知对齐相结合提供了一种有效途径。

## Abstract
Human trajectory forecasting is important for intelligent multimedia systems operating in visually complex environments, such as autonomous driving and crowd surveillance. Although Conditional Flow Matching (CFM) has shown strong ability in modeling trajectory distributions from spatio-temporal observations, existing approaches still focus primarily on supervised fitting, which may leave social norms and scene constraints insufficiently reflected in generated trajectories. To address this issue, we propose TIGFlow-GRPO, a two-stage generative framework that aligns flow-based trajectory generation with behavioral rules. In the first stage, we build a CFM-based predictor with a Trajectory-Interaction-Graph (TIG) module to model fine-grained visual-spatial interactions and strengthen context encoding. This stage captures both agent-agent and agent-scene relations more effectively, providing more informative conditional features for subsequent alignment. In the second stage, we perform Flow-GRPO post-training,where deterministic flow rollout is reformulated as stochastic ODE-to-SDE sampling to enable trajectory exploration, and a composite reward combines view-aware social compliance with map-aware physical feasibility. By evaluating trajectories explored through SDE rollout, GRPO progressively steers multimodal predictions toward behaviorally plausible futures. Experiments on the ETH/UCY and SDD datasets show that TIGFlow-GRPO improves forecasting accuracy and long-horizon stability while generating trajectories that are more socially compliant and physically feasible. These results suggest that the proposed framework provides an effective way to connect flow-based trajectory modeling with behavior-aware alignment in dynamic multimedia environments.

---

## 论文详细总结（自动生成）

### 论文总结：TIGFlow-GRPO 轨迹预测框架

#### 1. 核心问题与研究动机
论文关注的是**行人轨迹预测**，这是自动驾驶和人群监控等智能系统的核心任务。
*   **研究背景**：人类运动受意图、视觉感知、社交规范及物理环境（如障碍物）的共同影响，具有高度的多模态不确定性。
*   **核心问题**：现有的生成式模型（如条件流匹配 CFM）主要依赖监督学习来拟合数据分布，但往往难以直接优化非微分的约束（如社交礼仪、地图边界）。这导致生成的轨迹虽然在统计上合理，但在物理上可能不可行（如穿墙）或违反社交规范（如碰撞）。
*   **研究动机**：如何将流匹配（Flow Matching）的生成能力与强化学习（RL）的对齐能力结合，使预测轨迹既准确又符合现实世界的行为规则。

#### 2. 方法论
论文提出了一个名为 **TIGFlow-GRPO** 的两阶段框架：

*   **第一阶段：条件流匹配预训练（TIG-GAT 编码）**
    *   **TIG-GAT 模块**：构建“轨迹交互图”，采用以目标为中心的动态图机制。它根据行人的**视野（Field-of-View）**筛选邻居，并利用**门控注意力机制（Gated Attention）**融合社交上下文和物理特征，生成丰富的条件特征。
    *   **CFM 主干**：学习从高斯先验到目标轨迹分布的连续向量场。
*   **第二阶段：Flow-GRPO 行为对齐后训练**
    *   **ODE 到 SDE 的转换**：标准流匹配是确定性的（ODE），不利于强化学习探索。作者将其重新表述为随机微分方程（SDE）采样，在生成过程中引入受控的随机性，从而创建可探索的策略空间。
    *   **GRPO 优化**：采用组相对策略优化（Group Relative Policy Optimization），通过计算一组生成样本的相对优势来更新模型，无需额外的判别器（Critic）网络，降低了开销。
    *   **复合奖励函数**：
        1.  **视角感知社交奖励**：根据行人的朝向和距离，对不同视野区域（强视角、弱视角、后方）的冲突行为进行差异化惩罚。
        2.  **地图感知语义奖励**：利用预计算的**有符号距离场（SDF）**，对轨迹偏离可行区域或进入障碍物区域进行连续惩罚。

#### 3. 实验设计
*   **数据集**：
    *   **ETH/UCY**：包含 5 个场景，主要用于评估社交交互建模能力。
    *   **Stanford Drone Dataset (SDD)**：包含复杂的校园环境，主要用于评估地图约束下的物理可行性。
*   **评估指标**：
    *   精度指标：$ADE_{min}$、$FDE_{min}$（最小位移误差）；$ADE_{avg}$、$FDE_{avg}$（平均位移误差）。
    *   合规性指标：**碰撞率（Collision Rate, Col）**，直接衡量社交合规性。
*   **对比方法**：包括 GroupNet、MID、EqMotion、EigenTraj、SingularTraj、MoFlow（基准模型）以及最新的 DD-MDN 等。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **软件**：基于 PyTorch 实现。
*   **训练细节**：论文提到了预训练阶段使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$；后训练阶段每组采样 $G=4$ 条轨迹进行 GRPO 优化。具体的训练时长未明确说明，但单卡配置表明该方法具有较好的计算效率。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流场景（ETH/UCY 的 5 个子集 + SDD）上进行了全面测试。
*   **消融实验**：针对 TIG-GAT 模块、社交奖励、地图奖励以及 GRPO 阶段分别做了消融，验证了各组件的必要性。
*   **长时分析**：专门测试了在 1.2s 到 4.8s 不同预测时长下的性能漂移情况。
*   **评价**：实验设计较为充分，涵盖了精度、鲁棒性、社交合规性等多个维度，对比基线涵盖了扩散模型、流匹配和图神经网络等多种主流范式，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **精度提升**：TIGFlow-GRPO 在 ETH/UCY 上的平均 ADE/FDE 达到 0.20/0.31，优于所有对比基线。
*   **社交合规性**：在 24 组实验设置中，该方法在 23 组中降低了碰撞率，平均碰撞率从 8.72% 降至 6.45%。
*   **长时稳定性**：在长达 4.8s 的预测周期内，该方法比基准模型 MoFlow 表现出更强的抗漂移能力。
*   **行为对齐**：通过 GRPO 后训练，模型能够有效抑制那些虽然符合统计分布但违反物理规则的“离群”轨迹。

#### 7. 优点
*   **创新性结合**：首次将 GRPO 这一高效的 RL 对齐技术引入流匹配轨迹预测领域。
*   **探索机制巧妙**：通过 ODE-to-SDE 的转换解决了流模型在强化学习中缺乏探索性的痛点。
*   **感知驱动**：社交奖励考虑了行人的视野（FoV），比单纯基于距离的惩罚更符合人类真实行为逻辑。
*   **物理一致性**：引入 SDF 连续反馈，使模型能更好地感知复杂的地图边界。

#### 8. 不足与局限
*   **视觉输入受限**：目前主要依赖坐标和语义地图，尚未充分利用原始视频像素或多模态大模型（VLM）进行更深层的场景推理。
*   **奖励权重敏感性**：复合奖励函数涉及多个超参数（如各奖励项的权重），在不同场景下可能需要精细调优。
*   **实时性讨论不足**：虽然单卡可练，但论文未详细给出推理阶段的延迟数据，对于自动驾驶等高实时性要求的应用，SDE 多步采样的效率仍需关注。

（完）
