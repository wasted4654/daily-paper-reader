---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v1"
tags: ["query:genad"]
score: 8.0
evidence: 为自动驾驶分割可行的车辆轨迹
tldr: 本研究提出GHOST，一种利用大规模行车记录仪视频进行自监督学习的自动驾驶轨迹预测方法。通过单目SfM恢复自车运动轨迹并投影至地面生成伪标签，训练深度分割网络从单张RGB图像预测可行使路径。该方法无需人工标注或显式道路建模，能隐式捕捉场景布局和车道拓扑，在NuScenes等数据集上表现出良好的泛化能力和跨平台迁移潜力。
selection_source: fresh_fetch
motivation: 旨在解决复杂城市环境下自动驾驶路径规划对大规模人工标注数据的依赖问题。
method: 利用单目SfM从海量视频中提取自车运动轨迹并投影为地面掩码，作为自监督信号训练图像分割网络。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过微调迁移至电动滑板车平台。
conclusion: 大规模自车运动蒸馏能有效提取结构化路径建议，为无需显式建模的轨迹假设估计提供了新途径。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下从单目图像中分割自动驾驶的可行车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成已行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步证明了通过轻量级微调将其迁移到电动滑板车平台的可行性。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且可泛化的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

### 论文总结：GHOST —— 基于观测到的 SfM 轨迹的地面投影假设预测

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶中的路径规划通常依赖昂贵的人工语义标注、高精地图（HD Maps）或校准后的传感器阵列。然而，互联网上存在海量的行车记录仪视频，这些视频隐含了人类驾驶员在复杂环境下的决策逻辑。
**核心问题**：如何利用这些无标注、未校准的单目视频作为“隐式监督”，学习车辆在各种场景下可行的行驶轨迹？
**研究背景**：论文提出 GHOST 框架，旨在通过自监督学习，从大规模互联网视频中提取车辆运动先验，使模型能够仅凭单张 RGB 图像预测出多条潜在的可行路径。

#### 2. 方法论：核心思想与关键技术
GHOST 的工作流程分为三个主要阶段：
*   **SfM 轨迹提取**：
    *   使用 **COLMAP** 对原始单目视频进行运动恢复结构（SfM）处理，提取相机的 6-DoF 位姿和稀疏 3D 点云。
    *   为了处理大规模数据，将长视频切分为 20 分钟的片段进行并行化处理。
*   **后处理与地面投影（伪标签生成）**：
    *   **高度估计**：利用 Theil-Sen 稳健回归算法，从稀疏点云中识别地面点，计算相机相对于地面的高度 $H_{cam}$。
    *   **掩码生成**：将未来 5 秒的相机轨迹投影到地面，并根据相机高度赋予一定的物理宽度，生成空间轨迹掩码（Ground Truth Mask）。
*   **轨迹分割模型训练**：
    *   **架构**：采用带有 ResNet34 编码器的 UNet 或 SegFormer 结构。
    *   **非对称损失函数（Asymmetric Loss）**：这是核心创新点。通过加权负对数似然损失，大幅增加对“漏报”（False Negatives）的惩罚，减少对“误报”（False Positives）的惩罚。
    *   **目的**：这种设计鼓励模型预测出所有潜在的可行区域（多假设），而不仅仅是复现视频中那一条特定的行驶路径。

#### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：从 YouTube 抓取 30 小时行车记录仪视频，涵盖 14 个城市、多种天气（雨、雪、晴）和光照条件，最终生成 65 万帧标注数据。
    *   **评估**：在 **NuScenes**（OpenLane-V2 子集）上进行验证。该数据集包含波士顿和新加坡的场景，对模型而言是完全的分布外（OOD）数据。
    *   **迁移实验**：在电动滑板车（GoPro 采集）上进行微调实验。
*   **Benchmark 与对比**：
    *   使用 **Soft IoU**（Ruzicka 相似度）作为评价指标。
    *   对比了 ResNet34 和 SegFormer 两种骨干网络。
    *   在滑板车实验中对比了“从头训练”与“预训练+微调”的效果。

#### 4. 资源与算力
*   **软件工具**：使用了 COLMAP 和 Ceres Solver（进行 GPU 加速的优化）。
*   **训练细节**：预训练阶段在 65 万帧数据上训练 25 个 epoch，学习率为 $3 \times 10^{-4}$；滑板车微调阶段训练 10 个 epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100 或 RTX 4090）及具体的训练总时长。但提到通过并行化处理 20 分钟的视频片段来解决 SfM 计算复杂度过高的问题。

#### 5. 实验数量与充分性
*   **实验规模**：使用了 65 万帧的大规模数据集，这在自监督轨迹预测领域具有较强的说服力。
*   **充分性**：
    *   **跨域验证**：在 NuScenes 上的测试证明了模型对地理位置、左右舵驾驶切换的泛化能力。
    *   **多假设验证**：专门针对多车道场景进行了定量评估，证明模型不会坍缩到单一路径。
    *   **平台迁移**：滑板车实验展示了模型从汽车到微型移动工具的适配性。
*   **客观性**：实验设计较为客观，通过非对称损失的设计逻辑解释了模型为何能产生超出演示轨迹的预测。

#### 6. 主要结论与发现
*   **自监督可行性**：大规模本车运动蒸馏（Ego-motion Distillation）可以有效地为分割模型提供结构化监督。
*   **多模态预测**：通过非对称损失训练的模型能够捕捉场景的底层结构（如交叉口、分叉路），预测出多条可行的行驶路径。
*   **泛化与迁移**：模型不仅能适应未见过的城市环境，还能通过少量数据微调快速迁移到不同类型的车辆平台（如滑板车）。

#### 7. 优点
*   **高扩展性**：无需人工标注，理论上可以利用无限的互联网视频数据。
*   **鲁棒性**：SfM 流程能够处理带有水印、镜头畸变和极端天气的不良视频。
*   **地图无关性**：不依赖高精地图或昂贵的传感器（如 LiDAR），仅靠单目视觉即可实现复杂的路径假设预测。

#### 8. 不足与局限
*   **几何约束限制**：单目 SfM 在车辆直线行驶（低视差）时容易失效，导致约 35% 的视频帧无法生成有效掩码。
*   **平面假设**：高度估计依赖于局部地面平坦的假设，在剧烈坡度变化（如山路或立交桥）时可能产生误差。
*   **动态障碍物**：目前的伪标签生成仅基于自车轨迹，未显式考虑其他动态障碍物的避让，虽然模型在实验中表现出一定的避障倾向，但缺乏显式的安全性约束。
*   **评估噪声**：使用 HD Map 作为 Ground Truth 进行评估时，由于地图拓扑不全或车道变更未记录，可能导致模型预测正确但评分较低的情况。

（完）
