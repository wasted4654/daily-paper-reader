---
title: "FAR-Drive: Frame-AutoRegressive Video Generation in Closed-Loop Autonomous Driving"
title_zh: FAR-Drive：闭环自动驾驶中的帧级自回归视频生成
authors: "Yaoru Li, Federico Landi, Marco Godi, Xin Jin, Ruiju Fu, Yufei Ma, Muyang Sun, Heyu Si, Qi Guo"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14938v1"
tags: ["query:genad"]
score: 8.0
evidence: 用于闭环自动驾驶模拟的生成式视频模型
tldr: 针对自动驾驶仿真中缺乏交互性和长时一致性的问题，本文提出FAR-Drive框架。该框架采用帧级自回归视频生成技术，结合多视图扩散Transformer和细粒度结构化控制，实现了几何一致的多摄像头生成。通过自适应参考时界调节和混合强制自回归训练，有效解决了长时一致性与自回归退化问题。实验证明，该方法在nuScenes数据集上达到SOTA水平，并实现了单GPU亚秒级推理延迟，为闭环自动驾驶仿真提供了高效可靠的方案。
selection_source: fresh_fetch
motivation: 现有的生成式视频模型多为开环运行，难以支持自动驾驶中智能体动作与环境演化之间的细粒度闭环交互。
method: 提出一种基于多视图扩散Transformer的帧级自回归框架，并结合两阶段训练策略及系统级推理优化。
result: 在nuScenes数据集上取得了领先的闭环仿真性能，并在单张GPU上实现了低于一秒的推理延迟。
conclusion: FAR-Drive成功解决了自动驾驶仿真中的长时一致性、自回归退化和推理延迟挑战，为构建可扩展的闭环模拟器奠定了基础。
---

## 摘要
尽管自动驾驶取得了快速进展，但由于缺乏可扩展且具有交互性的模拟环境，驾驶系统的可靠训练和评估仍受到根本性限制。近期的生成式视频模型实现了卓越的视觉保真度，但大多数模型在开环设置下运行，无法支持智能体动作与环境演化之间细粒度的帧级交互。构建基于学习的自动驾驶闭环模拟器面临三大挑战：保持长时程的时间和跨视图一致性、缓解迭代自调节下的自回归退化，以及满足低延迟推理约束。在这项工作中，我们提出了 FAR-Drive，这是一个用于自动驾驶的帧级自回归视频生成框架。我们引入了一种具有细粒度结构化控制的多视图扩散 Transformer，实现了几何一致的多摄像头生成。为了解决长时程一致性和迭代退化问题，我们设计了一种由自适应参考时界调节和混合强制自回归训练组成的阶段式训练策略，逐步提高了自调节下的模型一致性和鲁棒性。为了满足低延迟交互需求，我们进一步集成了系统级效率优化以加速推理。在 nuScenes 数据集上的实验表明，我们的方法在现有的闭环自动驾驶模拟方法中达到了最先进的性能，同时在单块 GPU 上保持了亚秒级的延迟。

## Abstract
Despite rapid progress in autonomous driving, reliable training and evaluation of driving systems remain fundamentally constrained by the lack of scalable and interactive simulation environments. Recent generative video models achieve remarkable visual fidelity, yet most operate in open-loop settings and fail to support fine-grained frame-level interaction between agent actions and environment evolution. Building a learning-based closed-loop simulator for autonomous driving poses three major challenges: maintaining long-horizon temporal and cross-view consistency, mitigating autoregressive degradation under iterative self-conditioning, and satisfying low-latency inference constraints. In this work, we propose FAR-Drive, a frame-level autoregressive video generation framework for autonomous driving. We introduce a multi-view diffusion transformer with fine-grained structured control, enabling geometrically consistent multi-camera generation. To address long-horizon consistency and iterative degradation, we design a two-stage training strategy consisting of adaptive reference horizon conditioning and blend-forcing autoregressive training, which progressively improves consistency and robustness under self-conditioning. To meet low-latency interaction requirements, we further integrate system-level efficiency optimizations for inference acceleration. Experiments on the nuScenes dataset demonstrate that our method achieves state-of-the-art performance among existing closed-loop autonomous driving simulation approaches, while maintaining sub-second latency on a single GPU.

---

## 论文详细总结（自动生成）

以下是对论文《FAR-Drive: Frame-AutoRegressive Video Generation in Closed-Loop Autonomous Driving》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自动驾驶系统的训练和评估高度依赖模拟环境。现有的生成式视频模型虽然视觉保真度高，但大多处于“开环”状态（即视频内容固定，不随驾驶动作改变）。
*   **核心问题**：构建“闭环”模拟器（智能体动作能实时改变环境演化）面临三大挑战：
    1.  **长时一致性**：在长时间序列中保持多摄像头视图的几何对齐和时间连贯。
    2.  **自回归退化**：迭代生成过程中，早期误差会随时间累积，导致画面崩溃（曝光偏差）。
    3.  **低延迟需求**：闭环交互要求模拟器能以极快的速度响应动作并生成下一帧。

### 2. 方法论：核心思想与关键技术
FAR-Drive 提出了一个帧级自回归（Frame-level AutoRegressive）框架，核心技术包括：
*   **多视图 MMDiT 架构**：基于 Stable Diffusion 3 的多模态扩散 Transformer（MMDiT），引入因果时间注意力和跨视图空间注意力，确保多视角视频在时间和空间上的统一。
*   **双路控制架构**：采用类似 ControlNet 的设计，一个主干网络负责生成，一个控制分支处理结构化信号（3D 框投影、BEV 语义图、路网布局等）。
*   **两阶段训练策略**：
    1.  **自适应参考时界调节 (ARHC)**：动态改变训练时的历史参考帧数量（0 到 L-1 帧），使模型适应从“图生视频”到“视频续写”的多种场景。
    2.  **混合强制自回归训练 (Blend-Forcing)**：在训练中将模型生成的潜在表示（Latent）与真实值（GT）按比例 $\alpha$ 混合。随着训练进行，逐渐增加生成内容的比例，从而缓解推理时的分布偏移。
*   **推理优化**：引入 Latent KV Cache（缓存历史帧特征）、条件缓存（跨扩散步复用控制信号）以及高压缩比 VAE（1x32x32），显著降低单帧生成延迟。

### 3. 实验设计
*   **数据集**：使用自动驾驶主流数据集 **nuScenes**（1000 个场景，包含 6 视角摄像头、3D 标注和 BEV 地图）。
*   **Benchmark 与对比方法**：
    *   对比了 MagicDrive (v1, v2, 3D)、DreamForge、Drive-WM、GenAD、Vista 等约 20 种最先进的驾驶视频生成方法。
*   **评价指标**：
    *   视觉质量：FID（图像）、FVD（视频）。
    *   结构一致性：mAP（目标检测准确性）、mIoU（地图布局一致性）。

### 4. 资源与算力
*   **模型规模**：总参数量约为 **3.7B**（其中生成主干 2.5B，控制分支 0.8B）。
*   **训练细节**：预训练 10k 步，因果微调 20k 步，混合强制训练 7k 步。Batch Size 为 64。
*   **推理算力**：文中明确提到了在 **NPU** 和 **NVIDIA H200 GPU** 上测试了延迟。在 H200 上，3 步采样下可达到约 **0.84s - 1.15s** 的单帧生成速度。

### 5. 实验数量与充分性
*   **实验规模**：进行了详尽的对比实验和消融实验。
*   **充分性**：
    *   **长时测试**：模型在仅用 8 帧序列训练的情况下，成功实现了长达 **229 帧**（约 23 秒）的稳定生成，证明了 Blend-Forcing 的有效性。
    *   **消融实验**：针对采样步数、KV Cache、CFG（无分类器引导）等关键变量进行了定量分析。
    *   **客观性**：使用了感知指标（FID/FVD）和下游任务指标（mAP/mIoU）双重验证，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **SOTA 性能**：FAR-Drive 在 nuScenes 上的结构化指标（mAP/mIoU）显著优于现有方法，视觉质量也处于领先地位。
*   **稳定性突破**：通过 Blend-Forcing 策略，有效解决了自回归生成中常见的“画面随时间模糊或扭曲”的问题。
*   **效率可行性**：证明了在不使用复杂蒸馏技术的情况下，通过系统级优化即可在单 GPU 上实现亚秒级闭环仿真。

### 7. 优点
*   **细粒度交互**：支持逐帧（Frame-level）的动作反馈，比传统的块级（Chunk-level）生成更符合真实驾驶反馈逻辑。
*   **鲁棒性强**：Blend-Forcing 策略为自回归扩散模型提供了一种简单且高效的训练范式。
*   **工程优化完备**：不仅关注模型架构，还通过 KV Cache 等手段解决了实际部署中的延迟痛点。

### 8. 不足与局限
*   **数据集局限**：实验主要集中在 nuScenes，对于更复杂、更多样化的长尾场景（如极端天气、罕见事故）的覆盖仍有待验证。
*   **实时性差距**：虽然达到了亚秒级延迟，但距离真正的实时仿真（如 10FPS 以上）仍有计算开销上的挑战。
*   **算力门槛**：3.7B 参数量的模型在训练和大规模部署时仍需要较高的计算资源支持。

（完）
