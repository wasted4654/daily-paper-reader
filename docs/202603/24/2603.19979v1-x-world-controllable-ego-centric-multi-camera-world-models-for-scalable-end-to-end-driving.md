---
title: "X-World: Controllable Ego-Centric Multi-Camera World Models for Scalable End-to-End Driving"
title_zh: X-World：用于可扩展端到端驾驶的可控自我中心多摄像头世界模型
authors: "Chaoda Zheng, Sean Li, Jinhao Deng, Zhennan Wang, Shijia Chen, Liqiang Xiao, Ziheng Chi, Hongbin Lin, Kangjie Chen, Boyang Wang, Yu Zhang, Xianming Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19979v1"
tags: ["query:genad"]
score: 9.0
evidence: 用于端到端驾驶的动作条件生成式世界模型
tldr: X-World是一个受动作驱动的多摄像头生成式世界模型，旨在解决自动驾驶端到端评估中实车测试成本高、场景覆盖有限且难以复现的问题。它能根据历史传感器数据和未来动作序列生成逼真的多视角视频，并支持对交通参与者、道路元素及天气环境的精细控制。该模型通过多视图潜空间生成器确保了跨视角几何一致性和长时序稳定性，为可扩展且可复现的自动驾驶评估提供了重要基础。
selection_source: fresh_fetch
motivation: 传统的实车路测成本高昂且场景覆盖受限，亟需一种能根据驾驶动作生成逼真、可控且长时稳定的未来观测模拟器。
method: 提出一种多视图潜空间视频生成器，通过融合动作序列、场景元素控制和文本提示，显式增强跨视角几何一致性与时序连贯性。
result: 实验证明X-World在多视角一致性、长时序动态稳定性以及对动作和场景控制的忠实遵循方面均达到了高质量生成水平。
conclusion: X-World为端到端自动驾驶提供了一个实用、高质量且可扩展的仿真评估平台，支持可复现的闭环测试。
---

## 摘要
在自动驾驶的端到端时代，视觉-语言-动作（VLA）策略直接将原始传感器流映射为驾驶动作，可扩展且可靠的评估变得日益关键。然而，目前的评估流程仍严重依赖现实世界的道路测试，这不仅成本高昂，且存在场景覆盖有限的偏差，且难以复现。这些挑战促使开发一种现实世界模拟器，能够在给定动作下生成逼真的未来观测结果，同时在长时程内保持可控和稳定。我们提出了 X-World，这是一种动作条件的多摄像头生成式世界模型，直接在视频空间中模拟未来观测。给定同步的多视图摄像头历史记录和未来动作序列，X-World 生成遵循指令动作的未来多摄像头视频流。为了确保场景推演的可复现性和可编辑性，X-World 进一步支持对动态交通参与者和静态道路元素的可选控制，并保留了用于外观级控制（如天气和时间）的文本提示接口。除了世界模拟，X-World 还能通过以外观提示为条件，在保留底层动作和场景动力学的同时实现视频风格迁移。X-World 的核心是一个多视图潜变量视频生成器，旨在显式地促进在多种控制信号下的跨视图几何一致性和时间连贯性。实验表明，X-World 实现了高质量的多视图视频生成，具有：(i) 摄像头间强大的视图一致性，(ii) 长时程推演中稳定的时间动力学，以及 (iii) 具有严格动作遵循和对可选场景控制忠实遵循的高可控性。这些特性使 X-World 成为可扩展和可复现评估的实用基础。

## Abstract
Scalable and reliable evaluation is increasingly critical in the end-to-end era of autonomous driving, where vision--language--action (VLA) policies directly map raw sensor streams to driving actions. Yet, current evaluation pipelines still rely heavily on real-world road testing, which is costly, biased toward limited scenario coverage, and difficult to reproduce. These challenges motivate a real-world simulator that can generate realistic future observations under proposed actions, while remaining controllable and stable over long horizons. We present X-World, an action-conditioned multi-camera generative world model that simulates future observations directly in video space. Given synchronized multi-view camera history and a future action sequence, X-World generates future multi-camera video streams that follow the commanded actions. To ensure reproducible and editable scene rollouts, X-World further supports optional controls over dynamic traffic agents and static road elements, and retains a text-prompt interface for appearance-level control (e.g., weather and time of day). Beyond world simulation, X-World also enables video style transfer by conditioning on appearance prompts while preserving the underlying action and scene dynamics. At the core of X-World is a multi-view latent video generator designed to explicitly encourage cross-view geometric consistency and temporal coherence under diverse control signals. Experiments show that X-World achieves high-quality multi-view video generation with (i) strong view consistency across cameras, (ii) stable temporal dynamics over long rollouts, and (iii) high controllability with strict action following and faithful adherence to optional scene controls. These properties make X-World a practical foundation for scalable and reproducible evaluation.

---

## 论文详细总结（自动生成）

### X-World: 用于可扩展端到端驾驶的可控自我中心多摄像头世界模型 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着自动驾驶进入端到端（End-to-End）时代，视觉-语言-动作（VLA）模型直接将传感器流映射为驾驶动作。然而，传统的评估手段面临瓶颈：
*   **实车测试成本高昂**：且难以覆盖罕见的危险场景（长尾场景）。
*   **不可复现性**：真实道路环境瞬息万变，难以对不同算法进行公平的“苹果对苹果”对比。
*   **闭环评估缺失**：端到端模型对微小的分布偏移敏感，离线评估（如感知指标）无法反映真实的驾驶表现。

**X-World 的核心目标**是构建一个**动作条件（Action-conditioned）的多摄像头生成式世界模型**，作为“真实世界模拟器”，通过生成符合物理规律和驾驶指令的未来视频流，支持可扩展的闭环评估和强化学习训练。

#### 2. 方法论：核心思想与关键技术
X-World 基于潜变量视频生成范式（Latent Video Generation），其核心架构和流程如下：

*   **核心架构**：
    *   **基础模型**：基于 WAN 2.2 (5B参数版本)，采用 3D 因果变分自编码器（VAE）进行 16× 空间压缩和 4× 时间压缩。
    *   **多视图 DiT (Diffusion Transformer)**：引入了**视图-时序自注意力机制（View-temporal self-attention）**，在不同摄像头视角和时间步之间交替执行注意力计算，确保跨视角几何一致性和时序连贯性。
*   **多模态控制信号注入**：
    *   **动作控制**：输入未来运动学序列（速度、曲率、俯仰、翻滚），通过 Fourier 特征编码后经由 `adaLN-Zero` 注入。
    *   **场景控制**：通过**解耦交叉注意力（Decoupled cross-attention）**分别注入动态交通参与者（类别+坐标）和静态道路元素（车道线、边界），减少不同控制信号间的干扰。
    *   **外观控制**：保留文本提示接口，用于控制天气、时间、地域风格。
*   **分阶段训练流程**：
    *   **阶段 I（双向训练）**：使用 Rectified Flow 目标函数，在 81 帧的短片段上训练，使模型获得高质量的生成能力和精确的可控性。
    *   **阶段 II（因果少步训练）**：将模型转换为**块状因果（Chunk-wise causal）架构**，采用自强制（Self-forcing）和分布匹配蒸馏（DMD）技术，实现 4 步快速去噪生成，支持低延迟的流式交互。
*   **长时序推理**：引入**滚动键值缓存（Rolling KV Cache）**，通过 FIFO 机制维持固定大小的上下文窗口，支持长达 24 秒以上的稳定视频推演。

#### 3. 实验设计
*   **数据集**：使用小鹏汽车（XPeng）大规模高精度真实驾驶数据集。
    *   **传感器**：7 路同步摄像头（覆盖 360 度）。
    *   **标注**：利用 VLM 自动生成语义描述（天气、路况、密度等），并结合高精度感知网络提取动态轨迹和静态地图元素。
*   **验证场景**：
    *   **动作遵循实验**：验证模型在相同初始场景下，执行不同动作（如左转 vs 右转）时的生成差异。
    *   **元素控制实验**：通过修改边界框和车道线坐标，验证模型对场景编辑的忠实度。
    *   **闭环评估案例**：展示了在反事实场景（如绕过违停车辆）和安全关键场景（如鬼探头）中的策略表现。
*   **对比基准**：主要作为技术报告展示其在多视角一致性、长时稳定性方面的定性优势，未提供与开源 SOTA 模型的详尽定量对比表。

#### 4. 资源与算力
*   **算力说明**：文中明确提到“投入了大量的计算资源”用于数据标注和模型迭代，但**未披露具体的 GPU 型号（如 H100/A100）、集群规模及具体的训练时长**。
*   **模型规模**：基于 5B 参数量的 WAN 2.2 模型进行微调和架构改造。

#### 5. 实验数量与充分性
*   **实验维度**：涵盖了动作控制、动态/静态元素控制、长时序生成（24s）、多视角一致性、外观风格迁移等多个维度。
*   **充分性评价**：
    *   **定性实验充分**：提供了大量视觉案例，证明了模型在复杂城市环境下的生成质量。
    *   **闭环应用深入**：通过 Scenario 1 & 2 展示了该模型在实际 VLA 2.0 策略评估中的应用价值。
    *   **定量缺失**：缺乏标准数据集（如 nuScenes）上的 FID、FVD 或动作遵循误差等定量指标对比，这在一定程度上削弱了其作为学术论文的严谨性，但符合工业界技术报告的特征。

#### 6. 主要结论与发现
*   **生成质量**：X-World 能够生成具有强跨视角一致性和长时序稳定性的 360 度环视视频。
*   **高可控性**：模型能够严格遵循驾驶动作指令，并支持对交通流和道路结构的精细编辑。
*   **实用性**：通过阶段 II 的优化，模型支持流式、交互式推演，可直接用于在线强化学习（Online RL）和闭环仿真。
*   **数据合成**：该模型可作为“数据工厂”，通过风格迁移（如将国内数据转为海外风格）加速全球化部署。

#### 7. 优点（亮点）
*   **架构创新**：块状因果架构结合 Rolling KV Cache，巧妙平衡了生成质量与实时交互需求。
*   **控制精细**：不仅有动作控制，还引入了显式的动态/静态元素控制，优于仅靠文本或高层指令的世界模型。
*   **多视角一致性**：通过专门的 View-temporal 模块解决了环视摄像头之间物体错位或消失的顽疾。
*   **闭环闭环验证**：直接展示了在端到端驾驶策略评估中的应用，证明了其工程落地价值。

#### 8. 不足与局限
*   **定量对比不足**：未与 DriveWM、GAIA-1 或 Waymo World Model 等同类工作进行标准 Benchmark 的数据对比。
*   **依赖感知质量**：训练过程高度依赖高精度感知模型提供的轨迹和地图标注，若标注存在偏差，会影响生成的物理真实性。
*   **算力透明度**：未公开训练细节，外界难以评估其复现成本。
*   **累积误差风险**：尽管采用了自强制训练，但在极长时序（如数分钟）的推演中，潜空间的漂移风险依然存在。

（完）
